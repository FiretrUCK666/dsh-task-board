window.__ModuleLoader__.load({
	id: "@firetruck666/dsh-task-board",
	factory: (require) => {
		var module = { exports: {} };
		var exports = module.exports;
		Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
		let react_dom_client = require("react-dom/client");
		let react = require("react");
		let react_dom = require("react-dom");
		let react_jsx_runtime = require("react/jsx-runtime");
		//#region src/client/platform.ts
		/**
		* Create a snapshot store. Synchronous flush (controlled inputs need
		* same-tick echo); persistence is intentionally omitted — this plugin's
		* stores are projections, not durable state.
		*
		* @param init - initial state.
		* @returns the store.
		*/
		function createSnapshotStore(init) {
			let state = init;
			const listeners = /* @__PURE__ */ new Set();
			return {
				getSnapshot: () => state,
				subscribe: (fn) => {
					listeners.add(fn);
					return () => {
						listeners.delete(fn);
					};
				},
				update: (mutator) => {
					const draft = Array.isArray(state) ? [...state] : typeof state === "object" && state !== null ? { ...state } : state;
					mutator(draft);
					state = draft;
					for (const listener of [...listeners]) listener();
				},
				set: (next) => {
					if (next === state) return;
					state = next;
					for (const listener of [...listeners]) listener();
				}
			};
		}
		/** The oldest event seq covered by one record window (undefined when empty). */
		function floorSeqOf(records) {
			let floor;
			for (const record of records) {
				const event = record.event;
				if (typeof event !== "object" || event === null) continue;
				if (typeof event.seq !== "number" || !Number.isFinite(event.seq)) continue;
				if (floor === void 0 || event.seq < floor) floor = event.seq;
			}
			return floor;
		}
		/**
		* Build the domain API face from the alpha.3 client context. Every method
		* maps to a `ctx.remote` namespace (Typert endpoints follow the
		* `<namespace>/<method>` convention, e.g. `session/prompt`) and returns the
		* `{ result }` envelope the rc.7 surface used, so consuming code is
		* unchanged. The `create` method routes through the sessions service (the
		* wire's create carries a stricter request), and `history` reads the cold
		* inspect endpoint (header + event prefix) and takes its tail window.
		*
		* @param ctx - client root context.
		* @returns the API face.
		*/
		function buildApi(ctx) {
			const remoteSession = ctx.get("remote.session");
			const remoteSkills = ctx.get("remote.skills");
			const remoteAgentPresets = ctx.get("remote.agentPresets");
			const remoteFileUploads = ctx.get("remote.fileUploads");
			const sessionsService = ctx.sessions;
			const warnedMissing = /* @__PURE__ */ new Set();
			const warnedFailure = /* @__PURE__ */ new Set();
			const methodOf = (ns, method, value) => {
				if (value === void 0) {
					const label = `${ns}.${method}`;
					if (!warnedMissing.has(label)) {
						warnedMissing.add(label);
						console.error(`[dsh-task-board] remote method missing on the live host: ${label} — a host upgrade may have renamed it`);
					}
				}
				return value;
			};
			const unavailable = async (label) => ({ result: {
				ok: false,
				error: {
					code: "remote/unavailable",
					message: `the live host does not serve ${label}`
				}
			} });
			/** Log one named endpoint failure so a board surface's 「暂不可用」is diagnosable from the console. */
			const warnFailure = (label, error) => {
				if (warnedFailure.has(label)) return;
				warnedFailure.add(label);
				console.warn(`[dsh-task-board] ${label} failed -> ${error.code}: ${error.message}`);
			};
			/** Map a remote call result into the domain face's envelope. */
			const asResult = async (label, call) => {
				const result = await call;
				if (result.ok) return { result: {
					ok: true,
					value: result.value
				} };
				warnFailure(label, result.error);
				return { result: {
					ok: false,
					error: result.error
				} };
			};
			return {
				sessions: {
					prompt: async (request) => {
						const call = methodOf("session", "prompt", remoteSession?.prompt);
						if (call === void 0) return unavailable("session.prompt");
						const result = await asResult("session.prompt", call({
							requestId: `tb-${Date.now().toString(36)}-${Math.random().toString(36).slice(2, 10)}`,
							sessionId: request.sessionId,
							mode: request.mode,
							content: request.content
						}));
						return result.result.ok ? { result: {
							ok: true,
							value: { accepted: true }
						} } : { result: result.result };
					},
					selectModel: (request) => {
						const call = methodOf("session", "selectModel", remoteSession?.selectModel);
						if (call === void 0) return unavailable("session.selectModel");
						return asResult("session.selectModel", call({
							sessionId: request.sessionId,
							provider: request.provider,
							model: request.model,
							...request.reasoningEffort !== void 0 ? { reasoningEffort: request.reasoningEffort } : {}
						}));
					},
					create: async (request) => {
						return { result: {
							ok: true,
							value: { sessionId: await sessionsService.create({ workspaceId: request.workspaceId }) }
						} };
					},
					history: async (request) => {
						const follow = methodOf("session", "follow", remoteSession?.follow);
						if (follow === void 0) return unavailable("session.follow");
						try {
							const controller = new AbortController();
							const stream = follow({
								address: {
									kind: "session",
									sessionId: request.sessionId
								},
								...request.maxMessages !== void 0 ? { maxMessages: request.maxMessages } : {}
							}, controller.signal);
							let events = [];
							let hasMore = false;
							let floorSeq;
							let throughSeq;
							let projections;
							try {
								for await (const frame of stream) {
									if (frame.type !== "snapshot") continue;
									events = (frame.records ?? []).map((record) => ({ event: record.event }));
									hasMore = frame.hasMore === true;
									floorSeq = floorSeqOf(events);
									throughSeq = frame.cursor;
									projections = frame.projections;
									break;
								}
							} finally {
								controller.abort();
							}
							return { result: {
								ok: true,
								value: {
									events,
									hasMore,
									...floorSeq !== void 0 ? { floorSeq } : {},
									...throughSeq !== void 0 ? { throughSeq } : {},
									...projections !== void 0 ? { projections } : {}
								}
							} };
						} catch (error) {
							const failure = error;
							const failureBody = {
								code: typeof failure.code === "string" ? failure.code : "follow/stream-failed",
								message: typeof failure.message === "string" ? failure.message : String(error)
							};
							warnFailure("session.follow", failureBody);
							return { result: {
								ok: false,
								error: failureBody
							} };
						}
					},
					page: async (request) => {
						const call = methodOf("session", "page", remoteSession?.page);
						if (call === void 0) return unavailable("session.page");
						const result = await asResult("session.page", call({
							address: {
								kind: "session",
								sessionId: request.sessionId
							},
							...request.throughSeq !== void 0 ? { throughSeq: request.throughSeq } : {},
							beforeSeq: request.beforeSeq,
							maxMessages: request.maxMessages
						}));
						if (!result.result.ok) return { result: result.result };
						const records = result.result.value.records ?? [];
						return { result: {
							ok: true,
							value: {
								events: records.map((record) => ({ event: record.event })),
								hasMore: result.result.value.hasMore === true,
								...(() => {
									const floor = floorSeqOf(records.map((record) => ({ event: record.event })));
									return floor !== void 0 ? { floorSeq: floor } : {};
								})()
							}
						} };
					},
					rename: (request) => {
						const call = methodOf("session", "rename", remoteSession?.rename);
						if (call === void 0) return unavailable("session.rename");
						return asResult("session.rename", call({
							sessionId: request.sessionId,
							title: request.title
						}));
					},
					attachment: (request) => {
						const call = methodOf("session", "attachment", remoteSession?.attachment);
						if (call === void 0) return unavailable("session.attachment");
						return asResult("session.attachment", call({
							sessionId: request.sessionId,
							attachmentId: request.attachmentId
						}));
					},
					models: async () => {
						const call = methodOf("session", "modelCatalog", remoteSession?.modelCatalog);
						if (call === void 0) return unavailable("session.modelCatalog");
						const result = await call();
						if (!result.ok) {
							warnFailure("session.modelCatalog", result.error);
							return { result: {
								ok: false,
								error: result.error
							} };
						}
						const catalog = result.value;
						return { result: {
							ok: true,
							value: {
								current: {
									provider: catalog.default?.provider ?? "",
									model: catalog.default?.model ?? "",
									...catalog.default?.reasoningEffort !== void 0 ? { reasoningEffort: catalog.default.reasoningEffort } : {}
								},
								groups: catalog.groups ?? []
							}
						} };
					}
				},
				skills: { list: (request) => {
					const call = methodOf("skills", "list", remoteSkills?.list);
					if (call === void 0) return unavailable("skills.list");
					return asResult("skills.list", call({ sessionId: request.sessionId }));
				} },
				agentPresets: {
					list: () => {
						const call = methodOf("agentPresets", "list", remoteAgentPresets?.list);
						if (call === void 0) return unavailable("agentPresets.list");
						return asResult("agentPresets.list", call());
					},
					select: (request) => {
						const call = methodOf("agentPresets", "select", remoteAgentPresets?.select);
						if (call === void 0) return unavailable("agentPresets.select");
						return asResult("agentPresets.select", call(request.sessionId, request.agentPreset));
					}
				},
				fileUploads: { upload: async (request) => {
					const call = methodOf("fileUploads", "upload", remoteFileUploads?.upload);
					if (call === void 0) return unavailable("fileUploads.upload");
					const result = await asResult("fileUploads.upload", call(request.sessionId, {
						data: request.data,
						...request.name !== void 0 ? { name: request.name } : {}
					}));
					if (!result.result.ok) return { result: result.result };
					const receiptId = result.result.value.receiptId;
					if (typeof receiptId !== "string" || receiptId === "") return { result: {
						ok: false,
						error: {
							code: "fileUploads/bad-receipt",
							message: "the host returned no file receipt"
						}
					} };
					return { result: {
						ok: true,
						value: { receiptId }
					} };
				} },
				events: { mux: () => ({ async *[Symbol.asyncIterator]() {
					console.warn("[dsh-task-board] live question stream unavailable on dsh 0.1.5: question live-stream disabled");
					await new Promise(() => {});
				} }) },
				respond: async () => {
					console.warn("[dsh-task-board] question answer path unavailable on dsh 0.1.5: answers go through the native session");
					return { accepted: false };
				}
			};
		}
		/**
		* Adapt one alpha.3 bound session object to the core `SessionDriver` face.
		*
		* The alpha.3 session snapshot dropped the rc.7 `turnEnds` map (turn
		* bookkeeping moved into submission/queue mirrors), so the adapter derives
		* the ONE provably-ours turn boundary: the blank-session FIRST turn — the
		* `awaitingFirstTurn` edge stays pending until that first turn opens, and a
		* running→idle transition afterwards proves it closed. Later turns on reused
		* sessions settle through the host session list plus history tail (the
		* settlement authority), which keeps the rc.7 false-positive guard (a
		* previous turn ending while our prompt still queued) intact: the adapter
		* never fabricates a count it cannot prove.
		*
		* The internal subscription is deliberately permanent for the session's
		* lifetime — the derivation must never miss an edge — and is released by the
		* returned `dispose` (the caller registers it on its own fiber).
		*
		* @param session - the bound session object.
		* @returns the driver adapter plus its disposal.
		*/
		function sessionDriverOf(session) {
			const turnEnds = /* @__PURE__ */ new Map();
			let awaitingFirst = false;
			let lastRunning;
			let latest = session.getSnapshot();
			const track = () => {
				latest = session.getSnapshot();
				if (latest.awaitingFirstTurn === true) awaitingFirst = true;
				if (awaitingFirst && lastRunning === true && latest.running === false) turnEnds.set(turnEnds.size + 1, Date.now());
				lastRunning = latest.running;
			};
			const dispose = session.subscribe(track);
			track();
			return {
				driver: {
					rename: (title) => session.rename(title),
					prompt: (content, mode) => session.prompt(content, mode),
					command: (line) => session.command(line),
					getSnapshot: () => ({
						running: latest.running,
						lastAgentError: latest.lastAgentError,
						turnEnds
					}),
					subscribe: (fn) => session.subscribe(fn)
				},
				dispose
			};
		}
		//#endregion
		//#region src/core/question-rpc.ts
		/** Whether a raw item is a plan review whose body lives in `detail` alone:
		*  intent-tagged plan-review + non-empty detail (mirrors the official
		*  planReviewOf narrowing — question text is NOT required there). Shared by
		*  the legacy wire normalizer below and the official-snapshot mirror, so a
		*  detail-carried plan is never silently dropped on either path. */
		function isDetailOnlyPlanItem(value) {
			if (typeof value !== "object" || value === null) return false;
			const source = value;
			const intent = source.intent;
			if (typeof intent !== "object" || intent === null) return false;
			if (intent.kind !== "plan-review") return false;
			return typeof source.detail === "string" && source.detail.trim() !== "";
		}
		/** Normalize one wire question item; drop anything malformed. */
		function normalizeWireQuestion(value) {
			if (typeof value !== "object" || value === null) return void 0;
			const source = value;
			const questionText = typeof source.question === "string" ? source.question : "";
			if (questionText === "" && !isDetailOnlyPlanItem(value)) return void 0;
			const detail = typeof source.detail === "string" ? source.detail : void 0;
			const item = {
				id: typeof source.id === "string" && source.id !== "" ? source.id : questionText !== "" ? questionText : detail !== void 0 ? detail.slice(0, 40) : "plan",
				question: questionText
			};
			if (detail !== void 0) item.detail = detail;
			if (typeof source.header === "string") item.header = source.header;
			if (Array.isArray(source.options)) {
				const options = source.options.map((option) => {
					if (typeof option !== "object" || option === null) return void 0;
					const entry = option;
					if (typeof entry.label !== "string") return void 0;
					const result = { label: entry.label };
					if (typeof entry.description === "string") result.description = entry.description;
					return result;
				}).filter((option) => option !== void 0);
				if (options.length > 0) item.options = options;
			}
			if (source.multiSelect === true || source.multiSelect === "true") item.multiSelect = true;
			const intent = source.intent;
			if (typeof intent === "object" && intent !== null) {
				const intentSource = intent;
				if (intentSource.kind === "plan-review" && typeof intentSource.approve === "string") item.intent = {
					kind: "plan-review",
					approve: intentSource.approve
				};
			}
			return item;
		}
		/** Normalize a whole wire question list; undefined when nothing is usable. */
		function wireQuestionsOf(value) {
			if (!Array.isArray(value)) return void 0;
			const questions = value.map(normalizeWireQuestion).filter((question) => question !== void 0);
			return questions.length > 0 ? questions : void 0;
		}
		/** Immutable application of one legacy frame onto the pending map (same map on no-op). */
		function reduceQuestionFrames(pending, frame) {
			if (frame.type === "question/resolved") {
				if (!pending.has(frame.questionRpcId)) return pending;
				const next = new Map(pending);
				next.delete(frame.questionRpcId);
				return next;
			}
			const questions = wireQuestionsOf(frame.questions);
			if (questions === void 0) return pending;
			const existing = pending.get(frame.rpcId);
			if (existing !== void 0 && existing.sessionId === frame.sessionId && existing.questions.length === questions.length && existing.questions.every((item, index) => questionItemKey(item) === questionItemKey(questions[index]))) return pending;
			const next = new Map(pending);
			next.set(frame.rpcId, {
				rpcId: frame.rpcId,
				sessionId: frame.sessionId,
				questions,
				isPlanReview: questions.some((item) => item.intent?.kind === "plan-review")
			});
			return next;
		}
		/** A stable content key of one question item (dedupe/idempotence comparison). */
		function questionItemKey(item) {
			return `${item.id}|${item.question}|${item.detail ?? ""}|${item.header ?? ""}|${item.multiSelect === true ? "m" : "s"}|${item.intent?.approve ?? ""}|${(item.options ?? []).map((option) => option.label).join(",")}`;
		}
		/** The pending wire question for one session (newest wins), when any. */
		function pendingQuestionOf(pending, sessionId) {
			if (sessionId === void 0) return void 0;
			let latest;
			for (const question of pending.values()) if (question.sessionId === sessionId) latest = question;
			return latest;
		}
		/** The plan-review question of a batch, when the batch IS a plan review. */
		function planQuestionOf(question) {
			return question.questions.find((item) => item.intent?.kind === "plan-review");
		}
		/** The approve label of a plan-review question (the intent names it). */
		function approveLabelOf(item) {
			if (item?.intent?.kind === "plan-review") return item.intent.approve;
			return item?.options?.[0]?.label;
		}
		/** The decline label of a plan-review question (first non-approve option). */
		function declineLabelOf(item) {
			const approve = item?.intent?.approve;
			if (approve === void 0) return void 0;
			return item?.options?.find((option) => option.label !== approve)?.label;
		}
		/**
		* The answer batch for a plan-review decision.
		* - approve: select the intent's approve label;
		* - decline without amendments: select the first non-approve option;
		* - decline with amendments: revise-with-feedback — a custom-only answer
		*   (host validation forbids a single-select option alongside custom text).
		*/
		function planDecisionAnswers(item, decision, amend) {
			const trimmed = amend.trim();
			if (decision === "decline" && trimmed !== "") return [{
				id: item.id,
				selected: [],
				custom: trimmed
			}];
			if (decision === "approve") {
				const label = approveLabelOf(item);
				return label !== void 0 ? [{
					id: item.id,
					selected: [label]
				}] : [{
					id: item.id,
					selected: []
				}];
			}
			const label = declineLabelOf(item);
			return label !== void 0 ? [{
				id: item.id,
				selected: [label]
			}] : [{
				id: item.id,
				selected: []
			}];
		}
		/**
		* The whole answer batch from per-question drafts: one entry per question,
		* in batch order, ids matching the requested frame (a skipped question is an
		* empty selection; a missing answer is a server-side rejection, never sent
		* silently). Custom text rides alongside selections only for multi-select.
		*/
		function answerBatchOf(question, drafts) {
			return question.questions.map((item, index) => {
				const draft = drafts[index] ?? { selected: [] };
				const custom = draft.custom?.trim() ?? "";
				return {
					id: item.id,
					selected: [...draft.selected],
					...custom !== "" && (item.multiSelect === true || draft.selected.length === 0) ? { custom } : {}
				};
			});
		}
		//#endregion
		//#region src/client/board/question-tracker.ts
		/** Open lazily on first reader; frames flow only while someone consumes. */
		var QuestionTracker = class {
			api;
			pending = /* @__PURE__ */ new Map();
			listeners = /* @__PURE__ */ new Set();
			/** Raw session-event fan-out (the legacy stream's `session/event` frames):
			*  the board's legacy native-turn channel (see controller.recordNativeTurn). */
			sessionListeners = /* @__PURE__ */ new Set();
			controller;
			reconnectTimer;
			constructor(api) {
				this.api = api;
			}
			/**
			* Observe raw native session events as they stream in (every session, live
			* — the stream replays only pending approval/question frames, so listeners
			* never see history re-fired). @returns the disposer.
			*/
			addSessionListener(listener) {
				this.sessionListeners.add(listener);
				this.start();
				return () => {
					this.sessionListeners.delete(listener);
				};
			}
			/** Start the live stream once (idempotent); aborts on teardown. */
			start() {
				if (this.controller !== void 0 || this.reconnectTimer !== void 0) return;
				const controller = new AbortController();
				this.controller = controller;
				this.pump(controller);
			}
			/** Close the stream and clear all listeners (plugin teardown). */
			dispose() {
				this.controller?.abort();
				this.controller = void 0;
				if (this.reconnectTimer !== void 0) clearTimeout(this.reconnectTimer);
				this.reconnectTimer = void 0;
				this.listeners.clear();
				this.sessionListeners.clear();
				this.pending = /* @__PURE__ */ new Map();
			}
			async pump(controller) {
				try {
					for await (const envelope of this.api.events.mux({}, controller.signal)) {
						const frame = envelope.payload;
						if (frame.type === "session/event") for (const listener of [...this.sessionListeners]) try {
							listener(frame.sessionId, frame.event);
						} catch {}
						else if (frame.type === "question/requested") this.apply({
							type: "question/requested",
							rpcId: envelope.rpcId,
							sessionId: frame.sessionId,
							questions: frame.questions
						});
						else if (frame.type === "question/resolved") this.apply({
							type: "question/resolved",
							questionRpcId: frame.questionRpcId
						});
					}
				} catch {}
				if (this.controller !== controller) return;
				this.controller = void 0;
				this.reconnectTimer = setTimeout(() => {
					this.reconnectTimer = void 0;
					this.start();
				}, 1e3);
			}
			apply(frame) {
				const next = reduceQuestionFrames(this.pending, frame);
				if (next === this.pending) return;
				this.pending = next;
				for (const listener of [...this.listeners]) listener();
			}
			pendingOf(sessionId) {
				this.start();
				return pendingQuestionOf(this.pending, sessionId);
			}
			subscribe(listener) {
				this.start();
				this.listeners.add(listener);
				return () => {
					this.listeners.delete(listener);
				};
			}
			async answer(rpcId, sessionId, answers) {
				this.start();
				return this.respond({
					type: "client-response",
					rpcId,
					result: {
						ok: true,
						value: {
							sessionId,
							answer: { answers: [...answers] }
						}
					}
				});
			}
			async cancel(rpcId) {
				this.start();
				return this.respond({
					type: "client-response",
					rpcId,
					result: {
						ok: false,
						error: {
							code: "cancelled",
							message: "the user closed this question request",
							details: {}
						}
					}
				});
			}
			/** The one wire boundary: POST /api/respond with the client-response form. */
			async respond(message) {
				try {
					return (await this.api.respond(message)).accepted;
				} catch {
					return false;
				}
			}
		};
		//#endregion
		//#region src/core/question-mirror.ts
		/**
		* Official pending-interaction mirror — the 0.1.5 answer-side read model.
		*
		* On dsh 0.1.5 the waterfall (`user-questions/request`) is a CLAIM chain, not
		* a broadcast: the board must never register its own listener (it would race
		* the native composer). The official read-only projection is the uiSession
		* `pendingInteractions` snapshot (`Map<sessionId, interaction>`), where each
		* interaction carries its session, kind and full question batch — AND, on the
		* shipped host, the carrier's own `answer`/`cancel` (the native
		* `PendingQuestion`). The board subscribes to that snapshot for DISPLAY and,
		* when the carrier exposes those actions, settles the request through the
		* very object it received: same claim, no second answerer, so the two
		* surfaces can never disagree about who answered. A carrier without them is
		* display-only and the card degrades to navigate-to-answer.
		*
		* Framework-free and DOM-free, so the rules unit-test in isolation (the live
		* subscription and the action calls live in the client adapter).
		*/
		/** Fold one session's wire content + waiting signal into its card (pure). */
		function awaitingOf(question, waitingKind) {
			if (question !== void 0) return {
				type: "content",
				question
			};
			if (waitingKind === "plan-review" || waitingKind === "question") return {
				type: "shell",
				waitingKind
			};
		}
		/**
		* Normalize one mirror question item; drop anything malformed. Same grammar
		* as the historical wire model (question-rpc.ts): the question text is
		* mandatory — EXCEPT a detail-carried plan review (see isDetailOnlyPlanItem),
		* whose body lives in `detail` by official contract.
		*/
		function normalizeMirrorQuestion(value) {
			if (typeof value !== "object" || value === null) return void 0;
			const source = value;
			const questionText = typeof source.question === "string" ? source.question : "";
			if (questionText === "" && !isDetailOnlyPlanItem(value)) return void 0;
			const detail = typeof source.detail === "string" ? source.detail : void 0;
			const item = {
				id: typeof source.id === "string" && source.id !== "" ? source.id : questionText !== "" ? questionText : detail !== void 0 ? detail.slice(0, 40) : "plan",
				question: questionText
			};
			if (detail !== void 0) item.detail = detail;
			if (typeof source.header === "string") item.header = source.header;
			if (Array.isArray(source.options)) {
				const options = source.options.map((option) => {
					if (typeof option !== "object" || option === null) return void 0;
					const entry = option;
					if (typeof entry.label !== "string") return void 0;
					const result = { label: entry.label };
					if (typeof entry.description === "string") result.description = entry.description;
					return result;
				}).filter((option) => option !== void 0);
				if (options.length > 0) item.options = options;
			}
			if (source.multiSelect === true || source.multiSelect === "true") item.multiSelect = true;
			return item;
		}
		/** Normalize a whole mirror question list; undefined when nothing is usable. */
		function mirrorQuestionsOf(value) {
			if (!Array.isArray(value)) return void 0;
			const questions = value.map(normalizeMirrorQuestion).filter((question) => question !== void 0);
			return questions.length > 0 ? questions : void 0;
		}
		//#endregion
		//#region src/client/board/pending-mirror.ts
		/** Whether a snapshot entry can settle its own pending request. */
		function canAnswer(interaction) {
			if (typeof interaction !== "object" || interaction === null) return false;
			const carrier = interaction;
			return typeof carrier.answer === "function" && typeof carrier.cancel === "function";
		}
		/** The one reader of the official carrier shape (structural, never instanceof). */
		function wireQuestionOf(sessionId, mapKey, interaction) {
			if (typeof interaction !== "object" || interaction === null) return void 0;
			const carrier = interaction;
			if (carrier.kind !== "question" && carrier.kind !== "plan-review") return void 0;
			if (carrier.sessionId !== sessionId) return void 0;
			const questions = mirrorQuestionsOf(carrier.questions);
			if (questions === void 0) return void 0;
			const rawKey = carrier.key;
			return {
				rpcId: typeof rawKey === "string" && rawKey !== "" ? rawKey : mapKey,
				sessionId,
				questions,
				isPlanReview: carrier.kind === "plan-review"
			};
		}
		/** In-place answering over the official pending snapshot (see module doc). */
		var PendingMirror = class {
			uiSession;
			constructor(uiSession) {
				this.uiSession = uiSession;
			}
			/**
			* The current projection, DERIVED on every read from the official snapshot
			* — there is no cached copy that a missed notification could leave stale,
			* so "what the card shows" and "what an answer settles" are always the same
			* generation of the snapshot.
			*/
			get entries() {
				return reduceSnapshot(this.uiSession?.pendingInteractions.getSnapshot());
			}
			pendingOf(sessionId) {
				if (sessionId === void 0) return void 0;
				return this.entries.get(sessionId)?.wire;
			}
			/**
			* Whether ANY live interaction can be settled from here. Read fresh by the
			* card on every render: a data-only snapshot entry (no `answer`/`cancel`)
			* keeps the read-only navigate shell, so "can answer" is never claimed for
			* a carrier that cannot.
			*/
			get answerInPlace() {
				for (const entry of this.entries.values()) if (canAnswer(entry.carrier)) return true;
				return false;
			}
			subscribe(listener) {
				const source = this.uiSession?.pendingInteractions;
				if (source === void 0) return () => {};
				return source.subscribe(listener);
			}
			/**
			* Settle the session's live request with the whole answer batch. The
			* identity guard is the contract: the answered carrier must be the SAME
			* object (key and identity) the official snapshot publishes RIGHT NOW — a
			* request answered or cancelled elsewhere, or replaced by a newer one,
			* rejects instead of settling something stale. False = not accepted; the
			* card keeps itself open and reports it.
			*/
			async answer(rpcId, sessionId, answers) {
				const carrier = liveCarrierOf(this.entries, rpcId, sessionId);
				if (carrier === void 0) return false;
				try {
					await carrier.answer({ answers: [...answers] });
					return true;
				} catch (error) {
					console.error("[dsh-task-board] answering the pending question failed", error);
					return false;
				}
			}
			/** Reject the whole ask (the tool call settles as cancelled, like the native ×). */
			async cancel(rpcId) {
				const entries = this.entries;
				for (const [sessionId, entry] of entries) {
					if (entry.wire.rpcId !== rpcId) continue;
					const carrier = liveCarrierOf(entries, rpcId, sessionId);
					if (carrier === void 0) return false;
					try {
						await carrier.cancel();
						return true;
					} catch (error) {
						console.error("[dsh-task-board] cancelling the pending question failed", error);
						return false;
					}
				}
				return false;
			}
		};
		/**
		* The live carrier behind one displayed card, or undefined when it went
		* stale. The displayed rpcId is the INTERACTION's own key (the snapshot is
		* keyed by it, never by session), so this compares both the projection's
		* newest-wins entry and the carrier's identity: a newer interaction replaced
		* the card the user is looking at, and the answer is refused rather than
		* delivered to whatever came after it.
		*/
		function liveCarrierOf(entries, rpcId, sessionId) {
			if (sessionId === void 0) return void 0;
			const entry = entries.get(sessionId);
			if (entry === void 0) return void 0;
			if (entry.wire.rpcId !== rpcId) return void 0;
			return canAnswer(entry.carrier) ? entry.carrier : void 0;
		}
		/** Project the whole official snapshot into per-session newest-wins entries. */
		function reduceSnapshot(snapshot) {
			const next = /* @__PURE__ */ new Map();
			if (snapshot === void 0) return next;
			for (const [mapKey, interaction] of snapshot) {
				const sessionId = interaction?.sessionId;
				if (typeof sessionId !== "string" || sessionId === "") continue;
				const wire = wireQuestionOf(sessionId, mapKey, interaction);
				if (wire === void 0) continue;
				next.set(sessionId, {
					wire,
					carrier: interaction
				});
			}
			return next;
		}
		//#endregion
		//#region src/core/schedule.ts
		/** Inclusive ranges per field, in cron order. */
		const FIELD_RANGES = [
			[0, 59],
			[0, 23],
			[1, 31],
			[1, 12],
			[0, 7]
		];
		/**
		* Parse a 5-field cron expression.
		* @returns the match sets, or null when the expression is invalid.
		*/
		function parseCron(expr) {
			const fields = expr.trim().split(/\s+/);
			if (fields.length !== 5) return null;
			const sets = [];
			for (let index = 0; index < 5; index++) {
				const [min, max] = FIELD_RANGES[index];
				const set = /* @__PURE__ */ new Set();
				if (!parseField(fields[index], min, max, set)) return null;
				sets.push(set);
			}
			const weekdays = /* @__PURE__ */ new Set();
			for (const day of sets[4]) weekdays.add(day === 7 ? 0 : day);
			return {
				minutes: sets[0],
				hours: sets[1],
				days: sets[2],
				months: sets[3],
				weekdays,
				dayWildcard: fields[2] === "*",
				weekdayWildcard: fields[4] === "*"
			};
		}
		/** Whether the expression parses. */
		function isValidCron(expr) {
			return parseCron(expr) !== null;
		}
		/** Whether a set is an evenly spaced arithmetic sequence. */
		function arithmeticStep(values) {
			const sorted = [...values].sort((a, b) => a - b);
			if (sorted.length < 2) return void 0;
			const step = sorted[1] - sorted[0];
			if (step < 1) return void 0;
			for (let index = 1; index < sorted.length; index++) if (sorted[index] - sorted[index - 1] !== step) return void 0;
			return step;
		}
		const MINUTES_PER_HOUR = 60;
		const HOURS_PER_DAY = 24;
		/**
		* Describe a cron expression in human terms: every N minutes/hours, daily
		* at a time, on weekdays, weekly on fixed weekdays, or monthly on fixed
		* days. Returns `{ kind: 'custom' }` for valid-but-unusual expressions and
		* undefined for invalid ones. The UI renders the description through its
		* own locale templates.
		*/
		function describeCron(expr) {
			const schedule = parseCron(expr);
			if (schedule === null) return void 0;
			const { minutes, hours, days, months, weekdays, dayWildcard, weekdayWildcard } = schedule;
			const time = (hour, minute) => `${String(hour).padStart(2, "0")}:${String(minute).padStart(2, "0")}`;
			const allMonths = months.size === 12;
			const allDays = days.size === 31 && dayWildcard;
			const allWeekdays = weekdays.size === 7 && weekdayWildcard;
			if (allDays && allWeekdays && allMonths) {
				if (minutes.size === MINUTES_PER_HOUR) return { kind: "everyMinute" };
				const minuteStep = arithmeticStep(minutes);
				if (minuteStep !== void 0 && minuteStep * minutes.size === MINUTES_PER_HOUR) return {
					kind: "everyMinutes",
					minutes: minuteStep
				};
				if (minutes.size === 1) {
					const minute = [...minutes][0];
					if (hours.size === HOURS_PER_DAY) return {
						kind: "everyHours",
						hours: 1
					};
					const hourStep = arithmeticStep(hours);
					if (hourStep !== void 0 && hourStep * hours.size === HOURS_PER_DAY) return {
						kind: "everyHours",
						hours: hourStep
					};
					if (hours.size === 1) return {
						kind: "dailyAt",
						time: time([...hours][0], minute)
					};
				}
				return { kind: "custom" };
			}
			if (minutes.size === 1 && hours.size === 1 && allMonths) {
				const minute = [...minutes][0];
				const hour = [...hours][0];
				if (allDays && !weekdayWildcard) {
					const weekdaysSorted = [...weekdays].sort((a, b) => a - b);
					if (weekdaysSorted.length === 5 && weekdaysSorted.every((day, index) => day === index + 1)) return {
						kind: "weekdaysAt",
						time: time(hour, minute)
					};
					return {
						kind: "weeklyAt",
						weekdays: weekdaysSorted,
						time: time(hour, minute)
					};
				}
				if (allWeekdays && !dayWildcard) return {
					kind: "monthlyAt",
					days: [...days].sort((a, b) => a - b),
					time: time(hour, minute)
				};
			}
			return { kind: "custom" };
		}
		/**
		* Compute the next matching instant after `fromMs` (ms epoch), in local time,
		* at minute granularity, strictly greater than `fromMs`. Returns the ms epoch
		* of the matching minute's start, or undefined when nothing matches within
		* 366 days (e.g. `0 0 30 2 *`).
		*/
		function nextRunAtMs(expr, fromMs) {
			const schedule = parseCron(expr);
			if (schedule === null) return void 0;
			const from = new Date(fromMs);
			const scan = new Date(from.getFullYear(), from.getMonth(), from.getDate(), from.getHours(), from.getMinutes() + 1, 0, 0);
			const limitMs = fromMs + 366 * 24 * 60 * 60 * 1e3;
			while (scan.getTime() <= limitMs) {
				if (matches(schedule, scan)) return scan.getTime();
				scan.setMinutes(scan.getMinutes() + 1);
			}
		}
		/** Parse one comma-list field into the match set. */
		function parseField(field, min, max, out) {
			if (field === "*") {
				for (let value = min; value <= max; value++) out.add(value);
				return true;
			}
			for (const part of field.split(",")) {
				if (part === "") return false;
				const [range, stepRaw] = part.split("/");
				let low;
				let high;
				if (range === "*") {
					low = min;
					high = max;
				} else if (range.includes("-")) {
					const [a, b] = range.split("-");
					if (a === "" || b === "" || !isDigits(a) || !isDigits(b)) return false;
					low = Number(a);
					high = Number(b);
				} else if (isDigits(range)) {
					low = Number(range);
					high = Number(range);
				} else return false;
				if (low < min || high > max || low > high) return false;
				const step = stepRaw === void 0 ? 1 : isDigits(stepRaw) ? Number(stepRaw) : NaN;
				if (!Number.isInteger(step) || step < 1) return false;
				for (let value = low; value <= high; value += step) out.add(value);
			}
			return true;
		}
		/** Day/weekday OR semantics: a restricted day field alone gates, and vice versa. */
		function matches(schedule, date) {
			if (!schedule.minutes.has(date.getMinutes())) return false;
			if (!schedule.hours.has(date.getHours())) return false;
			if (!schedule.months.has(date.getMonth() + 1)) return false;
			const dayMatches = schedule.days.has(date.getDate());
			const weekdayMatches = schedule.weekdays.has(date.getDay());
			if (schedule.dayWildcard) return weekdayMatches;
			if (schedule.weekdayWildcard) return dayMatches;
			return dayMatches || weekdayMatches;
		}
		function isDigits(value) {
			return /^\d+$/.test(value);
		}
		//#endregion
		//#region src/core/tasks.ts
		/** The task's live bindings: the multi-source list, with the legacy single
		*  `bind` field projected as a one-element list (old data parses untouched).
		*  EVERY reader goes through here — never the raw field. */
		function taskBindsOf(task) {
			const binds = task.binds;
			if (binds !== void 0 && binds.length > 0) return binds;
			return task.bind !== void 0 ? [task.bind] : [];
		}
		/** Whether two bindings name the SAME source (same kind, same id). */
		function sameBind(a, b) {
			if (a.kind === "session" && b.kind === "session") return a.sessionId === b.sessionId;
			if (a.kind === "workspace" && b.kind === "workspace") return a.workspaceId === b.workspaceId;
			return false;
		}
		/** The card's source-line label — ONE derivation for every card: the bound
		*  session's title when the task has a session source and that title differs
		*  from the task's own title (a source named exactly like the task is the
		*  task itself — never a repeated line), else the workspace label, skipped
		*  the same way. Empty = no source line (the card shows no source, never a
		*  guessed default). Callers pass the RESOLVED titles (live titles, raw ids
		*  as fallback); this is pure display policy. */
		function cardSourceLabel(task, boundTitle, workspaceTitle) {
			if (boundTitle !== "" && boundTitle !== task.title) return boundTitle;
			if (workspaceTitle !== "" && workspaceTitle !== task.title) return workspaceTitle;
			return "";
		}
		/** Brand an unknown string as a schedule mode. */
		function isScheduleMode(value) {
			return value === "cron" || value === "chain";
		}
		/** The five kanban columns, in display order. */
		const COLUMNS = [
			{
				status: "backlog",
				label: "待规划"
			},
			{
				status: "todo",
				label: "待办"
			},
			{
				status: "running",
				label: "进行中"
			},
			{
				status: "review",
				label: "待审核"
			},
			{
				status: "done",
				label: "已完成"
			}
		];
		/** Statuses a user may move a card to manually (execution states are owned by the runner). */
		const MANUAL_STATUSES = [
			"backlog",
			"todo",
			"done"
		];
		/**
		* The status one column over, along the board's own COLUMNS order — the keyboard
		* equivalent of dragging a card one column. Pure, and deliberately derived from
		* COLUMNS rather than from a second hand-written list: a column added or
		* reordered in the board is reachable by `[` / `]` the same day.
		*
		* Returns undefined at either end and for a status the board does not show, so
		* the caller can stay silent instead of inventing a move. Whether the step is
		* actually ALLOWED is `resolveCardDrop`'s judgment, not this function's: this
		* answers "which column is next", never "may I".
		*/
		function adjacentStatus(status, direction) {
			const at = COLUMNS.findIndex((column) => column.status === status);
			if (at < 0) return void 0;
			return COLUMNS[at + direction]?.status;
		}
		/** All valid statuses (closed union guard). */
		const ALL_STATUSES = [
			"backlog",
			"todo",
			"running",
			"review",
			"done"
		];
		/** Brand an unknown string as a status; undefined when it is not one. */
		function isTaskStatus(value) {
			return typeof value === "string" && ALL_STATUSES.includes(value);
		}
		/**
		* The landing column of a task created by an external sidebar drop: the
		* column the item was dropped into — all five columns are respected, so a
		* drop on 进行中 / 待审核 / 已完成 stays exactly where it landed.
		*/
		function landingStatusOf(dropStatus) {
			return dropStatus;
		}
		/**
		* Whether the task has anything EXECUTABLE to drive: its execution prompt is
		* non-empty (trimmed). THE one judgment every drive path reads — a task with
		* no prompt can never be run, automated, cruised or comment-driven (the
		* refine flow itself is the exception: it PRODUCES the prompt, it does not
		* execute it). Empty means strictly blank; placeholder text is not empty.
		*/
		function taskExecutable(task) {
			return task.prompt.trim() !== "";
		}
		/**
		* 真执行自动补全（缺则补、填则守、任何执行时刻）：新建任务允许
		* 标题/描述全空——**任何一次**真正执行（手动/重复/接续链/定时/巡航/自动化）
		* 时，缺什么补什么；已存在的字段永不覆盖（用户内容保真——「不会再动」=
		* 已填的不动，缺的补齐）。评论轮/完善轮/外源轮不是任务执行，不触发。
		* 标题缺 → 执行 Prompt 的第一个非空行（trim + 40 字符上限）；
		* 描述缺 → 整个执行 Prompt（trim）。
		* 返回需要补的字段（无则 undefined）。
		*/
		function supplementLaunchFields(task) {
			if (!taskExecutable(task)) return void 0;
			const prompt = task.prompt.trim();
			if (prompt === "") return void 0;
			const supplements = {};
			if (task.title.trim() === "") supplements.title = (prompt.split(/\r?\n/).map((line) => line.trim()).find((line) => line !== "") ?? "").slice(0, 40);
			if (task.description.trim() === "") supplements.description = prompt;
			return supplements.title !== void 0 || supplements.description !== void 0 ? supplements : void 0;
		}
		/**
		* Whether the task's COLUMN currently allows automation to run. One shared
		* judgment for EVERY automation kind — the task-level schedule rule and the
		* session-level rules read the same set: only todo/running are drivable;
		* backlog (shelved), review (a human decision is pending) and done
		* (completed) pause whichever rule armed them.
		*/
		function taskColumnAllowsAutomation(task) {
			return task.status !== "backlog" && task.status !== "review" && task.status !== "done";
		}
		/** The readiness of a task's schedule rule (see {@link RuleReadiness}). */
		function ruleReadiness(task) {
			const schedule = task.schedule;
			if (schedule === void 0 || !schedule.enabled) return { kind: "disabled" };
			if (!taskExecutable(task)) return { kind: "blocked" };
			if (schedule.mode === "chain") return { kind: "active" };
			if (!taskColumnAllowsAutomation(task)) return {
				kind: "paused",
				status: task.status
			};
			return { kind: "active" };
		}
		/**
		* Disarm a task's schedule rule for good — the completed-task shut-off. The
		* rule's identity (cron expression, mode, budget, prime, counters) is kept
		* so re-arming it later resumes from the same configured behavior; only
		* `enabled` and the pending `nextRunAt` are cleared. A no-op on tasks with
		* no rule or an already-disarmed one.
		*/
		function disarmSchedule(task, now) {
			const schedule = task.schedule;
			if (schedule === void 0 || !schedule.enabled) return task;
			return withSchedule(task, {
				enabled: false,
				nextRunAt: void 0
			}, now);
		}
		/**
		* The task's latest execution (rounds are appended chronologically). The ONE
		* "what's the newest round" derivation every surface reads — the card's
		* settled chip, the board's waiting probe, the detail's failure hint.
		*/
		function latestExecutionOf(task) {
			return task.executions[task.executions.length - 1];
		}
		/**
		* The outcome of the card's OWN most recent plain run — the answer to "how did
		* this task's last execution end?", used by every surface that tints, labels
		* or explains a card's result.
		*
		* It is deliberately NOT `latestExecutionOf(task)?.result`: with per-session
		* lanes the newest record is routinely something else (a comment round, an
		* observed native turn, or a comment still SAVED with no result at all), which
		* made the same card read as failed on one surface and succeeded on another,
		* and once tinted the 「N 次执行」 chip red while every run had succeeded.
		*/
		function lastPlainResult(task) {
			const runs = plainRunsOf(task);
			return runs[runs.length - 1]?.result;
		}
		/**
		* Whether arming a chain rule needs the one-shot "endless loop" confirmation:
		* an unlimited chain (no run budget) keeps firing real agent sessions until a
		* run fails or the user stops it. One guard, shared by every surface that can
		* arm a chain (the detail editor and the overview's enable switch) — a second
		* surface must never re-derive this decision.
		*/
		function chainUnlimited(mode, maxRuns) {
			return mode === "chain" && (maxRuns === void 0 || maxRuns < 1);
		}
		/** Admitted image media types (the INTAKE gate reads this — new uploads
		*  outside it are rejected or transcoded at the door; stored rows are
		*  grandfathered, see below). */
		const IMAGE_MEDIA_TYPES = [
			"image/png",
			"image/jpeg",
			"image/webp",
			"image/gif"
		];
		/** Whether a raw value is a storable prompt image (non-empty data + a media
		*  type string). Deliberately LENIENT (no whitelist): storage walls must
		*  never retroactively delete what an older version stored — the whitelist
		*  gates new intake and the send layer, never the ledger. Crash-safety (no
		*  undefined derefs downstream) is what storage guarantees. */
		function isWellFormedImage(entry) {
			if (typeof entry !== "object" || entry === null) return false;
			const candidate = entry;
			return typeof candidate.data === "string" && candidate.data !== "" && typeof candidate.mediaType === "string" && candidate.mediaType !== "";
		}
		/** Whether a raw value is a storable file ref (non-empty receipt + name;
		*  bytes only needs to be a number for display math — NaN displays ugly but
		*  never crashes and never deletes). Same leniency law as images above. */
		function isWellFormedFile(entry) {
			if (typeof entry !== "object" || entry === null) return false;
			const candidate = entry;
			return typeof candidate.receiptId === "string" && candidate.receiptId !== "" && typeof candidate.name === "string" && candidate.name !== "" && typeof candidate.bytes === "number";
		}
		/** Keep well-formed prompt images (a dirty element washes out, never the
		*  row and never a downstream crash). */
		function normalizePromptImages(raw) {
			if (!Array.isArray(raw)) return void 0;
			const kept = raw.filter(isWellFormedImage);
			return kept.length > 0 ? kept : void 0;
		}
		/** Keep well-formed file refs (same law as images). */
		function normalizePromptFiles(raw) {
			if (!Array.isArray(raw)) return void 0;
			const kept = raw.filter(isWellFormedFile);
			return kept.length > 0 ? kept : void 0;
		}
		/** Create a task from user input. */
		function createTask(input, now, id, order = 0) {
			const status = input.status ?? "todo";
			return {
				id,
				title: input.title.trim(),
				description: input.description.trim(),
				prompt: input.prompt.trim(),
				status,
				order,
				createdAt: now,
				updatedAt: now,
				viewedAt: now,
				statusHistory: [{
					status,
					at: now
				}],
				executions: [],
				...input.promptImages !== void 0 && input.promptImages.length > 0 ? { promptImages: input.promptImages.map((image) => ({ ...image })) } : {},
				...input.promptFiles !== void 0 && input.promptFiles.length > 0 ? { promptFiles: input.promptFiles.map((file) => ({ ...file })) } : {},
				...input.workspaceId !== void 0 ? { workspaceId: input.workspaceId } : {},
				...input.provider !== void 0 ? { provider: input.provider } : {},
				...input.model !== void 0 ? { model: input.model } : {},
				...input.reasoningEffort !== void 0 ? { reasoningEffort: input.reasoningEffort } : {},
				...input.agentPreset !== void 0 ? { agentPreset: input.agentPreset } : {},
				...input.permission !== void 0 ? { permission: input.permission } : {},
				...input.color !== void 0 && input.color !== "" ? { color: input.color } : {}
			};
		}
		/** Clone a task with an updated status and a fresh updatedAt. A real column
		*  move appends to the status history (cycle/streak/throughput derivations
		*  read it — without it every duration is a guess); a same-status touch only
		*  refreshes updatedAt. A legacy row moving for the first time backfills its
		*  birth column from createdAt (the honest birth instant, same as the read
		*  path — one birth rule, never two). */
		function withStatus(task, status, now) {
			if (task.status === status) return {
				...task,
				updatedAt: now
			};
			const birthAt = Number.isFinite(task.createdAt) && task.createdAt > 0 ? task.createdAt : task.updatedAt;
			const birth = task.statusHistory === void 0 ? [{
				status: task.status,
				at: birthAt
			}] : [];
			return {
				...task,
				status,
				updatedAt: now,
				statusHistory: [
					...birth,
					...task.statusHistory ?? [],
					{
						status,
						at: now
					}
				]
			};
		}
		/**
		* Merge a schedule patch into a task's schedule rule (creating it when
		* absent), with a fresh updatedAt. Keys present in the patch overwrite the
		* current value — including explicit `undefined`, which clears a field (used
		* to disarm `nextRunAt`); absent keys keep their current value.
		*/
		function withSchedule(task, patch, now) {
			const current = task.schedule;
			const schedule = {
				enabled: current?.enabled ?? false,
				mode: current?.mode ?? "cron",
				cron: current?.cron ?? "",
				nextRunAt: current?.nextRunAt,
				lastTriggeredAt: current?.lastTriggeredAt,
				maxRuns: current?.maxRuns,
				runCount: current?.runCount ?? 0,
				missedToleranceMs: current?.missedToleranceMs,
				primed: true
			};
			if ("enabled" in patch) schedule.enabled = patch.enabled ?? false;
			if ("mode" in patch) schedule.mode = patch.mode ?? "cron";
			if ("cron" in patch) schedule.cron = patch.cron ?? "";
			if ("nextRunAt" in patch) schedule.nextRunAt = patch.nextRunAt;
			if ("lastTriggeredAt" in patch) schedule.lastTriggeredAt = patch.lastTriggeredAt;
			if ("maxRuns" in patch) schedule.maxRuns = patch.maxRuns;
			if ("runCount" in patch) schedule.runCount = patch.runCount ?? 0;
			if ("missedToleranceMs" in patch) schedule.missedToleranceMs = patch.missedToleranceMs;
			return {
				...task,
				updatedAt: now,
				schedule
			};
		}
		/**
		* Open a fresh execution on a task: move it to 'running' and append a
		* running execution record. Returns the new task and the new execution.
		*/
		function startExecution(task, now, executionId) {
			const execution = {
				id: executionId,
				sessionId: void 0,
				startedAt: now,
				endedAt: void 0,
				result: void 0,
				error: void 0,
				viewedAt: now
			};
			return {
				task: withStatus({
					...task,
					executions: [...task.executions, execution]
				}, "running", now),
				execution
			};
		}
		/** Paused-readiness explanation keyed by the pausing status. */
		/**
		* Create an externally-observed round (see `ExecutionRecord.external`): the
		* board detected native-side activity on a related session (out-of-band
		* chat). It enters the session's comment thread and drives the task state
		* like a running round, but is never queued/injected — it is already
		* happening. `refine: true` marks a refinement-session round, which keeps
		* the task in its column.
		*/
		function newExternalRound(options) {
			return {
				id: options.id,
				sessionId: options.sessionId,
				startedAt: options.now,
				endedAt: void 0,
				result: void 0,
				error: void 0,
				comment: options.text ?? "",
				sessionAnchor: options.sessionId,
				external: true,
				...options.refine === true ? { refine: true } : {},
				...options.anchor !== void 0 ? { anchor: options.anchor } : {},
				...options.imageOnly === true ? { imageOnly: true } : {}
			};
		}
		/**
		* Create a new comment-continuation round (pure): the single factory for
		* both comment anchors. An execution-anchored comment (`parentExecutionId`)
		* continues a settled run and reuses its session; a session-anchored
		* comment (`sessionAnchor`) continues a linked session and reuses that
		* session. Either way the round carries the session id it will be injected
		* into, so the dispatcher's eligibility scan and the injection hand-off work
		* unchanged — one queue, one model, two surfaces.
		*/
		function newCommentRound(options) {
			return {
				id: options.id,
				sessionId: options.sessionId,
				startedAt: options.now,
				endedAt: void 0,
				result: void 0,
				error: void 0,
				comment: options.text,
				...options.command === true ? { command: true } : {},
				...options.images !== void 0 && options.images.length > 0 ? { promptImages: options.images.map((image) => ({ ...image })) } : {},
				...options.files !== void 0 && options.files.length > 0 ? { promptFiles: options.files.map((file) => ({ ...file })) } : {},
				...options.parentExecutionId !== void 0 ? { parentExecutionId: options.parentExecutionId } : {},
				...options.sessionAnchor !== void 0 ? { sessionAnchor: options.sessionAnchor } : {},
				...options.ruleId !== void 0 ? { ruleId: options.ruleId } : {}
			};
		}
		/**
		* Create a direct-send round (直发模式): the message was already delivered to
		* the native session, so the round is settled-succeeded at birth — it exists
		* to make the line visible in the session's comment thread (next to drive
		* comments and execution comments), never to queue or drive anything.
		*/
		function newDirectRound(options) {
			return {
				id: options.id,
				sessionId: options.sessionId,
				startedAt: options.now,
				endedAt: options.now,
				result: "succeeded",
				error: void 0,
				comment: options.text,
				sessionAnchor: options.sessionId,
				direct: true
			};
		}
		/**
		* Whether the card already holds completed work awaiting the human gate:
		* any settled non-refine round with a succeeded/failed outcome (plain runs,
		* comment continuations, externally-observed turns, direct sends — all are
		* completions the user has not confirmed). Cancelled rounds never count:
		* they are noise/abort, not work. Refine rounds never count: preparation
		* keeps its column by contract (`settleRefine`).
		* Used ONLY to decide where a `cancelled` settle lands (review vs todo) —
		* success/failure always land in review (or stay running for incomplete
		* batches/chains or sibling lanes).
		*/
		function hasCompletedWork(task) {
			return task.executions.some((round) => round.endedAt !== void 0 && (round.result === "succeeded" || round.result === "failed") && round.refine !== true);
		}
		/**
		* THE one column decision for every non-refine settle (success/failure/
		* cancel). Pure so the whole board — live watches, reconciles, watchdogs,
		* spurious-external sweeps — lands in the same column for the same facts.
		* Priority:
		* 1. another lane still open → `running` (the column aggregates sessions);
		* 2. succeeded/failed → `review` (chain/batch incomplete stays `running`);
		* 3. cancelled → keep a parked column as-is; from `running`, return to
		*    `review` when completed work exists (the human gate survives noise),
		*    else `todo` (nothing completed — back to the queue).
		*/
		function settleColumnOf(task, outcome, othersOpen, chainIncomplete, batchIncomplete) {
			if (othersOpen) return "running";
			if (outcome === "cancelled") {
				if (task.status !== "running") return task.status;
				return hasCompletedWork(task) ? "review" : "todo";
			}
			if (chainIncomplete || batchIncomplete) return "running";
			return "review";
		}
		/**
		* Settle a running execution: record the outcome on the NAMED round (any
		* round of the card can be the one finishing — a card may run several
		* sessions at once) and move the card into the column its whole set of
		* sessions adds up to. No-op (returns the input task) when the round is
		* unknown or already settled.
		*
		* A settled run always lands in 'review' — the human gate between execution
		* and completion: succeeded runs await human confirmation, failed runs await
		* a decision (comment to steer, rerun, or move on). The only exception is a
		* scheduled batch that keeps the card 'running' between runs: a succeeded
		* run belonging to an armed schedule whose next automatic run is still to
		* come stays 'running' — for a budgeted cron batch (`runCount < maxRuns`:
		* the scheduler increments the counter at fire-accept, before the run can
		* settle, so a just-settled scheduled run is already counted) and for chain
		* mode (`runCount + 1 < maxRuns`: the hand-off increments the counter when
		* it launches the NEXT run, while the armed first run is never counted, so
		* a just-settled run is one behind; unlimited chains always stay 'running').
		* A cancelled run returns to 'todo' ONLY when the card holds no completed
		* work; with prior success/failure it lands in 'review' (noise must never
		* swallow the human gate — see `settleColumnOf`).
		*/
		function settleExecution(task, executionId, outcome, now, error) {
			const index = task.executions.findIndex((execution) => execution.id === executionId);
			if (index === -1) return task;
			const execution = task.executions[index];
			if (execution.endedAt !== void 0) return task;
			const settled = {
				...execution,
				endedAt: now,
				result: outcome,
				error
			};
			const executions = [...task.executions];
			executions[index] = settled;
			const schedule = task.schedule;
			const chainIncomplete = outcome === "succeeded" && schedule !== void 0 && schedule.enabled && schedule.mode === "chain" && (schedule.maxRuns === void 0 || schedule.runCount + 1 < schedule.maxRuns);
			const batchIncomplete = outcome === "succeeded" && schedule !== void 0 && schedule.enabled && schedule.mode === "cron" && schedule.maxRuns !== void 0 && schedule.runCount < schedule.maxRuns;
			const status = settleColumnOf(task, outcome, openExecutionRoundsOf({
				...task,
				executions
			}).length > 0, chainIncomplete, batchIncomplete);
			return withStatus({
				...task,
				executions
			}, status, now);
		}
		/**
		* Whether ONE round is genuinely in flight (someone is working on it right
		* now), as opposed to saved-and-waiting or already settled. The single
		* structural judgment behind `openRoundsOf` — derived from the round itself,
		* never from the card's column, so a card parked mid-flight still reports the
		* work that is really running.
		*
		* Enumerated by KIND (each category is real, and a comment body does NOT mean
		* "queued" — an observed native turn carries its user text too):
		* - a plain run: in flight from creation until it settles;
		* - a refinement round: in flight while open (its session is working);
		* - an EXTERNAL round (a native turn the board observed): in flight while
		*   open — it is already happening, it is never queued or injected, and its
		*   `comment` is only the thread body;
		* - a comment round: in flight only ONCE INJECTED. A saved comment is queued
		*   — it must never show a spinner, block a rerun, hold a slot or mark its
		*   session busy.
		*/
		function isOpenRound(round) {
			if (round.endedAt !== void 0) return false;
			if (round.external === true) return true;
			if (round.refine === true) return true;
			if (round.comment !== void 0) return round.injectedAt !== void 0;
			return true;
		}
		/** Every in-flight round of a task, in submission order. THE truth the
		*  dispatcher, the budget and the column state machine all read — a card may
		*  legitimately run several sessions at once (one lane per session), so
		*  "the task's latest execution" is no longer the only thing that can be
		*  running. */
		function openRoundsOf(task) {
			return task.executions.filter(isOpenRound);
		}
		/**
		* Every in-flight EXECUTION round (refinement excluded). Refinement is
		* preparation inside its own column — it holds a budget slot while working
		* but must never hold the card in 进行中 nor block a plain settle from
		* landing in 待审核 (the display truth `executing` already excludes it;
		* the column gate follows the same law here).
		*/
		function openExecutionRoundsOf(task) {
			return task.executions.filter((round) => round.refine !== true && isOpenRound(round));
		}
		/** Whether a given session of the task is busy (an in-flight round anchored
		*  to it). A comment may only inject into a session that is idle, and a
		*  session's own comments always go in order — the lane is the session. */
		function sessionIsBusy(task, sessionId) {
			return task.executions.some((round) => isOpenRound(round) && round.sessionId === sessionId);
		}
		/**
		* Whether the task is genuinely executing right now: ANY of its rounds is in
		* flight. A pending comment round (saved while the cruise is off, the task
		* not running) is NOT an open run — it must never show a spinner on the card,
		* block a rerun, or block a drag. An open requirement-refinement round IS:
		* the task's session is working, so a plain run must not start on top of it.
		* One shared judgment for the card, the drop rules and the run guard.
		*/
		function hasOpenRun(task) {
			return openRoundsOf(task).length > 0;
		}
		/**
		* How many comment rounds of a task are saved but not yet injected (the
		* task's comment queue). These wait for the dispatcher — the budget, the
		* cruise toggle, or the task's own busy round — and can be cancelled.
		*/
		function pendingCommentCount(task) {
			let count = 0;
			for (const round of task.executions) if (round.comment !== void 0 && round.injectedAt === void 0 && round.endedAt === void 0 && round.external !== true) count += 1;
			return count;
		}
		/**
		* The task's plain runs in chronological order — every execution record
		* except comment and refine rounds. The single numbering source for the
		* execution history list (TaskDetail) and the review page header ("第 N 次
		* 执行"): continuations and refinements are not part of the run sequence.
		*/
		function plainRunsOf(task) {
			return task.executions.filter((execution) => execution.comment === void 0 && execution.refine !== true);
		}
		/**
		* The task's requirement-refinement rounds in chronological order (the
		* conversation turns of the task's bound refine session).
		*/
		function refineRoundsOf(task) {
			return task.executions.filter((round) => round.refine === true);
		}
		/** Whether a requirement-refinement round is currently running for the task. */
		function refining(task) {
			return task.executions.some((round) => round.refine === true && round.endedAt === void 0);
		}
		/**
		* Whether the task has anything to refine: title, description or execution
		* prompt — at least one must be non-blank, because the refine instruction is
		* built from exactly these three fields. An all-blank task would send an
		* empty requirement round (a wasted run that only flips the card's lights);
		* the UI disables the entry and says so instead of launching it.
		*/
		function refinable(task) {
			return task.title.trim() !== "" || task.description.trim() !== "" || task.prompt.trim() !== "";
		}
		/**
		* Whether the task is genuinely EXECUTING right now (display truth): an open
		* plain run, external round or injected comment — but NOT a lone refinement
		* round. Refining is preparation inside the backlog column (its own 完善中
		* chip + breathing); reading it as 进行中 is the "一点完善整卡变进行中" bug.
		* Blocking semantics (run guard, concurrency budget, drop rules, reconcile
		* drive) stay on {@link hasOpenRun} — this is display only, never a gate.
		*/
		function executing(task) {
			return task.executions.some((round) => isOpenRound(round) && round.refine !== true);
		}
		/** Bind (or re-bind) the task's requirement-refinement session. */
		function withRefineSession(task, sessionId, now) {
			if (task.refineSessionId === sessionId) return task;
			return {
				...task,
				refineSessionId: sessionId,
				updatedAt: now
			};
		}
		/**
		* Settle a requirement-refinement round: record the outcome on the round
		* without moving the task out of its column (refinement is preparation, not
		* execution — the card stays exactly where it is). No-op when the round is
		* unknown or already settled.
		*/
		function settleRefine(task, executionId, outcome, now, error) {
			const index = task.executions.findIndex((round) => round.id === executionId);
			if (index === -1) return task;
			const round = task.executions[index];
			if (round.endedAt !== void 0) return task;
			const executions = [...task.executions];
			executions[index] = {
				...round,
				endedAt: now,
				result: outcome,
				error,
				viewedAt: now
			};
			return {
				...task,
				updatedAt: now,
				executions
			};
		}
		/**
		* Decide what dropping a card onto a column does, reconciling the manual
		* move with the execution owner. Automation is never a drop obstacle: a
		* chain card is freely movable — "leaving the lane" is expressed as a
		* pause/stop in the controller's moveTask, never as a rejection here.
		* - Dropping on 'running' reruns the task (the same "run again" semantics
		*   as the detail button); a live run is a no-op/reject, shared with every
		*   other surface.
		* - While an execution is open, 'review'/'done' are refused: the runner
		*   owns those transitions and would overwrite a manual move when the run
		*   settles.
		* - Anything else is a plain manual move; dropping on the current column is
		*   a no-op (except 'running' via the rerun rule above).
		*/
		function resolveCardDrop(task, target) {
			const busy = hasOpenRun(task);
			if (target === "running") {
				if (!busy) return { kind: "run" };
				if (task.status === target) return { kind: "none" };
				return {
					kind: "reject",
					reason: "busy"
				};
			}
			if (task.status === target) return { kind: "none" };
			if (busy && (target === "review" || target === "done")) return {
				kind: "reject",
				reason: "busy"
			};
			if (task.status === "running" && busy) return {
				kind: "reject",
				reason: "busy"
			};
			return {
				kind: "move",
				status: target
			};
		}
		/**
		* Move a card into a column at a given position, renumbering the target
		* column's sort keys. `beforeId` inserts before that card (undefined =
		* column tail); a same-column move removes the card first, so the insertion
		* index is naturally off-by-one safe. Only the target column's orders are
		* rewritten — other columns keep their relative order (gaps are harmless,
		* since sorting only compares within a column).
		*
		* THE order-key invariant: the ARRAY order is never trusted — only the `order`
		* keys are truth, because the store keeps array positions while every render
		* sorts by key. Splicing against raw array order lands the card at a visually
		* wrong gap, defeats the same-spot check (phantom updatedAt churn on every
		* sibling → sync storms and whole-column FLIP flashes), and scrambles the
		* column on the next drag. Both lists below are key-sorted first — the same
		* law promoteToColumnTop already follows.
		*/
		function applyCardOrder(tasks, movedId, targetStatus, beforeId, now) {
			const moved = tasks.find((task) => task.id === movedId);
			if (moved === void 0) return [...tasks];
			if (beforeId === movedId) return [...tasks];
			const target = tasks.filter((task) => task.id !== movedId).filter((task) => task.status === targetStatus).sort((a, b) => a.order - b.order);
			const at = beforeId === void 0 ? target.length : target.findIndex((task) => task.id === beforeId);
			const position = at < 0 ? target.length : at;
			const ordered = [
				...target.slice(0, position),
				moved,
				...target.slice(position)
			];
			const currentOrder = tasks.filter((task) => task.status === targetStatus).sort((a, b) => a.order - b.order).map((task) => task.id);
			if (currentOrder.length === ordered.length && currentOrder.every((id, index) => ordered[index]?.id === id)) return [...tasks];
			return tasks.map((task) => {
				if (task.id === movedId) return withStatus({
					...task,
					order: ordered.findIndex((row) => row.id === task.id)
				}, targetStatus, now);
				if (task.status === targetStatus) {
					const order = ordered.findIndex((row) => row.id === task.id);
					return order === task.order ? task : {
						...task,
						order,
						updatedAt: now
					};
				}
				return task;
			});
		}
		/**
		* Promote a card to the TOP of its column — the "newest state first" rule:
		* a task that just entered a column (freshly created, newly bound from the
		* workspace, or passing through a status change) reads as the newest item
		* of that column. Existing cards shift down, preserving their relative
		* order; a manual reorder later overrides the promotion. Only the target
		* column's orders are rewritten.
		*/
		function promoteToColumnTop(tasks, movedId, targetStatus, now) {
			const moved = tasks.find((task) => task.id === movedId);
			if (moved === void 0) return [...tasks];
			if (moved.status === targetStatus && moved.order === 0) return [...tasks];
			const others = tasks.filter((task) => task.id !== movedId && task.status === targetStatus).sort((a, b) => a.order - b.order);
			return tasks.map((task) => {
				if (task.id === movedId) return withStatus({
					...task,
					order: 0
				}, targetStatus, now);
				if (task.status === targetStatus) {
					const order = others.findIndex((row) => row.id === task.id) + 1;
					return order === task.order ? task : {
						...task,
						order,
						updatedAt: now
					};
				}
				return task;
			});
		}
		//#endregion
		//#region src/core/automation.ts
		/** Brand an unknown value as a valid rule (persisted-state guard). */
		function isSessionRule(value) {
			if (typeof value !== "object" || value === null) return false;
			const rule = value;
			if (typeof rule.id !== "string" || rule.id === "") return false;
			if (typeof rule.sessionId !== "string" || rule.sessionId === "") return false;
			const usePrompt = rule.usePrompt === true;
			if (typeof rule.instruction !== "string" || rule.instruction === "" && !usePrompt) return false;
			if (rule.send !== "queue" && rule.send !== "steer") return false;
			if (typeof rule.enabled !== "boolean") return false;
			if ((rule.trigger === "on-complete" ? "on-complete" : "cron") === "on-complete") return rule.nextAt === void 0;
			if (typeof rule.cron !== "string" || rule.cron === "") return false;
			if (rule.enabled === false) return true;
			return typeof rule.nextAt === "number" && Number.isFinite(rule.nextAt);
		}
		/** Parse + validate a persisted rule list (invalid rows dropped; legacy rows
		*  without a trigger normalize to cron, without usePrompt to custom text). */
		function normalizeSessionRules(raw) {
			if (!Array.isArray(raw)) return void 0;
			const out = [];
			const seen = /* @__PURE__ */ new Set();
			for (const row of raw) {
				if (!isSessionRule(row) || seen.has(row.id)) continue;
				seen.add(row.id);
				const trigger = row.trigger === "on-complete" ? "on-complete" : "cron";
				out.push({
					id: row.id,
					sessionId: row.sessionId,
					instruction: row.instruction,
					...row.usePrompt === true ? { usePrompt: true } : {},
					trigger,
					...trigger === "cron" ? {
						cron: row.cron,
						nextAt: row.nextAt
					} : { cron: "" },
					send: row.send,
					enabled: row.enabled,
					...typeof row.lastAt === "number" ? { lastAt: row.lastAt } : {}
				});
			}
			return out.length > 0 ? out : void 0;
		}
		/** The projection row back to a full rule (the rows are the single read shape;
		*  this is the one bridge back for consumers that need the rule's own shape,
		*  e.g. the readiness judgment). */
		function sessionRuleOf(row) {
			return {
				id: row.ruleId,
				sessionId: row.sessionId,
				instruction: row.instruction,
				...row.usePrompt === true ? { usePrompt: true } : {},
				trigger: row.trigger,
				cron: row.cron,
				send: row.send,
				enabled: row.enabled,
				...row.nextAt !== void 0 ? { nextAt: row.nextAt } : {},
				...row.lastAt !== void 0 ? { lastAt: row.lastAt } : {}
			};
		}
		function sessionRuleReadiness(task, rule) {
			if (!rule.enabled) return { kind: "disabled" };
			if (rule.usePrompt === true && !taskExecutable(task)) return { kind: "blocked" };
			if (rule.trigger === "on-complete") return { kind: "active" };
			return taskColumnAllowsAutomation(task) ? { kind: "active" } : {
				kind: "paused",
				status: task.status
			};
		}
		/** The task's unified automation projection: session rules first, then the
		*  task-level schedule rule (when present) — every surface reads one shape. */
		function automationRowsOf(task) {
			const rows = (task.rules ?? []).map((rule) => ({
				kind: "session-rule",
				ruleId: rule.id,
				sessionId: rule.sessionId,
				instruction: rule.instruction,
				...rule.usePrompt === true ? { usePrompt: true } : {},
				trigger: rule.trigger,
				cron: rule.cron,
				send: rule.send,
				enabled: rule.enabled,
				...rule.nextAt !== void 0 ? { nextAt: rule.nextAt } : {},
				...rule.lastAt !== void 0 ? { lastAt: rule.lastAt } : {}
			}));
			const schedule = task.schedule;
			if (schedule !== void 0) rows.push({
				kind: "schedule",
				mode: schedule.mode,
				enabled: schedule.enabled,
				...schedule.cron !== void 0 ? { cron: schedule.cron } : {},
				runCount: schedule.runCount,
				...schedule.maxRuns !== void 0 ? { maxRuns: schedule.maxRuns } : {},
				...schedule.nextRunAt !== void 0 ? { nextRunAt: schedule.nextRunAt } : {}
			});
			return rows;
		}
		/**
		* When a rule should fire next given its current due instant (the next cron
		* match AFTER that instant, like the task scheduler's forward roll). Returns
		* the new due instant, or undefined when the expression is unparseable (or
		* the rule has no cron slot — an on-complete rule never rolls).
		*/
		function nextSessionRuleAt(rule) {
			if (rule.trigger !== "cron" || rule.nextAt === void 0) return void 0;
			return nextRunAtMs(rule.cron, rule.nextAt);
		}
		/** Add / replace rules immutably on a task. */
		function withSessionRules(task, rules) {
			if (rules === void 0 || rules.length === 0) {
				const next = { ...task };
				if ("rules" in next) delete next.rules;
				return next;
			}
			return {
				...task,
				rules
			};
		}
		/** Tasks with LIVE automation — an ENABLED schedule, or at least one ENABLED
		*  session rule. The overview's membership: only what is actually armed shows (a
		*  disarmed schedule is managed from the task detail / the expanded editor,
		*  never listed as if it were running). */
		function automationTasksOf(tasks) {
			return tasks.filter(hasLiveAutomation);
		}
		/** Whether ONE task carries live automation (THE membership predicate —
		*  the overview filter and the `has:auto` search facet both read this, so
		*  the two can never disagree on what "automated" means). Armed = managed
		*  here: an enabled rule on a paused column (backlog/review/done) still
		*  counts — the heartbeat holds (not drops) its slot, and the overview is
		*  where it is managed. Firing liveness is a ticker concern, not membership. */
		function hasLiveAutomation(task) {
			return task.schedule?.enabled === true || task.rules !== void 0 && task.rules.some((rule) => rule.enabled);
		}
		/** Switch every session rule of a task off (the done-column shut-off: a
		*  completed task's rules must never fire again, exactly like its schedule
		*  disarms — the configuration survives, so re-arming resumes each rule).
		*  Due slots clear with the switch (a re-armed rule recomputes from now —
		*  a stale slot must never surprise-fire on resume, task-level disarm law). */
		function disarmSessionRules(task) {
			if (task.rules === void 0) return task;
			return withSessionRules(task, task.rules.map((rule) => ({
				...rule,
				enabled: false,
				nextAt: void 0,
				lastAt: void 0
			})));
		}
		/** Which automation is blocked on the empty prompt (THE cause predicate —
		*  card chips, tooltips, the detail disclosure and the overview row all read
		*  this, so the explanation can never name a different cause than the badge).
		*  `both` reads schedule-first (the task rule is the louder signal); session
		*  rules keep their own cause word downstream. */
		function blockedCauseOf(task) {
			const schedule = task.schedule?.enabled === true && ruleReadiness(task).kind === "blocked";
			const session = task.rules !== void 0 && task.rules.some((rule) => sessionRuleReadiness(task, rule).kind === "blocked");
			if (schedule && session) return "both";
			if (schedule) return "schedule";
			if (session) return "session";
		}
		/** Whether the last plain run failed while its task waits in review (THE
		*  failure word — card tooltip, detail disclosure and overview row read this
		*  instead of each computing "failed?" their own way). */
		function pausedFailedOf(task) {
			return task.status === "review" && lastPlainResult(task) === "failed";
		}
		//#endregion
		//#region src/core/refine.ts
		/**
		* The first-turn instruction for a task's refine session.
		* @param task - the backlog task whose requirement is being fleshed out.
		* @param english - whether to write the instruction in English.
		*/
		function buildRefinePrompt(task, english) {
			const title = task.title.trim();
			const description = task.description.trim();
			const prompt = task.prompt.trim();
			const requirement = [
				...title !== "" ? [english ? `**Title**: ${title}` : `**标题**：${title}`] : [],
				...description !== "" ? [english ? `**Description**: ${description}` : `**描述**：${description}`] : [],
				...prompt !== "" ? [english ? `**Current prompt**: ${prompt}` : `**当前 Prompt**：${prompt}`] : []
			].join("\n");
			if (english) return [
				"You are a requirement research and refinement assistant. Work through the raw requirement below:",
				"",
				"1. Research first: understand the requirement, identify what is vague or missing, and gather what you need — use any tool available to you (web search, page fetching, etc.).",
				"2. Ask before assuming: if key information is missing or several directions are possible, ask me one question at a time and wait for my answer (I reply in this conversation). Do not guess.",
				"3. Deliver: combine your research with my answers into a final, ready-to-run execution prompt — clear goal, scope, constraints, acceptance criteria and steps, in the language of the requirement.",
				"",
				"Output format (strict):",
				"## Requirement Summary",
				"(one or two sentences)",
				"## Final Execution Prompt",
				"(the complete instruction text, ready to copy and execute)",
				"",
				"Raw requirement:",
				requirement
			].join("\n");
			return [
				"你是需求调研与方案完善助手。请围绕下面的「原始需求」展开工作：",
				"",
				"1. 先调研：理解需求，识别模糊点与缺口，收集所需资料——可以使用你拥有的一切工具（联网搜索、抓取网页等）。",
				"2. 先问再定：关键信息缺失或存在多个可选方向时，逐条向我提问确认（我会在对话中回答），不要臆断。",
				"3. 最终产出：综合调研结果与我的回答，产出一份可直接执行的「最终执行 Prompt」——写清目标、范围、约束、验收标准与执行步骤，语言与需求的输入语言一致。",
				"",
				"输出格式（严格遵守）：",
				"## 需求摘要",
				"（一两句话概括）",
				"## 最终执行 Prompt",
				"（可整体复制执行的完整指令文本）",
				"",
				"原始需求如下：",
				requirement
			].join("\n");
		}
		//#endregion
		//#region src/core/linked-sessions.ts
		/** Short display label of a directory path (last non-empty segment). THE one
		*  cwd→label derivation, shared with the session panels' workspace rows. */
		function workspaceLabelOf(cwd) {
			const segment = cwd.split(/[\\/]+/).filter(Boolean).pop();
			return segment !== void 0 && segment !== "" ? segment : cwd;
		}
		/**
		* The session's REAL name, or undefined. A durable title that is exactly the
		* workspace (project) BASENAME is the host's DETERMINISTIC auto-name — the
		* fallback it projects after the first message when nothing named the
		* session — NOT a name a human chose. Every title surface (run rows, linked
		* rows, panel headers) reads through here, so a fresh session never presents
		* its folder as its title. A user who deliberately names a session after its
		* own folder is the accepted, genuinely-ambiguous edge.
		*/
		function realTitleOf(title, cwd) {
			if (title === void 0 || title === "") return void 0;
			if (cwd !== void 0 && cwd !== "" && title === workspaceLabelOf(cwd)) return void 0;
			return title;
		}
		/** The default title of a freshly dragged-in binding (from its native source). */
		function boundSourceTitle(bind, ctx) {
			if (bind.kind === "session") {
				const session = ctx.sessions[bind.sessionId];
				if (session !== void 0) {
					if (session.title !== void 0 && session.title !== "") return session.title;
					const base = session.cwd !== void 0 ? workspaceLabelOf(session.cwd) : void 0;
					if (base !== void 0 && base !== "") return base;
				}
				return bind.sessionId;
			}
			return ctx.workspaces.find((workspace) => workspace.id === bind.workspaceId)?.title ?? bind.workspaceId;
		}
		/** Classify an id drawn from a sidebar drag as a native session or workspace. */
		function resolveExternalKind(id, ctx) {
			if (id in ctx.sessions) return "session";
			if (ctx.workspaces.some((workspace) => workspace.id === id)) return "workspace";
		}
		/** One derived row from a native session source (real title + cwd label).
		*  The TITLE slot carries only a REAL name: the workspace folder has its own
		*  `workspaceLabel` slot, and the host's deterministic auto-name (== the
		*  folder basename) is not a name — an unnamed row keeps the id and the
		*  single 未命名 grammar (`sessionRowTitleOf`) renders it. */
		function rowOf(sessionId, source) {
			const workspaceLabel = source.cwd !== void 0 ? workspaceLabelOf(source.cwd) : void 0;
			return {
				sessionId,
				title: realTitleOf(source.title, source.cwd) ?? sessionId,
				...workspaceLabel !== void 0 ? { workspaceLabel } : {},
				running: source.running,
				pendingInteraction: source.pendingInteraction,
				completed: source.completed === true,
				updatedAt: source.updatedAt
			};
		}
		/**
		* Derive the linked-session rows of one bind from the native snapshot.
		*
		* - session bind → exactly that session (never filtered by archived/blank:
		*   the user dragged it in on purpose; only the hide set applies);
		* - workspace bind → NO rows by itself (a workspace is a source/config
		*   association, never a subscription — folder drops snapshot their members
		*   as explicit session binds at creation instead);
		* - unbound → no rows.
		*/
		function deriveLinkedSessions(bind, sources) {
			if (bind === void 0 || bind.kind === "workspace") return [];
			if (sources.hidden.includes(bind.sessionId)) return [];
			const source = sources.byId[bind.sessionId];
			if (source === void 0) return [];
			return [rowOf(bind.sessionId, source)];
		}
		/** One calendar day (cross-midnight normalization). */
		const DAY_MS = 864e5;
		/** Brand an unknown value as a valid window (persisted-state guard). */
		function isCruiseWindow(value) {
			if (typeof value !== "object" || value === null) return false;
			const window = value;
			const startOk = window.startAt === void 0 || typeof window.startAt === "number" && Number.isFinite(window.startAt);
			const endOk = window.endAt === void 0 || typeof window.endAt === "number" && Number.isFinite(window.endAt);
			return startOk && endOk && (typeof window.startAt === "number" || typeof window.endAt === "number");
		}
		/**
		* Normalize a window: a within-the-same-intent end earlier than the start is
		* a cross-midnight window (22:00 → 02:00 is legal) — yield the end on the
		* start's next day. An end more than a whole day BEFORE the start is NOT a
		* cross-midnight window; it is a date mistake (windowRangeIssueOf returns
		* 'end-too-early') and is left as-entered — the editor rejects it with a
		* specific message instead of the stale rule silently surviving as a
		* "everyday 22:00→01:00 of yesterday" artifact.
		*/
		function normalizeWindow(window) {
			const startAt = window.startAt;
			let endAt = window.endAt;
			if (startAt !== void 0 && endAt !== void 0 && endAt <= startAt && endAt > startAt - 864e5) endAt += DAY_MS;
			return {
				...startAt !== void 0 ? { startAt } : {},
				...endAt !== void 0 ? { endAt } : {}
			};
		}
		/** The one validation verdict for a (normalized) candidate window. */
		function windowRangeIssueOf(window, now) {
			const { startAt, endAt } = window;
			if (startAt === void 0 && endAt === void 0) return "both-empty";
			if (startAt !== void 0 && endAt !== void 0) {
				if (endAt === startAt) return "same-instant";
				if (endAt <= startAt - 864e5) return "end-too-early";
				if (endAt <= now) return "end-past";
				if (startAt <= now) return "start-past";
				return;
			}
			if (endAt !== void 0 && endAt <= now) return "end-past";
		}
		/** Whether a window covers `now` (its appointment is live right now). */
		function isWindowActive(window, now) {
			return (window.startAt === void 0 || window.startAt <= now) && (window.endAt === void 0 || window.endAt > now);
		}
		/** Derive one window's display grammar (see {@link CruiseWindowGrammar}). */
		function cruiseWindowGrammarOf(window) {
			if (window.startAt !== void 0 && window.endAt !== void 0) return {
				kind: "range",
				startAt: window.startAt,
				endAt: window.endAt
			};
			if (window.startAt !== void 0) return {
				kind: "from-start",
				startAt: window.startAt
			};
			return {
				kind: "until-end",
				endAt: window.endAt
			};
		}
		/**
		* The sort key of one window: start-less (only-end = 立即开启) leads, then
		* windows ascend by start; a tie on start ascends by end, an open end
		* (stays on) last. The list order is derived — never a stored order.
		*/
		function windowSortKeyOf(window) {
			return {
				start: window.startAt ?? 0,
				end: window.endAt ?? Number.MAX_SAFE_INTEGER
			};
		}
		/** Whether a window is structurally empty (no start and no end). */
		function isEmptyWindow(window) {
			return window.startAt === void 0 && window.endAt === void 0;
		}
		/**
		* The existing window a candidate duplicates (same normalized bounds), or
		* undefined. The single write point rejects exact duplicates — an identical
		* window adds no schedule and would only confuse the list; the cross-midnight
		* normalization is applied to both sides so "22:00 → 02:00" and its re-entry
		* match one another.
		*/
		function duplicateWindowOf(windows, candidate) {
			const normalized = normalizeWindow(candidate);
			return windows.find((window) => {
				const current = normalizeWindow(window);
				return current.startAt === normalized.startAt && current.endAt === normalized.endAt;
			});
		}
		/**
		* The window covering `now` (preferring the one ending latest; undefined =
		* off): a window covers when its start (if any) is not in the future and its
		* end (if any) is not in the past. A start-less window is on since minus
		* infinity (from now until its end); an end-less window extends to infinity
		* (on from its start).
		*/
		function coveringWindow(state, now) {
			let best;
			for (const window of state.schedule) {
				if (window.startAt !== void 0 && window.startAt > now) continue;
				if (window.endAt !== void 0 && window.endAt <= now) continue;
				if (best === void 0 || (window.endAt ?? Infinity) > (best.endAt ?? Infinity)) best = window;
			}
			return best;
		}
		/**
		* Apply a manual toggle: flips `enabled` and records the MANUAL intent — the
		* window list is never written, so clicking 开启/关闭 cannot grow the
		* schedule. Manual ranks above the schedule between boundary events; the next
		* boundary takes over again (tickCruise clears `manual`).
		*/
		function applyManualToggle(state, on) {
			return state.enabled === on ? state : {
				...state,
				enabled: on,
				manual: on
			};
		}
		/**
		* One heartbeat tick (the scheduler calls this every minute):
		* - a window whose start instant falls within the last tick flips the cruise
		*   ON (manual cleared — the appointment takes over); its end instant flips
		*   it OFF likewise; ends run after starts within the same tick.
		* - boundaries older than one tick are missed and skipped ("错过即跳过"),
		*   which also covers stale windows persisted before a restart.
		* - fully-past windows are pruned: an explicit endAt <= now, or an end-less
		*   window whose startAt has passed (its one-shot ON already fired).
		* With no boundary event the state stays exactly as it is (manual wins
		* between appointments).
		*/
		function tickCruise(state, now) {
			let enabled = state.enabled;
			let manual = state.manual;
			const schedule = [];
			for (const window of state.schedule) {
				const startFired = window.startAt !== void 0 && window.startAt > now - 6e4 && window.startAt <= now;
				const endFired = window.endAt !== void 0 && window.endAt > now - 6e4 && window.endAt <= now;
				if (startFired) {
					enabled = true;
					manual = void 0;
				}
				if (endFired) {
					enabled = false;
					manual = void 0;
				}
				if (!(window.endAt !== void 0 ? window.endAt <= now : window.startAt !== void 0 && window.startAt <= now)) schedule.push(window);
			}
			return schedule.length === state.schedule.length && enabled === state.enabled && manual === state.manual ? state : {
				...state,
				enabled,
				schedule,
				manual
			};
		}
		/**
		* Replace the window list (the editor's add/remove path): normalized
		* (cross-midnight) + sorted, empties dropped. v4 semantics — editing the
		* PLAN never flips the switch; the windows only flip it at their own
		* instants. The single exception: an ADDED start-less window (只填结束) has
		* its start instant AT CREATION — adding one fires the start boundary now
		* (cruise turns on, manual clears), because "立即开启、到点关" is exactly
		* what that window says. Removing windows leaves the switch untouched, so
		* manual on/off after clearing the plan always matches up.
		*/
		function setCruiseSchedule(state, windows, now) {
			const schedule = sortWindows(windows.map(normalizeWindow).filter((window) => !isEmptyWindow(window)));
			if (sameWindowList(schedule, state.schedule)) return state;
			const addedStartless = schedule.some((window) => window.startAt === void 0 && (window.endAt ?? 0) > now) && !state.schedule.some((window) => window.startAt === void 0);
			const enabled = addedStartless ? true : state.enabled;
			const manual = addedStartless ? void 0 : state.manual;
			if (enabled === state.enabled && manual === state.manual) return {
				...state,
				schedule
			};
			return {
				...state,
				schedule,
				enabled,
				manual
			};
		}
		/** Whether two window lists are identical (normalized bounds, any order). */
		function sameWindowList(a, b) {
			if (a.length !== b.length) return false;
			return [...a].every((window) => b.some((other) => other.startAt === window.startAt && other.endAt === window.endAt));
		}
		function cruiseStatusLineOf(state, now) {
			const covering = coveringWindow(state, now);
			if (state.enabled) {
				if (covering !== void 0) return {
					kind: "window-on",
					...covering.endAt !== void 0 ? { endAt: covering.endAt } : {}
				};
				return { kind: "manual-on" };
			}
			if (covering !== void 0) return {
				kind: "manual-off",
				...covering.endAt !== void 0 ? { endAt: covering.endAt } : {}
			};
			const nextStart = sortWindows(state.schedule.filter((window) => (window.startAt ?? 0) > now))[0];
			return nextStart?.startAt !== void 0 ? {
				kind: "scheduled-off",
				startAt: nextStart.startAt
			} : { kind: "off" };
		}
		/** Sort windows: 立即开启 (start-less) first, then by start, then by end. */
		function sortWindows(windows) {
			return [...windows].sort((a, b) => {
				const keyA = windowSortKeyOf(a);
				const keyB = windowSortKeyOf(b);
				return keyA.start - keyB.start || keyA.end - keyB.end;
			});
		}
		//#endregion
		//#region src/core/session-activity.ts
		/**
		* THE newest native user message in a transcript tail — the line the user
		* typed in the native chat that started the observed turn. The LATEST user
		* message is the truth and never falls back to an older one: a picture-only
		* message reports `{ text: undefined, hasImage: true }` so the thread shows a
		* 图片消息 placeholder instead of stale text from an earlier message.
		* undefined when the tail carries no user message at all.
		*/
		function latestUserMessage(events) {
			if (!Array.isArray(events)) return void 0;
			for (let index = events.length - 1; index >= 0; index -= 1) {
				const turn = turnOfMessageEvent(events[index]);
				if (turn !== void 0) return turn;
			}
		}
		/**
		* Parse ONE native event into the turn facts the board records: a
		* `user/message` from the user source (the legacy live stream's
		* `session/event` payload carries exactly this shape). undefined for
		* anything else — the caller ignores assistant chatter, tool events and
		* system frames.
		*/
		function nativeTurnOf(event) {
			return turnOfMessageEvent(event);
		}
		/** The one reader of the user-message wire shape (transcript tail + live frame). */
		function turnOfMessageEvent(event) {
			const entry = event;
			if (typeof entry !== "object" || entry === null || entry.type !== "user/message") return void 0;
			const data = entry.data;
			if (typeof data !== "object" || data === null) return void 0;
			if (data.source?.kind !== "user") return void 0;
			const text = contentTextOf(data.content);
			return {
				...text !== "" ? { text } : {},
				hasImage: hasImageBlock(data.content),
				...typeof entry.seq === "number" && Number.isFinite(entry.seq) ? { anchor: entry.seq } : {}
			};
		}
		/**
		* Join a native message's text blocks (each trimmed); returns '' when the
		* content carries no text. THE shared block-joiner for native content — the
		* activity reader and the review transcript read the same wire shape, so the
		* join grammar lives here, not in two private copies.
		*/
		function contentTextOf(content) {
			if (!Array.isArray(content)) return "";
			const parts = [];
			for (const block of content) {
				if (typeof block !== "object" || block === null) continue;
				const entry = block;
				if (entry.type === "text" && typeof entry.text === "string") {
					const text = entry.text.trim();
					if (text !== "") parts.push(text);
				}
			}
			return parts.join("\n");
		}
		/** Whether a message's blocks carry an image block (the "what was said" of a
		*  picture-only message is the picture itself). */
		function hasImageBlock(content) {
			if (!Array.isArray(content)) return false;
			for (const block of content) {
				if (typeof block !== "object" || block === null) continue;
				if (block.type === "image") return true;
			}
			return false;
		}
		/**
		* Scan all candidates for out-of-band turns — the STATE rule (see the module
		* doc): a related session running RIGHT NOW whose current run period is not
		* consumed yet fires one external round. Board-owned turns (the direct-send
		* grace, a direct round of this run) consume the period WITHOUT firing —
		* the turn is already in the ledger, by identity, not by coverage. A lane
		* veto (`hasOpenRoundOn`: another round is holding the lane) is COVERAGE,
		* not identity: it must NOT consume — the veto can lift while the session
		* is still running (a queued comment settles, a stale round clears), and a
		* consumed-but-unfired period never re-arms until idle. Consuming on a
		* transient veto is the "card never lights" machine: the one edge that
		* could have fired is eaten, and a long native turn offers no second edge.
		* (Turns that finished before the pass are covered by the controller's
		* wake-evidence pass, not here.)
		*/
		function detectExternalTurns(candidates, book, byId) {
			const found = [];
			for (const { taskId, candidate } of candidates) for (const session of candidate.sessions) {
				const current = byId[session.sessionId]?.running ?? false;
				book.running.set(session.sessionId, current);
				if (!current) {
					book.recorded.delete(session.sessionId);
					continue;
				}
				if (book.recorded.has(session.sessionId)) continue;
				if (candidate.inBoardTurnOn(session.sessionId)) {
					book.recorded.add(session.sessionId);
					continue;
				}
				if (candidate.hasOpenRoundOn(session.sessionId)) continue;
				book.recorded.add(session.sessionId);
				found.push({
					taskId,
					sessionId: session.sessionId,
					refine: session.refine
				});
			}
			return found;
		}
		/** Whether a grace deadline (epoch ms) is still in the future. */
		function withinGrace(graceUntil, now) {
			return graceUntil !== void 0 && graceUntil > now;
		}
		/** How long after a board direct-send a running flip is NOT a new external turn. */
		const DIRECT_GRACE_MS = 6e4;
		//#endregion
		//#region src/core/task-live.ts
		/**
		* THE related-session set of a task (de-duplicated, stable order — refine
		* first, then binds, then execution rounds, then injected linked ids; the
		* same order every consumer has always read):
		* - the task's refine session,
		* - every bound session source (session binds; a workspace bind contributes
		*   through the linked ids below),
		* - every session an execution round ran in,
		* - every live linked session id the caller derived from the native
		*   workspace snapshots (`linkedSessionIdsOf`) — bound workspaces surface
		*   their CURRENT members, so a workspace member counts without ever having
		*   carried a board round.
		*
		* `removedSessions` is the authoritative NOT-related gate and is subtracted
		* from EVERY source here — the same set the display rows already filter on, so
		* a session the user deleted from the card (hidden-tray 删除) can never drive
		* the card's live state or an external round, even while its session bind
		* lingers (the bind survives when a workspace bind is also present). The
		* corollary the add-session picker relies on: a removed session leaves this
		* set, so it becomes re-offerable again (删除 = 可再拖回/再选回).
		* @param task - the task owning the sessions.
		* @param linkedSessionIds - the task's live linked-session ids (the
		*   controller derives them from the workspaces face; undefined = skip).
		*/
		function relatedSessionIdsOf(task, linkedSessionIds) {
			const removed = new Set(task.removedSessions ?? []);
			const seen = /* @__PURE__ */ new Set();
			const out = [];
			const push = (sessionId, refine) => {
				if (sessionId === void 0 || sessionId === "" || seen.has(sessionId) || removed.has(sessionId)) return;
				seen.add(sessionId);
				out.push({
					sessionId,
					refine
				});
			};
			push(task.refineSessionId, true);
			for (const bind of taskBindsOf(task)) if (bind.kind === "session") push(bind.sessionId, false);
			for (const round of task.executions) push(round.sessionId, false);
			for (const sessionId of linkedSessionIds ?? []) push(sessionId, false);
			return out;
		}
		/**
		* The one live-state derivation:
		* - waiting — any related session is pending on the user (approval /
		*   plan-review / question); the human's turn outranks everything;
		* - running — any related session reports `running` (the agent is working);
		* - idle — otherwise.
		* Sources are injected callbacks so the module stays framework-free and
		* unit-testable; the controller wires the native list snapshot.
		*/
		function taskLiveStateOf(task, isRunningOf, waitingOf, linkedSessionIds) {
			const sessions = relatedSessionIdsOf(task, linkedSessionIds);
			for (const { sessionId } of sessions) {
				const waiting = waitingOf(sessionId);
				if (waiting !== void 0 && waiting !== null) return "waiting";
			}
			for (const { sessionId } of sessions) if (isRunningOf(sessionId)) return "running";
			return "idle";
		}
		/**
		* Whether a round is "direct-like": a direct steer round is settled at birth
		* and has NO host turn/end settle event — its completion is only visible as
		* the native session flipping back to idle, so the controller's fallback has
		* to judge it. Board execution rounds, injected comment rounds, refinement
		* rounds and externally-observed rounds all have their own event paths and
		* are never fallback material.
		*/
		function isDirectLike(round) {
			return round !== void 0 && round.direct === true && round.endedAt !== void 0;
		}
		/** The fallback column for a finished direct-like round: a steer that ran to
		*  completion lands in 待审核 (a human gate, exactly like a successful run). */
		const DIRECT_FALLBACK_STATUS = "review";
		/**
		* The task's newest direct-like round, or undefined. A direct round is
		* settled at birth, so it is never "the last row" for long — under
		* per-session lanes any later append (a saved comment on another conversation,
		* an observed native turn) slips past a `latestExecutionOf` check and the
		* steer's completion would never be seen. Searching from the end keeps the
		* judgment about ONE round (the newest steer) while making its position in the
		* array irrelevant.
		*/
		function newestDirectLike(task) {
			for (let index = task.executions.length - 1; index >= 0; index -= 1) {
				const round = task.executions[index];
				if (round !== void 0 && isDirectLike(round)) return round;
			}
		}
		//#endregion
		//#region src/core/run-presets.ts
		/** Storage key for the user's run-config preset overrides. */
		const RUN_PRESETS_STORAGE_KEY = "dsh.taskBoard.runPresets.v1";
		/** The built-in preset (empty config, never persisted, never deletable). */
		const DEPLOY_DEFAULT_PRESET = {
			id: "deploy-default",
			name: "部署默认",
			config: {}
		};
		/** The six run-config keys a preset may carry (unknown keys are stripped). */
		const CONFIG_KEYS = [
			"workspaceId",
			"provider",
			"model",
			"reasoningEffort",
			"agentPreset",
			"permission"
		];
		/** Structural row check: non-empty id/name strings + a clean config object. */
		function isPresetShape$1(value) {
			if (typeof value !== "object" || value === null) return false;
			const row = value;
			if (typeof row.id !== "string" || row.id === "") return false;
			if (typeof row.name !== "string" || row.name === "") return false;
			if (row.id === "deploy-default") return false;
			return typeof row.config === "object" && row.config !== null;
		}
		/** Keep only the known config keys with string values. */
		function cleanConfig(value) {
			if (typeof value !== "object" || value === null) return {};
			const source = value;
			const cleaned = {};
			for (const key of CONFIG_KEYS) {
				const entry = source[key];
				if (typeof entry === "string") cleaned[key] = entry;
			}
			return cleaned;
		}
		/** Normalize a raw row list: valid custom presets only, duplicates dropped. */
		function normalizeRunPresets(value) {
			if (!Array.isArray(value)) return [];
			const seen = /* @__PURE__ */ new Set();
			const rows = [];
			for (const entry of value) {
				if (!isPresetShape$1(entry)) continue;
				if (seen.has(entry.id)) continue;
				seen.add(entry.id);
				rows.push({
					id: entry.id,
					name: entry.name,
					config: cleanConfig(entry.config)
				});
			}
			return rows;
		}
		/** Normalize a whole persisted document (corrupt → built-in only). */
		function normalizeRunPresetDocument(value) {
			if (typeof value !== "object" || value === null) return { presets: [] };
			const row = value;
			const presets = normalizeRunPresets(row.presets);
			const defaultId = typeof row.defaultId === "string" && row.defaultId !== "" ? row.defaultId : void 0;
			return {
				presets,
				...defaultId !== void 0 ? { defaultId } : {}
			};
		}
		/** The merged list a UI shows: 部署默认 first, then the custom presets. */
		function mergedRunPresets(doc) {
			return [DEPLOY_DEFAULT_PRESET, ...doc.presets];
		}
		/** Find a preset by id (built-in or custom); undefined when it does not exist. */
		function findRunPreset(doc, id) {
			return mergedRunPresets(doc).find((preset) => preset.id === id);
		}
		/** THE default resolution: a valid defaultId (that exists) wins; anything
		*  else — never set, dangling, deleted — falls back to 部署默认. */
		function defaultRunPresetOf(doc) {
			return (doc.defaultId !== void 0 ? findRunPreset(doc, doc.defaultId) : void 0) ?? DEPLOY_DEFAULT_PRESET;
		}
		/**
		* The localStorage-backed store. Reads/writes degrade exactly like the
		* schedule-preset store: a throwing or corrupt read yields the built-in
		* document, a throwing write is skipped (persistence loss, never a crash).
		*/
		var LocalStorageRunPresetStore = class {
			storage;
			key;
			constructor(storage = localStorage, key = RUN_PRESETS_STORAGE_KEY) {
				this.storage = storage;
				this.key = key;
			}
			load() {
				try {
					const raw = this.storage.getItem(this.key);
					if (raw === null) return { presets: [] };
					return normalizeRunPresetDocument(JSON.parse(raw));
				} catch {
					return { presets: [] };
				}
			}
			save(doc) {
				try {
					this.storage.setItem(this.key, JSON.stringify(doc));
				} catch {}
			}
			clear() {
				try {
					this.storage.removeItem(this.key);
				} catch {}
			}
		};
		//#endregion
		//#region src/core/store.ts
		/**
		* Task persistence: a small storage seam with a localStorage backend.
		*
		* The task-board client plugin runs in the browser, and dsh exposes no
		* browser-writable file channel, so tasks persist in the browser's
		* localStorage under a versioned key — the same persistence mechanism dsh's
		* own client snapshot stores use (`createSnapshotStore` persist). Data
		* survives page refreshes and dsh restarts (same origin), and survives
		* plugin uninstall (the key is simply left in place).
		*
		* The seam keeps the backend swappable (e.g. an IndexedDB or a host-file
		* channel later); tests run against the in-memory backend and a jsdom
		* localStorage backend.
		*/
		/** Storage key for the task ledger document. */
		const DEFAULT_STORAGE_KEY = "dsh.taskBoard.v1";
		/**
		* Structural row check with the status left unvalidated (see {@link parseLedger}).
		* The `schedule` field is deliberately NOT checked here: a malformed schedule
		* never drops the task row — {@link normalizeSchedule} repairs or drops the
		* schedule alone.
		*/
		function isTaskRecordShape(value) {
			if (typeof value !== "object" || value === null) return false;
			const record = value;
			if (typeof record.id !== "string" || record.id === "") return false;
			if (typeof record.title !== "string") return false;
			if (typeof record.description !== "string") return false;
			if (typeof record.prompt !== "string") return false;
			if (typeof record.createdAt !== "number") return false;
			if (typeof record.updatedAt !== "number") return false;
			if (!Array.isArray(record.executions)) return false;
			for (const execution of record.executions) {
				if (typeof execution !== "object" || execution === null) return false;
				const entry = execution;
				if (typeof entry.id !== "string") return false;
				if (entry.sessionId !== void 0 && typeof entry.sessionId !== "string") return false;
				if (typeof entry.startedAt !== "number") return false;
				if (entry.endedAt !== void 0 && typeof entry.endedAt !== "number") return false;
				if (entry.result !== void 0 && entry.result !== "succeeded" && entry.result !== "failed" && entry.result !== "cancelled") return false;
				if (entry.error !== void 0 && typeof entry.error !== "string") return false;
			}
			return true;
		}
		/** Normalize an unknown persisted status back into the closed status union. */
		function normalizeStatus(status) {
			if (isTaskStatus(status)) return status;
			if (status === "failed") return "review";
			return "todo";
		}
		/**
		* Repair a persisted schedule rule: drop rules without a usable cron string
		* (cron mode), coerce booleans/numbers, normalize the mode (legacy rules
		* default to 'cron'), and leave `nextRunAt`/`lastTriggeredAt` undefined
		* when missing (a fresh recompute or the next tick fixes them).
		*/
		function normalizeSchedule(schedule) {
			if (typeof schedule !== "object" || schedule === null) return void 0;
			const rule = schedule;
			const mode = isScheduleMode(rule.mode) ? rule.mode : "cron";
			if (typeof rule.cron !== "string") {
				if (mode !== "chain") return void 0;
			} else if (mode === "cron" && (rule.cron.trim() === "" || !isValidCron(rule.cron))) return;
			const maxRuns = rule.maxRuns;
			const runCount = rule.runCount;
			const missedToleranceMs = rule.missedToleranceMs;
			return {
				enabled: rule.enabled === true,
				mode,
				cron: typeof rule.cron === "string" ? rule.cron : "",
				nextRunAt: typeof rule.nextRunAt === "number" ? rule.nextRunAt : void 0,
				lastTriggeredAt: typeof rule.lastTriggeredAt === "number" ? rule.lastTriggeredAt : void 0,
				maxRuns: typeof maxRuns === "number" && Number.isInteger(maxRuns) && maxRuns > 0 ? maxRuns : void 0,
				runCount: typeof runCount === "number" && Number.isInteger(runCount) && runCount >= 0 ? runCount : 0,
				missedToleranceMs: typeof missedToleranceMs === "number" && Number.isFinite(missedToleranceMs) && missedToleranceMs > 0 ? Math.floor(missedToleranceMs) : void 0,
				primed: true
			};
		}
		/** Parse + validate a persisted ledger document; invalid rows are dropped. */
		function parseLedger(raw) {
			if (raw === null) return [];
			let parsed;
			try {
				parsed = JSON.parse(raw);
			} catch (error) {
				console.error("[dsh-task-board] persisted task ledger is not valid JSON; starting empty", error);
				return [];
			}
			if (!Array.isArray(parsed)) {
				console.error("[dsh-task-board] persisted task ledger is not an array; starting empty");
				return [];
			}
			const tasks = [];
			for (const row of parsed) {
				if (!isTaskRecordShape(row)) {
					console.warn("[dsh-task-board] dropping invalid task row from persisted ledger", row);
					continue;
				}
				const task = {
					...row,
					status: normalizeStatus(row.status)
				};
				task.schedule = normalizeSchedule(row.schedule);
				const rawOrder = row.order;
				task.order = typeof rawOrder === "number" && Number.isFinite(rawOrder) ? rawOrder : tasks.length;
				const rawViewed = row.viewedAt;
				task.viewedAt = typeof rawViewed === "number" ? rawViewed : task.executions.reduce((latest, round) => Math.max(latest, round.endedAt ?? round.startedAt), 0);
				task.executions = task.executions.map((round) => {
					if (round.viewedAt !== void 0) return round;
					return {
						...round,
						viewedAt: round.endedAt ?? round.startedAt
					};
				});
				const rawBind = row.bind;
				if (rawBind?.kind === "session" && typeof rawBind.sessionId === "string") task.bind = {
					kind: "session",
					sessionId: rawBind.sessionId
				};
				else if (rawBind?.kind === "workspace" && typeof rawBind.workspaceId === "string") task.bind = {
					kind: "workspace",
					workspaceId: rawBind.workspaceId
				};
				else delete task.bind;
				const rawBinds = row.binds;
				if (Array.isArray(rawBinds)) {
					const binds = rawBinds.map((entry) => {
						if (typeof entry !== "object" || entry === null) return void 0;
						const source = entry;
						if (source.kind === "session" && typeof source.sessionId === "string") return {
							kind: "session",
							sessionId: source.sessionId
						};
						if (source.kind === "workspace" && typeof source.workspaceId === "string") return {
							kind: "workspace",
							workspaceId: source.workspaceId
						};
					}).filter((entry) => entry !== void 0);
					if (binds.length > 0) task.binds = binds;
					else delete task.binds;
				} else delete task.binds;
				const rawHidden = row.hidden;
				const cleanIdArray = (value) => {
					if (!Array.isArray(value)) return void 0;
					const ids = value.filter((item) => typeof item === "string");
					return ids.length > 0 ? ids : void 0;
				};
				if (rawHidden !== null && typeof rawHidden === "object") {
					const executions = cleanIdArray(rawHidden.executions);
					const sessions = cleanIdArray(rawHidden.sessions);
					if (executions !== void 0 || sessions !== void 0) task.hidden = {
						...executions !== void 0 ? { executions } : {},
						...sessions !== void 0 ? { sessions } : {}
					};
				}
				if (task.hidden === void 0) delete task.hidden;
				const removedSessions = cleanIdArray(row.removedSessions);
				if (removedSessions !== void 0) task.removedSessions = removedSessions;
				else delete task.removedSessions;
				const sessionsOrder = cleanIdArray(row.sessionsOrder);
				if (sessionsOrder !== void 0) task.sessionsOrder = sessionsOrder;
				else delete task.sessionsOrder;
				delete task.tags;
				delete task.dueAt;
				delete task.priority;
				delete task.labels;
				const rawColor = row.color;
				if (typeof rawColor === "string" && rawColor !== "") task.color = rawColor;
				else delete task.color;
				const rawImages = row.promptImages;
				const images = normalizePromptImages(rawImages);
				if (images !== void 0) task.promptImages = images;
				else delete task.promptImages;
				const rawFiles = row.promptFiles;
				const files = normalizePromptFiles(rawFiles);
				if (files !== void 0) task.promptFiles = files;
				else delete task.promptFiles;
				const rawHistory = row.statusHistory;
				const birthAt = typeof task.createdAt === "number" && Number.isFinite(task.createdAt) && task.createdAt > 0 ? task.createdAt : task.updatedAt;
				if (Array.isArray(rawHistory)) {
					const history = rawHistory.filter((entry) => typeof entry === "object" && entry !== null && isTaskStatus(entry.status) && typeof entry.at === "number" && Number.isFinite(entry.at) && entry.at > 0).map((entry) => ({
						status: entry.status,
						at: entry.at
					})).sort((a, b) => a.at - b.at);
					if (history.length === 0) history.push({
						status: task.status,
						at: birthAt
					});
					const last = history[history.length - 1];
					if (last !== void 0 && last.status !== task.status) history.push({
						status: task.status,
						at: task.updatedAt
					});
					task.statusHistory = history;
				} else task.statusHistory = [{
					status: task.status,
					at: birthAt
				}];
				const rules = normalizeSessionRules(row.rules);
				if (rules !== void 0) task.rules = rules;
				else delete task.rules;
				tasks.push(task);
			}
			return tasks;
		}
		/** localStorage-backed store (the browser backend). */
		var LocalStorageTaskStore = class {
			key;
			storage;
			/**
			* @param key - storage key for the ledger document.
			* @param storage - storage backend (defaults to the global localStorage; tests inject fakes).
			*/
			constructor(key = DEFAULT_STORAGE_KEY, storage = globalThis.localStorage) {
				this.key = key;
				this.storage = storage;
			}
			load() {
				if (this.storage === void 0) return [];
				try {
					return parseLedger(this.storage.getItem(this.key));
				} catch (error) {
					console.error("[dsh-task-board] task ledger read failed; starting empty", error);
					return [];
				}
			}
			save(tasks) {
				if (this.storage === void 0) return;
				try {
					this.storage.setItem(this.key, JSON.stringify(tasks));
				} catch (error) {
					console.error("[dsh-task-board] task ledger write failed (persistence skipped)", error);
				}
			}
			clear() {
				if (this.storage === void 0) return;
				try {
					this.storage.removeItem(this.key);
				} catch (error) {
					console.error("[dsh-task-board] task ledger clear failed", error);
				}
			}
		};
		//#endregion
		//#region src/core/presets.ts
		/** Storage key for the user's preset overrides. */
		const DEFAULT_PRESET_STORAGE_KEY = "dsh.taskBoard.presets.v1";
		/** The built-in preset list (never persisted; always resettable to). */
		const DEFAULT_PRESETS = [
			{
				id: "every-5min",
				label: "每 5 分钟",
				cron: "*/5 * * * *"
			},
			{
				id: "every-10min",
				label: "每 10 分钟",
				cron: "*/10 * * * *"
			},
			{
				id: "every-15min",
				label: "每 15 分钟",
				cron: "*/15 * * * *"
			},
			{
				id: "every-30min",
				label: "每 30 分钟",
				cron: "*/30 * * * *"
			},
			{
				id: "hourly",
				label: "每小时",
				cron: "0 * * * *"
			},
			{
				id: "every-2hours",
				label: "每 2 小时",
				cron: "0 */2 * * *"
			},
			{
				id: "daily-00",
				label: "每天 00:00",
				cron: "0 0 * * *"
			},
			{
				id: "daily-08",
				label: "每天 08:00",
				cron: "0 8 * * *"
			},
			{
				id: "daily-09",
				label: "每天 09:00",
				cron: "0 9 * * *"
			},
			{
				id: "daily-12",
				label: "每天 12:00",
				cron: "0 12 * * *"
			},
			{
				id: "daily-14",
				label: "每天 14:00",
				cron: "0 14 * * *"
			},
			{
				id: "daily-18",
				label: "每天 18:00",
				cron: "0 18 * * *"
			},
			{
				id: "daily-20",
				label: "每天 20:00",
				cron: "0 20 * * *"
			},
			{
				id: "daily-22",
				label: "每天 22:00",
				cron: "0 22 * * *"
			},
			{
				id: "weekly-mon9",
				label: "每周一 09:00",
				cron: "0 9 * * 1"
			},
			{
				id: "weekly-workdays9",
				label: "工作日 09:00",
				cron: "0 9 * * 1-5"
			},
			{
				id: "weekly-sun0",
				label: "每周日 00:00",
				cron: "0 0 * * 0"
			},
			{
				id: "monthly-1",
				label: "每月 1 号 09:00",
				cron: "0 9 1 * *"
			},
			{
				id: "monthly-15",
				label: "每月 15 号 09:00",
				cron: "0 9 15 * *"
			}
		];
		/** Structural row check: an override must carry id/label/cron strings. */
		function isPresetShape(value) {
			if (typeof value !== "object" || value === null) return false;
			const row = value;
			return typeof row.id === "string" && row.id !== "" && typeof row.label === "string" && typeof row.cron === "string" && row.cron !== "";
		}
		/** Parse + validate the persisted override document; invalid rows are dropped. */
		function parsePresets(raw) {
			if (raw === null) return [];
			let parsed;
			try {
				parsed = JSON.parse(raw);
			} catch (error) {
				console.error("[dsh-task-board] persisted schedule presets are not valid JSON; starting with defaults", error);
				return [];
			}
			if (!Array.isArray(parsed)) {
				console.error("[dsh-task-board] persisted schedule presets are not an array; starting with defaults");
				return [];
			}
			return parsed.filter(isPresetShape);
		}
		/** Merge the built-ins with user overrides: custom ids replace the built-in of the same id. */
		function mergePresets(defaults, custom) {
			const overridden = new Set(custom.map((preset) => preset.id));
			return [...defaults.filter((preset) => !overridden.has(preset.id)), ...custom];
		}
		/** localStorage-backed preset store (the browser backend). */
		var LocalStoragePresetStore = class {
			key;
			storage;
			/**
			* @param key - storage key for the override document.
			* @param storage - storage backend (defaults to the global localStorage; tests inject fakes).
			*/
			constructor(key = DEFAULT_PRESET_STORAGE_KEY, storage = globalThis.localStorage) {
				this.key = key;
				this.storage = storage;
			}
			load() {
				if (this.storage === void 0) return [];
				try {
					return parsePresets(this.storage.getItem(this.key));
				} catch (error) {
					console.error("[dsh-task-board] preset overrides read failed; starting with defaults", error);
					return [];
				}
			}
			save(presets) {
				if (this.storage === void 0) return;
				try {
					this.storage.setItem(this.key, JSON.stringify(presets));
				} catch (error) {
					console.error("[dsh-task-board] preset overrides write failed (persistence skipped)", error);
				}
			}
			clear() {
				if (this.storage === void 0) return;
				try {
					this.storage.removeItem(this.key);
				} catch (error) {
					console.error("[dsh-task-board] preset overrides clear failed", error);
				}
			}
		};
		//#endregion
		//#region src/core/board-doc.ts
		/**
		* The board document: the ONE host-owned truth every browser replica syncs
		* against — the task ledger plus the board's shared state sections (cruise,
		* schedule presets, run presets) — and the merge grammar that makes
		* concurrent multi-device edits converge deterministically.
		*
		* Sync model (host = authority, browsers = optimistic replicas):
		* - A client commits its whole local view: the full tasks array, the section
		*   values, and the deletions it observed since its last synced baseline
		*   (each delete carries the `updatedAt` the client saw, so the host can tell
		*   a delete from a stale copy).
		* - The host resolves record by record — last-writer-wins on the record's own
		*   `updatedAt`, tombstones suppressing deletes that a stale replica would
		*   otherwise resurrect — and section by section (LWW on the section write
		*   stamp), then returns the authoritative document. Every replica converges
		*   on that response, so drift is impossible without compare-and-swap.
		* - `revision` is the host's monotonic change counter (the SSE resync signal
		*   and the "did anything actually move" test), never a write gate.
		*
		* Clock-skew rule: record conflicts are resolved by AUTHORSHIP, not by
		* clocks — a commit declares `changed` (the ids its own edits moved against
		* the last synced baseline), the host accepts those unconditionally (its
		* serial commit order decides, so a phone whose clock runs minutes behind
		* still wins with its newest gesture), and every record the replica does NOT
		* claim merges by `updatedAt` LWW (an untouched stale copy can never clobber
		* a newer edit). Tombstones are stamped one millisecond above the newest
		* `updatedAt` the host ever saw for that id, so a delete always beats the
		* stale copies other replicas still hold, while a genuinely newer edit (a
		* concurrent revive) still wins. `stamps` records the host wall time each
		* row was last accepted (diagnostics / future pruning).
		*
		* Framework-free pure logic: host and client share this one grammar; tests
		* drive it directly.
		*/
		/** The default cruise section value of a never-written board. */
		const DEFAULT_CRUISE_VALUE = {
			enabled: false,
			limit: 5,
			schedule: []
		};
		/** Clamp one concurrency budget to the shared bounds (THE one clamp: writes
		*  floor + clamp through this, reads normalize through this — never two
		*  grammars). NaN falls to MIN (self-guarding:
		*  no caller memory required — infinities clamp naturally to their end). */
		function clampCruiseLimit(value) {
			if (Number.isNaN(value)) return 1;
			return Math.min(20, Math.max(1, Math.floor(value)));
		}
		/** A fresh empty document (host first boot; revision 0 marks "never committed"). */
		function emptyBoardDoc(now) {
			return {
				revision: 0,
				tasks: [],
				cruise: {
					value: DEFAULT_CRUISE_VALUE,
					at: now
				},
				schedulePresets: {
					value: [],
					at: now
				},
				runPresets: {
					value: { presets: [] },
					at: now
				},
				tombstones: {},
				stamps: {},
				bornAt: now
			};
		}
		/** Normalize an unknown cruise value (the controller constructor's grammar,
		*  shared so host and client judge persisted cruise state identically). */
		function normalizeCruiseValue(value) {
			if (typeof value !== "object" || value === null) return { ...DEFAULT_CRUISE_VALUE };
			const row = value;
			const limit = clampCruiseLimit(typeof row.limit === "number" && Number.isFinite(row.limit) ? row.limit : DEFAULT_CRUISE_VALUE.limit);
			return {
				enabled: row.enabled === true,
				...row.manual === true || row.manual === false ? { manual: row.manual } : {},
				limit,
				schedule: Array.isArray(row.schedule) ? sortWindows(row.schedule.filter(isCruiseWindow).map(normalizeWindow)) : []
			};
		}
		/**
		* The AUTHORSHIP set of a commit: ids whose CONTENT moved between the
		* baseline the replica synced and the view it now commits. An untouched
		* copy of a host row (same content, same serialization) is never claimed,
		* so the claim can only vouch for edits this replica genuinely made —
		* reorders, title changes, new tasks all count; a stale copy the replica
		* never touched does not.
		*
		* READ STATE IS NOT AUTHORSHIP: `viewedAt` (on the task and on its rounds)
		* is a pure "has the human seen this" bookkeeping field — opening a card is
		* not editing it. It is stripped before the comparison, so a viewedAt-only
		* flip never claims the row. Without this, a viewer whose commit rode a
		* baseline from BEFORE the engine recorded an external round would claim
		* authorship of the whole row and (claims win unconditionally, last-write-
		* wins by arrival) silently delete the engine's round and revert the card's
		* column — the exact "I opened the card and its 进行中 disappeared" machine.
		* Read-state writes still propagate: they simply merge under plain LWW.
		*/
		function changedIdsOf(baseline, next) {
			const before = new Map(baseline.map((task) => [task.id, authorshipKey(task)]));
			const changed = [];
			for (const task of next) {
				const previous = before.get(task.id);
				if (previous === void 0 || previous !== authorshipKey(task)) changed.push(task.id);
			}
			return changed;
		}
		/** One row's authorship fingerprint: its JSON minus every read-state field. */
		function authorshipKey(task) {
			const { viewedAt: _taskViewed, ...rest } = task;
			return JSON.stringify({
				...rest,
				executions: task.executions.map(({ viewedAt: _roundViewed, ...round }) => round)
			});
		}
		function boardViewOf(doc) {
			return {
				tasks: doc.tasks,
				cruise: doc.cruise.value,
				schedulePresets: doc.schedulePresets.value,
				runPresets: doc.runPresets.value
			};
		}
		//#endregion
		//#region src/core/colors.ts
		/** Curated preset palette (10 hues, clear on light and dark). Data, not CSS. */
		const PALETTE = [
			"#e5484d",
			"#f76b15",
			"#f5a524",
			"#f2cd35",
			"#30a46c",
			"#12a594",
			"#3e63dd",
			"#6e56cf",
			"#ab4aba",
			"#e93d82"
		];
		/** Set (or clear, with undefined) a task's card accent color. */
		function withTaskColor(task, color) {
			return color === void 0 ? {
				...task,
				..."color" in task ? { color: void 0 } : {}
			} : {
				...task,
				color
			};
		}
		//#endregion
		//#region src/core/session-agents.ts
		/**
		* Applied-preset ledger: which agent preset the board last successfully
		* composed each session from. The host offers a preset SWITCH (`select`)
		* but no read-back — no list field, no models-API field, no projection
		* carries a session's composed preset — so a display that reads "the
		* session's preset" from the host reads a ghost field (always undefined,
		* always "部署默认"). The board, on the other hand, knows exactly what it
		* applied where (every `selectAgentPreset.ok` flows through the execution
		* service): recording that is the only honest source.
		*
		* Device-local localStorage (like drafts): a display hint, not board truth
		* — the shared document is untouched, so no migration and no merge grammar.
		* Read priority stays host-first (`sessionInfo` prefers a served
		* `summary.agentPreset` when a future host serves one); the ledger is the
		* fallback, "部署默认" the last resort. Pure functions + a storage seam, so
		* everything unit-tests in isolation.
		*/
		/** The localStorage key for the applied-preset ledger (never renamed). */
		const SESSION_AGENT_STORAGE_KEY = "dsh.taskBoard.sessionAgents.v1";
		/**
		* Record a successful application (no-op for blank presets; later wins).
		* Failures must never be recorded — the session kept its previous preset.
		*/
		function recordApplied(ledger, sessionId, preset, now) {
			if (preset.trim() === "") return ledger;
			return {
				...ledger,
				[sessionId]: {
					preset,
					at: now
				}
			};
		}
		/** The board-applied preset of a session, or undefined when never applied. */
		function appliedOf(ledger, sessionId) {
			return ledger[sessionId]?.preset;
		}
		/** Store-level read (undefined store = unknown, never a throw). */
		function appliedPresetOf(store, sessionId) {
			if (store === void 0) return void 0;
			return appliedOf(store.load(), sessionId);
		}
		/** Structural check: string presets only (anything else is dropped). */
		function normalizeLedger(raw) {
			if (typeof raw !== "object" || raw === null) return {};
			const out = {};
			for (const [sessionId, entry] of Object.entries(raw)) {
				if (typeof entry !== "object" || entry === null) continue;
				const record = entry;
				if (typeof record.preset !== "string" || record.preset.trim() === "") continue;
				out[sessionId] = {
					preset: record.preset,
					at: typeof record.at === "number" && Number.isFinite(record.at) ? record.at : 0
				};
			}
			return out;
		}
		/** localStorage-backed ledger (device-local, like drafts). */
		var LocalStorageSessionAgentStore = class {
			load() {
				try {
					const raw = globalThis.localStorage?.getItem(SESSION_AGENT_STORAGE_KEY);
					if (raw === void 0 || raw === null) return {};
					return normalizeLedger(JSON.parse(raw));
				} catch {
					return {};
				}
			}
			save(ledger) {
				try {
					globalThis.localStorage?.setItem(SESSION_AGENT_STORAGE_KEY, JSON.stringify(ledger));
				} catch {}
			}
		};
		//#endregion
		//#region src/core/task-templates.ts
		/** The localStorage key for the user's template library (never renamed). */
		const TEMPLATE_STORAGE_KEY = "dsh.taskBoard.templates.v1";
		/**
		* Snapshot a task as a template: content + run configuration come along,
		* instance state never does.
		* Images ride along (the user attached them to the prompt deliberately —
		* dropping them silently would violate the no-silent-drop law);
		* schedule/rules stay behind (a template must never surprise-fire — arming
		* is an explicit act on the stamped card).
		*/
		function templateFromTask(task, id, name) {
			const trimmed = name.trim();
			return {
				id,
				name: trimmed === "" ? task.title.trim() === "" ? "Untitled template" : task.title : trimmed,
				title: task.title,
				description: task.description,
				prompt: task.prompt,
				...task.promptImages !== void 0 && task.promptImages.length > 0 ? { promptImages: task.promptImages.map((image) => ({ ...image })) } : {},
				...task.promptFiles !== void 0 && task.promptFiles.length > 0 ? { promptFiles: task.promptFiles.map((file) => ({ ...file })) } : {},
				...task.workspaceId !== void 0 ? { workspaceId: task.workspaceId } : {},
				...task.provider !== void 0 ? { provider: task.provider } : {},
				...task.model !== void 0 ? { model: task.model } : {},
				...task.reasoningEffort !== void 0 ? { reasoningEffort: task.reasoningEffort } : {},
				...task.agentPreset !== void 0 ? { agentPreset: task.agentPreset } : {},
				...task.permission !== void 0 ? { permission: task.permission } : {},
				...task.color !== void 0 ? { color: task.color } : {}
			};
		}
		/** Stamp a fresh backlog input from a template (the card lands in 待规划). */
		function templateToNewInput(template) {
			return {
				title: template.title,
				description: template.description,
				prompt: template.prompt,
				status: "backlog",
				...template.promptImages !== void 0 && template.promptImages.length > 0 ? { promptImages: template.promptImages.map((image) => ({ ...image })) } : {},
				...template.promptFiles !== void 0 && template.promptFiles.length > 0 ? { promptFiles: template.promptFiles.map((file) => ({ ...file })) } : {},
				...template.workspaceId !== void 0 ? { workspaceId: template.workspaceId } : {},
				...template.provider !== void 0 ? { provider: template.provider } : {},
				...template.model !== void 0 ? { model: template.model } : {},
				...template.reasoningEffort !== void 0 ? { reasoningEffort: template.reasoningEffort } : {},
				...template.agentPreset !== void 0 ? { agentPreset: template.agentPreset } : {},
				...template.permission !== void 0 ? { permission: template.permission } : {},
				...template.color !== void 0 ? { color: template.color } : {}
			};
		}
		/** Structural check: non-empty id/name strings (content may be blank). */
		function isTemplateRow(row) {
			if (typeof row !== "object" || row === null) return false;
			const candidate = row;
			return typeof candidate.id === "string" && candidate.id !== "" && typeof candidate.name === "string" && candidate.name !== "";
		}
		/** Normalize a raw stored list: valid rows only, duplicates dropped. */
		function normalizeTemplates(raw) {
			if (!Array.isArray(raw)) return [];
			const seen = /* @__PURE__ */ new Set();
			const out = [];
			for (const row of raw) {
				if (!isTemplateRow(row) || seen.has(row.id)) continue;
				seen.add(row.id);
				const promptImages = normalizePromptImages(row.promptImages);
				const promptFiles = normalizePromptFiles(row.promptFiles);
				out.push({
					id: row.id,
					name: row.name,
					title: typeof row.title === "string" ? row.title : "",
					description: typeof row.description === "string" ? row.description : "",
					prompt: typeof row.prompt === "string" ? row.prompt : "",
					...promptImages !== void 0 ? { promptImages } : {},
					...promptFiles !== void 0 ? { promptFiles } : {},
					...typeof row.workspaceId === "string" ? { workspaceId: row.workspaceId } : {},
					...typeof row.provider === "string" ? { provider: row.provider } : {},
					...typeof row.model === "string" ? { model: row.model } : {},
					...typeof row.reasoningEffort === "string" ? { reasoningEffort: row.reasoningEffort } : {},
					...typeof row.agentPreset === "string" ? { agentPreset: row.agentPreset } : {},
					...typeof row.permission === "string" ? { permission: row.permission } : {},
					...typeof row.color === "string" && row.color !== "" ? { color: row.color } : {}
				});
			}
			return out;
		}
		/** localStorage-backed template library (device-local, like drafts). */
		var LocalStorageTemplateStore = class {
			load() {
				try {
					const raw = globalThis.localStorage?.getItem(TEMPLATE_STORAGE_KEY);
					if (raw === void 0 || raw === null) return [];
					return normalizeTemplates(JSON.parse(raw));
				} catch {
					return [];
				}
			}
			save(templates) {
				try {
					globalThis.localStorage?.setItem(TEMPLATE_STORAGE_KEY, JSON.stringify([...templates]));
				} catch {}
			}
		};
		//#endregion
		//#region src/core/session-display.ts
		/**
		* Collect every round belonging to an execution's session:
		* - Rounds with the same sessionId (comments injected into this session,
		*   refine rounds using the same refine session).
		* - Rounds whose parentExecutionId matches (comments attributed by parent
		*   rather than session — legacy data compatibility).
		* The execution itself is always included.
		*/
		function sessionRoundsOf(task, execution) {
			const id = execution.id;
			const sid = execution.sessionId;
			return task.executions.filter((round) => round.id === id || sid !== void 0 && round.sessionId === sid && (round.sessionAnchor === void 0 || round.external === true) || round.parentExecutionId === id);
		}
		/**
		* Derive the live state of an execution's session from its rounds.
		* @param task - the task owning the execution.
		* @param execution - the execution whose session we're displaying.
		* @param waitingKind - the interaction kind if the session is waiting on the
		*   user (from the controller's pendingInteractionOf); undefined otherwise.
		* @param nativeRunning - the session's NATIVE running flag (the host list).
		*   TRUE means the agent is working right now — no matter which surface
		*   started the turn (a plain run, a direct steer, a session rule, an
		*   out-of-band native chat). Without it a direct-steer round (settled at
		*   birth) would leave the row dark while the session was genuinely running —
		*   the "插话后卡片/会话行不动" bug. Board open rounds and refine rounds
		*   keep their own semantics below; this only ADDS the native truth.
		*/
		function sessionDisplay(task, execution, waitingKind, nativeRunning = false) {
			const rounds = sessionRoundsOf(task, execution);
			if (rounds.length === 0) return {
				state: "cancelled",
				lastActivity: void 0,
				waitingKind: void 0
			};
			const open = rounds.filter((r) => isOpenRound(r));
			if (open.length > 0) {
				const openRound = open.reduce((latest, r) => r.startedAt > latest.startedAt ? r : latest);
				if (waitingKind !== void 0) return {
					state: "waiting",
					lastActivity: openRound.startedAt,
					waitingKind
				};
				return {
					state: "running",
					lastActivity: openRound.startedAt,
					waitingKind: void 0
				};
			}
			if (waitingKind !== void 0) return {
				state: "waiting",
				lastActivity: rounds[0].startedAt,
				waitingKind
			};
			if (nativeRunning) return {
				state: "running",
				lastActivity: rounds[0].startedAt,
				waitingKind: void 0
			};
			const latest = [...rounds].filter((r) => r.endedAt !== void 0).sort((a, b) => (b.endedAt ?? 0) - (a.endedAt ?? 0))[0];
			if (latest === void 0) return {
				state: "cancelled",
				lastActivity: void 0,
				waitingKind: void 0
			};
			return {
				state: latest.result === "succeeded" ? "succeeded" : latest.result === "failed" ? "failed" : latest.result === "cancelled" ? "cancelled" : "cancelled",
				lastActivity: latest.endedAt,
				waitingKind: void 0
			};
		}
		/**
		* The time range of an execution's session (reflecting all its rounds' activity).
		* - startedAt = earliest round's start.
		* - endedAt = latest settled round's end; undefined if any round is still open.
		* - duration = endedAt - startedAt; undefined if the session is still open.
		*/
		function sessionTimes(task, execution) {
			const rounds = sessionRoundsOf(task, execution);
			if (rounds.length === 0) return {
				startedAt: execution.startedAt,
				endedAt: void 0,
				duration: void 0
			};
			const startedAt = Math.min(...rounds.map((r) => r.startedAt));
			const endedAt = rounds.some((r) => isOpenRound(r)) ? void 0 : Math.max(...rounds.map((r) => r.endedAt ?? 0).filter((t) => t > 0));
			return {
				startedAt,
				endedAt,
				duration: endedAt !== void 0 ? endedAt - startedAt : void 0
			};
		}
		/**
		* Count how many sessions (executions + refine) are waiting on the user.
		* Used by the task card badge to show "N 待处理" when the task has pending
		* interactions across its sessions. ONE row per waiting SESSION (deduped):
		* three executions on the same waiting session wait once, not three times —
		* the same session-keyed law the notification center already uses.
		*/
		function taskPendingCount(task, pendingInteractionOf) {
			const items = [];
			const seen = /* @__PURE__ */ new Set();
			for (const execution of task.executions) {
				if (execution.sessionId === void 0 || seen.has(execution.sessionId)) continue;
				const waitingKind = pendingInteractionOf(execution.sessionId);
				if (waitingKind !== void 0) {
					seen.add(execution.sessionId);
					items.push({
						executionId: execution.id,
						sessionId: execution.sessionId,
						waitingKind
					});
				}
			}
			if (task.refineSessionId !== void 0 && !seen.has(task.refineSessionId)) {
				const waitingKind = pendingInteractionOf(task.refineSessionId);
				if (waitingKind !== void 0) {
					seen.add(task.refineSessionId);
					items.push({
						sessionId: task.refineSessionId,
						waitingKind
					});
				}
			}
			return {
				count: items.length,
				items
			};
		}
		/** The activity instant of a round: when it settled, else its last start. */
		function roundActivity(round) {
			return round.endedAt ?? round.startedAt;
		}
		/**
		* The viewed baseline of an execution row: when the user last opened its
		* review page; absent, the run's own start (every created/normalized run
		* carries a viewedAt, so this only guards test fixtures). A stable anchor —
		* a run starts viewed at its start, its settlement (or a later comment)
		* then lights the unread dot until the review page opens.
		*/
		function executionViewedBaseline(execution) {
			return execution.viewedAt ?? execution.startedAt;
		}
		/**
		* Whether an execution's session has content newer than the last time its
		* review page was opened: any round of the session (the run itself plus its
		* comments) with activity after the baseline. The single source for the
		* execution row's unread dot and the card's "新" badge.
		*/
		function executionUnviewed(task, execution) {
			const baseline = executionViewedBaseline(execution);
			return sessionRoundsOf(task, execution).some((round) => roundActivity(round) > baseline);
		}
		/**
		* The viewed baseline of a task card: when the user last opened the task
		* detail; absent (legacy) it equals the task's newest round activity, so
		* already-seen content stays quiet after an upgrade.
		*/
		function taskViewedBaseline(task) {
			if (task.viewedAt !== void 0) return task.viewedAt;
			let latest = 0;
			for (const round of task.executions) latest = Math.max(latest, roundActivity(round));
			return latest;
		}
		/**
		* Whether the task has any unviewed content: any round — a run settling, a
		* comment being injected or settling, a refine turn — with activity newer
		* than the card's viewed baseline. Drives the card's breathing glow.
		*/
		function taskUnviewed(task) {
			const baseline = taskViewedBaseline(task);
			return task.executions.some((round) => roundActivity(round) > baseline);
		}
		/**
		* How many plain-run executions of the task are unviewed — the "新 N" count
		* on the card. Comment/refine-only unread (no unviewed plain runs) shows a
		* bare "新" instead.
		*/
		function taskUnviewedCount(task) {
			let count = 0;
			for (const execution of task.executions) {
				if (execution.comment !== void 0 || execution.refine === true) continue;
				if (executionUnviewed(task, execution)) count += 1;
			}
			return count;
		}
		//#endregion
		//#region src/core/session-list.ts
		/**
		* The displayed title of one session row: the native title, else the
		* 未命名 placeholder — the ONE unnamed-grammar. A session without a durable
		* title is not nameless data: the host names it automatically from the
		* first real message (deterministic fallback + provider cadence), so the
		* honest display until then is exactly what the native New Session flow
		* would show. Title-less rows therefore read 「未命名」 (never a workspace
		* label, never a raw id dressed up as a name); the workspace still shows
		* in its own slot.
		*/
		const UNTITLED_SESSION_KEY = "detail.sessionUntitled";
		function sessionRowTitleOf(nativeTitle, fallback) {
			return nativeTitle !== void 0 && nativeTitle !== "" ? nativeTitle : fallback;
		}
		/**
		* The activity window a session has ON this task — the earliest round's
		* start, the latest round's end, and the duration between them. The ONE
		* derivation for every session row (a board-run session and a bound
		* session's externally-observed turns read the same records), so the
		* 「开始 / 结束 / 耗时」line of a linked row is never a different grammar
		* from an execution row's. Empty when the session has no rounds.
		*/
		function sessionWindowOf(task, sessionId) {
			if (sessionId === void 0) return {};
			let startedAt;
			let endedAt;
			for (const round of task.executions) {
				if (round.sessionId !== sessionId) continue;
				if (startedAt === void 0 || round.startedAt < startedAt) startedAt = round.startedAt;
				if (round.endedAt !== void 0 && (endedAt === void 0 || round.endedAt > endedAt)) endedAt = round.endedAt;
			}
			if (startedAt === void 0) return {};
			return {
				startedAt,
				...endedAt !== void 0 ? { endedAt } : {},
				...endedAt !== void 0 && endedAt >= startedAt ? { duration: endedAt - startedAt } : {}
			};
		}
		/**
		* The derived per-session hidden set: `hidden.sessions` (session ids) plus
		* `hidden.executions` mapped to their execution's session id. THE single
		* source of truth for "is this session hidden" — hide is defined once in
		* terms of sessions, and the unified list filters by this set alone.
		*/
		function hiddenSessionIdsOf(task) {
			const ids = /* @__PURE__ */ new Set();
			for (const sessionId of task.hidden?.sessions ?? []) ids.add(sessionId);
			for (const executionId of task.hidden?.executions ?? []) {
				const execution = task.executions.find((round) => round.id === executionId);
				if (execution?.sessionId !== void 0) ids.add(execution.sessionId);
			}
			return ids;
		}
		/**
		* Order + de-duplicate a task's sessions into the single visible list:
		* - run candidates: the latest plain run per session (comment rounds share
		*   their parent run's session and never create extra rows; refine rounds are
		*   not sessions of the board executions).
		* - linked candidates: the live linked rows.
		* - de-duplicate by sessionId, run wins over linked (a session the task both
		*   executed and bound reads as the task's own run — it carries the execution
		*   identity, the quiet run number and the unread state).
		* - hidden sessions are dropped; run rows sort by latest activity, then
		*   linked rows in workspace order.
		*/
		function taskSessionsOf(task, ctx) {
			const hidden = hiddenSessionIdsOf(task);
			const removed = task.removedSessions ?? [];
			const isArchived = (sessionId) => ctx.archivedOf?.(sessionId) === true;
			const runBySession = /* @__PURE__ */ new Map();
			for (const execution of plainRunsOf(task)) {
				if (execution.sessionId === void 0) continue;
				const sessionId = execution.sessionId;
				if (removed.includes(sessionId) || isArchived(sessionId)) continue;
				runBySession.set(sessionId, {
					sessionId,
					title: sessionRowTitleOf(ctx.titleOf(sessionId), ctx.untitledLabel ?? task.title),
					executionId: execution.id,
					display: sessionDisplay(task, execution, ctx.pendingInteractionOf(sessionId), ctx.nativeRunningOf?.(sessionId) ?? false),
					updatedAt: sessionTimes(task, execution).endedAt ?? execution.startedAt,
					unviewed: executionUnviewed(task, execution)
				});
			}
			const rows = [];
			for (const row of runBySession.values()) if (!hidden.has(row.sessionId)) rows.push(row);
			rows.sort((a, b) => b.updatedAt - a.updatedAt);
			for (const linked of ctx.linked) {
				if (hidden.has(linked.sessionId) || removed.includes(linked.sessionId) || isArchived(linked.sessionId) || runBySession.has(linked.sessionId)) continue;
				const linkedTitle = linked.title !== linked.sessionId ? linked.title : sessionRowTitleOf(ctx.titleOf(linked.sessionId), ctx.untitledLabel ?? linked.sessionId);
				rows.push({
					sessionId: linked.sessionId,
					title: linkedTitle,
					...linked.workspaceLabel !== void 0 ? { workspaceLabel: linked.workspaceLabel } : {},
					display: {
						state: linked.pendingInteraction !== void 0 ? "waiting" : linked.running ? "running" : linked.completed ? "succeeded" : "cancelled",
						lastActivity: linked.updatedAt,
						waitingKind: linked.pendingInteraction
					},
					updatedAt: linked.updatedAt,
					unviewed: false
				});
			}
			return orderedSessionsOf(task, rows);
		}
		/**
		* The displayed order of the unified list: the user's manual array first
		* (rows inside it follow its exact order), then every other row — a session
		* that arrived after the reorder (a new bind, a fresh run, a rerun) lands at
		* the TOP, newest-activity first. A manual order never hides a row; it only
		* overrides the default sort.
		*/
		function orderedSessionsOf(task, rows) {
			const order = task.sessionsOrder ?? [];
			if (order.length === 0) return [...rows];
			const indexOf = new Map(order.map((id, index) => [id, index]));
			const listed = rows.filter((row) => indexOf.has(row.sessionId));
			listed.sort((a, b) => (indexOf.get(a.sessionId) ?? 0) - (indexOf.get(b.sessionId) ?? 0));
			return [...rows.filter((row) => !indexOf.has(row.sessionId)).sort((a, b) => b.updatedAt - a.updatedAt), ...listed];
		}
		//#endregion
		//#region src/core/goal-verbs.ts
		/** No current goal to mutate (the official `noCurrentGoal` verbatim). */
		function noCurrentGoal() {
			return {
				ok: false,
				error: {
					code: "no-current-goal",
					message: "no current goal to mutate"
				}
			};
		}
		function isRecord$1(value) {
			return typeof value === "object" && value !== null;
		}
		/**
		* The session's current projected CAS ref, read at verb call time (no
		* staleness fence: the RPC's CAS is the guard). undefined = no current goal.
		*/
		function goalRefOf(binding) {
			const snapshot = binding?.projections?.faceOf("goal")?.getSnapshot();
			if (!isRecord$1(snapshot)) return void 0;
			const inner = snapshot.goal;
			if (!isRecord$1(inner)) return void 0;
			if (typeof inner.id !== "string" || inner.id === "") return void 0;
			if (typeof inner.revision !== "number" || !Number.isFinite(inner.revision)) return void 0;
			return {
				id: inner.id,
				revision: inner.revision
			};
		}
		function messageOf$1(error) {
			return error instanceof Error ? error.message : String(error);
		}
		/** Normalize one verb call outcome: a Typert `{ok}` envelope passes through;
		*  a throw becomes a named remote failure (never a crash at the click site). */
		function toVerbResult(raw, verb) {
			if (isRecord$1(raw)) {
				if (raw.ok === true) return { ok: true };
				const error = raw.error;
				if (isRecord$1(error) && typeof error.message === "string") return {
					ok: false,
					error: {
						code: typeof error.code === "string" && error.code !== "" ? error.code : `goal/${verb}-failed`,
						message: error.message
					}
				};
			}
			return {
				ok: false,
				error: {
					code: `goal/${verb}-failed`,
					message: messageOf$1(raw)
				}
			};
		}
		/**
		* Bind the five verbs to one session from the wiring face. Thin wrapper over
		* {@link makeGoalVerbs} so callers hold one object, not three.
		*/
		function verbsOf(service, sessionId) {
			return makeGoalVerbs(service.bindingOf, service.remote, sessionId);
		}
		/**
		* Bind the five verbs to one session. `bindingOf` resolves the live binding
		* at CALL time (the ref must be fresh); `remote` is the `remote.goals` stub.
		* Either absent = every verb reports no-current-goal / remote-unavailable.
		*/
		function makeGoalVerbs(bindingOf, remote, sessionId) {
			const needRef = () => {
				if (remote === void 0) return {
					ok: false,
					error: {
						code: "remote/unavailable",
						message: "the live host does not serve remote.goals"
					}
				};
				return goalRefOf(bindingOf(sessionId)) ?? noCurrentGoal();
			};
			const call = async (verb, run) => {
				const ref = needRef();
				if (!("id" in ref)) return ref;
				try {
					return toVerbResult(await run(ref), verb);
				} catch (error) {
					return {
						ok: false,
						error: {
							code: `goal/${verb}-failed`,
							message: messageOf$1(error)
						}
					};
				}
			};
			return {
				ref: () => goalRefOf(bindingOf(sessionId)),
				get: () => {
					if (remote === void 0) return Promise.reject(/* @__PURE__ */ new Error("the live host does not serve remote.goals"));
					return remote.get(sessionId);
				},
				edit: (objective) => call("edit", (ref) => remote.edit(sessionId, ref, { objective })),
				pause: () => call("pause", (ref) => remote.pause(sessionId, ref)),
				resume: () => call("resume", (ref) => remote.resume(sessionId, ref)),
				clear: () => call("clear", (ref) => remote.clear(sessionId, ref))
			};
		}
		//#endregion
		//#region src/core/controller.ts
		/** The concurrency budget's real ceiling — the shared board-doc bound (writes
		*  clamp and reads normalize to one pair). Re-exported so board surfaces
		*  keep importing it from the controller. */
		const MAX_CRUISE_LIMIT = 20;
		/** The selected task (resolved from the ledger), or undefined. */
		function selectedTaskOf(snapshot) {
			if (snapshot.selectedTaskId === void 0) return void 0;
			return snapshot.tasks.find((task) => task.id === snapshot.selectedTaskId);
		}
		function randomUuid() {
			const bytes = globalThis.crypto?.getRandomValues(/* @__PURE__ */ new Uint8Array(16));
			if (bytes === void 0) return `t-${Date.now().toString(36)}-${Math.random().toString(36).slice(2, 10)}`;
			bytes[6] = bytes[6] & 15 | 64;
			bytes[8] = bytes[8] & 63 | 128;
			const hex = Array.from(bytes, (byte) => byte.toString(16).padStart(2, "0")).join("");
			return `${hex.slice(0, 8)}-${hex.slice(8, 12)}-${hex.slice(12, 16)}-${hex.slice(16, 20)}-${hex.slice(20)}`;
		}
		/** Read the current selection off a session-list snapshot (structural). */
		function currentOf(sessions) {
			return sessions.list.getSnapshot().current;
		}
		/**
		* Board controller (see module doc). All mutations bump the snapshot and
		* persist through the store; UI and DOM mounts subscribe and re-render.
		*/
		var BoardController = class BoardController {
			deps;
			tasks = [];
			boardOpen = false;
			selectedTaskId;
			/**
			* The card a `done` move just DISARMED, kept so the move can be undone.
			*
			* Why this is the one undo the board needs: a column change is visible and
			* reversible (drag it back), and delete already asks first. But moving a card to
			* 已完成 silently switches its schedule and every session rule OFF — a documented,
			* deliberate behaviour that nothing on screen announces, and the only way back was
			* to re-arm each rule by hand in the editor. That is the ledger quietly losing a
			* fact, which is exactly what this product must not do.
			*
			* One slot, not a history stack: the board's promise is 「板上不丢事」, not a
			* general undo, and a single named offer is honest about its own limit.
			*/
			lastDisarm;
			listeners = /* @__PURE__ */ new Set();
			disposers = [];
			now;
			uuid;
			cruiseState;
			/** Scheduler Forbid-policy skip ledger mirrored from the `onSkips` sink. */
			schedulerSkips = {
				overlap: 0,
				missed: 0
			};
			/** Scheduler heartbeat stamp mirrored from the `onHeartbeat` sink: the last
			*  fully completed tick (volatile — never persisted, never synced). */
			schedulerHeartbeatOkAt = void 0;
			/** @param deps - store, execution service, and the sessions navigation face. */
			constructor(deps) {
				this.deps = deps;
				this.now = deps.now ?? (() => Date.now());
				this.uuid = deps.uuid ?? randomUuid;
				this.cruiseState = normalizeCruiseValue(deps.cruiseStorage?.read());
			}
			/** Load the persisted ledger and start the navigation/status subscriptions. */
			start() {
				this.tasks = this.deps.store.load();
				if (this.sessionsReady()) this.reconcileRunningTasks();
				else {
					let fired = false;
					const unsub = this.deps.sessions.list.subscribe(() => {
						if (this.disposed || fired) return;
						if (this.sessionsReady()) {
							fired = true;
							unsub();
							this.reconcileRunningTasks();
						}
					});
					this.disposers.push(() => {
						if (!fired) {
							fired = true;
							unsub();
						}
					});
				}
				this.disposers.push(this.deps.sessions.list.subscribe(() => {
					this.onSessionsChanged();
				}));
				if (this.deps.workspaces !== void 0) this.disposers.push(this.deps.workspaces.list.subscribe(() => {
					this.notify();
				}));
				if (this.cruiseState.enabled) this.dispatch();
				this.notify();
			}
			/** Stop all subscriptions and drop retained state (idempotent). */
			dispose() {
				this.disposed = true;
				for (const dispose of this.disposers.splice(0)) dispose();
				this.queuedLaunches.length = 0;
				this.listeners.clear();
				if (this.reconcileTimer !== void 0) clearTimeout(this.reconcileTimer);
				this.reconcileTimer = void 0;
			}
			/**
			* Take or yield the engine seat (the synced wiring calls this from the
			* host lease callback). Taking the seat immediately re-pumps the queue and
			* reconciles (catch-up for anything that came due while this replica was a
			* viewer); yielding it stops the pump (in-flight launches finish on their
			* own, and the new engine's reconcile picks up the rest).
			*/
			setEngine(on) {
				if (this.disposed || this.engine === on) return;
				this.engine = on;
				if (on) {
					this.reconcileRunningTasks();
					this.dispatch();
				}
				this.notify();
			}
			/** Whether this replica currently holds the engine seat. */
			isEngine() {
				return this.engine;
			}
			/**
			* Whether the native session list has served its baseline (absent phase =
			* ready — old wirings and fakes predate the flag and must not stall).
			*/
			sessionsReady() {
				return this.deps.sessions.list.getSnapshot().phase !== "pending";
			}
			/**
			* Adopt a remotely-authored board view (the sync client's onRemote):
			* replace the ledger and cruise state, notify, and (engine only) re-pump.
			* Deliberately does NOT persist — this state came from the host, saving it
			* back would echo the commit. The controller's own newer-in-flight writes
			* are preserved by the sync client's dirty overlay before this is called.
			*/
			applyRemote(view) {
				if (this.disposed) return;
				this.tasks = [...view.tasks];
				this.cruiseState = normalizeCruiseValue(view.cruise);
				this.notify();
				if (this.engine) this.dispatch();
			}
			getSnapshot() {
				return {
					tasks: this.tasks,
					boardOpen: this.boardOpen,
					selectedTaskId: this.selectedTaskId,
					cruise: { ...this.cruiseState },
					stats: {
						running: this.inFlightCount(),
						queued: this.queuedLaunches.length
					},
					skips: { ...this.schedulerSkips },
					heartbeat: { lastOkAt: this.schedulerHeartbeatOkAt },
					engine: {
						held: this.engine,
						synced: this.syncActive,
						hostProto: this.hostProto,
						bootedAt: this.hostBootedAt
					}
				};
			}
			/** Whether multi-device sync is live (the wiring reports it once at boot);
			*  in fallback mode this device is always the engine and the lease story
			*  does not apply. */
			syncActive = false;
			/** The host's engine-lease protocol version (the wiring mirrors it from the
			*  sync client on every seat announcement). 1 = pre-visibility host. The
			*  default is CURRENT on purpose: a replica that has not read a lease yet
			*  makes no claim, so a boot race can never flash a false stale banner. */
			hostProto = 2;
			/** Which host process is answering (undefined = no lease read yet). The
			*  stale-host dialog shows it as a real time, so a user who DID restart the
			*  harness can tell at a glance whether this connection lands on that
			*  process or on a second, un-restarted instance behind the same URL. */
			hostBootedAt = void 0;
			/** Mirror the host's boot instant into the snapshot (change → notify). */
			setHostBoot(bootedAt) {
				if (this.hostBootedAt === bootedAt) return;
				this.hostBootedAt = bootedAt;
				this.notify();
			}
			/** Mirror the host's lease protocol into the snapshot (change → notify).
			*  A host restart moves the protocol WITHOUT moving the seat — the wiring
			*  calls this from the seat listener, so the stale-host banner clears live
			*  instead of surviving until the next manual refresh. */
			setHostProto(proto) {
				if (this.hostProto === proto) return;
				this.hostProto = proto;
				this.notify();
			}
			/** Whether a live seat re-read is available (drives the dialog's
			*  「重新检查」 button — it hides in fallback mode, where there is no host
			*  to re-read). */
			canRecheckSeat() {
				return this.deps.seatRecheck !== void 0;
			}
			/** Force one seat re-read from the host (the engine-note dialog's
			*  「重新检查」): the sync client renews the lease and its seat announcement
			*  flows back through setHostProto/setEngine, so a stale banner clears in
			*  the same tap once the host has actually restarted. A no-op when the
			*  wiring provides no re-read face. */
			recheckSeat() {
				return this.deps.seatRecheck !== void 0 ? this.deps.seatRecheck() : Promise.resolve();
			}
			/** Set (or clear, with undefined) a task's accent color. */
			setTaskColor(taskId, color) {
				this.tasks = this.tasks.map((task) => task.id === taskId ? withTaskColor({
					...task,
					updatedAt: this.now()
				}, color) : task);
				this.persistAndNotify();
			}
			/** The run-catalog face for form selects, or undefined when not wired. */
			runCatalog() {
				return this.deps.runCatalog;
			}
			/**
			* The schedule-preset store every preset surface reads/writes: the synced
			* document section when multi-device sync is live, the localStorage default
			* otherwise. One accessor = one source of truth, never a re-`new` per
			* component (which would fork the synced state from the shared document).
			*/
			presetStore() {
				return this.deps.presetStore ?? new LocalStoragePresetStore();
			}
			/** The run-preset store (same single-source discipline as presetStore()). */
			runPresetStore() {
				return this.deps.runPresetStore ?? new LocalStorageRunPresetStore();
			}
			/** The OFFICIAL '@' reference bridge (file + session discovery), or
			*  undefined when the deployment does not expose the Remote namespaces —
			*  the '@' menu then stays closed (the same graceful degradation as a
			*  missing slash catalog). */
			referenceSources() {
				return this.deps.reference;
			}
			/**
			* The best session to scope an input's '@' menu to — ONE deterministic
			* resolution shared by every board input (the reference RPCs are
			* session-scoped: file discovery uses the session's cwd, session
			* discovery excludes the target itself):
			* 1. the task's own related session (refine → execution → linked, the
			*    same order every surface reads);
			* 2. the currently staged native session;
			* 3. the first session of the native list.
			* undefined only when there is no task and no session at all.
			*/
			referenceSessionOf(taskId) {
				if (taskId !== void 0) {
					const task = this.tasks.find((candidate) => candidate.id === taskId);
					if (task !== void 0) {
						for (const { sessionId } of this.relatedSessionsOf(task)) if (sessionId !== void 0) return sessionId;
					}
				}
				const state = this.deps.sessions.list.getSnapshot();
				if (state.current !== void 0) return state.current;
				const first = Object.keys(state.byId)[0];
				return first !== void 0 ? first : void 0;
			}
			/**
			* The board's own session catalog (id + latest title, native list order):
			* the '@' MENU's second source for the sessions half. It is used when the
			* host's `candidates` half is unavailable — the gateway refuses the agent
			* lookup for subagent-routed (agent-busy) target sessions, which fails the
			* official discovery too; the board's catalog still lists every session,
			* and its rows carry the OFFICIAL mention text (see session-mention.ts),
			* so a picked row resolves through the host's pre-step parser exactly like
			* a host candidate.
			*/
			referenceSessionCatalog() {
				const state = this.deps.sessions.list.getSnapshot();
				return Object.entries(state.byId).map(([sessionId, summary]) => ({
					sessionId,
					label: typeof summary.title === "string" && summary.title !== "" ? summary.title : sessionId
				}));
			}
			/**
			* Read a session's recent history events for the review page's transcript
			* (the fold happens in the UI), together with the native projection
			* baseline (context pressure / breakdown) riding the history tail page.
			* undefined when no reader is wired.
			*/
			loadTranscript(sessionId) {
				return this.deps.transcript?.(sessionId) ?? Promise.resolve(void 0);
			}
			/** Read one earlier history page backward from `beforeSeq`. */
			loadTranscriptPage(sessionId, beforeSeq, throughSeq) {
				return this.deps.transcriptPage?.(sessionId, beforeSeq, throughSeq) ?? Promise.resolve(void 0);
			}
			/** Read one durable message image back as base64 (official attachment read,
			*  cached + deduped by the wiring); undefined when unavailable. */
			loadImage(sessionId, attachmentId) {
				return this.deps.loadImage?.(sessionId, attachmentId) ?? Promise.resolve(void 0);
			}
			/**
			* Stage one file's exact bytes on a session (the official file-lane
			* pre-step): returns a stager bound to `sessionId`, or undefined when the
			* host serves no upload face. Receipts are per-Agent — a receipt minted
			* here MUST NOT cross sessions (the execution send layer re-stages by
			* name when a stored receipt is rejected).
			*/
			uploadFile(sessionId) {
				const upload = this.deps.uploadFile;
				if (upload === void 0) return void 0;
				return (target, file) => upload(target === sessionId ? sessionId : target, file);
			}
			/** The session-config face (review page's model/permission panel), or undefined. */
			sessionConfig() {
				return this.deps.sessionConfig;
			}
			/**
			* The native goal verbs for one session's goal strip (official
			* `remote.goals` mutations with the call-time CAS ref), or undefined when
			* the host does not serve them — the strip then stays read-only.
			*/
			goalVerbs(sessionId) {
				const service = this.deps.goalService;
				if (service === void 0) return void 0;
				return verbsOf(service, sessionId);
			}
			/** The goal activation subscription (official `goal/activation-changed`), or undefined. */
			subscribeGoalActivation(sessionId, listener) {
				const service = this.deps.goalService;
				if (service === void 0) return void 0;
				return service.subscribeActivation(sessionId, listener);
			}
			/**
			* The user interaction an execution session is currently blocked on
			* (`approval` / `plan-review` / `question`), straight from the native
			* session-list summary (the same signal as the sidebar's amber dot).
			* undefined = the session is not waiting (or no longer listed).
			*/
			pendingInteractionOf(sessionId) {
				if (sessionId === void 0) return void 0;
				return this.deps.sessions.list.getSnapshot().byId[sessionId]?.pendingInteraction;
			}
			/** The open ask_user_question batch for a session (the interaction card's
			*  content source — the same request the native composer renders). */
			questionPendingOf(sessionId) {
				return this.deps.questionRpc?.pendingOf(sessionId);
			}
			/** Subscribe to pending-question changes across sessions. */
			subscribeQuestions(listener) {
				return this.deps.questionRpc?.subscribe(listener) ?? (() => {});
			}
			/** Whether the board can answer a pending question in place. True while the
			*  official snapshot carries an interactive carrier (the native
			*  `PendingQuestion` exposes `answer`/`cancel`; settling through it resolves
			*  the ONE host waterfall call — nothing new is registered, so first answer
			*  still wins). False for a display-only snapshot entry or a host with no
			*  uiSession: the card degrades to its navigate-to-answer shell. */
			get questionAnswerInPlace() {
				return this.deps.questionRpc?.answerInPlace ?? false;
			}
			/** Deliver one answer batch to the suspended ask. True = accepted; false =
			*  refused or stale — the card keeps itself open and reports the reason. */
			answerQuestion(rpcId, sessionId, answers) {
				return this.deps.questionRpc?.answer(rpcId, sessionId, answers) ?? Promise.resolve(false);
			}
			/** Reject the whole ask (the model sees ASK_CANCELLED and continues). */
			cancelQuestion(rpcId) {
				return this.deps.questionRpc?.cancel(rpcId) ?? Promise.resolve(false);
			}
			/** The session's real workspace root + composed agent preset.
			*
			*  Truth order (the "显示的必须是生效的" law): the host's served
			*  `summary.agentPreset` first (a future host may serve it — costs nothing
			*  to prefer), then the board's applied-preset ledger (what THIS board
			*  successfully composed the session from — the only source that knows
			*  today), else absent ("部署默认"). Reading the host field alone is how
			*  the row lied 100% of the time: the field is declared "when known" but
			*  no host serves it.
			*/
			sessionInfo(sessionId) {
				if (sessionId === void 0) return void 0;
				const summary = this.deps.sessions.list.getSnapshot().byId[sessionId];
				if (summary === void 0) return void 0;
				const applied = appliedPresetOf(this.deps.sessionAgentStore ?? new LocalStorageSessionAgentStore(), sessionId);
				const agentPreset = summary.agentPreset ?? applied;
				return {
					...summary.cwd !== void 0 ? { cwd: summary.cwd } : {},
					...agentPreset !== void 0 ? { agentPreset } : {}
				};
			}
			/** The session's display title (native list summary), or undefined when the
			*  session is gone/unknown. Drives the execution row's identity slot.
			*
			*  A durable title equal to the workspace (project) BASENAME is the host's
			*  deterministic auto-name, not a real name — reported as undefined so the
			*  single 未命名 grammar shows 「未命名」 until a provider-generated or
			*  user-pinned title arrives (the recurring "不填标题却显示工作区名" bug).
			*  One judgment with the linked rows: both call `realTitleOf`. */
			sessionTitle(sessionId) {
				if (sessionId === void 0) return void 0;
				const row = this.deps.sessions.list.getSnapshot().byId[sessionId];
				return realTitleOf(row?.title, row?.cwd);
			}
			/** The localized 未命名 placeholder the session rows show for a session
			*  the host has not titled yet (set by the client wiring; undefined in
			*  tests = legacy task-title fallback). */
			untitledSessionLabel;
			subscribe(fn) {
				this.listeners.add(fn);
				return () => {
					this.listeners.delete(fn);
				};
			}
			openBoard() {
				if (this.boardOpen) return;
				this.lastCurrent = currentOf(this.deps.sessions);
				this.boardOpen = true;
				this.notify();
			}
			closeBoard() {
				if (!this.boardOpen) return;
				this.boardOpen = false;
				this.notify();
			}
			toggleBoard() {
				if (this.boardOpen) this.closeBoard();
				else this.openBoard();
			}
			openTask(id) {
				if (this.tasks.some((task) => task.id === id)) {
					const at = this.now();
					let changed = false;
					this.tasks = this.tasks.map((task) => {
						if (task.id !== id || task.viewedAt === at) return task;
						changed = true;
						return {
							...task,
							viewedAt: at
						};
					});
					if (changed) this.persistAndNotify();
					this.selectedTaskId = id;
					this.notify();
				}
			}
			/**
			* Mark one execution as viewed (the user opened its review page), clearing
			* its row's unread dot AND the task card's unread ring — ONE baseline
			* (task.viewedAt) governs the card's glow, the row's dot/halo is the same
			* moment seen; opening the review page means the task's content is seen.
			* Persisted so the cleared state survives refreshes. A no-op (no persist)
			* for unknown tasks or executions.
			*/
			markExecutionViewed(taskId, executionId) {
				const at = this.now();
				let changed = false;
				this.tasks = this.tasks.map((task) => {
					if (task.id !== taskId) return task;
					if (!task.executions.some((round) => round.id === executionId)) return task;
					const executions = task.executions.map((round) => {
						if (round.id !== executionId || round.viewedAt === at) return round;
						changed = true;
						return {
							...round,
							viewedAt: at
						};
					});
					if (executions !== task.executions || (task.viewedAt ?? 0) < at) {
						changed = true;
						return {
							...task,
							executions,
							viewedAt: Math.max(task.viewedAt ?? 0, at)
						};
					}
					return task;
				});
				if (changed) this.persistAndNotify();
			}
			closeTask() {
				if (this.selectedTaskId === void 0) return;
				this.selectedTaskId = void 0;
				this.notify();
			}
			/**
			* Mark every task (and every round) viewed — the notification center's
			* "全部标为已读". One write, one persist: read-state only moves forward
			* (see board-doc authorship), so a bulk clear never clobbers content.
			*/
			markAllViewed() {
				const at = this.now();
				let changed = false;
				this.tasks = this.tasks.map((task) => {
					const executions = task.executions.map((round) => {
						if (round.viewedAt === at) return round;
						changed = true;
						return {
							...round,
							viewedAt: at
						};
					});
					if ((task.viewedAt ?? 0) >= at && executions.every((round, index) => round === task.executions[index])) return task;
					changed = true;
					return {
						...task,
						viewedAt: Math.max(task.viewedAt ?? 0, at),
						executions
					};
				});
				if (changed) this.persistAndNotify();
			}
			/**
			* Mark ONE task (and its rounds) viewed without navigating — a triage row's
			* inline "标已读". Same monotone read-state law as `markAllViewed`, scoped
			* to a single record so the board stays where it is.
			*/
			markTaskViewed(taskId) {
				const at = this.now();
				let changed = false;
				this.tasks = this.tasks.map((task) => {
					if (task.id !== taskId) return task;
					const executions = task.executions.map((round) => {
						if (round.viewedAt === at) return round;
						changed = true;
						return {
							...round,
							viewedAt: at
						};
					});
					if ((task.viewedAt ?? 0) >= at && executions.every((round, index) => round === task.executions[index])) return task;
					changed = true;
					return {
						...task,
						viewedAt: Math.max(task.viewedAt ?? 0, at),
						executions
					};
				});
				if (changed) this.persistAndNotify();
			}
			/**
			* Open a task FROM the notification center: the same navigation as
			* `openTask`, but the notification's shadow expires — the viewed session's
			* rounds read viewed (other sessions' dots survive: looking at s-1 never
			* clears s-2). ONE funnel for notification/feed opens; pure card clicks
			* keep `openTask` (the card ring clears while row dots wait for their
			* review pages). Unknown ids are ignored.
			*/
			openTaskFromNotification(id, sessionId) {
				if (!this.tasks.some((task) => task.id === id)) return;
				if (sessionId !== void 0) this.markTaskSessionViewed(id, sessionId);
				else this.markTaskViewed(id);
				this.selectedTaskId = id;
				this.notify();
			}
			/**
			* Mark ONE session's rounds viewed (plus the task baseline, so the card
			* ring follows the view) — a notification row's session entering the
			* detail. Same monotone read-state law as `markTaskViewed`, scoped to the
			* session's rounds so sibling sessions keep their dots. No matching round
			* (unknown session) = pure no-op: the baseline never moves for nothing seen.
			*/
			markTaskSessionViewed(taskId, sessionId) {
				const at = this.now();
				let changed = false;
				let matched = false;
				this.tasks = this.tasks.map((task) => {
					if (task.id !== taskId) return task;
					const executions = task.executions.map((round) => {
						if (round.sessionId !== sessionId || round.viewedAt === at) return round;
						matched = true;
						changed = true;
						return {
							...round,
							viewedAt: at
						};
					});
					if (!matched) return task;
					if (executions.every((round, index) => round === task.executions[index]) && (task.viewedAt ?? 0) >= at) return task;
					changed = true;
					return {
						...task,
						executions,
						viewedAt: Math.max(task.viewedAt ?? 0, at)
					};
				});
				if (changed) this.persistAndNotify();
			}
			/**
			* Mark an explicit id set viewed (bulk triage of a filtered group). One
			* write, one persist; unknown ids are ignored.
			*/
			markTasksViewed(taskIds) {
				if (taskIds.length === 0) return;
				const wanted = new Set(taskIds);
				const at = this.now();
				let changed = false;
				this.tasks = this.tasks.map((task) => {
					if (!wanted.has(task.id)) return task;
					const executions = task.executions.map((round) => {
						if (round.viewedAt === at) return round;
						changed = true;
						return {
							...round,
							viewedAt: at
						};
					});
					if ((task.viewedAt ?? 0) >= at && executions.every((round, index) => round === task.executions[index])) return task;
					changed = true;
					return {
						...task,
						viewedAt: Math.max(task.viewedAt ?? 0, at),
						executions
					};
				});
				if (changed) this.persistAndNotify();
			}
			/**
			* 缺则补、填则守：任务被创建（四出生门：手动/复制/模板实例/绑定创建）或被真正
			* 启动（唯一发射门 launchTask）的时刻，空标题/空描述从执行 Prompt 补齐
			* （`supplementLaunchFields` 纯函数）——已填字段永不覆盖。这是整个特性的唯一
			* 作用点：任何创建与启动路径（手动/重复/接续链/定时/巡航/自动化/创建/启动）
			* 都得到同一套语义，卡片在出生与开跑时永远不会无故空着头；编辑路径永不补
			* （清空即意图）。
			*/
			supplementedTask(task) {
				const supplements = supplementLaunchFields(task);
				return supplements !== void 0 ? {
					...task,
					...supplements
				} : task;
			}
			createTask(input) {
				const task = this.supplementedTask(createTask(input, this.now(), this.uuid(), this.nextOrder()));
				this.tasks = promoteToColumnTop([...this.tasks, task], task.id, task.status, this.now());
				this.persistAndNotify();
				return this.tasks.find((candidate) => candidate.id === task.id) ?? task;
			}
			/**
			* Create a task bound to a live native source (a session or a whole
			* workspace folder dragged in from the sidebar). The bind wires the card's
			* "链接会话" section; everything else behaves like a plain task — title is
			* optional like any new task (the first real run supplements it).
			*
			* Workspace snapshot semantics: a workspace bind is a source association,
			* but at CREATION the card also snapshots the workspace's CURRENT live
			* sessions (visible, unarchived, non-blank) as explicit session binds —
			* the user dragged the folder "with what's in it", not an empty shell.
			* Later sessions never auto-join (no flooding); archived sessions leave
			* the rows at once (see linkedOf).
			* @param bind - the live binding.
			* @param input - title/description/prompt/landing column.
			* @returns the created task, or undefined for a bad bind.
			*/
			createBoundTask(bind, input) {
				if (bind === void 0) return void 0;
				const task = this.supplementedTask(createTask(input, this.now(), this.uuid(), this.nextOrder()));
				const binds = bind.kind === "workspace" ? [bind, ...this.snapshotWorkspaceSessions(bind.workspaceId).map((sessionId) => ({
					kind: "session",
					sessionId
				}))] : [bind];
				const boundTask = {
					...task,
					binds
				};
				this.tasks = promoteToColumnTop([...this.tasks, boundTask], boundTask.id, boundTask.status, this.now());
				this.persistAndNotify();
				this.reconcileBoundTask(boundTask.id);
				return boundTask;
			}
			/**
			* The workspace's CURRENT live session snapshot for a folder drop.
			* Membership comes from the registry's OWN ownership account (`sessionIds`
			* on the workspace row, display order) — never cwd/title guessing across
			* the whole list (same-named folders elsewhere are not this workspace).
			* Only when the row carries no account (old host) does the legacy cwd scan
			* apply. Either way each candidate must be listed RIGHT NOW, unarchived
			* and non-blank; unknown ids (stale slots) are skipped, never guessed.
			* Pure derivation over the two snapshots (no I/O), so tests drive it with
			* fakes. Later sessions never join (snapshot, not subscription).
			*/
			snapshotWorkspaceSessions(workspaceId) {
				const state = this.deps.sessions.list.getSnapshot();
				const workspaces = this.deps.workspaces?.list.getSnapshot();
				const archived = new Set(workspaces?.archivedSessionIds ?? []);
				const owned = workspaces?.items.find((item) => item.id === workspaceId)?.sessionIds;
				const seen = /* @__PURE__ */ new Set();
				const out = [];
				let skippedArchived = 0;
				let skippedBlank = 0;
				let skippedUnknown = 0;
				const take = (id) => {
					const row = state.byId[id];
					if (row === void 0) {
						skippedUnknown += 1;
						return;
					}
					if (archived.has(id)) {
						skippedArchived += 1;
						return;
					}
					if (row.blank === true) {
						skippedBlank += 1;
						return;
					}
					if (seen.has(id)) return;
					seen.add(id);
					out.push(id);
				};
				if (owned !== void 0) for (const id of owned) take(id);
				else {
					const order = state.ids ?? Object.keys(state.byId);
					for (const id of order) {
						const row = state.byId[id];
						if (row === void 0 || !this.sessionInWorkspace(id, row, workspaceId)) {
							skippedUnknown += 1;
							continue;
						}
						take(id);
					}
				}
				console.info(`[dsh-task-board] workspace snapshot ${workspaceId}: took ${out.length} (archivedSet=${archived.size}, via=${owned !== void 0 ? "ownership" : "cwd-scan"}, skipped: archived=${skippedArchived} blank=${skippedBlank} unknown-or-unmatched=${skippedUnknown})`);
				return out;
			}
			/** Whether a session row belongs to a workspace (cwd path or id match). */
			sessionInWorkspace(id, row, workspaceId) {
				if (row.workspaceId !== void 0 && row.workspaceId !== "") return row.workspaceId === workspaceId;
				const title = this.deps.workspaces?.list.getSnapshot().items.find((item) => item.id === workspaceId)?.title;
				if (row.cwd !== void 0 && row.cwd !== "" && title !== void 0 && title !== "") {
					if (row.cwd.split(/[\\/]+/).filter(Boolean).pop() === title) return true;
				}
				return false;
			}
			/**
			* Copy a task as a fresh template ("复制为模板"): the same content, run
			* configuration, due date and task-level automation CONFIGURATION (mode,
			* cron, budget — runCount reset to 0), landing in 待规划, DISARMED: like a
			* stamped template, a copy must never surprise-fire (arming is an explicit
			* act on the new card). Executions, hide state, live bindings and SESSION
			* RULES are never copied — a template has none of the source's sessions
			* (a session rule automates a SPECIFIC session of the source task; the
			* template is a new blank card, "绘画规则" would point at nothing).
			* @param id - the source task.
			* @returns the new task, or undefined when the source is unknown.
			*/
			copyTask(id) {
				const source = this.tasks.find((task) => task.id === id);
				if (source === void 0) return void 0;
				const now = this.now();
				let task = this.supplementedTask(createTask({
					title: source.title,
					description: source.description,
					prompt: source.prompt,
					status: "backlog",
					workspaceId: source.workspaceId,
					provider: source.provider,
					model: source.model,
					reasoningEffort: source.reasoningEffort,
					agentPreset: source.agentPreset,
					permission: source.permission
				}, now, this.uuid(), this.nextOrder()));
				const schedule = source.schedule;
				if (schedule !== void 0) task = withSchedule(task, {
					enabled: false,
					mode: schedule.mode,
					cron: schedule.cron,
					maxRuns: schedule.maxRuns,
					runCount: 0,
					nextRunAt: void 0
				}, now);
				task = {
					...task,
					...source.color !== void 0 ? { color: source.color } : {},
					...source.promptImages !== void 0 && source.promptImages.length > 0 ? { promptImages: source.promptImages.map((image) => ({ ...image })) } : {},
					...source.promptFiles !== void 0 && source.promptFiles.length > 0 ? { promptFiles: source.promptFiles.map((file) => ({ ...file })) } : {}
				};
				this.tasks = promoteToColumnTop([...this.tasks, task], task.id, task.status, this.now());
				this.persistAndNotify();
				return this.tasks.find((candidate) => candidate.id === task.id) ?? task;
			}
			/**
			* The template library (named, reusable blueprints — see task-templates.ts).
			* Templates are device-local; the board document is untouched, so saving
			* and stamping never sync and never need a migration.
			*/
			listTemplates() {
				return (this.deps.templateStore ?? new LocalStorageTemplateStore()).load();
			}
			/**
			* Snapshot a task as a named template ("存为模板"). The name defaults to
			* the task's title (deduplicated with a numeric suffix); instance state
			* never rides along.
			* @param id - the source task.
			* @param name - the template label (blank = the task's title).
			* @returns the saved template, or undefined when the source is unknown.
			*/
			saveTemplate(id, name) {
				const source = this.tasks.find((task) => task.id === id);
				if (source === void 0) return void 0;
				const store = this.deps.templateStore ?? new LocalStorageTemplateStore();
				const taken = new Set(store.load().map((template) => template.name));
				let label = (name ?? "").trim() === "" ? source.title.trim() : (name ?? "").trim();
				if (label === "") label = "Untitled template";
				let candidate = label;
				for (let n = 2; taken.has(candidate); n++) candidate = `${label} ${n}`;
				const template = templateFromTask(source, this.uuid(), candidate);
				store.save([...store.load(), template]);
				this.notify();
				return template;
			}
			/** Delete one template by id (a no-op for unknown ids). */
			deleteTemplate(id) {
				const store = this.deps.templateStore ?? new LocalStorageTemplateStore();
				const next = store.load().filter((template) => template.id !== id);
				if (next.length === store.load().length) return false;
				store.save(next);
				this.notify();
				return true;
			}
			/**
			* Stamp a fresh 待规划 card from a template (content + run configuration;
			* schedule/rules never ride — arming is explicit on the new card).
			* @param id - the template id.
			* @returns the new task, or undefined for an unknown template.
			*/
			instantiateTemplate(id) {
				const template = (this.deps.templateStore ?? new LocalStorageTemplateStore()).load().find((candidate) => candidate.id === id);
				if (template === void 0) return void 0;
				const task = this.supplementedTask(createTask(templateToNewInput(template), this.now(), this.uuid(), this.nextOrder()));
				this.tasks = promoteToColumnTop([...this.tasks, task], task.id, task.status, this.now());
				this.persistAndNotify();
				return this.tasks.find((candidate) => candidate.id === task.id) ?? task;
			}
			/** Whether a session is natively archived (registry-global archive set).
			*  Absent workspaces face = nothing is known archived (degrade open). */
			archivedOf(sessionId) {
				return (this.deps.workspaces?.list.getSnapshot())?.archivedSessionIds.includes(sessionId) === true;
			}
			/**
			* The live linked-session rows of a task (pure derivation over the native
			* session snapshot; see linked-sessions.ts). ONLY explicit session binds
			* contribute — a workspace bind is a source association and surfaces no
			* session rows, so a chat created in the main UI never appears on a card
			* by itself. Dragging more session sources in ADDS to the set (same
			* session, one row).
			*/
			linkedOf(task) {
				const binds = taskBindsOf(task);
				if (binds.length === 0) return [];
				const byId = this.deps.sessions.list.getSnapshot().byId;
				const rows = [];
				const seen = /* @__PURE__ */ new Set();
				const removed = task.removedSessions ?? [];
				for (const bind of binds) for (const row of deriveLinkedSessions(bind, {
					byId,
					hidden: task.hidden?.sessions ?? []
				})) {
					if (seen.has(row.sessionId) || removed.includes(row.sessionId) || this.archivedOf(row.sessionId)) continue;
					seen.add(row.sessionId);
					rows.push(row);
				}
				return rows;
			}
			/** Hide one session of the task (run or linked) — the unified per-session
			*  hide: recorded in both families (the derived hidden-session set in
			*  session-list.ts reads either, so a run session and a linked view of the
			*  same session stay hidden together). Non-destructive; numbering stays. */
			hideTaskSession(taskId, sessionId) {
				this.userEdit(taskId, (task) => {
					const sessions = task.hidden?.sessions ?? [];
					if (sessions.includes(sessionId)) return task;
					const runIds = task.executions.filter((round) => round.sessionId === sessionId).map((round) => round.id);
					const executions = [.../* @__PURE__ */ new Set([...task.hidden?.executions ?? [], ...runIds])];
					return {
						...task,
						hidden: {
							...runIds.length > 0 ? { executions } : {},
							sessions: [...sessions, sessionId]
						}
					};
				});
			}
			/** Restore every hidden session (the "恢复全部已隐藏" action). */
			unhideTaskSessions(taskId) {
				this.userEdit(taskId, (task) => {
					if (task.hidden === void 0) return task;
					const rest = { ...task };
					delete rest.hidden;
					return rest;
				});
			}
			/**
			* The task's unified session list — see {@link taskSessionsOf}: one
			* de-duplicated view of its run sessions and linked sessions, the single
			* source for the detail's 会话 section.
			*/
			sessionsOf(task) {
				return taskSessionsOf(task, {
					linked: this.linkedOf(task),
					titleOf: (sessionId) => this.sessionTitle(sessionId),
					pendingInteractionOf: (sessionId) => this.pendingInteractionOf(sessionId),
					nativeRunningOf: (sessionId) => this.nativeRunningOf(sessionId),
					archivedOf: (sessionId) => this.archivedOf(sessionId),
					untitledLabel: this.untitledSessionLabel
				});
			}
			/**
			* Reorder the task's 会话 list by dragging: the moved row lands BEFORE
			* `beforeId` (or at the end when undefined). The full current display order
			* is persisted as the manual order, so sessions that arrive later keep
			* landing at the TOP (the default newest-activity rule) until the user drags
			* them too. A USER-INTENT write: it bumps `updatedAt` like any edit (a
			* record carrying a new order but the old stamp used to lose the sync
			* merge — the two-device order drift this closes). @returns true when the
			* order changed.
			*/
			reorderTaskSession(taskId, sessionId, beforeId) {
				return this.userEdit(taskId, (task) => {
					const ids = taskSessionsOf(task, {
						linked: this.linkedOf(task),
						titleOf: (sid) => this.sessionTitle(sid),
						pendingInteractionOf: (sid) => this.pendingInteractionOf(sid),
						archivedOf: (sid) => this.archivedOf(sid),
						untitledLabel: this.untitledSessionLabel
					}).map((row) => row.sessionId);
					if (!ids.includes(sessionId)) return task;
					const rest = ids.filter((id) => id !== sessionId);
					let at = beforeId === void 0 ? rest.length : rest.indexOf(beforeId);
					if (at < 0) at = rest.length;
					const next = [
						...rest.slice(0, at),
						sessionId,
						...rest.slice(at)
					];
					if (next.join() === ids.join()) return task;
					return {
						...task,
						sessionsOrder: next
					};
				});
			}
			/**
			* Create one fresh native session from a task's detail ("新建会话") and
			* bind it to the task: the session is created through the execution
			* service (composed with the given run configuration — the detail form's
			* fields), then joins the task's source set through {@link addTaskSource}
			* (persisted, additive, and instantly reconciled — the same path a sidebar
			* drag takes). The task itself is untouched: no execution record, no
			* dispatcher, no automation involvement. A config failure after the
			* session exists is an honest partial success: the session stays bound
			* and the error is surfaced for a retry of the config only.
			*
			* Title semantics (conflict-free by the native design): a non-blank title
			* is applied through the OFFICIAL user rename — it pins the title against
			* automatic regeneration, exactly as if typed in the native composer. A
			* blank title sends NOTHING: the host's automatic naming chain (first
			* user message → deterministic fallback + provider cadence) stays fully
			* intact, so an unnamed session names itself after the first real chat —
			* never a guessed placeholder that fights the native behavior. A rename
			* failure is a partial success too (the session exists); it is surfaced
			* as `titleError` alongside `configError`.
			* @param taskId - the task to bind the session to.
			* @param config - the run configuration (plus optional title) to compose
			*   the session with.
			* @returns the session id (+ optional configError/titleError), or
			*   ok:false with the creation error.
			*/
			async createTaskSession(taskId, config) {
				if (this.deps.exec.createSession === void 0) return {
					ok: false,
					error: "session creation is unavailable"
				};
				if (this.tasks.find((candidate) => candidate.id === taskId) === void 0) return {
					ok: false,
					error: "unknown task"
				};
				const title = config.title?.trim() ?? "";
				const { title: _ignored, ...launchConfig } = config;
				const result = await this.deps.exec.createSession(launchConfig);
				if (!result.ok) return result;
				let titleError;
				if (title !== "") {
					const renamed = await this.deps.exec.renameSession?.(result.sessionId, title);
					if (renamed !== void 0 && !renamed.ok) titleError = renamed.error;
				}
				this.addTaskSource(taskId, {
					kind: "session",
					sessionId: result.sessionId
				});
				return {
					...result,
					...titleError !== void 0 ? { titleError } : {}
				};
			}
			/**
			* Rename one of a task's native sessions ("会话行重命名"): the OFFICIAL
			* user-title write — the accepted title pins against automatic
			* regeneration, so the native sidebar, the board rows and every future
			* reload all read the same durable title (one truth, zero drift). A
			* blank title is rejected (the native rename contract normalizes empty
			* to invalid — a session cannot be UN-titled, only re-titled).
			* @param taskId - the task owning the session (an unknown task refuses).
			* @param sessionId - the native session to rename.
			* @param title - the new title (trimmed; blank = rejected).
			* @returns ok, or ok:false with an error string.
			*/
			async renameTaskSession(taskId, sessionId, title) {
				const trimmed = title.trim();
				if (trimmed === "") return {
					ok: false,
					error: "empty title"
				};
				const task = this.tasks.find((candidate) => candidate.id === taskId);
				if (task === void 0) return {
					ok: false,
					error: "unknown task"
				};
				if (!relatedSessionIdsOf(task, this.linkedOf(task).map((row) => row.sessionId)).some((entry) => entry.sessionId === sessionId)) return {
					ok: false,
					error: "session does not belong to this task"
				};
				const renamed = await this.deps.exec.renameSession?.(sessionId, trimmed);
				if (renamed === void 0) return {
					ok: false,
					error: "rename channel unavailable"
				};
				if (!renamed.ok) return renamed;
				this.notify();
				return { ok: true };
			}
			/**
			* ADD a live source (a sidebar session or workspace folder) to an EXISTING
			* task — the "drag a folder/session into the open task's 会话 area" path.
			* NEVER replaces: an already-bound identical source is an idempotent no-op,
			* anything else joins the multi-source set. Persisted. A session bind
			* surfaces its session as a linked row; a workspace bind is a source
			* association only — it NEVER surfaces the folder's conversations (a chat
			* created in the main UI must not appear on a card by itself).
			*
			* An explicit re-add is also the RESTORE gesture: a session bind re-added
			* leaves the removed set (the hidden tray's 删除 is reversible BY THE
			* USER'S HAND — dragging the session back shows it again).
			* @returns true when the binding was added or anything was restored, false
			* for a pure no-op / unknown task.
			*/
			addTaskSource(taskId, bind) {
				const changed = this.userEdit(taskId, (task) => {
					const isSame = taskBindsOf(task).some((existing) => sameBind(existing, bind));
					const removed = task.removedSessions;
					let next = task;
					let restored = false;
					if (removed !== void 0 && removed.length > 0) {
						const carried = this.sourceMemberIdsOf(bind);
						const kept = carried.length > 0 ? removed.filter((id) => !carried.includes(id)) : removed;
						if (kept.length !== removed.length) {
							restored = true;
							next = { ...next };
							if (kept.length > 0) next.removedSessions = kept;
							else delete next.removedSessions;
						}
					}
					if (!isSame) next = {
						...next,
						binds: [...taskBindsOf(next), bind]
					};
					return restored || !isSame ? next : task;
				});
				if (changed) this.reconcileBoundTask(taskId);
				return changed;
			}
			/** The sessions a re-added bind contributes to the display set: a session
			*  bind is itself (its removal is reversible by dragging it back); a
			*  workspace bind contributes none — it surfaces no session rows at all. */
			sourceMemberIdsOf(bind) {
				return bind.kind === "session" ? [bind.sessionId] : [];
			}
			/** Permanently remove ONE session from the task (the hidden-tray 删除):
			*  its rounds are deleted, its hide state is cleared, and it joins the
			*  REMOVED set so a bound workspace can never re-derive it (the delete was
			*  irreversible — deleting a workspace member must actually remove it). A
			*  live session binding that points ONLY at this session is unbound (a
			*  deleted source cannot stay bound). The task itself and every other
			*  session remain.
			*  @returns true when anything was removed. */
			removeTaskSession(taskId, sessionId) {
				return this.userEdit(taskId, (task) => {
					const kept = task.executions.filter((round) => round.sessionId !== sessionId);
					const wasHidden = task.hidden?.sessions?.includes(sessionId) === true;
					if (kept.length === task.executions.length && !wasHidden) return task;
					const removed = task.removedSessions ?? [];
					const next = {
						...task,
						executions: kept,
						...!removed.includes(sessionId) ? { removedSessions: [...removed, sessionId] } : {}
					};
					const hidden = task.hidden;
					if (hidden !== void 0) {
						const sessions = (hidden.sessions ?? []).filter((id) => id !== sessionId);
						const executions = (hidden.executions ?? []).filter((id) => task.executions.find((round) => round.id === id && round.sessionId === sessionId) === void 0);
						if (sessions.length > 0 || executions.length > 0) next.hidden = {
							...sessions.length > 0 ? { sessions } : {},
							...executions.length > 0 ? { executions } : {}
						};
						else delete next.hidden;
					}
					const order = task.sessionsOrder;
					if (order !== void 0) {
						const kept = order.filter((id) => id !== sessionId);
						if (kept.length > 0) next.sessionsOrder = kept;
						else delete next.sessionsOrder;
					}
					const binds = taskBindsOf(next);
					if (binds.length === 1 && binds[0].kind === "session" && binds[0].sessionId === sessionId) {
						const unbound = { ...next };
						delete unbound.bind;
						delete unbound.binds;
						return unbound;
					}
					return next;
				});
			}
			/** Restore ONE hidden session (single-item restore; the bulk "恢复全部"
			*  stays available too — a folder's many hidden rows can be brought back
			*  one by one without restoring everything). */
			unhideTaskSession(taskId, sessionId) {
				this.userEdit(taskId, (task) => {
					if (task.hidden === void 0) return task;
					const sessions = (task.hidden.sessions ?? []).filter((id) => id !== sessionId);
					const executions = (task.hidden.executions ?? []).filter((executionId) => task.executions.find((round) => round.id === executionId)?.sessionId !== sessionId);
					if (sessions.length === (task.hidden.sessions?.length ?? 0) && executions.length === (task.hidden.executions?.length ?? 0)) return task;
					const hidden = {};
					if (sessions.length > 0) hidden.sessions = sessions;
					if (executions.length > 0) hidden.executions = executions;
					if (Object.keys(hidden).length === 0) {
						const rest = { ...task };
						delete rest.hidden;
						return rest;
					}
					return {
						...task,
						hidden
					};
				});
			}
			/** Default title for a freshly dragged-in binding (from its native source). */
			boundSourceTitleOf(bind) {
				const workspaces = this.deps.workspaces?.list.getSnapshot();
				return boundSourceTitle(bind, {
					sessions: this.deps.sessions.list.getSnapshot().byId,
					workspaces: workspaces?.items ?? []
				});
			}
			/** Classify a sidebar-drag id (known session / known workspace / unknown). */
			externalKindOf(id) {
				const workspaces = this.deps.workspaces;
				return resolveExternalKind(id, {
					sessions: this.deps.sessions.list.getSnapshot().byId,
					workspaces: workspaces?.list.getSnapshot().items ?? []
				});
			}
			/** Next column sort key: one past the largest order in the ledger. */
			nextOrder() {
				let max = -1;
				for (const task of this.tasks) if (task.order > max) max = task.order;
				return max + 1;
			}
			/**
			* Update a task's editable fields: content (title/description/prompt) and
			* run configuration. A run-config key present in the patch with an empty
			* string or `undefined` clears the field (the run then falls back to
			* defaults); a value sets it; absent keys keep their current value. Text
			* fields are trimmed; a blank title is allowed and STAYS blank (the card
			* shows an 未命名 placeholder — the 缺则补 supplement runs at birth and
			* at launch only, never on edit). The next execution — manual or
			* scheduled — reads the updated record, so edits apply from the
			* following run onward.
			* @returns true when applied, false when rejected (unknown task).
			*/
			updateTask(id, patch) {
				if (this.tasks.find((candidate) => candidate.id === id) === void 0) return false;
				const title = patch.title?.trim() ?? "";
				const applied = {};
				if (patch.title !== void 0) applied.title = title;
				if (patch.description !== void 0) applied.description = patch.description.trim();
				if (patch.prompt !== void 0) applied.prompt = patch.prompt.trim();
				if ("promptImages" in patch) applied.promptImages = patch.promptImages !== void 0 && patch.promptImages.length > 0 ? patch.promptImages.map((image) => ({ ...image })) : void 0;
				if ("promptFiles" in patch) applied.promptFiles = patch.promptFiles !== void 0 && patch.promptFiles.length > 0 ? patch.promptFiles.map((file) => ({ ...file })) : void 0;
				for (const key of [
					"workspaceId",
					"provider",
					"model",
					"reasoningEffort",
					"agentPreset",
					"permission"
				]) if (key in patch) {
					const value = patch[key];
					applied[key] = value === void 0 || value === "" ? void 0 : value;
				}
				if ("color" in patch) applied.color = patch.color !== void 0 && patch.color !== "" ? patch.color : void 0;
				this.tasks = this.tasks.map((candidate) => candidate.id === id ? {
					...candidate,
					...applied,
					updatedAt: this.now()
				} : candidate);
				this.persistAndNotify();
				return true;
			}
			/**
			* Move a card into a column, optionally at a specific position (before the
			* card with `beforeId`; undefined = column tail). The target column's sort
			* keys are renumbered; a same-column move is reorder-only.
			*
			* Moving a card to 'done' is the completion hand-off: any armed schedule
			* rule is disarmed outright ({@link disarmSchedule}) and every session rule
			* switches off ({@link disarmSessionRules}) — a completed task's
			* timer/chain must never fire again. Moving it back out of done (manual
			* drag or comment revive — one law, whichever hand) resets the spent run
			* budget but leaves the rules OFF until the user re-arms them.
			*
			* Automation never locks a card in place (see resolveCardDrop); leaving
			* the lane speaks its own language: a chain hand-off happens ONLY at a
			* settle — a manual move (except done) never disarms the rule and never
			* starts anything, so 完成后接续 stays armed and matches up ("跑到一半
			* 移动卡片不会误杀链").
			*/
			moveTask(id, status, beforeId) {
				const previous = this.tasks.find((task) => task.id === id);
				if (previous === void 0) return;
				const moved = applyCardOrder(this.tasks, id, status, beforeId, this.now());
				if (moved.find((task) => task.id === id) === previous) return;
				this.tasks = moved;
				this.tasks = this.tasks.map((task) => {
					if (task.id !== id) return task;
					if (status === "done") {
						if (status !== previous.status) this.lastDisarm = { task: previous };
						return disarmSessionRules(disarmSchedule(task, this.now()));
					}
					if (previous.status === "done" && task.schedule !== void 0) return {
						...task,
						schedule: {
							...task.schedule,
							runCount: 0
						}
					};
					return task;
				});
				if (this.lastDisarm !== void 0 && this.lastDisarm.task.id === id && status !== "done") this.lastDisarm = void 0;
				const after = this.tasks.find((task) => task.id === id);
				if (after !== void 0 && previous !== void 0 && previous.status === "backlog" && status === "todo" && after.schedule?.enabled === true && after.schedule.mode === "chain") this.runTask(id, "chain");
				this.persistAndNotify();
			}
			/**
			* Approve a review task from the notification center: mark it viewed and
			* move it to done — but never while any of its rounds is still open. An
			* approve mid-flight would yank the column AND hard-disarm the automation
			* driving the live sessions (moving to done is a hard stop, see
			* {@link moveTask}), so a busy card refuses untouched. The UI disables —
			* or, where the reason must stay reachable, explains — through this same
			* judgment; the card drop and the detail move buttons already enforce the
			* identical busy law at their own doors.
			* @param taskId - the task to approve.
			* @returns true when approved; false when unknown or busy (state untouched).
			*/
			approveTask(taskId) {
				const task = this.tasks.find((candidate) => candidate.id === taskId);
				if (task === void 0 || hasOpenRun(task)) return false;
				this.markTaskViewed(taskId);
				this.moveTask(taskId, "done");
				return true;
			}
			deleteTask(id) {
				this.tasks = this.tasks.filter((task) => task.id !== id);
				if (this.selectedTaskId === id) this.selectedTaskId = void 0;
				if (this.lastDisarm?.task.id === id) this.lastDisarm = void 0;
				this.persistAndNotify();
			}
			/** What the last `done` move disarmed, for the undo affordance. */
			undoableDisarmOf() {
				if (this.lastDisarm === void 0) return void 0;
				if (!this.tasks.some((task) => task.id === this.lastDisarm.task.id)) return void 0;
				return {
					taskId: this.lastDisarm.task.id,
					title: this.lastDisarm.task.title
				};
			}
			/**
			* Put a card back the way it was before the `done` move that disarmed it: the
			* stored snapshot is restored wholesale (status, order, schedule and session
			* rules together) and only `updatedAt` is refreshed — the ledger stamps a change
			* when it happens, and this is a change.
			*
			* Wholesale restore rather than a field-by-field inverse on purpose: the move
			* touched several things (`disarmSchedule`, `disarmSessionRules`) and any inverse
			* written by hand would be a second description of what the move does — the one
			* that drifts the first time the move changes. Restoring the snapshot cannot drift
			* from the move it undoes.
			*
			* The task is put back AT ITS OLD POSITION (any tasks that arrived after it stay
			* after it), so undo restores the list the user was looking at.
			*
			* @returns true when a DISARMED card was restored; false when there is nothing to
			*          undo (so the caller can keep the affordance honest).
			*/
			undoMoveToDone() {
				if (this.lastDisarm === void 0) return false;
				const snapshot = this.lastDisarm.task;
				if (this.tasks.find((task) => task.id === snapshot.id) === void 0) {
					this.lastDisarm = void 0;
					return false;
				}
				const restored = {
					...snapshot,
					updatedAt: this.now()
				};
				const without = this.tasks.filter((task) => task.id !== snapshot.id);
				const at = this.tasks.findIndex((task) => task.id === snapshot.id);
				const next = [
					...without.slice(0, at),
					restored,
					...without.slice(at)
				];
				this.tasks = next;
				this.lastDisarm = void 0;
				this.persistAndNotify();
				return true;
			}
			/**
			* Update a task's schedule rule. A blank or invalid cron expression is
			* rejected (returns false, state untouched) in cron mode. When the rule
			* ends up enabled the next run instant is computed immediately (cron); a
			* disabled rule carries no next-run instant. Arming makes a rule active at
			* once (no manual-first gate): cron fires on its next due instant through
			* the scheduler tick, and an armed chain in a drivable column starts its
			* first run right here.
			* @param id - the task to schedule.
			* @param patch - fields to change (absent fields keep their current value).
			*   `maxRuns` sets the total scheduled-run budget (undefined = unlimited);
			*   arming a schedule also resets its run counter when maxRuns changes.
			* @returns true when applied, false when rejected (invalid cron / unknown task).
			*/
			setSchedule(id, patch) {
				const task = this.tasks.find((candidate) => candidate.id === id);
				if (task === void 0) return false;
				const current = task.schedule;
				const mode = patch.mode ?? current?.mode ?? "cron";
				const cron = patch.cron !== void 0 ? patch.cron.trim() : current?.cron ?? "";
				if (mode === "cron" && (cron === "" || !isValidCron(cron))) return false;
				const enabled = patch.enabled ?? current?.enabled ?? false;
				if (enabled && mode === "chain" && !taskExecutable(task)) return false;
				const maxRunsChanged = patch.maxRuns !== void 0 && patch.maxRuns !== current?.maxRuns;
				const maxRuns = patch.maxRuns !== void 0 ? patch.maxRuns : current?.maxRuns;
				const nextRunAt = enabled && mode === "cron" ? nextRunAtMs(cron, this.now()) : void 0;
				this.tasks = this.tasks.map((candidate) => candidate.id === id ? withSchedule(candidate, {
					enabled,
					mode,
					cron,
					nextRunAt,
					...maxRunsChanged ? {
						maxRuns,
						runCount: 0
					} : {}
				}, this.now()) : candidate);
				this.persistAndNotify();
				if (enabled && mode === "chain" && taskExecutable(task) && !hasOpenRun(task)) this.runTask(id, "chain");
				return true;
			}
			/**
			* Auto launches waiting for a free slot. Only schedule/chain triggers ever
			* queue here: a manual run is an explicit user action that always starts
			* immediately (it still occupies a slot, so auto launches wait for the
			* budget it consumes). The trigger kind is not read at drain time
			* (re-validation re-derives eligibility), so only the task id is kept.
			*/
			queuedLaunches = [];
			/** How many rounds are genuinely open right now (the concurrency truth).
			*  ROUNDS, not cards: the budget bounds how many conversations run at once,
			*  and one card may legitimately own several of them at the same time (its
			*  own run plus a comment injected into a second bound session). Counting
			*  cards made 「并行数 3」 silently mean 「三张卡，每卡一条」. */
			inFlightCount() {
				let count = 0;
				for (const task of this.tasks) count += openRoundsOf(task).length;
				return count;
			}
			/** Reentrancy guard for {@link dispatch} (launches notify → re-dispatch). */
			dispatching = false;
			dispatchQueued = false;
			disposed = false;
			/**
			* Whether THIS replica holds the engine seat (the host lease). The engine
			* is the only replica that drives time-based automation and launches:
			* scheduler ticks, the dispatch pump, reconciliation, external-turn
			* recording and the settle hand-offs. A non-engine replica still serves
			* user actions (its writes sync; a Run relays to the engine) and mirrors
			* remote state, but never pumps — so many open boards cannot double-fire.
			* Defaults to true: the single-browser/localStorage mode is always the
			* engine, and the synced wiring only ever lowers it after the host says so.
			*/
			engine = true;
			/**
			* The one concurrency-bounded launch decision point. Called after every
			* ledger mutation (through {@link persistAndNotify}) and the cruise
			* switches: while the in-flight budget has room, it starts work in
			* priority order — queued schedule/chain launches first, then comment
			* continuations (a human instruction beats a fresh run), then cruise
			* pickups of todo tasks. Every candidate is re-validated at launch time,
			* so stale work (a task moved to done, deleted, or busy in between) is
			* dropped instead of launched; reentrant notifications are folded into
			* the current pass, never nested.
			*/
			dispatch() {
				if (this.disposed) return;
				if (!this.engine) return;
				if (this.dispatching) {
					this.dispatchQueued = true;
					return;
				}
				this.dispatching = true;
				this.dispatchQueued = false;
				try {
					let attempts = this.queuedLaunches.length;
					while (attempts-- > 0 && this.inFlightCount() < this.cruiseState.limit) if (!this.drainQueuedLaunch()) break;
					while (this.inFlightCount() < this.cruiseState.limit) {
						const next = this.nextEligible();
						if (next === void 0) break;
						if (next.kind === "comment") this.launchComment(next.task, next.round);
						else this.launchTask(next.task);
					}
				} finally {
					this.dispatching = false;
					if (this.dispatchQueued) this.dispatch();
				}
			}
			/**
			* Start the oldest queued schedule/chain launch, or drop it when it went
			* stale (task deleted, completed, paused, or busy). Returns whether a
			* request was consumed (so the caller keeps draining).
			*/
			drainQueuedLaunch() {
				const queued = this.queuedLaunches[0];
				if (queued === void 0) return false;
				this.queuedLaunches.shift();
				const task = this.tasks.find((candidate) => candidate.id === queued.taskId);
				if (task === void 0 || ruleReadiness(task).kind !== "active" || !taskExecutable(task)) return true;
				if (hasOpenRun(task)) {
					if (task.schedule?.mode === "chain") this.queuedLaunches.push(queued);
					return true;
				}
				this.launchTask(task);
				return true;
			}
			/**
			* The next runnable unit under the budget, or undefined: the earliest
			* eligible comment continuation across tasks (global FIFO by submission
			* time; a task's own comments always run in order because its first
			* uninjected round is the only one eligible while the task is drivable),
			* else the first todo task the cruise may pick up (skipping tasks that
			* still own queued comments — the comment has priority over a fresh run).
			* Comments and pickups only flow while the cruise is on; without it,
			* comments stay saved and todo tasks stay idle.
			*/
			nextEligible() {
				const head = (task, rule) => {
					if (openRoundsOf(task).some((round) => round.sessionId === void 0)) return void 0;
					const laneHead = /* @__PURE__ */ new Map();
					for (const round of task.executions) {
						if (round.comment === void 0 || round.sessionId === void 0) continue;
						if (round.injectedAt !== void 0 || round.endedAt !== void 0) continue;
						if (round.external === true) continue;
						const ahead = laneHead.get(round.sessionId);
						if (ahead === void 0 || round.startedAt < ahead.startedAt) laneHead.set(round.sessionId, round);
					}
					let best;
					for (const [sessionId, round] of laneHead) {
						if (round.ruleId !== void 0 !== rule) continue;
						if (sessionIsBusy(task, sessionId)) continue;
						if (best === void 0 || round.startedAt < best.startedAt) best = round;
					}
					return best;
				};
				const scan = (rule) => {
					let best;
					for (const task of this.tasks) {
						if (task.status === "done") continue;
						const round = head(task, rule);
						if (round !== void 0 && (best === void 0 || round.startedAt < best.round.startedAt)) best = {
							kind: "comment",
							task,
							round
						};
					}
					return best;
				};
				const ruleRound = scan(true);
				if (!this.cruiseState.enabled) return ruleRound;
				const best = ruleRound ?? scan(false);
				if (best !== void 0) return best;
				const todo = this.tasks.find((task) => {
					if (task.status !== "todo" || hasOpenRun(task)) return false;
					if (!taskExecutable(task)) return false;
					return !task.executions.some((candidate) => candidate.comment !== void 0 && candidate.injectedAt === void 0 && candidate.endedAt === void 0 && candidate.external !== true);
				});
				return todo !== void 0 ? {
					kind: "task",
					task: todo
				} : void 0;
			}
			/**
			* Launch a plain execution round: move the task to 'running', append the
			* execution record, and hand off to the ExecutionService. Automation no
			* longer awaits a manual first run — arming a schedule activates it at
			* once (see setSchedule / ruleReadiness) — so any trigger just starts.
			* THE 缺则补 supplement lives here (plus at every birth): every real
			* launch — manual, rerun, chain, cron, cruise pickup, queued auto run —
			* reads the supplemented record, so no launch path can run a card with an
			* empty head (its title names the fresh session).
			*/
			launchTask(task) {
				const { task: next, execution } = startExecution(this.supplementedTask(task), this.now(), this.uuid());
				const withExecution = this.tasks.map((candidate) => candidate.id === task.id ? next : candidate);
				this.tasks = promoteToColumnTop(withExecution, task.id, "running", this.now());
				this.persistAndNotify();
				this.activeExecutionIds.add(execution.id);
				this.deps.exec.run(next, execution, (event) => {
					this.handleExecutionEvent(event);
				});
			}
			/**
			* Inject one comment continuation: mark the round injected, move the task
			* to 'running', and send the text to the execution session (a fresh turn
			* in the same session, watched like a plain run). Only ever called by
			* {@link dispatch}, after eligibility was validated — or by a session
			* rule with `send: 'steer'`, which injects immediately (the same watched
			* round, only the handoff is now instead of the FIFO lane).
			*/
			launchComment(task, round, mode = "queue") {
				if (round.sessionId === void 0 || round.comment === void 0) return;
				const marked = {
					...round,
					injectedAt: this.now()
				};
				const running = withStatus({
					...task,
					executions: task.executions.map((candidate) => candidate.id === round.id ? marked : candidate)
				}, "running", this.now());
				this.tasks = promoteToColumnTop(this.tasks.map((candidate) => candidate.id === task.id ? running : candidate), task.id, "running", this.now());
				this.persistAndNotify();
				this.activeExecutionIds.add(round.id);
				this.deps.exec.commentRun(this.tasks.find((candidate) => candidate.id === task.id) ?? running, marked, round.sessionId, round.comment, (event) => {
					this.handleExecutionEvent(event);
				}, mode);
			}
			/**
			* Execute a task for real. A manual run starts immediately — an explicit
			* user action is never queued — and occupies a slot like any other round.
			* Auto triggers (cron due instants, chain hand-offs) start right away when
			* the in-flight budget has room and otherwise queue for {@link dispatch};
			* either way the call reports accepted so the scheduler rolls the schedule
			* forward exactly once. A second call while the task's latest run is still
			* open is ignored.
			*
			* A manual run is not a prerequisite for an armed rule: arming activates
			* the schedule at once (see setSchedule), so auto triggers drive the task
			* on their own — this door merely reports whether THIS launch was accepted.
			*/
			async runTask(id, trigger = "manual") {
				const task = this.tasks.find((candidate) => candidate.id === id);
				if (task === void 0) return false;
				if (!taskExecutable(task)) return false;
				if (hasOpenRun(task)) return false;
				if (!this.engine) {
					if (this.deps.requestLaunch === void 0) return false;
					this.deps.requestLaunch(id, trigger);
					return true;
				}
				if (trigger === "manual" || this.inFlightCount() < this.cruiseState.limit) {
					this.launchTask(task);
					return true;
				}
				if (!this.queuedLaunches.some((candidate) => candidate.taskId === id)) this.queuedLaunches.push({ taskId: id });
				return true;
			}
			/**
			* Continue an armed chain schedule after a settle: persist the incremented
			* counter (disarming after the final budgeted run) and start the next run.
			* The judgment is the CARD's own plain runs, not "the last row" — under
			* per-session lanes a comment or a native turn can be the newest record
			* while the card's execution finished earlier.
			*
			* Every attempt is guarded four ways: the card must be quiet (no lane in
			* flight), its last PLAIN run must have SUCCEEDED (失败不续), and the global
			* budget must have a free slot, and the prompt must be non-empty. An
			* emptied prompt holds the link without consuming budget (except the
			* final run, which still disarms). Any of those failing defers the hand-off
			* without consuming a run — and because the derivation is re-read from the
			* ledger, the next settle or the scheduler's recovery tick picks it up again
			* instead of losing the link.
			*/
			maybeContinueChain(id) {
				const task = this.tasks.find((candidate) => candidate.id === id);
				const schedule = task?.schedule;
				if (schedule === void 0 || !schedule.enabled || schedule.mode !== "chain") return;
				if (task === void 0) return;
				if (hasOpenRun(task)) return;
				const runs = plainRunsOf(task);
				const latest = runs[runs.length - 1];
				if (latest === void 0 || latest.endedAt === void 0 || latest.result !== "succeeded") return;
				if (schedule.maxRuns !== void 0 && schedule.runCount >= schedule.maxRuns) return;
				if (this.queuedLaunches.some((candidate) => candidate.taskId === id)) return;
				if (schedule.maxRuns !== void 0 && schedule.runCount + 1 >= schedule.maxRuns) {
					this.applyScheduleNextRun(id, void 0, this.now(), schedule.runCount + 1, true);
					return;
				}
				if (!taskExecutable(task)) return;
				this.applyScheduleNextRun(id, void 0, this.now(), schedule.runCount + 1, false);
				this.runTask(id, "chain");
			}
			/**
			* Roll a task's schedule forward (scheduler callback): persist the next due
			* instant, the trigger instant of this run, the incremented run counter,
			* and optionally disarm the schedule (after the final budgeted run).
			* No-op when the task has no schedule rule (it was deleted mid-tick, for
			* example).
			*/
			applyScheduleNextRun(id, nextRunAt, lastTriggeredAt, runCount, disable) {
				this.tasks = this.tasks.map((task) => task.id === id && task.schedule !== void 0 ? withSchedule(task, {
					nextRunAt,
					lastTriggeredAt,
					...runCount !== void 0 ? { runCount } : {},
					...disable === true ? { enabled: false } : {}
				}, this.now()) : task);
				this.persistAndNotify();
			}
			/**
			* Jump to an execution's session transcript. Selecting the session changes
			* `current`, which closes the board (the conversation view takes over).
			* Refuses to navigate when the session no longer exists (deleted/archived):
			* navigating a stale id would silently land on a fresh-session screen.
			* @param sessionId - the execution session to open.
			* @returns true when the session exists and the navigation was requested.
			*/
			openSession(sessionId) {
				if (!this.deps.sessions.exists(sessionId)) {
					console.warn(`[dsh-task-board] execution session ${sessionId} no longer exists; refusing to navigate`);
					return false;
				}
				this.deps.sessions.open(sessionId);
				return true;
			}
			/** Re-run a settled task: move it back to 'todo' first, then execute. */
			/**
			* Promote the task and start a fresh round, reporting whether the launch was
			* actually accepted — the same answer `runTask` gives.
			*
			* The return value is load-bearing: the detail sheet used to close BEFORE
			* firing, so a refused launch (empty prompt, or a round already open) looked
			* exactly like a successful one — the sheet vanished, nothing ran, and the
			* board said nothing. Callers that want the old fire-and-forget shape can
			* still ignore it; the UI keeps the sheet open and names the reason.
			*/
			async rerunTask(id) {
				const task = this.tasks.find((candidate) => candidate.id === id);
				if (task === void 0) return false;
				if (task.status === "done") this.moveTask(id, "todo");
				else if (task.status !== "running") {
					this.tasks = promoteToColumnTop(this.tasks, id, "todo", this.now());
					this.persistAndNotify();
				}
				return await this.runTask(id, "manual");
			}
			/** Whether the runtime offers the direct-message channel (the linked
			*  panel's composer is disabled without it). */
			directMessageAvailable() {
				return this.deps.sessionMessage !== void 0;
			}
			/**
			* Send one message directly to any native session — the linked-session
			* panel's composer. This is deliberately NOT the task-execution path: the
			* message is delivered to the native session exactly as if typed in its
			* own conversation (host `sessions.prompt` / command registry), never
			* creating execution records, never entering the dispatcher, never
			* touching task state, cruise, chain or schedules.
			*
			* The one recording: on success a DIRECT round is appended to the task —
			* the sent line becomes visible in the session's comment thread next to
			* drive comments and execution comments (one shared thread per session),
			* while remaining purely a record: never queued, never injected, never
			* driving the task.
			* @param taskId - the task owning the session (its thread records the line).
			* @param sessionId - the native session to address.
			* @param text - the message; a leading '/' routes through the command
			*   registry (unmatched lines fall back to plain text, matching the
			*   native composer's default sink).
			* @returns ok, or ok:false with an error string (faces absent, session
			*   gone, prompt rejected).
			*/
			sendSessionMessage(taskId, sessionId, text) {
				const trimmed = text.trim();
				if (trimmed === "") return Promise.resolve({
					ok: false,
					error: "empty message"
				});
				return this.sendRawMessage(sessionId, trimmed).then((result) => {
					if (!result.ok) return result;
					this.directGraceUntil.set(sessionId, this.now() + DIRECT_GRACE_MS);
					this.tasks = this.tasks.map((task) => task.id === taskId ? {
						...task,
						updatedAt: this.now(),
						executions: [...task.executions, newDirectRound({
							id: this.uuid(),
							now: this.now(),
							text: trimmed,
							sessionId
						})]
					} : task);
					this.persistAndNotify();
					return result;
				});
			}
			/**
			* Related-session labels of a task (for the composer's @ mention): the
			*  task's sessions, each with a native title (falling back to the raw id),
			*  de-duplicated in related-session order. */
			sessionLabelsOf(taskId) {
				const task = this.tasks.find((candidate) => candidate.id === taskId);
				if (task === void 0) return [];
				const byId = this.deps.sessions.list.getSnapshot().byId;
				const seen = /* @__PURE__ */ new Set();
				const out = [];
				for (const { sessionId } of this.relatedSessionsOf(task)) {
					if (sessionId === void 0 || seen.has(sessionId)) continue;
					seen.add(sessionId);
					const title = byId[sessionId]?.title;
					out.push({
						sessionId,
						title: typeof title === "string" && title !== "" ? title : sessionId
					});
				}
				return out;
			}
			/**
			* Steering send (插话): deliver a comment line to the session IMMEDIATELY —
			* slash-aware, recorded as a settled message round so it stays visible next
			* to queued comments in the same thread. It does NOT enter the dispatcher,
			* does NOT consume the concurrency budget and does NOT respect the cruise
			* gate: it is exactly "interrupt the current turn now" — the counterpart of
			* the default queued send (submitSessionComment). One message, two send
			* modes: queue (调度器注入) vs steer (现在直达).
			*/
			steerComment(taskId, sessionId, text) {
				return this.steerCommentWithImages(taskId, sessionId, text, void 0);
			}
			/** The attachment-carrying twin of steerComment: images (temporary bytes)
			*  and files (staged receipts) ride the same direct-send path as text. */
			steerCommentWithImages(taskId, sessionId, text, images, files) {
				const trimmed = text.trim();
				if (trimmed === "" && (images === void 0 || images.length === 0) && (files === void 0 || files.length === 0)) return Promise.resolve({
					ok: false,
					error: "empty message"
				});
				this.reviveTaskIfDone(taskId);
				return this.sendRawMessage(sessionId, trimmed, images, "steer", files).then((result) => {
					if (!result.ok) return result;
					this.directGraceUntil.set(sessionId, this.now() + DIRECT_GRACE_MS);
					this.tasks = this.tasks.map((task) => task.id === taskId ? {
						...task,
						updatedAt: this.now(),
						executions: [...task.executions, newDirectRound({
							id: this.uuid(),
							now: this.now(),
							text: trimmed,
							sessionId
						})]
					} : task);
					this.persistAndNotify();
					return { ok: true };
				});
			}
			/** Create a session rule for one of the task's sessions. Two triggers use
			*  the SAME model: cron (cron via the task schedule parser; an unparseable
			*  expression is rejected) and on-complete (no cron, fires at run settle —
			*  完成后续跑). Content mode: `usePrompt` sends the task's CURRENT execution
			*  prompt (绘画 = 定时/每次完成注入执行 Prompt 到单个会话); otherwise the
			*  custom `instruction` text is sent. ONE rule per session: a second rule
			*  for a session the task already automates is rejected outright — a
			*  session's automation is one definition (change it by editing), never a
			*  stack of conflicting triggers ("同时设 7 个" 不可能出现). */
			createSessionRule(taskId, input) {
				const instruction = input.instruction.trim();
				const usePrompt = input.usePrompt === true;
				if (instruction === "" && !usePrompt) return void 0;
				const trigger = input.trigger === "on-complete" ? "on-complete" : "cron";
				const cron = trigger === "cron" ? input.cron.trim() : "";
				const nextAt = trigger === "cron" ? nextRunAtMs(cron, this.now()) : void 0;
				if (trigger === "cron" && (cron === "" || nextAt === void 0)) return void 0;
				if (this.tasks.find((task) => task.id === taskId)?.rules?.some((rule) => rule.sessionId === input.sessionId)) return void 0;
				const rule = {
					id: this.uuid(),
					sessionId: input.sessionId,
					instruction: usePrompt ? "" : instruction,
					...usePrompt ? { usePrompt: true } : {},
					trigger,
					cron,
					send: input.send,
					enabled: true,
					...nextAt !== void 0 ? { nextAt } : {}
				};
				return this.userEdit(taskId, (task) => {
					if (task.rules?.some((candidate) => candidate.sessionId === input.sessionId) === true) return task;
					return withSessionRules(task, [...task.rules ?? [], rule]);
				}) ? rule : void 0;
			}
			/**
			* Edit an existing session rule: replace its target / instruction / content
			* mode / trigger / cron / send mode in place (the rule id and enable state
			* stay). An invalid patch (blank instruction for a custom rule, unparseable
			* cron, unknown rule) is rejected outright — the old rule is left
			* untouched, never half-applied. A cron change recomputes the due instant
			* from now (the rule restarts its schedule); switching to on-complete drops
			* the due slot and vice versa.
			* @returns true when the rule was updated.
			*/
			updateSessionRule(taskId, ruleId, patch) {
				const now = this.now();
				return this.userEdit(taskId, (task) => {
					if (task.rules === void 0) return task;
					let touched = false;
					const rules = task.rules.map((rule) => {
						if (rule.id !== ruleId) return rule;
						const usePrompt = patch.usePrompt === true;
						const instruction = usePrompt ? "" : (patch.instruction ?? rule.instruction).trim();
						const trigger = patch.trigger ?? rule.trigger;
						const cron = trigger === "cron" ? patch.cron?.trim() ?? rule.cron : "";
						const sessionId = patch.sessionId ?? rule.sessionId;
						const send = patch.send ?? rule.send;
						if (sessionId === "") return rule;
						if (!usePrompt && instruction === "") return rule;
						if (trigger === "cron") {
							if (cron === "" || cron !== rule.cron && nextRunAtMs(cron, now) === void 0) return rule;
						}
						const nextAt = trigger === "cron" ? cron !== rule.cron ? nextRunAtMs(cron, now) ?? rule.nextAt : rule.nextAt : void 0;
						const updated = {
							...rule,
							sessionId,
							instruction,
							trigger,
							cron,
							send,
							...usePrompt ? { usePrompt: true } : { usePrompt: void 0 },
							...nextAt !== void 0 ? { nextAt } : { nextAt: void 0 }
						};
						if (updated.sessionId === rule.sessionId && updated.instruction === rule.instruction && updated.trigger === rule.trigger && updated.usePrompt === rule.usePrompt && updated.cron === rule.cron && updated.send === rule.send && updated.nextAt === rule.nextAt) return rule;
						touched = true;
						return updated;
					});
					return touched ? withSessionRules(task, rules) : task;
				});
			}
			/** Toggle a session rule's enabled state (the row's live switch). Switching
			*  a cron rule back on recomputes its due slot from now (a disarmed rule
			*  carries no appointment — resuming from a stale slot would surprise-fire).
			*  Switching off clears the slots too (one off-shape everywhere: done-disarm
			*  and toggle-off persist identically, so no path can smuggle a stale slot
			*  back to life). */
			toggleSessionRule(taskId, ruleId, enabled) {
				const now = this.now();
				this.userEdit(taskId, (task) => {
					if (task.rules === void 0) return task;
					const current = task.rules;
					const rules = current.map((rule) => {
						if (rule.id !== ruleId) return rule;
						if (rule.enabled === enabled) return rule;
						if (!enabled) return {
							...rule,
							enabled,
							nextAt: void 0,
							lastAt: void 0
						};
						if (rule.trigger !== "cron") return {
							...rule,
							enabled
						};
						const nextAt = nextRunAtMs(rule.cron, now);
						if (nextAt === void 0) return rule;
						return {
							...rule,
							enabled,
							nextAt,
							lastAt: void 0
						};
					});
					if (!rules.some((rule, index) => rule !== current[index])) return task;
					return withSessionRules(task, rules);
				});
			}
			/** Remove a session rule. */
			deleteSessionRule(taskId, ruleId) {
				this.userEdit(taskId, (task) => {
					if (task.rules === void 0) return task;
					const rules = task.rules.filter((rule) => rule.id !== ruleId);
					if (rules.length === task.rules.length) return task;
					return withSessionRules(task, rules);
				});
			}
			/**
			* The minute heartbeat for CRON session rules (the scheduler's
			* sessionRulesTick): for every enabled cron rule whose due instant has
			* passed, send its preset instruction to the target session (slash-aware;
			* the sent line is recorded as a direct round so it shows in the session's
			* thread), then roll forward to the next cron match. A session that is
			* gone is skipped (its due slot is kept — it fires when the session
			* returns); an unparseable expression auto-disables the rule (错过即跳过),
			* never re-fires forever. On-complete rules have no due slot — they fire
			* at run settle (fireOnCompleteRules), never here.
			*/
			async tickSessionRules(now) {
				const byId = this.deps.sessions.list.getSnapshot().byId;
				const updates = /* @__PURE__ */ new Map();
				for (const task of this.tasks) {
					if (task.rules === void 0 || task.rules.length === 0) continue;
					const rules = task.rules.map((rule) => ({ ...rule }));
					let taskChanged = false;
					for (const rule of rules) {
						if (!rule.enabled || rule.trigger !== "cron") continue;
						if (rule.nextAt === void 0) continue;
						if (byId[rule.sessionId] === void 0) continue;
						if (!taskColumnAllowsAutomation(task)) continue;
						if (rule.nextAt > now) continue;
						const text = rule.usePrompt === true ? task.prompt.trim() : rule.instruction;
						if (text === "") {
							const rolled = nextSessionRuleAt(rule);
							rule.lastAt = now;
							rule.nextAt = rolled ?? rule.nextAt;
							rule.enabled = rolled === void 0 ? false : rule.enabled;
							taskChanged = true;
							continue;
						}
						if (task.executions.some((round) => round.ruleId === rule.id && round.endedAt === void 0)) {
							const rolled = nextSessionRuleAt(rule);
							rule.lastAt = now;
							rule.nextAt = rolled ?? rule.nextAt;
							rule.enabled = rolled === void 0 ? false : rule.enabled;
							taskChanged = true;
							continue;
						}
						let fired;
						if (rule.send === "queue") {
							if (this.queueRuleComment(task.id, rule.sessionId, text, text.trimStart().startsWith("/"), rule.id) === void 0) continue;
							fired = { ok: true };
						} else fired = await this.sendSessionMessage(task.id, rule.sessionId, text);
						if (!fired.ok) continue;
						taskChanged = true;
						const next = nextSessionRuleAt(rule);
						rule.lastAt = now;
						rule.nextAt = next ?? rule.nextAt;
						rule.enabled = next === void 0 ? false : rule.enabled;
					}
					if (taskChanged) {
						const latest = this.tasks.find((candidate) => candidate.id === task.id) ?? task;
						updates.set(task.id, withSessionRules(latest, rules));
					}
				}
				if (updates.size > 0) {
					this.tasks = this.tasks.map((task) => updates.get(task.id) ?? task);
					this.persistAndNotify();
				}
			}
			/** The raw host send for a direct line (slash-aware, no recording).
			*  Attachments are durable refs appended to the message content (images =
			*  temporary bytes, files = staged receipts); `mode` is the official
			*  prompt disposition (queue / steer). */
			sendRawMessage(sessionId, text, images, mode = "queue", files) {
				const direct = this.deps.sessionMessage;
				if (text.startsWith("/")) {
					const command = this.deps.sessionCommand;
					if (command !== void 0) return command(sessionId, text).then((result) => {
						if (!result.ok) return {
							ok: false,
							error: result.error
						};
						if (result.matched) return { ok: true };
						if (direct === void 0) return {
							ok: false,
							error: "direct message unavailable"
						};
						return direct(sessionId, text, images, mode, files);
					});
				}
				if (direct === void 0) return Promise.resolve({
					ok: false,
					error: "direct message unavailable"
				});
				return direct(sessionId, text, images, mode, files);
			}
			/**
			* Save a comment continuation against a settled execution: a new comment
			* round is appended (same session, not yet injected) and the task stays in
			* place. Comments are a per-SESSION FIFO queue — the lane is the
			* conversation: any number may be saved, and the shared dispatcher injects
			* them one at a time into THAT session (a session takes its next comment
			* only after its current round settles), while other sessions of the same
			* card keep running independently. The in-flight budget bounds how many
			* sessions run across the board. Injection only happens while the
			* auto-cruise is on; without it the comments stay saved (and cancellable)
			* until the cruise drives the board. A completed task cannot be commented
			* (its work is done); every other state can — a comment for a session that
			* is busy queues for when that session settles.
			*
			* A round with `command` set is a slash command, not a turn: the line is
			* executed through the native command registry when injected (unknown
			* commands fall back to plain text), matching the native composer's '/'
			* behavior. Every round records the execution it continues
			* (`parentExecutionId`), so the review page shows each execution's own
			* comments — never the whole task's.
			* @param taskId - the task owning the execution.
			* @param executionId - the settled execution to continue (its session is reused).
			* @param text - the comment to send to the session's agent.
			* @param command - whether the comment is a slash-command line.
			* @returns the queued comment round, or undefined when rejected (unknown
			*   task/execution, execution not settled).
			*/
			submitComment(taskId, executionId, text, command = false, images, files) {
				const trimmed = text.trim();
				if (trimmed === "") return void 0;
				const task = this.tasks.find((candidate) => candidate.id === taskId);
				if (task === void 0) return void 0;
				if (task.status === "done") this.reviveTaskIfDone(taskId);
				const execution = task.executions.find((candidate) => candidate.id === executionId);
				if (execution === void 0 || execution.sessionId === void 0 || execution.endedAt === void 0) return void 0;
				const round = newCommentRound({
					id: this.uuid(),
					now: this.now(),
					text: trimmed,
					command,
					sessionId: execution.sessionId,
					parentExecutionId: execution.id,
					...images !== void 0 && images.length > 0 ? { images } : {},
					...files !== void 0 && files.length > 0 ? { files } : {}
				});
				this.tasks = this.tasks.map((candidate) => candidate.id === taskId ? {
					...candidate,
					updatedAt: this.now(),
					executions: [...candidate.executions, round]
				} : candidate);
				this.persistAndNotify();
				return round;
			}
			/** A comment on a completed task revives it: moving the task back to 待办 is
			*  the SAME column transition as a drag (the schedule re-arms per column
			*  rules), so the comment drives the task instead of hitting a dead end. */
			reviveTaskIfDone(taskId) {
				const task = this.tasks.find((candidate) => candidate.id === taskId);
				if (task === void 0 || task.status !== "done") return;
				this.moveTask(taskId, "todo");
			}
			/**
			* Save a comment continuation against a linked session — the drive-mode
			* composer of the linked-session panel. Semantics are identical to
			* {@link submitComment}: the session-anchored round enters the task's
			* per-task FIFO comment queue and the shared dispatcher injects it into
			* the linked session through the same concurrency budget, exactly like a
			* comment submitted from an execution's review page. The only difference
			* is the anchor (`sessionAnchor` instead of `parentExecutionId`), which
			* puts the round in the linked session's own comment thread and reuses
			* that session instead of a settled execution's. A completed task is
			* REVIVED by its comment (moved back to 待办) — the comment drives it
			* instead of being a dead end; every other state queues as usual.
			* @param taskId - the task owning the linked session.
			* @param sessionId - the linked session to continue (never created).
			* @param text - the comment to send to the session's agent.
			* @param command - whether the comment is a slash-command line.
			* @returns the queued comment round, or undefined when rejected (unknown
			*   task).
			*/
			submitSessionComment(taskId, sessionId, text, command = false, images, files) {
				const trimmed = text.trim();
				if (trimmed === "") return void 0;
				const task = this.tasks.find((candidate) => candidate.id === taskId);
				if (task === void 0) return void 0;
				if (task.status === "done") this.reviveTaskIfDone(taskId);
				return this.queueRuleComment(taskId, sessionId, trimmed, command, void 0, void 0, images, files);
			}
			/**
			* Session-RULE enqueue: the same session-anchored round as
			* {@link submitSessionComment} marked with the rule's id. A rule's content
			* (a custom instruction, or the task's own execution prompt for a usePrompt
			* rule — already validated by the readiness judgment) IS the work, so the
			* task's prompt never mis-blocks it ("对单个会话发指令" 与任务 Prompt 无关);
			* same for a user comment: a comment is human words, never a task
			* execution, so no prompt gate applies to any comment path. Every other
			* guard stays: done revival, blank text, unknown task. `ruleId` marks the
			* round as a rule's own turn — a succeeded settle of such a round re-fires
			* its on-complete rule (完成后续跑 loop); a user comment carries none and
			* never loops.
			*/
			queueRuleComment(taskId, sessionId, text, command = false, ruleId, injectedAt, images, files) {
				const trimmed = text.trim();
				if (trimmed === "") return void 0;
				const task = this.tasks.find((candidate) => candidate.id === taskId);
				if (task === void 0) return void 0;
				if (task.status === "done") this.reviveTaskIfDone(taskId);
				const round = {
					...newCommentRound({
						id: this.uuid(),
						now: this.now(),
						text: trimmed,
						command,
						sessionId,
						sessionAnchor: sessionId,
						...ruleId !== void 0 ? { ruleId } : {},
						...images !== void 0 && images.length > 0 ? { images } : {},
						...files !== void 0 && files.length > 0 ? { files } : {}
					}),
					...injectedAt !== void 0 ? { injectedAt } : {}
				};
				this.tasks = this.tasks.map((candidate) => candidate.id === taskId ? {
					...candidate,
					updatedAt: this.now(),
					executions: [...candidate.executions, round]
				} : candidate);
				this.persistAndNotify();
				return round;
			}
			/**
			* Cancel a comment round that has not been injected yet: removes it from
			* the task (a saved or queued comment is editable by deleting it and
			* writing a new one). A round that was already injected (the session is
			* running it) or settled cannot be cancelled.
			* @param executionId - the pending comment round's id.
			* @returns true when the round was removed.
			*/
			cancelComment(executionId) {
				const task = this.tasks.find((candidate) => candidate.executions.some((execution) => execution.id === executionId));
				if (task === void 0) return false;
				const round = task.executions.find((candidate) => candidate.id === executionId);
				if (round === void 0 || round.comment === void 0) return false;
				if (round.injectedAt !== void 0 || round.endedAt !== void 0) return false;
				this.tasks = this.tasks.map((candidate) => candidate.id === task.id ? {
					...candidate,
					updatedAt: this.now(),
					executions: candidate.executions.filter((execution) => execution.id !== executionId)
				} : candidate);
				this.persistAndNotify();
				return true;
			}
			/**
			* Start (or continue) a backlog task's requirement refinement: launch a
			* refine round in the task's refine session, created lazily through the
			* same session machinery as executions and inheriting the task's run
			* configuration (workspace/model/effort/permission — nothing extra to
			* configure). The first round sends the built-in refine instruction (the
			* agent researches with its own tools, asks the user anything unclear, and
			* delivers a ready-to-run prompt); later rounds are the user's answers.
			* @param taskId - the backlog task to refine.
			* @param english - whether to write the refine instruction in English.
			* @returns true when a round was launched.
			*/
			startRefine(taskId, english = false) {
				const task = this.tasks.find((candidate) => candidate.id === taskId);
				if (task === void 0 || task.status !== "backlog" || hasOpenRun(task)) return false;
				if (!refinable(task)) return false;
				const round = {
					id: this.uuid(),
					sessionId: task.refineSessionId,
					startedAt: this.now(),
					endedAt: void 0,
					result: void 0,
					error: void 0,
					refine: true
				};
				this.tasks = this.tasks.map((candidate) => candidate.id === taskId ? {
					...candidate,
					updatedAt: this.now(),
					executions: [...candidate.executions, round]
				} : candidate);
				this.persistAndNotify();
				this.activeExecutionIds.add(round.id);
				const launchTask = this.tasks.find((candidate) => candidate.id === taskId);
				if (launchTask === void 0) return true;
				this.deps.exec.run(launchTask, round, (event) => {
					this.handleExecutionEvent(event);
				}, {
					prompt: buildRefinePrompt(launchTask, english),
					sessionId: task.refineSessionId,
					fresh: task.refineSessionId === void 0,
					renameTo: `${task.title} · 完善需求`
				});
				return true;
			}
			/**
			* Send the user's answer into the task's refine session (the AI asked and
			* is waiting): launch a refine round with the answer text, delivered
			* immediately — the session is already counted in-flight, so answers never
			* queue behind the cruise gate or the comment FIFO.
			* @param taskId - the task whose refine session receives the answer.
			* @param text - the answer text.
			* @returns true when the answer was launched.
			*/
			answerRefine(taskId, text, images, files) {
				const trimmed = text.trim();
				if (trimmed === "") return false;
				const task = this.tasks.find((candidate) => candidate.id === taskId);
				if (task === void 0 || task.refineSessionId === void 0) return false;
				const round = {
					id: this.uuid(),
					sessionId: task.refineSessionId,
					startedAt: this.now(),
					endedAt: void 0,
					result: void 0,
					error: void 0,
					refine: true
				};
				this.tasks = this.tasks.map((candidate) => candidate.id === taskId ? {
					...candidate,
					updatedAt: this.now(),
					executions: [...candidate.executions, round]
				} : candidate);
				this.persistAndNotify();
				this.activeExecutionIds.add(round.id);
				const launchTask = this.tasks.find((candidate) => candidate.id === taskId);
				if (launchTask === void 0) return true;
				this.deps.exec.run(launchTask, round, (event) => {
					this.handleExecutionEvent(event);
				}, {
					prompt: trimmed,
					...images !== void 0 && images.length > 0 ? { images } : {},
					...files !== void 0 && files.length > 0 ? { files } : {},
					sessionId: task.refineSessionId,
					fresh: false,
					renameTo: `${task.title} · 完善需求`
				});
				return true;
			}
			/**
			* Write a refined prompt onto the task (the user confirms the text shown
			* in the refine panel; nothing is ever applied automatically).
			* @param taskId - the task to update.
			* @param prompt - the refined execution prompt text.
			* @returns true when the task was updated.
			*/
			applyRefineResult(taskId, prompt) {
				const trimmed = prompt.trim();
				if (this.tasks.find((candidate) => candidate.id === taskId) === void 0 || trimmed === "") return false;
				this.tasks = this.tasks.map((candidate) => candidate.id === taskId ? {
					...candidate,
					prompt: trimmed,
					updatedAt: this.now()
				} : candidate);
				this.persistAndNotify();
				return true;
			}
			/** Turn the auto-cruise on or off (persisted) — a MANUAL toggle: it flips
			*  `enabled` directly and NEVER writes the scheduled windows, so clicking
			*  开启/关闭 repeatedly cannot accumulate window records. Scheduled window
			*  boundaries still flip the state (预约语义). On re-pumps the dispatch
			*  queue immediately — pending comments and todo pickups start under the
			*  concurrency budget, comments first. */
			setCruiseEnabled(on) {
				const next = applyManualToggle(this.cruiseState, on);
				if (next === this.cruiseState) return;
				this.cruiseState = next;
				this.deps.cruiseStorage?.write(this.cruiseState);
				if (on) this.dispatch();
				this.notify();
			}
			/** Change the concurrency budget (persisted; the dispatcher re-pumps). The
			*  one clamp lives in board-doc ({@link clampCruiseLimit}) — writes and
			*  reads share it. Non-finite input is ignored (a NaN write must never
			*  clear the budget). */
			setCruiseLimit(limit) {
				if (!Number.isFinite(limit)) return;
				const clamped = clampCruiseLimit(limit);
				if (this.cruiseState.limit === clamped) return;
				this.cruiseState = {
					...this.cruiseState,
					limit: clamped
				};
				this.deps.cruiseStorage?.write(this.cruiseState);
				this.dispatch();
				this.notify();
			}
			/** Mirror the scheduler's Forbid-policy skip ledger into the snapshot (the
			*  wiring calls this from the scheduler's `onSkips` sink). Cumulative and
			*  read-only: skips are telemetry, never persisted, never retried. */
			setSchedulerSkips(stats) {
				if (this.schedulerSkips.overlap === stats.overlap && this.schedulerSkips.missed === stats.missed) return;
				this.schedulerSkips = {
					overlap: stats.overlap,
					missed: stats.missed
				};
				this.notify();
			}
			/** Mirror the scheduler heartbeat stamp into the snapshot (the wiring calls
			*  this from the scheduler's `onHeartbeat` sink). Volatile telemetry only:
			*  never persisted, never synced, never part of `applyRemote`. */
			setSchedulerHeartbeat(okAt) {
				if (this.schedulerHeartbeatOkAt === okAt) return;
				this.schedulerHeartbeatOkAt = okAt;
				this.notify();
			}
			/** Replace the cruise's scheduled windows (the editor's add/remove path).
			*  The effective state is recomputed at once against the NEW list: a window
			*  may now cover the present (cruise turns on), or the covering window may
			*  have just been removed (cruise turns off). Expired-window pruning is the
			*  heartbeat's job (tickCruise), so a just-added window is never yanked
			*  before its first tick. */
			setCruiseSchedule(windows) {
				const next = setCruiseSchedule(this.cruiseState, windows, this.now());
				if (next === this.cruiseState) return;
				this.cruiseState = next;
				this.deps.cruiseStorage?.write(this.cruiseState);
				if (this.cruiseState.enabled) this.dispatch();
				this.notify();
			}
			/** The scheduler heartbeat for cruise windows (每分钟): a window's start
			*  instant flips the cruise ON, its end instant flips it OFF; fully-past
			*  windows are pruned and persisted away — "到点自动开启 / 到点自动关闭",
			*  过期记录自动消失. Persist and pump dispatch when anything changed. */
			tickCruise(now) {
				if (!this.engine) return;
				const next = tickCruise(this.cruiseState, now);
				if (next === this.cruiseState) return;
				const turnedOn = next.enabled && !this.cruiseState.enabled;
				this.cruiseState = next;
				this.deps.cruiseStorage?.write(this.cruiseState);
				if (turnedOn) this.dispatch();
				this.notify();
			}
			handleExecutionEvent(event) {
				if (this.disposed) return;
				if (event.kind === "started") {
					this.tasks = this.tasks.map((task) => task.id === event.taskId ? attachSessionId(task, event.executionId, event.sessionId, this.now()) : task);
					if (this.tasks.some((task) => task.id === event.taskId && task.executions.some((round) => round.id === event.executionId && round.refine === true))) this.tasks = this.tasks.map((task) => task.id === event.taskId ? withRefineSession(task, event.sessionId, this.now()) : task);
					this.persistAndNotify();
					return;
				}
				this.activeExecutionIds.delete(event.executionId);
				const before = this.tasks.find((task) => task.id === event.taskId);
				const settledRound = before?.executions.find((round) => round.id === event.executionId);
				this.tasks = this.tasks.map((task) => task.id === event.taskId ? this.settleRound(task, event.executionId, event.outcome, event.error) : task);
				const after = this.tasks.find((task) => task.id === event.taskId);
				if (before !== void 0 && after !== void 0 && before.status !== after.status) this.tasks = promoteToColumnTop(this.tasks, event.taskId, after.status, this.now());
				if (this.engine) this.maybeContinueChain(event.taskId);
				this.persistAndNotify();
				if (this.engine) this.settledFollowUp(settledRound, event.taskId, event.outcome);
			}
			/**
			* EVERY way a round can settle runs the SAME post-settle appointments —
			* the live run watch ({@link handleExecutionEvent}) and the recovery /
			* background reconcile ({@link reconcileRunningTasks}): a completion found
			* after a page reload, a missed list flip or through a cold session must
			* still keep automation alive ("任务完成了一次却没有任何反应" 正是这个缺口).
			* 1. on-complete rules (fireOnCompleteRules — ANY completion is the
			*    任务完成 appointment: a plain run, a user comment round or a native/
			*    external turn all landed the card in 「待审核」; refine rounds are
			*    preparation, never a completion, and the rule's OWN round is excluded
			*    — its loop is the dedicated fireLoopRule hook, so a settle never
			*    double-fires; the one-in-flight guard is the second backstop);
			* 2. the rule's own loop (fireLoopRule — a ruleId round's succeeded settle
			*    continues 完成后继续; failure/cancel never does).
			* The chain hand-off stays OUTSIDE (both call sites run it BEFORE their
			* persist — a freed slot is booked before any comment/cruise work competes
			* for it).
			*/
			settledFollowUp(settledRound, taskId, outcome) {
				if (settledRound?.refine !== true && settledRound?.ruleId === void 0) this.fireOnCompleteRules(taskId);
				if (settledRound?.ruleId !== void 0 && outcome === "succeeded") this.fireLoopRule(taskId, settledRound.ruleId, settledRound.sessionId ?? "");
			}
			/**
			* 完成后续跑（on-complete 循环规则）的一次发送：一条带 ruleId 标记的可观察
			* 指令轮（注入成功结算后再触发）。发送只有一种文法——进入自动化车道（下一轮
			* 可用即注入、预算内排队，与巡航开关无关）；queue/steer 只决定同一完成时刻
			* 的发送次序（插话先发，排队按序——车道内一律 FIFO + 预算，绝不瞬时齐发，
			* 也绝不饿死任何一条）。**一条规则同时在途至多一轮**：规则已有未结算的指令轮
			* （在跑或排队中）时跳过本次发送——任何一次任务完成都不会给同一规则叠第二个
			* 轮子，「轮子在跑」就是「继续进行中」；它结算成功时 fireLoopRule 续上下一轮。
			* 「关闭/删除/会话消失/内容不可用」= 停止；失败/取消 = 不续。
			*/
			fireRuleRound(task, rule, text) {
				if (task.executions.some((candidate) => candidate.ruleId === rule.id && candidate.endedAt === void 0)) return;
				const round = this.queueRuleComment(task.id, rule.sessionId, text, text.trimStart().startsWith("/"), rule.id, rule.send === "steer" ? this.now() : void 0);
				if (round === void 0) return;
				this.tasks = this.tasks.map((candidate) => candidate.id === task.id ? withSessionRules(candidate, (candidate.rules ?? []).map((candidateRule) => candidateRule.id === rule.id ? {
					...candidateRule,
					lastAt: this.now()
				} : candidateRule)) : candidate);
				this.persistAndNotify();
				if (rule.send === "steer") {
					const current = this.tasks.find((candidate) => candidate.id === task.id);
					const currentRound = current?.executions.find((candidate) => candidate.id === round.id);
					if (current !== void 0 && currentRound !== void 0) this.launchComment(current, currentRound, "steer");
				}
			}
			/**
			* 任务一次执行结算时触发其 on-complete 会话规则（"完成后续跑"——永续循环：
			* 规则轮成功结算后继续下一轮，直到关闭/删除/会话消失/内容不可用）。发送文法
			* = 一条 ruleId 标记的观察轮（queue/steer，与巡航无关）；判定 = 规则启用 +
			* 内容可得（usePrompt 规则要求任务执行 Prompt 非空；自定义规则内容自带）+
			* 目标会话在场；每次结算每个规则至多一次（lastAt 由 fireRuleRound 记录）。
			* 列暂停不适用：结算瞬间任务刚被移动，这里的"完成"才是约定本身。
			*/
			async fireOnCompleteRules(taskId) {
				const task = this.tasks.find((candidate) => candidate.id === taskId);
				if (task === void 0 || task.rules === void 0 || task.rules.length === 0) return;
				const byId = this.deps.sessions.list.getSnapshot().byId;
				const due = [];
				for (const rule of task.rules) {
					if (rule.trigger !== "on-complete" || !rule.enabled) continue;
					const text = rule.usePrompt === true ? task.prompt.trim() : rule.instruction;
					if (text === "") continue;
					if (byId[rule.sessionId] === void 0) continue;
					due.push({
						rule,
						text
					});
				}
				for (const { rule, text } of [...due].sort((a, b) => (a.rule.send === "steer" ? 0 : 1) - (b.rule.send === "steer" ? 0 : 1))) this.fireRuleRound(task, rule, text);
			}
			/**
			* 完成后续跑：规则自己的指令轮（ruleId 标记）**成功**结算后的再触发——同一
			* 规则再发一条，一轮接一轮；失败/取消不续（错误不风暴）、规则被关/会话消失/
			* 内容不可用即停；用户手写评论（无 ruleId）永不触发。
			*/
			async fireLoopRule(taskId, ruleId, sessionId) {
				const task = this.tasks.find((candidate) => candidate.id === taskId);
				const rule = task?.rules?.find((candidate) => candidate.id === ruleId);
				if (task === void 0 || rule === void 0 || rule.trigger !== "on-complete" || !rule.enabled) return;
				if (rule.sessionId !== sessionId) return;
				const text = rule.usePrompt === true ? task.prompt.trim() : rule.instruction;
				if (text === "") return;
				if (this.deps.sessions.list.getSnapshot().byId[sessionId] === void 0) return;
				this.fireRuleRound(task, rule, text);
			}
			/** Settle a round with the rule its kind demands: refine rounds keep the
			*  task in its column (settlement of a plain run may move the card). */
			settleRound(task, executionId, outcome, error) {
				if (task.executions.find((candidate) => candidate.id === executionId)?.refine === true) return settleRefine(task, executionId, outcome, this.now(), error);
				const next = settleExecution(task, executionId, outcome, this.now(), error);
				if (next !== task) console.info(`[dsh-task-board] settled task=${task.id} round=${executionId} outcome=${outcome} ${task.status}->${next.status}${error !== void 0 ? ` error=${error}` : ""}`);
				return next;
			}
			/** Reconcile running tasks and close the board when the user navigates. */
			onSessionsChanged() {
				this.scheduleReconcile();
				if (!this.boardOpen) return;
				const current = currentOf(this.deps.sessions);
				if (current !== this.lastCurrent) this.closeBoard();
				this.lastCurrent = current;
				this.notify();
			}
			lastCurrent = void 0;
			/** Execution ids launched on this page; they settle via their live watch, never list reconciliation. */
			activeExecutionIds = /* @__PURE__ */ new Set();
			/** One entry per card (the steer round whose completion was already
			*  reported), replaced on the next steer — bounded by the card count. */
			directFallbackRounds = /* @__PURE__ */ new Map();
			/** Debounce timer for {@link reconcileRunningTasks}. */
			reconcileTimer = void 0;
			/** Whether a reconcile pass is underway (single-flight guard). */
			reconcileInFlight = false;
			/**
			* A session-list change arrived but no pass ran yet (debounce pending or a
			* pass in flight). Accumulated instead of dropped so the final
			* running→finished flip of an execution session is never lost.
			*/
			reconcilePending = false;
			/** Native-activity detection state (see session-activity.ts): observed
			*  running baselines per session, when external rounds were created, and
			*  which sessions' CURRENT run periods are already consumed. */
			activityBook = {
				running: /* @__PURE__ */ new Map(),
				externalSince: /* @__PURE__ */ new Map(),
				recorded: /* @__PURE__ */ new Set()
			};
			/** Latest wake stamp per session (see recordActivityWake): a stamp advance
			*  is a turn the status edge may have missed — the next reconcile pass
			*  re-checks the session even when its running flag did not move. */
			activityWake = /* @__PURE__ */ new Map();
			/** Sessions whose current turn the board itself recorded (a direct-send):
			*  they must not re-trigger external detection while in grace. */
			directGraceUntil = /* @__PURE__ */ new Map();
			/**
			* Debounce + single-flight trigger for the running-task reconciliation.
			* Session-list notifications arrive in bursts (one per session status
			* change); both guards together keep a burst from reading the history API
			* once per running task. Notifications that arrive while a pass is queued
			* or running are accumulated ({@link reconcilePending}) and re-checked
			* after the pass, so none is silently dropped.
			*/
			scheduleReconcile() {
				if (!this.engine) return;
				this.reconcilePending = true;
				if (this.reconcileTimer !== void 0 || this.reconcileInFlight) return;
				this.reconcileTimer = setTimeout(() => {
					this.reconcileTimer = void 0;
					if (this.disposed) return;
					this.reconcileRunningTasks();
				}, this.deps.reconcileDebounceMs ?? 350);
			}
			/** How old an execution must be before list reconciliation may settle it. */
			static ACTIVE_RECONCILE_GRACE_MS = 1e4;
			/** The delivery watchdog deadline: an OPEN round whose session has produced
			*  no turn evidence and has sat idle this long is a round that never
			*  reached the agent (a lost inject, a session that vanished mid-flight).
			*  Left open it would hold a concurrency slot AND swallow every future
			*  native turn of that session (hasOpenRoundOn gates external recording),
			*  which is why 「行亮进行中、卡片永远不动」 used to recur forever. The
			*  watchdog releases it as cancelled, and external detection re-arms. */
			static OPEN_ROUND_WATCHDOG_MS = 3 * 6e4;
			/**
			* The watchdog verdict for one open round: a synthetic `cancelled` settle
			* when the round is past the deadline and its session is NOT running (or
			* gone), undefined while any evidence could still arrive (still connecting,
			* session still working, deadline not reached). Never judges a round whose
			* session is actively working — a long run is not a zombie.
			*/
			zombieRoundEvent(task, execution) {
				const since = execution.injectedAt ?? execution.startedAt;
				if (this.now() - since <= BoardController.OPEN_ROUND_WATCHDOG_MS) return void 0;
				if (execution.sessionId === void 0) return {
					kind: "settled",
					taskId: task.id,
					executionId: execution.id,
					outcome: "cancelled",
					error: "run never reached a session"
				};
				const summary = this.deps.sessions.list.getSnapshot().byId[execution.sessionId];
				if (summary?.running === true) return void 0;
				if (summary !== void 0 && summary.pendingInteraction !== void 0) return void 0;
				return {
					kind: "settled",
					taskId: task.id,
					executionId: execution.id,
					outcome: "cancelled",
					error: "no turn evidence"
				};
			}
			/** Settle tasks left 'running' whose sessions already finished. */
			async reconcileRunningTasks() {
				if (this.disposed || this.reconcileInFlight || !this.engine) return;
				this.reconcileInFlight = true;
				this.reconcilePending = false;
				try {
					let changed = await this.scanExternalActivity();
					const linkedIds = this.tasks.map((task) => ({
						task,
						ids: this.linkedOf(task).map((row) => row.sessionId)
					}));
					if (this.driveLiveStates(linkedIds)) changed = true;
					const events = [];
					for (const task of this.tasks) for (const execution of openRoundsOf(task)) {
						if (!(task.status === "running" || execution.refine === true)) {
							const zombie = this.zombieRoundEvent(task, execution);
							if (zombie !== void 0) events.push({
								taskId: task.id,
								round: execution,
								event: zombie,
								externalText: void 0
							});
							continue;
						}
						if (task.status === "running" && this.activeExecutionIds.has(execution.id)) {
							const sessionId = execution.sessionId;
							if (sessionId === void 0) continue;
							const summary = this.deps.sessions.list.getSnapshot().byId[sessionId];
							const finished = summary !== void 0 && !summary.running;
							const pastGrace = this.now() - execution.startedAt > BoardController.ACTIVE_RECONCILE_GRACE_MS;
							if (!(finished && pastGrace)) continue;
						}
						const event = await this.deps.exec.reconcile(task, execution.id);
						if (this.disposed) return;
						if (event !== void 0 && event.kind === "settled") {
							const externalText = await this.externalTextIfNeeded(task, event.executionId);
							events.push({
								taskId: task.id,
								round: execution,
								event,
								externalText
							});
							this.activeExecutionIds.delete(event.executionId);
						} else {
							const zombie = this.zombieRoundEvent(task, execution);
							if (zombie !== void 0) events.push({
								taskId: task.id,
								round: execution,
								event: zombie,
								externalText: void 0
							});
						}
					}
					const continued = [];
					const applied = [];
					for (const { taskId, round, event, externalText } of events) {
						const current = this.tasks.find((candidate) => candidate.id === taskId);
						if (current === void 0) continue;
						const next = this.settleRound(current, event.executionId, event.outcome, event.error);
						if (next === current) continue;
						const settled = externalText === void 0 ? next : {
							...next,
							executions: next.executions.map((candidate) => candidate.id === event.executionId && candidate.external === true && (candidate.comment === void 0 || candidate.comment === "") ? {
								...candidate,
								comment: externalText
							} : candidate)
						};
						this.tasks = this.tasks.map((candidate) => candidate.id === taskId ? settled : candidate);
						changed = true;
						continued.push(taskId);
						applied.push({
							round,
							event
						});
					}
					if (this.engine) for (const id of continued) this.maybeContinueChain(id);
					if (this.cancelSpuriousExternal()) changed = true;
					if (changed) this.persistAndNotify();
					for (const { round, event } of applied) {
						if (this.disposed) return;
						if (!this.engine) return;
						this.settledFollowUp(round, event.taskId, event.outcome);
					}
				} finally {
					this.reconcileInFlight = false;
					if (this.reconcilePending) {
						this.reconcilePending = false;
						this.scheduleReconcile();
					}
				}
			}
			/**
			* Drive task status from the native live state for EVENTLESS rounds only:
			* a direct steer (插话) is settled at birth — no turn/end settle event
			* exists — so its life is entirely the session's running flip. When the
			* agent works, the task joins 进行中 (like any real execution); when the
			* session stops, the steer's completion is a completion: it lands in
			* 待审核 and goes through the SAME settledFollowUp appointment (on-complete
			* rules, chain hand-off) as a watched settle. Board-open rounds and
			* refine/external rounds keep their own event paths; this pass never
			* settles anything twice (isDirectLike is only true for already-settled
			* direct rounds, and the fallback fires exactly once — status was
			* 'running' before the transition).
			*/
			driveLiveStates(linked) {
				const byId = this.deps.sessions.list.getSnapshot().byId;
				const linkedIdsOf = (taskId) => linked?.find((entry) => entry.task.id === taskId)?.ids;
				let changed = false;
				for (const task of this.tasks) {
					const latest = newestDirectLike(task);
					if (latest === void 0) continue;
					const live = taskLiveStateOf(task, (sessionId) => byId[sessionId]?.running === true, (sessionId) => byId[sessionId]?.pendingInteraction, linkedIdsOf(task.id));
					const now = this.now();
					if (live === "running") {
						if (task.status !== "running") {
							this.tasks = promoteToColumnTop(this.tasks.map((candidate) => candidate.id === task.id ? withStatus(candidate, "running", now) : candidate), task.id, "running", now);
							changed = true;
						}
					} else if (live === "idle" && task.status === "running" && !hasOpenRun(task)) {
						if (this.directFallbackRounds.get(task.id) === latest.id) continue;
						this.directFallbackRounds.set(task.id, latest.id);
						const next = withStatus(task, DIRECT_FALLBACK_STATUS, now);
						this.tasks = this.tasks.map((candidate) => candidate.id === task.id ? next : candidate);
						changed = true;
						if (!this.disposed) this.settledFollowUp(latest, task.id, "succeeded");
					}
				}
				return changed;
			}
			/** THE live-state question for one task (card breathing source): waiting >
			*  running > idle — same single derivation for every surface. The related
			*  set is the task's OWN sessions (refine + explicit binds + execution
			*  rounds; a workspace bind contributes none), so the card and its rows
			*  always answer the same question from the same set. */
			liveStateOf(taskId) {
				const task = this.tasks.find((candidate) => candidate.id === taskId);
				if (task === void 0) return "idle";
				const byId = this.deps.sessions.list.getSnapshot().byId;
				return taskLiveStateOf(task, (sessionId) => byId[sessionId]?.running === true, (sessionId) => byId[sessionId]?.pendingInteraction, this.linkedOf(task).map((row) => row.sessionId));
			}
			/** Whether one session is genuinely running right now (the native truth). */
			nativeRunningOf(sessionId) {
				return this.deps.sessions.list.getSnapshot().byId[sessionId]?.running === true;
			}
			/**
			* Every related session of a task (de-duplicated, refine first) — THE one
			* derivation from task-live.ts, consumed by the external-activity scanner,
			* the bound-task reconcile and the '@' reference scoping. The controller
			* only supplies the linked ids (explicit session binds); everything else
			* (binds, execution rounds, refine session) is pure task shape.
			*/
			relatedSessionsOf(task) {
				return relatedSessionIdsOf(task, this.linkedOf(task).map((row) => row.sessionId));
			}
			/** The ids of every session already RELATED to a task (binds, execution
			*  rounds, the refine session, live linked members) — the single source for
			*  "do not offer this session again". The add-session picker filters on this,
			*  so a refine session (which the old hand-rolled bind+execution set missed)
			*  never shows as a re-bindable candidate. */
			relatedSessionIdSet(task) {
				return new Set(this.relatedSessionsOf(task).map((row) => row.sessionId));
			}
			/**
			* Detect out-of-band activity on related sessions (see session-activity.ts)
			* and record external rounds: the round enters the session's comment thread,
			* a non-refine round moves the card to 「进行中」, a refine round keeps the
			* column but turns `refining` on. The round body is the user's native
			* message text captured at observation (so the thread shows what was said)
			* and the round carries the message's seq as its TURN ANCHOR — the dedup
			* key shared with the live frame channel (recordNativeTurn), so the same
			* native turn can never be recorded twice, by either channel, on either
			* device. Returns whether anything changed.
			*/
			async scanExternalActivity() {
				const byId = this.deps.sessions.list.getSnapshot().byId;
				const now = this.now();
				const woken = /* @__PURE__ */ new Set();
				for (const sessionId of this.activityWake.keys()) {
					this.activityWake.delete(sessionId);
					if (byId[sessionId] === void 0) continue;
					woken.add(sessionId);
				}
				const turns = detectExternalTurns(this.tasks.map((task) => ({
					taskId: task.id,
					candidate: {
						sessions: this.relatedSessionsOf(task),
						hasOpenRoundOn: (sessionId) => sessionIsBusy(task, sessionId),
						inBoardTurnOn: (sessionId) => this.inBoardTurnOn(task, sessionId, now)
					}
				})), this.activityBook, byId);
				let changed = false;
				for (const turn of turns) {
					const msg = await this.userMessageOf(turn.sessionId);
					if (this.disposed) return changed;
					if (this.recordExternalRound(turn.taskId, turn.sessionId, turn.refine, msg)) changed = true;
				}
				for (const sessionId of woken) {
					if (turns.some((turn) => turn.sessionId === sessionId)) continue;
					const related = [];
					for (const task of this.tasks) {
						const candidate = this.relatedSessionsOf(task).find((entry) => entry.sessionId === sessionId);
						if (candidate !== void 0) related.push({
							taskId: task.id,
							refine: candidate.refine
						});
					}
					if (related.length === 0) continue;
					const msg = await this.userMessageOf(sessionId);
					if (this.disposed) return changed;
					for (const { taskId, refine } of related) if (this.recordExternalRound(taskId, sessionId, refine, msg)) changed = true;
				}
				return changed;
			}
			/** Whether the session's CURRENT turn is already board-owned: the live
			*  direct-send grace, or a direct round the board recorded for this same
			*  running period (persisted-startAt window — survives a reload, where the
			*  in-memory grace alone would let a long direct turn be double-recorded). */
			inBoardTurnOn(task, sessionId, now) {
				if (withinGrace(this.directGraceUntil.get(sessionId), now)) return true;
				return task.executions.some((round) => round.direct === true && round.sessionId === sessionId && now - round.startedAt <= 6e4);
			}
			/**
			* The ONE external-round write (both detection channels land here): append
			* the round to the CURRENT record (anchor-dedup re-checked at write time —
			* a stale snapshot is never written over a newer one), drive the card into
			* 「进行中」 (a refine round keeps its column), promote to the column top and
			* persist. @returns whether the ledger actually moved.
			*/
			recordExternalRound(taskId, sessionId, refine, msg) {
				const now = this.now();
				let changed = false;
				this.tasks = this.tasks.map((task) => {
					if (task.id !== taskId) return task;
					if (sessionIsBusy(task, sessionId)) return task;
					if (msg?.anchor !== void 0 && task.executions.some((round) => round.sessionId === sessionId && round.anchor === msg.anchor)) return task;
					if (this.inBoardTurnOn(task, sessionId, now)) return task;
					changed = true;
					const withRound = {
						...task,
						updatedAt: now,
						executions: [...task.executions, newExternalRound({
							id: this.uuid(),
							now,
							sessionId,
							refine,
							...msg?.text !== void 0 ? { text: msg.text } : {},
							...msg?.anchor !== void 0 ? { anchor: msg.anchor } : {},
							...msg !== void 0 && msg.text === void 0 && msg.hasImage ? { imageOnly: true } : {}
						})]
					};
					return refine || withRound.status === "running" ? withRound : withStatus(withRound, "running", now);
				});
				if (!changed) return false;
				this.activityBook.externalSince.set(sessionId, now);
				if (!refine) this.tasks = promoteToColumnTop(this.tasks, taskId, "running", now);
				this.persistAndNotify();
				return true;
			}
			/**
			* The legacy live-turn channel: the live frame of a native `user/message`
			* (wired by the client on hosts that still serve it). A turn that starts
			* AND finishes between two reconcile passes can no longer be missed — the
			* frame arrives the instant the user chats. Engine-only (one recorder;
			* replicas get the round through the synced ledger) and idempotent
			* against the state backstop via the persisted turn anchor. On 0.1.5 the
			* legacy stream is gone and `recordActivityWake` is the live channel; both
			* land on the same write path.
			*/
			recordNativeTurn(sessionId, turn) {
				if (this.disposed || !this.engine) return;
				let related = false;
				for (const task of this.tasks) {
					const candidate = this.relatedSessionsOf(task).find((entry) => entry.sessionId === sessionId);
					if (candidate === void 0) continue;
					related = true;
					this.activityBook.recorded.add(sessionId);
					this.recordExternalRound(task.id, sessionId, candidate.refine, turn);
				}
				if (related) this.activityBook.running.set(sessionId, true);
			}
			/**
			* The 0.1.5 wake channel: the host's `api-session/activity` event (or the
			* equivalent list `updatedAt` advance) fired for a durable user message.
			* The wake carries no text and no anchor — it only records the stamp and
			* schedules a reconcile pass, which reads the transcript tail for the
			* facts and lands on the same anchor-deduped write path. Engine-only like
			* the legacy frame channel (a viewer never schedules engine work — the
			* lease holder's pass reads the synced ledger); a stale (non-advancing)
			* stamp never re-schedules. Consumed stamps clear when read so a restart
			* re-primes from the live list instead of replaying history.
			*/
			recordActivityWake(sessionId, stamp) {
				if (this.disposed || !this.engine) return;
				if (!Number.isFinite(stamp)) return;
				const previous = this.activityWake.get(sessionId);
				if (previous !== void 0 && stamp <= previous) return;
				this.activityWake.set(sessionId, stamp);
				this.scheduleReconcile();
			}
			/** The newest native user message of a session (the line that started the
			*  observed turn, or a picture-only marker); undefined when the transcript
			*  is unavailable or the tail window missed it. */
			async userMessageOf(sessionId) {
				if (sessionId === void 0 || this.deps.transcript === void 0) return void 0;
				const result = await this.deps.transcript(sessionId);
				if (result === void 0) return void 0;
				return latestUserMessage(result.events);
			}
			/** The newest native user message TEXT of a session (legacy-path helper). */
			async userTextOf(sessionId) {
				return (await this.userMessageOf(sessionId))?.text;
			}
			/** The backfill text for an external round settling with an empty body
			*  (legacy records / an observation that missed the text); undefined when
			*  there is nothing to fill. The text lands on the CURRENT record at write
			*  time (Stage 2), never on a pre-await snapshot. */
			async externalTextIfNeeded(task, executionId) {
				const execution = task.executions.find((round) => round.id === executionId);
				if (execution === void 0 || execution.external !== true) return void 0;
				if (execution.comment !== void 0 && execution.comment !== "") return void 0;
				return await this.userTextOf(execution.sessionId);
			}
			/**
			* Instant state sync for an ACTIVE binding: right after a session is
			* dragged in / created / picked (createBoundTask / addTaskSource /
			* createTaskSession), evaluate its live state — a related session that is
			* running RIGHT NOW gets an open external round and the card jumps to
			* 「进行中」 immediately (its completion later settles to 「待审核」 through
			* the ordinary reconcile), and the new content turns the card unviewed
			* (breathing glow / 「新」) just like native activity. Baselines are set
			* for every related session here too, so the state backstop keeps working
			* from this point on. Idempotent: the shared write path re-checks the
			* open-round and turn-anchor guards at write time — a session with a round
			* for this turn is never double-recorded.
			*/
			async reconcileBoundTask(taskId) {
				const task = this.tasks.find((candidate) => candidate.id === taskId);
				if (task === void 0) return;
				const byId = this.deps.sessions.list.getSnapshot().byId;
				const now = this.now();
				let runningSessionId;
				let runningRefine = false;
				let leftover = false;
				for (const session of this.relatedSessionsOf(task)) {
					const current = byId[session.sessionId]?.running ?? false;
					this.activityBook.running.set(session.sessionId, current);
					if (!current) {
						this.activityBook.recorded.delete(session.sessionId);
						continue;
					}
					if (this.inBoardTurnOn(task, session.sessionId, now)) {
						this.activityBook.recorded.add(session.sessionId);
						continue;
					}
					if (runningSessionId !== void 0 || sessionIsBusy(task, session.sessionId)) {
						leftover = true;
						continue;
					}
					runningSessionId = session.sessionId;
					runningRefine = session.refine;
					this.activityBook.recorded.add(session.sessionId);
				}
				if (leftover) this.scheduleReconcile();
				if (runningSessionId === void 0) return;
				const msg = await this.userMessageOf(runningSessionId);
				if (this.disposed) return;
				if (!this.recordExternalRound(taskId, runningSessionId, runningRefine, msg)) return;
				this.tasks = this.tasks.map((candidate) => candidate.id === taskId ? {
					...candidate,
					viewedAt: now - 1
				} : candidate);
				this.persistAndNotify();
			}
			/**
			* Cancel an open external round whose session has already finished WITHOUT
			* any real turn evidence past the settle grace — the flip was spurious
			* (a session created/unarchived, a transient signal) and the card must not
			* stay stranded in 「进行中」. Real turns settle through the reconcile above
			* (they have evidence), so this only ever cancels noise.
			*/
			cancelSpuriousExternal() {
				const now = this.now();
				const byId = this.deps.sessions.list.getSnapshot().byId;
				let changed = false;
				for (const task of this.tasks) for (const latest of openRoundsOf(task)) {
					const sessionId = latest.sessionId;
					if (latest.external !== true || sessionId === void 0) continue;
					const summary = byId[sessionId];
					const since = this.activityBook.externalSince.get(sessionId) ?? latest.startedAt;
					if (summary?.running === true || now - since <= 9e4) continue;
					const next = this.settleRound(task, latest.id, "cancelled", void 0);
					if (next !== task) {
						this.tasks = this.tasks.map((candidate) => candidate.id === task.id ? next : candidate);
						changed = true;
					}
				}
				return changed;
			}
			/**
			* THE funnel for USER-INTENT ledger edits: a mutation returning a new
			* record always carries a fresh `updatedAt` — the sync merge ranks edits by
			* that stamp and the card shows it as 更新于, so forgetting a bump becomes
			* structurally impossible (the class of bug where hide / rule edits were
			* silently swallowed on other replicas dies here). Engine-derived writes
			* (settle, external rounds, nextAt ticks) and read-state writes (viewedAt)
			* deliberately do NOT route through this funnel — they own their own
			* freshness semantics. @returns whether the ledger moved.
			*/
			userEdit(taskId, mutate) {
				let changed = false;
				this.tasks = this.tasks.map((task) => {
					if (task.id !== taskId) return task;
					const next = mutate(task);
					if (next === task) return task;
					changed = true;
					return {
						...next,
						updatedAt: this.now()
					};
				});
				if (changed) this.persistAndNotify();
				return changed;
			}
			persistAndNotify() {
				this.deps.store.save(this.tasks);
				this.notify();
				this.dispatch();
			}
			notify() {
				for (const fn of [...this.listeners]) fn();
			}
		};
		/** Record which session ran an execution (once the execution service reports it). */
		function attachSessionId(task, executionId, sessionId, now) {
			return {
				...task,
				updatedAt: now,
				executions: task.executions.map((execution) => execution.id === executionId ? {
					...execution,
					sessionId
				} : execution)
			};
		}
		//#endregion
		//#region src/core/projections.ts
		function isRecord(value) {
			return typeof value === "object" && value !== null;
		}
		function num(value) {
			return typeof value === "number" && Number.isFinite(value) ? value : void 0;
		}
		function str(value) {
			return typeof value === "string" ? value : void 0;
		}
		/**
		* Pick the board's projection slice out of the tail page's raw values.
		* Unknown, malformed or partial values are dropped key by key — one bad
		* domain never hides the rows the other domains returned.
		*/
		function pickTranscriptProjections(values) {
			if (values === void 0) return {};
			const projections = {};
			const pressure = values.contextPressure;
			if (isRecord(pressure)) projections.contextPressure = {
				...num(pressure.pressureTokens) !== void 0 ? { pressureTokens: pressure.pressureTokens } : {},
				...num(pressure.projectedTokens) !== void 0 ? { projectedTokens: pressure.projectedTokens } : {},
				...num(pressure.contextWindow) !== void 0 ? { contextWindow: pressure.contextWindow } : {}
			};
			const breakdown = values.contextBreakdown;
			if (isRecord(breakdown)) {
				const systemTokens = num(breakdown.systemTokens);
				const toolsTokens = num(breakdown.toolsTokens);
				const messageTokens = num(breakdown.messageTokens);
				if (systemTokens !== void 0 && toolsTokens !== void 0 && messageTokens !== void 0) projections.contextBreakdown = {
					systemTokens,
					toolsTokens,
					messageTokens
				};
			}
			const permissions = values.permissions;
			if (isRecord(permissions)) {
				const options = permissions.options;
				const currentValue = permissions.currentValue;
				if (Array.isArray(options) && typeof currentValue === "string") {
					const rows = [];
					for (const option of options) {
						if (!isRecord(option)) continue;
						if (typeof option.value === "string" && typeof option.name === "string") rows.push({
							value: option.value,
							name: option.name,
							...typeof option.description === "string" ? { description: option.description } : {}
						});
					}
					if (rows.length > 0) projections.permissions = {
						options: rows,
						currentValue
					};
				}
			}
			const todos = values.todos;
			if (Array.isArray(todos)) {
				const rows = [];
				for (const item of todos) {
					if (!isRecord(item)) continue;
					if (typeof item.content !== "string" || item.content === "") continue;
					const status = item.status === "in_progress" || item.status === "completed" ? item.status : "pending";
					rows.push({
						content: item.content,
						status
					});
				}
				if (rows.length > 0) projections.todos = rows;
			}
			const tokenUsage = values.tokenUsage;
			if (isRecord(tokenUsage)) {
				const uncachedInputTokens = num(tokenUsage.uncachedInputTokens);
				const outputTokens = num(tokenUsage.outputTokens);
				const cacheReadTokens = num(tokenUsage.cacheReadTokens);
				const cacheWriteTokens = num(tokenUsage.cacheWriteTokens);
				if (uncachedInputTokens !== void 0 && outputTokens !== void 0 && cacheReadTokens !== void 0 && cacheWriteTokens !== void 0) projections.tokenUsage = {
					uncachedInputTokens,
					outputTokens,
					cacheReadTokens,
					cacheWriteTokens
				};
			}
			const sessionStats = values.sessionStats;
			if (isRecord(sessionStats)) {
				const turns = num(sessionStats.turns);
				const steps = num(sessionStats.steps);
				const llmMs = num(sessionStats.llmMs);
				const toolMs = num(sessionStats.toolMs);
				const ttftMs = num(sessionStats.ttftMs);
				const ttftSteps = num(sessionStats.ttftSteps);
				const decodeMs = num(sessionStats.decodeMs);
				const decodeTokens = num(sessionStats.decodeTokens);
				if (turns !== void 0 && steps !== void 0 && llmMs !== void 0 && toolMs !== void 0 && ttftMs !== void 0 && ttftSteps !== void 0 && decodeMs !== void 0 && decodeTokens !== void 0) projections.sessionStats = {
					turns,
					steps,
					llmMs,
					toolMs,
					ttftMs,
					ttftSteps,
					decodeMs,
					decodeTokens
				};
			}
			const goal = values.goal;
			if (goal === null) projections.goal = null;
			else if (isRecord(goal)) {
				const inner = goal.goal;
				if (isRecord(inner)) {
					const id = str(inner.id);
					const revision = num(inner.revision);
					const objective = str(inner.objective);
					const phase = inner.phase;
					const maxGoalRounds = num(inner.maxGoalRounds);
					const roundsStarted = num(goal.roundsStarted);
					const createdAt = num(goal.createdAt);
					const updatedAt = num(goal.updatedAt);
					if (id !== void 0 && id !== "" && revision !== void 0 && objective !== void 0 && objective !== "" && (phase === "active" || phase === "paused" || phase === "blocked") && maxGoalRounds !== void 0 && roundsStarted !== void 0 && createdAt !== void 0 && updatedAt !== void 0) {
						const snapshot = {
							id,
							revision,
							objective,
							phase,
							maxGoalRounds,
							roundsStarted,
							createdAt,
							updatedAt
						};
						const blockedReason = inner.blockedReason;
						if (phase === "blocked" && isRecord(blockedReason)) {
							const code = str(blockedReason.code);
							const message = str(blockedReason.message);
							if (code !== void 0 && message !== void 0 && message.trim() !== "") snapshot.blockedReason = {
								code,
								message: message.trim()
							};
						}
						projections.goal = snapshot;
					}
				}
			}
			return projections.contextPressure !== void 0 || projections.contextBreakdown !== void 0 || projections.permissions !== void 0 || projections.todos !== void 0 || projections.tokenUsage !== void 0 || projections.sessionStats !== void 0 || projections.goal !== void 0 ? { projections } : {};
		}
		//#endregion
		//#region src/core/execution.ts
		/** Session ids whose history-probe failure already logged (reconcile loop hot
		*  path: warn once per session). Plain Set with no eviction: ids are durable
		*  workspace sessions with slow churn, so the set stays small. */
		const warnedHistoryProbe = /* @__PURE__ */ new Set();
		/** Human copy for a run failure. */
		function messageOf(error) {
			if (error instanceof Error) return error.message;
			return String(error);
		}
		/** Whether a `turn/end` payload closed the turn with an error reason. */
		function isErrorTurnEnd(data) {
			if (typeof data !== "object" || data === null) return false;
			const reason = data.reason;
			return typeof reason === "object" && reason !== null && reason.kind === "error";
		}
		/**
		* Run one task to completion (or to a settled failure).
		*
		* @param task - the task being executed.
		* @param execution - the freshly opened execution record (id + start time).
		* @param onEvent - callback for started/settled events.
		* @param options - launch variant (refine rounds reuse the task's refine
		*   session and send the refine instruction; see {@link RunOptions}).
		* @returns resolves when the run settles (or fails to start); never rejects —
		*   every failure path is reported as a settled event.
		*/
		var ExecutionService = class {
			env;
			/** @param env - the runtime faces (real or fake). */
			constructor(env) {
				this.env = env;
			}
			/**
			* Sessions missing from the host list, by consecutive passes that missed
			* them. A destructive verdict (cancel for "no longer exists") requires TWO
			* consecutive misses: a single absent snapshot is a list race (host
			* restart, pagination, a mid-rebuild read), never proof the session is
			* gone — and a one-pass cancel is exactly the "finished sessions fall back
			* to 待办" machine (the card's real outcome is discarded for a transient
			* gap). Seen sessions clear immediately.
			*/
			missingSessions = /* @__PURE__ */ new Map();
			/**
			* Create one guaranteed-fresh native session and compose it with a run
			* configuration — the board's "新建会话" engine. Deliberately NOT a task
			* execution: no execution record, no dispatcher, no watch. The session is
			* created first (via the createSession face; degraded to the workspace
			* blank-reuse entry when the face is absent), then the SAME per-session
			* setup chain a plain run uses is applied in the same order — model route,
			* agent preset (safe: a fresh session is always blank), permission.
			*
			* Failure policy: the session creation itself failing is THE failure (the
			* caller gets ok:false, nothing exists). A CONFIG step failing is NOT: the
			* session already exists on the host and is perfectly usable — it is
			* returned with `configError` so the caller can surface an honest partial
			* success instead of silently dropping a created session or pretending
			* nothing happened. Never rejects.
			*/
			async createSession(config) {
				try {
					const sessionId = await this.resolveNewSessionId(config.workspaceId);
					const driver = this.driverOf(sessionId);
					if (driver === void 0) return {
						ok: true,
						sessionId,
						configError: "execution session is not ready"
					};
					let configError;
					if (config.provider !== void 0 && config.model !== void 0 && this.env.selectModel !== void 0) {
						const selection = await this.env.selectModel(sessionId, {
							provider: config.provider,
							model: config.model,
							...config.reasoningEffort !== void 0 ? { reasoningEffort: config.reasoningEffort } : {}
						});
						if (!selection.ok) configError = `model selection failed: ${selection.error}`;
					}
					if (configError === void 0 && config.agentPreset !== void 0 && this.env.selectAgentPreset !== void 0) {
						const applied = await this.env.selectAgentPreset(sessionId, config.agentPreset);
						if (!applied.ok) configError = `agent preset switch failed: ${applied.error}`;
						else this.env.onAgentApplied?.(sessionId, config.agentPreset);
					}
					if (configError === void 0 && config.permission !== void 0) {
						const applied = await this.applyPermission(driver, config.permission);
						if (!applied.ok) configError = applied.error;
					}
					return {
						ok: true,
						sessionId,
						...configError !== void 0 ? { configError } : {}
					};
				} catch (error) {
					return {
						ok: false,
						error: messageOf(error)
					};
				}
			}
			/**
			* Rename a native session with an explicit user title — the OFFICIAL
			* user-title write. Prefers the host-level face (any session id, exactly
			* like the comment channel); degraded to the client binding driver when
			* no face is wired (works only for sessions with a live binding). Never
			* rejects.
			*/
			async renameSession(sessionId, title) {
				const trimmed = title.trim();
				if (trimmed === "") return {
					ok: false,
					error: "empty title"
				};
				if (this.env.renameSession !== void 0) return this.env.renameSession(sessionId, trimmed);
				const driver = this.driverOf(sessionId);
				if (driver === void 0) return {
					ok: false,
					error: "rename channel unavailable"
				};
				try {
					const result = await driver.rename(trimmed);
					return result.ok ? { ok: true } : {
						ok: false,
						error: messageOf(result.error)
					};
				} catch (error) {
					return {
						ok: false,
						error: messageOf(error)
					};
				}
			}
			/**
			* The session id a "new session" lands in: the createSession face wins
			* (a guaranteed-fresh host session); without it the workspace blank-reuse
			* entry degrades gracefully (the native New Session flow itself).
			*/
			async resolveNewSessionId(workspaceId) {
				if (this.env.createSession !== void 0) {
					const workspace = this.env.workspaces.list.getSnapshot();
					const resolved = workspaceId ?? workspace.recentWorkspaceId ?? workspace.items[0]?.workspaceId;
					if (resolved === void 0) throw new Error("no workspace available to run the task in");
					return this.env.createSession(resolved);
				}
				return this.connectSession(workspaceId);
			}
			async run(task, execution, onEvent, options) {
				try {
					const effectiveText = (options?.prompt ?? task.prompt).trim();
					const effectiveImages = options?.images ?? task.promptImages ?? [];
					const effectiveFiles = options?.files ?? task.promptFiles ?? [];
					if (effectiveText === "" && effectiveImages.length === 0 && effectiveFiles.length === 0) {
						onEvent({
							kind: "settled",
							taskId: task.id,
							executionId: execution.id,
							outcome: "failed",
							error: "empty prompt: task has nothing executable"
						});
						return;
					}
					const sessionId = options?.sessionId ?? await this.connectSession(task.workspaceId);
					const fresh = options?.fresh ?? true;
					onEvent({
						kind: "started",
						taskId: task.id,
						executionId: execution.id,
						sessionId
					});
					const driver = this.driverOf(sessionId);
					if (driver === void 0) {
						onEvent({
							kind: "settled",
							taskId: task.id,
							executionId: execution.id,
							outcome: "failed",
							error: "execution session is not ready"
						});
						return;
					}
					await driver.rename(options?.renameTo ?? task.title).catch(() => {});
					if (task.provider !== void 0 && task.model !== void 0 && this.env.selectModel !== void 0) {
						const selection = await this.env.selectModel(sessionId, {
							provider: task.provider,
							model: task.model,
							...task.reasoningEffort !== void 0 ? { reasoningEffort: task.reasoningEffort } : {}
						});
						if (!selection.ok) {
							onEvent({
								kind: "settled",
								taskId: task.id,
								executionId: execution.id,
								outcome: "failed",
								error: `model selection failed: ${selection.error}`
							});
							return;
						}
					}
					if (fresh && task.agentPreset !== void 0 && this.env.selectAgentPreset !== void 0) {
						const applied = await this.env.selectAgentPreset(sessionId, task.agentPreset);
						if (!applied.ok) {
							onEvent({
								kind: "settled",
								taskId: task.id,
								executionId: execution.id,
								outcome: "failed",
								error: `agent preset switch failed: ${applied.error}`
							});
							return;
						}
						this.env.onAgentApplied?.(sessionId, task.agentPreset);
					}
					if (task.permission !== void 0) {
						const applied = await this.applyPermission(driver, task.permission);
						if (!applied.ok) {
							onEvent({
								kind: "settled",
								taskId: task.id,
								executionId: execution.id,
								outcome: "failed",
								error: applied.error
							});
							return;
						}
					}
					const line = this.promptLine(task, options?.prompt);
					if (this.env.sendCommand !== void 0 && line.trimStart().startsWith("/")) {
						if (await this.deliverCommandLine(task, execution, sessionId, line.trim(), onEvent, false) !== "fallback") return;
					}
					const baseline = driver.getSnapshot().turnEnds.size;
					const accepted = await this.sendPrompt(driver, task, options?.prompt, options?.images, options?.files);
					if (!accepted.ok) {
						onEvent({
							kind: "settled",
							taskId: task.id,
							executionId: execution.id,
							outcome: "failed",
							error: messageOf(accepted.error)
						});
						return;
					}
					this.watchForSettlement(driver, task.id, execution.id, sessionId, onEvent, baseline);
				} catch (error) {
					onEvent({
						kind: "settled",
						taskId: task.id,
						executionId: execution.id,
						outcome: "failed",
						error: messageOf(error)
					});
				}
			}
			/**
			* Continue an existing execution session with a comment: send the text to
			* the session's agent (a fresh turn) and watch it settle like a plain run.
			* The comment round carries the same session — no new session is created.
			* Settlement uses the host session list + raw history signals, which work
			* for sessions that are not the currently staged one; when a binding
			* driver is available its snapshot watch is used as an additional fast
			* path. Never rejects: every failure path reports a settled event.
			*
			* A round flagged `command` is a slash command, not necessarily just a
			* turn: the line is executed through the native command registry (never
			* sent to the model as text). A matched command that opened a real turn
			* (e.g. `/plan <message>`) is watched to that turn's end — the board never
			* settles on `matched` alone, so a plan-mode turn stays 「进行中」 through
			* its plan review and execution — while a matched pure-configuration
			* command (e.g. `/permission`) settles right after the detection window. An
			* unmatched line falls back to plain-text delivery — the native
			* composer's default-sink semantics, so no user input is ever dropped.
			* Without a command face the round degrades to plain text.
			*/
			async commentRun(task, execution, sessionId, text, onEvent, mode = "queue") {
				try {
					if (execution.command === true) {
						if (await this.deliverCommandLine(task, execution, sessionId, text, onEvent, true) === "fallback") await this.commentRun(task, {
							...execution,
							command: false
						}, sessionId, text, onEvent, mode);
						return;
					}
					const driver = this.driverOf(sessionId);
					const result = await (this.env.sendComment ?? (async (id, content, m = "queue", imgs, files) => {
						const bound = this.driverOf(id);
						if (bound === void 0) return {
							ok: false,
							error: "comment session is not ready"
						};
						const parts = [];
						if (content.trim() !== "") parts.push({
							type: "text",
							text: content
						});
						for (const image of imgs ?? []) parts.push({
							type: "image",
							mediaType: image.mediaType,
							data: image.data,
							...image.name !== void 0 ? { name: image.name } : {}
						});
						for (const file of files ?? []) parts.push({
							type: "file",
							receiptId: file.receiptId
						});
						return bound.prompt(parts, m);
					}))(sessionId, text, mode, execution.promptImages, execution.promptFiles);
					if (!result.ok) {
						onEvent({
							kind: "settled",
							taskId: task.id,
							executionId: execution.id,
							outcome: "failed",
							error: `comment rejected: ${result.error}`
						});
						return;
					}
					const baseline = driver !== void 0 ? driver.getSnapshot().turnEnds.size : 0;
					this.watchForSettlement(driver, task.id, execution.id, sessionId, onEvent, baseline, true);
				} catch (error) {
					onEvent({
						kind: "settled",
						taskId: task.id,
						executionId: execution.id,
						outcome: "failed",
						error: messageOf(error)
					});
				}
			}
			/**
			* Deliver one slash-command line through the native command registry — the
			* single shared path for BOTH the execution prompt (run) and every comment
			* composer (review page / linked-session drive mode). Mirrors the native
			* composer's '/' semantics exactly:
			* - matched commands execute through the registry (never as prompt text);
			* - a matched command may START A REAL MODEL TURN (e.g. `/plan <message>`
			*   turns plan mode on and steers the message into the agent): the round is
			*   then watched until that turn truly settles — never on `matched` alone;
			* - a matched command that opens no turn (a pure configuration command:
			*   `/permission`, `/goal`, `/plan off`, a bare `/plan`, `/compact`, …)
			*   settles right after the bounded detection window;
			* - an unmatched line (or a missing command face) returns 'fallback' so the
			*   caller delivers the line as plain text (the native default-sink).
			* Never rejects: every failure path reports a settled event or 'fallback'.
			*/
			async deliverCommandLine(task, execution, sessionId, line, onEvent, sessionKnown) {
				const command = this.env.sendCommand;
				if (command === void 0) return "fallback";
				const result = await command(sessionId, line);
				if (!result.ok) {
					onEvent({
						kind: "settled",
						taskId: task.id,
						executionId: execution.id,
						outcome: "failed",
						error: `command rejected: ${result.error}`
					});
					return "settled";
				}
				if (!result.matched) return "fallback";
				const driver = this.driverOf(sessionId);
				const baseline = driver !== void 0 ? driver.getSnapshot().turnEnds.size : 0;
				const outcome = result.outcome;
				if (outcome?.kind === "error") {
					onEvent({
						kind: "settled",
						taskId: task.id,
						executionId: execution.id,
						outcome: "failed",
						error: outcome.text
					});
					return "settled";
				}
				if (await this.waitForCommandWork(sessionId, baseline, this.env.commandGraceMs ?? 2e3)) {
					this.watchForSettlement(driver, task.id, execution.id, sessionId, onEvent, baseline, sessionKnown);
					return "watched";
				}
				onEvent({
					kind: "settled",
					taskId: task.id,
					executionId: execution.id,
					outcome: "succeeded",
					...outcome !== void 0 && outcome.text !== void 0 && outcome.text !== "" ? { error: outcome.text } : {}
				});
				return "settled";
			}
			/**
			* Watch a session after a matched command to decide whether it started real
			* model work (a turn) or was a pure configuration command. A turn's start
			* flips the session `running` (live list summary or driver snapshot) or, if
			* it finished unusually fast, grows the driver's turn-end counter past the
			* pre-command baseline; either proves the command opened a turn. When
			* `graceMs` elapses with no such evidence the command is judged a
			* configuration command. Subscriptions and the timer are always disposed.
			* @returns true = a turn started (the caller watches to its end); false =
			*   the command settled by itself (the caller settles the round at once).
			*/
			waitForCommandWork(sessionId, baseline, graceMs) {
				return new Promise((resolve) => {
					let done = false;
					const disposers = [];
					const finish = (startedTurn) => {
						if (done) return;
						done = true;
						clearTimeout(timer);
						for (const dispose of disposers) dispose();
						resolve(startedTurn);
					};
					const timer = setTimeout(() => finish(false), graceMs);
					const check = () => {
						if (done) return;
						const summary = this.env.sessions.list.getSnapshot().byId[sessionId];
						if (summary !== void 0 && summary.running) {
							finish(true);
							return;
						}
						const driver = this.driverOf(sessionId);
						if (driver !== void 0) {
							const snapshot = driver.getSnapshot();
							if (snapshot.running || snapshot.turnEnds.size > baseline) {
								finish(true);
								return;
							}
						}
					};
					disposers.push(this.env.sessions.list.subscribe(check));
					const driver = this.driverOf(sessionId);
					if (driver !== void 0) disposers.push(driver.subscribe(check));
					check();
				});
			}
			/**
			* The resolved delivery line of a run: the prompt override / task prompt,
			* verbatim (no title fallback — `taskExecutable` is THE one execution
			* gate in the controller; the service never guesses intent from the title.
			* A blank line with no attachments fails fast in `run` below).
			*/
			promptLine(task, promptOverride) {
				return promptOverride ?? task.prompt;
			}
			/**
			* Inspect a reloaded/background task that was left 'running' and emit a
			* settled event when its session already finished.
			*
			* A session that was never opened keeps a cold conversation snapshot (the
			* runtime only maintains the window for the staged/current session), so the
			* settled outcome is decided by the strongest available signal, in order:
			* 1. the list summary — missing session → cancelled; still running → pending;
			* 2. a warm conversation snapshot → `lastAgentError` decides failed/succeeded;
			* 3. the raw history tail (when a history face is wired) — a `turn/end`
			*    error reason proves failure;
			* 4. otherwise a finished session counts as succeeded.
			*
			* @param task - a task with an open round.
			* @param executionId - WHICH open round to judge. A card may run several
			*   sessions at once, so "the last record" is not "the running one"; the
			*   caller sweeps every open round and names the one it is asking about.
			*   Absent = the latest round (the legacy single-lane call).
			* @returns a settled event when the session state proves completion, else undefined.
			*/
			async reconcile(task, executionId) {
				const execution = executionId === void 0 ? task.executions[task.executions.length - 1] : task.executions.find((candidate) => candidate.id === executionId);
				if (execution === void 0 || execution.sessionId === void 0 || execution.endedAt !== void 0) return void 0;
				const list = this.env.sessions.list.getSnapshot();
				if (list.phase !== "ready") return void 0;
				const summary = list.byId[execution.sessionId];
				if (summary === void 0) {
					const misses = (this.missingSessions.get(execution.sessionId) ?? 0) + 1;
					if (misses < 2) {
						this.missingSessions.set(execution.sessionId, misses);
						return;
					}
					this.missingSessions.delete(execution.sessionId);
					return {
						kind: "settled",
						taskId: task.id,
						executionId: execution.id,
						outcome: "cancelled",
						error: "execution session no longer exists"
					};
				}
				this.missingSessions.delete(execution.sessionId);
				if (summary.running) return void 0;
				const driver = this.driverOf(execution.sessionId);
				if (driver !== void 0) {
					const snapshot = driver.getSnapshot();
					if (snapshot.turnEnds.size > 0) {
						const outcome = snapshot.lastAgentError !== null ? "failed" : "succeeded";
						return {
							kind: "settled",
							taskId: task.id,
							executionId: execution.id,
							outcome,
							error: snapshot.lastAgentError ?? void 0
						};
					}
				}
				const signal = await this.historyTurnEndSignal(execution.sessionId);
				if (signal !== void 0) return {
					kind: "settled",
					taskId: task.id,
					executionId: execution.id,
					outcome: signal,
					error: signal === "failed" ? "agent turn failed" : void 0
				};
				if (this.env.history === void 0) return {
					kind: "settled",
					taskId: task.id,
					executionId: execution.id,
					outcome: "succeeded"
				};
			}
			/**
			* Probe the raw history tail for a finished turn. Returns the outcome the
			* turn proves — 'failed' when its `turn/end` carries an error reason,
			* 'succeeded' for any other `turn/end` — or undefined when the tail shows
			* no turn at all (the session was created but never ran) or history is
			* unavailable. A failed history read must not block settlement; it reports
			* "no signal" and the caller decides.
			*/
			async historyTurnEndSignal(sessionId) {
				const history = this.env.history;
				if (history === void 0) return void 0;
				try {
					const tail = await history.loadTail(sessionId);
					if (tail === void 0) return void 0;
					for (const event of tail.events) if (event.type === "turn/end") return isErrorTurnEnd(event.data) ? "failed" : "succeeded";
					return;
				} catch (error) {
					if (!warnedHistoryProbe.has(sessionId)) {
						warnedHistoryProbe.add(sessionId);
						console.error("[dsh-task-board] history turn probe failed", error);
					}
					return;
				}
			}
			async connectSession(workspaceId) {
				const workspace = this.env.workspaces.list.getSnapshot();
				const resolved = workspaceId ?? workspace.recentWorkspaceId ?? workspace.items[0]?.workspaceId;
				if (resolved === void 0) throw new Error("no workspace available to run the task in");
				const candidates = workspace.items.find((item) => item.workspaceId === resolved)?.sessionIds ?? [];
				const summaries = this.env.sessions.list.getSnapshot().byId;
				const reusable = candidates.find((id) => summaries[id]?.blank === true);
				if (reusable !== void 0) return reusable;
				if (this.env.createSession !== void 0) return this.env.createSession(resolved);
				throw new Error("no session available to run the task in: the workspace has no blank session and session creation is unavailable");
			}
			driverOf(sessionId) {
				return this.env.sessions.binding(sessionId)?.session;
			}
			async sendPrompt(driver, task, promptOverride, images, files) {
				const text = promptOverride ?? task.prompt;
				const attached = images ?? task.promptImages ?? [];
				const attachedFiles = files ?? task.promptFiles ?? [];
				const parts = [
					...text === "" ? [] : [{
						type: "text",
						text
					}],
					...attached.map((image) => ({
						type: "image",
						mediaType: image.mediaType,
						data: image.data,
						...image.name !== void 0 ? { name: image.name } : {}
					})),
					...attachedFiles.map((file) => ({
						type: "file",
						receiptId: file.receiptId
					}))
				];
				try {
					return await driver.prompt(parts, "queue");
				} catch (error) {
					return {
						ok: false,
						error
					};
				}
			}
			/**
			* Apply a task's permission preset to the execution session through the
			* native `/permission` command (the GUI picker's write path). A rejected
			* command or a name the host does not recognize reports failure so the
			* caller can settle the run; the command itself never starts a turn.
			*/
			async applyPermission(driver, permission) {
				try {
					const result = await driver.command(`/permission ${permission}`);
					if (!result.ok) return {
						ok: false,
						error: `permission switch failed: ${messageOf(result.error)}`
					};
					if (!result.value.matched) return {
						ok: false,
						error: `permission switch failed: the host offers no /permission command for "${permission}"`
					};
					return { ok: true };
				} catch (error) {
					return {
						ok: false,
						error: `permission switch failed: ${messageOf(error)}`
					};
				}
			}
			/**
			* Subscribe to the execution session and settle the run once the accepted
			* turn completes (turn counter advanced past the acceptance baseline and
			* the session is no longer running). Never settles while the session is
			* still running; unsubscribes on settle.
			*
			* The execution session is usually NOT the UI's current session, so its
			* conversation snapshot stays cold (the runtime only maintains the window
			* for the staged/current session) and the driver watch above would never
			* observe the turn — and for comment continuations the session may not
			* even have a binding. The host session list is the completion signal
			* there: subscribe to it as well, and settle once the list reports the
			* session no longer running AND a turn actually finished (driver snapshot
			* or raw history tail) — a session that was merely created but never
			* started (queue window) must not settle.
			*/
			watchForSettlement(driver, taskId, executionId, sessionId, onEvent, baseline, sessionKnown = false) {
				let settled = false;
				let unsubscribe = [];
				let misses = 0;
				let idleStreak = 0;
				const settle = (outcome, error) => {
					if (settled) return;
					settled = true;
					for (const dispose of unsubscribe) dispose();
					onEvent({
						kind: "settled",
						taskId,
						executionId,
						outcome,
						error
					});
				};
				const check = () => {
					if (settled || driver === void 0) return;
					const snapshot = driver.getSnapshot();
					if (snapshot.running || snapshot.turnEnds.size <= baseline) return;
					settle(snapshot.lastAgentError !== null ? "failed" : "succeeded", snapshot.lastAgentError ?? void 0);
				};
				const checkList = () => {
					if (settled) return;
					const list = this.env.sessions.list.getSnapshot();
					if (list.phase !== "ready") return;
					const summary = list.byId[sessionId];
					if (summary === void 0) {
						if (sessionKnown) {
							misses += 1;
							if (misses >= 2) settle("cancelled", "comment session no longer exists");
						}
						return;
					}
					misses = 0;
					if (summary.running) {
						idleStreak = 0;
						return;
					}
					const snapshot = driver?.getSnapshot();
					if (snapshot !== void 0 && snapshot.turnEnds.size > baseline) {
						settle(snapshot.lastAgentError !== null ? "failed" : "succeeded", snapshot.lastAgentError ?? void 0);
						return;
					}
					if (driver === void 0 && this.env.history === void 0) {
						idleStreak += 1;
						if (idleStreak >= 2) settle("succeeded");
						return;
					}
					idleStreak = 0;
					this.historyTurnEndSignal(sessionId).then((signal) => {
						if (signal !== void 0) settle(signal, signal === "failed" ? "agent turn failed" : void 0);
					});
				};
				unsubscribe = [this.env.sessions.list.subscribe(checkList)];
				if (driver !== void 0) unsubscribe.push(driver.subscribe(check));
				check();
				checkList();
			}
		};
		//#endregion
		//#region src/core/scheduler.ts
		/**
		* Browser-side scheduler: the heartbeat behind scheduled task runs.
		*
		* The board is a pure client plugin with no server channel, so "定时任务"
		* lives in the tab: a timer ticks every minute (plus immediately on tab
		* visibility recovery) and triggers any task whose `schedule.nextRunAt` is
		* due, rolling the schedule forward to the next cron match before triggering
		* so the same tick never double-fires. Missed runs are skipped, never queued:
		* a task still running at its due instant is skipped by the controller's
		* runTask guard and simply waits for the next cron match.
		*
		* Overlap policy is Forbid, explicit (K8s `concurrencyPolicy: Forbid`,
		* node-cron `noOverlap`, shell `flock -n`: three names, one semantic) — a
		* due instant that arrives while the task still runs is SKIPPED (never queued
		* behind the run, never killing it) and counted, and a due instant that a
		* newer grid point already superseded AND that is older than the missed-slot
		* tolerance is skipped without catch-up (a slept tab never avalanches; a
		* merely-late tick still fires). Skips are observable through
		* {@link skipStats}; they never enter the retry path (a skip is not a failure).
		*
		* Framework-free: all runtime access flows through the injected deps
		* (structural faces), so tests drive ticks directly without timers.
		*/
		/**
		* Heartbeat liveness (Healthchecks `Period + Grace`, single-machine edition):
		* the scheduler is stale when no tick fully completed within one period plus
		* one grace window. An absent stamp (no tick yet) reads alive — a fresh load
		* must never flash red — as does a future stamp (a clock that stepped back).
		*/
		function isHeartbeatStale(lastOkAt, now, periodMs = SCHEDULER_TICK_MS, graceMs = periodMs) {
			if (lastOkAt === void 0 || now <= lastOkAt) return false;
			return now - lastOkAt > periodMs + graceMs;
		}
		/** Tick cadence when the host wires none (one minute — the missed-tolerance
		*  default and the heartbeat period both derive from the tick, so a custom
		*  cadence retunes the whole scheduler by passing `tickMs`). */
		const SCHEDULER_TICK_MS = 6e4;
		/**
		* The schedule heartbeat (see module doc). `tick` is public so tests and
		* callers can drive a check without waiting for the interval.
		*/
		var SchedulerService = class {
			deps;
			timer;
			disposed = false;
			/** Forbid-policy skip ledger (in-memory only — skips are telemetry, never
			*  persisted, so a minute of overlap can never spam the synced document). */
			skippedOverlap = 0;
			skippedMissed = 0;
			/** Heartbeat stamps (in-memory only, same telemetry discipline as the skip
			*  ledger): the last tick entry vs. the last fully completed tick. */
			lastTickAt = void 0;
			lastOkAt = void 0;
			/** @param deps - tasks/clock/trigger/apply faces (see {@link SchedulerDeps}). */
			constructor(deps) {
				this.deps = deps;
			}
			/** Forbid-policy telemetry: how many due slots were skipped while the task
			*  still ran (`overlap`) vs. how many were too stale to catch up (`missed`). */
			skipStats() {
				return {
					overlap: this.skippedOverlap,
					missed: this.skippedMissed
				};
			}
			/** Heartbeat stamps for liveness checks (read-only; undefined = no tick yet). */
			heartbeat() {
				return {
					lastTickAt: this.lastTickAt,
					lastOkAt: this.lastOkAt
				};
			}
			/** Start ticking: one immediate check (catch-up after reload) + the interval. */
			start() {
				if (this.disposed) return;
				this.tick();
				this.timer = setInterval(() => {
					this.tick();
				}, this.deps.tickMs ?? 6e4);
				this.deps.environment?.addEventListener("visibilitychange", this.onVisibility);
			}
			/** Stop ticking and drop listeners (idempotent). */
			dispose() {
				this.disposed = true;
				if (this.timer !== void 0) clearInterval(this.timer);
				this.timer = void 0;
				this.deps.environment?.removeEventListener("visibilitychange", this.onVisibility);
			}
			/**
			* Check every enabled schedule and trigger the due ones. Idempotent per
			* task per tick: the schedule is rolled forward only after runTask accepts
			* the run, so a rejected run keeps its due slot and is retried on the next
			* tick instead of being silently dropped.
			*/
			async tick() {
				if (this.disposed) return;
				if (this.deps.ready !== void 0 && !this.deps.ready()) return;
				const now = this.deps.now();
				this.lastTickAt = now;
				this.deps.cruiseTick?.(now);
				for (const task of this.deps.tasks()) {
					const schedule = task.schedule;
					if (schedule === void 0 || !schedule.enabled) continue;
					const readiness = ruleReadiness(task);
					if (schedule.mode === "chain") {
						if (task.status === "done") continue;
						if (hasOpenRun(task)) continue;
						const runs = plainRunsOf(task);
						const lastRun = runs[runs.length - 1];
						if (runs.length > 0 && lastRun?.result !== "succeeded") continue;
						if (schedule.maxRuns !== void 0 && schedule.runCount + 1 >= schedule.maxRuns) continue;
						await this.deps.runTask(task.id);
						continue;
					}
					if (readiness.kind === "paused" || readiness.kind === "blocked") {
						if (schedule.nextRunAt === void 0 || schedule.nextRunAt > now) continue;
						const next = nextRunAtMs(schedule.cron, schedule.nextRunAt);
						if (next !== void 0) this.deps.applySchedule(task.id, next, void 0);
						continue;
					}
					if (schedule.nextRunAt === void 0) {
						const repaired = nextRunAtMs(schedule.cron, now);
						if (repaired === void 0) continue;
						this.deps.applySchedule(task.id, repaired, void 0);
						continue;
					}
					if (schedule.nextRunAt > now) continue;
					if (hasOpenRun(task)) {
						const next = nextRunAtMs(schedule.cron, schedule.nextRunAt);
						if (next !== void 0) this.deps.applySchedule(task.id, next, void 0);
						this.skippedOverlap += 1;
						this.deps.onSkips?.(this.skipStats());
						continue;
					}
					const tolerance = schedule.missedToleranceMs ?? this.deps.tickMs ?? 6e4;
					const nextAfterDue = nextRunAtMs(schedule.cron, schedule.nextRunAt);
					if (nextAfterDue !== void 0 && nextAfterDue <= now && now - schedule.nextRunAt > tolerance) {
						const next = nextRunAtMs(schedule.cron, now);
						if (next !== void 0) this.deps.applySchedule(task.id, next, void 0);
						this.skippedMissed += 1;
						this.deps.onSkips?.(this.skipStats());
						continue;
					}
					const runCount = schedule.runCount + 1;
					const finalRun = schedule.maxRuns !== void 0 && runCount >= schedule.maxRuns;
					if (!await this.deps.runTask(task.id)) continue;
					if (finalRun) this.deps.applySchedule(task.id, void 0, now, runCount, true);
					else {
						const next = nextRunAtMs(schedule.cron, schedule.nextRunAt);
						this.deps.applySchedule(task.id, next, now, runCount);
					}
				}
				await this.deps.sessionRulesTick?.(now);
				this.lastOkAt = now;
				this.deps.onHeartbeat?.({
					tickAt: this.lastTickAt ?? now,
					okAt: now
				});
			}
			onVisibility = () => {
				this.tick();
			};
		};
		//#endregion
		//#region src/core/host-sync.ts
		/** Default empty document for a client that never synced (fallback mode). */
		function emptyBaseline(now) {
			return emptyBoardDoc(now);
		}
		/** Commit retry budget: five attempts (2s → 4s → 8s → 16s → 30s), then the
		*  dirty state parks until the next local write retries it. An unbounded 2s
		*  loop would spin forever against a dead host (battery/log spam for zero
		*  convergence); the poll/SSE backstop keeps reads converging while parked,
		*  and the next write reopens the cycle because the user is watching then. */
		const MAX_COMMIT_ATTEMPTS = 5;
		const COMMIT_RETRY_BASE_MS = 2e3;
		const COMMIT_RETRY_CAP_MS = 3e4;
		/** Whether a legacy view carries anything worth migrating (defaults alone do not).
		*  The cruise default rides THE shared default value, never a retyped literal. */
		function hasLegacyContent(view) {
			return view.tasks.length > 0 || view.cruise.enabled === true || view.cruise.manual !== void 0 || view.cruise.schedule.length > 0 || view.cruise.limit !== DEFAULT_CRUISE_VALUE.limit || view.schedulePresets.length > 0 || view.runPresets.presets.length > 0 || view.runPresets.defaultId !== void 0;
		}
		var BoardSyncClient = class {
			deps;
			/** Identity of this tab for lease/relay purposes (stable per instance). */
			clientId;
			mode = "unavailable";
			baseline;
			dirty = {};
			/** The snapshot currently in flight (identity-compared on ack). */
			inFlight;
			/** Authorship claims accrued since the last fully-acked commit (see setTasks). */
			claims = /* @__PURE__ */ new Set();
			refire = false;
			/** The host's lease protocol version. undefined = this replica has NEVER
			*  read a lease — "no evidence yet" must never be reported as "old host"
			*  (a failed first probe would otherwise flash a false stale banner). */
			hostLeaseProto = void 0;
			/** When the answering host process booted (undefined = never read). */
			hostBoot = void 0;
			engine = false;
			disposed = false;
			commitCancel;
			/** Backoff-retry timer (separate from the debounce above: a fresh write
			*  reschedules the debounce but never cancels a pending backoff — the two
			*  lanes have different owners and must never overwrite each other). */
			backoffCancel;
			/** User-intended deletions accrued at edit time (see setTasks) — flushed
			*  verbatim, never recomputed against a newer baseline. */
			deleted = [];
			/** Consecutive un-acked commits (reset on ack and on every fresh local
			*  write — a user acting reopens the cycle because they are watching). */
			commitAttempts = 0;
			resyncCancel;
			loopCancels = [];
			remoteListener;
			engineListener;
			commandListener;
			backupListener;
			now;
			defer;
			commitDebounceMs;
			leaseTtlMs;
			leaseRenewMs;
			pollMs;
			resyncCoalesceMs;
			log;
			constructor(deps) {
				this.deps = deps;
				this.now = deps.now ?? (() => Date.now());
				this.defer = deps.defer;
				this.clientId = deps.uuid?.() ?? `tab-${Math.random().toString(36).slice(2, 12)}`;
				this.commitDebounceMs = deps.commitDebounceMs ?? 250;
				this.leaseTtlMs = deps.leaseTtlMs ?? 2e4;
				this.leaseRenewMs = deps.leaseRenewMs ?? 7e3;
				this.pollMs = deps.pollMs ?? 3e4;
				this.resyncCoalesceMs = deps.resyncCoalesceMs ?? 120;
				this.log = deps.log ?? ((message, error) => error === void 0 ? console.error(message) : console.error(message, error));
				this.baseline = emptyBaseline(this.now());
			}
			/**
			* Fetch the authoritative document (bounded retries), run the migration
			* probe, then wire the watch/lease/poll loops.
			* @param legacy - reads the pre-sync local view (localStorage); when the
			*   host document is still empty it is uploaded as the initial commit,
			*   otherwise the host truth wins and the local copy goes to `onBackup`.
			* @returns the mode this client settled into.
			*/
			async start(legacy) {
				for (let attempt = 0; attempt < 3 && this.mode !== "synced"; attempt++) {
					const result = await this.deps.transport.fetch(this.clientId, void 0).catch(() => void 0);
					if (result !== void 0 && result.available && result.doc !== void 0) {
						this.baseline = result.doc;
						this.mode = "synced";
						break;
					}
					if (attempt < 2) await new Promise((resolve) => this.defer(resolve, 400));
				}
				if (this.mode !== "synced") return "unavailable";
				if (this.disposed) return this.mode;
				const legacyView = legacy?.();
				if (legacyView !== void 0 && hasLegacyContent(legacyView)) {
					const at = this.now();
					const hostIds = new Map(this.baseline.tasks.map((task) => [task.id, task]));
					const tasksIdentical = legacyView.tasks.length === this.baseline.tasks.length && legacyView.tasks.every((task) => hostIds.get(task.id)?.updatedAt === task.updatedAt);
					const hostView = boardViewOf(this.baseline);
					const sectionsIdentical = JSON.stringify({
						cruise: legacyView.cruise,
						schedulePresets: legacyView.schedulePresets,
						runPresets: legacyView.runPresets
					}) === JSON.stringify({
						cruise: hostView.cruise,
						schedulePresets: hostView.schedulePresets,
						runPresets: hostView.runPresets
					});
					if (!(tasksIdentical && sectionsIdentical)) {
						if (this.baseline.revision === 0) this.dirty = {
							tasks: legacyView.tasks,
							cruise: {
								value: legacyView.cruise,
								at
							},
							schedulePresets: {
								value: legacyView.schedulePresets,
								at
							},
							runPresets: {
								value: legacyView.runPresets,
								at
							}
						};
						else {
							const merged = new Map(hostIds);
							for (const task of legacyView.tasks) {
								const host = merged.get(task.id);
								if (host === void 0 || task.updatedAt > host.updatedAt) merged.set(task.id, task);
							}
							this.dirty = { tasks: [...merged.values()] };
							this.backupListener?.(legacyView);
						}
						await this.flush();
					}
				}
				this.loopCancels.push(this.deps.transport.openStream(this.clientId, {
					onEvent: (event) => this.onStreamEvent(event),
					onOpen: () => {
						this.streamOpens += 1;
						this.lastFrameAt = this.now();
						this.log(`[dsh-task-board] stream open #${this.streamOpens}`);
						this.scheduleResync();
						this.renewLease();
					}
				}));
				this.loopCancels.push(this.every(this.leaseRenewMs, () => this.renewLease()));
				this.loopCancels.push(this.every(this.pollMs, () => this.poll()));
				if (this.deps.visibility !== void 0) {
					const visibility = this.deps.visibility;
					this.loopCancels.push(visibility.onVisible(() => {
						this.renewLease();
						this.poll();
					}));
					this.loopCancels.push(visibility.onHidden(() => {
						this.deps.transport.lease(this.clientId, {
							ttlMs: this.leaseTtlMs,
							active: false
						}).catch(() => void 0);
					}));
				}
				await this.renewLease();
				return this.mode;
			}
			/** Stop every loop, close the stream, release the lease best-effort. */
			dispose() {
				if (this.disposed) return;
				this.disposed = true;
				this.commitCancel?.();
				this.commitCancel = void 0;
				this.backoffCancel?.();
				this.backoffCancel = void 0;
				this.resyncCancel?.();
				for (const cancel of this.loopCancels.splice(0)) cancel();
				if (this.mode === "synced") this.deps.transport.lease(this.clientId, { release: true }).catch(() => void 0);
				this.remoteListener = void 0;
				this.engineListener = void 0;
				this.commandListener = void 0;
				this.backupListener = void 0;
			}
			getMode() {
				return this.mode;
			}
			/** Whether the host truth is adopted (false before/during boot and in
			*  fallback mode). Store seams read the offline mirror while false, so the
			*  board mounts on local data instantly and converges when the line allows —
			*  the entry never waits on the network. */
			isSynced() {
				return this.mode === "synced";
			}
			isEngine() {
				return this.engine;
			}
			/** The effective local view: baseline overlaid with un-acked dirty state.
			*  Tasks merge by id (never whole-array overlay): the dirty array keeps its
			*  order, and baseline-only rows (remote arrivals during a parked spell)
			*  append read-only — a parked replica keeps READING convergence even
			*  though its writes wait. Accrued deletions stay excluded on both sides. */
			view() {
				const base = boardViewOf(this.baseline);
				const deletedIds = new Set(this.deleted.map((entry) => entry.id));
				if (this.dirty.tasks === void 0 && this.dirty.cruise === void 0 && this.dirty.schedulePresets === void 0 && this.dirty.runPresets === void 0) return deletedIds.size === 0 ? base : {
					...base,
					tasks: base.tasks.filter((task) => !deletedIds.has(task.id))
				};
				const dirtyIds = new Set((this.dirty.tasks ?? []).map((task) => task.id));
				return {
					tasks: [...(this.dirty.tasks ?? base.tasks).filter((task) => !deletedIds.has(task.id)), ...this.dirty.tasks === void 0 ? [] : base.tasks.filter((task) => !dirtyIds.has(task.id) && !deletedIds.has(task.id))],
					cruise: this.dirty.cruise?.value ?? base.cruise,
					schedulePresets: this.dirty.schedulePresets?.value ?? base.schedulePresets,
					runPresets: this.dirty.runPresets?.value ?? base.runPresets
				};
			}
			baselineRevision() {
				return this.baseline.revision;
			}
			setTasks(tasks) {
				if (this.dirty.tasks === tasks) return;
				const previous = this.view().tasks;
				for (const id of changedIdsOf(previous, tasks)) this.claims.add(id);
				const kept = new Set(tasks.map((task) => task.id));
				for (const id of [...this.claims]) if (!kept.has(id)) this.claims.delete(id);
				const stamps = new Map(this.baseline.tasks.map((task) => [task.id, task.updatedAt]));
				const accrued = new Map(this.deleted.map((entry) => [entry.id, entry.baseUpdatedAt]));
				for (const row of previous) {
					if (kept.has(row.id)) continue;
					if (accrued.has(row.id)) continue;
					const stamp = stamps.get(row.id);
					if (stamp !== void 0) accrued.set(row.id, stamp);
				}
				for (const id of accrued.keys()) if (kept.has(id)) accrued.delete(id);
				this.deleted = [...accrued].map(([id, baseUpdatedAt]) => ({
					id,
					baseUpdatedAt
				}));
				this.dirty.tasks = tasks;
				this.scheduleCommit();
			}
			setCruise(value) {
				this.dirty.cruise = {
					value,
					at: this.now()
				};
				this.scheduleCommit();
			}
			setSchedulePresets(value) {
				this.dirty.schedulePresets = {
					value,
					at: this.now()
				};
				this.scheduleCommit();
			}
			setRunPresets(value) {
				this.dirty.runPresets = {
					value,
					at: this.now()
				};
				this.scheduleCommit();
			}
			/** Relay one user-initiated launch to the engine (non-engine replicas). */
			requestLaunch(taskId, trigger) {
				this.deps.transport.command(this.clientId, {
					type: "run",
					taskId,
					trigger,
					clientId: this.clientId
				}).catch((error) => this.log("[dsh-task-board] launch relay failed", error));
			}
			onRemote(listener) {
				this.remoteListener = listener;
			}
			/** Fires whenever the SEAT changes — either half of it: which replica
			*  holds the engine, or the host's lease protocol version (a host restart
			*  flips the protocol without flipping the seat; the listener re-reads
			*  `hostProtoVersion()` alongside the held flag). */
			onEngine(listener) {
				this.engineListener = listener;
			}
			onCommand(listener) {
				this.commandListener = listener;
			}
			onBackup(listener) {
				this.backupListener = listener;
			}
			/** Stream health (diagnostic only): opens, remote frames, last frame time.
			*  A stream that opened but never delivers is the classic tunnel/proxy kill
			*  — the poll loop below calls it out instead of letting "no sync" stay a
			*  mystery. */
			streamOpens = 0;
			streamFrames = 0;
			lastFrameAt = 0;
			streamDeathNoted = false;
			onStreamEvent(event) {
				if (this.disposed) return;
				this.streamFrames += 1;
				this.lastFrameAt = this.now();
				this.streamDeathNoted = false;
				if (event.type === "commit") {
					if (event.clientId !== this.clientId) this.scheduleResync();
					return;
				}
				if (event.type === "lease") {
					if (event.holder === void 0) this.renewLease();
					else if (event.holder !== this.clientId) this.setEngine(false);
					return;
				}
				if (event.type === "command") {
					if (this.engine) this.commandListener?.(event.command);
				}
			}
			scheduleCommit() {
				if (this.mode !== "synced" || this.disposed) return;
				this.commitAttempts = 0;
				this.commitCancel?.();
				this.commitCancel = this.defer(() => {
					this.commitCancel = void 0;
					this.flush();
				}, this.commitDebounceMs);
			}
			/** Send the current dirty view (one in flight at a time, trailing refire). */
			async flush() {
				if (this.mode !== "synced" || this.disposed) return;
				this.backoffCancel?.();
				this.backoffCancel = void 0;
				if (this.inFlight !== void 0) {
					this.refire = true;
					return;
				}
				const tasks = this.dirty.tasks ?? this.baseline.tasks;
				const snapshot = {};
				if (this.dirty.tasks !== void 0) snapshot.tasks = this.dirty.tasks;
				if (this.dirty.cruise !== void 0) snapshot.cruise = this.dirty.cruise;
				if (this.dirty.schedulePresets !== void 0) snapshot.schedulePresets = this.dirty.schedulePresets;
				if (this.dirty.runPresets !== void 0) snapshot.runPresets = this.dirty.runPresets;
				const commit = {
					clientId: this.clientId,
					tasks,
					changed: [...this.claims],
					sectionClaims: [
						...this.dirty.cruise !== void 0 ? ["cruise"] : [],
						...this.dirty.schedulePresets !== void 0 ? ["schedulePresets"] : [],
						...this.dirty.runPresets !== void 0 ? ["runPresets"] : []
					],
					deleted: [...this.deleted],
					cruise: this.dirty.cruise ?? this.baseline.cruise,
					schedulePresets: this.dirty.schedulePresets ?? this.baseline.schedulePresets,
					runPresets: this.dirty.runPresets ?? this.baseline.runPresets
				};
				this.inFlight = snapshot;
				const sectionNames = [
					...snapshot.tasks !== void 0 ? ["tasks"] : [],
					...snapshot.cruise !== void 0 ? ["cruise"] : [],
					...snapshot.schedulePresets !== void 0 ? ["schedulePresets"] : [],
					...snapshot.runPresets !== void 0 ? ["runPresets"] : []
				];
				let commitBytes = 0;
				try {
					commitBytes = JSON.stringify(commit).length;
				} catch {
					commitBytes = -1;
				}
				this.log(`[dsh-task-board] commit send sections=${sectionNames.join(",") || "none"} bytes=${commitBytes}`);
				const result = await this.deps.transport.commit(commit).catch((error) => {
					this.log("[dsh-task-board] commit transport failed", error);
				});
				this.inFlight = void 0;
				if (this.disposed) return;
				if (result === void 0 || !result.available || result.doc === void 0) {
					this.refire = false;
					this.commitAttempts += 1;
					if (this.commitAttempts > MAX_COMMIT_ATTEMPTS) {
						if (this.commitAttempts === 6) this.log("[dsh-task-board] commit retry budget spent (keeping dirty state parked)");
						return;
					}
					const delay = Math.min(COMMIT_RETRY_BASE_MS * 2 ** (this.commitAttempts - 1), COMMIT_RETRY_CAP_MS);
					this.log(`[dsh-task-board] commit not acked (keeping dirty state, retry ${this.commitAttempts}/${MAX_COMMIT_ATTEMPTS} in ${delay}ms)`);
					this.backoffCancel = this.defer(() => {
						this.backoffCancel = void 0;
						this.flush();
					}, delay);
					return;
				}
				this.commitAttempts = 0;
				this.log(`[dsh-task-board] commit acked revision=${result.doc.revision}`);
				this.adopt(result.doc);
				if (snapshot.tasks !== void 0 && this.dirty.tasks === snapshot.tasks) delete this.dirty.tasks;
				if (snapshot.cruise !== void 0 && this.dirty.cruise === snapshot.cruise) delete this.dirty.cruise;
				if (snapshot.schedulePresets !== void 0 && this.dirty.schedulePresets === snapshot.schedulePresets) delete this.dirty.schedulePresets;
				if (snapshot.runPresets !== void 0 && this.dirty.runPresets === snapshot.runPresets) delete this.dirty.runPresets;
				if (this.dirty.tasks === void 0) {
					this.claims.clear();
					this.deleted = [];
				}
				const refire = this.refire;
				this.refire = false;
				if (refire || this.dirty.tasks !== void 0 || this.dirty.cruise !== void 0 || this.dirty.schedulePresets !== void 0 || this.dirty.runPresets !== void 0) this.scheduleCommit();
			}
			/** Adopt an authoritative document (never backwards) and notify the replica. */
			adopt(doc) {
				if (doc.revision < this.baseline.revision) return;
				if (doc === this.baseline) return;
				this.baseline = doc;
				this.remoteListener?.(this.view(), doc.revision);
			}
			/** Coalesced resync after a remote-change frame. */
			scheduleResync() {
				if (this.resyncCancel !== void 0) return;
				this.resyncCancel = this.defer(() => {
					this.resyncCancel = void 0;
					this.poll();
				}, this.resyncCoalesceMs);
			}
			/** Self-heal a parked writer: when a read proves the host reachable again
			*  (successful poll, reopened stream, foreground return) and dirty state
			*  waits with no timer driving it, probe once through the single funnel.
			*  The probe is BUDGET-NEUTRAL (it never restarts the retry cycle — a
			*  failed probe falls straight back into silence, a success resets the
			*  counter): otherwise every poll would reopen a full backoff chain and
			*  parking would never hold. Only a real user write reopens the budget. */
			probeParked() {
				if (this.mode !== "synced" || this.disposed || this.inFlight !== void 0) return;
				if (this.commitAttempts <= MAX_COMMIT_ATTEMPTS) return;
				if (this.commitCancel !== void 0 || this.backoffCancel !== void 0) return;
				if (this.dirty.tasks === void 0 && this.dirty.cruise === void 0 && this.dirty.schedulePresets === void 0 && this.dirty.runPresets === void 0) return;
				this.commitAttempts = 6;
				this.flush();
			}
			/** Fetch only when the host revision moved past the baseline. */
			async poll() {
				if (this.mode !== "synced" || this.disposed) return;
				if (this.streamOpens > 0 && !this.streamDeathNoted && this.now() - this.lastFrameAt > Math.max(this.pollMs * 2, 3e4)) {
					this.streamDeathNoted = true;
					this.log(`[dsh-task-board] stream suspected dead (opens=${this.streamOpens} frames=${this.streamFrames}, converging by poll)`);
				}
				const result = await this.deps.transport.fetch(this.clientId, this.baseline.revision).catch(() => void 0);
				if (result === void 0) {
					this.log("[dsh-task-board] board resync failed (keeping the last known truth)");
					return;
				}
				this.probeParked();
				if (!result.available || result.unchanged || result.doc === void 0) return;
				this.adopt(result.doc);
			}
			/** Renew (or take) the engine lease; publish SEAT changes (held AND the
			*  host's protocol version — a host restart changes only the latter). The
			*  request carries this tab's VISIBILITY — the host lets a visible replica
			*  preempt a hidden holder, so the engine always sits where the user is. */
			async renewLease() {
				if (this.mode !== "synced" || this.disposed) return;
				const state = await this.deps.transport.lease(this.clientId, {
					ttlMs: this.leaseTtlMs,
					active: this.deps.visibility?.is() ?? true
				}).catch(() => void 0);
				if (state === void 0) return;
				const proto = typeof state.proto === "number" ? state.proto : 1;
				const bootedAt = typeof state.bootedAt === "number" && Number.isFinite(state.bootedAt) ? state.bootedAt : void 0;
				const changed = this.engine !== state.held || this.hostLeaseProto !== proto || this.hostBoot !== bootedAt;
				this.hostLeaseProto = proto;
				this.hostBoot = bootedAt;
				this.engine = state.held;
				if (changed) this.engineListener?.(state.held);
			}
			/** The host's engine-lease protocol (1 = pre-visibility, 2 = preemption);
			*  undefined until the first lease read — "not yet known" is not "old". */
			hostProtoVersion() {
				return this.hostLeaseProto;
			}
			/** When the answering host process booted; undefined until the first lease
			*  read. The stale-host dialog shows it so "我明明重启了" is settled by a
			*  clock reading (old process vs. a different instance behind the URL). */
			hostBootTime() {
				return this.hostBoot;
			}
			/** Yield the seat on a lease frame (the protocol cannot change there —
			*  only the held flag is announced). */
			setEngine(held) {
				if (this.engine === held) return;
				this.engine = held;
				this.engineListener?.(held);
			}
			/** A self-rescheduling loop (first step after `ms`; cancel stops it). */
			every(ms, step) {
				let alive = true;
				let cancel;
				const tick = async () => {
					if (!alive) return;
					try {
						await step();
					} catch (error) {
						this.log("[dsh-task-board] board sync loop failed", error);
					}
					if (!alive) return;
					cancel = this.defer(() => {
						tick();
					}, ms);
				};
				cancel = this.defer(() => {
					tick();
				}, ms);
				return () => {
					alive = false;
					cancel?.();
				};
			}
		};
		/** TaskStore over the synced document (+ optional warm mirror). Reads take
		*  the offline mirror until the host truth is adopted, so first paint never
		*  waits on the network; writes always warm the mirror AND mark the synced
		*  view dirty (a pre-sync write rides the boot migration via the mirror). */
		var SyncedTaskStore = class {
			sync;
			mirror;
			constructor(sync, mirror) {
				this.sync = sync;
				this.mirror = mirror;
			}
			load() {
				if (!this.sync.isSynced()) return this.mirror?.load() ?? this.sync.view().tasks;
				return this.sync.view().tasks;
			}
			save(tasks) {
				this.mirror?.save(tasks);
				this.sync.setTasks(tasks);
			}
			clear() {
				this.mirror?.clear();
				this.sync.setTasks([]);
			}
		};
		/** PresetStore over the synced schedule-presets section. The mirror is
		*  write-only once synced (the synced view is the freshest truth); before
		*  adoption it is the read source (same offline-first discipline as tasks). */
		var SyncedPresetStore = class {
			sync;
			mirror;
			constructor(sync, mirror) {
				this.sync = sync;
				this.mirror = mirror;
			}
			load() {
				if (!this.sync.isSynced()) return this.mirror?.load() ?? this.sync.view().schedulePresets;
				return this.sync.view().schedulePresets;
			}
			save(presets) {
				this.mirror?.save(presets);
				this.sync.setSchedulePresets([...presets]);
			}
			clear() {
				this.mirror?.clear();
				this.sync.setSchedulePresets([]);
			}
		};
		/** RunPresetStore over the synced run-presets section (mirror write-only,
		*  same discipline as SyncedPresetStore). */
		var SyncedRunPresetStore = class {
			sync;
			mirror;
			constructor(sync, mirror) {
				this.sync = sync;
				this.mirror = mirror;
			}
			load() {
				if (!this.sync.isSynced()) return this.mirror?.load() ?? this.sync.view().runPresets;
				return this.sync.view().runPresets;
			}
			save(doc) {
				this.mirror?.save(doc);
				this.sync.setRunPresets(doc);
			}
			clear() {
				this.mirror?.clear();
				this.sync.setRunPresets({ presets: [] });
			}
		};
		/**
		* A cruise-state store over the synced cruise section (the controller's
		* CruiseStorageFace: read returns the current value, write marks it dirty).
		* The offline mirror (write-only, same discipline as the other synced
		* stores) keeps the local cruise key fresh for fallback-mode first paint.
		*/
		var SyncedCruiseStore = class {
			sync;
			mirror;
			constructor(sync, mirror) {
				this.sync = sync;
				this.mirror = mirror;
			}
			read() {
				return this.sync.view().cruise;
			}
			write(state) {
				this.mirror?.write(state);
				this.sync.setCruise(state);
			}
		};
		/**
		* Build a single-flight, TTL-cached, timeout-bounded reader keyed by session
		* id. The returned function never rejects: it resolves the value or undefined.
		*/
		function createTranscriptReader(deps) {
			const cache = /* @__PURE__ */ new Map();
			const now = deps.now ?? (() => Date.now());
			const ttlMs = deps.ttlMs ?? 1200;
			const timeoutMs = deps.timeoutMs ?? 8e3;
			return (sessionId) => {
				const existing = cache.get(sessionId);
				if (existing !== void 0) {
					if (existing.value !== void 0 && now() - existing.settledAt < ttlMs) return Promise.resolve(existing.value);
					if (existing.value === void 0) return existing.promise;
				}
				let settleTimeout;
				const flight = new Promise((resolve) => {
					let done = false;
					const finish = (value) => {
						if (done) return;
						done = true;
						settleTimeout?.();
						resolve(value);
					};
					settleTimeout = deps.defer(() => finish(void 0), timeoutMs);
					deps.read(sessionId).then((value) => finish(value), () => finish(void 0));
				});
				const entry = {
					promise: flight,
					settledAt: now()
				};
				cache.set(sessionId, entry);
				flight.then((value) => {
					if (cache.get(sessionId) !== entry) return;
					if (value === void 0) cache.delete(sessionId);
					else {
						entry.value = value;
						entry.settledAt = now();
					}
				});
				return flight;
			};
		}
		//#endregion
		//#region src/client/board/activity-wake.ts
		/** The official wake event name (host → client, per user message). */
		const ACTIVITY_EVENT = "api-session/activity";
		/** Read a numeric stamp from an unknown row field (absent/garbage = undefined). */
		function stampOf(value) {
			return typeof value === "number" && Number.isFinite(value) ? value : void 0;
		}
		/**
		* Subscribe to the native-activity wake channel. Every wake calls
		* `onWake(sessionId, stamp)`; the controller owns dedup + engine gating +
		* the tail read. @returns the shared disposer.
		*/
		function watchSessionActivity(ctx, onWake) {
			const disposers = [];
			try {
				const remote = ctx.get("remote");
				const subscribe = remote?.$on;
				if (typeof subscribe === "function") disposers.push(subscribe.call(remote, ACTIVITY_EVENT, ((sessionId, updatedAt) => {
					if (typeof sessionId !== "string" || sessionId === "") return;
					const stamp = stampOf(updatedAt);
					if (stamp === void 0) return;
					onWake(sessionId, stamp);
				})));
			} catch {}
			const seen = /* @__PURE__ */ new Map();
			let primed = false;
			const poll = () => {
				let byId;
				try {
					byId = ctx.sessions.list.getSnapshot().byId;
				} catch {
					return;
				}
				if (!primed) {
					primed = true;
					for (const [id, row] of Object.entries(byId)) {
						const stamp = stampOf(row?.updatedAt);
						if (stamp !== void 0) seen.set(id, stamp);
					}
					return;
				}
				for (const [id, row] of Object.entries(byId)) {
					const stamp = stampOf(row?.updatedAt);
					if (stamp === void 0) continue;
					const previous = seen.get(id);
					seen.set(id, stamp);
					if (previous !== void 0 && stamp > previous) onWake(id, stamp);
				}
			};
			try {
				disposers.push(ctx.sessions.list.subscribe(poll));
			} catch {}
			poll();
			return () => {
				for (const dispose of disposers.splice(0)) try {
					dispose();
				} catch {}
			};
		}
		//#endregion
		//#region src/client/board-transport.ts
		/** The board route base path (the naming matrix's `/api/dsh-task-board/*`). */
		const ROUTE = "/api/dsh-task-board/board";
		/**
		* Build the transport. `EventSource` is feature-detected: an environment
		* without it (an exotic WebView) still converges through the poll loop, just
		* with higher latency.
		*
		* Every request is time-bounded (`timeoutMs`, default 15s): a hanging socket
		* (dead tunnel, half-open proxy) must resolve to `undefined` like any other
		* failure — an unsettled boot fetch holds `sync.start()` forever, and the
		* sidebar entry never binds, which is exactly the mobile "点了没反应、进不去".
		* A timeout is a failure like any other: the sync client falls back and the
		* poll/EventSource loop recovers when the line is back.
		*/
		function createBoardTransport(options) {
			const timeoutMs = options?.timeoutMs ?? 15e3;
			const bounded = async (run) => {
				const ctrl = new AbortController();
				const timer = setTimeout(() => {
					ctrl.abort();
				}, timeoutMs);
				try {
					return await run(ctrl.signal);
				} catch {
					return;
				} finally {
					clearTimeout(timer);
				}
			};
			return {
				async fetch(clientId, since) {
					return bounded(async (signal) => {
						try {
							const params = new URLSearchParams({ clientId });
							if (since !== void 0) params.set("since", String(since));
							const response = await fetch(`${ROUTE}?${params.toString()}`, {
								headers: { accept: "application/json" },
								signal
							});
							if (!response.ok) return void 0;
							const envelope = await response.json();
							return envelope.ok ? envelope.value : void 0;
						} catch {
							return;
						}
					});
				},
				async commit(commit) {
					return bounded(async (signal) => {
						try {
							const response = await fetch(ROUTE, {
								method: "POST",
								headers: { "content-type": "application/json" },
								body: JSON.stringify(commit),
								signal
							});
							if (!response.ok) return void 0;
							const envelope = await response.json();
							return envelope.ok ? envelope.value : void 0;
						} catch {
							return;
						}
					});
				},
				async lease(clientId, options) {
					return bounded(async (signal) => {
						try {
							const envelope = await (await fetch(`${ROUTE}/lease`, {
								method: "POST",
								headers: { "content-type": "application/json" },
								body: JSON.stringify(options.release ? {
									clientId,
									release: true
								} : {
									clientId,
									...options.ttlMs !== void 0 ? { ttlMs: options.ttlMs } : {},
									...options.active === false ? { active: false } : {}
								}),
								signal
							})).json();
							return envelope.ok ? envelope.value?.lease : void 0;
						} catch {
							return;
						}
					});
				},
				async command(clientId, command) {
					await bounded(async (signal) => {
						try {
							await fetch(`${ROUTE}/command`, {
								method: "POST",
								headers: { "content-type": "application/json" },
								body: JSON.stringify({
									clientId,
									command
								}),
								signal
							});
						} catch {}
					});
				},
				openStream(clientId, handlers) {
					const Source = globalThis.EventSource;
					if (Source === void 0) return () => void 0;
					const source = new Source(`${ROUTE}/events?clientId=${encodeURIComponent(clientId)}`);
					source.onopen = () => handlers.onOpen();
					source.onmessage = (event) => {
						try {
							handlers.onEvent(JSON.parse(event.data));
						} catch {}
					};
					return () => source.close();
				}
			};
		}
		//#endregion
		//#region src/client/locales.ts
		/**
		* Task-board copy: zh-first dictionaries with an English fallback, selected
		* by the document language. Kept dependency-free (no dsh locale service) so
		* the DOM-injected entry row and the standalone board tree share one tiny
		* lookup.
		*/
		/** zh dictionary (key-set source of truth). */
		const zh = {
			"entry.label": "任务看板",
			"entry.opening": "正在打开任务看板…",
			"board.title": "任务看板",
			"board.close": "返回对话",
			"board.new": "新建任务",
			"board.search": "筛选任务…",
			"board.filterClear": "清空筛选",
			"board.filterRemove": "移除筛选 {token}",
			"board.organize": "整理",
			"board.organizeColor": "卡片颜色",
			"board.organizeCount": "已选 {n} 张卡片",
			"board.organizeSelectAll": "全选",
			"board.organizeClear": "清选",
			"board.organizeDone": "完成",
			"board.organizeRun": "批量执行 {m}/{n} 张",
			"board.organizeRunTitle": "执行所有选中的可执行卡片（空 Prompt 的不动；并发上限节流）",
			"board.organizeDelete": "删除",
			"board.deleteSelectedTitle": "删除选中的 {n} 张卡片？",
			"board.deleteSelectedConfirm": "删除后任务与执行记录不可恢复。",
			"board.deleteSelectedOk": "删除 {n} 张卡片",
			"board.cruiseSettings": "自动巡航设置",
			"board.cruiseLimitShort": "并发",
			"board.cruiseSchedule": "定时窗口",
			"board.cruiseScheduleEmpty": "还没有定时窗口：到点自动开启，可设结束时刻；结束早于开始 = 跨到次日。",
			"board.cruiseWindowStart": "开启时刻",
			"board.cruiseWindowEnd": "结束时刻",
			"board.cruiseWindowStartPlaceholder": "不填=立即开启",
			"board.cruiseWindowEndPlaceholder": "不填=一直保持",
			"board.cruiseWindowRange": "{start} → {end}",
			"board.cruiseWindowFromStart": "{time} 起保持开启",
			"board.cruiseWindowOnlyEnd": "已开启 · 至 {time}",
			"board.cruiseWindowActive": "生效中",
			"board.cruiseWindowAdd": "添加",
			"board.cruiseWindowRemove": "移除",
			"board.cruiseWindowErrorStart": "请先选择开始或结束时刻",
			"board.cruiseWindowErrorSame": "开始与结束相同：请把结束设到开始之后（或次日凌晨）",
			"board.cruiseWindowErrorDuplicate": "该窗口已存在：请调整开始或结束时刻",
			"board.cruiseWindowErrorRange": "结束 {end} 比开始 {start} 早 {days} 天：跨午夜只支持一晚（如 22:00 → 次日 02:00），请检查日期",
			"board.cruiseWindowErrorStartPast": "开始时刻已过：必须晚于当前时间（留空 = 立即开始）",
			"board.cruiseWindowErrorEndPast": "结束时刻已过：必须晚于当前时间",
			"board.cruiseTimePlaceholder": "如 2025-01-05 09:00",
			"board.cruiseCalendar": "打开日历选择日期",
			"board.cruiseTimeInvalid": "时间格式不对，试试 2025-01-05 09:00",
			"board.cruiseNextDay": "次日",
			"board.cruiseSemanticsHint": "开始 = 到点自动开 · 结束 = 到点自动关 · 结束早于开始 = 次日凌晨（仅一晚）",
			"board.cruiseStatusWindow": "窗口开启中 · 至 {time}",
			"board.cruiseStatusWindowHold": "窗口开启中 · 保持开启",
			"board.cruiseStatusManualOn": "手动开启中 · 无定时窗口约束",
			"board.cruiseStatusManualOff": "已手动关闭 · 窗口至 {time} 仍生效",
			"board.cruiseStatusManualOffHold": "已手动关闭 · 窗口保持开启",
			"board.cruiseStatusScheduled": "关闭 · {time} 自动开启",
			"board.cruiseStatusOff": "当前关闭 · 未排程",
			"board.empty": "暂无任务",
			"board.emptyFirstRun": "还没有任务，新建第一张卡开始吧",
			"board.emptyFirstRunHint": "卡片会在 DSH 会话里真跑：执行后就在这一列，跑完落待审核等你通过。",
			"board.emptyFiltered": "没有匹配的任务，调整筛选或清空试试",
			"board.status.backlog": "待规划",
			"board.statusRunning": "正在跑 {n}",
			"board.statusQueued": "排队 {n}",
			"board.statusSkipped": "跳过 {n}",
			"board.heartbeatStale": "心跳停止 · 自 {time} 无调度",
			"board.status": "状态",
			"board.status.todo": "待办",
			"board.status.running": "进行中",
			"board.status.review": "待审核",
			"board.status.done": "已完成",
			"board.statusShort.backlog": "规划",
			"board.statusShort.todo": "待办",
			"board.statusShort.running": "进行",
			"board.statusShort.review": "审核",
			"board.statusShort.done": "完成",
			"board.columnHint.backlog": "待规划池：未承诺，先澄清再拉",
			"board.columnHint.todo": "待办：有空位即可开工",
			"board.columnHint.running": "进行中：一次只做少量，完工才拉新",
			"board.columnHint.review": "待审核：等候他人决断，通过或打回",
			"board.columnHint.done": "已完成：终局，自动化已关闭",
			"board.runs": "次执行",
			"board.updated": "更新于",
			"board.created": "创建于",
			"board.cruise": "自动巡航",
			"board.cruiseTitle": "自动巡航：板级批量执行全部待办任务（受并发上限约束，评论留言经同一调度器注入）",
			"board.cruiseLimit": "并发上限（默认 5，同时最多 N 个会话在执行，含评论续跑）",
			"board.flowStats": "流速{parts}",
			"board.flowDays": "p85 {n}天",
			"board.flowSubDay": "p85 1天内",
			"board.flowThroughput": "吞吐 {n}/周",
			"new.title": "标题",
			"new.titlePlaceholder": "一句话描述要做什么",
			"new.description": "描述",
			"new.descriptionPlaceholder": "补充背景、范围与验收（可选）",
			"new.prompt": "执行 Prompt",
			"new.promptPlaceholder": "发给 agent 的完整指令（可选）",
			"new.attachHint": "附件随每次执行连同文字一起发出：图片最多 {maxImages} 张（自动压缩）；其他类型文件请在会话留言里添加（留言图片最多 {commentImages} 张、文件最多 {commentFiles} 个）。",
			"new.status": "初始状态",
			"new.statusHint": "开启自动化的任务会按计划自动执行（无需先手动执行）；未配置自动化的待办任务需手动「执行」或开启板级「自动巡航」才会跑。",
			"new.submit": "创建",
			"new.cancel": "取消",
			"new.runConfig": "运行配置",
			"new.agentPreset": "Agent",
			"new.agentPresetDefault": "默认（部署默认 Agent）",
			"new.agentPresetDefaultTag": "默认",
			"new.workspace": "工作区",
			"new.workspaceDefault": "默认（最近使用的工作区）",
			"new.model": "模型",
			"new.modelDefault": "默认（会话默认模型）",
			"new.effort": "思考程度",
			"new.effortDefault": "默认（模型默认）",
			"new.permission": "权限",
			"new.permissionDefault": "默认（会话默认权限）",
			"new.runPresets": "配置预设",
			"runPreset.placeholder": "选择配置预设",
			"runPreset.manage": "管理预设",
			"runPreset.manageTitle": "运行配置预设",
			"runPreset.deployDefault": "部署默认",
			"runPreset.deployDefaultHint": "部署默认：工作区/Agent/模型/思考程度/权限全部跟随部署原生默认",
			"runPreset.deployDefaultNote": "跟随部署原生默认",
			"runPreset.defaultBadge": "默认",
			"runPreset.setDefault": "设为默认",
			"runPreset.edit": "编辑",
			"runPreset.editTitle": "编辑此预设（用当前表单的配置更新）",
			"runPreset.delete": "删除",
			"runPreset.deleteTitle": "删除此预设",
			"runPreset.add": "新建预设",
			"runPreset.name": "预设名称",
			"runPreset.namePlaceholder": "例如：日常开发",
			"runPreset.nameRequired": "请填写预设名称",
			"runPreset.save": "保存预设",
			"runPreset.formHint": "直接在下方选择/修改各配置项（工作区/Agent/模型/思考程度/权限）——新建时以当前表单配置为起点。",
			"runPreset.configCount": "{n} 项配置",
			"runPreset.hint": "「设为默认」后每次新建任务都会自动应用它；删除默认预设时自动回退「部署默认」。",
			"detail.title": "任务详情",
			"detail.close": "关闭",
			"detail.edit": "编辑",
			"detail.save": "保存",
			"detail.cancel": "取消",
			"detail.prompt": "执行 Prompt",
			"detail.description": "描述",
			"detail.runConfig": "运行配置（下次执行）",
			"detail.runConfigDefault": "默认设置",
			"detail.runConfigCustom": "已自定义 {n} 项",
			"detail.sessions": "会话",
			"detail.executionHint": "状态为该次执行自身的结果；评论与后续交互见各行动态与评论页",
			"detail.noExecution": "尚未执行",
			"detail.run": "执行",
			"detail.rerun": "重新执行",
			"detail.duplicate": "复制为模板",
			"detail.duplicateTitle": "以当前内容创建一张新的待规划任务卡（执行记录/链接/会话规则不复制，定时规则去武装跟随）",
			"detail.saveTemplate": "存为模板",
			"detail.savedTemplate": "已存为模板",
			"detail.saveTemplateTitle": "把当前内容存入模板库（绑定/执行记录/自动化不带走；新建任务时可从模板开始）",
			"new.fromTemplate": "从模板开始",
			"new.pickTemplate": "选择模板…",
			"new.deleteTemplate": "删除模板",
			"new.deleteTemplateTitle": "删除当前选中的模板（需确认）",
			"new.deleteTemplateConfirm": "确定删除这个模板吗？已用它建好的卡不受影响。",
			"detail.copyPrompt": "复制",
			"detail.copied": "已复制",
			"detail.copyFailed": "复制失败",
			"detail.editDraftRestored": "已恢复你上次未保存的编辑（保存或取消后清除）",
			"color.custom": "自定义颜色",
			"detail.delete": "删除",
			"detail.viewSession": "查看会话",
			"detail.handle": "处理",
			"detail.handleHint": "等待你处理 · {kind}",
			"detail.sessionActive": "会话进行中",
			"detail.executionStarted": "开始",
			"detail.executionEnded": "结束",
			"detail.duration": "耗时 {d}",
			"detail.result.succeeded": "成功",
			"detail.result.failed": "失败",
			"detail.result.cancelled": "已取消",
			"detail.result.running": "进行中",
			"detail.comments": "评论 {n}",
			"detail.latestComment": "最新：「{text}」",
			"detail.hide": "隐藏",
			"detail.hideRow": "隐藏该行（仅不显示，不归档不删除；编号保持不变）",
			"detail.restoreHidden": "恢复全部已隐藏",
			"detail.restoreOne": "恢复",
			"detail.hiddenTrayTitle": "已隐藏 {n} 个会话",
			"detail.sessionRemoveTitle": "移除「{name}」的全部记录？",
			"detail.sessionRemoveConfirm": "该会话在本任务中的评论与执行记录将被永久删除，且不可恢复；任务与其余会话不受影响。",
			"detail.sessionRemoveOk": "移除",
			"detail.linkedDone": "已完成",
			"detail.linkedIdle": "未运行",
			"detail.idleHint": "会话当前没有进行中的对话",
			"detail.sessionPanel": "会话详情",
			"detail.sessionUpdated": "更新于",
			"detail.sessionCountBadge": "会话 · 共 {n} 个",
			"detail.sessionUnavailable": "该会话已不可用，无法发送",
			"detail.sessionNew": "新建会话",
			"detail.sessionNewTitle": "新建会话",
			"detail.addSession": "添加会话",
			"detail.addSessionTitle": "添加已有会话",
			"detail.addSessionSearch": "搜索会话…",
			"detail.addSessionEmpty": "没有可添加的会话",
			"detail.sessionNewHint": "按下方运行配置创建一个全新会话并加入本任务的会话列表；标题/描述/Prompt 属于任务，不在此设置。",
			"detail.sessionNewSubmit": "创建",
			"detail.sessionNewTitleLabel": "会话标题（选填）",
			"detail.sessionNewTitlePlaceholder": "一句话描述要做什么",
			"detail.sessionNewTitleHint": "留空 = 交给宿主自动命名（首次对话后自动生成）；填写 = 立即以此命名并固定，不再被自动改名。",
			"detail.sessionNewTitleFailed": "会话已创建并加入列表，但标题设置未生效：{error}（可在会话行上重命名）",
			"detail.sessionUntitled": "未命名",
			"detail.renameSessionTitle": "重命名会话（与原生会话列表同步）",
			"detail.rename": "重命名",
			"detail.renameOk": "确定",
			"detail.renameCancel": "取消",
			"detail.renameEmpty": "标题不能为空",
			"detail.sessionDrivePlaceholder": "给这个会话留言，等于驱动本任务继续…",
			"detail.sessionDriveHint": "排队按序注入；插话即时送达；排队中可取消。",
			"detail.promptEmpty": "执行 Prompt 为空：先填写执行 Prompt 才能驱动任务。",
			"detail.schedule.blocked": "执行 Prompt 为空",
			"detail.schedule.blockedSession": "会话规则用 Prompt 为空",
			"delete.title": "删除任务",
			"delete.confirm": "确定删除「{name}」吗？删除后不可恢复。",
			"delete.ok": "删除",
			"delete.cancel": "取消",
			"status.move.backlog": "移到待规划",
			"status.move.todo": "移到待办",
			"status.move.done": "移到已完成",
			"board.undoDisarm": "撤销",
			"board.undoDisarmText": "「{title}」已移到已完成，它的定时与规则同时关闭。",
			"board.dragRejectBusy": "这条还有一轮在跑，所以不能移到{column}。等它结束，或先插话。",
			"detail.runBlockedBusy": "这条任务已经有一轮在跑了：等它结束，或先插话。",
			"detail.rerunHint": "以当前最新执行 Prompt 开启全新一轮执行（评论是在旧会话中继续对话）",
			"detail.refine": "需求完善",
			"detail.refine.start": "AI 完善需求",
			"detail.refine.rounds": "已完善 {n} 轮",
			"detail.refine.idleHint": "先让 AI 调研你的需求、向你提问确认，再产出一份可直接执行的执行 Prompt（沿用任务的运行配置，无需额外设置）",
			"detail.refine.answerPlaceholder": "回答 AI 的问题，或继续补充要求…",
			"detail.refine.send": "发送",
			"detail.refine.apply": "应用到任务",
			"detail.refine.applied": "已应用到任务",
			"detail.refine.noResult": "AI 尚未给出结果",
			"detail.refine.emptyHint": "标题、描述、执行 Prompt 全是空的：先写点需求再完善，空需求无法开始",
			"time.justNow": "刚刚",
			"detail.schedule": "自动化",
			"detail.schedule.off": "未启用",
			"detail.schedule.enable": "启用自动执行",
			"detail.schedule.mode.cron": "按时间表",
			"detail.schedule.mode.chain": "完成后接续",
			"detail.schedule.mode.cronHint": "在设定的时间点执行（Cron 表达式）",
			"detail.schedule.mode.chainHint": "上一次运行完成后立即自动开始下一次",
			"detail.schedule.chainNote": "完成后自动接续下一次；失败或达到次数上限即停。运行中保持「进行中」。",
			"detail.schedule.cron": "Cron 表达式",
			"detail.schedule.presets": "预设",
			"detail.schedule.managePresets": "管理预设",
			"detail.schedule.presets.title": "管理预设",
			"detail.schedule.presets.name": "名称",
			"detail.schedule.presets.add": "添加",
			"detail.schedule.presets.save": "保存",
			"detail.schedule.presets.delete": "删除",
			"detail.schedule.presets.newName": "预设名称…",
			"detail.schedule.presets.newCron": "0 9 * * *",
			"detail.schedule.presets.restore": "恢复默认",
			"detail.schedule.presets.restoreConfirm": "将清空全部自定义预设并恢复内置默认列表，确定吗？",
			"detail.schedule.presets.empty": "暂无自定义预设",
			"detail.schedule.presets.custom": "自定义",
			"schedule.desc.everyMinute": "每分钟",
			"schedule.desc.everyMinutes": "每 {n} 分钟",
			"schedule.desc.everyHours": "每 {n} 小时",
			"schedule.desc.dailyAt": "每天 {time}",
			"schedule.desc.weekdaysAt": "工作日 {time}",
			"schedule.desc.weeklyAt": "每周 {days} {time}",
			"schedule.desc.monthlyAt": "每月 {days} 号 {time}",
			"schedule.desc.custom": "自定义表达式",
			"board.automation": "自动化",
			"board.automationTitle": "统一管理任务级自动执行与会话级规则",
			"board.notify": "通知",
			"board.notifyCount": "通知 · {n} 条未读",
			"board.notifyNew": "{base}，其中 {n} 条是刚到的",
			"board.notifyEmpty": "暂无等你处理的事项",
			"board.notifyFilter.all": "全部",
			"board.notifyFilter.waiting": "等你处理",
			"board.notifyFilter.review": "待审核",
			"board.notifyFilterEmpty": "该分组暂无事项",
			"board.notifyMarkAll": "全部标为已读",
			"board.notifyMarkAllTitle": "清除全部未读提醒（内容保留）",
			"board.notifyMarkGroup": "标本组已读",
			"board.notifyMarkGroupTitle": "清除当前分组的未读提醒（内容保留）",
			"board.notifyGoSession": "去会话",
			"board.notifySnooze": "稍后见",
			"board.notifySnoozeTitle": "先收起，下次有动静时再浮起",
			"board.notifyApprove": "通过",
			"review.sendBack": "打回",
			"review.sendBackTitle": "打回 = 回到「待办」，当作还要再改一轮",
			"board.notifyApproveBlocked": "这张卡还有会话在跑 · 等它们结束后再通过",
			"board.notifyMarkOne": "标已读",
			"board.notifyReview": "待审核",
			"board.notifyReviewFailed": "待决策",
			"board.thumbBar": "快捷操作",
			"board.activity": "动态",
			"board.activityTitle": "全板近况：新建、开始、完成、留言、外部、直发、完善（只读，点行进任务详情）",
			"board.activityEmpty": "暂无动态",
			"board.activityCreated": "新建",
			"board.activitySettled": "完成",
			"board.activityComment": "留言",
			"board.activityRefined": "完善",
			"board.activitySucceeded": "成功",
			"board.activityFailed": "失败",
			"board.activityCancelled": "取消",
			"board.activityStarted": "开始",
			"board.activityQueued": "排队中",
			"board.activityRunning": "注入中",
			"board.activityDirect": "直发",
			"board.activityExternal": "外部对话",
			"board.activityFilter.all": "全部",
			"board.activityFilter.run": "执行",
			"board.activityFilter.comment": "留言",
			"board.activityFilter.other": "其他",
			"board.activitySearch": "筛选动态…",
			"board.activityMore": "加载更多（还剩 {n} 条）",
			"board.activityNew": "{n} 条新动态",
			"board.activityGroupRest": "其余 {n} 条已折叠，进详情看完整",
			"board.activityOpen": "进详情",
			"board.activityPreviewHint": "发生于 {time}，点进详情查看完整上下文",
			"board.activityUnviewed": "仅未读",
			"board.dayToday": "今天",
			"board.dayYesterday": "昨天",
			"auto.title": "自动化",
			"auto.empty": "还没有启用任何自动化：在任务详情里启用「自动化」，或给某个任务添加「会话规则」。",
			"auto.openDetail": "详情",
			"auto.editAutomation": "编辑自动化",
			"auto.ruleCount": "{n} 条会话规则",
			"auto.schedule.paused": "已暂停",
			"auto.rules": "会话规则",
			"auto.taskSchedule": "任务自动化",
			"auto.cron": "Cron",
			"auto.rule.enable": "启用",
			"auto.rule.off": "未启用",
			"auto.rule.edit": "编辑",
			"auto.rule.editTitle": "编辑此会话规则",
			"auto.rule.next": "下次 {time}",
			"auto.rule.delete": "删除",
			"auto.rule.deleteTitle": "删除此会话规则",
			"auto.form.new": "新增会话规则",
			"auto.form.session": "目标会话",
			"auto.form.instruction": "执行 Prompt",
			"auto.form.instructionPlaceholder": "定时发送给该会话的自定义指令；/ 开头走原生命令注册表…",
			"auto.form.content": "指令内容",
			"auto.content.custom": "自定义指令",
			"auto.content.usePrompt": "任务执行 Prompt",
			"auto.form.usePromptHint": "发送时使用任务当前的执行 Prompt（改 Prompt 即时生效）；配合按时间表 = 定时注入，配合任务完成后 = 完成后继续、一轮接一轮。",
			"auto.form.cron": "Cron 表达式",
			"auto.form.send": "发送方式",
			"auto.form.save": "保存",
			"auto.form.hint": "/ 开头走命令注册表；排队 = 入队调度器按序注入，插话 = 立即发送。",
			"auto.form.hintLoop": "完成后继续：先让任务完成一次（给会话留言/对话也算），完成后自动发第一条；之后一轮指令成功结束才发下一轮；排队 = 按序等轮次，插话 = 立即注入（都在预算内，随时可关闭）。",
			"auto.rule.awaiting": "等待任务完成触发",
			"auto.rule.awaitingTitle": "自动化已启动：先让任务完成一次（给会话留言/对话也算）才发第一条指令；之后一轮成功结束才自动续下一轮，直到关闭/删除/会话消失。",
			"ref.section.files": "文件与文件夹",
			"ref.section.sessions": "Session 对话",
			"ref.candidate.file": "文件",
			"ref.candidate.folder": "文件夹",
			"ref.candidate.session": "Session",
			"ref.candidate.noCwd": "（无工作目录）",
			"auto.form.invalidSession": "请选择目标会话",
			"auto.form.invalidInstruction": "请填写执行 Prompt",
			"auto.form.invalidCron": "请输入合法的 5 段 Cron 表达式",
			"auto.form.saveFailed": "保存失败，请重试",
			"auto.form.noSession": "该任务还没有关联会话：先把会话拖入任务，才能添加会话规则。",
			"auto.form.trigger": "触发方式",
			"auto.trigger.cron": "按时间表",
			"auto.trigger.onComplete": "任务完成后",
			"auto.rule.onComplete": "完成后继续 · 一轮接一轮",
			"detail.schedule.nextRun": "下次运行",
			"detail.schedule.lastTriggered": "上次触发",
			"detail.schedule.invalid": "Cron 表达式无效",
			"detail.schedule.invalidRuns": "运行次数需为 ≥1 的整数（留空 = 不限次数）",
			"detail.schedule.maxRuns": "最多运行次数",
			"detail.schedule.runsSoFar": "已运行",
			"detail.schedule.notScheduled": "尚未排程",
			"detail.schedule.dueSoon": "即将运行",
			"detail.schedule.unlimited": "不限次数",
			"detail.schedule.skip": "跳过本次",
			"detail.schedule.stopChain": "停止接续",
			"detail.schedule.stopChainTitle": "确认停止接续？",
			"detail.schedule.stopChainConfirm": "停止后不会自动开始下一次运行；需要时在自动化里重新启用即可。",
			"detail.schedule.unlimitedTitle": "确认开启无限接续",
			"detail.schedule.unlimitedConfirm": "「完成后接续」会在每次完成后立即开始下一次、无限重复执行（持续占用真实会话与资源）。确认开启吗？也可以先设置「最多运行次数」来限制。",
			"detail.schedule.unlimitedOk": "开启无限接续",
			"detail.schedule.paused": "已暂停",
			"detail.schedule.pausedFailedShort": "失败",
			"detail.schedule.pausedFailed": "上次自动运行失败，自动执行已暂停。",
			"detail.schedule.paused.review": "任务处于待审核，自动执行已暂停：确认结果后，点「重新执行」或移到待办即可继续。",
			"detail.schedule.paused.backlog": "任务处于待规划，自动执行已暂停：点「执行」立即开始，或移到待办后按计划继续。",
			"detail.schedule.paused.done": "任务已完成：自动执行已暂停，移回「待办」后自动按计划继续。",
			"review.title": "执行评论",
			"review.contextInjection": "上下文注入",
			"review.refresh": "刷新",
			"review.config": "运行配置（当前会话）",
			"review.configApplied": "已应用",
			"review.configLoading": "会话配置读取中…",
			"review.configUnavailable": "会话配置暂不可用",
			"review.model": "模型",
			"review.effort": "思考程度",
			"review.effortDefault": "默认（模型默认）",
			"review.permission": "权限",
			"review.usage": "用量",
			"review.usageTotal": "累计用量",
			"review.usageInput": "输入 {n}",
			"review.usageOutput": "输出 {n}",
			"review.usageCacheRead": "缓存读 {n}",
			"review.usageCacheWrite": "缓存写 {n}",
			"review.usageReasoning": "思考 {n}",
			"review.usageTps": "{n} tok/s",
			"review.usageHitRate": "缓存命中 {p}%",
			"review.statsTurns": "共 {turns} 轮 {steps} 步",
			"review.statsTime": "模型 {llm} · 工具 {tool}",
			"review.statsTtft": "TTFT均值 {d}",
			"review.transcriptEmpty": "暂无对话内容",
			"review.transcriptUnavailable": "会话历史暂不可用",
			"review.retry": "重试",
			"review.loadEarlier": "加载更早的对话",
			"review.loadingEarlierBusy": "正在加载…",
			"review.loadEarlierFailed": "刚才没能加载（点按钮重试）",
			"review.loadEarlierUnsupported": "当前服务端不支持加载更早的对话（升级服务端后可用）",
			"review.imageMissing": "[图片]",
			"detail.emptyField": "暂无内容",
			"review.railHeadTitle": "上下文与运行配置",
			"board.engineViewer": "引擎在另一端 · 排队中",
			"board.engineViewerHint": "定时与排队注入由持有席位的设备执行；该设备不可见时这台会自动接管。排队中的留言在等它。",
			"board.engineStale": "服务端未重启 · 点此了解",
			"board.bundleStale": "页面仍是旧包（客户端 {c} / 服务端 {s}）· 点此强制刷新",
			"board.bundleStaleTitle": "这个页面加载的是旧的客户端包（服务端已在提供新的）。点一下重新加载取回新包；任务数据不受影响。",
			"board.engineNoteOk": "知道了",
			"board.engineNoteRecheck": "重新检查",
			"board.engineNoteProto": "当前连接的服务端协议版本：{n}（看板需要 ≥ {min}）",
			"board.engineNoteBoot": "该服务端进程启动于 {time}。若这不是你刚做的那次重启，说明当前地址把你连到了另一个实例。",
			"board.engineNoteBootUnknown": "该服务端未报告启动时间（早于本看板构建的旧服务端）。",
			"board.engineNoteStaleMulti": "若你确认已重启却仍显示本条，多半是当前地址连的是另一个未重启的服务端进程（例如另一个端口/实例）——重启那个进程，或点「重新检查」确认。",
			"board.engineStaleHint": "看板插件已更新同步与引擎席位的修复，但这些改动在 host 半区，需要重启 dsh web 才生效；当前运行的仍是旧版 host。重启前，执行席位可能停在后台设备上，排队的工作会等待。",
			"review.loading": "加载中…",
			"review.noSession": "该次执行没有关联会话",
			"review.comments": "评论",
			"review.noComments": "还没有评论：留个评论等于在会话中继续对话（自动巡航开启后注入）",
			"review.commentPlaceholder": "给这次执行留个评论，等于在会话中继续对话…",
			"review.commentSend": "发送评论",
			"review.attachFile": "添加附件",
			"review.attachBusy": "图片压缩中…",
			"review.attachFileBusy": "文件上传中…",
			"review.attachWorking": "正在处理附件…",
			"review.draftImagesDropped": "图片太大未随草稿保存（先发送或移除图片）",
			"review.draftFilesGone": "以下文件需重新添加：{names}",
			"attach.rejectType": "「{name}」不是支持的图片类型",
			"attach.rejectSize": "「{name}」压缩后仍超过大小上限",
			"attach.rejectDecode": "「{name}」无法解码为图片",
			"attach.rejectCount": "最多 {max} 张，「{name}」未添加",
			"attach.rejectNoSession": "「{name}」暂不能作为文件发送（会话未就绪）",
			"attach.rejectFileCount": "最多 {max} 个文件，「{name}」未添加",
			"attach.rejectUpload": "「{name}」上传失败：{error}",
			"review.sendMode": "发送方式",
			"review.sendQueue": "排队",
			"review.sendQueueTitle": "排队：经调度器注入（默认）",
			"review.sendSteer": "插话",
			"review.sendSteerTitle": "插话：立即发到该会话，不经队列/预算",
			"review.commentPending": "已保存 · 开启自动巡航后注入",
			"review.commentQueued": "排队中 · 第 {n} 位",
			"review.commentRunning": "运行中",
			"review.commentSucceeded": "成功",
			"review.commentFailed": "失败",
			"review.commentCancelled": "已取消",
			"review.commentImage": "图片消息 (无文本)",
			"review.commentImages": "含 {n} 张图片",
			"review.commentFiles": "含 {n} 个文件",
			"review.commentCancel": "取消",
			"review.jumpLatest": "滑到最新",
			"review.waiting": "等待回应",
			"review.waitingTitle": "会话正在等待你处理（{kind}）：点「查看会话」前往处理",
			"review.meterUsed": "上下文已用",
			"review.meterOf": "上下文已用 {percent}",
			"review.meterFigures": "~{used} / {window}",
			"review.meterSystem": "系统提示词",
			"review.meterTools": "工具",
			"review.meterMessages": "对话消息",
			"review.sessionWorkspace": "会话工作区",
			"review.sessionAgent": "Agent",
			"review.sessionUnknown": "未知",
			"review.sessionDefaultAgent": "部署默认",
			"review.planAwaiting": "计划待确认",
			"review.planConfirm": "确认执行",
			"review.planDecline": "拒绝",
			"review.interactionAmendPlaceholder": "写修改意见（可选）…",
			"review.interactionAmendHint": "填了修改意见点「拒绝」= 以修订反馈提交（不选拒绝标签）；留空点「拒绝」= 直接拒绝",
			"review.questionIndex": "问题 {n} / {total}",
			"review.interactionTypePlaceholder": "输入答案…",
			"review.interactionNext": "下一题",
			"review.interactionNextQuestion": "跳到下一题（不提交）",
			"review.interactionPrev": "上一题",
			"review.interactionSubmit": "提交",
			"review.interactionSkip": "跳过本题",
			"review.interactionAbandon": "放弃整组",
			"review.interactionDiscuss": "去聊天里说",
			"review.interactionGoAnswer": "去会话回答",
			"review.interactionBodyMissing": "未能读取到具体内容，请去会话查看处理",
			"review.interactionRejected": "提交失败：问题已失效或已被处理",
			"review.interactionUnanswered": "请先选择一项或填写答案",
			"review.interactionCollapse": "收起问题卡片",
			"review.interactionExpand": "展开问题卡片",
			"review.interactionProgress": "{n} / {total}",
			"review.interactionSubmitting": "提交中…",
			"review.interactionRecommended": "推荐",
			"review.interactionIncomplete": "请先完成这道问题。",
			"review.sessionContext": "会话上下文",
			"review.todosDone": "已完成 {n}",
			"review.todosActive": "进行中 {n}",
			"review.todosPending": "待办 {n}",
			"review.goalActiveWith": "目标进行中",
			"review.goalActive": "目标进行中",
			"review.goal": "目标",
			"review.goalPhaseActive": "进行中",
			"review.goalPhaseDisarmed": "未运行",
			"review.goalPhasePaused": "已暂停",
			"review.goalPhaseBlocked": "受阻",
			"review.goalObjectiveAria": "目标内容",
			"review.goalPause": "暂停目标",
			"review.goalResume": "恢复目标",
			"review.goalEdit": "编辑目标",
			"review.goalClear": "清除目标",
			"review.goalSave": "保存目标",
			"review.goalCancel": "取消编辑",
			"review.subagents": "{n} 个子代理",
			"card.scheduled": "定时",
			"card.chain": "接续",
			"card.chaining": "接续中",
			"card.chainingTitle": "运行中 · 每次完成后自动接续下一次",
			"card.autoPausedFailed": "失败 · 已暂停",
			"card.autoBlocked": "已阻止",
			"card.autoPausedFailedTitle": "上次自动运行失败，自动执行已暂停（任务在待审核）",
			"card.quickRun": "快速执行：以当前最新 Prompt 开启新一轮执行",
			"card.batchProgress": "批次进度（已运行次数 / 总次数）",
			"card.waiting": "等待回应",
			"waiting.approval": "权限审批",
			"waiting.plan-review": "计划确认",
			"waiting.question": "提问",
			"card.waitingTitle": "会话正在等待你处理（{kind}）：打开任务后「查看会话」前往处理",
			"card.pending": "待处理",
			"card.colorNone": "移除颜色",
			"card.untitled": "未命名任务",
			"card.pendingItem": "第 {n} 次执行：{kind}",
			"card.pendingRefine": "需求完善：{kind}",
			"card.commentQueue": "评论排队",
			"card.refining": "完善中",
			"card.refiningTitle": "需求完善进行中",
			"card.newContent": "新",
			"card.newContentTitle": "有未查看的内容（新执行结果或新评论），点开查看",
			"card.commentQueueTitle": "已保存 {n} 条评论，等待注入（自动巡航开启且有空闲槽位时按顺序执行）",
			"card.commentQueueDone": "已保存 {n} 条评论：任务已完成不会运行它们，移回「待办」后才会按顺序注入",
			"card.nextWaiting": "需你处理 · 点开前往会话",
			"card.nextRunning": "Agent 工作中 · 无需操作",
			"card.nextRefining": "AI 正在完善需求 · 完成后需你确认应用",
			"card.nextQueued": "已排队 {n} 条 · 巡航开启后按序注入",
			"card.nextFailed": "失败待决策 · 留言 steering 或重跑",
			"card.nextReview": "待你确认完成",
			"card.nextScheduled": "已排程 · 到点自动运行",
			"card.nextChain": "接续中 · 完成后自动下一轮",
			"card.awaitingDecision": "待你决断",
			"card.newComment": "新留言",
			"card.newCommentTitle": "这条卡片有没看过的评论",
			"card.keyboardLabel": "{title}，{column}。[ 与 ] 键移到相邻列；回车打开。",
			"card.sessionsForAt": "相关会话 {n} 个：{sessions}",
			"card.awaitingDecisionTitle": "已跑完，等你在待审核列通过或打回；看过之后仍然算未决",
			"board.demand": "等你处理 {n} 项 · 待审核 {m} 张",
			"board.demandTitle": "等你处理 = 挂起等你作答的会话；待审核 = 已跑完但还没通过或打回的任务（看过也算）",
			"prompt.commandList": "命令列表",
			"prompt.noCommands": "无匹配命令",
			"prompt.noReferences": "没有匹配的文件或会话",
			"prompt.loading": "正在查找…",
			"prompt.skillUserOnly": "仅用户可用",
			"board.update.check": "检查更新",
			"board.update.checking": "检查中…",
			"board.update.latest": "已是最新",
			"board.update.available": "有新版 {v}",
			"board.update.availableGit": "远端有新提交",
			"board.update.failed": "检查失败，请重试",
			"board.update.title": "版本更新",
			"board.update.current": "当前版本 {v}",
			"board.update.source": "安装方式：{mode}",
			"board.update.mode.npm": "npm 安装",
			"board.update.mode.github": "GitHub 安装",
			"board.update.mode.local": "本地开发",
			"board.update.mode.unknown": "未知",
			"board.update.newVersion": "最新版本 {v}",
			"board.update.published": "发布于 {time}",
			"board.update.restart": "更新后请重启 dsh web，再刷新页面。任务数据不受影响。",
			"board.update.copy": "复制",
			"board.update.hint.npm": "npm 安装：执行下面命令更新。",
			"board.update.hint.github": "GitHub 安装：执行下面命令更新。",
			"board.update.hint.local": "本地开发：在仓库目录执行下面命令。",
			"board.update.gitBehind": "远端已有新提交（{head} → {remote}），更新命令会拉取它们。",
			"board.update.gitDirty": "工作区有未提交改动，更新前请先存档或提交。",
			"board.update.gitUpToDate": "本地已与远端同步。",
			"settings.title": "任务看板",
			"settings.description": "控制看板在 agent 系统提示中的播报行为。",
			"settings.enabled": "启用任务看板",
			"settings.enabledHint": "关闭后隐藏侧边栏入口与看板视图。",
			"settings.announceToAgent": "向 agent 播报任务看板",
			"settings.announceToAgentHint": "开启：每条 agent 系统提示都会包含本看板的说明；关闭：不播报，agent 仅在用户主动提及时了解看板。",
			"settings.inherit": "继承",
			"settings.on": "开",
			"settings.off": "关",
			"settings.overridden": "已覆盖",
			"settings.reset": "恢复默认",
			"settings.readOnly": "当前部署的设置只读。",
			"settings.expand": "展开设置",
			"settings.collapse": "收起设置",
			"settings.save": "保存",
			"settings.saving": "保存中…",
			"settings.discard": "放弃",
			"settings.unsaved": "未保存",
			"settings.saveFailed": "部署未接受这些值，已保留供你修改。",
			"settings.invalidNumber": "请输入数字，留空则使用默认值。",
			"settings.unavailable": "配置服务暂不可用"
		};
		/** en dictionary, complete against the zh key set. */
		const en = {
			"entry.label": "Task Board",
			"entry.opening": "Opening the task board…",
			"board.title": "Task Board",
			"board.close": "Back to chat",
			"board.new": "New Task",
			"board.search": "Filter tasks…",
			"board.filterClear": "Clear filter",
			"board.filterRemove": "Remove filter {token}",
			"board.organize": "Tidy",
			"board.organizeColor": "Card color",
			"board.organizeCount": "{n} selected",
			"board.organizeSelectAll": "Select all",
			"board.organizeClear": "Clear",
			"board.organizeDone": "Done",
			"board.organizeRun": "Run {m}/{n} selected",
			"board.organizeRunTitle": "Run every selected executable card (blank prompts stay put; the concurrency cap throttles)",
			"board.organizeDelete": "Delete",
			"board.deleteSelectedTitle": "Delete {n} selected cards?",
			"board.deleteSelectedConfirm": "Deleted tasks and their run records cannot be recovered.",
			"board.deleteSelectedOk": "Delete {n} cards",
			"board.cruiseSettings": "Auto-cruise settings",
			"board.cruiseLimitShort": "Concurrency",
			"board.cruiseSchedule": "Scheduled windows",
			"board.cruiseScheduleEmpty": "No windows yet: set a start time to auto-enable; optionally an end; an end before the start crosses to the next day.",
			"board.cruiseWindowStart": "Start time",
			"board.cruiseWindowEnd": "End time",
			"board.cruiseWindowStartPlaceholder": "Blank = start now",
			"board.cruiseWindowEndPlaceholder": "Blank = stay on",
			"board.cruiseWindowRange": "{start} → {end}",
			"board.cruiseWindowFromStart": "From {time}, stays on",
			"board.cruiseWindowOnlyEnd": "On now · until {time}",
			"board.cruiseWindowActive": "active",
			"board.cruiseWindowAdd": "Add",
			"board.cruiseWindowRemove": "Remove",
			"board.cruiseWindowErrorStart": "Pick a start or end time first",
			"board.cruiseWindowErrorSame": "Start and end are the same: put the end after the start (or early the next day)",
			"board.cruiseWindowErrorDuplicate": "This window already exists: adjust the start or end",
			"board.cruiseWindowErrorRange": "End {end} is {days} day(s) before start {start}: cross-midnight is one night only (e.g. 22:00 → 02:00) — check the dates",
			"board.cruiseWindowErrorStartPast": "Start time is in the past: it must be later than now (blank = start now)",
			"board.cruiseWindowErrorEndPast": "End time is in the past: it must be later than now",
			"board.cruiseTimePlaceholder": "e.g. 2025-01-05 09:00",
			"board.cruiseCalendar": "Open the calendar to pick a date",
			"board.cruiseTimeInvalid": "Unrecognized time — try 2025-01-05 09:00",
			"board.cruiseNextDay": "next day",
			"board.cruiseSemanticsHint": "Start = auto-on · end = auto-off · end before start = next-midnight morning (one night only)",
			"board.cruiseStatusWindow": "Window on · until {time}",
			"board.cruiseStatusWindowHold": "Window on · stays on",
			"board.cruiseStatusManualOn": "On manually · no scheduled window",
			"board.cruiseStatusManualOff": "Turned off manually · window still live until {time}",
			"board.cruiseStatusManualOffHold": "Turned off manually · window stays on",
			"board.cruiseStatusScheduled": "Off · {time} auto on",
			"board.cruiseStatusOff": "Off · nothing scheduled",
			"board.empty": "Empty",
			"board.emptyFirstRun": "No tasks yet — create the first card to begin",
			"board.emptyFirstRunHint": "A card really runs inside a DSH session: it works in this column, and lands in Review for you to approve.",
			"board.emptyFiltered": "No matching tasks — adjust or clear the filter",
			"board.status.backlog": "Backlog",
			"board.statusRunning": "Running {n}",
			"board.statusQueued": "Queued {n}",
			"board.statusSkipped": "Skipped {n}",
			"board.heartbeatStale": "Heartbeat stale · none since {time}",
			"board.status": "Status",
			"board.status.todo": "To Do",
			"board.status.running": "In Progress",
			"board.status.review": "In Review",
			"board.status.done": "Done",
			"board.statusShort.backlog": "Plan",
			"board.statusShort.todo": "Todo",
			"board.statusShort.running": "Run",
			"board.statusShort.review": "Review",
			"board.statusShort.done": "Done",
			"board.columnHint.backlog": "Backlog: options, refine before pulling",
			"board.columnHint.todo": "Todo: pull when capacity allows",
			"board.columnHint.running": "Running: keep few in flight, finish before starting new",
			"board.columnHint.review": "Review: needs a human decision, approve or send back",
			"board.columnHint.done": "Done: terminal, automation is off",
			"board.runs": "runs",
			"board.updated": "Updated",
			"board.created": "Created",
			"board.cruise": "Auto Cruise",
			"board.cruiseTitle": "Auto-cruise: runs all todo tasks board-wide within the concurrency cap (comments inject through the same dispatcher)",
			"board.cruiseLimit": "Concurrency cap (default 5: at most N sessions running at once, comments included)",
			"board.flowStats": "Flow {parts}",
			"board.flowDays": "p85 {n}d",
			"board.flowSubDay": "p85 under a day",
			"board.flowThroughput": "throughput {n}/wk",
			"new.title": "Title",
			"new.titlePlaceholder": "What should be done, in one line",
			"new.description": "Description",
			"new.descriptionPlaceholder": "Background, scope, acceptance criteria (optional)",
			"new.prompt": "Run Prompt",
			"new.promptPlaceholder": "The full instruction sent to the agent (optional)",
			"new.attachHint": "Attachments send with the text on every run: up to {maxImages} images (auto-compressed); other file types attach in session comments (up to {commentImages} images, {commentFiles} files).",
			"new.status": "Initial status",
			"new.statusHint": "Tasks with automation enabled run on their own plan (no manual first run needed); plain todo tasks without automation only run when you hit Run or switch on the board's auto-cruise.",
			"new.submit": "Create",
			"new.cancel": "Cancel",
			"new.runConfig": "Run Configuration",
			"new.agentPreset": "Agent",
			"new.agentPresetDefault": "Default (deployment default Agent)",
			"new.agentPresetDefaultTag": "default",
			"new.workspace": "Workspace",
			"new.workspaceDefault": "Default (most recently used workspace)",
			"new.model": "Model",
			"new.modelDefault": "Default (session default model)",
			"new.effort": "Reasoning Effort",
			"new.effortDefault": "Default (model default)",
			"new.permission": "Permission",
			"new.permissionDefault": "Default (session default permission)",
			"new.runPresets": "Run preset",
			"runPreset.placeholder": "Choose a run preset",
			"runPreset.manage": "Manage presets",
			"runPreset.manageTitle": "Run-config presets",
			"runPreset.deployDefault": "Deployment default",
			"runPreset.deployDefaultHint": "Deployment default: workspace/agent/model/effort/permission all follow the native deployment defaults",
			"runPreset.deployDefaultNote": "Follows the native deployment defaults",
			"runPreset.defaultBadge": "Default",
			"runPreset.setDefault": "Set default",
			"runPreset.edit": "Edit",
			"runPreset.editTitle": "Edit this preset (refresh from the current form config)",
			"runPreset.delete": "Delete",
			"runPreset.deleteTitle": "Delete this preset",
			"runPreset.add": "New preset",
			"runPreset.name": "Preset name",
			"runPreset.namePlaceholder": "e.g. daily work",
			"runPreset.nameRequired": "Enter a preset name",
			"runPreset.save": "Save preset",
			"runPreset.formHint": "Pick or change the configuration fields below (workspace/agent/model/effort/permission) — a new preset starts from the current form config.",
			"runPreset.configCount": "{n} fields",
			"runPreset.hint": "After \"Set default\", every new task applies it automatically; deleting the default preset falls back to \"Deployment default\".",
			"detail.title": "Task Detail",
			"detail.close": "Close",
			"detail.edit": "Edit",
			"detail.save": "Save",
			"detail.cancel": "Cancel",
			"detail.prompt": "Run Prompt",
			"detail.description": "Description",
			"detail.runConfig": "Run Config (next run)",
			"detail.runConfigDefault": "Defaults",
			"detail.runConfigCustom": "{n} customized",
			"detail.sessions": "Sessions",
			"detail.executionHint": "The status is this run's own outcome; comments and follow-ups appear in the row dynamics and the review page",
			"detail.noExecution": "Not executed yet",
			"detail.run": "Run",
			"detail.rerun": "Run Again",
			"detail.duplicate": "Duplicate as template",
			"detail.duplicateTitle": "Create a new backlog card from this task (runs, links and session rules are not copied; the schedule comes along disarmed)",
			"detail.saveTemplate": "Save as template",
			"detail.savedTemplate": "Saved as template",
			"detail.saveTemplateTitle": "Save this content to the template library (no bindings/runs/automation; start new tasks from it)",
			"new.fromTemplate": "Start from template",
			"new.pickTemplate": "Pick a template…",
			"new.deleteTemplate": "Delete template",
			"new.deleteTemplateTitle": "Delete the picked template (confirms first)",
			"new.deleteTemplateConfirm": "Delete this template? Cards already made from it are unaffected.",
			"detail.copyPrompt": "Copy",
			"detail.copied": "Copied",
			"detail.copyFailed": "Copy failed",
			"detail.editDraftRestored": "Restored your last unsaved edits (cleared on save or cancel)",
			"color.custom": "Custom color",
			"detail.delete": "Delete",
			"detail.viewSession": "View Session",
			"detail.handle": "Handle",
			"detail.handleHint": "Waiting for you to handle · {kind}",
			"detail.sessionActive": "Session active",
			"detail.executionStarted": "Started",
			"detail.executionEnded": "Ended",
			"detail.duration": "Duration {d}",
			"detail.result.succeeded": "Succeeded",
			"detail.result.failed": "Failed",
			"detail.result.cancelled": "Cancelled",
			"detail.result.running": "Running",
			"detail.comments": "{n} comments",
			"detail.latestComment": "Latest: \"{text}\"",
			"detail.hide": "Hide",
			"detail.hideRow": "Hide this row (display only — not archived, not deleted; numbering stays stable)",
			"detail.restoreHidden": "Restore all hidden",
			"detail.restoreOne": "Restore",
			"detail.hiddenTrayTitle": "{n} hidden sessions",
			"detail.sessionRemoveTitle": "Remove every record of \"{name}\"?",
			"detail.sessionRemoveConfirm": "This session's comments and run records on this task will be deleted permanently and cannot be recovered; the task and its other sessions stay untouched.",
			"detail.sessionRemoveOk": "Remove",
			"detail.linkedDone": "Done",
			"detail.linkedIdle": "Idle",
			"detail.idleHint": "The session has no active conversation",
			"detail.sessionPanel": "Session detail",
			"detail.sessionUpdated": "Updated",
			"detail.sessionCountBadge": "Session · {n} linked",
			"detail.sessionUnavailable": "This session is no longer available",
			"detail.sessionNew": "New Session",
			"detail.sessionNewTitle": "New Session",
			"detail.addSession": "Add Session",
			"detail.addSessionTitle": "Add an Existing Session",
			"detail.addSessionSearch": "Search sessions…",
			"detail.addSessionEmpty": "No sessions to add",
			"detail.sessionNewHint": "Creates a fresh session from the run configuration below and adds it to this task's session list; title/description/prompt belong to the task and are not set here.",
			"detail.sessionNewSubmit": "Create",
			"detail.sessionNewTitleLabel": "Session title (optional)",
			"detail.sessionNewTitlePlaceholder": "What should be done, in one line",
			"detail.sessionNewTitleHint": "Leave blank to let the host name it automatically (generated after the first message); fill it in to name it now and pin the title against auto-renaming.",
			"detail.sessionNewTitleFailed": "The session was created and added to the list, but the title did not apply: {error} (rename it on the session row)",
			"detail.sessionUntitled": "Untitled",
			"detail.renameSessionTitle": "Rename session (syncs with the native session list)",
			"detail.rename": "Rename",
			"detail.renameOk": "OK",
			"detail.renameCancel": "Cancel",
			"detail.renameEmpty": "The title cannot be empty",
			"detail.sessionDrivePlaceholder": "Comment here to drive this task in this session…",
			"detail.sessionDriveHint": "Injected in order; steer sends now; queued ones can be cancelled.",
			"detail.promptEmpty": "Execution prompt is empty: fill it in before driving the task.",
			"detail.schedule.blocked": "Execution prompt is empty",
			"detail.schedule.blockedSession": "A session rule sends an empty execution prompt",
			"delete.title": "Delete Task",
			"delete.confirm": "Delete \"{name}\"? This cannot be undone.",
			"delete.ok": "Delete",
			"delete.cancel": "Cancel",
			"status.move.backlog": "Move to Backlog",
			"status.move.todo": "Move to To Do",
			"status.move.done": "Move to Done",
			"board.undoDisarm": "Undo",
			"board.undoDisarmText": "\"{title}\" moved to Done, so its schedule and rules were switched off.",
			"board.dragRejectBusy": "A round is still running, so this cannot move to {column}. Wait for it, or steer it.",
			"detail.runBlockedBusy": "This task already has a round running: wait for it, or steer it.",
			"detail.rerunHint": "Start a fresh round with the current run prompt (comments continue the old session)",
			"detail.refine": "Refine Requirement",
			"detail.refine.start": "Refine with AI",
			"detail.refine.rounds": "{n} round(s) refined",
			"detail.refine.idleHint": "The AI researches your requirement, asks you anything unclear, then delivers a ready-to-run execution prompt (it reuses the task's run configuration — nothing extra to set up)",
			"detail.refine.answerPlaceholder": "Answer the AI, or add more requirements…",
			"detail.refine.send": "Send",
			"detail.refine.apply": "Apply to task",
			"detail.refine.applied": "Applied to the task",
			"detail.refine.noResult": "No result yet",
			"detail.refine.emptyHint": "Title, description and prompt are all empty: write some requirement first, an empty one cannot start",
			"time.justNow": "just now",
			"detail.schedule": "Automation",
			"detail.schedule.off": "Off",
			"detail.schedule.enable": "Enable auto run",
			"detail.schedule.mode.cron": "On a schedule",
			"detail.schedule.mode.chain": "Run after completion",
			"detail.schedule.mode.cronHint": "Execute at fixed times (cron expression)",
			"detail.schedule.mode.chainHint": "Start the next run as soon as the previous one completes",
			"detail.schedule.chainNote": "Each completed run starts the next automatically; the chain stops on failure or budget. Running keeps the task in progress.",
			"detail.schedule.cron": "Cron expression",
			"detail.schedule.presets": "Presets",
			"detail.schedule.managePresets": "Manage presets",
			"detail.schedule.presets.title": "Manage presets",
			"detail.schedule.presets.name": "Name",
			"detail.schedule.presets.add": "Add",
			"detail.schedule.presets.save": "Save",
			"detail.schedule.presets.delete": "Delete",
			"detail.schedule.presets.newName": "Preset name…",
			"detail.schedule.presets.newCron": "0 9 * * *",
			"detail.schedule.presets.restore": "Restore defaults",
			"detail.schedule.presets.restoreConfirm": "This clears all custom presets and restores the built-in defaults. Continue?",
			"detail.schedule.presets.empty": "No custom presets yet",
			"detail.schedule.presets.custom": "Custom",
			"schedule.desc.everyMinute": "Every minute",
			"schedule.desc.everyMinutes": "Every {n} minutes",
			"schedule.desc.everyHours": "Every {n} hours",
			"schedule.desc.dailyAt": "Daily at {time}",
			"schedule.desc.weekdaysAt": "Weekdays at {time}",
			"schedule.desc.weeklyAt": "{days} at {time}",
			"schedule.desc.monthlyAt": "On the {days} of the month at {time}",
			"schedule.desc.custom": "Custom expression",
			"board.automation": "Automation",
			"board.automationTitle": "Manage task-level auto-run and session rules in one place",
			"board.notify": "Notifications",
			"board.notifyCount": "Notifications · {n} unread",
			"board.notifyNew": "{base}, {n} of them just arrived",
			"board.notifyEmpty": "Nothing waiting on you",
			"board.notifyFilter.all": "All",
			"board.notifyFilter.waiting": "Needs you",
			"board.notifyFilter.review": "Review",
			"board.notifyFilterEmpty": "Nothing in this group",
			"board.notifyMarkAll": "Mark all read",
			"board.notifyMarkAllTitle": "Clear all unread reminders (content stays)",
			"board.notifyMarkGroup": "Mark group read",
			"board.notifyMarkGroupTitle": "Clear unread reminders in the current group (content stays)",
			"board.notifyGoSession": "Open session",
			"board.notifySnooze": "Later",
			"board.notifySnoozeTitle": "Hide for now; resurfaces on next activity",
			"board.notifyApprove": "Approve",
			"review.sendBack": "Send back",
			"review.sendBackTitle": "Send back = return it to Todo, meaning it needs another round",
			"board.notifyApproveBlocked": "Sessions on this card are still running · approve after they finish",
			"board.notifyMarkOne": "Mark read",
			"board.notifyReview": "Review",
			"board.notifyReviewFailed": "Decide",
			"board.thumbBar": "Quick actions",
			"board.activity": "Activity",
			"board.activityTitle": "Board activity: creations, starts, completions, comments, external turns, direct sends, refinements (read-only, rows open the task)",
			"board.activityEmpty": "No activity yet",
			"board.activityCreated": "Created",
			"board.activitySettled": "Settled",
			"board.activityComment": "Comment",
			"board.activityRefined": "Refined",
			"board.activitySucceeded": "Succeeded",
			"board.activityFailed": "Failed",
			"board.activityCancelled": "Cancelled",
			"board.activityStarted": "Started",
			"board.activityQueued": "Queued",
			"board.activityRunning": "Injecting",
			"board.activityDirect": "Direct",
			"board.activityExternal": "External",
			"board.activityFilter.all": "All",
			"board.activityFilter.run": "Runs",
			"board.activityFilter.comment": "Comments",
			"board.activityFilter.other": "Other",
			"board.activitySearch": "Filter activity…",
			"board.activityMore": "Show more ({n} left)",
			"board.activityNew": "{n} new updates",
			"board.activityGroupRest": "{n} more folded — open the task for all",
			"board.activityOpen": "Open task",
			"board.activityPreviewHint": "Happened at {time}; open the task for full context",
			"board.activityUnviewed": "Unviewed only",
			"board.dayToday": "Today",
			"board.dayYesterday": "Yesterday",
			"auto.title": "Automation",
			"auto.empty": "No automation active yet: enable \"Automation\" in a task's detail, or add a \"Session rule\" to a task.",
			"auto.openDetail": "Detail",
			"auto.editAutomation": "Edit automation",
			"auto.ruleCount": "{n} session rules",
			"auto.schedule.paused": "Paused",
			"auto.rules": "Session rules",
			"auto.taskSchedule": "Task automation",
			"auto.cron": "Cron",
			"auto.rule.enable": "On",
			"auto.rule.off": "Off",
			"auto.rule.edit": "Edit",
			"auto.rule.editTitle": "Edit this session rule",
			"auto.rule.next": "Next {time}",
			"auto.rule.delete": "Delete",
			"auto.rule.deleteTitle": "Delete this session rule",
			"auto.form.new": "New session rule",
			"auto.form.session": "Target session",
			"auto.form.instruction": "Run prompt",
			"auto.form.instructionPlaceholder": "The custom instruction to send this session on schedule; a leading / runs through the native command registry…",
			"auto.form.content": "Content",
			"auto.content.custom": "Custom instruction",
			"auto.content.usePrompt": "Task run prompt",
			"auto.form.usePromptHint": "Sends the task's CURRENT run prompt (edits apply at once); with On schedule = timed injection, with After completion = keeps continuing, round after round.",
			"auto.form.cron": "Cron expression",
			"auto.form.send": "Send mode",
			"auto.form.save": "Save",
			"auto.form.hint": "A leading / runs through the command registry; queue = into the dispatcher, steer = immediately.",
			"auto.form.hintLoop": "Continues after completion: it fires from the NEXT completion (a comment or a chat counts); each round fires the next only after it succeeds; queue = in order, steer = immediately (both budget-bound; stop it any time).",
			"auto.rule.awaiting": "Awaiting a completion",
			"auto.rule.awaitingTitle": "Armed: the first instruction fires at the next completion (a comment or a chat in the session counts); after that, each successful round fires the next one until it is turned off or the session disappears.",
			"ref.section.files": "Files & folders",
			"ref.section.sessions": "Session conversations",
			"ref.candidate.file": "File",
			"ref.candidate.folder": "Folder",
			"ref.candidate.session": "Session",
			"ref.candidate.noCwd": "(no cwd)",
			"auto.form.invalidSession": "Choose a target session",
			"auto.form.invalidInstruction": "Enter a run prompt",
			"auto.form.invalidCron": "Enter a valid 5-field cron expression",
			"auto.form.saveFailed": "Failed to save the rule. Please try again.",
			"auto.form.noSession": "This task has no sessions yet: drag a session into the task first to add a session rule.",
			"auto.form.trigger": "Trigger",
			"auto.trigger.cron": "On schedule",
			"auto.trigger.onComplete": "After completion",
			"auto.rule.onComplete": "Continues after completion, round after round",
			"detail.schedule.nextRun": "Next run",
			"detail.schedule.lastTriggered": "Last triggered",
			"detail.schedule.invalid": "Invalid cron expression",
			"detail.schedule.invalidRuns": "Run count must be an integer ≥ 1 (blank = unlimited)",
			"detail.schedule.maxRuns": "Max runs",
			"detail.schedule.runsSoFar": "Runs so far",
			"detail.schedule.notScheduled": "Not scheduled yet",
			"detail.schedule.dueSoon": "Due soon",
			"detail.schedule.unlimited": "unlimited",
			"detail.schedule.skip": "Skip next",
			"detail.schedule.stopChain": "Stop chaining",
			"detail.schedule.stopChainTitle": "Stop this chain?",
			"detail.schedule.stopChainConfirm": "No further run starts automatically after stopping; re-enable it in Automation whenever you want it back.",
			"detail.schedule.unlimitedTitle": "Enable unlimited chaining?",
			"detail.schedule.unlimitedConfirm": "Run-after-completion starts the next run immediately after each one finishes, repeating forever (continuously consuming real sessions and resources). Enable it? You can also set a \"Max runs\" budget to cap the loop.",
			"detail.schedule.unlimitedOk": "Enable unlimited chaining",
			"detail.schedule.paused": "Paused",
			"detail.schedule.pausedFailedShort": "failed",
			"detail.schedule.pausedFailed": "The last automated run failed, so auto-run is paused.",
			"detail.schedule.paused.review": "The task is in Review, so auto-run is paused: after reviewing, hit \"Run Again\" to continue, or move it to To Do to take over.",
			"detail.schedule.paused.backlog": "The task is shelved in Backlog, so auto-run is paused: hit \"Run\" to start now, or move it to To Do to continue on schedule.",
			"detail.schedule.paused.done": "Task is Done: auto-run is paused — move it back to To Do to resume on schedule.",
			"review.title": "Review & Comment",
			"review.contextInjection": "Context injection",
			"review.refresh": "Refresh",
			"review.config": "Run Config (this session)",
			"review.configApplied": "Applied",
			"review.configLoading": "Loading session config…",
			"review.configUnavailable": "Session config unavailable right now",
			"review.model": "Model",
			"review.effort": "Reasoning Effort",
			"review.effortDefault": "Default (model default)",
			"review.permission": "Permission",
			"review.usage": "Usage",
			"review.usageTotal": "Total usage",
			"review.usageInput": "in {n}",
			"review.usageOutput": "out {n}",
			"review.usageCacheRead": "cache read {n}",
			"review.usageCacheWrite": "cache write {n}",
			"review.usageReasoning": "reasoning {n}",
			"review.usageTps": "{n} tok/s",
			"review.usageHitRate": "{p}% cache hit",
			"review.statsTurns": "{turns} turns · {steps} steps",
			"review.statsTime": "model {llm} · tools {tool}",
			"review.statsTtft": "avg TTFT {d}",
			"review.transcriptEmpty": "No conversation yet",
			"review.transcriptUnavailable": "Session history is unavailable right now",
			"review.retry": "Retry",
			"review.loadEarlier": "Load earlier messages",
			"review.loadingEarlierBusy": "Loading…",
			"review.loadEarlierFailed": "That load failed (tap the button to retry)",
			"review.loadEarlierUnsupported": "This host version cannot load earlier messages (update the server)",
			"review.imageMissing": "[image]",
			"detail.emptyField": "Nothing yet",
			"review.railHeadTitle": "Context & run config",
			"board.engineViewer": "engine on another device",
			"board.engineViewerHint": "Scheduled work and queued comments run on the seat holder; a visible device takes the seat over when that one is hidden. Queued comments are waiting for it.",
			"board.engineStale": "Server not restarted · details",
			"board.bundleStale": "Page runs an old bundle ({c} vs server {s}) · tap to force reload",
			"board.bundleStaleTitle": "This page still runs the previous client bundle while the server already serves a newer one. Tap to reload and pick it up; board data is untouched.",
			"board.engineNoteOk": "Got it",
			"board.engineNoteRecheck": "Re-check",
			"board.engineNoteProto": "Connected server protocol: {n} (board needs ≥ {min})",
			"board.engineNoteBoot": "This server process started at {time}. If that is not the restart you just did, the address is serving you from a different instance.",
			"board.engineNoteBootUnknown": "This server has not reported a boot time (a host older than this board build).",
			"board.engineNoteStaleMulti": "If you have confirmed a restart yet this still shows, the current address is most likely connected to a DIFFERENT, un-restarted server process (another port/instance) — restart that one, or press Re-check to confirm.",
			"board.engineStaleHint": "The board plugin ships a newer engine-seat protocol (visibility preemption), but the running host process is older — host changes need a dsh web restart. Until then the seat may stay on a background device and queued work waits.",
			"review.loading": "Loading…",
			"review.noSession": "This run has no session attached",
			"review.comments": "Comments",
			"review.noComments": "No comments yet: leave one to keep the conversation going (injected when auto cruise is on)",
			"review.commentPlaceholder": "Leave a comment to keep talking to the agent in this session…",
			"review.commentSend": "Send comment",
			"review.attachFile": "Add file",
			"review.attachBusy": "Compressing image…",
			"review.attachFileBusy": "Uploading file…",
			"review.attachWorking": "Working on attachments…",
			"review.draftImagesDropped": "Images too large to keep with the draft (send or remove them first)",
			"review.draftFilesGone": "Re-add these files: {names}",
			"attach.rejectType": "\"{name}\" is not a supported image type",
			"attach.rejectSize": "\"{name}\" is still too large after compression",
			"attach.rejectDecode": "\"{name}\" could not be decoded as an image",
			"attach.rejectCount": "At most {max} images — \"{name}\" not added",
			"attach.rejectNoSession": "\"{name}\" cannot be sent as a file yet (session not ready)",
			"attach.rejectFileCount": "At most {max} files — \"{name}\" not added",
			"attach.rejectUpload": "\"{name}\" upload failed: {error}",
			"review.sendMode": "Send mode",
			"review.sendQueue": "Queue",
			"review.sendQueueTitle": "Queue: injected by the dispatcher (default)",
			"review.sendSteer": "Steer",
			"review.sendSteerTitle": "Steer: deliver to the session now, bypassing queue/budget",
			"review.commentPending": "Saved · injected when auto cruise is on",
			"review.commentQueued": "Queued · #{n}",
			"review.commentRunning": "Running",
			"review.commentSucceeded": "Succeeded",
			"review.commentFailed": "Failed",
			"review.commentCancelled": "Cancelled",
			"review.commentImage": "Image message (no text)",
			"review.commentImages": "with {n} image(s)",
			"review.commentFiles": "with {n} file(s)",
			"review.commentCancel": "Cancel",
			"review.jumpLatest": "Latest",
			"review.waiting": "Waiting for you",
			"review.waitingTitle": "The session is waiting for you ({kind}): use \"View session\" to handle it",
			"review.meterUsed": "Context used",
			"review.meterOf": "{percent} of context used",
			"review.meterFigures": "~{used} / {window}",
			"review.meterSystem": "System prompt",
			"review.meterTools": "Tools",
			"review.meterMessages": "Messages",
			"review.sessionWorkspace": "Session workspace",
			"review.sessionAgent": "Agent",
			"review.sessionUnknown": "Unknown",
			"review.sessionDefaultAgent": "Deployment default",
			"review.planAwaiting": "Plan awaiting confirmation",
			"review.planConfirm": "Approve",
			"review.planDecline": "Decline",
			"review.interactionAmendPlaceholder": "Amendment (optional)…",
			"review.interactionAmendHint": "Decline with an amendment = submit it as revision feedback (no decline option); decline empty = refuse outright",
			"review.questionIndex": "Question {n} / {total}",
			"review.interactionTypePlaceholder": "Type an answer…",
			"review.interactionNext": "Next",
			"review.interactionNextQuestion": "Go to the next question (does not submit)",
			"review.interactionPrev": "Previous",
			"review.interactionSubmit": "Submit",
			"review.interactionSkip": "Skip this question",
			"review.interactionAbandon": "Dismiss all",
			"review.interactionDiscuss": "Chat about it",
			"review.interactionGoAnswer": "Answer in session",
			"review.interactionBodyMissing": "Could not read the details — open the session to handle it",
			"review.interactionRejected": "Answer rejected: the question is no longer pending",
			"review.interactionUnanswered": "Pick an option or type an answer first",
			"review.interactionCollapse": "Collapse the question card",
			"review.interactionExpand": "Expand the question card",
			"review.interactionProgress": "{n} / {total}",
			"review.interactionSubmitting": "Submitting…",
			"review.interactionRecommended": "Recommended",
			"review.interactionIncomplete": "Please complete this question first.",
			"review.sessionContext": "Session context",
			"review.todosDone": "{n} done",
			"review.todosActive": "{n} active",
			"review.todosPending": "{n} pending",
			"review.goalActiveWith": "Goal running",
			"review.goalActive": "Goal running",
			"review.goal": "Goal",
			"review.goalPhaseActive": "Ongoing",
			"review.goalPhaseDisarmed": "Inactive",
			"review.goalPhasePaused": "Paused",
			"review.goalPhaseBlocked": "Blocked",
			"review.goalObjectiveAria": "Goal objective",
			"review.goalPause": "Pause goal",
			"review.goalResume": "Resume goal",
			"review.goalEdit": "Edit goal",
			"review.goalClear": "Clear goal",
			"review.goalSave": "Save goal",
			"review.goalCancel": "Cancel edit",
			"review.subagents": "{n} subagents",
			"card.scheduled": "scheduled",
			"card.chain": "chain",
			"card.chaining": "chaining",
			"card.chainingTitle": "Running · continues automatically after each completion",
			"card.autoPausedFailed": "Failed · paused",
			"card.autoBlocked": "Blocked",
			"card.autoPausedFailedTitle": "The last automated run failed and auto-run is paused (task is in review)",
			"card.quickRun": "Quick run: starts a fresh round with the current prompt",
			"card.batchProgress": "Batch progress (runs so far / total runs)",
			"card.waiting": "Waiting for you",
			"waiting.approval": "Approval",
			"waiting.plan-review": "Plan review",
			"waiting.question": "Question",
			"card.waitingTitle": "The session is waiting for you ({kind}): open the task and use \"View session\" to handle it",
			"card.pending": "Pending",
			"card.colorNone": "Remove color",
			"card.untitled": "Untitled task",
			"card.pendingItem": "Run {n}: {kind}",
			"card.pendingRefine": "Refinement: {kind}",
			"card.commentQueue": "Comment queue",
			"card.refining": "Refining",
			"card.refiningTitle": "Requirement refinement in progress",
			"card.newContent": "New",
			"card.newContentTitle": "Unviewed content (new run result or new comments) — open to review",
			"card.commentQueueTitle": "{n} saved comments waiting to be injected (they run in order once auto cruise is on and a slot is free)",
			"card.commentQueueDone": "{n} saved comments: the task is done so they will not run — move the task back to To Do to inject them in order",
			"card.nextWaiting": "Needs you · open to handle",
			"card.nextRunning": "Agent working · nothing to do",
			"card.nextRefining": "Refining requirements · confirm to apply",
			"card.nextQueued": "Queued {n} · injects in order when cruise is on",
			"card.nextFailed": "Failed · steer with a comment or rerun",
			"card.nextReview": "Awaiting your confirmation",
			"card.nextScheduled": "Scheduled · runs automatically",
			"card.nextChain": "Chaining · next round after completion",
			"card.awaitingDecision": "Awaiting your decision",
			"card.newComment": "New comment",
			"card.newCommentTitle": "This card has comments you have not read",
			"card.keyboardLabel": "{title}, {column}. Press [ or ] to move it one column; Enter opens it.",
			"card.sessionsForAt": "{n} related session(s): {sessions}",
			"card.awaitingDecisionTitle": "The run finished and is waiting in Review for you to approve or send back; it still counts after you have looked",
			"board.demand": "{n} need you · {m} in review",
			"board.demandTitle": "Need you = sessions suspended on your answer. In review = runs that finished and nobody has approved or sent back yet (looked-at ones still count)",
			"prompt.commandList": "Command list",
			"prompt.noCommands": "No matching commands",
			"prompt.noReferences": "No matching files or sessions",
			"prompt.loading": "Searching…",
			"prompt.skillUserOnly": "User only",
			"board.update.check": "Check for updates",
			"board.update.checking": "Checking…",
			"board.update.latest": "Up to date",
			"board.update.available": "Update {v}",
			"board.update.availableGit": "Origin has updates",
			"board.update.failed": "Check failed — retry",
			"board.update.title": "Plugin update",
			"board.update.current": "Current version {v}",
			"board.update.source": "Installed via {mode}",
			"board.update.mode.npm": "npm",
			"board.update.mode.github": "GitHub",
			"board.update.mode.local": "local checkout",
			"board.update.mode.unknown": "unknown",
			"board.update.newVersion": "Latest version {v}",
			"board.update.published": "Published {time}",
			"board.update.restart": "After updating, restart dsh web and refresh the page. Board data is untouched.",
			"board.update.copy": "Copy",
			"board.update.hint.npm": "npm install: run the command below to update.",
			"board.update.hint.github": "GitHub install: run the command below to update.",
			"board.update.hint.local": "Local checkout: run the command below in the repository directory.",
			"board.update.gitBehind": "New commits on origin ({head} → {remote}) — the update command pulls them.",
			"board.update.gitDirty": "Uncommitted changes in the worktree — stash or commit before pulling.",
			"board.update.gitUpToDate": "The local checkout is in sync with origin.",
			"settings.title": "Task Board",
			"settings.description": "How the board announces itself in each agent system prompt.",
			"settings.enabled": "Enable the task board",
			"settings.enabledHint": "When off, the sidebar entry and board view are hidden.",
			"settings.announceToAgent": "Announce the task board to agents",
			"settings.announceToAgentHint": "On: every agent system prompt includes a note about this board. Off: no announcement; agents learn about the board only when you mention it.",
			"settings.inherit": "Inherit",
			"settings.on": "On",
			"settings.off": "Off",
			"settings.overridden": "Overridden",
			"settings.reset": "Reset to default",
			"settings.readOnly": "This deployment stores settings read-only.",
			"settings.expand": "Show settings",
			"settings.collapse": "Hide settings",
			"settings.save": "Save",
			"settings.saving": "Saving…",
			"settings.discard": "Discard",
			"settings.unsaved": "Unsaved",
			"settings.saveFailed": "The deployment did not accept these values; they were left for you to correct.",
			"settings.invalidNumber": "Enter a number, or leave blank to use the default.",
			"settings.unavailable": "Configuration service is temporarily unavailable."
		};
		/** Whether the active UI language is English (drives unit-style formatting). */
		function isEnglish() {
			return (typeof document !== "undefined" ? document.documentElement.lang : "zh").toLowerCase().startsWith("en");
		}
		/** Active dictionary, picked by the document language at call time. */
		function dictionary() {
			return isEnglish() ? en : zh;
		}
		/** Translate a key with optional {name} template params. */
		function t(key, params) {
			let text = dictionary()[key];
			if (params !== void 0) for (const [name, value] of Object.entries(params)) text = text.replaceAll(`{${name}}`, value);
			return text;
		}
		//#endregion
		//#region \0dsh-css:src/client/board.module.css.mjs
		const css$1 = "[data-dsh-taskboard-view]{--dsh-tb-glass:var(--dsw-alias-bg-base);--dsh-tb-bg:var(--dsw-alias-bg-base);--dsh-tb-bg-raised:var(--dsw-alias-bg-layer-2);--dsh-tb-surface-float:var(--dsw-alias-bg-layer-1);--dsh-tb-surface-sunken:var(--dsw-alias-bg-layer-2);--dsh-tb-surface-menu:var(--dsw-specific-menu);--dsh-tb-chip-fill:var(--dsh-tb-input-bg);--dsh-tb-mask:color-mix(in srgb, var(--dsw-alias-bg-mask-1) 60%, var(--dsw-alias-bg-mask-3));--dsh-tb-border:1px solid var(--dsw-alias-border-l2);--dsh-tb-border-soft:1px solid var(--dsw-alias-border-l1);--dsh-tb-separator:none;--dsh-tb-radius-sm:10px;--dsh-tb-radius-md:14px;--dsh-tb-radius-lg:20px;--dsh-tb-radius-xl:28px;--dsh-tb-pill:999px;--dsh-tb-strip-gap:10px;--dsh-tb-button-radius:var(--dsh-tb-pill);--dsh-tb-button-h:28px;--dsh-tb-button-h-sm:24px;--dsh-tb-rail-inset:14px;--dsh-tb-lead-icon:12px;--dsh-tb-lead-gap:7px;--dsh-tb-lead-x:calc(var(--dsh-tb-lead-icon) + var(--dsh-tb-lead-gap));--dsh-tb-row-line:20px;--dsh-tb-dot:6px;--dsh-tb-hint-line:18px;--dsh-tb-shadow-1:var(--dsw-shadow-lv1);--dsh-tb-shadow-2:var(--dsw-shadow-lv2);--dsh-tb-shadow-3:var(--dsw-shadow-lv3);--dsh-tb-text-1:var(--dsw-alias-label-primary);--dsh-tb-text-2:var(--dsw-alias-label-secondary);--dsh-tb-text-3:var(--dsw-alias-label-tertiary);--dsh-tb-accent:var(--dsw-alias-state-business-primary);--dsh-tb-attention:var(--dsw-alias-state-warn-primary);--dsh-tb-success:var(--dsw-alias-state-success-primary);--dsh-tb-danger:var(--dsw-alias-state-error-primary);--dsh-tb-attention-alpha:color-mix(in srgb, var(--dsh-tb-attention) 22%, transparent);--dsh-tb-attention-alpha-soft:color-mix(in srgb, var(--dsh-tb-attention) 10%, transparent);--dsh-tb-ink-on-fill:var(--dsw-static-neutral-bluish-1000);--dsh-tb-hover:var(--dsw-alias-interactive-bg-hover);--dsh-tb-input-bg:var(--dsw-specific-input-major);--dsh-tb-focus-ring:color-mix(in srgb, var(--dsw-alias-state-business-primary) 22%, transparent);--dsh-tb-focus-color:var(--dsw-alias-state-business-primary);--dsh-tb-motion:.16s ease;--dsh-tb-breath:2.6s ease-in-out;--dsh-tb-breath-spread:3px;--dsh-tb-breath-halo:14px}@keyframes Sr3ygG_dshTbBreathRing{0%,to{box-shadow:0 0 #0000}50%{box-shadow:0 0 0 var(--dsh-tb-breath-spread) var(--dsh-tb-breath-color,var(--dsh-tb-attention-alpha))}}@keyframes Sr3ygG_dshTbBreathHalo{0%,to{box-shadow:inset 0 0 #0000}50%{box-shadow:inset 0 0 var(--dsh-tb-breath-halo) var(--dsh-tb-breath-color,var(--dsh-tb-attention-alpha-soft))}}[data-pane=conversation],[class*=centerCol]{position:relative}[data-dsh-taskboard-view]{z-index:200;-webkit-text-size-adjust:100%;text-size-adjust:100%;display:none;position:absolute;inset:0;container:Sr3ygG_dsh-tb/inline-size}html[data-dsh-taskboard-active] [data-dsh-taskboard-view]{display:block}[data-dsh-taskboard-view] p{margin:0}html[data-dsh-taskboard-active] [data-pane=conversation]>:not([data-dsh-taskboard-view]),html[data-dsh-taskboard-active] [class*=centerCol]>:not([data-dsh-taskboard-view]){display:none!important}.Sr3ygG_sidebarFooterAction{box-sizing:border-box;cursor:pointer;width:auto;min-width:0;height:42px;color:var(--dsw-alias-label-primary);background:0 0;border:none;border-radius:12px;flex:1;align-items:center;gap:8px;margin:0;padding:0 10px 0 8px;font-family:inherit;font-size:14px;line-height:22px;display:flex;overflow:hidden}.Sr3ygG_sidebarFooterAction:hover{background:var(--dsw-alias-interactive-bg-hover)}.Sr3ygG_sidebarFooterAction[data-pending]{opacity:.7;animation:1.4s ease-in-out infinite Sr3ygG_dshTbFooterPending}@keyframes Sr3ygG_dshTbFooterPending{0%,to{opacity:1}50%{opacity:.55}}.Sr3ygG_sidebarFooterIcon{flex:none;justify-content:center;align-items:center;width:16px;height:16px;display:inline-flex}.Sr3ygG_sidebarFooterLabel{white-space:nowrap;overflow:hidden}.Sr3ygG_board{box-sizing:border-box;background:var(--dsh-tb-bg);min-width:0;height:100%;min-height:0;color:var(--dsh-tb-text-1);font-family:var(--dsw-font-family);flex-direction:column;gap:12px;padding:14px 16px 16px;display:flex}.Sr3ygG_boardHeader{flex-direction:column;flex:none;gap:10px;display:flex}.Sr3ygG_boardRow{flex:none;align-items:center;gap:10px;min-width:0;display:flex}.Sr3ygG_boardBack{width:28px;height:28px;color:var(--dsh-tb-text-2);border-radius:var(--dsh-tb-pill);cursor:pointer;transition:background var(--dsh-tb-motion), color var(--dsh-tb-motion);background:0 0;border:none;flex:none;justify-content:center;align-items:center;padding:0;display:inline-flex}.Sr3ygG_boardBack:hover{background:var(--dsh-tb-hover);color:var(--dsh-tb-text-1)}.Sr3ygG_boardBack:focus-visible{outline:2px solid var(--dsw-alias-state-business-primary);outline-offset:2px}.Sr3ygG_boardTitle{letter-spacing:-.02em;min-width:0;color:var(--dsh-tb-text-1);white-space:nowrap;text-overflow:ellipsis;margin:0;font-size:16px;font-weight:600;overflow:hidden}.Sr3ygG_boardSpacer{flex:1;min-width:12px}.Sr3ygG_boardState{flex:0 auto;align-items:center;gap:10px;min-width:0;display:inline-flex}.Sr3ygG_boardModes{flex:none;align-items:center;gap:10px;display:inline-flex}.Sr3ygG_notifyBell{width:28px;height:28px;position:relative}.Sr3ygG_notifyBadge{min-width:16px;height:16px;color:var(--dsh-tb-ink-on-fill);background:var(--dsh-tb-attention);border-radius:var(--dsh-tb-pill);font-variant-numeric:tabular-nums;white-space:nowrap;justify-content:center;align-items:center;padding:0 4px;font-size:11px;font-weight:600;line-height:1;display:inline-flex;position:absolute;top:-4px;right:-6px}.Sr3ygG_notifyDot{background:var(--dsh-tb-attention);border-radius:50%;width:8px;height:8px;position:absolute;top:-2px;left:-4px}.Sr3ygG_notifyList{flex-direction:column;gap:4px;min-width:0;margin:0;padding:0;list-style:none;display:flex}.Sr3ygG_notifyRow{box-sizing:border-box;width:100%;min-width:0;color:var(--dsh-tb-text-1);border-radius:var(--dsh-tb-radius-sm);text-align:left;font:inherit;background:0 0;border:none;flex-wrap:wrap;align-items:center;gap:8px;padding:8px 10px;display:flex}@media (hover:hover){.Sr3ygG_notifyRow:hover{background:var(--dsh-tb-hover)}}.Sr3ygG_notifyMain{min-width:0;color:inherit;cursor:pointer;text-align:left;font:inherit;background:0 0;border:none;flex:1;align-items:center;gap:8px;padding:0;display:flex}.Sr3ygG_notifyActions{flex-wrap:wrap;flex:none;align-items:center;gap:6px;display:inline-flex}.Sr3ygG_notifyTask{text-overflow:ellipsis;white-space:nowrap;flex:1;min-width:0;font-size:13px;overflow:hidden}.Sr3ygG_notifySession{text-overflow:ellipsis;white-space:nowrap;max-width:40%;color:var(--dsh-tb-text-3);flex:none;font-size:12px;overflow:hidden}.Sr3ygG_boardRowTools{justify-content:space-between}.Sr3ygG_boardNewTask{flex:none}.Sr3ygG_thumbBar{display:none}.Sr3ygG_boardStatus{min-width:0;color:var(--dsh-tb-text-3);flex:0 auto;align-items:center;gap:6px;font-size:12px;line-height:1;display:inline-flex}.Sr3ygG_boardStatusText{text-overflow:ellipsis;white-space:nowrap;min-width:0;overflow:hidden}.Sr3ygG_dragRejectHint{max-width:100%;color:var(--dsh-tb-attention);align-self:flex-start;margin:0;font-size:12px;line-height:18px}.Sr3ygG_undoDisarmRow{max-width:100%;color:var(--dsh-tb-text-2);flex-wrap:wrap;align-self:flex-start;align-items:center;gap:8px;margin:0;font-size:12px;line-height:18px;display:flex}.Sr3ygG_undoDisarmText{overflow-wrap:anywhere;min-width:0}.Sr3ygG_boardDemand{min-width:0;height:var(--dsh-tb-button-h);color:var(--dsh-tb-attention);background:var(--dsh-tb-chip-fill);border:var(--dsh-tb-border);border-radius:var(--dsh-tb-pill);cursor:pointer;-webkit-appearance:none;transition:background var(--dsh-tb-motion), border-color var(--dsh-tb-motion);flex:0 auto;align-self:flex-start;align-items:center;gap:6px;padding:0 12px;font-size:12px;font-weight:500;line-height:1;display:inline-flex}.Sr3ygG_boardDemand:hover{background:var(--dsh-tb-hover);border-color:var(--dsw-alias-border-l3)}.Sr3ygG_boardDemand:focus-visible{outline:2px solid var(--dsh-tb-focus-color);outline-offset:2px}.Sr3ygG_boardDemandDot{width:var(--dsh-tb-dot);height:var(--dsh-tb-dot);background:var(--dsh-tb-attention);border-radius:50%;flex:none}.Sr3ygG_boardDemandText{text-overflow:ellipsis;white-space:nowrap;min-width:0;overflow:hidden}.Sr3ygG_boardStatusButton{min-width:0;font:inherit;color:var(--dsh-tb-text-3);cursor:pointer;background:0 0;border:none;flex:0 auto;align-items:center;gap:6px;margin:0;padding:0;font-size:12px;line-height:1;display:inline-flex}.Sr3ygG_boardStatusButton:hover{color:var(--dsh-tb-text-1)}.Sr3ygG_boardStatusButton[data-warn=true]{color:var(--dsh-tb-attention)}.Sr3ygG_boardStatusButton[data-warn=true] .Sr3ygG_boardStatusDot{background:var(--dsh-tb-attention);box-shadow:0 0 4px var(--dsh-tb-attention)}.Sr3ygG_boardStatus[data-warn=true]{color:var(--dsh-tb-attention)}.Sr3ygG_boardStatus[data-warn=true] .Sr3ygG_boardStatusDot{background:var(--dsh-tb-attention);box-shadow:0 0 4px var(--dsh-tb-attention)}.Sr3ygG_boardStatusDot{background:var(--dsh-tb-accent);width:6px;height:6px;box-shadow:0 0 4px var(--dsh-tb-accent);border-radius:50%;flex:none;transform:translateY(-.5px)}.Sr3ygG_search{box-sizing:border-box;min-width:140px;max-width:520px;height:var(--dsh-tb-button-h);color:var(--dsh-tb-text-1);background:var(--dsh-tb-chip-fill);border:var(--dsh-tb-border-soft);border-radius:var(--dsh-tb-pill);outline:none;flex:260px;padding:0 12px;font-size:13px}.Sr3ygG_search::placeholder{color:var(--dsh-tb-text-3)}.Sr3ygG_cruiseWrap{flex:none;position:relative}.Sr3ygG_cruisePill{height:var(--dsh-tb-button-h);box-sizing:border-box;border-radius:var(--dsh-tb-pill);transition:background var(--dsh-tb-motion);gap:2px;padding:0 4px 0 6px}.Sr3ygG_cruisePill:hover{background:var(--dsh-tb-hover)}.Sr3ygG_cruiseMore{width:22px;height:22px;color:var(--dsh-tb-text-3);border-radius:var(--dsh-tb-pill);cursor:pointer;transition:background var(--dsh-tb-motion), color var(--dsh-tb-motion);background:0 0;border:none;flex:none;justify-content:center;align-items:center;padding:0;display:inline-flex}.Sr3ygG_cruiseMore:hover{background:var(--dsh-tb-hover);color:var(--dsh-tb-text-1)}.Sr3ygG_cruiseMore:focus-visible{outline:2px solid var(--dsw-alias-state-business-primary);outline-offset:1px}:is(.Sr3ygG_cruisePopover,.Sr3ygG_slashMenu,.Sr3ygG_sessionContextPanel){border:var(--dsh-tb-border);position:absolute}.Sr3ygG_cruisePopover{z-index:30;background:var(--dsh-tb-surface-float);border-radius:var(--dsh-tb-radius-lg);width:300px;box-shadow:var(--dsh-tb-shadow-3);color:var(--dsh-tb-text-1);flex-direction:column;gap:12px;padding:14px;animation:.14s Sr3ygG_dshTbFadeUp;display:flex;top:calc(100% + 6px);right:0}.Sr3ygG_cruisePopoverHead{justify-content:space-between;align-items:center;gap:10px;display:flex}.Sr3ygG_cruisePopoverLimit{align-items:center;gap:6px;display:inline-flex}.Sr3ygG_cruisePopoverLabel{color:var(--dsh-tb-text-2);white-space:nowrap;font-size:12px}.Sr3ygG_cruiseSchedule{border-top:var(--dsh-tb-separator);flex-direction:column;gap:8px;padding-top:8px;display:flex}.Sr3ygG_cruiseScheduleHead{justify-content:space-between;align-items:baseline;gap:8px;display:flex}.Sr3ygG_cruiseScheduleTitle{color:var(--dsh-tb-text-3);white-space:nowrap;flex:none;font-size:12px;font-weight:500}.Sr3ygG_cruiseStateLine{text-overflow:ellipsis;white-space:nowrap;min-width:0;color:var(--dsh-tb-text-2);font-size:11px;overflow:hidden}.Sr3ygG_cruiseWindowList{flex-direction:column;gap:2px;margin:0;padding:0;list-style:none;display:flex}.Sr3ygG_cruiseWindowRow{border-radius:var(--dsh-tb-radius-sm);align-items:center;gap:8px;padding:3px 6px;animation:.14s backwards Sr3ygG_dshTbFadeUp;display:flex}.Sr3ygG_cruiseWindowRow:hover{background:var(--dsh-tb-hover)}.Sr3ygG_cruiseWindowTime{text-overflow:ellipsis;white-space:nowrap;font-variant-numeric:tabular-nums;min-width:0;color:var(--dsh-tb-text-2);flex:1;font-size:12px;display:block;overflow:hidden}.Sr3ygG_cruiseWindowAdd{flex-direction:column;gap:8px;display:flex}.Sr3ygG_timeField{flex-direction:column;gap:4px;min-width:0;display:flex;position:relative}.Sr3ygG_timeFieldLabel{color:var(--dsh-tb-text-2);white-space:nowrap;font-size:12px}.Sr3ygG_timeFieldRow{align-items:center;gap:6px;min-width:0;display:flex}.Sr3ygG_timeFieldRow .Sr3ygG_input{font-variant-numeric:tabular-nums;flex:1;width:100%;min-width:0;height:30px;padding:0 10px;font-size:12px}.Sr3ygG_timeFieldCalendar{width:30px;height:30px;color:var(--dsh-tb-text-2);border:var(--dsh-tb-border-soft);border-radius:var(--dsh-tb-pill);cursor:pointer;transition:background var(--dsh-tb-motion), color var(--dsh-tb-motion), box-shadow var(--dsh-tb-motion), transform var(--dsh-tb-motion);background:0 0;flex:none;justify-content:center;align-items:center;padding:0;display:inline-flex}.Sr3ygG_timeFieldCalendar:hover{background:var(--dsh-tb-hover);color:var(--dsh-tb-text-1)}.Sr3ygG_timeFieldCalendar:active{background:var(--dsh-tb-hover);box-shadow:inset 0 1px 2px color-mix(in srgb, var(--dsw-static-neutral-1000) 16%, transparent);transform:translateY(1px)}.Sr3ygG_timeFieldCalendar:focus-visible{outline:2px solid var(--dsw-alias-state-business-primary);outline-offset:1px}.Sr3ygG_timeFieldHiddenDate{opacity:0;pointer-events:none;width:1px;height:1px;position:absolute}.Sr3ygG_timeFieldError{color:var(--dsw-alias-state-error-primary);font-size:11px}.Sr3ygG_timeFieldHint{color:var(--dsh-tb-text-2);font-size:11px}:is(.Sr3ygG_columnTabs,.Sr3ygG_cards,.Sr3ygG_modalBackdrop,.Sr3ygG_columns){scrollbar-width:none}:is(.Sr3ygG_columnTabs,.Sr3ygG_cards,.Sr3ygG_modalBackdrop,.Sr3ygG_columns)::-webkit-scrollbar{display:none}.Sr3ygG_columns{flex:1;grid-template-columns:repeat(5,minmax(0,1fr));gap:12px;min-height:0;display:grid}.Sr3ygG_columnTabs{flex:none;gap:6px;padding:1px 2px 4px;display:none;overflow-x:auto}.Sr3ygG_columnTab{box-sizing:border-box;border:var(--dsh-tb-border);border-radius:var(--dsh-tb-pill);background:var(--dsh-tb-chip-fill);height:28px;color:var(--dsh-tb-text-2);font:inherit;cursor:pointer;transition:color var(--dsh-tb-motion), border-color var(--dsh-tb-motion);flex:none;gap:6px;padding:0 10px;font-size:12px}.Sr3ygG_columnTab:hover{color:var(--dsh-tb-text-1)}.Sr3ygG_columnTab[data-active=true]{color:var(--dsh-tb-text-1);border-color:var(--dsh-tb-accent);background:color-mix(in srgb, var(--dsh-tb-accent) 10%, transparent)}.Sr3ygG_columnTab:focus-visible{outline:2px solid var(--dsh-tb-accent);outline-offset:1px}.Sr3ygG_columnTabCount{color:var(--dsh-tb-text-3);font-size:11px}.Sr3ygG_column{background:var(--dsh-tb-bg);border:var(--dsh-tb-border-soft);border-radius:var(--dsh-tb-radius-lg);flex-direction:column;min-height:0;display:flex;overflow:hidden}.Sr3ygG_columnHeader{flex:none;align-items:center;gap:6px;padding:10px 20px;display:flex}.Sr3ygG_columnTitle{color:var(--dsh-tb-text-1);text-overflow:ellipsis;white-space:nowrap;flex:1;margin:0;font-size:13px;font-weight:500;overflow:hidden}.Sr3ygG_columnCount{min-width:0;font-family:inherit;font-size:12px;line-height:inherit;color:var(--dsh-tb-text-3);background:var(--dsh-tb-surface-sunken);border-radius:var(--dsh-tb-pill);cursor:inherit;border:1px solid #0000;flex:none;padding:0 7px}.Sr3ygG_statusDot{border-radius:50%;flex:none;width:8px;height:8px;transform:translateY(-.5px)}.Sr3ygG_statusDot[data-status=backlog]{background:var(--dsw-alias-label-tertiary)}.Sr3ygG_statusDot[data-status=todo]{background:var(--dsw-alias-state-business-primary)}.Sr3ygG_statusDot[data-status=running]{background:var(--dsw-alias-state-warn-primary)}.Sr3ygG_statusDot[data-status=review]{background:var(--dsw-alias-state-warn-secondary)}.Sr3ygG_statusDot[data-status=done]{background:var(--dsw-alias-state-success-primary)}.Sr3ygG_cards{scroll-behavior:smooth;flex-direction:column;flex:1;gap:8px;min-height:0;padding:6px 8px 22px;display:flex;position:relative;overflow-y:auto}@media (prefers-reduced-motion:reduce){.Sr3ygG_cards{scroll-behavior:auto}}.Sr3ygG_columnEmpty{text-align:center;color:var(--dsh-tb-text-2);pointer-events:none;flex-direction:column;gap:6px;padding:16px 8px;font-size:12px;line-height:1.5;display:flex}.Sr3ygG_columnEmptyHint{color:var(--dsh-tb-text-2);overflow-wrap:anywhere;font-size:12px}.Sr3ygG_card{text-align:left;background:color-mix(in srgb, var(--card-tint,transparent) 6%, var(--dsh-tb-surface-float));border:var(--dsh-tb-border);border-radius:var(--dsh-tb-radius-md);cursor:pointer;min-width:0;color:var(--dsh-tb-text-1);transition:box-shadow var(--dsh-tb-motion), border-color var(--dsh-tb-motion), transform var(--dsh-tb-motion), background var(--dsh-tb-motion);flex-direction:column;flex:none;gap:6px;padding:9px 12px;font-family:inherit;display:flex;position:relative;overflow:hidden}.Sr3ygG_card.Sr3ygG_selectedCard{border-color:var(--dsw-alias-state-business-primary);box-shadow:0 0 0 3px color-mix(in srgb, var(--dsw-alias-state-business-primary) 22%, transparent), 0 0 0 5px color-mix(in srgb, var(--dsw-alias-state-business-primary) 10%, transparent), 0 4px 18px color-mix(in srgb, var(--dsw-alias-state-business-primary) 22%, transparent), var(--dsh-tb-shadow-2);animation:Sr3ygG_dshTbSelectIn var(--dsh-tb-motion) ease-out;transform:translateY(-1px)}@keyframes Sr3ygG_dshTbSelectIn{0%{opacity:.55}to{opacity:1}}.Sr3ygG_cardSelectedBadge{z-index:2;width:18px;height:18px;color:var(--dsh-tb-ink-on-fill);background:var(--dsw-alias-state-business-primary);pointer-events:none;border-radius:50%;justify-content:center;align-items:center;display:inline-flex;position:absolute;top:8px;right:8px}.Sr3ygG_card.Sr3ygG_selectedCard .Sr3ygG_cardQuickRun{right:32px}.Sr3ygG_card:hover{box-shadow:var(--dsh-tb-shadow-2);border-color:var(--dsw-alias-border-l3);transform:translateY(-1px)}.Sr3ygG_card.Sr3ygG_selectedCard:hover{border-color:var(--dsw-alias-state-business-primary);box-shadow:0 0 0 3px color-mix(in srgb, var(--dsw-alias-state-business-primary) 22%, transparent), 0 0 0 5px color-mix(in srgb, var(--dsw-alias-state-business-primary) 10%, transparent), 0 4px 18px color-mix(in srgb, var(--dsw-alias-state-business-primary) 22%, transparent), var(--dsh-tb-shadow-2)}.Sr3ygG_dropIndicator{background:var(--dsw-alias-state-business-primary);height:4px;box-shadow:0 0 0 1px var(--dsw-alias-bg-base);pointer-events:none;z-index:1;border-radius:3px;position:absolute;left:8px;right:8px}.Sr3ygG_column[data-dragreject]{animation:.18s Sr3ygG_dshTbRejectFlash}@keyframes Sr3ygG_dshTbRejectFlash{0%,to{border-color:var(--dsh-tb-border-soft)}50%{border-color:var(--dsh-tb-danger)}}.Sr3ygG_card[data-status=running]{border-color:var(--dsw-alias-state-warn-primary)}.Sr3ygG_cardQuickRun{z-index:1;width:26px;height:26px;color:var(--dsh-tb-text-2);background:var(--dsh-tb-surface-float);border:1px solid var(--dsw-alias-border-l2);border-radius:var(--dsh-tb-pill);cursor:pointer;box-shadow:var(--dsh-tb-shadow-1);opacity:0;pointer-events:none;transition:opacity var(--dsh-tb-motion), background var(--dsh-tb-motion), color var(--dsh-tb-motion), border-color var(--dsh-tb-motion);justify-content:center;align-items:center;padding:0;display:inline-flex;position:absolute;top:8px;right:8px}.Sr3ygG_card:hover .Sr3ygG_cardQuickRun,.Sr3ygG_cardQuickRun:focus-visible{opacity:1;pointer-events:auto}.Sr3ygG_cardQuickRun:hover{color:var(--dsh-tb-accent);background:var(--dsh-tb-hover);border-color:var(--dsw-alias-border-l3)}.Sr3ygG_cardQuickRun[aria-disabled=true]{opacity:0;cursor:default}.Sr3ygG_card[data-unviewed]{animation:Sr3ygG_dshTbBreathRing var(--dsh-tb-breath) infinite}.Sr3ygG_card[data-active]{animation:Sr3ygG_dshTbBreathHalo var(--dsh-tb-breath) infinite}.Sr3ygG_cardTitleRow{align-items:flex-start;gap:7px;min-width:0;display:flex}.Sr3ygG_cardColorMark{width:8px;height:8px;box-shadow:0 0 0 1px color-mix(in srgb, var(--card-tint) 30%, transparent);border-radius:50%;flex:none;margin-top:5px}.Sr3ygG_cardTitle{overflow-wrap:anywhere;-webkit-line-clamp:2;-webkit-box-orient:vertical;min-width:0;font-size:14px;font-weight:500;line-height:1.35;display:-webkit-box;overflow:hidden}.Sr3ygG_cardExcerpt{min-width:0;color:var(--dsw-alias-label-secondary);overflow-wrap:anywhere;-webkit-line-clamp:2;-webkit-box-orient:vertical;font-size:12px;line-height:1.4;display:-webkit-box;overflow:hidden}.Sr3ygG_cardMeta{min-width:0;color:var(--dsw-alias-label-tertiary);flex-direction:column;gap:6px;margin-top:0;padding-top:6px;font-size:11px;display:flex}.Sr3ygG_cardMetaRow{align-items:center;gap:8px;min-width:0;display:flex}.Sr3ygG_cardWorkspace{min-width:0;color:var(--dsw-alias-label-secondary);flex:1;align-items:center;gap:5px;display:inline-flex}.Sr3ygG_cardWorkspaceDot{background:var(--dsw-alias-label-tertiary);border-radius:50%;flex:none;width:6px;height:6px}.Sr3ygG_cardWorkspaceName{text-overflow:ellipsis;white-space:nowrap;min-width:0;overflow:hidden}.Sr3ygG_cardTime{text-overflow:ellipsis;white-space:nowrap;flex:0 auto;min-width:0;overflow:hidden}.Sr3ygG_cardBadges{flex-wrap:wrap;align-items:center;gap:8px;min-width:0;display:flex}.Sr3ygG_cardSessions{flex-wrap:wrap;align-items:center;gap:6px;min-width:0;display:flex}.Sr3ygG_cardSessionDot{background:var(--dsw-alias-label-tertiary);border-radius:50%;flex:none;width:8px;height:8px}.Sr3ygG_cardSessionDot[data-state=running]{background:var(--dsw-alias-state-warn-primary)}.Sr3ygG_cardSessionDot[data-state=waiting]{background:var(--dsw-alias-state-warn-secondary)}.Sr3ygG_cardSessionMore{color:var(--dsw-alias-label-tertiary);font-variant-numeric:tabular-nums;flex:none;font-size:11px}.Sr3ygG_visuallyHidden{clip-path:inset(50%);white-space:nowrap;border:0;width:1px;height:1px;margin:-1px;padding:0;position:absolute;overflow:hidden}.Sr3ygG_cardNext{min-width:0;color:var(--dsw-alias-label-secondary);overflow-wrap:anywhere;-webkit-line-clamp:2;-webkit-box-orient:vertical;font-size:12px;line-height:1.4;display:-webkit-box;overflow:hidden}.Sr3ygG_feedTools{flex-wrap:wrap;align-items:center;gap:8px;min-width:0;margin-bottom:8px;display:flex}.Sr3ygG_feedFilterGroup{flex-wrap:wrap;align-items:center;gap:6px;min-width:0;display:inline-flex}.Sr3ygG_filterChips{scrollbar-width:none;align-items:center;gap:6px;min-width:0;padding:2px 0 6px;display:flex;overflow-x:auto}.Sr3ygG_filterChips::-webkit-scrollbar{display:none}.Sr3ygG_filterChip{flex:none;align-items:center;gap:4px;display:inline-flex}.Sr3ygG_filterChipRemove{cursor:pointer;color:inherit;opacity:.6;background:0 0;border:none;flex:none;padding:0 2px;font-size:12px;line-height:1}.Sr3ygG_filterChipRemove:hover{opacity:1}.Sr3ygG_feedFilter{height:24px;color:var(--dsh-tb-text-2);border:var(--dsh-tb-border-soft);border-radius:var(--dsh-tb-pill);cursor:pointer;background:0 0;flex:none;padding:0 10px;font-size:12px}.Sr3ygG_feedFilter[data-active]{color:var(--dsh-tb-ink-on-fill);background:var(--dsw-alias-state-business-primary);border-color:#0000}.Sr3ygG_feedSearch{min-width:120px;height:24px;color:var(--dsh-tb-text-1);background:var(--dsh-tb-chip-fill);border:var(--dsh-tb-border-soft);border-radius:var(--dsh-tb-pill);flex:1;padding:0 10px;font-size:12px}.Sr3ygG_feedAction{height:24px;color:var(--dsh-tb-text-2);border:var(--dsh-tb-border-soft);border-radius:var(--dsh-tb-pill);cursor:pointer;background:0 0;flex:none;padding:0 10px;font-size:12px}.Sr3ygG_feedAction:hover{background:var(--dsh-tb-hover)}.Sr3ygG_feedDay{color:var(--dsw-alias-label-secondary);margin:10px 0 4px;font-size:12px;font-weight:600}.Sr3ygG_feedPreview{min-width:0;padding:4px 10px 8px}:is(.Sr3ygG_chip,.Sr3ygG_chipFill,.Sr3ygG_cruisePill,.Sr3ygG_columnTab){align-items:center;display:inline-flex}.Sr3ygG_chip{min-width:0;max-width:100%;color:var(--dsw-alias-label-secondary);white-space:nowrap;flex:0 auto;gap:5px;font-size:12px;font-weight:500;line-height:1.5}.Sr3ygG_chipBody{text-overflow:ellipsis;white-space:nowrap;min-width:0;display:block;overflow:hidden}.Sr3ygG_chipLead{flex:none;align-items:center;display:inline-flex}.Sr3ygG_chipFill{border-radius:var(--dsh-tb-pill);background:var(--dsh-tb-chip-fill);padding:2px 8px}.Sr3ygG_chip[data-kind=success]{color:var(--dsw-alias-state-success-primary)}.Sr3ygG_chip[data-kind=error]{color:var(--dsw-alias-state-error-primary)}.Sr3ygG_chip[data-kind=warn]{color:var(--dsw-alias-state-warn-primary)}.Sr3ygG_chip[data-kind=muted]{color:var(--dsw-alias-label-tertiary)}.Sr3ygG_spinner{border:2px solid;border-top-color:#0000;border-radius:50%;flex:none;width:10px;height:10px;animation:.8s linear infinite Sr3ygG_dshTbSpin;display:inline-block}@keyframes Sr3ygG_dshTbSpin{to{transform:rotate(360deg)}}.Sr3ygG_primaryButton,.Sr3ygG_ghostButton,.Sr3ygG_dangerButton,.Sr3ygG_dangerGhostButton{box-sizing:border-box;height:var(--dsh-tb-button-h);border-radius:var(--dsh-tb-button-radius);white-space:nowrap;cursor:pointer;-webkit-appearance:none;transition:background var(--dsh-tb-motion), border-color var(--dsh-tb-motion), color var(--dsh-tb-motion), box-shadow var(--dsh-tb-motion), transform var(--dsh-tb-motion);justify-content:center;align-items:center;gap:6px;padding:0 14px;font-size:13px;font-weight:500;line-height:1;display:inline-flex}.Sr3ygG_primaryButton{color:var(--dsw-alias-label-primary-foreground);background:var(--dsw-alias-button-primary-fill);box-shadow:var(--dsh-tb-shadow-1);border:1px solid #0000}.Sr3ygG_primaryButton:hover:not(:disabled){background:var(--dsw-alias-button-primary-hover);box-shadow:var(--dsh-tb-shadow-2)}.Sr3ygG_primaryButton:active:not(:disabled){box-shadow:inset 0 1px 2px color-mix(in srgb, var(--dsw-static-neutral-1000) 14%, transparent);transform:translateY(1px)}.Sr3ygG_primaryButton:disabled{opacity:.5;cursor:default}.Sr3ygG_ghostButton{color:var(--dsh-tb-text-1);background:var(--dsh-tb-chip-fill);border:var(--dsh-tb-border);padding:0 12px;font-size:12px}.Sr3ygG_ghostButton:hover:not(:disabled){background:var(--dsh-tb-hover);border-color:var(--dsw-alias-border-l3)}.Sr3ygG_ghostButton:active:not(:disabled){background:var(--dsw-alias-interactive-bg-pressed,var(--dsh-tb-hover))}.Sr3ygG_ghostButton:disabled{opacity:.45;cursor:default}.Sr3ygG_buttonPressed{background:var(--dsh-tb-hover);border-color:var(--dsw-alias-border-l3)}.Sr3ygG_dangerButton{color:var(--dsw-alias-label-primary-foreground);background:var(--dsh-tb-danger);box-shadow:var(--dsh-tb-shadow-1);border:1px solid #0000}.Sr3ygG_dangerButton:hover:not(:disabled){filter:brightness(1.07)}.Sr3ygG_dangerButton:active:not(:disabled){filter:brightness(.94);transform:translateY(1px)}.Sr3ygG_dangerButton:disabled{opacity:.5;cursor:default}.Sr3ygG_dangerGhostButton{color:var(--dsh-tb-danger);border:1px solid color-mix(in srgb, var(--dsh-tb-danger) 45%, transparent);background:0 0;padding:0 12px;font-size:12px}.Sr3ygG_dangerGhostButton:hover:not(:disabled){background:color-mix(in srgb, var(--dsh-tb-danger) 10%, transparent);border-color:var(--dsh-tb-danger)}.Sr3ygG_dangerGhostButton:active:not(:disabled){background:color-mix(in srgb, var(--dsh-tb-danger) 22%, transparent)}.Sr3ygG_dangerGhostButton:disabled{opacity:.45;cursor:default}.Sr3ygG_buttonSm{box-sizing:border-box;height:var(--dsh-tb-button-h-sm);border-radius:var(--dsh-tb-pill);padding:0 10px;font-size:12px}.Sr3ygG_iconButton{border-radius:var(--dsh-tb-pill);width:30px;height:30px;color:var(--dsh-tb-text-2);cursor:pointer;transition:background var(--dsh-tb-motion), color var(--dsh-tb-motion);background:0 0;border:none;justify-content:center;align-items:center;padding:0;font-size:14px;display:inline-flex}.Sr3ygG_iconButton:hover{background:var(--dsh-tb-hover);color:var(--dsh-tb-text-1)}.Sr3ygG_boardModes .Sr3ygG_notifyBell{background:var(--dsh-tb-chip-fill);border:var(--dsh-tb-border)}.Sr3ygG_switch{cursor:pointer;user-select:none;align-items:center;gap:8px;display:inline-flex}.Sr3ygG_switch:has(input:disabled){opacity:.5;cursor:default}.Sr3ygG_switch input{opacity:0;pointer-events:none;width:0;height:0;position:absolute}.Sr3ygG_switchTrack{border-radius:var(--dsh-tb-pill);background:var(--dsw-alias-interactive-bg-pressed,var(--dsh-tb-hover));border:var(--dsh-tb-border-soft);width:38px;height:22px;transition:background var(--dsh-tb-motion), border-color var(--dsh-tb-motion);flex:none;position:relative}.Sr3ygG_switchKnob{background:var(--dsw-alias-button-elevated-fill);width:16px;height:16px;box-shadow:0 1px 3px color-mix(in srgb, var(--dsw-static-neutral-1000) 32%, transparent);transition:transform var(--dsh-tb-motion);border-radius:50%;position:absolute;top:2px;left:2px}.Sr3ygG_switch input:checked+.Sr3ygG_switchTrack{background:var(--dsh-tb-success);border-color:#0000}.Sr3ygG_switch input:checked+.Sr3ygG_switchTrack .Sr3ygG_switchKnob{transform:translate(16px)}.Sr3ygG_switch input:focus-visible+.Sr3ygG_switchTrack{outline:2px solid var(--dsh-tb-focus-color);outline-offset:2px}.Sr3ygG_switchLabel{color:var(--dsh-tb-text-1);font-size:13px}.Sr3ygG_modalBackdrop{z-index:40;padding:24px 24px calc(24px + var(--dsh-tb-kb,0px));overscroll-behavior:contain;background:var(--dsh-tb-mask);animation:.14s Sr3ygG_dshTbFadeIn;display:flex;position:absolute;inset:0;overflow:hidden auto}:is(.Sr3ygG_modalScroll,.Sr3ygG_detailBody,.Sr3ygG_commentsScroll,.Sr3ygG_reviewRail,.Sr3ygG_sessionRailFolds,.Sr3ygG_reviewTranscriptScroll,.Sr3ygG_interactionCardBody,.Sr3ygG_slashMenu,.Sr3ygG_sessionContextPanel,.Sr3ygG_refineTranscriptArea){scrollbar-width:thin;scrollbar-color:var(--dsw-alias-border-l3) transparent}:is(.Sr3ygG_modalScroll,.Sr3ygG_detailBody,.Sr3ygG_commentsScroll,.Sr3ygG_reviewRail,.Sr3ygG_sessionRailFolds,.Sr3ygG_reviewTranscriptScroll,.Sr3ygG_interactionCardBody,.Sr3ygG_slashMenu,.Sr3ygG_sessionContextPanel,.Sr3ygG_refineTranscriptArea)::-webkit-scrollbar{width:8px;height:8px}:is(.Sr3ygG_modalScroll,.Sr3ygG_detailBody,.Sr3ygG_commentsScroll,.Sr3ygG_reviewRail,.Sr3ygG_sessionRailFolds,.Sr3ygG_reviewTranscriptScroll,.Sr3ygG_interactionCardBody,.Sr3ygG_slashMenu,.Sr3ygG_sessionContextPanel,.Sr3ygG_refineTranscriptArea)::-webkit-scrollbar-thumb{background:var(--dsw-alias-border-l3);background-clip:padding-box;border-radius:4px}:is(.Sr3ygG_modalScroll,.Sr3ygG_detailBody,.Sr3ygG_commentsScroll,.Sr3ygG_reviewRail,.Sr3ygG_sessionRailFolds,.Sr3ygG_reviewTranscriptScroll,.Sr3ygG_interactionCardBody,.Sr3ygG_slashMenu,.Sr3ygG_sessionContextPanel,.Sr3ygG_refineTranscriptArea)::-webkit-scrollbar-track{background:0 0}@keyframes Sr3ygG_dshTbFadeIn{0%{opacity:0}to{opacity:1}}@keyframes Sr3ygG_dshTbDialogIn{0%{opacity:0;transform:translateY(6px)scale(.985)}to{opacity:1;transform:translateY(0)scale(1)}}@keyframes Sr3ygG_dshTbFadeUp{0%{opacity:0;transform:translateY(3px)}to{opacity:1;transform:translateY(0)}}.Sr3ygG_modal,.Sr3ygG_detail,.Sr3ygG_review{background:var(--dsh-tb-surface-float);border:var(--dsh-tb-border);border-radius:var(--dsh-tb-radius-xl);max-height:100%;box-shadow:var(--dsh-tb-shadow-3);color:var(--dsh-tb-text-1);animation:Sr3ygG_dshTbDialogIn var(--dsh-tb-motion);flex-direction:column;margin:auto;display:flex;position:relative;overflow:hidden}.Sr3ygG_modal{width:min(520px,100%)}.Sr3ygG_modalScroll{flex-direction:column;flex:1;gap:16px;min-height:0;padding:14px 18px;display:flex;overflow-y:auto}.Sr3ygG_modalTitle{letter-spacing:-.01em;margin:0;font-size:16px;font-weight:600}.Sr3ygG_dialogHeader{border-bottom:var(--dsh-tb-separator);flex:none;justify-content:space-between;align-items:center;gap:10px;padding:14px 18px;display:flex}.Sr3ygG_confirmMessage{color:var(--dsw-alias-label-secondary);white-space:pre-wrap;overflow-wrap:break-word;margin:0;font-size:13px;line-height:1.5}.Sr3ygG_modalFooter{border-top:var(--dsh-tb-separator);flex:none;justify-content:flex-end;gap:10px;padding:14px 18px;display:flex}.Sr3ygG_field{flex-direction:column;gap:5px;display:flex}.Sr3ygG_fieldLabel{color:var(--dsw-alias-label-secondary);font-size:12px;font-weight:500}.Sr3ygG_templateRow{align-items:center;gap:8px;display:flex}.Sr3ygG_templateRow .Sr3ygG_selectWrap{flex:auto;min-width:0}.Sr3ygG_fieldHint{color:var(--dsw-alias-label-tertiary);font-size:12px;line-height:1.5}.Sr3ygG_input{height:34px;color:var(--dsh-tb-text-1);background:var(--dsh-tb-input-bg);border:var(--dsh-tb-border-soft);border-radius:var(--dsh-tb-radius-md);resize:vertical;-webkit-appearance:none;transition:border-color var(--dsh-tb-motion), box-shadow var(--dsh-tb-motion);outline:none;padding:0 12px;font-family:inherit;font-size:13px}textarea.Sr3ygG_input{height:auto;padding:8px 12px;line-height:1.5}.Sr3ygG_input:focus{border-color:var(--dsh-tb-focus-color);box-shadow:0 0 0 3px var(--dsh-tb-focus-ring)}.Sr3ygG_input::placeholder{color:var(--dsh-tb-text-3)}.Sr3ygG_input::-webkit-resizer{display:none}.Sr3ygG_formError{color:var(--dsw-alias-state-error-primary);margin:0;font-size:12px}.Sr3ygG_selectWrap{--dsh-tb-select-arrow-inset:30px;min-width:0;display:block;position:relative}.Sr3ygG_selectWrap select{-webkit-appearance:none;appearance:none;width:100%;min-width:0;padding-right:var(--dsh-tb-select-arrow-inset);text-overflow:ellipsis;white-space:nowrap;cursor:pointer;overflow:hidden}.Sr3ygG_selectWrap:after{content:\"\";top:50%;right:calc(var(--dsh-tb-select-arrow-inset) / 2 - 3px);border-right:1.5px solid var(--dsw-alias-label-tertiary);border-bottom:1.5px solid var(--dsw-alias-label-tertiary);pointer-events:none;width:7px;height:7px;position:absolute;transform:translateY(-70%)rotate(45deg)}.Sr3ygG_promptField{flex-direction:column;display:flex;position:relative}.Sr3ygG_slashMenu{z-index:30;background:var(--dsh-tb-surface-menu);border-radius:var(--dsh-tb-radius-md);max-height:280px;box-shadow:var(--dsw-shadow-lv3);padding:4px;top:calc(100% + 4px);left:0;right:0;overflow-y:auto}.Sr3ygG_slashMenu[data-direction=up]{top:auto;bottom:calc(100% + 4px)}.Sr3ygG_promptTextarea{resize:none;overflow-y:hidden}.Sr3ygG_slashMenuRow{text-align:left;width:100%;color:var(--dsw-alias-label-primary);border-radius:var(--dsh-tb-radius-sm);cursor:pointer;background:0 0;border:none;align-items:center;gap:8px;padding:6px 8px;font-size:12px;display:flex}.Sr3ygG_slashMenuRow:hover,.Sr3ygG_slashMenuRowActive{background:var(--dsw-alias-interactive-bg-hover)}.Sr3ygG_slashMenuName{flex:none;font-weight:500}.Sr3ygG_slashMenuDesc{text-overflow:ellipsis;white-space:nowrap;min-width:0;color:var(--dsw-alias-label-secondary);flex:1;overflow:hidden}.Sr3ygG_slashMenuHint{color:var(--dsw-alias-label-tertiary);flex:none}.Sr3ygG_slashMenuEmpty{color:var(--dsw-alias-label-tertiary);padding:6px 8px;font-size:12px}.Sr3ygG_slashMenuSection{text-transform:uppercase;letter-spacing:.02em;color:var(--dsw-alias-label-tertiary);padding:5px 8px 2px;font-size:11px;font-weight:600}.Sr3ygG_detail{width:min(640px,100%)}.Sr3ygG_detailHeader{border-bottom:var(--dsh-tb-separator);flex:none;align-items:center;gap:12px;padding:14px 18px;display:flex}.Sr3ygG_detailHeader .Sr3ygG_iconButton{width:28px;height:var(--dsh-tb-button-h)}.Sr3ygG_detailTitle{letter-spacing:-.01em;overflow-wrap:break-word;flex:1;min-width:0;margin:0;font-size:16px;font-weight:600}.Sr3ygG_detailBody{flex-direction:column;flex:1;gap:18px;min-height:0;padding:14px 18px;display:flex;overflow-y:auto}.Sr3ygG_detailSection{flex-direction:column;gap:6px;display:flex}.Sr3ygG_detailDisclosure{cursor:pointer;text-align:left;background:0 0;border:none;align-items:center;gap:6px;width:100%;padding:0;display:flex}.Sr3ygG_detailChevron{color:var(--dsw-alias-label-tertiary);flex:none;font-size:11px;transition:transform .14s}.Sr3ygG_detailDisclosure[aria-expanded=false] .Sr3ygG_detailChevron,.Sr3ygG_notifyMain[aria-expanded=false] .Sr3ygG_detailChevron{transform:rotate(-90deg)}.Sr3ygG_detailDisclosureTitle{color:var(--dsh-tb-text-3);flex:none;font-size:12px;font-weight:500}.Sr3ygG_detailDisclosureSummary{text-overflow:ellipsis;white-space:nowrap;min-width:0;color:var(--dsw-alias-label-secondary);flex:1;font-size:12px;overflow:hidden}.Sr3ygG_detailSection h4{letter-spacing:.02em;color:var(--dsh-tb-text-3);text-transform:none;margin:0;font-size:12px;font-weight:500}.Sr3ygG_sectionHead{grid-template-columns:minmax(0,1fr) auto;grid-template-areas:\"Sr3ygG_title Sr3ygG_action\";align-items:center;gap:8px;display:grid}.Sr3ygG_sectionHead h4{text-overflow:ellipsis;white-space:nowrap;grid-area:Sr3ygG_title;min-width:0;overflow:hidden}.Sr3ygG_sectionAction{grid-area:Sr3ygG_action;align-items:center;gap:8px;min-width:0;display:flex}.Sr3ygG_detailText{color:var(--dsw-alias-label-primary);white-space:pre-wrap;overflow-wrap:break-word;margin:0;font-size:14px;line-height:1.55}.Sr3ygG_configGrid{grid-template-columns:max-content minmax(0,1fr);gap:6px 18px;margin:0;display:grid}.Sr3ygG_configRow{display:contents}.Sr3ygG_configLabel{color:var(--dsw-alias-label-tertiary);white-space:nowrap;font-size:12px;line-height:1.6}.Sr3ygG_configValue{color:var(--dsw-alias-label-primary);overflow-wrap:anywhere;min-width:0;margin:0;font-size:12px;line-height:1.6}.Sr3ygG_scheduleGrid{grid-template-columns:max-content minmax(0,1fr);align-items:center;gap:6px 18px;display:grid}.Sr3ygG_scheduleLabel{color:var(--dsw-alias-label-secondary);white-space:nowrap;font-size:12px}.Sr3ygG_scheduleCronRow{flex-wrap:wrap;align-items:center;gap:8px;min-width:0;display:flex}.Sr3ygG_scheduleMaxRow{flex-wrap:wrap;align-items:center;gap:10px;min-width:0;display:flex}.Sr3ygG_scheduleInput{min-width:0;font-family:var(--dsw-font-markdown-code-block-small);flex:140px;font-size:13px}.Sr3ygG_scheduleMaxInput{flex:none;width:96px;font-size:13px}.Sr3ygG_inputInvalid,.Sr3ygG_inputInvalid:focus{border-color:var(--dsw-alias-state-error-primary)}.Sr3ygG_scheduleCronRow>.Sr3ygG_selectWrap,.Sr3ygG_scheduleMaxRow>.Sr3ygG_selectWrap{flex:0 auto;min-width:max(96px,8ch)}.Sr3ygG_schedulePreset{height:34px;color:var(--dsh-tb-text-1);background:var(--dsh-tb-input-bg);border:var(--dsh-tb-border-soft);border-radius:var(--dsh-tb-radius-md);transition:border-color var(--dsh-tb-motion), box-shadow var(--dsh-tb-motion);outline:none;padding:0 12px;font-size:13px}.Sr3ygG_scheduleManageButton{height:34px}.Sr3ygG_scheduleMeta{color:var(--dsw-alias-label-secondary);overflow-wrap:break-word;margin:0;font-size:12px}.Sr3ygG_scheduleActionRow{flex-wrap:wrap;align-items:center;gap:10px;min-width:0;display:flex}.Sr3ygG_segmentedRow{background:var(--dsw-alias-interactive-bg-hover);border-radius:var(--dsh-tb-radius-md);align-self:flex-start;gap:4px;padding:3px;display:inline-flex}.Sr3ygG_segmentedButton{height:22px;color:var(--dsh-tb-text-2);border-radius:var(--dsh-tb-pill);cursor:pointer;white-space:nowrap;transition:color var(--dsh-tb-motion), background-color var(--dsh-tb-motion), box-shadow var(--dsh-tb-motion);background:0 0;border:none;justify-content:center;align-items:center;padding:0 14px;font-size:12px;font-weight:500;line-height:1;display:inline-flex}.Sr3ygG_segmentedButton:hover{color:var(--dsh-tb-text-1)}.Sr3ygG_segmentedActive{color:var(--dsh-tb-text-1);background:var(--dsh-tb-surface-float);box-shadow:var(--dsh-tb-shadow-1)}.Sr3ygG_presetModal{width:min(560px,100%)}.Sr3ygG_modalForm{display:contents}.Sr3ygG_presetList{flex-direction:column;gap:8px;margin:0;padding:0;list-style:none;display:flex}.Sr3ygG_presetRow{border-radius:var(--dsh-tb-radius-sm);grid-template-columns:minmax(9ch,1fr) minmax(11ch,1.2fr) auto;align-items:center;gap:8px;padding:6px 4px;display:grid}.Sr3ygG_presetActions{align-items:center;gap:8px;display:inline-flex}.Sr3ygG_presetRowAction{height:34px}.Sr3ygG_presetRow:hover{background:var(--dsw-alias-interactive-bg-hover)}.Sr3ygG_presetName{width:100%;min-width:0}.Sr3ygG_presetCronWrap{min-width:0}.Sr3ygG_presetCron{width:100%;min-width:0;font-family:var(--dsw-font-markdown-code-block-small);font-size:13px}.Sr3ygG_presetHint{color:var(--dsw-alias-label-tertiary);grid-column:1/-1;font-size:12px}.Sr3ygG_presetEmpty{color:var(--dsw-alias-label-tertiary);padding:8px 0;font-size:12px}.Sr3ygG_runPresetRow{border-radius:var(--dsh-tb-radius-sm);grid-template-columns:minmax(8ch,1fr) auto auto;align-items:center;gap:8px;padding:6px 4px;display:grid}.Sr3ygG_runPresetRow:hover{background:var(--dsw-alias-interactive-bg-hover)}.Sr3ygG_runPresetName{text-overflow:ellipsis;white-space:nowrap;align-items:center;gap:6px;min-width:0;font-size:13px;font-weight:600;display:inline-flex;overflow:hidden}.Sr3ygG_runPresetDefaultBadge{color:var(--dsh-tb-accent);background:color-mix(in srgb, var(--dsh-tb-accent) 10%, transparent);border-radius:var(--dsh-tb-pill);flex:none;padding:1px 8px;font-size:12px}.Sr3ygG_presetNew{grid-template-columns:minmax(9ch,1fr) minmax(11ch,1.2fr) auto;align-items:center;gap:8px;display:grid}.Sr3ygG_contentBlock{color:var(--dsh-tb-text-1);background:var(--dsw-alias-markdown-code-block);border:var(--dsh-tb-border-soft);border-radius:var(--dsh-tb-radius-md);white-space:pre-wrap;overflow-wrap:break-word;margin:0;padding:10px 12px;font-size:13px;line-height:1.55}.Sr3ygG_promptBlock{position:relative}.Sr3ygG_promptBlockText{font-size:13px;line-height:1.5;font-family:var(--dsw-font-markdown-code-block-small);color:var(--dsh-tb-text-1);background:var(--dsw-alias-markdown-code-block);border:var(--dsh-tb-border-soft);border-radius:var(--dsh-tb-radius-md);white-space:pre-wrap;overflow-wrap:break-word;margin:0;padding:10px 40px 10px 12px}.Sr3ygG_promptCopy{z-index:1;width:26px;height:26px;color:var(--dsh-tb-text-2);background:var(--dsh-tb-surface-float);border:var(--dsh-tb-border-soft);border-radius:var(--dsh-tb-pill);cursor:pointer;box-shadow:var(--dsh-tb-shadow-1);opacity:0;transition:opacity var(--dsh-tb-motion), background var(--dsh-tb-motion), color var(--dsh-tb-motion), transform var(--dsh-tb-motion);justify-content:center;align-items:center;padding:0;display:inline-flex;position:absolute;top:8px;right:8px}.Sr3ygG_promptBlock:hover .Sr3ygG_promptCopy,.Sr3ygG_promptCopy:focus-visible{opacity:1}.Sr3ygG_promptCopy:hover{color:var(--dsh-tb-text-1);background:var(--dsh-tb-hover)}.Sr3ygG_promptCopy:active{transform:translateY(1px)}.Sr3ygG_sessionList{border:var(--dsh-tb-border-soft);border-radius:var(--dsh-tb-radius-md);margin:0;padding:0;list-style:none;position:relative;overflow:hidden}.Sr3ygG_sessionList[data-reordering]{padding-bottom:14px}.Sr3ygG_sessionList[data-reordering] .Sr3ygG_sessionRow{cursor:grabbing}.Sr3ygG_sessionList[data-reordering] .Sr3ygG_sessionRow:hover{box-shadow:none}.Sr3ygG_sessionDropZone{border-radius:var(--dsh-tb-radius-md);flex-direction:column;gap:8px;margin:-6px;padding:6px;display:flex}.Sr3ygG_detailEmptyField{color:var(--dsh-tb-text-3);margin:0;font-size:12px}.Sr3ygG_sessionRenameRow{flex-wrap:wrap;align-items:center;gap:8px;min-width:0;display:flex}.Sr3ygG_sessionRenameInput{flex:140px;min-width:0;height:24px;font-size:13px}.Sr3ygG_sessionDropZone[data-bindactive]{box-shadow:0 0 0 2px var(--dsh-tb-focus-ring);animation:Sr3ygG_dshTbBreathRing var(--dsh-tb-breath) infinite}.Sr3ygG_sessionDropZone[data-flash]{animation:.18s Sr3ygG_dshTbDropConfirm}.Sr3ygG_hiddenTray{background:var(--dsh-tb-surface-sunken);border:var(--dsh-tb-border-soft);border-radius:var(--dsh-tb-radius-md);flex-direction:column;gap:4px;padding:10px 12px;display:flex}.Sr3ygG_hiddenTrayHead{justify-content:space-between;align-items:center;gap:10px;display:flex}.Sr3ygG_hiddenTrayTitle{color:var(--dsw-alias-label-secondary);font-size:12px}.Sr3ygG_hiddenTrayList{flex-direction:column;margin:0;padding:0;list-style:none;display:flex}.Sr3ygG_hiddenTrayRow{align-items:center;gap:10px;padding:4px 0;display:flex}.Sr3ygG_hiddenTrayActions{flex:none;align-items:center;gap:8px;display:inline-flex}.Sr3ygG_hiddenTrayRow+.Sr3ygG_hiddenTrayRow{border-top:var(--dsh-tb-separator)}.Sr3ygG_hiddenTrayName{text-overflow:ellipsis;white-space:nowrap;min-width:0;color:var(--dsh-tb-text-1);flex:1;font-size:13px;overflow:hidden}.Sr3ygG_sessionRow{cursor:pointer;border-radius:var(--dsh-tb-radius-md);transition:background-color var(--dsh-tb-motion);flex-direction:column;gap:6px;padding:10px 12px;display:flex;position:relative}.Sr3ygG_sessionRow+.Sr3ygG_sessionRow{border-top:var(--dsh-tb-separator)}.Sr3ygG_sessionRow:after{content:\"\";border-radius:var(--dsh-tb-radius-sm);pointer-events:none;opacity:0;transition:opacity var(--dsh-tb-motion);animation:Sr3ygG_dshTbBreathHalo var(--dsh-tb-breath) infinite;animation-play-state:paused;position:absolute;inset:0}.Sr3ygG_sessionRow[data-glow=attention]:after{opacity:1;animation-play-state:running}.Sr3ygG_sessionRow:hover{background:var(--dsh-tb-hover)}.Sr3ygG_sessionRow:focus-visible{outline:2px solid var(--dsw-alias-state-business-primary);outline-offset:-2px}.Sr3ygG_sessionRowHandle:focus-visible,.Sr3ygG_rowHide:focus-visible{outline:2px solid var(--dsw-alias-state-business-primary);outline-offset:2px}.Sr3ygG_sessionRowTop{grid-template-columns:minmax(0,1fr) auto auto;grid-template-areas:\"Sr3ygG_lead Sr3ygG_chip Sr3ygG_act\";align-items:center;gap:10px;min-width:0;display:grid}.Sr3ygG_sessionRowLead{grid-area:Sr3ygG_lead;align-items:center;min-width:0;display:flex;position:relative}.Sr3ygG_sessionRowChip{grid-area:Sr3ygG_chip;min-width:0;display:inline-flex}.Sr3ygG_sessionRowLeading{align-items:center;gap:var(--dsh-tb-lead-gap);min-width:0;line-height:var(--dsh-tb-row-line);color:var(--dsh-tb-text-1);font-size:14px;font-weight:500;display:flex}.Sr3ygG_sessionToolbarActions{flex:none;align-items:center;gap:8px;display:inline-flex}.Sr3ygG_sessionHintRow{align-items:flex-start;gap:10px;min-width:0;margin:0;display:flex}.Sr3ygG_sessionHintRow .Sr3ygG_sessionToolbarActions{margin-top:calc((var(--dsh-tb-hint-line) - var(--dsh-tb-button-h-sm)) / 2)}.Sr3ygG_sessionHintText{flex:auto;min-width:0}.Sr3ygG_addSessionList{flex-direction:column;gap:4px;margin:0;padding:0;list-style:none;display:flex}.Sr3ygG_addSessionRow{border:var(--dsh-tb-border-soft);border-radius:var(--dsh-tb-radius-sm);width:100%;min-width:0;color:var(--dsh-tb-text-1);text-align:left;cursor:pointer;transition:background var(--dsh-tb-motion);background:0 0;align-items:center;gap:8px;padding:9px 10px;font-size:13px;display:flex}.Sr3ygG_addSessionRow:hover{background:var(--dsh-tb-hover)}.Sr3ygG_addSessionRow:focus-visible{outline:2px solid var(--dsh-tb-focus-color);outline-offset:1px}.Sr3ygG_addSessionLabel{text-overflow:ellipsis;white-space:nowrap;flex:1;min-width:0;overflow:hidden}.Sr3ygG_addSessionChevron{color:var(--dsh-tb-text-3);flex:none}.Sr3ygG_sessionRowActions{grid-area:Sr3ygG_act;justify-content:flex-end;align-items:center;gap:8px;display:inline-flex}.Sr3ygG_rowActionIcon{display:none}.Sr3ygG_sessionRowActions button{align-items:center;gap:5px;display:inline-flex}.Sr3ygG_sessionRowHandle{height:var(--dsh-tb-button-h-sm);color:var(--dsh-tb-ink-on-fill);background:var(--dsw-alias-state-warn-primary);border-radius:var(--dsh-tb-pill);cursor:pointer;white-space:nowrap;transition:background var(--dsh-tb-motion);border:none;flex:none;padding:0 10px;font-size:12px;font-weight:500}.Sr3ygG_sessionRowHandle:hover{background:var(--dsw-alias-state-warn-primary-hover,var(--dsw-alias-state-warn-primary));filter:brightness(1.1)}.Sr3ygG_sessionRowMeta{color:var(--dsw-alias-label-secondary);overflow-wrap:anywhere;font-variant-numeric:tabular-nums;font-size:12px}.Sr3ygG_executionError{color:var(--dsw-alias-state-error-primary);overflow-wrap:anywhere;font-size:12px}.Sr3ygG_reviewCommentError{color:var(--dsh-tb-text-2);overflow-wrap:anywhere;font-size:12px}.Sr3ygG_reviewCommentImage{color:var(--dsh-tb-text-3);font-size:12px;font-style:italic}.Sr3ygG_executionDynamics{min-width:0;color:var(--dsw-alias-label-secondary);align-items:center;gap:6px;font-size:12px;display:flex}.Sr3ygG_executionDynamicsLabel{color:var(--dsw-alias-label-tertiary);white-space:nowrap;flex:none}.Sr3ygG_executionComments{font-variant-numeric:tabular-nums;align-items:center;gap:8px;min-width:0;font-size:12px;display:flex}.Sr3ygG_executionCommentsCount{color:var(--dsw-alias-label-tertiary);white-space:nowrap;flex:none}.Sr3ygG_executionCommentsLatest{text-overflow:ellipsis;white-space:nowrap;min-width:0;color:var(--dsw-alias-label-secondary);flex:0 auto;overflow:hidden}.Sr3ygG_executionCommentsTime{color:var(--dsw-alias-label-tertiary);white-space:nowrap;flex:none}.Sr3ygG_icon{flex:none}.Sr3ygG_rowHide{color:var(--dsh-tb-text-3);border-radius:var(--dsh-tb-radius-sm);cursor:pointer;white-space:nowrap;transition:color var(--dsh-tb-motion), background var(--dsh-tb-motion);background:0 0;border:none;flex:none;padding:2px 6px;font-size:12px}.Sr3ygG_rowHide:hover{color:var(--dsh-tb-text-1);background:var(--dsh-tb-hover)}.Sr3ygG_sessionRowIcon{color:var(--dsh-tb-text-3);flex:none}.Sr3ygG_sessionRowName{text-overflow:ellipsis;white-space:nowrap;min-width:0;overflow:hidden}.Sr3ygG_sessionRowWorkspace{text-overflow:ellipsis;min-width:0;color:var(--dsh-tb-text-3);background:var(--dsh-tb-surface-sunken);border-radius:var(--dsh-tb-pill);white-space:nowrap;flex:0 auto;padding:1px 7px;font-size:11px;font-weight:400;overflow:hidden}.Sr3ygG_sessionFacts{min-width:0;padding-inline:var(--dsh-tb-rail-inset);flex-wrap:wrap;align-items:center;gap:10px;display:flex}.Sr3ygG_sessionFactTime{color:var(--dsh-tb-text-3);font-variant-numeric:tabular-nums;font-size:12px}.Sr3ygG_column[data-dropaccept=link]{border-color:var(--dsh-tb-accent);box-shadow:0 0 0 2px var(--dsh-tb-focus-ring);animation:Sr3ygG_dshTbBreathRing var(--dsh-tb-breath) infinite}.Sr3ygG_detailHint{color:var(--dsw-alias-label-tertiary);margin:0;font-size:12px;line-height:1.5}.Sr3ygG_autoModal{width:min(600px,100%)}.Sr3ygG_autoEmpty{text-align:center;color:var(--dsh-tb-text-3);margin:0;padding:16px 8px;font-size:12px}.Sr3ygG_autoList{flex-direction:column;gap:16px;display:flex}.Sr3ygG_autoTask{background:var(--dsh-tb-surface-sunken);border:var(--dsh-tb-border-soft);border-radius:var(--dsh-tb-radius-md);flex-direction:column;gap:8px;padding:12px;display:flex}.Sr3ygG_autoTaskHead{align-items:center;gap:8px;min-width:0;display:flex}.Sr3ygG_autoTaskTitle{text-overflow:ellipsis;white-space:nowrap;min-width:0;color:var(--dsh-tb-text-1);flex:1;font-size:13px;font-weight:600;overflow:hidden}.Sr3ygG_autoTaskExpand{width:100%;min-width:0;color:var(--dsh-tb-text-2);border-radius:var(--dsh-tb-radius-sm);cursor:pointer;text-align:left;font:inherit;background:0 0;border:none;align-items:center;gap:7px;padding:0;display:inline-flex}.Sr3ygG_autoTaskExpand:hover{color:var(--dsh-tb-text-1)}.Sr3ygG_autoTaskChevron{color:var(--dsh-tb-text-3);transition:transform var(--dsh-tb-motion);flex:none}.Sr3ygG_autoTaskExpand[aria-expanded=false] .Sr3ygG_autoTaskChevron{transform:rotate(-90deg)}.Sr3ygG_autoTaskSummary{text-overflow:ellipsis;white-space:nowrap;min-width:0;color:var(--dsh-tb-text-2);flex:1;font-size:12px;overflow:hidden}.Sr3ygG_autoMetaIcon{color:var(--dsh-tb-accent);flex:none}.Sr3ygG_autoRules{gap:16px;padding:0}.Sr3ygG_autoGroup{flex-direction:column;gap:8px;min-width:0;display:flex}.Sr3ygG_autoRuleList{flex-direction:column;gap:8px;margin:0;padding:0;list-style:none;display:flex}.Sr3ygG_autoRuleRow{background:var(--dsh-tb-surface-float);border-radius:var(--dsh-tb-radius-sm);flex-direction:column;gap:4px;padding:8px 10px;display:flex}.Sr3ygG_autoRuleTop{align-items:center;gap:8px;min-width:0;display:flex}.Sr3ygG_autoRuleSession{flex:1;align-items:center;gap:5px;min-width:0;display:flex}.Sr3ygG_autoRuleSessionTitle{text-overflow:ellipsis;white-space:nowrap;min-width:0;color:var(--dsh-tb-text-1);flex:1;font-size:12px;font-weight:600;overflow:hidden}.Sr3ygG_autoRuleInstruction{overflow-wrap:anywhere;white-space:normal;min-width:0;color:var(--dsh-tb-text-2);font-size:12px;line-height:1.5}.Sr3ygG_autoRuleMeta{overflow-wrap:anywhere;white-space:normal;min-width:0;color:var(--dsh-tb-text-3);font-size:12px}.Sr3ygG_autoRuleActions{grid-template-columns:minmax(0,1fr) auto;grid-template-areas:\"Sr3ygG_switch Sr3ygG_buttons\";align-items:center;gap:8px;display:grid}.Sr3ygG_autoRuleActions>.Sr3ygG_switch{grid-area:Sr3ygG_switch;justify-self:start}.Sr3ygG_autoRuleButtons{grid-area:Sr3ygG_buttons;justify-self:end;align-items:center;gap:8px;display:inline-flex}.Sr3ygG_autoAddAction{align-self:flex-start;display:inline-flex}.Sr3ygG_autoForm{background:var(--dsh-tb-surface-sunken);border:var(--dsh-tb-border-soft);border-radius:var(--dsh-tb-radius-md);flex-direction:column;gap:12px;padding:12px;display:flex}.Sr3ygG_autoField{flex-direction:column;gap:4px;min-width:0;display:flex}.Sr3ygG_autoFieldLabel{letter-spacing:.02em;color:var(--dsh-tb-text-3);font-size:12px;font-weight:500}.Sr3ygG_autoNameInput{width:100%;min-width:0}.Sr3ygG_autoFormActions{align-self:flex-end;gap:8px;display:inline-flex}.Sr3ygG_refineSection{flex-direction:column;gap:8px;padding:0;display:flex}.Sr3ygG_refineIdle{flex-direction:column;align-items:flex-start;gap:12px;padding:16px 0;display:flex}.Sr3ygG_refineHeader{align-items:center;gap:8px;padding:8px 0;display:flex}.Sr3ygG_refineRounds{color:var(--dsw-alias-label-secondary);font-variant-numeric:tabular-nums;font-size:13px}.Sr3ygG_waitingNotice{min-width:0;color:var(--dsh-tb-text-2);background:var(--dsh-tb-bg-raised);border:var(--dsh-tb-border-soft);border-radius:var(--dsh-tb-radius-sm);align-items:flex-start;gap:8px;padding:8px 12px;font-size:13px;line-height:1.5;display:flex}.Sr3ygG_waitingNotice>span{overflow-wrap:anywhere;min-width:0}.Sr3ygG_waitingNotice .Sr3ygG_chip{flex:none}.Sr3ygG_refineTranscriptArea{border:var(--dsh-tb-border);border-radius:var(--dsh-tb-radius-md);background:var(--dsh-tb-surface-sunken);box-sizing:border-box;width:100%;min-height:200px;max-height:400px;padding:12px;position:relative;overflow-y:auto}.Sr3ygG_refineTail{flex-direction:column;min-height:100%;display:flex}.Sr3ygG_refineTail ul{flex:1;gap:8px}.Sr3ygG_refineInputArea{flex-direction:column;gap:8px;width:100%;display:flex}.Sr3ygG_refineActionArea{flex-direction:column;gap:8px;margin-top:4px;display:flex}.Sr3ygG_refineApplyButton{width:100%}.Sr3ygG_moveRow{flex-wrap:wrap;gap:8px;display:flex}.Sr3ygG_detailFooter{border-top:var(--dsh-tb-separator);flex:none;grid-template-columns:minmax(0,1fr) auto auto;grid-template-areas:\"Sr3ygG_actions Sr3ygG_meta Sr3ygG_danger\";align-items:center;gap:12px;padding:12px 18px;display:grid}.Sr3ygG_detailFooterGroup{flex-wrap:wrap;grid-area:Sr3ygG_actions;align-items:center;gap:8px;min-width:0;display:flex}.Sr3ygG_detailFooterDanger{flex:none;grid-area:Sr3ygG_danger;align-items:center;gap:8px;display:inline-flex}.Sr3ygG_detailFooter>.Sr3ygG_detailMeta{grid-area:Sr3ygG_meta;place-self:center end;line-height:28px}.Sr3ygG_detailMeta{color:var(--dsw-alias-label-tertiary);font-size:11px}.Sr3ygG_detailBlockedField{color:var(--dsh-tb-attention);margin:0;font-size:12px;line-height:1.5}.Sr3ygG_sidebarFooterAction:focus-visible,.Sr3ygG_card:focus-visible,.Sr3ygG_primaryButton:focus-visible,.Sr3ygG_ghostButton:focus-visible,.Sr3ygG_dangerButton:focus-visible,.Sr3ygG_dangerGhostButton:focus-visible,.Sr3ygG_iconButton:focus-visible,.Sr3ygG_boardStatusButton:focus-visible,.Sr3ygG_slashMenuRow:focus-visible,.Sr3ygG_search:focus-visible,.Sr3ygG_input:focus-visible,.Sr3ygG_schedulePreset:focus-visible,.Sr3ygG_attachAdd:focus-visible,.Sr3ygG_reviewJumpLatest:focus-visible,.Sr3ygG_notifyMain:focus-visible,.Sr3ygG_filterChipRemove:focus-visible,.Sr3ygG_feedFilter:focus-visible,.Sr3ygG_feedAction:focus-visible,.Sr3ygG_detailDisclosure:focus-visible,.Sr3ygG_segmentedButton:focus-visible,.Sr3ygG_autoTaskExpand:focus-visible,.Sr3ygG_reviewCommentCancel:focus-visible,.Sr3ygG_attachRemove:focus-visible,.Sr3ygG_sessionContextHead:focus-visible,.Sr3ygG_tagCustomColor:focus-visible,button.Sr3ygG_interactionOption:focus-visible,.Sr3ygG_tagSwatch:focus-visible{outline:2px solid var(--dsw-alias-state-business-primary);outline-offset:2px}.Sr3ygG_card:active{box-shadow:var(--dsw-shadow-lv1);transform:translateY(0)}.Sr3ygG_card[draggable=true]{cursor:grab}.Sr3ygG_card[draggable=true]:active{cursor:grabbing}.Sr3ygG_card.Sr3ygG_dragging{opacity:.5;box-shadow:var(--dsh-tb-shadow-3);transition:box-shadow var(--dsh-tb-motion), transform var(--dsh-tb-motion), opacity var(--dsh-tb-motion);transform:scale(1.02)rotate(.5deg)}.Sr3ygG_board[data-dragging] .Sr3ygG_card:hover{box-shadow:none;transform:none}@media (prefers-reduced-motion:reduce){.Sr3ygG_card.Sr3ygG_dragging{transform:none}.Sr3ygG_card.Sr3ygG_selectedCard{animation:none;transform:none}.Sr3ygG_sidebarFooterAction[data-pending]{animation-duration:4s}}.Sr3ygG_column[data-dragover]{border-color:var(--dsw-alias-state-business-primary)}.Sr3ygG_sidebarFooterAction:active,.Sr3ygG_primaryButton:active:not(:disabled),.Sr3ygG_ghostButton:active:not(:disabled),.Sr3ygG_dangerButton:active:not(:disabled),.Sr3ygG_iconButton:active:not(:disabled){transform:translateY(1px)}.Sr3ygG_iconButton:hover:not(:disabled){background:var(--dsw-alias-interactive-bg-hover);color:var(--dsw-alias-label-primary)}.Sr3ygG_iconButton:disabled{opacity:.45;cursor:default}.Sr3ygG_search:focus,.Sr3ygG_schedulePreset:focus{border-color:var(--dsh-tb-focus-color);box-shadow:0 0 0 3px var(--dsh-tb-focus-ring)}@media (prefers-reduced-motion:reduce){.Sr3ygG_sidebarFooterAction,.Sr3ygG_card,.Sr3ygG_primaryButton,.Sr3ygG_ghostButton,.Sr3ygG_dangerButton,.Sr3ygG_iconButton,.Sr3ygG_sessionRow,.Sr3ygG_segmentedButton,.Sr3ygG_slashMenuRow,.Sr3ygG_search,.Sr3ygG_input,.Sr3ygG_schedulePreset,.Sr3ygG_switchTrack,.Sr3ygG_switchKnob,.Sr3ygG_reviewMessage .Sr3ygG_reviewMessageText{transition:none}.Sr3ygG_modalBackdrop,.Sr3ygG_modal,.Sr3ygG_review,.Sr3ygG_reviewMessage,.Sr3ygG_reviewContext,.Sr3ygG_cruisePopover,.Sr3ygG_cruiseWindowRow{animation:none}[data-dsh-taskboard-view]{--dsh-tb-breath:5s ease-in-out;--dsh-tb-breath-spread:2px;--dsh-tb-breath-halo:8px}.Sr3ygG_dropIndicator{box-shadow:inset 0 0 0 1px var(--dsh-tb-border)}.Sr3ygG_sessionDropZone[data-bindactive],.Sr3ygG_sessionDropZone[data-flash]{box-shadow:0 0 0 2px var(--dsh-tb-accent);animation:none}.Sr3ygG_column[data-dragreject]{box-shadow:inset 0 0 0 1px var(--dsh-tb-danger);animation:none}.Sr3ygG_spinner{animation-duration:1.4s}}.Sr3ygG_cruiseLimit{width:52px;color:var(--dsw-alias-label-primary);background:var(--dsw-specific-input-major);border:1px solid var(--dsw-alias-border-l2);border-radius:var(--dsh-tb-radius-sm);outline:none;padding:2px 6px;font-size:12px}.Sr3ygG_cruiseLimit:focus{border-color:var(--dsw-alias-state-business-primary)}.Sr3ygG_cruiseLimit:focus-visible{outline:2px solid var(--dsw-alias-state-business-primary);outline-offset:1px}.Sr3ygG_review{width:min(880px,100%);max-height:min(100%,860px);container:Sr3ygG_dsh-tb-panel/inline-size}.Sr3ygG_reviewHeader{border-bottom:var(--dsh-tb-separator);box-sizing:border-box;flex:none;grid-template-columns:minmax(0,1fr) auto auto auto auto;grid-template-areas:\"Sr3ygG_title Sr3ygG_badge Sr3ygG_context Sr3ygG_actions Sr3ygG_close\";align-items:center;gap:10px;min-height:52px;padding:14px 20px;display:grid;position:relative}.Sr3ygG_reviewHeader .Sr3ygG_iconButton{width:var(--dsh-tb-button-h-sm);height:var(--dsh-tb-button-h-sm)}.Sr3ygG_reviewTitleWrap{grid-area:Sr3ygG_title;align-items:center;gap:8px;min-width:0;display:flex}.Sr3ygG_reviewTitle{letter-spacing:-.01em;text-overflow:ellipsis;white-space:nowrap;min-width:0;margin:0;font-size:16px;font-weight:600;line-height:1.4;overflow:hidden}.Sr3ygG_reviewBadge{color:var(--dsw-alias-label-secondary);white-space:nowrap;flex:none;grid-area:Sr3ygG_badge;font-size:11px;font-weight:500;line-height:1.4}.Sr3ygG_reviewActions{flex:none;grid-area:Sr3ygG_actions;align-items:center;gap:8px;display:inline-flex}.Sr3ygG_reviewHeader>.Sr3ygG_iconButton{grid-area:Sr3ygG_close}.Sr3ygG_reviewHeaderContext{flex:none;grid-area:Sr3ygG_context;min-width:0;max-width:320px;padding:0}.Sr3ygG_reviewHeaderContext.Sr3ygG_sessionContextWrap{padding:0}.Sr3ygG_reviewHeaderContext[data-open]{display:contents}.Sr3ygG_reviewHeaderContext[data-open]>.Sr3ygG_sessionContextHead{grid-area:Sr3ygG_context;min-width:0;max-width:320px}.Sr3ygG_reviewHeaderContext[data-open]>.Sr3ygG_sessionContextPanel{grid-column:1/-1;width:auto;min-width:0;max-height:320px;position:static;top:auto;left:auto;right:auto;overflow-y:auto}.Sr3ygG_reviewBody{flex-direction:row;flex:1;gap:0;min-height:0;padding:0;display:flex;overflow:hidden}.Sr3ygG_reviewMain{flex-direction:column;flex:1;min-width:0;min-height:0;padding:14px 0 20px 20px;display:flex;overflow:hidden}.Sr3ygG_sessionRailFolds{flex-direction:column;flex:auto;gap:0;min-width:0;min-height:0;display:flex;overflow:hidden auto}.Sr3ygG_reviewRail{border-left:var(--dsh-tb-separator);background:var(--dsh-tb-surface-float);flex-direction:column;flex:none;width:min(344px,46cqw);min-width:0;min-height:0;display:flex;overflow:hidden}.Sr3ygG_sessionRailHead{border-bottom:var(--dsh-tb-separator);flex-direction:column;flex:none;min-width:0;padding-bottom:12px;display:flex}.Sr3ygG_sessionRailHead>.Sr3ygG_detailSection{min-width:0;padding-inline:var(--dsh-tb-rail-inset)}.Sr3ygG_sessionRailComments{flex-direction:column;flex:none;min-width:0;min-height:0;display:flex}.Sr3ygG_sessionRailComments[data-open=true]{flex:auto;min-height:0}.Sr3ygG_sessionRailComments>.Sr3ygG_detailSection{flex-direction:column;flex:auto;min-width:0;min-height:0;display:flex}.Sr3ygG_commentsScroll{min-width:0;min-height:0;padding:10px var(--dsh-tb-rail-inset) 10px;flex-direction:column;flex:auto;gap:10px;display:flex;overflow-y:auto}.Sr3ygG_sessionRailHead .Sr3ygG_detailDisclosure,.Sr3ygG_sessionRailComments .Sr3ygG_detailDisclosure{padding:8px 0}.Sr3ygG_sessionRailComments .Sr3ygG_detailDisclosure{margin-inline:var(--dsh-tb-rail-inset)}.Sr3ygG_reviewTranscriptScroll{flex-direction:column;flex:1;gap:16px;min-height:120px;padding-right:14px;display:flex;overflow-y:auto}.Sr3ygG_reviewOutcome{background:var(--dsw-alias-bg-layer-2);border:1px solid var(--dsw-alias-border-l1);border-radius:var(--dsh-tb-radius-md);flex:none;align-items:center;gap:10px;padding:8px 12px;display:flex}.Sr3ygG_reviewOutcomeMeta{color:var(--dsw-alias-label-tertiary);font-variant-numeric:tabular-nums;font-size:12px}.Sr3ygG_transcriptEarlierRow{flex-direction:column;flex:none;align-items:center;gap:4px;padding:2px 0 0;display:flex}.Sr3ygG_reviewTranscript{flex-direction:column;gap:16px;margin:0;padding:0;list-style:none;display:flex}.Sr3ygG_reviewMessage{max-width:100%;animation:.16s Sr3ygG_dshTbFadeUp;display:flex}.Sr3ygG_reviewMessage[data-role=user]{justify-content:flex-end}.Sr3ygG_reviewMessageText{min-width:0;max-width:100%;color:var(--dsw-alias-label-primary);overflow-wrap:break-word;font-size:16px;line-height:28px;display:block}.Sr3ygG_transcriptErrorRow{justify-content:space-between;align-items:center;gap:12px;display:flex}.Sr3ygG_reviewImageRow{flex-wrap:wrap;gap:8px;margin:6px 0;display:flex}.Sr3ygG_reviewImageBox{border-radius:var(--dsh-tb-radius-md);background:var(--dsh-tb-surface-sunken);border:var(--dsh-tb-border-soft);width:140px;max-width:40cqw;height:140px;max-height:40cqw;display:inline-flex;overflow:hidden}.Sr3ygG_reviewImage{object-fit:cover;width:100%;height:100%}.Sr3ygG_reviewImageMissing{color:var(--dsh-tb-text-3);align-items:center;font-size:12px;display:inline-flex}.Sr3ygG_reviewMessage[data-role=user] .Sr3ygG_reviewMessageText{background:var(--dsw-specific-bubble,var(--dsw-static-deepseek-50));max-width:min(525px,82%);color:var(--dsw-alias-label-primary);border-radius:22px;padding:10px 16px;font-size:16px;line-height:24px;transition:background-color .12s}.Sr3ygG_reviewMessage[data-role=user] .Sr3ygG_reviewMessageText:hover{background:var(--dsw-specific-bubble-highlight,var(--dsw-static-deepseek-200))}.Sr3ygG_reviewContext{color:var(--dsw-alias-label-tertiary);border-left:2px solid var(--dsw-alias-border-l2);opacity:.85;padding:4px 10px;font-size:12px;line-height:1.5;animation:.16s Sr3ygG_dshTbFadeUp}.Sr3ygG_reviewContextMeter{flex-direction:column;flex:none;gap:6px;display:flex}.Sr3ygG_reviewMeterHead{white-space:nowrap;grid-template-columns:auto auto minmax(0,1fr);grid-template-areas:\"Sr3ygG_reading Sr3ygG_percent Sr3ygG_figures\";align-items:baseline;gap:8px;min-width:0;display:grid}.Sr3ygG_reviewMeterReading{color:var(--dsw-alias-label-secondary);white-space:nowrap;grid-area:Sr3ygG_reading;font-size:12px}.Sr3ygG_reviewMeterPercent{color:var(--dsw-alias-label-primary);font-variant-numeric:tabular-nums;grid-area:Sr3ygG_percent;font-size:12px;font-weight:500}.Sr3ygG_reviewContextMeter[data-warn] .Sr3ygG_reviewMeterPercent{color:var(--dsh-tb-attention)}.Sr3ygG_reviewContextMeter[data-warn] .Sr3ygG_reviewMeterBar{box-shadow:0 0 0 1px var(--dsh-tb-attention)}.Sr3ygG_reviewMeterFigures{text-overflow:ellipsis;min-width:0;color:var(--dsw-alias-label-tertiary);font-variant-numeric:tabular-nums;white-space:nowrap;grid-area:Sr3ygG_figures;font-size:12px;overflow:hidden}.Sr3ygG_reviewMeterBar{border-radius:var(--dsh-tb-pill);background:var(--dsw-alias-interactive-bg-hover);gap:1px;height:4px;display:flex;overflow:hidden}.Sr3ygG_reviewMeterSegment{background:var(--meter-tint,var(--dsw-alias-label-tertiary));border-radius:1px;flex:none;min-width:2px;height:100%}.Sr3ygG_meterSystem{--meter-tint:var(--dsw-static-neutral-bluish-400)}.Sr3ygG_meterTools{--meter-tint:var(--dsw-static-deepseek-400)}.Sr3ygG_meterMessages{--meter-tint:var(--dsw-static-blue-450)}.Sr3ygG_reviewMeterRows{grid-template-columns:minmax(0,1fr);gap:4px;display:grid}.Sr3ygG_reviewMeterRow{color:var(--dsw-alias-label-secondary);grid-template-columns:auto minmax(0,1fr) auto;grid-template-areas:\"Sr3ygG_swatch Sr3ygG_label Sr3ygG_value\";align-items:center;column-gap:6px;min-width:0;font-size:12px;display:grid}.Sr3ygG_reviewMeterRow>:nth-child(2){text-overflow:ellipsis;white-space:nowrap;grid-area:Sr3ygG_label;min-width:0;overflow:hidden}.Sr3ygG_reviewMeterValue{font-variant-numeric:tabular-nums;white-space:nowrap;color:var(--dsw-alias-label-primary);grid-area:Sr3ygG_value}.Sr3ygG_reviewMeterSwatch{background:var(--meter-tint,var(--dsw-alias-label-tertiary));border-radius:2px;grid-area:Sr3ygG_swatch;width:8px;height:8px}.Sr3ygG_reviewUsage{color:var(--dsw-alias-label-tertiary);font-variant-numeric:tabular-nums;margin:0;font-size:12px}.Sr3ygG_reviewConfig{flex-direction:column;flex:none;gap:8px;display:flex}.Sr3ygG_reviewConfigTitle{color:var(--dsw-alias-label-secondary);font-size:13px;font-weight:500}.Sr3ygG_reviewConfigGrid{grid-template-columns:repeat(auto-fit,minmax(150px,1fr));gap:8px 12px;display:grid}.Sr3ygG_reviewConfigRow{flex-direction:column;gap:4px;min-width:0;display:flex}.Sr3ygG_reviewConfigLabel{color:var(--dsw-alias-label-secondary);white-space:nowrap;text-overflow:ellipsis;font-size:11px;overflow:hidden}.Sr3ygG_reviewConfigRow>.Sr3ygG_selectWrap{width:100%;min-width:0}.Sr3ygG_reviewConfigUnavailable,.Sr3ygG_reviewConfigMessage{color:var(--dsw-alias-state-error-primary);font-size:12px}.Sr3ygG_reviewConfigLoading{color:var(--dsh-tb-text-3);font-size:12px}.Sr3ygG_reviewConfigFailed{flex-wrap:wrap;align-items:center;gap:8px;min-width:0;display:flex}.Sr3ygG_reviewConfigValue{height:34px;color:var(--dsh-tb-text-2);background:var(--dsh-tb-input-bg);border:var(--dsh-tb-border-soft);border-radius:var(--dsh-tb-radius-md);text-overflow:ellipsis;white-space:nowrap;padding:0 12px;font-size:13px;line-height:32px;display:block;overflow:hidden}.Sr3ygG_reviewComments{flex-direction:column;gap:8px;margin:0;padding:0;list-style:none;display:flex}.Sr3ygG_reviewComment{background:var(--dsh-tb-surface-sunken);border:1px solid var(--dsw-alias-border-l1);border-radius:var(--dsh-tb-radius-md);flex-direction:column;gap:4px;padding:8px 12px;animation:.16s Sr3ygG_dshTbFadeUp;display:flex}.Sr3ygG_reviewCommentText{overflow-wrap:break-word;font-size:13px;line-height:1.5;display:block}.Sr3ygG_mdParagraph{white-space:pre-wrap;margin:0}.Sr3ygG_mdParagraph+.Sr3ygG_mdParagraph,.Sr3ygG_mdCodeBlock+.Sr3ygG_mdParagraph,.Sr3ygG_mdParagraph+.Sr3ygG_mdCodeBlock{margin-top:8px}.Sr3ygG_mdHeading{letter-spacing:-.01em;color:var(--dsh-tb-text-1);margin:12px 0 6px;font-weight:600;line-height:1.4}.Sr3ygG_mdHeading[data-level=\"1\"]{font-size:17px}.Sr3ygG_mdHeading[data-level=\"2\"]{font-size:15px}.Sr3ygG_mdHeading[data-level=\"3\"]{font-size:13px}.Sr3ygG_mdStrong{font-weight:600}.Sr3ygG_mdEm{font-style:italic}.Sr3ygG_mdCode{font-family:var(--dsw-font-markdown-code-block-small);background:var(--dsw-alias-markdown-code-block);border:1px solid var(--dsw-alias-border-l1);overflow-wrap:anywhere;word-break:break-word;border-radius:4px;padding:0 4px;font-size:.92em}.Sr3ygG_mdCodeBlock{font-family:var(--dsw-font-markdown-code-block-small);background:var(--dsw-alias-markdown-code-block);border:var(--dsh-tb-border-soft);border-radius:var(--dsh-tb-radius-md);white-space:pre;margin:8px 0;padding:10px 12px;font-size:12.5px;line-height:1.5;overflow-x:auto}.Sr3ygG_mdList{flex-direction:column;gap:4px;margin:6px 0;padding-left:20px;display:flex}.Sr3ygG_mdQuote{border-left:2px solid var(--dsw-alias-border-l2);color:var(--dsw-alias-label-secondary);margin:8px 0;padding:2px 12px}.Sr3ygG_mdLink{color:var(--dsw-alias-state-business-primary);text-decoration:none}.Sr3ygG_mdRule{border:none;border-top:1px solid var(--dsw-alias-separator-primary,var(--dsw-alias-bg-layer-2));margin:10px 0}.Sr3ygG_reviewCommentCommand{color:var(--dsw-alias-state-business-primary);font-weight:500}.Sr3ygG_reviewCommandOutcome{color:var(--dsw-alias-label-tertiary)}.Sr3ygG_reviewCommentMeta{grid-template-columns:auto minmax(0,1fr) auto;grid-template-areas:\"Sr3ygG_chip Sr3ygG_time Sr3ygG_cancel\";align-items:center;gap:8px;min-width:0;display:grid}.Sr3ygG_reviewCommentMeta>.Sr3ygG_chip{grid-area:Sr3ygG_chip}.Sr3ygG_reviewCommentTime{min-width:0;color:var(--dsw-alias-label-tertiary);font-variant-numeric:tabular-nums;grid-area:Sr3ygG_time;font-size:12px}.Sr3ygG_reviewCommentCancel{color:var(--dsw-alias-label-tertiary);cursor:pointer;white-space:nowrap;background:0 0;border:none;flex:none;grid-area:Sr3ygG_cancel;justify-self:end;padding:0;font-size:12px;transition:color .12s}.Sr3ygG_reviewCommentCancel:hover{color:var(--dsw-alias-state-error-primary)}.Sr3ygG_reviewJumpLatest{width:26px;height:26px;color:var(--dsh-tb-text-2);background:var(--dsh-tb-bg-raised);border:var(--dsh-tb-border-soft);border-radius:var(--dsh-tb-pill);cursor:pointer;box-shadow:var(--dsh-tb-shadow-2);transition:background var(--dsh-tb-motion), border-color var(--dsh-tb-motion), color var(--dsh-tb-motion), transform var(--dsh-tb-motion);flex:none;justify-content:center;align-self:flex-end;align-items:center;margin:8px 6px 0 0;padding:0;display:inline-flex;position:sticky;bottom:8px}.Sr3ygG_reviewJumpLatest:hover{background:var(--dsh-tb-hover);border-color:var(--dsw-alias-border-l3);color:var(--dsh-tb-text-1);transform:translateY(-1px)}.Sr3ygG_reviewComposer{padding:0 var(--dsh-tb-rail-inset) 12px;border-top:var(--dsh-tb-separator);background:var(--dsh-tb-surface-float);flex-direction:column;flex:none;gap:8px;display:flex}.Sr3ygG_reviewComposer>*{flex:none}.Sr3ygG_interactionCard{min-width:0;max-height:min(320px,100%);margin:0 var(--dsh-tb-rail-inset) 2px;background:var(--dsh-tb-surface-sunken);border:1px solid var(--dsw-alias-border-l2);border-radius:var(--dsh-tb-radius-md);color:var(--dsh-tb-text-1);flex-direction:column;flex:none;display:flex;overflow:hidden}.Sr3ygG_interactionCard[data-plan=true]{border-color:color-mix(in srgb, var(--dsh-tb-attention) 45%, transparent)}.Sr3ygG_commentsScroll .Sr3ygG_interactionCard{margin:0 0 2px}.Sr3ygG_interactionHead{flex:none;justify-content:space-between;align-items:flex-start;gap:8px;padding:12px 8px 0 12px;display:flex}.Sr3ygG_interactionHeading{min-width:0}.Sr3ygG_interactionEyebrow{color:var(--dsh-tb-text-3);margin-bottom:4px;font-size:11px;line-height:16px}.Sr3ygG_interactionTitle{overflow-wrap:anywhere;margin:0;font-size:16px;font-weight:500;line-height:1.4}.Sr3ygG_interactionHeadActions{flex:none;align-items:center;gap:2px;display:flex}.Sr3ygG_interactionHeadActions .Sr3ygG_iconButton{width:24px;height:24px;color:var(--dsh-tb-text-3)}.Sr3ygG_interactionHeadActions .Sr3ygG_iconButton:hover:not(:disabled){color:var(--dsh-tb-text-1)}.Sr3ygG_interactionBody{overscroll-behavior:contain;flex-direction:column;flex:auto;gap:8px;min-width:0;min-height:0;padding:8px 12px;display:flex;overflow-y:auto}.Sr3ygG_interactionCardBody{flex-direction:column;gap:8px;min-width:0;min-height:0;padding:0 12px;display:flex;overflow-y:auto}.Sr3ygG_interactionQuestion{color:var(--dsh-tb-text-1);overflow-wrap:break-word;font-size:13px;line-height:1.5}.Sr3ygG_interactionQuestionBlock{flex-direction:column;gap:6px;min-width:0;display:flex}.Sr3ygG_interactionDetail{color:var(--dsh-tb-text-2);overflow-wrap:break-word;font-size:13px;line-height:1.5}.Sr3ygG_interactionDetail p{margin:0}.Sr3ygG_interactionOptions{flex-direction:column;gap:2px;min-width:0;display:flex}span.Sr3ygG_interactionOptions{flex-flow:wrap;gap:6px}.Sr3ygG_interactionOption{width:auto;min-height:0;font:inherit;color:var(--dsh-tb-text-2);background:var(--dsh-tb-surface-float);border:1px solid var(--dsw-alias-border-l2);border-radius:var(--dsh-tb-pill);cursor:default;padding:4px 10px;font-size:12px;display:inline-flex}.Sr3ygG_interactionOptionButton{width:100%;min-height:36px;font:inherit;color:inherit;text-align:left;border-radius:var(--dsh-tb-radius-sm);cursor:pointer;transition:background var(--dsh-tb-motion), border-color var(--dsh-tb-motion);background:0 0;border:1px solid #0000;flex:none;align-items:flex-start;gap:8px;padding:6px 8px;font-size:13px;display:flex}.Sr3ygG_interactionOptionButton:hover:not(:disabled),.Sr3ygG_interactionOptionOn{background:var(--dsh-tb-hover)}.Sr3ygG_interactionOptionOn{border-color:var(--dsw-alias-border-l2)}.Sr3ygG_interactionOptionButton:disabled{cursor:default;opacity:.55}.Sr3ygG_interactionOptionNumber{width:20px;height:20px;color:var(--dsh-tb-text-2);background:var(--dsw-alias-bg-overlay);border-radius:var(--dsh-tb-radius-sm);flex:0 0 20px;place-items:center;margin-top:1px;font-size:12px;font-weight:500;line-height:18px;display:grid}.Sr3ygG_interactionCheck{border-radius:var(--dsh-tb-radius-sm);flex:0 0 20px;place-items:center;width:20px;height:20px;margin-top:1px;display:grid}.Sr3ygG_interactionCheck:before{content:\"\";border:1px solid var(--dsw-alias-border-l2);border-radius:var(--dsh-tb-radius-sm);width:14px;height:14px;transition:background var(--dsh-tb-motion), border-color var(--dsh-tb-motion);grid-area:1/1}.Sr3ygG_interactionCheck>svg{grid-area:1/1;position:relative}.Sr3ygG_interactionCheckOn{color:var(--dsw-alias-label-primary-foreground)}.Sr3ygG_interactionCheckOn:before{background:var(--dsh-tb-text-1);border-color:var(--dsh-tb-text-1)}.Sr3ygG_interactionOptionCopy{flex:1;min-width:0}.Sr3ygG_interactionOptionLine{flex-wrap:wrap;align-items:baseline;gap:2px 6px;min-width:0;display:flex}.Sr3ygG_interactionOptionLabel{overflow-wrap:anywhere;font-size:13px;font-weight:500;line-height:20px}.Sr3ygG_interactionBadge{color:var(--dsh-tb-accent);background:color-mix(in srgb, var(--dsh-tb-accent) 16%, transparent);border-radius:var(--dsh-tb-radius-sm);flex:none;padding:0 4px;font-size:11px;font-weight:600;line-height:16px}.Sr3ygG_interactionOptionDescription{color:var(--dsh-tb-text-3);overflow-wrap:anywhere;font-size:12px;line-height:18px}.Sr3ygG_interactionCustomRow{border-radius:var(--dsh-tb-radius-sm);width:100%;min-height:36px;transition:background var(--dsh-tb-motion), border-color var(--dsh-tb-motion);border:1px solid #0000;align-items:flex-start;gap:8px;padding:6px 8px;display:flex}.Sr3ygG_interactionCustomRow:hover,.Sr3ygG_interactionCustomRow:focus-within,.Sr3ygG_interactionCustomRowOn{background:var(--dsh-tb-hover)}.Sr3ygG_interactionCustomRow:focus-within,.Sr3ygG_interactionCustomRowOn{border-color:var(--dsw-alias-border-l2)}.Sr3ygG_interactionField{flex:1;min-width:0;display:grid}.Sr3ygG_interactionField>*{min-width:0;font:inherit;white-space:pre-wrap;word-break:break-word;overflow-wrap:anywhere;grid-area:1/1;padding:0;font-size:13px;line-height:20px}.Sr3ygG_interactionFieldMirror{box-sizing:content-box;visibility:hidden;max-height:120px;overflow:hidden}.Sr3ygG_interactionFieldInput{resize:none;color:var(--dsh-tb-text-1);caret-color:var(--dsh-tb-accent);background:0 0;border:none;outline:none;overflow-y:auto}.Sr3ygG_interactionFieldInput::placeholder{color:var(--dsh-tb-text-3)}.Sr3ygG_interactionFieldBlock{background:var(--dsh-tb-input-bg);border:1px solid var(--dsw-alias-border-l2);border-radius:var(--dsh-tb-radius-sm);flex:none;min-height:56px;margin:2px 12px;padding:8px 12px}.Sr3ygG_interactionFieldBlock:focus-within{border-color:var(--dsh-tb-focus-color)}.Sr3ygG_interactionFieldInline{flex:auto}.Sr3ygG_interactionFooter{flex-wrap:wrap;flex:none;justify-content:space-between;align-items:center;gap:8px;padding:8px 12px 10px;display:flex}.Sr3ygG_interactionPager{flex:none;align-items:center;gap:4px;display:flex}.Sr3ygG_interactionProgress{color:var(--dsh-tb-text-2);white-space:nowrap;word-spacing:-2px;padding:0 4px;font-size:13px;font-weight:500;line-height:20px}.Sr3ygG_interactionFeedback{min-width:0;min-height:16px;color:var(--dsh-tb-danger);text-align:right;overflow-wrap:anywhere;flex:1;font-size:11px;line-height:16px}.Sr3ygG_interactionFooterActions{flex:none;align-items:center;gap:8px;display:flex}.Sr3ygG_interactionStrip{color:var(--dsh-tb-text-2);border-bottom:1px solid var(--dsw-alias-border-l2);flex:none;align-items:center;gap:6px;padding:8px 12px;font-size:12px;display:flex}.Sr3ygG_interactionStripDot{width:var(--dsh-tb-dot);height:var(--dsh-tb-dot);background:var(--dsh-tb-attention);border-radius:50%;flex:none}.Sr3ygG_interactionPlanBody{overscroll-behavior:contain;flex:auto;min-width:0;min-height:0;padding:10px 12px;font-size:13px;line-height:1.5;overflow-y:auto}.Sr3ygG_interactionActions{flex-wrap:wrap;flex:none;align-items:center;gap:8px;padding:0 12px 10px;display:flex}.Sr3ygG_attachStrip{flex-wrap:nowrap;align-items:center;gap:6px;min-height:24px;display:flex;overflow-x:auto}[data-dsh-tb-dndover=\"\"]{outline:2px dashed var(--dsh-tb-accent);outline-offset:-2px;border-radius:var(--dsh-tb-radius-md)}.Sr3ygG_attachAdd{width:24px;height:24px;color:var(--dsh-tb-text-2);background:var(--dsh-tb-surface-sunken);border:1px solid var(--dsw-alias-border-l2);border-radius:var(--dsh-tb-pill);cursor:pointer;flex:none;justify-content:center;align-items:center;display:inline-flex}.Sr3ygG_attachAdd:hover{background:var(--dsh-tb-hover);color:var(--dsh-tb-text-1)}.Sr3ygG_attachChip{max-width:180px;color:var(--dsh-tb-text-2);background:var(--dsh-tb-surface-float);border:1px solid var(--dsw-alias-border-l2);border-radius:var(--dsh-tb-pill);white-space:nowrap;flex:none;align-items:center;gap:4px;padding:2px 6px 2px 8px;font-size:12px;display:inline-flex;overflow:hidden}.Sr3ygG_attachChipName{text-overflow:ellipsis;white-space:nowrap;min-width:0;overflow:hidden}.Sr3ygG_attachChipMeta{color:var(--dsh-tb-text-3);font-variant-numeric:tabular-nums;white-space:nowrap;flex:none;font-size:11px}.Sr3ygG_attachRemove{width:16px;height:16px;color:var(--dsh-tb-text-3);cursor:pointer;background:0 0;border:none;border-radius:4px;flex:none;justify-content:center;align-items:center;display:inline-flex}.Sr3ygG_attachRemove:hover{color:var(--dsh-tb-text-1)}.Sr3ygG_attachHint{color:var(--dsh-tb-text-2);font-size:12px}.Sr3ygG_attachError{min-width:0;color:var(--dsw-alias-state-error-primary);overflow-wrap:anywhere;flex:100%;font-size:12px}.Sr3ygG_sessionContextWrap{padding:0 var(--dsh-tb-rail-inset);flex:none;position:relative}.Sr3ygG_sessionContextHead{width:100%;min-width:0;color:var(--dsh-tb-text-2);border-radius:var(--dsh-tb-radius-sm);cursor:pointer;text-align:left;font:inherit;background:0 0;border:none;align-items:center;gap:7px;padding:0;display:inline-flex}.Sr3ygG_sessionContextHead:hover{color:var(--dsh-tb-text-1)}.Sr3ygG_sessionContextLead{color:var(--dsh-tb-accent);flex:none}.Sr3ygG_sessionContextTitle{flex:none;font-size:12px;font-weight:600}.Sr3ygG_sessionContextSummary{text-overflow:ellipsis;white-space:nowrap;min-width:0;color:var(--dsh-tb-text-3);flex:1;font-size:12px;overflow:hidden}.Sr3ygG_sessionContextChevron{color:var(--dsh-tb-text-3);transition:transform var(--dsh-tb-motion);flex:none}.Sr3ygG_sessionContextHead[aria-expanded=false] .Sr3ygG_sessionContextChevron{transform:rotate(-90deg)}.Sr3ygG_sessionContextPanel{z-index:1;background:var(--dsh-tb-surface-menu);border-radius:var(--dsh-tb-radius-md);max-height:320px;box-shadow:var(--dsh-tb-shadow-2);flex-direction:column;gap:6px;padding:10px 12px;display:flex;top:calc(100% + 6px);left:14px;right:14px;overflow-y:auto}.Sr3ygG_sessionContextRow{align-items:flex-start;gap:8px;min-width:0;display:flex}.Sr3ygG_sessionContextText{color:var(--dsh-tb-text-2);overflow-wrap:break-word;min-width:0;font-size:12px}.Sr3ygG_sessionContextText[data-done]{color:var(--dsh-tb-text-3);text-decoration:line-through}.Sr3ygG_sessionContextGoalOn{color:var(--dsh-tb-success);flex:none}.Sr3ygG_goalStrip{flex-wrap:wrap;align-items:center;gap:6px;min-width:0;display:flex}.Sr3ygG_goalStrip .Sr3ygG_sessionContextRow{flex:100%}.Sr3ygG_goalStrip .Sr3ygG_sessionContextText{flex:1}.Sr3ygG_goalStripActions{flex:none;align-items:center;gap:2px;display:inline-flex}.Sr3ygG_goalStripActions .Sr3ygG_iconButton{width:24px;height:24px}.Sr3ygG_goalStripInput{flex:140px;min-width:0}.Sr3ygG_goalStripError{min-width:0;color:var(--dsw-alias-state-error-primary);overflow-wrap:anywhere;flex:100%;font-size:12px}.Sr3ygG_goalStripBlocked{min-width:0;color:var(--dsh-tb-text-3);overflow-wrap:break-word;flex:100%;margin:0;font-size:12px}.Sr3ygG_sessionContextTodos{flex-direction:column;gap:5px;margin:0;padding:0;list-style:none;display:flex}.Sr3ygG_sessionContextTodo{align-items:flex-start;gap:8px;min-width:0;display:flex}.Sr3ygG_sessionContextTodoGlyph{flex:none;justify-content:center;align-items:center;width:14px;height:14px;margin-top:1px;display:inline-flex}.Sr3ygG_sessionContextGlyphPending{border:1px dashed var(--dsh-tb-text-3);border-radius:50%;width:9px;height:9px}.Sr3ygG_sessionContextGlyphActive{width:10px;height:10px}.Sr3ygG_sessionContextGlyphDone{color:var(--dsh-tb-success)}.Sr3ygG_sessionContextSubagent{border-color:color-mix(in srgb, var(--dsh-tb-accent) 45%, transparent);color:var(--dsh-tb-accent);flex:none}.Sr3ygG_sessionContextBlock{flex-direction:column;gap:6px;min-width:0;display:flex}.Sr3ygG_sessionContextSubList{flex-direction:column;gap:4px;min-width:0;margin:0;padding:0;list-style:none;display:flex}.Sr3ygG_sessionContextSubItem{align-items:flex-start;gap:7px;min-width:0;line-height:1.45;display:flex}.Sr3ygG_sessionContextSubDot{background:var(--dsh-tb-attention);border-radius:50%;flex:none;width:7px;height:7px;margin-top:4px}.Sr3ygG_reviewComposerRow{align-items:center;gap:10px;display:flex}@container Sr3ygG_dsh-tb (width<=648px){.Sr3ygG_review{width:calc(100% - 16px);max-height:calc(100% - 16px)}}@container Sr3ygG_dsh-tb-panel (width<=600px){.Sr3ygG_reviewBody{grid-template:\"Sr3ygG_folds\"minmax(0,auto)\"Sr3ygG_transcript\"minmax(0,1fr)\"Sr3ygG_composer\"minmax(0,auto)/minmax(0,1fr);display:grid;overflow:hidden}.Sr3ygG_reviewRail{display:contents}.Sr3ygG_sessionRailFolds{border-bottom:var(--dsh-tb-separator);grid-area:Sr3ygG_folds;gap:10px;min-height:0;max-height:340px;padding:10px 0;overflow-y:auto}.Sr3ygG_sessionRailHead{border-bottom:none;padding-bottom:0}.Sr3ygG_reviewMain{grid-area:Sr3ygG_transcript;min-height:0;padding:10px 14px;overflow:hidden}.Sr3ygG_reviewComposer{min-height:0;padding-top:10px;padding-bottom:calc(12px + env(safe-area-inset-bottom));grid-area:Sr3ygG_composer;overflow-y:auto}.Sr3ygG_commentsScroll{max-height:300px;overflow-y:auto}.Sr3ygG_reviewHeader{grid-template-columns:minmax(0,1fr) auto auto;grid-template-areas:\"Sr3ygG_title Sr3ygG_badge Sr3ygG_close\"\"Sr3ygG_context Sr3ygG_actions Sr3ygG_actions\";align-items:center;gap:8px 10px;min-height:0;padding:14px}.Sr3ygG_reviewTitleWrap{min-width:0}.Sr3ygG_reviewBadge{justify-self:start}.Sr3ygG_reviewHeader>.Sr3ygG_iconButton{justify-self:end}.Sr3ygG_reviewHeaderContext{justify-self:stretch;min-width:0;max-width:none}.Sr3ygG_reviewHeaderContext[data-open]>.Sr3ygG_sessionContextHead{max-width:none}.Sr3ygG_reviewHeaderContext[data-open]>.Sr3ygG_sessionContextPanel{max-height:240px}.Sr3ygG_reviewActions{justify-self:end}.Sr3ygG_reviewTranscriptScroll{min-height:0;overflow-y:auto}}.Sr3ygG_cardColorBar{flex-wrap:wrap;align-items:center;gap:5px;padding-top:2px;display:none}.Sr3ygG_card:hover .Sr3ygG_cardColorBar,.Sr3ygG_card:focus-within .Sr3ygG_cardColorBar{display:flex}.Sr3ygG_tagSwatches{flex-wrap:wrap;align-items:center;gap:6px;display:inline-flex}.Sr3ygG_tagSwatch{border:1px solid color-mix(in srgb, var(--dsw-static-neutral-1000) 22%, transparent);cursor:pointer;width:16px;height:16px;transition:transform var(--dsh-tb-motion), box-shadow var(--dsh-tb-motion);border-radius:50%;flex:none;padding:0}.Sr3ygG_tagSwatch:hover{transform:scale(1.12)}.Sr3ygG_tagSwatchOn{box-shadow:0 0 0 2px var(--dsh-tb-surface-float), 0 0 0 4px var(--dsw-alias-state-business-primary)}.Sr3ygG_tagSwatchNone{box-shadow:inset 0 0 0 1px var(--dsw-alias-border-l2);background:0 0}.Sr3ygG_tagSwatchNone:hover{background:var(--dsh-tb-surface-sunken)}.Sr3ygG_tagCustomWrap{border-radius:50%;flex:none;width:16px;height:16px;display:inline-flex;position:relative;overflow:hidden}.Sr3ygG_tagCustomEmpty{background:conic-gradient(red,#ff0,#0f0,#0ff,#00f,#f0f,red)}.Sr3ygG_tagCustomWrap:focus-within{box-shadow:0 0 0 2px var(--dsh-tb-surface-float), 0 0 0 4px var(--dsw-alias-state-business-primary)}.Sr3ygG_tagCustomColor{-webkit-appearance:none;appearance:none;opacity:0;cursor:pointer;background:0 0;border:0;border-radius:50%;width:100%;height:100%;padding:0;position:absolute;inset:0}.Sr3ygG_organizeBar{grid-template-columns:minmax(0,1fr) auto auto;grid-template-areas:\"Sr3ygG_colors Sr3ygG_count Sr3ygG_actions\";align-items:center;gap:8px 12px;width:100%;display:grid}.Sr3ygG_organizeGroup{flex-wrap:wrap;grid-area:Sr3ygG_colors;align-items:center;gap:6px;min-width:0;display:inline-flex}.Sr3ygG_organizeLabel{color:var(--dsh-tb-text-3);white-space:nowrap;flex:none;font-size:12px}.Sr3ygG_organizeCount{font-variant-numeric:tabular-nums;color:var(--dsh-tb-text-2);white-space:nowrap;flex:none;grid-area:Sr3ygG_count;font-size:12px}.Sr3ygG_organizeActions{flex-wrap:wrap;grid-area:Sr3ygG_actions;justify-content:flex-end;align-items:center;gap:6px;display:inline-flex}.Sr3ygG_organizeDanger{align-items:center;display:inline-flex}@container Sr3ygG_dsh-tb (width<=680px){.Sr3ygG_board{border-top:var(--dsh-tb-separator);--dsh-tb-dock-x:12px;--dsh-tb-dock-b:12px;padding:14px var(--dsh-tb-dock-x) calc(var(--dsh-tb-dock-b) + env(safe-area-inset-bottom));gap:10px}.Sr3ygG_boardRow{gap:8px}.Sr3ygG_boardRowNav{grid-template-columns:auto minmax(0,1fr) auto;grid-template-areas:\"Sr3ygG_back Sr3ygG_title Sr3ygG_cruise\"\"Sr3ygG_state Sr3ygG_state Sr3ygG_state\";align-items:center;display:grid}.Sr3ygG_boardRowNav:not(:has(.Sr3ygG_boardState)){grid-template-areas:\"Sr3ygG_back Sr3ygG_title Sr3ygG_cruise\"}.Sr3ygG_boardRowNav .Sr3ygG_boardBack{grid-area:Sr3ygG_back}.Sr3ygG_boardRowNav .Sr3ygG_boardTitle{grid-area:Sr3ygG_title}.Sr3ygG_boardRowNav .Sr3ygG_boardSpacer{display:none}.Sr3ygG_boardRowNav .Sr3ygG_boardState{grid-area:Sr3ygG_state}.Sr3ygG_boardRowNav .Sr3ygG_cruiseWrap{grid-area:Sr3ygG_cruise;justify-self:end}.Sr3ygG_boardRowNav .Sr3ygG_boardNewTask{grid-area:Sr3ygG_new;justify-self:end}.Sr3ygG_boardRowTools{grid-template-rows:var(--dsh-tb-button-h) var(--dsh-tb-button-h) auto;row-gap:var(--dsh-tb-strip-gap);grid-template-columns:minmax(0,1fr);grid-template-areas:\"Sr3ygG_modes\"\"Sr3ygG_search\"\"Sr3ygG_tabs\";display:grid}.Sr3ygG_boardRowTools .Sr3ygG_search{grid-area:Sr3ygG_search;min-width:0;max-width:none}.Sr3ygG_boardRowTools .Sr3ygG_boardModes{grid-area:Sr3ygG_modes;justify-self:start}.Sr3ygG_boardRowTools .Sr3ygG_columnTabs{grid-area:Sr3ygG_tabs}.Sr3ygG_organizeBar{grid-template-columns:minmax(0,1fr);grid-template-areas:\"Sr3ygG_colors\"\"Sr3ygG_count\"\"Sr3ygG_actions\"}.Sr3ygG_organizeBar .Sr3ygG_organizeActions{justify-content:flex-start}.Sr3ygG_columns{flex-direction:row;grid-template-columns:none;gap:10px;display:flex;overflow:auto hidden}.Sr3ygG_column{flex:0 0 clamp(200px,46cqw,320px)}.Sr3ygG_columnHeader{padding:12px 20px}.Sr3ygG_columnTabs{grid-template-columns:repeat(5,minmax(0,1fr));grid-template-rows:var(--dsh-tb-button-h);padding:var(--dsh-tb-strip-gap) 0;align-content:center;gap:6px;display:grid;overflow-x:visible}.Sr3ygG_boardNewTask,.Sr3ygG_boardModes .Sr3ygG_notifyBell,.Sr3ygG_boardModes .Sr3ygG_modeDynamic{display:none}.Sr3ygG_thumbBar{margin:0 calc(-1 * var(--dsh-tb-dock-x)) calc(-1 * (var(--dsh-tb-dock-b) + env(safe-area-inset-bottom)));padding:10px var(--dsh-tb-rail-inset) calc(10px + var(--dsh-tb-dock-b) + env(safe-area-inset-bottom));background:var(--dsh-tb-bg);border-top:var(--dsh-tb-separator);z-index:5;flex:none;align-items:center;gap:10px;display:flex}.Sr3ygG_thumbNew{flex:auto;min-width:0;display:flex}.Sr3ygG_thumbNew>*{flex:auto;justify-content:center}.Sr3ygG_columnTab{border-radius:var(--dsh-tb-pill);justify-content:center;min-width:0;padding:0 6px}.Sr3ygG_columnTab>span{min-width:0}.Sr3ygG_columnTab .Sr3ygG_statusDot,.Sr3ygG_columnTab .Sr3ygG_columnTabCount{flex:none}.Sr3ygG_columnTab .Sr3ygG_statusDot{display:none}.Sr3ygG_columnTabLabel{text-overflow:ellipsis;white-space:nowrap;overflow:hidden}.Sr3ygG_modalBackdrop{padding:8px 8px calc(8px + var(--dsh-tb-kb,0px))}.Sr3ygG_modalFooter,.Sr3ygG_reviewComposerRow{flex-wrap:wrap}.Sr3ygG_detailFooter{grid-template-columns:minmax(0,1fr) auto;grid-template-areas:\"Sr3ygG_actions Sr3ygG_actions\"\"Sr3ygG_meta Sr3ygG_danger\";row-gap:10px}.Sr3ygG_detailFooter>.Sr3ygG_detailMeta{justify-self:start}.Sr3ygG_scheduleGrid,.Sr3ygG_configGrid{grid-template-columns:minmax(0,1fr);row-gap:4px}.Sr3ygG_segmentedRow{flex-wrap:wrap;row-gap:4px}.Sr3ygG_presetNew,.Sr3ygG_presetRow{grid-template-columns:minmax(0,1fr);row-gap:6px}.Sr3ygG_runPresetRow{grid-template-columns:minmax(0,1fr) auto;grid-template-areas:\"Sr3ygG_name Sr3ygG_name\"\"Sr3ygG_note Sr3ygG_actions\";row-gap:4px}.Sr3ygG_runPresetRow .Sr3ygG_runPresetName{grid-area:Sr3ygG_name}.Sr3ygG_runPresetRow .Sr3ygG_detailHint{white-space:normal;grid-area:Sr3ygG_note;min-width:0}.Sr3ygG_runPresetRow .Sr3ygG_autoRuleActions{grid-area:Sr3ygG_actions;justify-self:end}.Sr3ygG_sectionAction{justify-self:end}.Sr3ygG_presetRow .Sr3ygG_presetActions{justify-self:start}.Sr3ygG_sessionRowTop{grid-template-columns:minmax(0,1fr) auto;grid-template-areas:\"Sr3ygG_lead Sr3ygG_act\"\"Sr3ygG_chip Sr3ygG_act\";align-items:start;row-gap:4px}.Sr3ygG_sessionRowTop .Sr3ygG_sessionRowActions{align-self:start}.Sr3ygG_sessionRowTop .Sr3ygG_sessionRowChip{justify-self:start;padding-left:0}.Sr3ygG_sessionRowLead{grid-template-columns:minmax(0,1fr);grid-template-areas:\"Sr3ygG_identity\";align-items:start;min-width:0;display:grid}.Sr3ygG_sessionRowLeading{grid-area:Sr3ygG_identity;grid-template-columns:var(--dsh-tb-lead-icon) minmax(0, 1fr);column-gap:var(--dsh-tb-lead-gap);grid-template-areas:\"Sr3ygG_icon Sr3ygG_name\"\"Sr3ygG_workspace Sr3ygG_workspace\";align-items:start;row-gap:4px;min-width:0;display:grid}.Sr3ygG_sessionRowLeading .Sr3ygG_sessionRowIcon{height:var(--dsh-tb-row-line);grid-area:Sr3ygG_icon;align-self:center;align-items:center;display:inline-flex}.Sr3ygG_sessionRowLeading .Sr3ygG_sessionRowName{grid-area:Sr3ygG_name;min-width:0}.Sr3ygG_sessionRowLeading .Sr3ygG_sessionRowWorkspace{flex:none;grid-area:Sr3ygG_workspace;justify-self:start;max-width:100%}.Sr3ygG_sessionRowActions .Sr3ygG_rowActionText{display:none}.Sr3ygG_sessionRowActions .Sr3ygG_rowActionIcon{display:inline-flex}.Sr3ygG_sessionRowActions{gap:4px}}@media (hover:none) and (pointer:coarse){.Sr3ygG_buttonSm,.Sr3ygG_rowHide,.Sr3ygG_cruiseMore,.Sr3ygG_boardBack,.Sr3ygG_iconButton,.Sr3ygG_sessionRowHandle,.Sr3ygG_segmentedButton,.Sr3ygG_attachAdd,.Sr3ygG_attachRemove,.Sr3ygG_reviewCommentCancel,.Sr3ygG_promptCopy,.Sr3ygG_timeFieldCalendar,.Sr3ygG_notifyMain,.Sr3ygG_filterChipRemove,.Sr3ygG_feedFilter,.Sr3ygG_feedAction,.Sr3ygG_detailDisclosure,.Sr3ygG_autoTaskExpand,.Sr3ygG_sessionContextHead,.Sr3ygG_tagCustomColor,.Sr3ygG_thumbNew>*,.Sr3ygG_thumbBar .Sr3ygG_iconButton{position:relative}.Sr3ygG_buttonSm:before,.Sr3ygG_rowHide:before,.Sr3ygG_cruiseMore:before,.Sr3ygG_boardBack:before,.Sr3ygG_iconButton:before,.Sr3ygG_sessionRowHandle:before,.Sr3ygG_segmentedButton:before,.Sr3ygG_attachAdd:before,.Sr3ygG_attachRemove:before,.Sr3ygG_reviewCommentCancel:before,.Sr3ygG_promptCopy:before,.Sr3ygG_timeFieldCalendar:before,.Sr3ygG_notifyMain:before,.Sr3ygG_filterChipRemove:before,.Sr3ygG_feedFilter:before,.Sr3ygG_feedAction:before,.Sr3ygG_detailDisclosure:before,.Sr3ygG_autoTaskExpand:before,.Sr3ygG_sessionContextHead:before,.Sr3ygG_tagCustomColor:before,.Sr3ygG_thumbNew>:before,.Sr3ygG_thumbBar .Sr3ygG_iconButton:before{content:\"\";border-radius:inherit;position:absolute;inset:-7px}.Sr3ygG_tagSwatch,.Sr3ygG_tagCustomWrap{width:24px;height:24px}.Sr3ygG_input,.Sr3ygG_promptTextarea,.Sr3ygG_search,.Sr3ygG_selectWrap select,.Sr3ygG_sessionRenameInput,.Sr3ygG_scheduleInput,.Sr3ygG_presetCron,.Sr3ygG_cruiseLimit,.Sr3ygG_timeFieldRow .Sr3ygG_input{font-size:16px}}";
		const tagId$1 = "@firetruck666/dsh-task-board/board.module.css";
		if (typeof document !== "undefined" && document.querySelector("style[data-plugin-css=" + JSON.stringify(tagId$1) + "]") === null) {
			const tag = document.createElement("style");
			tag.dataset.plugin = "@firetruck666/dsh-task-board";
			tag.dataset.pluginCss = tagId$1;
			tag.textContent = css$1;
			document.head.appendChild(tag);
		}
		var board_module_css_default = {
			"act": "Sr3ygG_act",
			"action": "Sr3ygG_action",
			"actions": "Sr3ygG_actions",
			"addSessionChevron": "Sr3ygG_addSessionChevron",
			"addSessionLabel": "Sr3ygG_addSessionLabel",
			"addSessionList": "Sr3ygG_addSessionList",
			"addSessionRow": "Sr3ygG_addSessionRow",
			"attachAdd": "Sr3ygG_attachAdd",
			"attachChip": "Sr3ygG_attachChip",
			"attachChipMeta": "Sr3ygG_attachChipMeta",
			"attachChipName": "Sr3ygG_attachChipName",
			"attachError": "Sr3ygG_attachError",
			"attachHint": "Sr3ygG_attachHint",
			"attachRemove": "Sr3ygG_attachRemove",
			"attachStrip": "Sr3ygG_attachStrip",
			"autoAddAction": "Sr3ygG_autoAddAction",
			"autoEmpty": "Sr3ygG_autoEmpty",
			"autoField": "Sr3ygG_autoField",
			"autoFieldLabel": "Sr3ygG_autoFieldLabel",
			"autoForm": "Sr3ygG_autoForm",
			"autoFormActions": "Sr3ygG_autoFormActions",
			"autoGroup": "Sr3ygG_autoGroup",
			"autoList": "Sr3ygG_autoList",
			"autoMetaIcon": "Sr3ygG_autoMetaIcon",
			"autoModal": "Sr3ygG_autoModal",
			"autoNameInput": "Sr3ygG_autoNameInput",
			"autoRuleActions": "Sr3ygG_autoRuleActions",
			"autoRuleButtons": "Sr3ygG_autoRuleButtons",
			"autoRuleInstruction": "Sr3ygG_autoRuleInstruction",
			"autoRuleList": "Sr3ygG_autoRuleList",
			"autoRuleMeta": "Sr3ygG_autoRuleMeta",
			"autoRuleRow": "Sr3ygG_autoRuleRow",
			"autoRuleSession": "Sr3ygG_autoRuleSession",
			"autoRuleSessionTitle": "Sr3ygG_autoRuleSessionTitle",
			"autoRuleTop": "Sr3ygG_autoRuleTop",
			"autoRules": "Sr3ygG_autoRules",
			"autoTask": "Sr3ygG_autoTask",
			"autoTaskChevron": "Sr3ygG_autoTaskChevron",
			"autoTaskExpand": "Sr3ygG_autoTaskExpand",
			"autoTaskHead": "Sr3ygG_autoTaskHead",
			"autoTaskSummary": "Sr3ygG_autoTaskSummary",
			"autoTaskTitle": "Sr3ygG_autoTaskTitle",
			"back": "Sr3ygG_back",
			"badge": "Sr3ygG_badge",
			"board": "Sr3ygG_board",
			"boardBack": "Sr3ygG_boardBack",
			"boardDemand": "Sr3ygG_boardDemand",
			"boardDemandDot": "Sr3ygG_boardDemandDot",
			"boardDemandText": "Sr3ygG_boardDemandText",
			"boardHeader": "Sr3ygG_boardHeader",
			"boardModes": "Sr3ygG_boardModes",
			"boardNewTask": "Sr3ygG_boardNewTask",
			"boardRow": "Sr3ygG_boardRow",
			"boardRowNav": "Sr3ygG_boardRowNav",
			"boardRowTools": "Sr3ygG_boardRowTools",
			"boardSpacer": "Sr3ygG_boardSpacer",
			"boardState": "Sr3ygG_boardState",
			"boardStatus": "Sr3ygG_boardStatus",
			"boardStatusButton": "Sr3ygG_boardStatusButton",
			"boardStatusDot": "Sr3ygG_boardStatusDot",
			"boardStatusText": "Sr3ygG_boardStatusText",
			"boardTitle": "Sr3ygG_boardTitle",
			"buttonPressed": "Sr3ygG_buttonPressed",
			"buttonSm": "Sr3ygG_buttonSm",
			"buttons": "Sr3ygG_buttons",
			"cancel": "Sr3ygG_cancel",
			"card": "Sr3ygG_card",
			"cardBadges": "Sr3ygG_cardBadges",
			"cardColorBar": "Sr3ygG_cardColorBar",
			"cardColorMark": "Sr3ygG_cardColorMark",
			"cardExcerpt": "Sr3ygG_cardExcerpt",
			"cardMeta": "Sr3ygG_cardMeta",
			"cardMetaRow": "Sr3ygG_cardMetaRow",
			"cardNext": "Sr3ygG_cardNext",
			"cardQuickRun": "Sr3ygG_cardQuickRun",
			"cardSelectedBadge": "Sr3ygG_cardSelectedBadge",
			"cardSessionDot": "Sr3ygG_cardSessionDot",
			"cardSessionMore": "Sr3ygG_cardSessionMore",
			"cardSessions": "Sr3ygG_cardSessions",
			"cardTime": "Sr3ygG_cardTime",
			"cardTitle": "Sr3ygG_cardTitle",
			"cardTitleRow": "Sr3ygG_cardTitleRow",
			"cardWorkspace": "Sr3ygG_cardWorkspace",
			"cardWorkspaceDot": "Sr3ygG_cardWorkspaceDot",
			"cardWorkspaceName": "Sr3ygG_cardWorkspaceName",
			"cards": "Sr3ygG_cards",
			"chip": "Sr3ygG_chip",
			"chipBody": "Sr3ygG_chipBody",
			"chipFill": "Sr3ygG_chipFill",
			"chipLead": "Sr3ygG_chipLead",
			"close": "Sr3ygG_close",
			"colors": "Sr3ygG_colors",
			"column": "Sr3ygG_column",
			"columnCount": "Sr3ygG_columnCount",
			"columnEmpty": "Sr3ygG_columnEmpty",
			"columnEmptyHint": "Sr3ygG_columnEmptyHint",
			"columnHeader": "Sr3ygG_columnHeader",
			"columnTab": "Sr3ygG_columnTab",
			"columnTabCount": "Sr3ygG_columnTabCount",
			"columnTabLabel": "Sr3ygG_columnTabLabel",
			"columnTabs": "Sr3ygG_columnTabs",
			"columnTitle": "Sr3ygG_columnTitle",
			"columns": "Sr3ygG_columns",
			"commentsScroll": "Sr3ygG_commentsScroll",
			"composer": "Sr3ygG_composer",
			"configGrid": "Sr3ygG_configGrid",
			"configLabel": "Sr3ygG_configLabel",
			"configRow": "Sr3ygG_configRow",
			"configValue": "Sr3ygG_configValue",
			"confirmMessage": "Sr3ygG_confirmMessage",
			"contentBlock": "Sr3ygG_contentBlock",
			"context": "Sr3ygG_context",
			"count": "Sr3ygG_count",
			"cruise": "Sr3ygG_cruise",
			"cruiseLimit": "Sr3ygG_cruiseLimit",
			"cruiseMore": "Sr3ygG_cruiseMore",
			"cruisePill": "Sr3ygG_cruisePill",
			"cruisePopover": "Sr3ygG_cruisePopover",
			"cruisePopoverHead": "Sr3ygG_cruisePopoverHead",
			"cruisePopoverLabel": "Sr3ygG_cruisePopoverLabel",
			"cruisePopoverLimit": "Sr3ygG_cruisePopoverLimit",
			"cruiseSchedule": "Sr3ygG_cruiseSchedule",
			"cruiseScheduleHead": "Sr3ygG_cruiseScheduleHead",
			"cruiseScheduleTitle": "Sr3ygG_cruiseScheduleTitle",
			"cruiseStateLine": "Sr3ygG_cruiseStateLine",
			"cruiseWindowAdd": "Sr3ygG_cruiseWindowAdd",
			"cruiseWindowList": "Sr3ygG_cruiseWindowList",
			"cruiseWindowRow": "Sr3ygG_cruiseWindowRow",
			"cruiseWindowTime": "Sr3ygG_cruiseWindowTime",
			"cruiseWrap": "Sr3ygG_cruiseWrap",
			"danger": "Sr3ygG_danger",
			"dangerButton": "Sr3ygG_dangerButton",
			"dangerGhostButton": "Sr3ygG_dangerGhostButton",
			"detail": "Sr3ygG_detail",
			"detailBlockedField": "Sr3ygG_detailBlockedField",
			"detailBody": "Sr3ygG_detailBody",
			"detailChevron": "Sr3ygG_detailChevron",
			"detailDisclosure": "Sr3ygG_detailDisclosure",
			"detailDisclosureSummary": "Sr3ygG_detailDisclosureSummary",
			"detailDisclosureTitle": "Sr3ygG_detailDisclosureTitle",
			"detailEmptyField": "Sr3ygG_detailEmptyField",
			"detailFooter": "Sr3ygG_detailFooter",
			"detailFooterDanger": "Sr3ygG_detailFooterDanger",
			"detailFooterGroup": "Sr3ygG_detailFooterGroup",
			"detailHeader": "Sr3ygG_detailHeader",
			"detailHint": "Sr3ygG_detailHint",
			"detailMeta": "Sr3ygG_detailMeta",
			"detailSection": "Sr3ygG_detailSection",
			"detailText": "Sr3ygG_detailText",
			"detailTitle": "Sr3ygG_detailTitle",
			"dialogHeader": "Sr3ygG_dialogHeader",
			"dragRejectHint": "Sr3ygG_dragRejectHint",
			"dragging": "Sr3ygG_dragging",
			"dropIndicator": "Sr3ygG_dropIndicator",
			"dsh-tb": "Sr3ygG_dsh-tb",
			"dsh-tb-panel": "Sr3ygG_dsh-tb-panel",
			"dshTbBreathHalo": "Sr3ygG_dshTbBreathHalo",
			"dshTbBreathRing": "Sr3ygG_dshTbBreathRing",
			"dshTbDialogIn": "Sr3ygG_dshTbDialogIn",
			"dshTbDropConfirm": "Sr3ygG_dshTbDropConfirm",
			"dshTbFadeIn": "Sr3ygG_dshTbFadeIn",
			"dshTbFadeUp": "Sr3ygG_dshTbFadeUp",
			"dshTbFooterPending": "Sr3ygG_dshTbFooterPending",
			"dshTbRejectFlash": "Sr3ygG_dshTbRejectFlash",
			"dshTbSelectIn": "Sr3ygG_dshTbSelectIn",
			"dshTbSpin": "Sr3ygG_dshTbSpin",
			"executionComments": "Sr3ygG_executionComments",
			"executionCommentsCount": "Sr3ygG_executionCommentsCount",
			"executionCommentsLatest": "Sr3ygG_executionCommentsLatest",
			"executionCommentsTime": "Sr3ygG_executionCommentsTime",
			"executionDynamics": "Sr3ygG_executionDynamics",
			"executionDynamicsLabel": "Sr3ygG_executionDynamicsLabel",
			"executionError": "Sr3ygG_executionError",
			"feedAction": "Sr3ygG_feedAction",
			"feedDay": "Sr3ygG_feedDay",
			"feedFilter": "Sr3ygG_feedFilter",
			"feedFilterGroup": "Sr3ygG_feedFilterGroup",
			"feedPreview": "Sr3ygG_feedPreview",
			"feedSearch": "Sr3ygG_feedSearch",
			"feedTools": "Sr3ygG_feedTools",
			"field": "Sr3ygG_field",
			"fieldHint": "Sr3ygG_fieldHint",
			"fieldLabel": "Sr3ygG_fieldLabel",
			"figures": "Sr3ygG_figures",
			"filterChip": "Sr3ygG_filterChip",
			"filterChipRemove": "Sr3ygG_filterChipRemove",
			"filterChips": "Sr3ygG_filterChips",
			"folds": "Sr3ygG_folds",
			"formError": "Sr3ygG_formError",
			"ghostButton": "Sr3ygG_ghostButton",
			"goalStrip": "Sr3ygG_goalStrip",
			"goalStripActions": "Sr3ygG_goalStripActions",
			"goalStripBlocked": "Sr3ygG_goalStripBlocked",
			"goalStripError": "Sr3ygG_goalStripError",
			"goalStripInput": "Sr3ygG_goalStripInput",
			"hiddenTray": "Sr3ygG_hiddenTray",
			"hiddenTrayActions": "Sr3ygG_hiddenTrayActions",
			"hiddenTrayHead": "Sr3ygG_hiddenTrayHead",
			"hiddenTrayList": "Sr3ygG_hiddenTrayList",
			"hiddenTrayName": "Sr3ygG_hiddenTrayName",
			"hiddenTrayRow": "Sr3ygG_hiddenTrayRow",
			"hiddenTrayTitle": "Sr3ygG_hiddenTrayTitle",
			"icon": "Sr3ygG_icon",
			"iconButton": "Sr3ygG_iconButton",
			"identity": "Sr3ygG_identity",
			"input": "Sr3ygG_input",
			"inputInvalid": "Sr3ygG_inputInvalid",
			"interactionActions": "Sr3ygG_interactionActions",
			"interactionBadge": "Sr3ygG_interactionBadge",
			"interactionBody": "Sr3ygG_interactionBody",
			"interactionCard": "Sr3ygG_interactionCard",
			"interactionCardBody": "Sr3ygG_interactionCardBody",
			"interactionCheck": "Sr3ygG_interactionCheck",
			"interactionCheckOn": "Sr3ygG_interactionCheckOn",
			"interactionCustomRow": "Sr3ygG_interactionCustomRow",
			"interactionCustomRowOn": "Sr3ygG_interactionCustomRowOn",
			"interactionDetail": "Sr3ygG_interactionDetail",
			"interactionEyebrow": "Sr3ygG_interactionEyebrow",
			"interactionFeedback": "Sr3ygG_interactionFeedback",
			"interactionField": "Sr3ygG_interactionField",
			"interactionFieldBlock": "Sr3ygG_interactionFieldBlock",
			"interactionFieldInline": "Sr3ygG_interactionFieldInline",
			"interactionFieldInput": "Sr3ygG_interactionFieldInput",
			"interactionFieldMirror": "Sr3ygG_interactionFieldMirror",
			"interactionFooter": "Sr3ygG_interactionFooter",
			"interactionFooterActions": "Sr3ygG_interactionFooterActions",
			"interactionHead": "Sr3ygG_interactionHead",
			"interactionHeadActions": "Sr3ygG_interactionHeadActions",
			"interactionHeading": "Sr3ygG_interactionHeading",
			"interactionOption": "Sr3ygG_interactionOption",
			"interactionOptionButton": "Sr3ygG_interactionOptionButton",
			"interactionOptionCopy": "Sr3ygG_interactionOptionCopy",
			"interactionOptionDescription": "Sr3ygG_interactionOptionDescription",
			"interactionOptionLabel": "Sr3ygG_interactionOptionLabel",
			"interactionOptionLine": "Sr3ygG_interactionOptionLine",
			"interactionOptionNumber": "Sr3ygG_interactionOptionNumber",
			"interactionOptionOn": "Sr3ygG_interactionOptionOn",
			"interactionOptions": "Sr3ygG_interactionOptions",
			"interactionPager": "Sr3ygG_interactionPager",
			"interactionPlanBody": "Sr3ygG_interactionPlanBody",
			"interactionProgress": "Sr3ygG_interactionProgress",
			"interactionQuestion": "Sr3ygG_interactionQuestion",
			"interactionQuestionBlock": "Sr3ygG_interactionQuestionBlock",
			"interactionStrip": "Sr3ygG_interactionStrip",
			"interactionStripDot": "Sr3ygG_interactionStripDot",
			"interactionTitle": "Sr3ygG_interactionTitle",
			"label": "Sr3ygG_label",
			"lead": "Sr3ygG_lead",
			"mdCode": "Sr3ygG_mdCode",
			"mdCodeBlock": "Sr3ygG_mdCodeBlock",
			"mdEm": "Sr3ygG_mdEm",
			"mdHeading": "Sr3ygG_mdHeading",
			"mdLink": "Sr3ygG_mdLink",
			"mdList": "Sr3ygG_mdList",
			"mdParagraph": "Sr3ygG_mdParagraph",
			"mdQuote": "Sr3ygG_mdQuote",
			"mdRule": "Sr3ygG_mdRule",
			"mdStrong": "Sr3ygG_mdStrong",
			"meta": "Sr3ygG_meta",
			"meterMessages": "Sr3ygG_meterMessages",
			"meterSystem": "Sr3ygG_meterSystem",
			"meterTools": "Sr3ygG_meterTools",
			"modal": "Sr3ygG_modal",
			"modalBackdrop": "Sr3ygG_modalBackdrop",
			"modalFooter": "Sr3ygG_modalFooter",
			"modalForm": "Sr3ygG_modalForm",
			"modalScroll": "Sr3ygG_modalScroll",
			"modalTitle": "Sr3ygG_modalTitle",
			"modeDynamic": "Sr3ygG_modeDynamic",
			"modes": "Sr3ygG_modes",
			"moveRow": "Sr3ygG_moveRow",
			"name": "Sr3ygG_name",
			"new": "Sr3ygG_new",
			"note": "Sr3ygG_note",
			"notifyActions": "Sr3ygG_notifyActions",
			"notifyBadge": "Sr3ygG_notifyBadge",
			"notifyBell": "Sr3ygG_notifyBell",
			"notifyDot": "Sr3ygG_notifyDot",
			"notifyList": "Sr3ygG_notifyList",
			"notifyMain": "Sr3ygG_notifyMain",
			"notifyRow": "Sr3ygG_notifyRow",
			"notifySession": "Sr3ygG_notifySession",
			"notifyTask": "Sr3ygG_notifyTask",
			"organizeActions": "Sr3ygG_organizeActions",
			"organizeBar": "Sr3ygG_organizeBar",
			"organizeCount": "Sr3ygG_organizeCount",
			"organizeDanger": "Sr3ygG_organizeDanger",
			"organizeGroup": "Sr3ygG_organizeGroup",
			"organizeLabel": "Sr3ygG_organizeLabel",
			"percent": "Sr3ygG_percent",
			"presetActions": "Sr3ygG_presetActions",
			"presetCron": "Sr3ygG_presetCron",
			"presetCronWrap": "Sr3ygG_presetCronWrap",
			"presetEmpty": "Sr3ygG_presetEmpty",
			"presetHint": "Sr3ygG_presetHint",
			"presetList": "Sr3ygG_presetList",
			"presetModal": "Sr3ygG_presetModal",
			"presetName": "Sr3ygG_presetName",
			"presetNew": "Sr3ygG_presetNew",
			"presetRow": "Sr3ygG_presetRow",
			"presetRowAction": "Sr3ygG_presetRowAction",
			"primaryButton": "Sr3ygG_primaryButton",
			"promptBlock": "Sr3ygG_promptBlock",
			"promptBlockText": "Sr3ygG_promptBlockText",
			"promptCopy": "Sr3ygG_promptCopy",
			"promptField": "Sr3ygG_promptField",
			"promptTextarea": "Sr3ygG_promptTextarea",
			"reading": "Sr3ygG_reading",
			"refineActionArea": "Sr3ygG_refineActionArea",
			"refineApplyButton": "Sr3ygG_refineApplyButton",
			"refineHeader": "Sr3ygG_refineHeader",
			"refineIdle": "Sr3ygG_refineIdle",
			"refineInputArea": "Sr3ygG_refineInputArea",
			"refineRounds": "Sr3ygG_refineRounds",
			"refineSection": "Sr3ygG_refineSection",
			"refineTail": "Sr3ygG_refineTail",
			"refineTranscriptArea": "Sr3ygG_refineTranscriptArea",
			"review": "Sr3ygG_review",
			"reviewActions": "Sr3ygG_reviewActions",
			"reviewBadge": "Sr3ygG_reviewBadge",
			"reviewBody": "Sr3ygG_reviewBody",
			"reviewCommandOutcome": "Sr3ygG_reviewCommandOutcome",
			"reviewComment": "Sr3ygG_reviewComment",
			"reviewCommentCancel": "Sr3ygG_reviewCommentCancel",
			"reviewCommentCommand": "Sr3ygG_reviewCommentCommand",
			"reviewCommentError": "Sr3ygG_reviewCommentError",
			"reviewCommentImage": "Sr3ygG_reviewCommentImage",
			"reviewCommentMeta": "Sr3ygG_reviewCommentMeta",
			"reviewCommentText": "Sr3ygG_reviewCommentText",
			"reviewCommentTime": "Sr3ygG_reviewCommentTime",
			"reviewComments": "Sr3ygG_reviewComments",
			"reviewComposer": "Sr3ygG_reviewComposer",
			"reviewComposerRow": "Sr3ygG_reviewComposerRow",
			"reviewConfig": "Sr3ygG_reviewConfig",
			"reviewConfigFailed": "Sr3ygG_reviewConfigFailed",
			"reviewConfigGrid": "Sr3ygG_reviewConfigGrid",
			"reviewConfigLabel": "Sr3ygG_reviewConfigLabel",
			"reviewConfigLoading": "Sr3ygG_reviewConfigLoading",
			"reviewConfigMessage": "Sr3ygG_reviewConfigMessage",
			"reviewConfigRow": "Sr3ygG_reviewConfigRow",
			"reviewConfigTitle": "Sr3ygG_reviewConfigTitle",
			"reviewConfigUnavailable": "Sr3ygG_reviewConfigUnavailable",
			"reviewConfigValue": "Sr3ygG_reviewConfigValue",
			"reviewContext": "Sr3ygG_reviewContext",
			"reviewContextMeter": "Sr3ygG_reviewContextMeter",
			"reviewHeader": "Sr3ygG_reviewHeader",
			"reviewHeaderContext": "Sr3ygG_reviewHeaderContext",
			"reviewImage": "Sr3ygG_reviewImage",
			"reviewImageBox": "Sr3ygG_reviewImageBox",
			"reviewImageMissing": "Sr3ygG_reviewImageMissing",
			"reviewImageRow": "Sr3ygG_reviewImageRow",
			"reviewJumpLatest": "Sr3ygG_reviewJumpLatest",
			"reviewMain": "Sr3ygG_reviewMain",
			"reviewMessage": "Sr3ygG_reviewMessage",
			"reviewMessageText": "Sr3ygG_reviewMessageText",
			"reviewMeterBar": "Sr3ygG_reviewMeterBar",
			"reviewMeterFigures": "Sr3ygG_reviewMeterFigures",
			"reviewMeterHead": "Sr3ygG_reviewMeterHead",
			"reviewMeterPercent": "Sr3ygG_reviewMeterPercent",
			"reviewMeterReading": "Sr3ygG_reviewMeterReading",
			"reviewMeterRow": "Sr3ygG_reviewMeterRow",
			"reviewMeterRows": "Sr3ygG_reviewMeterRows",
			"reviewMeterSegment": "Sr3ygG_reviewMeterSegment",
			"reviewMeterSwatch": "Sr3ygG_reviewMeterSwatch",
			"reviewMeterValue": "Sr3ygG_reviewMeterValue",
			"reviewOutcome": "Sr3ygG_reviewOutcome",
			"reviewOutcomeMeta": "Sr3ygG_reviewOutcomeMeta",
			"reviewRail": "Sr3ygG_reviewRail",
			"reviewTitle": "Sr3ygG_reviewTitle",
			"reviewTitleWrap": "Sr3ygG_reviewTitleWrap",
			"reviewTranscript": "Sr3ygG_reviewTranscript",
			"reviewTranscriptScroll": "Sr3ygG_reviewTranscriptScroll",
			"reviewUsage": "Sr3ygG_reviewUsage",
			"rowActionIcon": "Sr3ygG_rowActionIcon",
			"rowActionText": "Sr3ygG_rowActionText",
			"rowHide": "Sr3ygG_rowHide",
			"runPresetDefaultBadge": "Sr3ygG_runPresetDefaultBadge",
			"runPresetName": "Sr3ygG_runPresetName",
			"runPresetRow": "Sr3ygG_runPresetRow",
			"scheduleActionRow": "Sr3ygG_scheduleActionRow",
			"scheduleCronRow": "Sr3ygG_scheduleCronRow",
			"scheduleGrid": "Sr3ygG_scheduleGrid",
			"scheduleInput": "Sr3ygG_scheduleInput",
			"scheduleLabel": "Sr3ygG_scheduleLabel",
			"scheduleManageButton": "Sr3ygG_scheduleManageButton",
			"scheduleMaxInput": "Sr3ygG_scheduleMaxInput",
			"scheduleMaxRow": "Sr3ygG_scheduleMaxRow",
			"scheduleMeta": "Sr3ygG_scheduleMeta",
			"schedulePreset": "Sr3ygG_schedulePreset",
			"search": "Sr3ygG_search",
			"sectionAction": "Sr3ygG_sectionAction",
			"sectionHead": "Sr3ygG_sectionHead",
			"segmentedActive": "Sr3ygG_segmentedActive",
			"segmentedButton": "Sr3ygG_segmentedButton",
			"segmentedRow": "Sr3ygG_segmentedRow",
			"selectWrap": "Sr3ygG_selectWrap",
			"selectedCard": "Sr3ygG_selectedCard",
			"sessionContextBlock": "Sr3ygG_sessionContextBlock",
			"sessionContextChevron": "Sr3ygG_sessionContextChevron",
			"sessionContextGlyphActive": "Sr3ygG_sessionContextGlyphActive",
			"sessionContextGlyphDone": "Sr3ygG_sessionContextGlyphDone",
			"sessionContextGlyphPending": "Sr3ygG_sessionContextGlyphPending",
			"sessionContextGoalOn": "Sr3ygG_sessionContextGoalOn",
			"sessionContextHead": "Sr3ygG_sessionContextHead",
			"sessionContextLead": "Sr3ygG_sessionContextLead",
			"sessionContextPanel": "Sr3ygG_sessionContextPanel",
			"sessionContextRow": "Sr3ygG_sessionContextRow",
			"sessionContextSubDot": "Sr3ygG_sessionContextSubDot",
			"sessionContextSubItem": "Sr3ygG_sessionContextSubItem",
			"sessionContextSubList": "Sr3ygG_sessionContextSubList",
			"sessionContextSubagent": "Sr3ygG_sessionContextSubagent",
			"sessionContextSummary": "Sr3ygG_sessionContextSummary",
			"sessionContextText": "Sr3ygG_sessionContextText",
			"sessionContextTitle": "Sr3ygG_sessionContextTitle",
			"sessionContextTodo": "Sr3ygG_sessionContextTodo",
			"sessionContextTodoGlyph": "Sr3ygG_sessionContextTodoGlyph",
			"sessionContextTodos": "Sr3ygG_sessionContextTodos",
			"sessionContextWrap": "Sr3ygG_sessionContextWrap",
			"sessionDropZone": "Sr3ygG_sessionDropZone",
			"sessionFactTime": "Sr3ygG_sessionFactTime",
			"sessionFacts": "Sr3ygG_sessionFacts",
			"sessionHintRow": "Sr3ygG_sessionHintRow",
			"sessionHintText": "Sr3ygG_sessionHintText",
			"sessionList": "Sr3ygG_sessionList",
			"sessionRailComments": "Sr3ygG_sessionRailComments",
			"sessionRailFolds": "Sr3ygG_sessionRailFolds",
			"sessionRailHead": "Sr3ygG_sessionRailHead",
			"sessionRenameInput": "Sr3ygG_sessionRenameInput",
			"sessionRenameRow": "Sr3ygG_sessionRenameRow",
			"sessionRow": "Sr3ygG_sessionRow",
			"sessionRowActions": "Sr3ygG_sessionRowActions",
			"sessionRowChip": "Sr3ygG_sessionRowChip",
			"sessionRowHandle": "Sr3ygG_sessionRowHandle",
			"sessionRowIcon": "Sr3ygG_sessionRowIcon",
			"sessionRowLead": "Sr3ygG_sessionRowLead",
			"sessionRowLeading": "Sr3ygG_sessionRowLeading",
			"sessionRowMeta": "Sr3ygG_sessionRowMeta",
			"sessionRowName": "Sr3ygG_sessionRowName",
			"sessionRowTop": "Sr3ygG_sessionRowTop",
			"sessionRowWorkspace": "Sr3ygG_sessionRowWorkspace",
			"sessionToolbarActions": "Sr3ygG_sessionToolbarActions",
			"sidebarFooterAction": "Sr3ygG_sidebarFooterAction",
			"sidebarFooterIcon": "Sr3ygG_sidebarFooterIcon",
			"sidebarFooterLabel": "Sr3ygG_sidebarFooterLabel",
			"slashMenu": "Sr3ygG_slashMenu",
			"slashMenuDesc": "Sr3ygG_slashMenuDesc",
			"slashMenuEmpty": "Sr3ygG_slashMenuEmpty",
			"slashMenuHint": "Sr3ygG_slashMenuHint",
			"slashMenuName": "Sr3ygG_slashMenuName",
			"slashMenuRow": "Sr3ygG_slashMenuRow",
			"slashMenuRowActive": "Sr3ygG_slashMenuRowActive",
			"slashMenuSection": "Sr3ygG_slashMenuSection",
			"spinner": "Sr3ygG_spinner",
			"state": "Sr3ygG_state",
			"statusDot": "Sr3ygG_statusDot",
			"swatch": "Sr3ygG_swatch",
			"switch": "Sr3ygG_switch",
			"switchKnob": "Sr3ygG_switchKnob",
			"switchLabel": "Sr3ygG_switchLabel",
			"switchTrack": "Sr3ygG_switchTrack",
			"tabs": "Sr3ygG_tabs",
			"tagCustomColor": "Sr3ygG_tagCustomColor",
			"tagCustomEmpty": "Sr3ygG_tagCustomEmpty",
			"tagCustomWrap": "Sr3ygG_tagCustomWrap",
			"tagSwatch": "Sr3ygG_tagSwatch",
			"tagSwatchNone": "Sr3ygG_tagSwatchNone",
			"tagSwatchOn": "Sr3ygG_tagSwatchOn",
			"tagSwatches": "Sr3ygG_tagSwatches",
			"templateRow": "Sr3ygG_templateRow",
			"thumbBar": "Sr3ygG_thumbBar",
			"thumbNew": "Sr3ygG_thumbNew",
			"time": "Sr3ygG_time",
			"timeField": "Sr3ygG_timeField",
			"timeFieldCalendar": "Sr3ygG_timeFieldCalendar",
			"timeFieldError": "Sr3ygG_timeFieldError",
			"timeFieldHiddenDate": "Sr3ygG_timeFieldHiddenDate",
			"timeFieldHint": "Sr3ygG_timeFieldHint",
			"timeFieldLabel": "Sr3ygG_timeFieldLabel",
			"timeFieldRow": "Sr3ygG_timeFieldRow",
			"title": "Sr3ygG_title",
			"transcript": "Sr3ygG_transcript",
			"transcriptEarlierRow": "Sr3ygG_transcriptEarlierRow",
			"transcriptErrorRow": "Sr3ygG_transcriptErrorRow",
			"undoDisarmRow": "Sr3ygG_undoDisarmRow",
			"undoDisarmText": "Sr3ygG_undoDisarmText",
			"value": "Sr3ygG_value",
			"visuallyHidden": "Sr3ygG_visuallyHidden",
			"waitingNotice": "Sr3ygG_waitingNotice",
			"workspace": "Sr3ygG_workspace"
		};
		//#endregion
		//#region src/client/board/use-flip.ts
		/**
		* FLIP for card movement — structure-driven, never rect-driven.
		*
		* The original rect-diff approach treated ANY geometry change (a column
		* scroll, a hover lift, the dragging scale) as a move, so a busy board
		* repainted cards into their old positions — the "cards vanish / ghost
		* copies / endless flicker" symptoms. This version compares STRUCTURE only:
		* a card's column and its index inside it, read from the DOM order, never
		* from rects. A card animates only when its structure changed (a reorder or
		* a column move); a pure scroll or hover can never trigger it. The animation
		* runs on the Web Animations API (the browser owns the lifecycle — no
		* transitionend dependency, no inline-transform residue, auto-cancel on
		* unmount), and it is fully suppressed while the user is dragging: the
		* post-drop settle is the one moment it plays.
		*/
		const FLIP_MS = 240;
		const FLIP_EASING = "cubic-bezier(0.2, 0.8, 0.2, 1)";
		/** Whether the page asked for reduced motion (decorative animation off). */
		function reducedMotion() {
			return typeof window !== "undefined" && window.matchMedia?.("(prefers-reduced-motion: reduce)").matches === true;
		}
		/**
		* The ids whose structure (column or in-column order) actually changed
		* between two snapshots — the ONLY cards eligible for a FLIP. A card absent
		* from the previous snapshot is new content (no old rect to animate FROM), a
		* card absent from both sides is ignored.
		*/
		function flipCandidatesOf(previous, current) {
			const candidates = [];
			for (const [id, now] of current) {
				const before = previous.get(id);
				if (before === void 0) continue;
				if (before.status !== now.status || before.index !== now.index) candidates.push(id);
			}
			return candidates;
		}
		/** Collect the structural snapshot of a board region (columns → cards in
		*  DOM order) plus each card's rect at the same instant. */
		function snapshotRegion(container) {
			const structures = /* @__PURE__ */ new Map();
			const rects = /* @__PURE__ */ new Map();
			for (const column of container.querySelectorAll("[data-status]")) {
				const status = column.getAttribute("data-status") ?? "";
				let index = 0;
				for (const card of column.querySelectorAll("[data-task-id]")) {
					const id = card.getAttribute("data-task-id");
					if (id !== null) {
						structures.set(id, {
							status,
							index
						});
						rects.set(id, card.getBoundingClientRect());
					}
					index += 1;
				}
			}
			return {
				structures,
				rects
			};
		}
		/**
		* Animate the cards whose structure changed into their new positions.
		*
		* Compact horizontal track: a cross-column move on a phone lands OFF-SCREEN
		* (the target column is not the one under the eye), so the animation —
		* though it plays — is never seen: 「移动没有动效」. Before playing, the
		* moved card's TARGET column is brought into view (instant scroll: a smooth
		* one would fight the rects the animation is computed from). Desktop (no
		* horizontal scroll) never touches it.
		*/
		function useFlipRegion(containerRef, disabled) {
			const previousStructures = (0, react.useRef)(/* @__PURE__ */ new Map());
			const previousRects = (0, react.useRef)(/* @__PURE__ */ new Map());
			(0, react.useLayoutEffect)(() => {
				const container = containerRef.current;
				if (container === null) return;
				const { structures, rects } = snapshotRegion(container);
				if (!disabled && !reducedMotion()) {
					const candidates = flipCandidatesOf(previousStructures.current, structures);
					if (candidates.length > 0) {
						const track = container.querySelector("[data-dsh-tb-columns]");
						const scrollsSideways = track !== null && track.scrollWidth > track.clientWidth + 1;
						if (track !== null && scrollsSideways) {
							const targetColumn = container.querySelector(`[data-task-id="${candidates[0]}"]`)?.closest("section[data-status]");
							if (targetColumn !== null && targetColumn !== void 0) targetColumn.scrollIntoView({
								behavior: "auto",
								inline: "center",
								block: "nearest"
							});
						}
						for (const id of candidates) {
							const before = previousRects.current.get(id);
							if (before === void 0) continue;
							const after = rects.get(id);
							const element = container.querySelector(`[data-task-id="${id}"]`);
							if (element === null || after === void 0) continue;
							const live = element.getBoundingClientRect();
							const dx = before.left - live.left;
							const dy = before.top - live.top;
							if (dx === 0 && dy === 0) continue;
							element.animate([{ transform: `translate(${dx}px, ${dy}px)` }, { transform: "translate(0, 0)" }], {
								duration: FLIP_MS,
								easing: FLIP_EASING
							});
						}
					}
				}
				previousStructures.current = structures;
				previousRects.current = rects;
			});
		}
		//#endregion
		//#region src/client/board/use-narrow.ts
		/**
		* useSurfaceNarrow — the sanctioned signal for BEHAVIOR/STRUCTURE switches
		* (React cannot branch on a CSS container query).
		*
		* Geometry breakpoints live in CSS container queries (surface-relative); where
		* the DOM SHAPE or a default state must differ — the cruise settings ride a
		* compact anchored popover on a wide board but must become a full Dialog on a
		* phone; a review panel's config head starts collapsed on a phone and open on
		* a desktop (the comments fold starts open at every width) — this hook is
		* the switch.
		*
		* `useSurfaceNarrow` measures THE SURFACE itself (the floating panel the CSS
		* is querying), so JS and CSS can never disagree.
		*
		* @deprecated `useNarrow` (viewport proxy) is frozen: a mid-size window with
		* the shell sidebar open has a viewport that says "wide" while the board or
		* panel is already stacked, and a default that follows the viewport then
		* disagrees with the geometry the user is looking at. All shape switches
		* must use `useSurfaceNarrow`; the viewport-proxy export was removed once
		* zero importers remained — this note stays so future callers pick the
		* surface signal instead of re-adding a viewport one.
		*/
		/**
		* Is the surface identified by `selector` (the nearest ancestor matching it,
		* counted from `from`) narrower than `maxPx`? `from` may be a ref or a CSS
		* class token used as a selector. Falls back to the viewport proxy until the
		* first measurement lands (one frame), so a default never flashes the wrong
		* shape on mount.
		*/
		function useSurfaceNarrow(selector, maxPx, viewportFallback = 720) {
			const ref = (0, react.useRef)(null);
			const [narrow, setNarrow] = (0, react.useState)(() => globalThis.matchMedia?.(`(max-width: ${viewportFallback}px)`).matches === true);
			(0, react.useEffect)(() => {
				const surface = ref.current?.closest(selector) ?? null;
				if (surface === null) return;
				const measure = () => {
					setNarrow(surface.getBoundingClientRect().width <= maxPx);
				};
				measure();
				if (typeof ResizeObserver === "undefined") return;
				const observer = new ResizeObserver(measure);
				observer.observe(surface);
				return () => {
					observer.disconnect();
				};
			}, [selector, maxPx]);
			return [narrow, ref];
		}
		//#endregion
		//#region src/client/board/column-tabs.ts
		/**
		* Column-tabs math — the compact board's navigator strip. On a phone the
		* five status columns share a horizontal scroll track; the tabs (dot + name
		* + count, the column header's own grammar) jump a column into view and keep
		* the active tab honest with the track's scroll position. The position→tab
		* rule is pure so it is unit-testable without a DOM.
		*/
		/** The column index whose left edge sits nearest the scroll position (a
		*  column scrolled fully past the left edge still counts once it is the
		*  closest one to being centered — the first index wins exact ties). */
		function activeColumnIndexAt(scrollLeft, lefts) {
			let best = 0;
			let bestGap = Number.POSITIVE_INFINITY;
			for (let index = 0; index < lefts.length; index += 1) {
				const gap = Math.abs(lefts[index] - scrollLeft);
				if (gap < bestGap) {
					bestGap = gap;
					best = index;
				}
			}
			return best;
		}
		/**
		* The scroll offset that puts column `index` at the track's left edge — the
		* inverse of `activeColumnIndexAt`, clamped into the scrollable range. Tab
		* jumps, resize re-anchoring and the FLIP cross-column scroll all aim with
		* THIS one function, so the track's persistent state is the COLUMN IDENTITY
		* and the pixel value is always derived. (Scroll-snap stored pixels and
		* re-anchored to them on every container resize — that is what made the
		* board "shift a little" each time the shell sidebar opened or closed.)
		*/
		function scrollLeftForColumn(index, lefts, clientWidth, scrollWidth) {
			if (lefts.length === 0) return 0;
			const clamped = Math.min(Math.max(index, 0), lefts.length - 1);
			const max = Math.max(0, scrollWidth - clientWidth);
			return Math.min(Math.max(lefts[clamped] ?? 0, 0), max);
		}
		//#endregion
		//#region src/client/board/Chip.tsx
		/** One badge. */
		function Chip({ kind = "neutral", fill = true, title, label, className, icon, children }) {
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
				className: `${board_module_css_default.chip}${fill ? ` ${board_module_css_default.chipFill}` : ""}${className !== void 0 ? ` ${className}` : ""}`,
				"data-kind": kind,
				title,
				...(label ?? title) !== void 0 ? { "aria-label": label ?? title } : {},
				children: [icon !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
					className: board_module_css_default.chipLead,
					children: icon
				}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
					className: board_module_css_default.chipBody,
					children
				})]
			});
		}
		//#endregion
		//#region src/client/board/ui.tsx
		/**
		* Shared UI primitives for the board: Button, Section, Notice,
		* Icon. One source for button variants, section titles, the waiting notice
		* and the icon set — every surface consumes these instead of
		* hand-rolled markup, so the whole board speaks one design language and
		* follows the native --dsw-* tokens (light/dark + any skin plugin) without
		* any per-surface styling drift.
		*/
		/** The board's one button (see module doc). The click handler receives the
		*  native event so callers inside clickable rows can stopPropagation; plain
		*  zero-arg handlers stay assignable (fewer parameters are always valid).
		*  `size="sm"` is the quiet row/header variant (view-session, refresh, row
		*  actions): one compact size for every secondary in-list affordance, so the
		*  board never mixes a full-size button into a row. */
		function Button({ variant = "ghost", size, type = "button", className, disabled, pressed, onClick, title, children }) {
			return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
				type,
				className: `${variant === "primary" ? board_module_css_default.primaryButton : variant === "danger" ? board_module_css_default.dangerButton : variant === "dangerGhost" ? board_module_css_default.dangerGhostButton : board_module_css_default.ghostButton}${size === "sm" ? ` ${board_module_css_default.buttonSm}` : ""}${pressed === true ? ` ${board_module_css_default.buttonPressed}` : ""}${className !== void 0 ? ` ${className}` : ""}`,
				disabled,
				"aria-pressed": pressed === true ? "true" : void 0,
				onClick,
				title,
				children
			});
		}
		/**
		* An iOS-style toggle switch (pure CSS): a hidden checkbox driving a track +
		* knob. "On" fills the track with the success tone and slides the knob right;
		* keyboard focus draws a soft ring around the track. Every on/off control on
		* the board (cruise, schedule enable) renders through this component.
		*/
		function Switch({ checked, onChange, label, title, disabled }) {
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("label", {
				className: board_module_css_default.switch,
				title,
				children: [
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)("input", {
						type: "checkbox",
						checked,
						disabled,
						onChange: (event) => {
							onChange(event.target.checked);
						}
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.switchTrack,
						"aria-hidden": "true",
						children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", { className: board_module_css_default.switchKnob })
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.switchLabel,
						children: label
					})
				]
			});
		}
		/** A titled detail section: one shared title style for every detail module.
		*  `action` is an optional right-aligned affordance on the title row (the
		*  section's own "+ 新建" style button) — the head is a flex row, the title
		*  never shrinks, the action hugs the right edge. An EMPTY title suppresses
		*  the head entirely (an embedding surface supplies its own header — the
		*  automation editor's embedded use, where the wrapping disclosure IS the
		*  section). */
		function Section({ title, action, children, className }) {
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("section", {
				className: `${board_module_css_default.detailSection}${className !== void 0 ? ` ${className}` : ""}`,
				children: [(title !== "" || action !== void 0) && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
					className: board_module_css_default.sectionHead,
					children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("h4", { children: title }), action !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.sectionAction,
						children: action
					})]
				}), children]
			});
		}
		/** A compact collapsible block: a chevron + title + one-line live summary
		*  on the header row; the body renders only when expanded, and the summary
		*  always reflects the current state (single-source with the body). THE one
		*  disclosure grammar for every foldable module on the board (run config, the
		*  automation editor, the session rail head): collapsed it is quiet, one row,
		*  no buttons, and the chevron turns (collapsed = right, expanded = down) —
		*  never a second hand-rolled fold that forgets the turn. */
		function Disclosure({ title, summary, open, onToggle, children }) {
			const regionId = (0, react.useId)();
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("section", {
				className: board_module_css_default.detailSection,
				children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("button", {
					type: "button",
					className: board_module_css_default.detailDisclosure,
					"aria-expanded": open,
					"aria-controls": regionId,
					onClick: onToggle,
					children: [
						/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, {
							name: "chevronDown",
							className: board_module_css_default.detailChevron
						}),
						/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
							className: board_module_css_default.detailDisclosureTitle,
							children: title
						}),
						summary !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
							className: board_module_css_default.detailDisclosureSummary,
							children: summary
						})
					]
				}), open && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
					id: regionId,
					children
				})]
			});
		}
		/** The waiting notice: one neutral framed row + warn chip for every surface. */
		function Notice({ chip, children }) {
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
				className: board_module_css_default.waitingNotice,
				role: "status",
				children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Chip, {
					kind: "warn",
					fill: false,
					children: chip
				}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", { children })]
			});
		}
		const ICON_PATHS = {
			arrowDown: "M3 6.5 8 11.5 13 6.5",
			chevronDown: "M3 6 8 11 13 6",
			arrowUp: "M3 9.5 8 4.5 13 9.5",
			close: "M4 4 12 12M12 4 4 12",
			arrowRight: "M4 8h8M9 4l4 4-4 4",
			arrowLeft: "M12 8H4M7 4l-4 4 4 4",
			link: "M6.4 9.6 9.6 6.4M6 10l-1.8 1.8a2.1 2.1 0 0 1-3-3L3.7 6.2a2.1 2.1 0 0 1 3 0M10 6l1.8-1.8a2.1 2.1 0 0 1 3 3L12.3 9.8a2.1 2.1 0 0 1-3 0",
			play: "M5.5 3.5 12 8l-6.5 4.5z",
			bell: "M8 2.5a4 4 0 0 1 4 4c0 2.8 1 3.8 1.5 4.5h-11C3 10.5 4 9.5 4 6.5a4 4 0 0 1 4-4zM6.5 13.5a1.5 1.5 0 0 0 3 0",
			pause: "M5.5 4v8M10.5 4v8",
			calendar: "M3.5 6.5v6a1 1 0 0 0 1 1h7a1 1 0 0 0 1-1v-6zM3.5 6.5h9M5.5 3.5v3M10.5 3.5v3",
			copy: "M7 4h5a2 2 0 0 1 2 2v5M5 6h5a2 2 0 0 1 2 2v5a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2z",
			check: "M3.5 8.5 6.5 11.5 12.5 5",
			checklist: "M2.5 4h.5M2.5 8h.5M2.5 12h.5M5.5 4h8M5.5 8h8M5.5 12h8",
			eyeOff: "M2.5 8s2.2-3.2 5.5-3.2S13.5 8 13.5 8s-2.2 3.2-5.5 3.2S2.5 8 2.5 8zM2.5 13.5 13.5 2.5",
			pencil: "M2.5 13.5l1-3L11 3l2 2-7.5 7.5-3 1zM9.5 4.5l2 2"
		};
		function Icon({ name, className }) {
			return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("svg", {
				className: board_module_css_default.icon + (className !== void 0 ? ` ${className}` : ""),
				viewBox: "0 0 16 16",
				width: "12",
				height: "12",
				fill: "none",
				stroke: "currentColor",
				strokeWidth: "1.6",
				strokeLinecap: "round",
				strokeLinejoin: "round",
				"aria-hidden": "true",
				children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("path", { d: ICON_PATHS[name] })
			});
		}
		/**
		* THE one color-picker row of the whole board: preset palette dots + the
		* native custom-color dot + the trailing 「移除颜色」 dot. Effect colors are
		* DATA (applied inline from user choices), so the same row is safe wherever
		* a data color is chosen — the card hover bar and the board's organize bar
		* share exactly this grammar. Every entry is round and same-sized; the
		* selected ring appears only when `value` truly equals the entry — never a
		* guessed default, never a mixed shape.
		*/
		function ColorSwatches({ value, onChange, none = true, custom = true }) {
			const isCustom = value !== void 0 && !PALETTE.includes(value);
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
				className: board_module_css_default.tagSwatches,
				children: [
					PALETTE.map((color) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
						type: "button",
						className: `${board_module_css_default.tagSwatch}${value === color ? ` ${board_module_css_default.tagSwatchOn}` : ""}`,
						style: { background: color },
						"aria-label": color,
						title: color,
						onClick: () => {
							onChange(color);
						}
					}, color)),
					custom && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: `${board_module_css_default.tagCustomWrap}${isCustom ? ` ${board_module_css_default.tagSwatchOn}` : ` ${board_module_css_default.tagCustomEmpty}`}`,
						style: isCustom ? { background: value } : void 0,
						title: t("color.custom"),
						children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("input", {
							type: "color",
							className: board_module_css_default.tagCustomColor,
							value: value ?? "#ffffff",
							"aria-label": t("color.custom"),
							onChange: (event) => {
								onChange(event.target.value);
							}
						})
					}),
					none && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
						type: "button",
						className: `${board_module_css_default.tagSwatch} ${board_module_css_default.tagSwatchNone}${value === void 0 ? ` ${board_module_css_default.tagSwatchOn}` : ""}`,
						"aria-label": t("card.colorNone"),
						title: t("card.colorNone"),
						onClick: () => {
							onChange(void 0);
						}
					})
				]
			});
		}
		/**
		* The ONE segmented-control grammar (radiogroup of labeled chips): driving
		* mode (按时间表/完成后接续), rule trigger (按时间表/任务完成后) and the
		* send mode all read/speak alike — one component, one style, zero drift.
		*/
		function Segmented({ options, value, onChange, ariaLabel }) {
			return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
				className: board_module_css_default.segmentedRow,
				role: "radiogroup",
				"aria-label": ariaLabel,
				children: options.map((option) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
					type: "button",
					role: "radio",
					"aria-checked": value === option.value,
					className: `${board_module_css_default.segmentedButton}${value === option.value ? ` ${board_module_css_default.segmentedActive}` : ""}`,
					title: option.title,
					onClick: () => {
						onChange(option.value);
					},
					children: option.label
				}, option.value))
			});
		}
		/**
		* The composer's send-mode switch: 排队 (queue — the dispatcher injects the
		* message, default) vs 插话 (steer — deliver straight to the session now,
		* bypassing queue/budget/cruise). One switch at the send row, shared by the
		* review page and the session panel so both speak one grammar.
		*/
		function SendModeToggle({ steer, onChange }) {
			return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Segmented, {
				ariaLabel: t("review.sendMode"),
				options: [{
					value: "queue",
					label: t("review.sendQueue"),
					title: t("review.sendQueueTitle")
				}, {
					value: "steer",
					label: t("review.sendSteer"),
					title: t("review.sendSteerTitle")
				}],
				value: steer ? "steer" : "queue",
				onChange: (next) => {
					onChange(next === "steer");
				}
			});
		}
		//#endregion
		//#region src/client/board/escape-stack.ts
		/**
		* The board's ONE Escape grammar for its overlay family.
		*
		* Overlays legitimately stack (a confirm dialog over the preset manager over
		* the new-task modal; the review page over the task detail). A per-overlay
		* `document` keydown listener can never express "only the top one closes":
		* `stopPropagation` does not stop ANOTHER listener on the SAME node, so one
		* Escape used to pop the whole stack at once. This module keeps a single
		* LIFO stack of open overlays and routes ONE shared keydown listener to the
		* TOP overlay only — every overlay registers through this hook and the family
		* gets correct, drift-free Escape behavior from exactly one place.
		*/
		/** The live stack (bottom → top). Module-level: one per page, like the DOM. */
		const stack = [];
		let installed = false;
		function onKey(event) {
			if (event.key !== "Escape") return;
			const top = stack[stack.length - 1];
			if (top === void 0) return;
			event.stopPropagation();
			event.preventDefault();
			top.close();
		}
		/**
		* Register an overlay's close callback on the Escape stack for as long as it
		* is mounted. Safe with inline closures: the entry is refreshed on every
		* render, so the shared listener always calls the latest callback without
		* re-shuffling the stack order.
		*/
		function useEscapeStack(onClose) {
			const entryRef = (0, react.useRef)();
			if (entryRef.current === void 0) entryRef.current = { close: onClose };
			entryRef.current.close = onClose;
			(0, react.useEffect)(() => {
				const entry = entryRef.current;
				stack.push(entry);
				if (!installed) {
					document.addEventListener("keydown", onKey);
					installed = true;
				}
				return () => {
					const index = stack.lastIndexOf(entry);
					if (index >= 0) stack.splice(index, 1);
					if (stack.length === 0 && installed) {
						document.removeEventListener("keydown", onKey);
						installed = false;
					}
				};
			}, []);
		}
		//#endregion
		//#region src/client/board/dialog-focus.ts
		/**
		* Dialog focus loop (THE one implementation — Dialog, TaskDetail and
		* SessionFrame all read this; no overlay manages focus on its own):
		* opening moves focus to the first control (or the panel itself when there
		* is none), Tab cycles inside the panel (never leaks to the board behind),
		* and closing returns focus to whatever held it. Escape stays out of here —
		* the shared Escape stack (escape-stack.ts) owns keys, this owns focus.
		*/
		/** Tabbable AND visible controls inside a root (keyboard order = DOM order).
		*  Hidden inputs (`type=hidden`), aria-hidden subtrees and display:none
		*  members never join the loop — focusing an invisible step reads as a
		*  lost focus. One predicate, three surfaces (never per-surface filters). */
		function focusablesOf(root) {
			return [...root.querySelectorAll("button, [href], input, select, textarea, [tabindex]:not([tabindex=\"-1\"])")].filter((element) => {
				if (element.hasAttribute("disabled")) return false;
				if (element instanceof HTMLInputElement && element.type === "hidden") return false;
				if (element.closest("[aria-hidden=\"true\"]") !== null) return false;
				const style = element instanceof HTMLElement ? getComputedStyle(element) : void 0;
				return style === void 0 || style.display !== "none" && style.visibility !== "hidden";
			});
		}
		/** Move focus to the declared/first control of a panel (or the panel itself
		*  when control-less). A declared target joins only through the visible set
		*  (a hidden/disabled declaration falls back — declarations get no exemption
		*  from visibility). Shared by the mount effect and the refocus effect. */
		function focusFirst(panel) {
			if (panel === null) return;
			const visible = focusablesOf(panel);
			const declared = panel.querySelector("[data-autofocus]");
			((declared !== null && visible.includes(declared) ? declared : void 0) ?? visible[0] ?? panel).focus();
		}
		/** Own the focus loop for one panel: mount-focus + unmount-return + Tab trap.
		*  The caller spreads the returned `onKeyDown` onto the panel element. The
		*  mount target is the first `[data-autofocus]` descendant when one exists
		*  (the caller's declared intent — e.g. a filter box over a master switch),
		*  else the first tabbable control, else the panel itself. Native `autoFocus`
		*  is banned for MOUNT-time focus inside Dialog subtrees (it races this
		*  effect — declare intent with `data-autofocus` instead); late-mounted
		*  inline editors (row rename, goal edit) keep native `autoFocus`, which fires
		*  after this effect ran and therefore never competes with it.
		*
		*  `focusKey` re-aims focus WITHOUT touching the return chain: pass a value
		*  that flips when the panel's first control changes identity (e.g. the
		*  detail's edit mode swapping the Edit button for a form). The return chain
		*  stays mount-scoped — only the mount effect captures `previous`.
		*
		*  Return falls back down a chain: the opener when alive, else the first
		*  tabbable control of the board box (a deleted trigger must not strand
		*  keyboard users on body). */
		function useDialogFocus(ref, focusKey) {
			(0, react.useEffect)(() => {
				const previous = document.activeElement;
				focusFirst(ref.current);
				return () => {
					if (previous instanceof HTMLElement && document.contains(previous)) {
						previous.focus({ preventScroll: true });
						return;
					}
					const box = document.querySelector("[data-dsh-taskboard-view]");
					if (box !== null) focusablesOf(box)[0]?.focus({ preventScroll: true });
				};
			}, []);
			(0, react.useEffect)(() => {
				focusFirst(ref.current);
			}, [focusKey]);
			return { onKeyDown: (event) => {
				if (event.key !== "Tab") return;
				const panel = event.currentTarget;
				const items = focusablesOf(panel);
				if (items.length === 0) {
					event.preventDefault();
					panel.focus();
					return;
				}
				const first = items[0];
				const last = items[items.length - 1];
				if (event.shiftKey && document.activeElement === first) {
					event.preventDefault();
					last.focus();
				} else if (!event.shiftKey && document.activeElement === last) {
					event.preventDefault();
					first.focus();
				}
			} };
		}
		//#endregion
		//#region src/client/board/Dialog.tsx
		/**
		* Dialog: the shared modal skeleton — backdrop, centered panel, optional
		* header with a close button. Every overlay (new-task, preset manager,
		* confirmations) rides the same backdrop and panel styling, so the board's
		* dialogs share one look and one motion and can never drift apart. The
		* review page keeps its custom header (badge + session jump) but uses the
		* same backdrop/panel rules via its own classes.
		*
		* PORTAL IS THE DEFAULT: every board dialog anchors to the board box
		* (`[data-dsh-taskboard-view]`) — the one layer whose geometry is the honest
		* board-box reference. A dialog left in its DOM position instead anchors its
		* backdrop to the nearest positioned ancestor (a board header, an open panel)
		* and is clipped by its `overflow:hidden` — the "new-task run-config is dead /
		* size-limited / cut off" regression. A caller opts OUT only with an explicit
		* `portal={false}` when it is already a top-level board overlay (none today);
		* forgetting `portal` can no longer reintroduce the bug.
		*
		* ESC closes the dialog — through the SHARED Escape stack (escape-stack.ts),
		* so a nested overlay (a confirm over a manager) closes ONE layer per press,
		* never the whole stack at once. TaskDetail and SessionFrame register through
		* the same hook; the whole family shares one grammar.
		*
		* FOCUS is a closed loop here too: opening moves focus to the first control
		* (or the panel itself when there is none), Tab cycles inside the panel
		* (never leaks to the board behind), and closing returns focus to whatever
		* held it — the trigger the user came from. Every caller inherits this; no
		* dialog manages focus on its own.
		*/
		/** The board box: the anchor every board dialog's backdrop covers. Exported
		*  for overlay shells that portal directly (SessionFrame). */
		function boardBox() {
			return document.querySelector("[data-dsh-taskboard-view]") ?? document.body;
		}
		/** One centered modal panel (see module doc). */
		function Dialog({ title, label, onClose, className, children, portal = true }) {
			useEscapeStack(onClose);
			const panelRef = (0, react.useRef)(null);
			const { onKeyDown } = useDialogFocus(panelRef);
			const titleId = (0, react.useId)();
			const panel = /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
				className: board_module_css_default.modalBackdrop,
				onMouseDown: (event) => {
					if (event.target === event.currentTarget) onClose();
				},
				children: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
					ref: panelRef,
					tabIndex: -1,
					className: `${board_module_css_default.modal}${className !== void 0 ? ` ${className}` : ""}`,
					role: "dialog",
					"aria-modal": "true",
					"aria-label": label,
					"aria-labelledby": title !== void 0 ? titleId : void 0,
					onKeyDown,
					children: [title !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("header", {
						className: board_module_css_default.dialogHeader,
						children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("h2", {
							id: titleId,
							className: board_module_css_default.modalTitle,
							children: title
						}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
							type: "button",
							className: board_module_css_default.iconButton,
							"aria-label": t("detail.close"),
							onClick: onClose,
							children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, { name: "close" })
						})]
					}), children]
				})
			});
			if (!portal) return panel;
			return (0, react_dom.createPortal)(panel, boardBox());
		}
		//#endregion
		//#region src/client/board/drop-position.ts
		/**
		* The indicator bar's Y in the scroll container's CONTENT coordinate space:
		* the viewport gap top minus the container's viewport top PLUS the container's
		* scroll offset — without the scroll term the bar drifts upward by exactly
		* the scrolled amount, and drops land at a visually different gap than the
		* bar promised. Clamped to the content's top and bottom so the bar is never
		* clipped away from its gap.
		*/
		function indicatorTopOf(viewportTop, containerTop, scrollTop, contentHeight) {
			const top = viewportTop - containerTop + scrollTop;
			return Math.min(Math.max(top, 0), contentHeight);
		}
		/**
		* Compute the insertion gap for a drag at `dropY` over a column's cards (in
		* visual order, excluding the dragged card itself): the nearest gap center
		* to the pointer wins — a gap center is `gap / 2` above each card and
		* `gap / 2` below the last card (the column tail). `top` is the gap center
		* in the same coordinate space as the rects, so the indicator is always
		* drawn exactly where the drop lands.
		*/
		function insertionGapOf(cards, dropY, dragId, gap) {
			const visible = cards.filter((card) => card.id !== dragId);
			let best;
			const consider = (candidate) => {
				const distance = Math.abs(dropY - candidate.top);
				if (best === void 0 || distance < Math.abs(dropY - best.top)) best = candidate;
			};
			for (const card of visible) consider({
				beforeId: card.id,
				top: card.rect.top - gap / 2
			});
			if (visible.length > 0) {
				const last = visible[visible.length - 1];
				consider({
					beforeId: void 0,
					top: last.rect.top + last.rect.height + gap / 2
				});
			}
			return best ?? {
				beforeId: void 0,
				top: 0
			};
		}
		/**
		* The scroll step for a pointer at `pointerY` over a root spanning
		* [rectTop, rectBottom]: 0 outside the edge zones; positive (scroll down)
		* near the bottom, negative (scroll up) near the top, magnitude linear in
		* proximity (1..maxStep).
		*/
		function edgeScrollStep(pointerY, rectTop, rectBottom, edgePx = 48, maxStep = 14) {
			if (pointerY < rectTop || pointerY > rectBottom || edgePx <= 0) return 0;
			const fromTop = pointerY - rectTop;
			const fromBottom = rectBottom - pointerY;
			const step = (distance) => Math.max(1, Math.ceil(maxStep * (1 - distance / edgePx)));
			if (fromBottom < edgePx) return step(fromBottom);
			if (fromTop < edgePx) return -step(fromTop);
			return 0;
		}
		/**
		* The edge-scroll loop for one drag surface. `getRoot` resolves the scroll
		* container under the pointer (stable closure over a ref); `active` switches
		* the loop on for the surface's drag kind. Every frame while the pointer sits
		* inside the root: `onFrame(lastPointerY)` only when a step was actually
		* applied (the gap/indicator follow the scroll).
		*/
		function useDragAutoScroll(getRoot, active, onFrame) {
			const pointerRef = (0, react.useRef)(0);
			const frameRef = (0, react.useRef)(onFrame);
			frameRef.current = onFrame;
			(0, react.useEffect)(() => {
				if (!active) return;
				let raf = 0;
				let scrolling = false;
				const onDragOver = (event) => {
					pointerRef.current = event.clientY;
				};
				const stop = () => {
					pointerRef.current = NaN;
				};
				window.addEventListener("dragover", onDragOver, { passive: true });
				window.addEventListener("drop", stop);
				window.addEventListener("dragend", stop);
				const release = (root) => {
					if (scrolling) {
						scrolling = false;
						root.style.scrollBehavior = "";
					}
				};
				const tick = () => {
					const root = getRoot();
					if (root !== null) {
						const rect = root.getBoundingClientRect();
						const y = pointerRef.current;
						if (y >= rect.top && y <= rect.bottom) {
							const step = edgeScrollStep(y, rect.top, rect.bottom);
							if (step !== 0) {
								if (!scrolling) {
									scrolling = true;
									root.style.scrollBehavior = "auto";
								}
								root.scrollTop += step;
								frameRef.current?.(y);
							} else release(root);
						} else release(root);
					}
					raf = requestAnimationFrame(tick);
				};
				raf = requestAnimationFrame(tick);
				return () => {
					cancelAnimationFrame(raf);
					window.removeEventListener("dragover", onDragOver);
					window.removeEventListener("drop", stop);
					window.removeEventListener("dragend", stop);
				};
			}, [active, getRoot]);
		}
		//#endregion
		//#region src/client/board/format-time.ts
		/**
		* THE time-formatting module: every clock/label grammar of the board lives
		* here, never in a component. Two families:
		* - exact/local labels (`formatDateTime`, `formatDuration`) and the compact
		*   relative label (`formatTime`) used by cards, details and threads;
		* - compact cruise-time formatting (`formatCruiseTime`): window rows and the
		*   popover status line need both endpoints readable at a glance inside a
		*   ~300px popover — the full `YYYY-MM-DD HH:mm:ss` is too long next to the
		*   arrow and the remove button, so cruise surfaces use the short form —
		*   today collapses to the clock, any other day to month/day, cross-year
		*   adds the year. The exact value is always reachable through the
		*   element's `title`. Locale-aware (zh numeric-locale style vs en M/D),
		*   framework-free so it unit-tests in isolation.
		*/
		const pad = (value) => String(value).padStart(2, "0");
		/** Compact relative/absolute time label: just now → `Nm` → `Nh` → `Y-M-D`. */
		function formatTime(ms) {
			const date = new Date(ms);
			const minutes = Math.floor((Date.now() - ms) / 6e4);
			if (minutes < 1) return t("time.justNow");
			if (minutes < 60) return `${minutes}m`;
			if (minutes < 1440) return `${Math.floor(minutes / 60)}h`;
			return `${date.getFullYear()}-${String(date.getMonth() + 1).padStart(2, "0")}-${String(date.getDate()).padStart(2, "0")}`;
		}
		/** Exact local time label: `YYYY-MM-DD HH:mm:ss`. */
		function formatDateTime(ms) {
			const date = new Date(ms);
			return `${date.getFullYear()}-${pad(date.getMonth() + 1)}-${pad(date.getDate())} ${pad(date.getHours())}:${pad(date.getMinutes())}:${pad(date.getSeconds())}`;
		}
		/** Human duration label (zh: `X 分 Y 秒`; en: `Xm Ys`). */
		function formatDuration(ms) {
			const totalSeconds = Math.max(0, Math.round(ms / 1e3));
			const hours = Math.floor(totalSeconds / 3600);
			const minutes = Math.floor(totalSeconds % 3600 / 60);
			const seconds = totalSeconds % 60;
			if (hours > 0) return isEnglish() ? `${hours}h ${minutes}m` : `${hours} 小时 ${minutes} 分`;
			if (minutes > 0) return isEnglish() ? seconds > 0 ? `${minutes}m ${seconds}s` : `${minutes}m` : seconds > 0 ? `${minutes} 分 ${seconds} 秒` : `${minutes} 分`;
			return isEnglish() ? `${seconds}s` : `${seconds} 秒`;
		}
		/** Compact cruise time label: `HH:mm` when same-day, `M月D日 HH:mm` /
		*  `M/D HH:mm` when same-year, full date otherwise. */
		function formatCruiseTime(ms, now = Date.now()) {
			const date = new Date(ms);
			const base = new Date(now);
			const clock = `${pad(date.getHours())}:${pad(date.getMinutes())}`;
			if (date.getFullYear() === base.getFullYear() && date.getMonth() === base.getMonth() && date.getDate() === base.getDate()) return clock;
			if (date.getFullYear() === base.getFullYear()) return isEnglish() ? `${date.getMonth() + 1}/${date.getDate()} ${clock}` : `${date.getMonth() + 1}月${date.getDate()}日 ${clock}`;
			return isEnglish() ? `${date.getFullYear()}/${date.getMonth() + 1}/${date.getDate()} ${clock}` : `${date.getFullYear()}年${date.getMonth() + 1}月${date.getDate()}日 ${clock}`;
		}
		/** Whether `b` lies on the calendar day IMMEDIATELY AFTER `a` (a's 日历日 + 1,
		*  month/year rollovers included) — the ONE rule the "次日" display word
		*  obeys: a cross-midnight night (22:00 → 02:00, normalized +1 day) and an
		*  explicitly filled next-morning end both read 次日, while an end two days
		*  or a month later shows its REAL date (the "结束晚 33 天也显示次日" bug).
		*  Ends at or before the start are never "next day". */
		function isNextDay(a, b) {
			const from = new Date(a);
			const to = new Date(b);
			if (to.getTime() <= from.getTime()) return false;
			const previous = new Date(to);
			previous.setDate(previous.getDate() - 1);
			return previous.getFullYear() === from.getFullYear() && previous.getMonth() === from.getMonth() && previous.getDate() === from.getDate();
		}
		/** ONE line of copy per cruise window — the list's single grammar:
		*  both set → "{start} → {end}" (a cross-midnight end reads 次日/next day);
		*  only start → "{time} 起保持开启" / "From {time}, stays on";
		*  only end → "已开启 · 至 {time}" / "On now · until {time}" (added = live);
		*  a LIVE window (covering now) leads with "生效中 ·" / "active ·".
		*  Pure (locale + a fixed `now`) so the exact strings are testable in both
		*  languages; the full instants stay in the element's `title`. */
		function cruiseWindowLabelOf(window, now = Date.now()) {
			const grammar = cruiseWindowGrammarOf(window);
			const prefix = grammar.kind !== "until-end" && isWindowActive(window, now) ? `${t("board.cruiseWindowActive")} · ` : "";
			if (grammar.kind === "range") {
				const end = isNextDay(grammar.startAt, grammar.endAt) ? `${t("board.cruiseNextDay")} ${formatCruiseTime(grammar.endAt, grammar.endAt)}` : formatCruiseTime(grammar.endAt, grammar.startAt);
				return `${prefix}${t("board.cruiseWindowRange", {
					start: formatCruiseTime(grammar.startAt, now),
					end
				})}`;
			}
			if (grammar.kind === "from-start") return `${prefix}${t("board.cruiseWindowFromStart", { time: formatCruiseTime(grammar.startAt, now) })}`;
			return t("board.cruiseWindowOnlyEnd", { time: formatCruiseTime(grammar.endAt, now) });
		}
		//#endregion
		//#region src/core/board-events.ts
		/**
		* Collect every moment of every task, newest first (no cap — callers slice).
		* Running rounds ARE moments (started/observed/injected) — the old activity
		* feed skipped them, which hid "who is working now" from history. Empty
		* comment bodies are kept as moments (the row shows a placeholder) except
		* when the caller filters them — the derivation never drops facts.
		*/
		function boardEventsOf(tasks, ctx = {}) {
			const events = [];
			function waitingAt(task, sessionId) {
				const round = task.executions.find((candidate) => candidate.sessionId === sessionId);
				return round !== void 0 ? round.endedAt ?? round.startedAt : task.updatedAt;
			}
			for (const task of tasks) {
				const baseline = ctx.viewedBaselineOf?.(task);
				const isUnviewed = (at) => baseline === void 0 ? void 0 : at > baseline;
				events.push({
					key: `${task.id}|created`,
					taskId: task.id,
					taskTitle: task.title,
					kind: "created",
					state: "settled",
					at: task.createdAt,
					unviewed: isUnviewed(task.createdAt)
				});
				for (const round of task.executions) {
					const atOf = (fallback) => fallback;
					if (round.direct === true) {
						events.push({
							key: `${task.id}|${round.id}|direct`,
							taskId: task.id,
							taskTitle: task.title,
							kind: "direct",
							state: "settled",
							at: round.endedAt ?? round.startedAt,
							result: round.result,
							...round.comment !== void 0 && round.comment.trim() !== "" ? { text: round.comment } : {},
							...round.sessionId !== void 0 ? { sessionId: round.sessionId } : {},
							unviewed: isUnviewed(atOf(round.endedAt ?? round.startedAt))
						});
						continue;
					}
					if (round.refine === true) {
						const open = round.endedAt === void 0;
						events.push({
							key: `${task.id}|${round.id}|refined`,
							taskId: task.id,
							taskTitle: task.title,
							kind: "refined",
							state: open ? "running" : "settled",
							at: open ? round.startedAt : round.endedAt ?? round.startedAt,
							...!open && round.result !== void 0 ? { result: round.result } : {},
							...round.sessionId !== void 0 ? { sessionId: round.sessionId } : {},
							unviewed: isUnviewed(open ? round.startedAt : round.endedAt ?? round.startedAt)
						});
						continue;
					}
					if (round.external === true) {
						const open = isOpenRound(round);
						const at = open ? round.startedAt : round.endedAt ?? round.startedAt;
						events.push({
							key: `${task.id}|${round.id}|external`,
							taskId: task.id,
							taskTitle: task.title,
							kind: "external",
							state: open ? "running" : "settled",
							at,
							...!open && round.result !== void 0 ? { result: round.result } : {},
							...round.comment !== void 0 && round.comment.trim() !== "" ? { text: round.comment } : {},
							...round.sessionId !== void 0 ? { sessionId: round.sessionId } : {},
							...round.ruleId !== void 0 ? { ruleId: round.ruleId } : {},
							unviewed: isUnviewed(at)
						});
						continue;
					}
					if (round.comment !== void 0) {
						const queued = round.injectedAt === void 0 && round.endedAt === void 0;
						const running = !queued && round.endedAt === void 0;
						const state = queued ? "queued" : running ? "running" : "settled";
						const at = state === "queued" ? round.startedAt : state === "running" ? round.injectedAt ?? round.startedAt : round.endedAt ?? round.startedAt;
						events.push({
							key: `${task.id}|${round.id}|comment`,
							taskId: task.id,
							taskTitle: task.title,
							kind: "comment",
							state,
							at,
							...state === "settled" && round.result !== void 0 ? { result: round.result } : {},
							...round.comment.trim() !== "" ? { text: round.comment } : {},
							...round.sessionId !== void 0 ? { sessionId: round.sessionId } : {},
							...round.ruleId !== void 0 ? { ruleId: round.ruleId } : {},
							unviewed: isUnviewed(at)
						});
						continue;
					}
					const open = isOpenRound(round);
					const at = open ? round.startedAt : round.endedAt ?? round.startedAt;
					events.push({
						key: `${task.id}|${round.id}|run`,
						taskId: task.id,
						taskTitle: task.title,
						kind: "run",
						state: open ? "running" : "settled",
						at,
						...!open && round.result !== void 0 ? { result: round.result } : {},
						...round.sessionId !== void 0 ? { sessionId: round.sessionId } : {},
						unviewed: isUnviewed(at)
					});
				}
				if (ctx.pendingOf !== void 0) {
					const seen = /* @__PURE__ */ new Set();
					const pushWaiting = (sessionId) => {
						if (sessionId === void 0 || seen.has(sessionId)) return;
						const waitingKind = ctx.pendingOf?.(sessionId);
						if (waitingKind === void 0) return;
						seen.add(sessionId);
						events.push({
							key: `${task.id}|waiting|${sessionId}`,
							taskId: task.id,
							taskTitle: task.title,
							kind: "waiting",
							state: "running",
							at: waitingAt(task, sessionId),
							sessionId,
							waitingKind,
							unviewed: true
						});
					};
					for (const { sessionId } of relatedSessionIdsOf(task, ctx.linkedIdsOf?.(task))) pushWaiting(sessionId);
				}
			}
			return events.sort((a, b) => b.at - a.at);
		}
		/** Day bucket key (local calendar day) for activity grouping — pure. */
		function dayBucketOf(at) {
			const date = new Date(at);
			return `${date.getFullYear()}-${`${date.getMonth() + 1}`.padStart(2, "0")}-${`${date.getDate()}`.padStart(2, "0")}`;
		}
		//#endregion
		//#region src/client/board/ConfirmDialog.tsx
		/**
		* Generic confirm dialog used by destructive actions (task delete). Rides
		* the shared Dialog skeleton.
		*/
		/** Small confirm overlay. AlWAYS portaled: a confirmation is an overlay on
		*  an overlay — it must escape the enclosing dialog's box (see Dialog). */
		function ConfirmDialog({ title, message, confirmLabel, danger, onCancel, onConfirm }) {
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(Dialog, {
				title,
				label: title,
				onClose: onCancel,
				portal: true,
				children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
					className: board_module_css_default.modalScroll,
					children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
						className: board_module_css_default.confirmMessage,
						children: message
					})
				}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("footer", {
					className: board_module_css_default.modalFooter,
					children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
						onClick: onCancel,
						children: t("delete.cancel")
					}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
						variant: danger ? "danger" : "primary",
						onClick: onConfirm,
						children: confirmLabel
					})]
				})]
			});
		}
		//#endregion
		//#region src/client/board/AttachmentStrip.tsx
		/**
		* Composer attachment strip: the file-drag/pick entrance of every composer
		* that attaches files (review page, session panel, refinement, task prompt).
		* It is PRESENTATION + the picker affordance; the ledger state, intake
		* (image encode/compress + file staging) and the drop/paste wiring live in
		* the shared `useComposerImages` hook, which the composer spreads onto its
		* container so a drop anywhere on the composer (not just this strip) works.
		* A plus button opens the picker (any file kind — images route to the image
		* lane, everything else to the staged-file lane); encoded images and staged
		* files show as removable chips; a busy row names the in-flight work and a
		* rejection line names the failed file — nothing is ever silently dropped.
		* One grammar everywhere.
		*/
		/**
		* The busy line for an in-flight intake, named by WHAT is in flight (never
		* the settled ledger — a staging file is not in the ledger yet). undefined
		* kind falls back to the image line (the only intake a file-lane-less
		* surface can start).
		*/
		function attachBusyLabel(kind) {
			if (kind === "file") return t("review.attachFileBusy");
			if (kind === "mixed") return t("review.attachWorking");
			return t("review.attachBusy");
		}
		/** Format a byte count compactly (B/KB/MB, one decimal under 100). */
		function formatBytes(bytes) {
			if (bytes < 1024) return `${bytes} B`;
			if (bytes < 1024 * 1024) {
				const v = bytes / 1024;
				return `${v >= 100 ? String(Math.round(v)) : String(Math.round(v * 10) / 10)} KB`;
			}
			const v = bytes / (1024 * 1024);
			return `${v >= 100 ? String(Math.round(v)) : String(Math.round(v * 10) / 10)} MB`;
		}
		function AttachmentStrip({ images, files, onAdd, onRemoveImage, onRemoveFile, busy, busyLabel, error }) {
			const fileRef = (0, react.useRef)(null);
			const staged = files ?? [];
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
				className: board_module_css_default.attachStrip,
				children: [
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
						type: "button",
						className: board_module_css_default.attachAdd,
						"aria-label": t("review.attachFile"),
						title: t("review.attachFile"),
						onClick: () => {
							fileRef.current?.click();
						},
						children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, { name: "link" })
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)("input", {
						ref: fileRef,
						type: "file",
						multiple: true,
						hidden: true,
						onChange: (event) => {
							if (event.currentTarget.files !== null) onAdd(event.currentTarget.files);
							event.currentTarget.value = "";
						}
					}),
					images.map((image) => /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
						className: board_module_css_default.attachChip,
						title: image.name,
						children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
							className: board_module_css_default.attachChipName,
							children: image.name
						}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
							type: "button",
							className: board_module_css_default.attachRemove,
							"aria-label": image.name,
							onClick: () => {
								onRemoveImage(image.id);
							},
							children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, { name: "close" })
						})]
					}, image.id)),
					staged.map((file) => /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
						className: board_module_css_default.attachChip,
						title: `${file.name} · ${formatBytes(file.bytes)}`,
						children: [
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
								className: board_module_css_default.attachChipName,
								children: file.name
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
								className: board_module_css_default.attachChipMeta,
								children: formatBytes(file.bytes)
							}),
							onRemoveFile !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
								type: "button",
								className: board_module_css_default.attachRemove,
								"aria-label": file.name,
								onClick: () => {
									onRemoveFile(file.id);
								},
								children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, { name: "close" })
							})
						]
					}, file.id)),
					busy === true && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.attachHint,
						children: busyLabel ?? t("review.attachBusy")
					}),
					error !== void 0 && error !== "" && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.attachError,
						children: error
					})
				]
			});
		}
		//#endregion
		//#region src/client/board/attach.ts
		/** The official temporary-bytes part for one draft image. */
		function toPromptImage(image) {
			return {
				mediaType: image.mediaType,
				data: image.data,
				name: image.name
			};
		}
		/** A staged file as the controller's PromptFile (receipt + name + bytes). */
		function toPromptFile(file) {
			return {
				receiptId: file.receiptId,
				name: file.name,
				bytes: file.bytes
			};
		}
		/** The comment composer: generous — the wire carries it once, per send. */
		const COMMENT_IMAGE_BUDGET = {
			maxEdge: 1568,
			maxBytes: 4 * 1024 * 1024,
			quality: .82
		};
		/** Task-prompt images PERSIST in the shared board document (every device
		*  syncs them), so the budget is deliberately tight: a few hundred KB each,
		*  a handful per task. */
		const TASK_IMAGE_BUDGET = {
			maxEdge: 1280,
			maxBytes: 512 * 1024,
			quality: .78
		};
		/**
		* PURE decision for one candidate file (unit-tested): the intake route.
		* Images route to the canvas lane; EVERYTHING else routes to the file lane
		* (upload-then-receipt) — a non-image is never a "type rejection" anymore.
		* A pre-compression image may exceed maxBytes and still be fine (the
		* re-encode shrinks it), so size only rejects a PASSTHROUGH (gif) or the
		* final encoded result — never a re-encode candidate up front.
		*/
		function intakeDecision(type, size, budget) {
			if (!type.startsWith("image/")) return { kind: "file" };
			if (type === "image/gif") return size <= budget.maxBytes ? {
				kind: "passthrough",
				mediaType: "image/gif"
			} : {
				kind: "reject",
				reason: "size"
			};
			return { kind: "reencode" };
		}
		/**
		* PURE: turn an encoded data URL into the wire shape, enforcing the budget
		* and the native whitelist on the RESULT (a canvas in an exotic browser can
		* hand back a type the host would reject — that is a decode failure, said
		* out loud, never a silent drop).
		*/
		let sequence = 0;
		function finishEncoded(dataUrl, name, budget) {
			const match = /^data:([^;,]+)[^,]*,(.*)$/s.exec(dataUrl);
			const mediaType = match?.[1] ?? "";
			const data = match?.[2] ?? "";
			if (mediaType === "" || data === "" || !IMAGE_MEDIA_TYPES.includes(mediaType)) return {
				ok: false,
				reason: "decode",
				name
			};
			if (Math.ceil(data.length * 3 / 4) > budget.maxBytes) return {
				ok: false,
				reason: "size",
				name
			};
			sequence += 1;
			return {
				ok: true,
				image: {
					id: `img-${Date.now()}-${sequence}`,
					data,
					mediaType,
					name
				}
			};
		}
		/** Scale a length so the longest edge is at most `maxEdge`. */
		function scaleToFit(width, height, maxEdge) {
			const longest = Math.max(width, height);
			if (longest <= maxEdge) return {
				w: width,
				h: height
			};
			const ratio = maxEdge / longest;
			return {
				w: Math.max(1, Math.round(width * ratio)),
				h: Math.max(1, Math.round(height * ratio))
			};
		}
		/** Read a File as a data URL (browser API; resolves '' on error). */
		function readAsDataUrl(file) {
			return new Promise((resolve) => {
				const reader = new FileReader();
				reader.onload = () => resolve(typeof reader.result === "string" ? reader.result : "");
				reader.onerror = () => resolve("");
				reader.readAsDataURL(file);
			});
		}
		/** Decode an image File into a drawable source; undefined when undecodable. */
		async function decodeImage(file) {
			try {
				if (typeof createImageBitmap === "function") {
					const bitmap = await createImageBitmap(file);
					return {
						source: bitmap,
						width: bitmap.width,
						height: bitmap.height
					};
				}
			} catch {}
			try {
				const url = URL.createObjectURL(file);
				try {
					const image = await new Promise((resolve) => {
						const element = new Image();
						element.onload = () => {
							resolve(element);
						};
						element.onerror = () => {
							resolve(void 0);
						};
						element.src = url;
					});
					if (image === void 0) return void 0;
					return {
						source: image,
						width: image.naturalWidth,
						height: image.naturalHeight
					};
				} finally {
					URL.revokeObjectURL(url);
				}
			} catch {
				return;
			}
		}
		/**
		* Encode one image File into the wire form under `budget` (browser-only
		* middle: decode → canvas downscale → re-encode; the decision and the result
		* gate are the pure functions above). NEVER throws; every failure is an
		* IntakeOutcome with a reason the composer shows.
		*/
		async function encodeImageFile(file, budget = COMMENT_IMAGE_BUDGET) {
			const decision = intakeDecision(file.type, file.size, budget);
			if (decision.kind === "reject") return {
				ok: false,
				reason: decision.reason,
				name: file.name
			};
			if (decision.kind === "passthrough") return finishEncoded(await readAsDataUrl(file), file.name, budget);
			const decoded = await decodeImage(file);
			if (decoded === void 0 || decoded.width === 0 || decoded.height === 0) return {
				ok: false,
				reason: "decode",
				name: file.name
			};
			const { w, h } = scaleToFit(decoded.width, decoded.height, budget.maxEdge);
			const canvas = document.createElement("canvas");
			canvas.width = w;
			canvas.height = h;
			const context = canvas.getContext("2d");
			if (context === null) return {
				ok: false,
				reason: "decode",
				name: file.name
			};
			context.fillStyle = "white";
			context.fillRect(0, 0, w, h);
			context.imageSmoothingQuality = "high";
			context.drawImage(decoded.source, 0, 0, w, h);
			let dataUrl = canvas.toDataURL("image/webp", budget.quality);
			let outcome = finishEncoded(dataUrl, file.name, budget);
			if (!outcome.ok && outcome.reason === "size") {
				dataUrl = canvas.toDataURL("image/jpeg", Math.max(.5, budget.quality - .15));
				outcome = finishEncoded(dataUrl, file.name, budget);
			}
			return outcome;
		}
		//#endregion
		//#region src/client/board/composer-images.ts
		/**
		* The composer's attachment ledger — ONE mechanism for every surface that
		* can attach files (the comment composer, the refinement answer, the task's
		* execution prompt): images + staged files, intake from pick / drop / paste
		* + a busy count + the LAST rejection said out loud. A file that cannot be
		* staged is NEVER silently dropped (the old 「电脑端发不了图」 was silent
		* rejection); every rejection names its file and its reason.
		*
		* Two lanes, one ledger: IMAGES encode to temporary bytes in the browser
		* (see attach.ts); FILES upload their exact bytes first (same session) and
		* ride the prompt as opaque receipts. The host performs the durable
		* admission when it takes the prompt — this hook only produces the official
		* wire shapes.
		*/
		/** Image-lane or file-lane for one picked file (pure routing, no I/O). */
		function intakeDecisionOf(file) {
			return intakeDecision(file.type, file.size, {
				maxEdge: 0,
				maxBytes: Number.POSITIVE_INFINITY,
				quality: 1
			}).kind === "file" ? "file" : "image";
		}
		/** Combine per-lane in-flight counts into one label kind (pure, tested). */
		function busyKindOf(images, files) {
			if (images > 0 && files > 0) return "mixed";
			if (files > 0) return "file";
			if (images > 0) return "image";
		}
		/**
		* Encode a composer draft for the string draft store (pure, tested). Empty
		* drafts encode to '' (the store treats it as a clear).
		*/
		function encodeCommentDraft(text, images, files) {
			const kept = [];
			let chars = 0;
			let imagesDropped = false;
			for (const image of images) {
				if (chars + image.data.length > 25e5) {
					imagesDropped = true;
					continue;
				}
				chars += image.data.length;
				kept.push({
					name: image.name,
					data: image.data,
					mediaType: image.mediaType
				});
			}
			if (text === "" && kept.length === 0 && files.length === 0) return {
				value: "",
				imagesDropped
			};
			return {
				value: JSON.stringify({
					v: 1,
					text,
					images: kept,
					fileNames: files.map((file) => file.name)
				}),
				imagesDropped
			};
		}
		/**
		* Decode a stored comment draft (pure, tested): the v1 envelope, legacy raw
		* text (pre-attachment drafts), or corrupt → empty. Chip ids regenerate (a
		* restored chip is a new chip, never a key collision across sessions).
		*/
		function decodeCommentDraft(raw) {
			const empty = {
				text: "",
				images: [],
				fileNames: []
			};
			if (raw === void 0 || raw === "") return empty;
			try {
				const parsed = JSON.parse(raw);
				if (typeof parsed !== "object" || parsed === null) return {
					...empty,
					text: raw
				};
				const record = parsed;
				if (record.v !== 1) return {
					...empty,
					text: raw
				};
				const text = typeof record.text === "string" ? record.text : "";
				const images = [];
				if (Array.isArray(record.images)) record.images.forEach((row, index) => {
					if (typeof row !== "object" || row === null) return;
					const candidate = row;
					if (typeof candidate.data !== "string" || candidate.data === "") return;
					images.push({
						id: `restored-${index}-${candidate.data.slice(0, 8)}`,
						name: typeof candidate.name === "string" ? candidate.name : "",
						data: candidate.data,
						mediaType: candidate.mediaType === "image/png" || candidate.mediaType === "image/jpeg" || candidate.mediaType === "image/webp" || candidate.mediaType === "image/gif" ? candidate.mediaType : "image/jpeg"
					});
				});
				return {
					text,
					images,
					fileNames: Array.isArray(record.fileNames) ? record.fileNames.filter((name) => typeof name === "string" && name !== "") : []
				};
			} catch {
				return {
					...empty,
					text: raw
				};
			}
		}
		/** Whether a picked batch contains any non-image (a re-add clears the lost-file notice). */
		function pickedHasFiles(files) {
			return Array.from(files).some((file) => !file.type.startsWith("image/"));
		}
		/** One human line for one rejection reason (locale-owned). */
		function rejectMessage(reason, name, max) {
			switch (reason) {
				case "type": return t("attach.rejectType", { name });
				case "size": return t("attach.rejectSize", { name });
				case "decode": return t("attach.rejectDecode", { name });
				case "count": return t("attach.rejectCount", {
					name,
					max: String(max ?? 0)
				});
			}
		}
		function useComposerImages(budget, maxImages, controlled, filesOpts) {
			const [own, setOwn] = (0, react.useState)([]);
			const images = controlled !== void 0 ? controlled.images : own;
			const [ownFiles, setOwnFiles] = (0, react.useState)([]);
			const filesLedger = ownFiles;
			const [busyCount, setBusyCount] = (0, react.useState)(0);
			const [error, setError] = (0, react.useState)(void 0);
			const [dragOver, setDragOver] = (0, react.useState)(false);
			const busyLanesRef = (0, react.useRef)({
				image: 0,
				file: 0
			});
			const [busyKind, setBusyKind] = (0, react.useState)(void 0);
			const syncBusyKind = (0, react.useCallback)(() => {
				const lanes = busyLanesRef.current;
				setBusyKind(busyKindOf(lanes.image, lanes.file));
			}, []);
			const imagesRef = (0, react.useRef)(images);
			imagesRef.current = images;
			const filesRef = (0, react.useRef)(filesLedger);
			filesRef.current = filesLedger;
			const setImages = (0, react.useCallback)((next) => {
				imagesRef.current = next;
				if (controlled !== void 0) controlled.onChange(next);
				else setOwn(next);
				setError(void 0);
			}, [controlled]);
			const setFiles = (0, react.useCallback)((next) => {
				filesRef.current = next;
				setOwnFiles(next);
				setError(void 0);
			}, []);
			const maxFiles = filesOpts?.maxFiles ?? 5;
			const addFiles = (0, react.useCallback)(async (picked) => {
				const list = Array.from(picked);
				if (list.length === 0) return;
				setError(void 0);
				setBusyCount((count) => count + 1);
				try {
					const next = [...imagesRef.current];
					const nextFiles = [...filesRef.current];
					for (const file of list) {
						const lane = intakeDecisionOf(file);
						busyLanesRef.current[lane] += 1;
						syncBusyKind();
						try {
							if (lane === "image") {
								if (next.length >= maxImages) {
									setError(rejectMessage("count", file.name, maxImages));
									break;
								}
								const outcome = await encodeImageFile(file, budget);
								if (outcome.ok) next.push(outcome.image);
								else setError(rejectMessage(outcome.reason, file.name, maxImages));
								continue;
							}
							if (filesOpts === void 0 || filesOpts.sessionId === void 0) {
								setError(t("attach.rejectNoSession", { name: file.name }));
								continue;
							}
							if (nextFiles.length >= maxFiles) {
								setError(t("attach.rejectFileCount", {
									name: file.name,
									max: String(maxFiles)
								}));
								break;
							}
							const staged = await filesOpts.stage(filesOpts.sessionId, file);
							if (!staged.ok) {
								setError(t("attach.rejectUpload", {
									name: file.name,
									error: staged.error
								}));
								continue;
							}
							nextFiles.push({
								id: `file-${Date.now()}-${nextFiles.length}`,
								receiptId: staged.receiptId,
								name: file.name,
								bytes: file.size
							});
						} finally {
							busyLanesRef.current[lane] -= 1;
							syncBusyKind();
						}
					}
					imagesRef.current = next;
					filesRef.current = nextFiles;
					if (controlled !== void 0) controlled.onChange(next);
					else setOwn(next);
					setOwnFiles(nextFiles);
				} finally {
					setBusyCount((count) => count - 1);
				}
			}, [
				budget,
				maxImages,
				controlled,
				filesOpts,
				maxFiles,
				syncBusyKind
			]);
			return {
				images,
				files: filesLedger,
				setImages,
				setFiles,
				addFiles,
				busy: busyCount > 0,
				busyKind,
				error,
				dropProps: {
					"data-dsh-tb-dndover": dragOver ? "" : void 0,
					onDragOver: (event) => {
						if (Array.from(event.dataTransfer.types).includes("Files")) {
							event.preventDefault();
							setDragOver(true);
						}
					},
					onDragLeave: (event) => {
						if (!event.currentTarget.contains(event.relatedTarget)) setDragOver(false);
					},
					onDrop: (event) => {
						setDragOver(false);
						if (event.dataTransfer.files.length > 0) {
							event.preventDefault();
							addFiles(event.dataTransfer.files);
						}
					},
					onPaste: (event) => {
						const files = Array.from(event.clipboardData.files);
						if (files.length > 0) {
							event.preventDefault();
							addFiles(files);
						}
					}
				}
			};
		}
		//#endregion
		//#region src/client/board/file-reference-grammar.ts
		/**
		* Extract an `@path` or `@"path with spaces` token at the cursor. An `@`
		* inside another token, such as an email address, is not a completion trigger.
		* @param line - current editor line.
		* @param cursorCol - cursor column within that line.
		* @returns the active token, or `undefined` outside an `@` token.
		*/
		function activeAtToken(line, cursorCol) {
			const beforeCursor = line.slice(0, cursorCol);
			const quoted = /(?:^|\s)(@"([^"]*))$/u.exec(beforeCursor);
			if (quoted?.[1] !== void 0 && quoted[2] !== void 0) return {
				prefix: quoted[1],
				query: quoted[2],
				quoted: true
			};
			const plain = /(?:^|\s)(@([^\s]*))$/u.exec(beforeCursor);
			if (plain?.[1] === void 0 || plain[2] === void 0) return void 0;
			return {
				prefix: plain[1],
				query: plain[2],
				quoted: false
			};
		}
		/**
		* Format a selected path as prompt text. Whitespace uses the quoted
		* `@"path"` grammar; a quoted directory keeps that quote open after its
		* trailing slash so completion can descend another level.
		* @param candidate - selected file or directory.
		* @param preserveQuote - retain an explicitly opened quote even when unnecessary.
		* @returns the insertion value, or `undefined` for a path the editor grammar cannot represent safely.
		*/
		function formatFileMention(candidate, preserveQuote) {
			const path = candidate.kind === "directory" ? `${candidate.path}/` : candidate.path;
			if (/[\u0000-\u001f\u007f-\u009f"]/u.test(path)) return void 0;
			if (!(preserveQuote || /\s/u.test(path))) return `@${path}`;
			if (candidate.kind === "directory") return `@"${path}`;
			return `@"${path}"`;
		}
		//#endregion
		//#region src/client/board/slash-token.ts
		/**
		* Trigger autocomplete: pure text/token logic for the prompt input.
		* Framework-free so the token scan, candidate filtering and insertion rules
		* are unit-testable without React or the runtime. One tokenizer serves both
		* triggers — '/' (slash commands + skills) and '@' (files/sessions) — so the
		* two menus share the same token, filter and flip behaviour.
		*
		* The slash candidate model mirrors the native composer's '/' menu, which
		* merges two sources: host commands (the command registry) and skills (the
		* skill catalog — every skill name is itself a slash entry like `/skill-xxx`).
		*
		* The '@' token delegates to the OFFICIAL grammar
		* (`@deepseek-ai/dsh-file-reference/grammar`): `@"path with spaces` quoted
		* tokens, word-boundary rules and the same open-quote behaviour the harness's
		* own ui-reference input uses — the board never re-implements it.
		*/
		function isSeparator(char) {
			return char === " " || char === "	" || char === "\n" || char === "\r";
		}
		function tokenAt(text, caret, trigger) {
			if (caret < 0 || caret > text.length) return void 0;
			let start = caret;
			while (start > 0 && !isSeparator(text[start - 1])) start -= 1;
			if (start >= caret) return void 0;
			if (text[start] !== trigger) return void 0;
			let end = caret;
			while (end < text.length && !isSeparator(text[end])) end += 1;
			const leading = start === 0 || text[start - 1] === "\n" || text[start - 1] === "\r";
			return {
				start,
				end,
				query: text.slice(start + 1, caret),
				leading
			};
		}
		/**
		* Find the slash token at `caret`. Scans back to the nearest separator to
		* locate the word start, forward to the next separator for the word end;
		* when the word starts with '/', the caret is inside a slash token. The
		* filter query is the typed text between the '/' and the caret. Returns
		* undefined otherwise.
		*/
		function commandTokenAt(text, caret) {
			return tokenAt(text, caret, "/");
		}
		/**
		* Find the mention token at `caret` — the OFFICIAL `@` grammar from
		* `@deepseek-ai/dsh-file-reference/grammar`: an `@` must sit at a word
		* boundary, and an open quoted path (`@"…`) is one token that may span
		* whitespace. The span covers the whole trigger prefix (`@` or `@"`);
		* `quoted` rides the token so the reference bridge can suppress session
		* discovery inside quoted paths (the official source's rule).
		*/
		function mentionTokenAt(text, caret) {
			const at = activeAtToken(text, caret);
			if (at === void 0) return void 0;
			const start = caret - at.prefix.length;
			return {
				start,
				end: caret,
				query: at.query,
				leading: text.search(/\S/) === start,
				...at.quoted ? { quoted: true } : {}
			};
		}
		const byName = (a, b) => a.name.localeCompare(b.name);
		/**
		* Filter slash candidates against the query, mirroring the native composer:
		* commands match case-insensitively (prefix first, then includes) and only
		* appear at any position when they take no hint; skills match by
		* `startsWith` and appear at any position. Commands rank before skills;
		* each group stays sorted.
		*/
		function filterSlashCandidates(candidates, query, leading) {
			const needle = query.trim().toLowerCase();
			const commands = candidates.filter((candidate) => candidate.kind === "command").filter((candidate) => leading || candidate.hint === void 0).filter((candidate) => needle === "" || candidate.name.toLowerCase().includes(needle));
			const commandPrefix = commands.filter((candidate) => needle !== "" && candidate.name.toLowerCase().startsWith(needle)).sort(byName);
			const commandRest = commands.filter((candidate) => needle === "" || !candidate.name.toLowerCase().startsWith(needle)).sort(byName);
			const skills = candidates.filter((candidate) => candidate.kind === "skill").filter((candidate) => needle === "" || candidate.name.toLowerCase().startsWith(needle)).sort(byName);
			return [
				...commandPrefix,
				...commandRest,
				...skills
			];
		}
		/** The replacement text for a picked slash candidate, mirroring the native
		*  menu: commands complete to `/<name>`, skills and commands with an input
		*  hint add a trailing space so typing continues. */
		function commandCompletion(candidate) {
			const needsSpace = candidate.kind === "skill" || candidate.hint !== void 0;
			return `/${candidate.name}${needsSpace ? " " : ""}`;
		}
		/**
		* Replace the whole token word with the picked candidate's completion.
		* @returns the new text and the caret position after insertion.
		*/
		function insertCommand(text, token, candidate) {
			const completion = commandCompletion(candidate);
			return {
				text: `${text.slice(0, token.start)}${completion}${text.slice(token.end)}`,
				caret: token.start + completion.length
			};
		}
		/**
		* Replace the whole '@' token span with an official mention splice (a
		* `@path` / `@"path"` file mention or a canonical `@[label](dsh-session:…)`
		* session mention). Pure span surgery shared by every board input.
		* @returns the new text and the caret position after insertion.
		*/
		function insertMention(text, token, insert) {
			return {
				text: `${text.slice(0, token.start)}${insert}${text.slice(token.end)}`,
				caret: token.start + insert.length
			};
		}
		/** THE '@' menu capability gate — same discipline as '/' (which needs the
		*  slash catalog): the reference menu opens only when a target session
		*  scopes the discovery AND the official reference bridge exists. A missing
		*  capability never shows an empty "no match" menu — it behaves as if the
		*  trigger did not exist at all. */
		function referenceMenuAvailable(sessionId, bridgePresent) {
			return sessionId !== void 0 && bridgePresent;
		}
		/** Whether picking a row should re-open the menu right after insertion.
		*  ONLY the official directory-descent splice (`@"路径/` keeps its quote
		*  open for the next level) continues; a plain file mention, a session
		*  mention and every slash completion all close the menu silently — the
		*  cursor sits inside a live token after their splice and would otherwise
		*  instantly re-open the very menu the user just used. */
		function continueAfterPick(row) {
			return row?.continue === true;
		}
		//#endregion
		//#region src/client/board/session-mention.ts
		/**
		* THE OFFICIAL session-reference mention encoding, mirrored verbatim from the
		* deployment's `@deepseek-ai/dsh-session-reference` (the client bundle purity
		* gate forbids cross-plugin value imports — the one precedent is
		* `file-reference-grammar.ts`).
		*
		* The host's pre-step parser (`parseSessionReferenceText`) recognizes ONLY
		* this exact shape — `@[label](dsh-session:<base64url(JSON.stringify(id))>)`
		* — so ANY row the board renders must emit it byte-for-byte; the host then
		* materializes the cited session's snapshot into the prompt exactly like a
		* native mention (no agent-lookup is involved in that parse). The board's
		* own catalog rows use this mirror; `tests/session-mention.spec.ts` pins the
		* strings against fixtures generated by the deployed module.
		*/
		const SESSION_REFERENCE_SCHEME = "dsh-session:";
		/** base64url of a UTF-8 value (the deployed module uses Node's Buffer; the
		*  browser half uses TextEncoder + btoa — the same bytes). */
		function base64urlUtf8(value) {
			const bytes = new TextEncoder().encode(value);
			let binary = "";
			for (const byte of bytes) binary += String.fromCharCode(byte);
			return btoa(binary).replace(/\+/g, "-").replace(/\//g, "_").replace(/=+$/, "");
		}
		/** Canonical `dsh-session:` URI for one opaque session id (lossless). */
		function encodeSessionReferenceUri(sessionId) {
			return `${SESSION_REFERENCE_SCHEME}${base64urlUtf8(JSON.stringify(sessionId))}`;
		}
		/** Escape the display label for the Markdown mention — the deployed grammar
		*  verbatim: `]` and `\` get a backslash prefix (`[` is deliberately NOT
		*  escaped — the host's parse pattern does not unescape it either). */
		function escapeSessionReferenceLabel(label) {
			return label.replace(/[\\\]]/gu, (match) => `\\${match}`);
		}
		/** Render a host-neutral Markdown mention carrying the canonical URI. */
		function formatSessionReferenceMention(reference) {
			return `@[${escapeSessionReferenceLabel(reference.label ?? reference.sessionId)}](${encodeSessionReferenceUri(reference.sessionId)})`;
		}
		//#endregion
		//#region src/client/board/reference-source.ts
		/**
		* THE ONE '@' reference bridge of the board — every input box (comments,
		* refine answers, rule instructions, run prompts, interaction answers)
		* reads this module, so the whole board shares the OFFICIAL mechanism the
		* harness's own composer uses (`@deepseek-ai/dsh-client-ui-reference`):
		*
		* - candidates: the same two Remote namespaces (`remote.fileReferences` and
		*   `remote.sessionReferenceResolver`) fetched in parallel — files AND
		*   sessions in one menu, exactly like the native composer;
		* - insertion text: the OFFICIAL grammars from
		*   `@deepseek-ai/dsh-file-reference/grammar` (`@path` / `@"path"` with
		*   directory-descent quotes) and the canonical `@[label](dsh-session:…)`
		*   mention the host pre-serializes on each session candidate;
		* - resolution: nothing else is needed — the host scans ANY user message at
		*   agent pre-step (`parseSessionReferenceText`) and materializes cited
		*   sessions / guides files, no matter which surface typed the text.
		*
		* Failure contract (the OFFICIAL guarantee, from the web file/session
		* reference design note): "either candidate domain can fail independently
		* without hiding the rows the other domain returned." This module therefore
		* NEVER rejects and NEVER couples the two halves: a missing face, an RPC
		* rejection, a host `{ok:false}` envelope and a malformed candidate row each
		* degrade only their own half / row. What happened is reported in `diag`,
		* so the menu can show it honestly instead of a bare "no match".
		*/
		/** Resolve ONE discovery half in isolation: a missing face, a rejected RPC
		*  and a host error envelope all degrade to `{ok:false}` — the other half
		*  and the caller never see a throw (the official per-domain guarantee). */
		async function domainOf(face, sessionId, query, signal) {
			if (face === void 0) return { ok: false };
			try {
				const result = await face(sessionId, query, signal);
				if (result.ok) return {
					ok: true,
					items: result.value
				};
				const error = result.error;
				const code = typeof error === "object" && error !== null && typeof error.code === "string" ? error.code : void 0;
				return code !== void 0 ? {
					ok: false,
					code
				} : { ok: false };
			} catch {
				return { ok: false };
			}
		}
		/** One file candidate rendered as a menu row (official grammar + copy). */
		function fileRow(candidate, preserveQuote) {
			const mention = formatFileMention(candidate, preserveQuote);
			if (mention === void 0) return [];
			const name = candidate.path.slice(candidate.path.lastIndexOf("/") + 1);
			const directory = candidate.kind === "directory";
			return [{
				section: "files",
				name: `${t(directory ? "ref.candidate.folder" : "ref.candidate.file")} · ${name}${directory ? "/" : ""}`,
				description: candidate.path,
				key: `file:${candidate.path}`,
				insert: mention,
				...directory ? { continue: true } : {}
			}];
		}
		/** The official description date: `createdAt` is Unix epoch milliseconds
		*  (the session-reference contract). A missing or malformed value simply
		*  omits the date — one bad candidate must never take the menu down. */
		function safeCreatedAt(value) {
			if (typeof value === "number" && Number.isFinite(value)) try {
				return new Date(value).toISOString();
			} catch {
				return;
			}
			if (typeof value === "string") {
				const time = Date.parse(value);
				if (Number.isFinite(time)) try {
					return new Date(time).toISOString();
				} catch {
					return;
				}
			}
		}
		/** One session candidate rendered as a menu row (the host pre-serialized
		*  canonical `@[label](dsh-session:…)` mention rides verbatim). */
		function sessionRow(candidate) {
			const location = candidate.cwd ?? t("ref.candidate.noCwd");
			const date = safeCreatedAt(candidate.createdAt);
			const description = `${candidate.label === candidate.sessionId ? "" : `${candidate.sessionId} · `}${location}${date !== void 0 ? ` · ${date}` : ""}`;
			return {
				section: "sessions",
				name: `${t("ref.candidate.session")} · ${candidate.label}`,
				description,
				key: `session:${candidate.sessionId}`,
				insert: candidate.mention
			};
		}
		/**
		* Fetch the official candidates for one '@' token. Files always run; session
		* discovery is suppressed inside an open quoted path (`@"…`), exactly like
		* the official source. The two halves resolve independently (the official
		* per-domain failure guarantee) and this function NEVER rejects.
		*/
		async function listReferenceRows(bridge, sessionId, query, quoted, signal) {
			if (bridge === void 0) return {
				rows: [],
				diag: {}
			};
			const [fileSide, sessionSide] = await Promise.all([domainOf(bridge.fileReferences !== void 0 ? bridge.fileReferences.list.bind(bridge.fileReferences) : void 0, sessionId, query, signal), quoted ? Promise.resolve({
				ok: true,
				items: []
			}) : domainOf(bridge.sessionReferenceResolver !== void 0 ? bridge.sessionReferenceResolver.candidates.bind(bridge.sessionReferenceResolver) : void 0, sessionId, query, signal)]);
			if (signal.aborted) return {
				rows: [],
				diag: {}
			};
			const diag = {};
			if (!fileSide.ok) diag.files = fileSide.code !== void 0 ? { code: fileSide.code } : {};
			if (!sessionSide.ok) diag.sessions = sessionSide.code !== void 0 ? { code: sessionSide.code } : {};
			const rows = [];
			let skipped = 0;
			if (fileSide.ok) for (const candidate of fileSide.items) try {
				rows.push(...fileRow(candidate, quoted));
			} catch {
				skipped += 1;
			}
			if (sessionSide.ok) for (const candidate of sessionSide.items) try {
				rows.push(sessionRow(candidate));
			} catch {
				skipped += 1;
			}
			if (skipped > 0) diag.skipped = skipped;
			return {
				rows,
				diag
			};
		}
		/**
		* The board-catalog session rows: the SAME ReferenceRow shape as the official
		* half, sourced from the board's own session catalog (id + latest title,
		* native list order). Used only when the host `candidates` half is
		* unavailable — the gateway refuses the agent lookup for subagent-routed
		* (agent-busy) target sessions, which fails the official discovery too.
		* Inserted text stays the OFFICIAL canonical mention
		* (`formatSessionReferenceMention`), so the host's pre-step parser resolves a
		* picked row exactly like a host candidate. Self is excluded (the official
		* rule); the cap mirrors the host's candidateLimit default.
		*/
		function catalogSessionRowsOf(catalog, targetSessionId, limit = 50) {
			return catalog.filter((entry) => entry.sessionId !== targetSessionId).slice(0, limit).map((entry) => ({
				section: "sessions",
				name: `${t("ref.candidate.session")} · ${entry.label}`,
				description: `${entry.label === entry.sessionId ? "" : `${entry.sessionId} · `}${t("ref.candidate.noCwd")}`,
				key: `session:${entry.sessionId}`,
				insert: formatSessionReferenceMention(entry)
			}));
		}
		//#endregion
		//#region src/client/board/menu-direction.ts
		/**
		* Slash-menu direction: decide whether the autocomplete menu must open
		* upward instead of downward. The menu is absolutely positioned below the
		* prompt field by default; inside a modal that has overflow hidden, a menu
		* that would extend past the clipping container's bottom edge gets cut off
		* (scrolling the menu itself cannot help — the ancestor clips it). The
		* review page's composer sits at the bottom of the modal, so this is the
		* normal case there. Pure and framework-free so the decision unit-tests in
		* isolation.
		*/
		/** The menu's maximum rendered height (the CSS max-height). */
		const SLASH_MENU_MAX_HEIGHT = 280;
		/** The gap between the field and the menu (the CSS offset). */
		const MENU_GAP = 4;
		/**
		* Whether the menu should open upward. True when there is not enough room
		* below the field within the clipping container (the menu would be cut off)
		* and there is more room above than below. Rectangles are in the same
		* coordinate space (e.g. both from getBoundingClientRect).
		*
		* @param fieldRect - the prompt field's rectangle.
		* @param clipRect - the nearest clipping ancestor's rectangle (the modal),
		*   or undefined when no clipping ancestor was found (falls back to
		*   viewport-level judgment via the field's own position).
		* @param viewportHeight - the viewport height (fallback coordinate space).
		* @param menuHeight - the menu's rendered height; defaults to the max.
		*/
		function shouldFlipMenuUp(fieldRect, clipRect, viewportHeight, menuHeight = SLASH_MENU_MAX_HEIGHT) {
			const spaceBelow = clipRect !== void 0 ? clipRect.bottom - fieldRect.bottom : viewportHeight - fieldRect.bottom;
			const spaceAbove = clipRect !== void 0 ? fieldRect.top - clipRect.top : fieldRect.top;
			if (spaceBelow >= menuHeight + MENU_GAP) return false;
			return spaceAbove > spaceBelow;
		}
		//#endregion
		//#region src/client/board/PromptInput.tsx
		/**
		* Prompt input with trigger autocomplete: typing '/' pops the live slash
		* candidates (host commands plus skills — the same merged catalog the native
		* composer's '/' menu reads), and typing '@' pops the OFFICIAL reference
		* candidates (workspace files AND sessions — the same two Remote namespaces
		* the harness's own ui-reference source calls, see reference-source.ts).
		* Both menus share one navigation (ArrowUp/Down, Enter/Tab, Escape,
		* click-away), the same insertion grammar and the same direction logic: the
		* menu opens downward by default and flips upward when the space below the
		* field within its clipping container is insufficient (the review page's
		* composer sits at the bottom of the modal, so this is the normal case
		* there). The textarea auto-grows with its content up to a cap and then
		* scrolls internally — the native composer's pattern (resize: none), which
		* eliminates the unreachable resize-handle trap of a bottom-pinned input.
		* Shared by the new-task modal, the detail edit mode, the review page's
		* composer, the session panel, the refine answer and the interaction card.
		*/
		/** Menu row cap: keeps the list scannable and scrollbar-free. */
		const MAX_ROWS = 8;
		/** Auto-grow cap: taller content scrolls inside the textarea instead of
		*  stretching the modal. */
		const TEXTAREA_MAX_HEIGHT = 160;
		/** Degraded-@-menu reasons already warned about (hot path: warn once each). */
		const warnedDegraded = /* @__PURE__ */ new Set();
		/** The nearest ancestor that clips overflow (the modal shell), if any. */
		function clippingAncestorOf(element) {
			let current = element.parentElement;
			while (current !== null) {
				const style = getComputedStyle(current);
				if (style.overflow !== "visible" || style.overflowX !== "visible" || style.overflowY !== "visible") return current;
				current = current.parentElement;
			}
		}
		/** Whether two '@' tokens describe the same span (query included). */
		function sameMentionSpan(a, b) {
			return a.start === b.start && a.end === b.end && a.query === b.query && (a.quoted ?? false) === (b.quoted ?? false);
		}
		/** Prompt textarea with a slash-command and official-reference dropdown. */
		function PromptInput({ value, onChange, placeholder, rows, controller, sessionId, invalid }) {
			const textareaRef = (0, react.useRef)(null);
			const fieldRef = (0, react.useRef)(null);
			const valueRef = (0, react.useRef)(value);
			const caretRef = (0, react.useRef)(0);
			const [catalog, setCatalog] = (0, react.useState)(void 0);
			const [menu, setMenu] = (0, react.useState)(void 0);
			const [mentionReq, setMentionReq] = (0, react.useState)(void 0);
			(0, react.useEffect)(() => {
				let alive = true;
				(async () => {
					const rows = await controller.runCatalog()?.listSlashCandidates();
					if (!alive) return;
					setCatalog(rows ?? null);
				})();
				return () => {
					alive = false;
				};
			}, [controller]);
			(0, react.useEffect)(() => {
				if (mentionReq === void 0 || sessionId === void 0) return;
				const controllerRef = new AbortController();
				let alive = true;
				listReferenceRows(controller.referenceSources(), sessionId, mentionReq.query, mentionReq.quoted === true, controllerRef.signal).then(({ rows, diag }) => {
					if (!alive || controllerRef.signal.aborted) return;
					let nextRows = rows;
					if (diag.sessions !== void 0 && rows.every((row) => row.section !== "sessions")) {
						const catalog = catalogSessionRowsOf(controller.referenceSessionCatalog(), sessionId);
						if (catalog.length > 0) nextRows = [...rows, ...catalog];
					}
					if (diag.files !== void 0 || diag.sessions !== void 0) {
						const reason = JSON.stringify(diag);
						if (!warnedDegraded.has(reason)) {
							warnedDegraded.add(reason);
							console.warn("[dsh-task-board] @ reference menu degraded:", reason);
						}
					}
					setMenu((previous) => previous !== void 0 && previous.kind === "mention" && sameMentionSpan(previous.token, mentionReq) ? {
						...previous,
						rows: nextRows,
						diag,
						pending: false
					} : previous);
				}).catch(() => {
					if (alive) console.warn("[dsh-task-board] @ reference fetch failed unexpectedly");
				});
				return () => {
					alive = false;
					controllerRef.abort();
				};
			}, [
				mentionReq,
				sessionId,
				controller
			]);
			(0, react.useEffect)(() => {
				const element = textareaRef.current;
				if (element === null) return;
				element.style.height = "auto";
				const next = Math.min(element.scrollHeight, TEXTAREA_MAX_HEIGHT);
				element.style.height = `${next}px`;
				element.style.overflowY = element.scrollHeight > TEXTAREA_MAX_HEIGHT ? "auto" : "hidden";
			}, [value]);
			/** Recompute the menu from the latest text/caret (no-op without a trigger token). */
			const syncMenu = () => {
				const text = valueRef.current;
				const caret = caretRef.current;
				const mentionToken = referenceMenuAvailable(sessionId, controller.referenceSources() !== void 0) ? mentionTokenAt(text, caret) : void 0;
				const slashToken = commandTokenAt(text, caret);
				let kind;
				let token;
				if (mentionToken !== void 0) {
					kind = "mention";
					token = mentionToken;
				} else if (slashToken !== void 0) {
					kind = "slash";
					token = slashToken;
				}
				if (token === void 0 || kind === void 0) {
					setMenu(void 0);
					setMentionReq(void 0);
					return;
				}
				let flip = false;
				const field = fieldRef.current;
				if (field !== null) {
					const fieldRect = field.getBoundingClientRect();
					const clip = clippingAncestorOf(field);
					flip = shouldFlipMenuUp(fieldRect, clip !== void 0 ? clip.getBoundingClientRect() : void 0, window.innerHeight);
				}
				if (kind === "mention") {
					setMenu((previous) => previous !== void 0 && previous.kind === "mention" && sameMentionSpan(previous.token, token) ? previous : {
						token,
						kind,
						rows: previous?.kind === "mention" ? previous.rows : [],
						diag: previous?.kind === "mention" ? previous.diag : {},
						pending: previous?.kind === "mention" ? previous.pending : true,
						highlight: 0,
						flip
					});
					setMentionReq(token);
					return;
				}
				if (catalog === void 0 || catalog === null) {
					setMenu(void 0);
					setMentionReq(void 0);
					return;
				}
				const rows = filterSlashCandidates(catalog, token.query, token.leading).slice(0, MAX_ROWS);
				setMenu((previous) => previous !== void 0 && previous.kind === kind && previous.token.start === token.start && previous.token.end === token.end && previous.token.query === token.query ? previous : {
					token,
					kind,
					rows,
					diag: {},
					pending: false,
					highlight: 0,
					flip
				});
			};
			(0, react.useEffect)(() => {
				if (catalog === void 0 || catalog === null) return;
				syncMenu();
			}, [catalog]);
			/** Insert the picked candidate over the token span and restore focus/caret. */
			const accept = (row) => {
				if (menu === void 0 || row === void 0) {
					setMenu(void 0);
					return;
				}
				const result = menu.kind === "mention" ? insertMention(valueRef.current, menu.token, row.insert) : insertCommand(valueRef.current, menu.token, row);
				setMenu(void 0);
				setMentionReq(void 0);
				valueRef.current = result.text;
				caretRef.current = result.caret;
				onChange(result.text);
				requestAnimationFrame(() => {
					const element = textareaRef.current;
					if (element === null) return;
					element.focus();
					element.setSelectionRange(result.caret, result.caret);
					if (menu.kind === "mention" && continueAfterPick(row)) syncMenu();
				});
			};
			const onKeyDown = (event) => {
				if (menu === void 0) return;
				if (event.key === "ArrowDown" || event.key === "ArrowUp") {
					event.preventDefault();
					const delta = event.key === "ArrowDown" ? 1 : -1;
					setMenu({
						...menu,
						highlight: menu.rows.length === 0 ? 0 : (menu.highlight + delta + menu.rows.length) % menu.rows.length
					});
					return;
				}
				if (event.key === "Enter" || event.key === "Tab") {
					event.preventDefault();
					accept(menu.rows[menu.highlight]);
					return;
				}
				if (event.key === "Escape") {
					event.preventDefault();
					setMenu(void 0);
				}
			};
			const onInput = (event) => {
				const element = event.currentTarget;
				caretRef.current = element.selectionStart;
				valueRef.current = element.value;
				onChange(element.value);
				syncMenu();
			};
			const onSelect = (event) => {
				const element = event.currentTarget;
				caretRef.current = element.selectionStart;
				syncMenu();
			};
			const onBlur = () => {
				setMenu(void 0);
				setMentionReq(void 0);
			};
			/** The '@' menu items: the official sections in order (files, then
			*  sessions). A half that failed shows its notice in its own section
			*  slot — the bare "no match" line appears only when both halves
			*  genuinely returned nothing. */
			const mentionMenu = (menu) => {
				const bySection = {
					files: [],
					sessions: []
				};
				for (const row of menu.rows) bySection[row.section].push(row);
				const items = [];
				let rowIndex = -1;
				for (const section of ["files", "sessions"]) {
					const sectionRows = bySection[section];
					if (sectionRows.length === 0) continue;
					items.push(/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
						className: board_module_css_default.slashMenuSection,
						children: t(section === "files" ? "ref.section.files" : "ref.section.sessions")
					}, `section:${section}`));
					if (sectionRows.length > 0) for (const mention of sectionRows) {
						rowIndex += 1;
						items.push(/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("button", {
							type: "button",
							role: "option",
							"aria-selected": rowIndex === menu.highlight,
							className: `${board_module_css_default.slashMenuRow}${rowIndex === menu.highlight ? ` ${board_module_css_default.slashMenuRowActive}` : ""}`,
							onMouseDown: (event) => {
								event.preventDefault();
							},
							onClick: () => {
								accept(mention);
							},
							children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
								className: board_module_css_default.slashMenuName,
								children: mention.name
							}), mention.description !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
								className: board_module_css_default.slashMenuDesc,
								children: mention.description
							})]
						}, mention.key));
					}
				}
				if (items.length === 0) items.push(/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
					className: board_module_css_default.slashMenuEmpty,
					role: "option",
					children: menu.pending ? t("prompt.loading") : t("prompt.noReferences")
				}));
				return items;
			};
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
				className: board_module_css_default.promptField,
				ref: fieldRef,
				children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("textarea", {
					ref: textareaRef,
					className: `${board_module_css_default.input} ${board_module_css_default.promptTextarea}${invalid === true ? ` ${board_module_css_default.inputInvalid}` : ""}`,
					rows,
					value,
					placeholder,
					spellCheck: false,
					"aria-label": t("new.prompt"),
					onKeyDown,
					onChange: onInput,
					onSelect,
					onBlur
				}), menu !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
					className: board_module_css_default.slashMenu,
					"data-direction": menu.flip ? "up" : "down",
					role: "listbox",
					"aria-label": t("prompt.commandList"),
					children: menu.kind === "mention" ? mentionMenu(menu) : menu.rows.length === 0 ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
						className: board_module_css_default.slashMenuEmpty,
						role: "option",
						children: t("prompt.noCommands")
					}) : menu.rows.map((row, index) => {
						const slash = row;
						return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("button", {
							type: "button",
							role: "option",
							"aria-selected": index === menu.highlight,
							className: `${board_module_css_default.slashMenuRow}${index === menu.highlight ? ` ${board_module_css_default.slashMenuRowActive}` : ""}`,
							onMouseDown: (event) => {
								event.preventDefault();
							},
							onClick: () => {
								accept(slash);
							},
							children: [
								/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
									className: board_module_css_default.slashMenuName,
									children: ["/", slash.name]
								}),
								/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
									className: board_module_css_default.slashMenuDesc,
									children: slash.description
								}),
								slash.hint !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
									className: board_module_css_default.slashMenuHint,
									children: slash.hint
								})
							]
						}, slash.name);
					})
				})]
			});
		}
		//#endregion
		//#region src/client/permission-label.ts
		/**
		* Permission-presentation mirror: render one permission preset under the same
		* product-label rule as the native client picker
		* (`@deepseek-ai/dsh-client-ui-permission-presets` presentation), so the task
		* form shows exactly what the GUI's permission selector shows. The option
		* data itself is never hard-coded here — it arrives from the host's native
		* permission service; only this tiny display rule is mirrored.
		*/
		/** Machine value of the preset that requires an explicit GUI risk gate. */
		const FULL_ACCESS_VALUE = "danger-full-access";
		/**
		* Convert conventional kebab-case preset names into user-facing title case
		* (mirrors the native `displayPresetName`).
		* @param name - host-supplied preset label or key.
		* @returns the title-cased conventional key, or a non-kebab label unchanged.
		*/
		function displayPresetName(name) {
			if (!/^[a-z0-9]+(-[a-z0-9]+)*$/.test(name)) return name;
			return name.split("-").map((word) => word.charAt(0).toUpperCase() + word.slice(1)).join(" ");
		}
		/**
		* Render a permission preset under its product label (mirrors the native
		* `displayPermissionPreset`).
		* @param value - preset machine value.
		* @param name - host-supplied preset label or key.
		* @returns the Full access product label or the conventional display name.
		*/
		function permissionLabel(value, name) {
			return value === FULL_ACCESS_VALUE ? "Full access" : displayPresetName(name !== void 0 && name !== "" ? name : value);
		}
		//#endregion
		//#region src/client/board/RunConfigEditor.tsx
		/**
		* THE one run-configuration field editor (工作区 / Agent / 模型 / 思考程度 /
		* 权限): the task form's 运行配置 block and the preset manager's add/edit
		* form both render THIS — one set of fields, one catalog-loading path, zero
		* drift between what a task configures and what a preset stores.
		* Self-contained: loads its catalogs from the controller's runCatalog face
		* (workspaces sync; agent presets / model groups / permissions async) and
		* degrades each option list exactly like the form always did.
		*/
		/** Model-select value encoding: provider + model, joined by a NUL separator. */
		const MODEL_SEP$1 = "\0";
		/** The run configuration field editor (see module doc). */
		function RunConfigEditor({ value, onChange, controller }) {
			const [presets, setPresets] = (0, react.useState)([]);
			const [groups, setGroups] = (0, react.useState)([]);
			const [permissionRows, setPermissionRows] = (0, react.useState)(void 0);
			const catalog = controller.runCatalog();
			const workspaceRows = (0, react.useMemo)(() => catalog?.listWorkspaces() ?? [], [catalog]);
			(0, react.useEffect)(() => {
				let alive = true;
				(async () => {
					if (catalog === void 0) return;
					const [loadedPresets, loadedGroups, loadedPermissions] = await Promise.all([
						catalog.listAgentPresets(),
						catalog.listModelGroups(),
						catalog.listPermissions()
					]);
					if (alive) {
						setPresets(loadedPresets);
						setGroups(loadedGroups);
						setPermissionRows(loadedPermissions);
					}
				})();
				return () => {
					alive = false;
				};
			}, [catalog]);
			/** Efforts of the selected model (empty when none advertised). */
			const effortOptions = (0, react.useMemo)(() => {
				const provider = value.provider ?? "";
				const model = value.model ?? "";
				if (provider === "" || model === "") return [];
				return (groups.find((candidate) => candidate.provider === provider)?.models.find((candidate) => candidate.id === model))?.efforts ?? [];
			}, [
				groups,
				value.provider,
				value.model
			]);
			/** Select a provider/model pair; the effort resets with the model. */
			const setModel = (key) => {
				const sepIndex = key.indexOf(MODEL_SEP$1);
				onChange({
					...value,
					provider: sepIndex >= 0 ? key.slice(0, sepIndex) : void 0,
					model: sepIndex >= 0 ? key.slice(sepIndex + 1) : void 0,
					reasoningEffort: void 0
				});
			};
			const set = (patch) => onChange({
				...value,
				...patch
			});
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [
				/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("label", {
					className: board_module_css_default.field,
					children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.fieldLabel,
						children: t("new.agentPreset")
					}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.selectWrap,
						children: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("select", {
							className: board_module_css_default.input,
							value: value.agentPreset ?? "",
							onChange: (event) => {
								set({ agentPreset: event.target.value === "" ? void 0 : event.target.value });
							},
							children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("option", {
								value: "",
								children: t("new.agentPresetDefault")
							}), presets.map((preset) => /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("option", {
								value: preset.id,
								title: preset.description ?? preset.id,
								children: [preset.name ?? preset.id, preset.isDefault === true ? ` (${t("new.agentPresetDefaultTag")})` : ""]
							}, preset.id))]
						})
					})]
				}),
				/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("label", {
					className: board_module_css_default.field,
					children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.fieldLabel,
						children: t("new.workspace")
					}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.selectWrap,
						children: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("select", {
							className: board_module_css_default.input,
							value: value.workspaceId ?? "",
							onChange: (event) => {
								set({ workspaceId: event.target.value === "" ? void 0 : event.target.value });
							},
							children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("option", {
								value: "",
								children: t("new.workspaceDefault")
							}), workspaceRows.map((row) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)("option", {
								value: row.id,
								children: row.title
							}, row.id))]
						})
					})]
				}),
				/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("label", {
					className: board_module_css_default.field,
					children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.fieldLabel,
						children: t("new.model")
					}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.selectWrap,
						children: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("select", {
							className: board_module_css_default.input,
							value: value.provider !== void 0 && value.model !== void 0 ? `${value.provider}${MODEL_SEP$1}${value.model}` : "",
							onChange: (event) => {
								setModel(event.target.value);
							},
							children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("option", {
								value: "",
								children: t("new.modelDefault")
							}), groups.map((group) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)("optgroup", {
								label: group.provider,
								children: group.models.map((model) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)("option", {
									value: `${group.provider}${MODEL_SEP$1}${model.id}`,
									children: model.name ?? model.id
								}, `${group.provider}${MODEL_SEP$1}${model.id}`))
							}, group.provider))]
						})
					})]
				}),
				effortOptions.length > 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("label", {
					className: board_module_css_default.field,
					children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.fieldLabel,
						children: t("new.effort")
					}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.selectWrap,
						children: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("select", {
							className: board_module_css_default.input,
							value: value.reasoningEffort ?? "",
							onChange: (event) => {
								set({ reasoningEffort: event.target.value === "" ? void 0 : event.target.value });
							},
							children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("option", {
								value: "",
								children: t("new.effortDefault")
							}), effortOptions.map((option) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)("option", {
								value: option.id,
								children: option.name ?? option.id
							}, option.id))]
						})
					})]
				}),
				permissionRows !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("label", {
					className: board_module_css_default.field,
					children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.fieldLabel,
						children: t("new.permission")
					}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.selectWrap,
						children: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("select", {
							className: board_module_css_default.input,
							value: value.permission ?? "",
							onChange: (event) => {
								set({ permission: event.target.value === "" ? void 0 : event.target.value });
							},
							children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("option", {
								value: "",
								children: t("new.permissionDefault")
							}), permissionRows.map((row) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)("option", {
								value: row.id,
								title: row.description ?? row.id,
								children: permissionLabel(row.id, row.name)
							}, row.id))]
						})
					})]
				})
			] });
		}
		//#endregion
		//#region src/client/board/RunPresetManager.tsx
		/**
		* Run-config preset manager: the list (部署默认 + custom presets) with
		* set-default / edit / delete, and the add/edit form — **名称 + the SAME
		* RunConfigEditor as the task form**, so the preset's content is visible and
		* editable in place (a new preset seeds from the form's current config, so
		* "save what I just tuned" stays one click). Deleting whatever preset was
		* the default falls back to 部署默认 (the built-in, never deletable): the
		* fallback chain lives in run-presets.ts (defaultRunPresetOf).
		*/
		function presetId() {
			return `custom-${Date.now().toString(36)}-${Math.random().toString(36).slice(2, 8)}`;
		}
		/** The "N 项已配置" summary of a preset's set fields. */
		function configCount(config) {
			return Object.values(config).filter((value) => typeof value === "string" && value !== "").length;
		}
		/** The run-config preset manager (see module doc). */
		function RunPresetManager({ store, doc, current, controller, onChanged, onClose }) {
			const [formKey, setFormKey] = (0, react.useState)(void 0);
			const [name, setName] = (0, react.useState)("");
			const [config, setConfig] = (0, react.useState)({});
			const [error, setError] = (0, react.useState)(void 0);
			const persist = (next) => {
				store.save(next);
				onChanged(next);
			};
			const setDefault = (id) => {
				if (id === doc.defaultId) return;
				persist({
					...doc,
					defaultId: id
				});
			};
			const remove = (preset) => {
				if (preset.id === "deploy-default") return;
				const next = {
					presets: doc.presets.filter((candidate) => candidate.id !== preset.id),
					...doc.defaultId === preset.id ? {} : { defaultId: doc.defaultId }
				};
				persist(next);
			};
			const save = () => {
				const trimmed = name.trim();
				if (trimmed === "") {
					setError(t("runPreset.nameRequired"));
					return;
				}
				if (formKey === "new") persist({
					...doc,
					presets: [...doc.presets, {
						id: presetId(),
						name: trimmed,
						config
					}]
				});
				else if (formKey !== void 0) {
					if (doc.presets.find((candidate) => candidate.id === formKey) !== void 0) persist({
						...doc,
						presets: doc.presets.map((candidate) => candidate.id === formKey ? {
							...candidate,
							name: trimmed,
							config
						} : candidate)
					});
				}
				setFormKey(void 0);
				setName("");
				setError(void 0);
			};
			const startForm = (key) => {
				const existing = key !== "new" ? findRunPreset(doc, key) : void 0;
				setFormKey(key);
				setName(existing !== void 0 && existing.id !== "deploy-default" ? existing.name : "");
				setConfig(existing !== void 0 && existing.id !== "deploy-default" ? existing.config : current);
				setError(void 0);
			};
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(Dialog, {
				label: t("runPreset.manageTitle"),
				onClose,
				title: t("runPreset.manageTitle"),
				className: board_module_css_default.autoModal,
				portal: true,
				children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
					className: board_module_css_default.modalScroll,
					children: [
						/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
							className: board_module_css_default.presetList,
							children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
								className: board_module_css_default.runPresetRow,
								children: [
									/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
										className: board_module_css_default.runPresetName,
										title: t("runPreset.deployDefaultHint"),
										children: [t("runPreset.deployDefault"), doc.defaultId === void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
											className: board_module_css_default.runPresetDefaultBadge,
											children: t("runPreset.defaultBadge")
										})]
									}),
									/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
										className: board_module_css_default.detailHint,
										children: t("runPreset.deployDefaultNote")
									}),
									doc.defaultId !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
										className: board_module_css_default.autoRuleActions,
										children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
											size: "sm",
											onClick: () => {
												setDefault("deploy-default");
											},
											children: t("runPreset.setDefault")
										})
									})
								]
							}), doc.presets.map((preset) => /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
								className: board_module_css_default.runPresetRow,
								children: [
									/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
										className: board_module_css_default.runPresetName,
										title: preset.name,
										children: [preset.name, doc.defaultId === preset.id && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
											className: board_module_css_default.runPresetDefaultBadge,
											children: t("runPreset.defaultBadge")
										})]
									}),
									/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
										className: board_module_css_default.detailHint,
										children: t("runPreset.configCount", { n: String(configCount(preset.config)) })
									}),
									/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
										className: board_module_css_default.autoRuleActions,
										children: [
											doc.defaultId !== preset.id && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
												size: "sm",
												onClick: () => {
													setDefault(preset.id);
												},
												children: t("runPreset.setDefault")
											}),
											/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
												size: "sm",
												title: t("runPreset.editTitle"),
												onClick: () => {
													startForm(preset.id);
												},
												children: t("runPreset.edit")
											}),
											/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
												size: "sm",
												variant: "dangerGhost",
												title: t("runPreset.deleteTitle"),
												onClick: () => {
													remove(preset);
												},
												children: t("runPreset.delete")
											})
										]
									})
								]
							}, preset.id))]
						}),
						formKey === void 0 ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
							className: board_module_css_default.autoAddAction,
							children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
								size: "sm",
								onClick: () => {
									startForm("new");
								},
								children: t("runPreset.add")
							})
						}) : /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
							className: board_module_css_default.autoForm,
							children: [
								/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("label", {
									className: board_module_css_default.autoField,
									children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
										className: board_module_css_default.autoFieldLabel,
										children: t("runPreset.name")
									}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("input", {
										className: `${board_module_css_default.input} ${board_module_css_default.autoNameInput}`,
										value: name,
										"data-autofocus": true,
										placeholder: t("runPreset.namePlaceholder"),
										spellCheck: false,
										onChange: (event) => {
											setName(event.target.value);
											setError(void 0);
										},
										onKeyDown: (event) => {
											if (event.key === "Enter") save();
										}
									})]
								}),
								/* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
									className: board_module_css_default.detailHint,
									children: t("runPreset.formHint")
								}),
								error !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
									className: board_module_css_default.formError,
									children: error
								}),
								/* @__PURE__ */ (0, react_jsx_runtime.jsx)(RunConfigEditor, {
									value: config,
									onChange: setConfig,
									controller
								})
							]
						}),
						/* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
							className: board_module_css_default.detailHint,
							children: t("runPreset.hint")
						})
					]
				}), formKey !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("footer", {
					className: board_module_css_default.modalFooter,
					children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
						size: "sm",
						onClick: () => {
							setFormKey(void 0);
							setName("");
							setError(void 0);
						},
						children: t("detail.cancel")
					}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
						size: "sm",
						variant: "primary",
						onClick: save,
						children: t("runPreset.save")
					})]
				})]
			});
		}
		//#endregion
		//#region src/client/board/RunConfigFields.tsx
		/**
		* The run-configuration FIELDS block as one shared component: the
		* 配置预设 picker (instant-apply + manager) and the ONE RunConfigEditor.
		* Extracted verbatim from TaskForm so every surface that configures a
		* session's run (the task form, the detail's 新建会话) renders the exact
		* same fields in the exact same order — zero drift by construction.
		* Fully controlled: the parent owns the value object.
		*/
		function RunConfigFields({ value, onChange, controller }) {
			const [presetStore] = (0, react.useState)(() => controller.runPresetStore());
			const [presetDoc, setPresetDoc] = (0, react.useState)(() => normalizeRunPresetDocument(presetStore.load()));
			const [showPresetManager, setShowPresetManager] = (0, react.useState)(false);
			/** Pick a preset: INSTANTLY rewrites the fields. */
			const applyRunPreset = (id) => {
				const preset = findRunPreset(presetDoc, id);
				if (preset === void 0) return;
				onChange(preset.config);
			};
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
				className: board_module_css_default.field,
				children: [
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.fieldLabel,
						children: t("new.runConfig")
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("label", {
						className: board_module_css_default.field,
						children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
							className: board_module_css_default.fieldLabel,
							children: t("new.runPresets")
						}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
							className: board_module_css_default.selectWrap,
							children: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("select", {
								className: board_module_css_default.input,
								value: "",
								"aria-label": t("new.runPresets"),
								onChange: (event) => {
									if (event.target.value === "") return;
									if (event.target.value === "__manage") {
										setShowPresetManager(true);
										return;
									}
									applyRunPreset(event.target.value);
								},
								children: [
									/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("option", {
										value: "",
										children: [t("runPreset.placeholder"), "…"]
									}),
									mergedRunPresets(presetDoc).map((preset) => /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("option", {
										value: preset.id,
										children: [preset.name, preset.id === defaultRunPresetOf(presetDoc).id ? ` (${t("runPreset.defaultBadge")})` : ""]
									}, preset.id)),
									/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("option", {
										value: "__manage",
										children: [t("runPreset.manage"), "…"]
									})
								]
							})
						})]
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)(RunConfigEditor, {
						value,
						onChange,
						controller
					}),
					showPresetManager && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(RunPresetManager, {
						store: presetStore,
						doc: presetDoc,
						current: value,
						controller,
						onChanged: setPresetDoc,
						onClose: () => {
							setShowPresetManager(false);
						}
					})
				]
			});
		}
		//#endregion
		//#region src/client/board/TaskForm.tsx
		/** The shared new/edit task form. */
		function TaskForm({ draft, onChange, controller, withStatus = false, sessionId }) {
			/** The form's current run-config — the add preset seeds from it. */
			const currentConfig = {
				...draft.workspaceId !== "" ? { workspaceId: draft.workspaceId } : {},
				...draft.provider !== "" ? { provider: draft.provider } : {},
				...draft.model !== "" ? { model: draft.model } : {},
				...draft.reasoningEffort !== "" ? { reasoningEffort: draft.reasoningEffort } : {},
				...draft.agentPreset !== "" ? { agentPreset: draft.agentPreset } : {},
				...draft.permission !== "" ? { permission: draft.permission } : {}
			};
			/** The shared config block writes back into the draft fields. */
			const applyConfig = (next) => {
				onChange({
					...draft,
					workspaceId: next.workspaceId ?? "",
					agentPreset: next.agentPreset ?? "",
					provider: next.provider ?? "",
					model: next.model ?? "",
					reasoningEffort: next.reasoningEffort ?? "",
					permission: next.permission ?? ""
				});
			};
			const attachments = useComposerImages(TASK_IMAGE_BUDGET, 3, {
				images: draft.promptImages,
				onChange: (next) => {
					onChange({
						...draft,
						promptImages: [...next]
					});
				}
			});
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [
				/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("label", {
					className: board_module_css_default.field,
					children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.fieldLabel,
						children: t("new.title")
					}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("input", {
						className: board_module_css_default.input,
						value: draft.title,
						"data-autofocus": true,
						placeholder: t("new.titlePlaceholder"),
						onChange: (event) => {
							onChange({
								...draft,
								title: event.target.value
							});
						}
					})]
				}),
				/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("label", {
					className: board_module_css_default.field,
					children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.fieldLabel,
						children: t("new.description")
					}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("textarea", {
						className: board_module_css_default.input,
						rows: 3,
						value: draft.description,
						placeholder: t("new.descriptionPlaceholder"),
						onChange: (event) => {
							onChange({
								...draft,
								description: event.target.value
							});
						}
					})]
				}),
				/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
					className: board_module_css_default.field,
					...attachments.dropProps,
					children: [
						/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
							className: board_module_css_default.fieldLabel,
							children: t("new.prompt")
						}),
						/* @__PURE__ */ (0, react_jsx_runtime.jsx)(PromptInput, {
							value: draft.prompt,
							onChange: (next) => {
								onChange({
									...draft,
									prompt: next
								});
							},
							placeholder: t("new.promptPlaceholder"),
							rows: 4,
							controller,
							sessionId
						}),
						/* @__PURE__ */ (0, react_jsx_runtime.jsx)(AttachmentStrip, {
							images: draft.promptImages,
							files: draft.promptFiles,
							onAdd: attachments.addFiles,
							onRemoveImage: (id) => {
								onChange({
									...draft,
									promptImages: draft.promptImages.filter((image) => image.id !== id)
								});
							},
							onRemoveFile: (id) => {
								onChange({
									...draft,
									promptFiles: draft.promptFiles.filter((file) => file.id !== id)
								});
							},
							busy: attachments.busy,
							busyLabel: attachments.busy ? attachBusyLabel(attachments.busyKind) : void 0,
							error: attachments.error
						}),
						/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
							className: board_module_css_default.fieldHint,
							children: t("new.attachHint", {
								maxImages: String(3),
								commentImages: String(9),
								commentFiles: String(5)
							})
						})
					]
				}),
				withStatus && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("label", {
					className: board_module_css_default.field,
					children: [
						/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
							className: board_module_css_default.fieldLabel,
							children: t("new.status")
						}),
						/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
							className: board_module_css_default.selectWrap,
							children: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("select", {
								className: board_module_css_default.input,
								value: draft.status,
								onChange: (event) => {
									onChange({
										...draft,
										status: event.target.value
									});
								},
								children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("option", {
									value: "todo",
									children: t("board.status.todo")
								}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("option", {
									value: "backlog",
									children: t("board.status.backlog")
								})]
							})
						}),
						/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
							className: board_module_css_default.fieldHint,
							children: t("new.statusHint")
						})
					]
				}),
				/* @__PURE__ */ (0, react_jsx_runtime.jsx)(RunConfigFields, {
					value: currentConfig,
					onChange: applyConfig,
					controller
				})
			] });
		}
		//#endregion
		//#region src/client/board/drafts.ts
		/** Storage key for the draft document. */
		const DRAFT_STORAGE_KEY = "dsh.taskBoard.drafts.v1";
		/** Shared comment-composer draft key for one native session of one task. */
		function commentDraftKey(taskId, sessionId) {
			return `comment:${taskId}:${sessionId}`;
		}
		/** Task edit-form draft key (the whole TaskDraft, JSON-encoded). */
		function editDraftKey(taskId) {
			return `edit:${taskId}`;
		}
		/** Refinement answer-box draft key. */
		function refineDraftKey(taskId) {
			return `refine:${taskId}`;
		}
		/** New-session modal draft key for one task (title + run config, JSON). The
		*  half-typed name and the picked model/permission must survive closing the
		*  dialog — the same promise every other input surface keeps. */
		function newSessionDraftKey(taskId) {
			return `newsession:${taskId}`;
		}
		/** Session-rule form draft key (the instruction text) for one task + rule. */
		function ruleDraftKey(taskId, ruleId) {
			return `rule:${taskId}:${ruleId}`;
		}
		/** Answer-form draft key for one open question carrier.
		*
		*  Keyed by `rpcId` alone, and that is the point: the host mints one rpcId per
		*  open `ask()` and REPLAYS it with every requested frame, so the key survives
		*  a re-render, a remount and a page reload — which is exactly the window in
		*  which a half-completed answer used to be destroyed. A genuinely new request
		*  mints a new rpcId and therefore starts clean, so the isolation the component
		*  already got from `key={rpcId}` is preserved.
		*/
		function questionDraftKey(rpcId) {
			return `question:${rpcId}`;
		}
		/** localStorage-backed drafts (the browser backend). Failures degrade to
		*  memory-only safety: a write that errors (private mode, quota) never throws
		*  and simply leaves the previous persisted state; in-memory state stays
		*  live for the session. */
		var LocalStorageDraftStore = class {
			key;
			storage;
			constructor(key = DRAFT_STORAGE_KEY, storage = globalThis.localStorage) {
				this.key = key;
				this.storage = storage;
			}
			read() {
				if (this.storage === void 0) return {};
				try {
					const raw = this.storage.getItem(this.key);
					if (raw === null) return {};
					const parsed = JSON.parse(raw);
					if (typeof parsed !== "object" || parsed === null) return {};
					const map = {};
					for (const [name, value] of Object.entries(parsed)) if (typeof value === "string") map[name] = value;
					return map;
				} catch {
					return {};
				}
			}
			write(map) {
				if (this.storage === void 0) return;
				try {
					this.storage.setItem(this.key, JSON.stringify(map));
				} catch {}
			}
			get(key) {
				return this.read()[key];
			}
			set(key, text) {
				const map = this.read();
				if (text === "") {
					delete map[key];
					this.write(map);
					return;
				}
				map[key] = text;
				this.write(map);
			}
			clear(key) {
				const map = this.read();
				if (key in map) {
					delete map[key];
					this.write(map);
				}
			}
		};
		/** The default browser-backed singleton the surfaces use. */
		const draftStore = new LocalStorageDraftStore();
		/**
		* Answer-form drafts are a LIST of objects, while `DraftStore` holds one string
		* per slot — so the two typed helpers below are the only place that knows the
		* encoding. Keeping them here (rather than in the component) means the round
		* trip is testable on its own, and the component only ever sees `QuestionDraft[]`.
		*
		* Validation is deliberately strict and total: a draft slot is user-writable
		* storage that can hold anything at all (an older build's shape, a hand-edited
		* value, a truncated write). Anything that does not parse into the exact shape
		* is dropped rather than trusted, so a corrupted slot degrades to "no draft"
		* instead of throwing inside render or restoring a malformed answer.
		*/
		function saveQuestionDrafts(store, rpcId, drafts) {
			const key = questionDraftKey(rpcId);
			if (!drafts.some((draft) => {
				if (typeof draft !== "object" || draft === null) return false;
				const entry = draft;
				const selected = Array.isArray(entry.selected) ? entry.selected.length > 0 : false;
				const custom = typeof entry.custom === "string" && entry.custom !== "";
				return selected || custom || entry.skipped === true;
			})) {
				store.clear(key);
				return;
			}
			store.set(key, JSON.stringify(drafts));
		}
		/**
		* Read a saved answer batch back. Returns undefined when nothing usable is
		* stored — the caller then falls back to its own fresh drafts, so this can
		* never hand back a partial or malformed batch.
		*/
		function restoreQuestionDrafts(store, rpcId) {
			const raw = store.get(questionDraftKey(rpcId));
			if (raw === void 0) return void 0;
			let parsed;
			try {
				parsed = JSON.parse(raw);
			} catch {
				return;
			}
			if (!Array.isArray(parsed) || parsed.length === 0) return void 0;
			const out = [];
			for (const entry of parsed) {
				if (typeof entry !== "object" || entry === null) return void 0;
				const record = entry;
				if (!Array.isArray(record.selected)) return void 0;
				const selected = record.selected.filter((value) => typeof value === "string");
				if (selected.length !== record.selected.length) return void 0;
				if (record.custom !== void 0 && typeof record.custom !== "string") return void 0;
				if (record.skipped !== void 0 && typeof record.skipped !== "boolean") return void 0;
				out.push({
					selected,
					...record.custom !== void 0 ? { custom: record.custom } : {},
					...record.skipped !== void 0 ? { skipped: record.skipped } : {}
				});
			}
			return out;
		}
		//#endregion
		//#region src/client/board/task-draft.ts
		/** Normalize a parsed draft (a stored draft may predate a field — e.g.
		*  promptImages — so every consumer gets a complete, safe shape). Attachment
		*  arrays wash element-wise (never a throwing passthrough — a dirty stored
		*  draft must degrade to fewer chips, not a crash). */
		function normalizeDraft(parsed) {
			if (parsed === null || parsed === void 0) return void 0;
			if (typeof parsed.title !== "string" || typeof parsed.prompt !== "string") return void 0;
			return {
				title: parsed.title,
				description: parsed.description ?? "",
				prompt: parsed.prompt,
				promptImages: Array.isArray(parsed.promptImages) ? parsed.promptImages.filter((entry) => typeof entry === "object" && entry !== null && typeof entry.id === "string" && typeof entry.data === "string" && entry.data !== "") : [],
				promptFiles: Array.isArray(parsed.promptFiles) ? parsed.promptFiles.filter((entry) => typeof entry === "object" && entry !== null && typeof entry.id === "string" && typeof entry.receiptId === "string" && entry.receiptId !== "" && typeof entry.name === "string") : [],
				status: parsed.status === "todo" ? "todo" : "backlog",
				agentPreset: parsed.agentPreset ?? "",
				workspaceId: parsed.workspaceId ?? "",
				provider: parsed.provider ?? "",
				model: parsed.model ?? "",
				reasoningEffort: parsed.reasoningEffort ?? "",
				permission: parsed.permission ?? "",
				color: typeof parsed.color === "string" ? parsed.color : ""
			};
		}
		/** Build a draft from a task record. */
		function draftFromTask(task) {
			return {
				title: task.title,
				description: task.description,
				prompt: task.prompt,
				promptImages: (task.promptImages ?? []).map((image, index) => ({
					id: `loaded-${index}-${image.data.slice(0, 8)}`,
					data: image.data,
					mediaType: image.mediaType,
					name: image.name ?? ""
				})),
				promptFiles: (task.promptFiles ?? []).map((file, index) => ({
					id: `loaded-file-${index}-${file.receiptId.slice(0, 8)}`,
					receiptId: file.receiptId,
					name: file.name,
					bytes: file.bytes
				})),
				status: task.status === "backlog" ? "backlog" : "todo",
				agentPreset: task.agentPreset ?? "",
				workspaceId: task.workspaceId ?? "",
				provider: task.provider ?? "",
				model: task.model ?? "",
				reasoningEffort: task.reasoningEffort ?? "",
				permission: task.permission ?? "",
				color: task.color ?? ""
			};
		}
		/** Build a draft from a saved template (stamp-out fills the new-task form). */
		function draftFromTemplate(template) {
			return {
				title: template.title,
				description: template.description,
				prompt: template.prompt,
				promptImages: (template.promptImages ?? []).map((image, index) => ({
					id: `template-${index}-${image.data.slice(0, 8)}`,
					data: image.data,
					mediaType: image.mediaType,
					name: image.name ?? ""
				})),
				promptFiles: (template.promptFiles ?? []).map((file, index) => ({
					id: `template-file-${index}-${file.receiptId.slice(0, 8)}`,
					receiptId: file.receiptId,
					name: file.name,
					bytes: file.bytes
				})),
				status: "backlog",
				agentPreset: template.agentPreset ?? "",
				workspaceId: template.workspaceId ?? "",
				provider: template.provider ?? "",
				model: template.model ?? "",
				reasoningEffort: template.reasoningEffort ?? "",
				permission: template.permission ?? "",
				color: template.color ?? ""
			};
		}
		/** Draft → create input; empty fields are omitted (fall back to defaults). */
		function draftToNewInput(draft) {
			return {
				title: draft.title,
				description: draft.description,
				prompt: draft.prompt,
				...draft.promptImages.length > 0 ? { promptImages: draft.promptImages.map((image) => ({
					mediaType: image.mediaType,
					data: image.data,
					name: image.name
				})) } : {},
				...draft.promptFiles.length > 0 ? { promptFiles: draft.promptFiles.map((file) => toPromptFile(file)) } : {},
				status: draft.status,
				...draft.agentPreset !== "" ? { agentPreset: draft.agentPreset } : {},
				...draft.workspaceId !== "" ? { workspaceId: draft.workspaceId } : {},
				...draft.provider !== "" ? { provider: draft.provider } : {},
				...draft.model !== "" ? { model: draft.model } : {},
				...draft.reasoningEffort !== "" ? { reasoningEffort: draft.reasoningEffort } : {},
				...draft.permission !== "" ? { permission: draft.permission } : {},
				...draft.color !== "" ? { color: draft.color } : {}
			};
		}
		/** Draft → update patch; empty fields are set to undefined to clear them. */
		function draftToUpdatePatch(draft) {
			return {
				title: draft.title,
				description: draft.description,
				prompt: draft.prompt,
				promptImages: draft.promptImages.map((image) => ({
					mediaType: image.mediaType,
					data: image.data,
					name: image.name
				})),
				promptFiles: draft.promptFiles.map((file) => toPromptFile(file)),
				agentPreset: draft.agentPreset !== "" ? draft.agentPreset : void 0,
				workspaceId: draft.workspaceId !== "" ? draft.workspaceId : void 0,
				provider: draft.provider !== "" ? draft.provider : void 0,
				model: draft.model !== "" ? draft.model : void 0,
				reasoningEffort: draft.reasoningEffort !== "" ? draft.reasoningEffort : void 0,
				permission: draft.permission !== "" ? draft.permission : void 0,
				color: draft.color !== "" ? draft.color : void 0
			};
		}
		/** Whether a draft field counts as user-filled (a template must not cover
		*  it): non-empty text, a non-default status, a non-empty image set. */
		function filledText(value) {
			return value !== "";
		}
		/** Stamp a template onto a draft by filling BLANKS only: every field the user
		*  already touched keeps the user's value; the template supplies the rest.
		*  One merge law for all fields (title/description/prompt/images/status/run
		*  config) — never per-field preserve exceptions.
		*  The one atomic group is provider/model/reasoningEffort (a run route):
		*  mixing the user's provider with the template's model breeds invalid
		*  combos, so the user's triple wins only when the pair is complete —
		*  otherwise the template's whole triple rides (effort is tuned per model).
		*/
		function stampTemplate(draft, stamped) {
			const userRouteComplete = filledText(draft.provider) && filledText(draft.model);
			return {
				title: filledText(draft.title) ? draft.title : stamped.title,
				description: filledText(draft.description) ? draft.description : stamped.description,
				prompt: filledText(draft.prompt) ? draft.prompt : stamped.prompt,
				promptImages: draft.promptImages.length > 0 ? draft.promptImages : stamped.promptImages,
				promptFiles: draft.promptFiles.length > 0 ? draft.promptFiles : stamped.promptFiles,
				status: draft.status !== "backlog" ? draft.status : stamped.status,
				agentPreset: filledText(draft.agentPreset) ? draft.agentPreset : stamped.agentPreset,
				workspaceId: filledText(draft.workspaceId) ? draft.workspaceId : stamped.workspaceId,
				provider: userRouteComplete ? draft.provider : stamped.provider,
				model: userRouteComplete ? draft.model : stamped.model,
				reasoningEffort: userRouteComplete ? draft.reasoningEffort : stamped.reasoningEffort,
				permission: filledText(draft.permission) ? draft.permission : stamped.permission,
				color: filledText(draft.color) ? draft.color : stamped.color
			};
		}
		//#endregion
		//#region src/client/board/NewTaskModal.tsx
		/**
		* New-task modal: the shared task form (content + run configuration) on a
		* fresh draft. Creates through the controller (which persists immediately).
		*/
		/** The fresh draft shape ('' = default / not set; landing column 待规划). */
		function freshDraft() {
			return {
				title: "",
				description: "",
				prompt: "",
				promptImages: [],
				status: "backlog",
				agentPreset: "",
				workspaceId: "",
				provider: "",
				model: "",
				reasoningEffort: "",
				permission: "",
				color: "",
				promptFiles: []
			};
		}
		/** The initial draft of a NEW task: the run-config preset marked DEFAULT is
		*  applied (the fallback chain in run-presets.ts — never set / deleted ->
		*  部署默认 = the previous behavior). Only the run-config fields;
		*  title/description/prompt always start empty. */
		function initialDraft(presetStore) {
			const config = defaultRunPresetOf(normalizeRunPresetDocument(presetStore.load())).config;
			return {
				...freshDraft(),
				...config.workspaceId !== void 0 ? { workspaceId: config.workspaceId } : {},
				...config.provider !== void 0 ? { provider: config.provider } : {},
				...config.model !== void 0 ? { model: config.model } : {},
				...config.reasoningEffort !== void 0 ? { reasoningEffort: config.reasoningEffort } : {},
				...config.agentPreset !== void 0 ? { agentPreset: config.agentPreset } : {},
				...config.permission !== void 0 ? { permission: config.permission } : {}
			};
		}
		/** New-task form overlay. */
		function NewTaskModal({ controller, onClose }) {
			const [draft, setDraft] = (0, react.useState)(() => {
				const stored = draftStore.get("new");
				if (stored !== void 0) try {
					const parsed = normalizeDraft(JSON.parse(stored));
					if (parsed !== void 0) return parsed;
				} catch {}
				return initialDraft(controller.runPresetStore());
			});
			const changeDraft = (next) => {
				setDraft(next);
				draftStore.set("new", JSON.stringify(next));
			};
			const templates = controller.listTemplates();
			const [pickedId, setPickedId] = (0, react.useState)("");
			const [confirmDelete, setConfirmDelete] = (0, react.useState)(false);
			const applyTemplate = (id) => {
				setPickedId(id);
				const template = templates.find((candidate) => candidate.id === id);
				if (template === void 0) return;
				changeDraft(stampTemplate(draft, draftFromTemplate(template)));
			};
			const removePicked = () => {
				if (pickedId !== "") controller.deleteTemplate(pickedId);
				setPickedId("");
				setConfirmDelete(false);
			};
			const submit = () => {
				if (controller.createTask(draftToNewInput(draft)) === void 0) return;
				draftStore.clear("new");
				onClose();
			};
			return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Dialog, {
				label: t("board.new"),
				onClose,
				title: t("board.new"),
				children: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("form", {
					className: board_module_css_default.modalForm,
					onSubmit: (event) => {
						event.preventDefault();
						submit();
					},
					children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
						className: board_module_css_default.modalScroll,
						children: [templates.length > 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
							className: board_module_css_default.field,
							children: [
								/* @__PURE__ */ (0, react_jsx_runtime.jsx)("label", {
									className: board_module_css_default.fieldLabel,
									htmlFor: "dsh-tb-template-picker",
									children: t("new.fromTemplate")
								}),
								/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
									className: board_module_css_default.templateRow,
									children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
										className: board_module_css_default.selectWrap,
										children: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("select", {
											id: "dsh-tb-template-picker",
											className: board_module_css_default.input,
											value: pickedId,
											onChange: (event) => {
												applyTemplate(event.target.value);
											},
											children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("option", {
												value: "",
												children: t("new.pickTemplate")
											}), templates.map((template) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)("option", {
												value: template.id,
												children: template.name
											}, template.id))]
										})
									}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
										variant: "dangerGhost",
										title: t("new.deleteTemplateTitle"),
										disabled: pickedId === "",
										onClick: () => {
											setConfirmDelete(true);
										},
										children: t("new.deleteTemplate")
									})]
								}),
								confirmDelete && pickedId !== "" && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(ConfirmDialog, {
									title: t("new.deleteTemplate"),
									message: t("new.deleteTemplateConfirm"),
									confirmLabel: t("new.deleteTemplate"),
									danger: true,
									onCancel: () => {
										setConfirmDelete(false);
									},
									onConfirm: removePicked
								})
							]
						}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(TaskForm, {
							draft,
							onChange: changeDraft,
							controller,
							withStatus: true,
							sessionId: controller.referenceSessionOf(void 0)
						})]
					}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("footer", {
						className: board_module_css_default.modalFooter,
						children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
							onClick: onClose,
							children: t("new.cancel")
						}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
							type: "submit",
							variant: "primary",
							children: t("new.submit")
						})]
					})]
				})
			});
		}
		//#endregion
		//#region src/client/board/status.ts
		/** Status → locale key. */
		const STATUS_KEY = {
			backlog: "board.status.backlog",
			todo: "board.status.todo",
			running: "board.status.running",
			review: "board.status.review",
			done: "board.status.done"
		};
		/**
		* Status → SHORT locale key, read by the compact column navigator. Five equal
		* cells on a phone leave roughly one-and-a-half Chinese characters per label,
		* so the full names all collapse to 「待…」「进…」「已…」 and the navigator
		* stops naming anything at all. The short forms are ordinary locale data (never
		* a hardcoded string, never a truncation rule), and the full name stays on the
		* tab as its accessible name.
		*/
		const STATUS_SHORT_KEY = {
			backlog: "board.statusShort.backlog",
			todo: "board.statusShort.todo",
			running: "board.statusShort.running",
			review: "board.statusShort.review",
			done: "board.statusShort.done"
		};
		/** Status → pull-policy hint (one sentence per column: what may enter, what
		*  it means to sit here). Read by the column header tooltip — supplementary
		*  orientation, never the sole carrier of anything actionable. */
		const COLUMN_HINT_KEY = {
			backlog: "board.columnHint.backlog",
			todo: "board.columnHint.todo",
			running: "board.columnHint.running",
			review: "board.columnHint.review",
			done: "board.columnHint.done"
		};
		/**
		* Paused-rule explanation keyed by the pausing status: ONE map read by both
		* automation surfaces (the detail editor and the overview row derive the same
		* readiness reason, so the reason copy can never drift).
		*/
		const PAUSED_REASON_KEY = {
			backlog: "detail.schedule.paused.backlog",
			review: "detail.schedule.paused.review",
			done: "detail.schedule.paused.done"
		};
		//#endregion
		//#region src/client/board/cron-label.ts
		/**
		* ONE human-readable cron label for every surface that shows an expression
		* (the task detail's schedule meta, the automation overview's rule rows, the
		* preset manager's rows, the session-rule rows): the describeCron shape
		* mapped to locale-aware text. Parsing stays in core/schedule.ts; this is
		* purely the presentation mapping, so no surface can ever render a slightly
		* different "daily at 09:00" again.
		*/
		/** Short weekday names (0 = Sunday), locale-aware. */
		const WEEKDAYS_ZH = [
			"周日",
			"周一",
			"周二",
			"周三",
			"周四",
			"周五",
			"周六"
		];
		const WEEKDAYS_EN = [
			"Sun",
			"Mon",
			"Tue",
			"Wed",
			"Thu",
			"Fri",
			"Sat"
		];
		/**
		* The human label of a cron expression. An unparseable expression falls back
		* to the raw text (or to `invalid` when the caller wants an explicit silent
		* swap — the preset manager rows show '' instead of the raw expression).
		*/
		function cronHumanLabel(expr, invalid) {
			const description = describeCron(expr);
			if (description === void 0) return invalid ?? expr;
			switch (description.kind) {
				case "everyMinute": return t("schedule.desc.everyMinute");
				case "everyMinutes": return t("schedule.desc.everyMinutes", { n: String(description.minutes) });
				case "everyHours": return t("schedule.desc.everyHours", { n: String(description.hours) });
				case "dailyAt": return t("schedule.desc.dailyAt", { time: description.time });
				case "weekdaysAt": return t("schedule.desc.weekdaysAt", { time: description.time });
				case "weeklyAt": return t("schedule.desc.weeklyAt", {
					days: description.weekdays.map((day) => (isEnglish() ? WEEKDAYS_EN : WEEKDAYS_ZH)[day] ?? String(day)).join(isEnglish() ? ", " : "、"),
					time: description.time
				});
				case "monthlyAt": return t("schedule.desc.monthlyAt", {
					days: description.days.map(String).join(isEnglish() ? ", " : "、"),
					time: description.time
				});
				case "custom": return t("schedule.desc.custom");
			}
		}
		//#endregion
		//#region src/client/board/PresetManager.tsx
		/**
		* Schedule preset manager: a small modal for editing the quick-pick preset
		* list — rename/replace/delete any preset, add custom ones (with live cron
		* validation and description), and restore the built-in defaults.
		* Persistence rides the local preset store; the built-ins are code
		* constants and are never persisted.
		*/
		/** The merged list shown in the preset dropdown (defaults + custom). */
		function mergedPresets(store) {
			return mergePresets(DEFAULT_PRESETS, store.load());
		}
		/** Whether a preset id belongs to the built-in defaults. */
		function presetIsDefault(preset) {
			return DEFAULT_PRESETS.some((candidate) => candidate.id === preset.id);
		}
		/** Human cron hint for the manager rows ('' when invalid — the raw expression
		*  is noise there; the row already shows it in its own input). */
		function presetCronHint(cron) {
			return cronHumanLabel(cron, "");
		}
		/** One editable row: name + cron + save/delete. */
		function PresetRow({ preset, onSave, onDelete }) {
			const [label, setLabel] = (0, react.useState)(preset.label);
			const [cron, setCron] = (0, react.useState)(preset.cron);
			const dirty = label.trim() !== preset.label || cron.trim() !== preset.cron;
			const valid = isValidCron(cron);
			const hint = presetCronHint(cron);
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("li", {
				className: board_module_css_default.presetRow,
				children: [
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)("input", {
						className: `${board_module_css_default.input} ${board_module_css_default.presetName}`,
						value: label,
						"aria-label": t("detail.schedule.presets.name"),
						onChange: (event) => {
							setLabel(event.target.value);
						}
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.presetCronWrap,
						children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("input", {
							className: `${board_module_css_default.input} ${board_module_css_default.presetCron}${!valid ? ` ${board_module_css_default.inputInvalid}` : ""}`,
							value: cron,
							spellCheck: false,
							"aria-label": t("detail.schedule.cron"),
							onChange: (event) => {
								setCron(event.target.value);
							}
						})
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
						className: board_module_css_default.presetActions,
						children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
							className: board_module_css_default.presetRowAction,
							disabled: !dirty || !valid,
							onClick: () => {
								onSave({
									...preset,
									label: label.trim(),
									cron: cron.trim()
								});
							},
							children: t("detail.schedule.presets.save")
						}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
							className: board_module_css_default.presetRowAction,
							onClick: onDelete,
							children: t("detail.schedule.presets.delete")
						})]
					}),
					hint !== "" && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.presetHint,
						children: hint
					})
				]
			});
		}
		/** The preset manager overlay. */
		function PresetManager({ store, onClose }) {
			const [custom, setCustom] = (0, react.useState)(() => store.load());
			const [newLabel, setNewLabel] = (0, react.useState)("");
			const [newCron, setNewCron] = (0, react.useState)("");
			const [confirmRestore, setConfirmRestore] = (0, react.useState)(false);
			const [newError, setNewError] = (0, react.useState)(void 0);
			const persist = (next) => {
				setCustom(next);
				store.save(next);
			};
			/** Clear custom overrides and fall back to the built-in defaults. */
			const restoreDefaults = () => {
				store.clear();
				setCustom([]);
				setConfirmRestore(false);
			};
			const add = () => {
				const cron = newCron.trim();
				const label = newLabel.trim();
				if (label === "" || cron === "" || !isValidCron(cron)) {
					setNewError(t("detail.schedule.invalid"));
					return;
				}
				persist([...custom, {
					id: `custom-${Date.now().toString(36)}-${Math.random().toString(36).slice(2, 8)}`,
					label,
					cron
				}]);
				setNewLabel("");
				setNewCron("");
				setNewError(void 0);
			};
			const newHint = presetCronHint(newCron);
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(Dialog, {
				label: t("detail.schedule.presets.title"),
				onClose,
				title: t("detail.schedule.presets.title"),
				className: board_module_css_default.presetModal,
				portal: true,
				children: [
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
						className: board_module_css_default.modalScroll,
						children: [
							/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("ul", {
								className: board_module_css_default.presetList,
								children: [custom.length === 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("li", {
									className: board_module_css_default.presetEmpty,
									children: t("detail.schedule.presets.empty")
								}), custom.map((preset) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)(PresetRow, {
									preset,
									onSave: (next) => {
										persist(custom.map((candidate) => candidate.id === next.id ? next : candidate));
									},
									onDelete: () => {
										persist(custom.filter((candidate) => candidate.id !== preset.id));
									}
								}, preset.id))]
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
								className: board_module_css_default.presetNew,
								children: [
									/* @__PURE__ */ (0, react_jsx_runtime.jsx)("input", {
										className: `${board_module_css_default.input} ${board_module_css_default.presetName}`,
										value: newLabel,
										placeholder: t("detail.schedule.presets.newName"),
										"aria-label": t("detail.schedule.presets.name"),
										onChange: (event) => {
											setNewLabel(event.target.value);
											setNewError(void 0);
										}
									}),
									/* @__PURE__ */ (0, react_jsx_runtime.jsx)("input", {
										className: `${board_module_css_default.input} ${board_module_css_default.presetCron}${newCron.trim() !== "" && !isValidCron(newCron) ? ` ${board_module_css_default.inputInvalid}` : ""}`,
										value: newCron,
										placeholder: t("detail.schedule.presets.newCron"),
										spellCheck: false,
										"aria-label": t("detail.schedule.cron"),
										onChange: (event) => {
											setNewCron(event.target.value);
											setNewError(void 0);
										},
										onKeyDown: (event) => {
											if (event.key === "Enter") add();
										}
									}),
									/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
										variant: "primary",
										className: board_module_css_default.presetRowAction,
										onClick: add,
										children: t("detail.schedule.presets.add")
									})
								]
							}),
							newError !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
								className: board_module_css_default.formError,
								children: newError
							}),
							newHint !== "" && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
								className: board_module_css_default.scheduleMeta,
								children: newHint
							})
						]
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("footer", {
						className: board_module_css_default.modalFooter,
						children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
							onClick: () => {
								setConfirmRestore(true);
							},
							children: t("detail.schedule.presets.restore")
						}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
							onClick: onClose,
							children: t("detail.cancel")
						})]
					}),
					confirmRestore && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(ConfirmDialog, {
						title: t("detail.schedule.presets.restore"),
						message: t("detail.schedule.presets.restoreConfirm"),
						confirmLabel: t("detail.schedule.presets.restore"),
						danger: true,
						onCancel: () => {
							setConfirmRestore(false);
						},
						onConfirm: restoreDefaults
					})
				]
			});
		}
		//#endregion
		//#region src/client/board/automation-ui.tsx
		/**
		* Shared automation UI — the ONE rule grammar for every automation surface.
		* The task detail's 自动化 section and the board header's 自动化 overview
		* render the same rows and the same add/edit form, so no surface can carry a
		* second, weaker copy of a feature again. Everything reads the single read
		* projection (automationRowsOf) and the shared readiness semantics
		* (sessionRuleReadiness — the same column judgment as the task schedule).
		*
		* Contains:
		* - CronField: the one cron input/preset grammar (the schedule editor and
		*   the session-rule form share it, so the expressions always speak alike);
		* - SessionRuleRows: one row per session rule (identity + instruction +
		*   trigger + send mode + readiness + enable/edit/delete);
		* - SessionRuleForm: the ONE add/edit form for a session rule (the "给某个
		*   会话进行自动化" surface — full capability, never a reduced clone);
		* - SessionRulesSection: rows + form toggled in place, the only surface
		*   needing composition (used by the detail and the overview verbatim);
		* - AutomationEditor: THE one task-automation editor — task-level schedule
		*   (cron / after-completion chain) + the session-rules section, rendered by
		*   the detail's disclosure AND the board overview's task card verbatim.
		*/
		/**
		* The one cron grammar: text input + preset dropdown (+ optional preset
		* manager). The caller owns value/validation/commit time:
		* - onCommit fires on Enter/blur (the schedule editor persists that way);
		* - onPreset fires when a preset is picked (schedule editor = 即改即生效);
		*   without it the pick only sets the value (session form = save on 保存).
		*/
		function CronField({ value, presets, onChange, onCommit, onPreset, onManagePresets, invalid, label }) {
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
				className: board_module_css_default.scheduleCronRow,
				children: [
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)("input", {
						className: `${board_module_css_default.input} ${board_module_css_default.scheduleInput}${invalid === true ? ` ${board_module_css_default.inputInvalid}` : ""}`,
						value,
						placeholder: "0 9 * * *",
						spellCheck: false,
						"aria-label": label,
						onChange: (event) => {
							onChange(event.target.value);
						},
						onBlur: () => {
							onCommit?.(value);
						},
						onKeyDown: (event) => {
							if (event.key === "Enter") onCommit?.(value);
						}
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.selectWrap,
						children: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("select", {
							className: board_module_css_default.schedulePreset,
							value: "",
							"aria-label": t("detail.schedule.presets"),
							onChange: (event) => {
								if (event.target.value === "") return;
								if (onPreset !== void 0) onPreset(event.target.value);
								else onChange(event.target.value);
							},
							children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("option", {
								value: "",
								children: [t("detail.schedule.presets"), "…"]
							}), presets.map((preset) => /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("option", {
								value: preset.cron,
								children: [preset.label, !presetIsDefault(preset) ? ` (${t("detail.schedule.presets.custom")})` : ""]
							}, preset.id))]
						})
					}),
					onManagePresets !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
						className: board_module_css_default.scheduleManageButton,
						onClick: onManagePresets,
						children: t("detail.schedule.managePresets")
					})
				]
			});
		}
		/** The target session's display title (falls back to the raw id). */
		function ruleSessionTitle(controller, task, sessionId) {
			return controller.sessionLabelsOf(task.id).find((item) => item.sessionId === sessionId)?.title ?? sessionId;
		}
		/** THE one schedule-summary text (off / blocked-cause / paused(reason) /
		*  chain runs / cron human label + next instant), read by the detail's
		*  disclosure header, the automation overview's row and the card's tooltip —
		*  one grammar, three surfaces, never three branch sets. The failure word and
		*  the blocked cause derive inside (single derivation): callers pass the task
		*  and nothing else, so the three surfaces can never disagree. */
		function scheduleSummary(task) {
			const schedule = task.schedule;
			if (schedule === void 0 || !schedule.enabled) return t("detail.schedule.off");
			const failed = pausedFailedOf(task);
			const cause = blockedCauseOf(task);
			if (cause !== void 0) {
				const reason = cause === "session" ? `${t("card.autoBlocked")} · ${t("detail.schedule.blockedSession")}` : `${t("card.autoBlocked")} · ${t("detail.schedule.blocked")}`;
				return failed ? `${reason} · ${t("detail.schedule.pausedFailedShort")}` : reason;
			}
			const readiness = ruleReadiness(task);
			if (readiness.kind === "paused") return failed ? `${t("detail.schedule.paused")} · ${t("detail.schedule.pausedFailedShort")}` : `${t("detail.schedule.paused")} (${t(STATUS_KEY[readiness.status])})`;
			if (schedule.mode === "chain") {
				const budget = schedule.maxRuns;
				return `${t("detail.schedule.mode.chain")} · ${t("detail.schedule.runsSoFar")} ${schedule.runCount}${budget !== void 0 ? `/${budget}` : ""}`;
			}
			const next = schedule.nextRunAt;
			const nextLabel = next === void 0 ? t("detail.schedule.notScheduled") : next <= Date.now() ? t("detail.schedule.dueSoon") : formatDateTime(next);
			return `${t("detail.schedule.mode.cron")} · ${cronHumanLabel(schedule.cron ?? "")} · ${nextLabel}`;
		}
		/** One session-rule row (readiness / enable / edit / delete). */
		function SessionRuleRow({ task, controller, row, onEdit }) {
			const readiness = sessionRuleReadiness(task, sessionRuleOf(row));
			const title = ruleSessionTitle(controller, task, row.sessionId);
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("li", {
				className: board_module_css_default.autoRuleRow,
				children: [
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
						className: board_module_css_default.autoRuleTop,
						children: [
							/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
								className: board_module_css_default.autoRuleSession,
								children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, {
									name: "link",
									className: board_module_css_default.autoMetaIcon
								}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
									className: board_module_css_default.autoRuleSessionTitle,
									title,
									children: title
								})]
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Chip, {
								kind: "muted",
								title: t(row.send === "queue" ? "review.sendQueueTitle" : "review.sendSteerTitle"),
								children: t(row.send === "queue" ? "review.sendQueue" : "review.sendSteer")
							}),
							readiness.kind === "active" && row.trigger === "on-complete" && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Chip, {
								kind: "muted",
								fill: false,
								title: t("auto.rule.awaitingTitle"),
								children: t("auto.rule.awaiting")
							}),
							readiness.kind === "paused" && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(Chip, {
								kind: "warn",
								fill: false,
								children: [
									t("auto.schedule.paused"),
									" (",
									t(STATUS_KEY[readiness.status]),
									")"
								]
							}),
							readiness.kind === "disabled" && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Chip, {
								kind: "muted",
								fill: false,
								children: t("auto.rule.off")
							}),
							readiness.kind === "blocked" && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Chip, {
								kind: "muted",
								fill: false,
								children: t("detail.schedule.blocked")
							})
						]
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.autoRuleInstruction,
						title: row.usePrompt === true ? t("auto.content.usePrompt") : row.instruction,
						children: row.usePrompt === true ? t("auto.content.usePrompt") : row.instruction
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.autoRuleMeta,
						title: row.trigger === "cron" ? row.cron : void 0,
						children: row.trigger === "cron" ? /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [
							t("auto.cron"),
							" ",
							cronHumanLabel(row.cron),
							row.nextAt !== void 0 && row.nextAt > Date.now() && ` · ${t("auto.rule.next", { time: formatDateTime(row.nextAt) })}`
						] }) : t("auto.rule.onComplete")
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
						className: board_module_css_default.autoRuleActions,
						children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Switch, {
							checked: row.enabled,
							onChange: (next) => {
								controller.toggleSessionRule(task.id, row.ruleId, next);
							},
							label: t("auto.rule.enable")
						}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
							className: board_module_css_default.autoRuleButtons,
							children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
								size: "sm",
								title: t("auto.rule.editTitle"),
								onClick: onEdit,
								children: t("auto.rule.edit")
							}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
								size: "sm",
								variant: "dangerGhost",
								title: t("auto.rule.deleteTitle"),
								onClick: () => {
									controller.deleteSessionRule(task.id, row.ruleId);
								},
								children: t("auto.rule.delete")
							})]
						})]
					})
				]
			});
		}
		/** The ONE session-rule form (add + edit). Editing locks the target session
		*  (the rule's identity is its session; a changed target is a new rule —
		*  delete + add — and the form never hides that fact). */
		function SessionRuleForm({ task, controller, ruleId, onClose }) {
			const labels = controller.sessionLabelsOf(task.id);
			const existing = ruleId !== void 0 ? (task.rules ?? []).find((rule) => rule.id === ruleId) : void 0;
			const draftKey = ruleDraftKey(task.id, ruleId ?? "new");
			const draftInstruction = ruleId === void 0 ? draftStore.get(draftKey) ?? "" : "";
			const [sessionId, setSessionId] = (0, react.useState)(existing?.sessionId ?? labels[0]?.sessionId ?? "");
			const [instruction, setInstruction] = (0, react.useState)(draftInstruction !== "" ? draftInstruction : existing?.instruction ?? "");
			const [trigger, setTrigger] = (0, react.useState)(existing?.trigger ?? "cron");
			const [cron, setCron] = (0, react.useState)(existing?.trigger === "on-complete" ? "0 9 * * *" : existing?.cron ?? "0 9 * * *");
			const [usePrompt, setUsePrompt] = (0, react.useState)(existing?.usePrompt === true);
			const [steer, setSteer] = (0, react.useState)(existing?.send === "steer");
			const [error, setError] = (0, react.useState)(void 0);
			const [presetStore] = (0, react.useState)(() => controller.presetStore());
			const [presets, setPresets] = (0, react.useState)(() => mergedPresets(presetStore));
			const [showPresets, setShowPresets] = (0, react.useState)(false);
			const options = existing !== void 0 && !labels.some((label) => label.sessionId === existing.sessionId) ? [{
				sessionId: existing.sessionId,
				title: existing.sessionId
			}, ...labels] : labels;
			const submit = () => {
				const trimmedInstruction = instruction.trim();
				const trimmedCron = cron.trim();
				if (sessionId === "") {
					setError("session");
					return;
				}
				if (!usePrompt && trimmedInstruction === "") {
					setError("instruction");
					return;
				}
				if (trigger === "cron" && !isValidCron(trimmedCron)) {
					setError("cron");
					return;
				}
				const send = steer ? "steer" : "queue";
				const input = {
					sessionId,
					instruction: usePrompt ? "" : trimmedInstruction,
					trigger,
					...usePrompt ? { usePrompt: true } : {},
					cron: trigger === "cron" ? trimmedCron : "",
					send
				};
				if (existing !== void 0) {
					if (!controller.updateSessionRule(task.id, existing.id, input)) {
						setError("save");
						return;
					}
				} else if (controller.createSessionRule(task.id, input) === void 0) {
					setError("save");
					return;
				}
				draftStore.clear(draftKey);
				onClose();
			};
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
				className: board_module_css_default.autoForm,
				children: [labels.length === 0 && existing === void 0 ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
					className: board_module_css_default.detailHint,
					children: t("auto.form.noSession")
				}) : /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("label", {
						className: board_module_css_default.autoField,
						children: [
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
								className: board_module_css_default.autoFieldLabel,
								children: t("auto.form.session")
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
								className: board_module_css_default.selectWrap,
								children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("select", {
									className: `${board_module_css_default.input}${error === "session" ? ` ${board_module_css_default.inputInvalid}` : ""}`,
									value: sessionId,
									disabled: existing !== void 0,
									"aria-label": t("auto.form.session"),
									"aria-invalid": error === "session" ? true : void 0,
									onChange: (event) => {
										setSessionId(event.target.value);
										setError(void 0);
									},
									children: options.map((label) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)("option", {
										value: label.sessionId,
										children: label.title
									}, label.sessionId))
								})
							}),
							error === "session" && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
								className: board_module_css_default.formError,
								children: t("auto.form.invalidSession")
							})
						]
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("label", {
						className: board_module_css_default.autoField,
						children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
							className: board_module_css_default.autoFieldLabel,
							children: t("auto.form.trigger")
						}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Segmented, {
							ariaLabel: t("auto.form.trigger"),
							options: [{
								value: "cron",
								label: t("auto.trigger.cron")
							}, {
								value: "on-complete",
								label: t("auto.trigger.onComplete")
							}],
							value: trigger,
							onChange: (next) => {
								setTrigger(next);
								setError(void 0);
							}
						})]
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("label", {
						className: board_module_css_default.autoField,
						children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
							className: board_module_css_default.autoFieldLabel,
							children: t("auto.form.content")
						}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Segmented, {
							ariaLabel: t("auto.form.content"),
							options: [{
								value: "custom",
								label: t("auto.content.custom")
							}, {
								value: "usePrompt",
								label: t("auto.content.usePrompt")
							}],
							value: usePrompt ? "usePrompt" : "custom",
							onChange: (next) => {
								setUsePrompt(next === "usePrompt");
								setError(void 0);
							}
						})]
					}),
					!usePrompt ? /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("label", {
						className: board_module_css_default.autoField,
						children: [
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
								className: board_module_css_default.autoFieldLabel,
								children: t("auto.form.instruction")
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)(PromptInput, {
								value: instruction,
								onChange: (next) => {
									setInstruction(next);
									setError(void 0);
									draftStore.set(draftKey, next);
								},
								placeholder: t("auto.form.instructionPlaceholder"),
								rows: 3,
								controller,
								sessionId: sessionId === "" ? void 0 : sessionId,
								invalid: error === "instruction"
							}),
							error === "instruction" && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
								className: board_module_css_default.formError,
								children: t("auto.form.invalidInstruction")
							})
						]
					}) : /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
						className: board_module_css_default.detailHint,
						children: t("auto.form.usePromptHint")
					}),
					trigger === "cron" && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("label", {
						className: board_module_css_default.autoField,
						children: [
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
								className: board_module_css_default.autoFieldLabel,
								children: t("auto.form.cron")
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)(CronField, {
								value: cron,
								presets,
								invalid: error === "cron",
								onChange: (next) => {
									setCron(next);
									setError(void 0);
								},
								onManagePresets: () => {
									setShowPresets(true);
								},
								label: t("auto.form.cron")
							}),
							error === "cron" && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
								className: board_module_css_default.formError,
								children: t("auto.form.invalidCron")
							})
						]
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
						className: board_module_css_default.autoField,
						children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
							className: board_module_css_default.autoFieldLabel,
							children: t("auto.form.send")
						}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(SendModeToggle, {
							steer,
							onChange: setSteer
						})]
					}),
					error === "save" && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
						className: board_module_css_default.formError,
						children: t("auto.form.saveFailed")
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
						className: board_module_css_default.detailHint,
						children: t(trigger === "cron" ? "auto.form.hint" : "auto.form.hintLoop")
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
						className: board_module_css_default.autoFormActions,
						children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
							size: "sm",
							variant: "primary",
							onClick: submit,
							children: t("auto.form.save")
						}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
							size: "sm",
							onClick: onClose,
							children: t("detail.cancel")
						})]
					})
				] }), showPresets && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(PresetManager, {
					store: presetStore,
					onClose: () => {
						setShowPresets(false);
						setPresets(mergedPresets(presetStore));
					}
				})]
			});
		}
		/**
		* The session-rules module: one row per rule + the add/edit form in place —
		* an empty task shows only the 新增 affordance. Panel and detail render this
		* verbatim; editing a rule (the form with the full capability) can never
		* diverge between surfaces again.
		*/
		function SessionRulesSection({ controller, task }) {
			const [formKey, setFormKey] = (0, react.useState)(void 0);
			const rows = automationRowsOf(task).filter((row) => row.kind === "session-rule");
			const editable = controller.sessionLabelsOf(task.id).length > 0;
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(Section, {
				title: t("auto.rules"),
				className: board_module_css_default.autoRules,
				action: formKey === void 0 ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
					size: "sm",
					disabled: !editable,
					onClick: () => {
						setFormKey("new");
					},
					children: t("auto.form.new")
				}) : void 0,
				children: [rows.length > 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("ul", {
					className: board_module_css_default.autoRuleList,
					children: rows.map((row) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)(SessionRuleRow, {
						task,
						controller,
						row,
						onEdit: () => {
							setFormKey(row.ruleId);
						}
					}, row.ruleId))
				}), formKey !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(SessionRuleForm, {
					task,
					controller,
					ruleId: formKey === "new" ? void 0 : formKey,
					onClose: () => {
						setFormKey(void 0);
					}
				})]
			});
		}
		/**
		* THE one task-automation editor — the task-level schedule (cron /
		* after-completion chain: switch + driving mode + fields + live meta +
		* skip/stop, 即改即生效) and then the session rules (SessionRulesSection,
		* the same shared module).
		*
		* The task detail's 自动化 disclosure and the board's 自动化 overview task
		* cards render THIS verbatim — the board therefore has the SAME full
		* capability as the detail (including 完成后接续), and no surface can ever
		* drift into a reduced second editor. The two side-effect confirms (arming /
		* stopping an unlimited chain) live here so both surfaces share the same
		* gate (chainUnlimited).
		*/
		function AutomationEditor({ controller, task, embedded = false }) {
			const schedule = task.schedule;
			const [cron, setCron] = (0, react.useState)(schedule?.cron || "0 9 * * *");
			const [enabled, setEnabled] = (0, react.useState)(schedule?.enabled ?? false);
			const [mode, setMode] = (0, react.useState)(schedule?.mode ?? "cron");
			const [maxRuns, setMaxRuns] = (0, react.useState)(schedule?.maxRuns?.toString() ?? "");
			const [nextRunAt, setNextRunAt] = (0, react.useState)(schedule?.nextRunAt);
			const [lastTriggeredAt, setLastTriggeredAt] = (0, react.useState)(schedule?.lastTriggeredAt);
			const [error, setError] = (0, react.useState)(void 0);
			const [showPresets, setShowPresets] = (0, react.useState)(false);
			const [presetStore] = (0, react.useState)(() => controller.presetStore());
			const [presets, setPresets] = (0, react.useState)(() => mergedPresets(presetStore));
			const [confirm, setConfirm] = (0, react.useState)(void 0);
			(0, react.useEffect)(() => {
				setCron(schedule?.cron || "0 9 * * *");
				setEnabled(schedule?.enabled ?? false);
				setMode(schedule?.mode ?? "cron");
				setMaxRuns(schedule?.maxRuns?.toString() ?? "");
				setNextRunAt(schedule?.nextRunAt);
				setLastTriggeredAt(schedule?.lastTriggeredAt);
				setError(void 0);
			}, [
				task.id,
				schedule?.enabled,
				schedule?.mode,
				schedule?.cron,
				schedule?.nextRunAt,
				schedule?.lastTriggeredAt,
				schedule?.maxRuns,
				schedule?.runCount
			]);
			/** Validate + persist the current cron text (Enter or blur). */
			const saveCron = (value) => {
				const trimmed = value.trim();
				setCron(trimmed);
				if (trimmed === "" || !isValidCron(trimmed)) {
					setError("cron");
					return;
				}
				setError(void 0);
				controller.setSchedule(task.id, { cron: trimmed });
			};
			/** Persist the run budget (blank = unlimited). */
			const saveMaxRuns = (value) => {
				const trimmed = value.trim();
				setMaxRuns(trimmed);
				if (trimmed === "") {
					controller.setSchedule(task.id, { maxRuns: void 0 });
					return;
				}
				const parsed = Number(trimmed);
				if (!Number.isInteger(parsed) || parsed < 1) {
					setError("runs");
					return;
				}
				setError(void 0);
				controller.setSchedule(task.id, { maxRuns: parsed });
			};
			/** Arm/disarm the schedule. Arming a chain without a run budget (unlimited)
			*  risks an endless loop of real agent sessions — confirm once first. */
			const applyEnabled = (next) => {
				if (next && mode === "cron" && cron.trim() !== schedule?.cron) controller.setSchedule(task.id, { cron: cron.trim() });
				if (!controller.setSchedule(task.id, {
					enabled: next,
					mode
				})) {
					if (next && mode === "chain") setError("promptEmpty");
					return;
				}
				setEnabled(next);
			};
			/** Arm/disarm the schedule (arming first persists the edited cron). */
			const toggleEnabled = (next) => {
				const trimmed = cron.trim();
				if (next && mode === "cron" && (trimmed === "" || !isValidCron(trimmed))) {
					setError("cron");
					return;
				}
				setError(void 0);
				if (next && chainUnlimited(mode, maxRuns.trim() === "" ? void 0 : Number(maxRuns))) {
					setConfirm("unlimited-enable");
					return;
				}
				applyEnabled(next);
			};
			/** Switch the driving mode (cron ↔ chain). Switching back to cron first
			*  persists the editor's current expression, so the stored rule is never
			*  left with an empty cron (which cron mode would reject). */
			const switchMode = (next) => {
				if (next === mode) return;
				if (next === "cron" && (cron.trim() === "" || !isValidCron(cron))) {
					setError("cron");
					return;
				}
				setError(void 0);
				setMode(next);
				if (next === "cron" && cron.trim() !== schedule?.cron) controller.setSchedule(task.id, { cron: cron.trim() });
				if (controller.setSchedule(task.id, { mode: next })) controller.setSchedule(task.id, {
					enabled,
					mode: next
				});
			};
			/** Stop an active chain: only the automation stops (enabled:false — the
			*  card's column is untouched). Unlimited chains confirm once (endless
			*  loop of real agent sessions is a big side effect). */
			const stopChain = () => {
				if (chainUnlimited(mode, maxRuns.trim() === "" ? void 0 : Number(maxRuns))) {
					setConfirm("stop-chain");
					return;
				}
				if (controller.setSchedule(task.id, {
					enabled: false,
					mode
				})) setEnabled(false);
			};
			const applyStopChain = () => {
				if (controller.setSchedule(task.id, {
					enabled: false,
					mode
				})) setEnabled(false);
			};
			/** Skip the next cron firing: roll nextRunAt forward to the following
			*  match while keeping the rule armed — a "defer once", never a catch-up. */
			const skipNext = () => {
				if (mode !== "cron" || nextRunAt === void 0) return;
				const next = nextRunAtMs(cron, nextRunAt);
				if (next === void 0) return;
				setNextRunAt(next);
				controller.applyScheduleNextRun(task.id, next, lastTriggeredAt);
			};
			const applyPreset = (preset) => {
				if (preset === "") return;
				setCron(preset);
				setError(void 0);
				controller.setSchedule(task.id, { cron: preset });
			};
			const closePresets = () => {
				setShowPresets(false);
				setPresets(mergedPresets(presetStore));
			};
			const readiness = ruleReadiness(task);
			const chainRuns = schedule?.runCount ?? 0;
			const chainBudget = schedule?.maxRuns;
			const lastLabel = lastTriggeredAt === void 0 ? "—" : formatDateTime(lastTriggeredAt);
			const canSkip = enabled && mode === "cron" && readiness.kind === "active" && nextRunAt !== void 0 && nextRunAt > Date.now();
			const stoppedReason = readiness.kind === "paused" ? {
				extraFailed: readiness.status === "review" && lastPlainResult(task) === "failed",
				key: PAUSED_REASON_KEY[readiness.status]
			} : readiness.kind === "blocked" ? {
				extraFailed: false,
				key: "detail.schedule.blocked"
			} : void 0;
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [
				/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Section, {
					title: embedded ? "" : t("auto.taskSchedule"),
					className: board_module_css_default.autoRules,
					children: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
						className: board_module_css_default.autoGroup,
						children: [
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Switch, {
								checked: enabled,
								onChange: toggleEnabled,
								label: t("detail.schedule.enable")
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Segmented, {
								ariaLabel: t("detail.schedule"),
								options: [{
									value: "cron",
									label: t("detail.schedule.mode.cron"),
									title: t("detail.schedule.mode.cronHint")
								}, {
									value: "chain",
									label: t("detail.schedule.mode.chain"),
									title: t("detail.schedule.mode.chainHint")
								}],
								value: mode,
								onChange: (next) => {
									switchMode(next);
								}
							}),
							mode === "cron" ? /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
								className: board_module_css_default.scheduleGrid,
								children: [
									/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
										className: board_module_css_default.scheduleLabel,
										children: t("detail.schedule.cron")
									}),
									/* @__PURE__ */ (0, react_jsx_runtime.jsx)(CronField, {
										value: cron,
										presets,
										invalid: error === "cron",
										onChange: (next) => {
											setCron(next);
											setError(void 0);
										},
										onCommit: saveCron,
										onPreset: applyPreset,
										onManagePresets: () => {
											setShowPresets(true);
										},
										label: t("detail.schedule.cron")
									}),
									/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
										className: board_module_css_default.scheduleLabel,
										children: t("detail.schedule.maxRuns")
									}),
									/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
										className: board_module_css_default.scheduleMaxRow,
										children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("input", {
											className: `${board_module_css_default.input} ${board_module_css_default.scheduleMaxInput}${error === "runs" ? ` ${board_module_css_default.inputInvalid}` : ""}`,
											value: maxRuns,
											type: "number",
											min: 1,
											placeholder: "∞",
											spellCheck: false,
											"aria-label": t("detail.schedule.maxRuns"),
											"aria-invalid": error === "runs" ? true : void 0,
											onChange: (event) => {
												setMaxRuns(event.target.value);
												setError(void 0);
											},
											onBlur: () => {
												saveMaxRuns(maxRuns);
											},
											onKeyDown: (event) => {
												if (event.key === "Enter") saveMaxRuns(maxRuns);
											}
										}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
											className: board_module_css_default.scheduleMeta,
											children: [
												t("detail.schedule.runsSoFar"),
												" ",
												schedule?.runCount ?? 0,
												schedule?.maxRuns !== void 0 && ` / ${schedule.maxRuns}`
											]
										})]
									})
								]
							}) : /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [
								/* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
									className: board_module_css_default.scheduleMeta,
									children: t("detail.schedule.chainNote")
								}),
								/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
									className: board_module_css_default.scheduleActionRow,
									children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
										className: board_module_css_default.scheduleMeta,
										children: [
											t("detail.schedule.runsSoFar"),
											" ",
											chainRuns,
											chainBudget !== void 0 ? ` / ${chainBudget}` : ` · ${t("detail.schedule.unlimited")}`
										]
									}), enabled && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
										size: "sm",
										variant: "ghost",
										onClick: stopChain,
										children: t("detail.schedule.stopChain")
									})]
								}),
								stoppedReason !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("p", {
									className: board_module_css_default.scheduleMeta,
									children: [stoppedReason.extraFailed && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [t("detail.schedule.pausedFailed"), " "] }), t(stoppedReason.key)]
								}),
								/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
									className: board_module_css_default.scheduleGrid,
									children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
										className: board_module_css_default.scheduleLabel,
										children: t("detail.schedule.maxRuns")
									}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
										className: board_module_css_default.scheduleMaxRow,
										children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("input", {
											className: `${board_module_css_default.input} ${board_module_css_default.scheduleMaxInput}${error === "runs" ? ` ${board_module_css_default.inputInvalid}` : ""}`,
											value: maxRuns,
											type: "number",
											min: 1,
											placeholder: "∞",
											spellCheck: false,
											"aria-label": t("detail.schedule.maxRuns"),
											"aria-invalid": error === "runs" ? true : void 0,
											onChange: (event) => {
												setMaxRuns(event.target.value);
												setError(void 0);
											},
											onBlur: () => {
												saveMaxRuns(maxRuns);
											},
											onKeyDown: (event) => {
												if (event.key === "Enter") saveMaxRuns(maxRuns);
											}
										}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
											className: board_module_css_default.scheduleMeta,
											children: [
												t("detail.schedule.runsSoFar"),
												" ",
												schedule?.runCount ?? 0,
												schedule?.maxRuns !== void 0 && ` / ${schedule.maxRuns}`
											]
										})]
									})]
								})
							] }),
							error !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
								className: board_module_css_default.formError,
								children: t(error === "cron" ? "detail.schedule.invalid" : error === "runs" ? "detail.schedule.invalidRuns" : "detail.promptEmpty")
							}),
							mode === "cron" && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [(() => {
								if (readiness.kind === "paused" || readiness.kind === "blocked") return null;
								const when = [];
								if (nextRunAt !== void 0) when.push(nextRunAt > Date.now() ? `${t("detail.schedule.nextRun")} ${formatDateTime(nextRunAt)}` : t("detail.schedule.dueSoon"));
								if (lastTriggeredAt !== void 0) when.push(`${t("detail.schedule.lastTriggered")} ${lastLabel}`);
								if (when.length === 0 && !canSkip) return null;
								return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
									className: board_module_css_default.scheduleActionRow,
									children: [when.length > 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
										className: board_module_css_default.scheduleMeta,
										children: when.join(" · ")
									}), canSkip && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
										size: "sm",
										variant: "ghost",
										onClick: skipNext,
										children: t("detail.schedule.skip")
									})]
								});
							})(), stoppedReason !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("p", {
								className: board_module_css_default.scheduleMeta,
								children: [stoppedReason.extraFailed && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [t("detail.schedule.pausedFailed"), " "] }), t(stoppedReason.key)]
							})] })
						]
					})
				}),
				/* @__PURE__ */ (0, react_jsx_runtime.jsx)(SessionRulesSection, {
					controller,
					task
				}),
				showPresets && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(PresetManager, {
					store: presetStore,
					onClose: closePresets
				}),
				confirm === "unlimited-enable" && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(ConfirmDialog, {
					title: t("detail.schedule.unlimitedTitle"),
					message: t("detail.schedule.unlimitedConfirm"),
					confirmLabel: t("detail.schedule.unlimitedOk"),
					danger: true,
					onCancel: () => {
						setConfirm(void 0);
					},
					onConfirm: () => {
						setConfirm(void 0);
						applyEnabled(true);
					}
				}),
				confirm === "stop-chain" && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(ConfirmDialog, {
					title: t("detail.schedule.stopChainTitle"),
					message: t("detail.schedule.stopChainConfirm"),
					confirmLabel: t("detail.schedule.stopChain"),
					danger: true,
					onCancel: () => {
						setConfirm(void 0);
					},
					onConfirm: () => {
						setConfirm(void 0);
						applyStopChain();
					}
				})
			] });
		}
		//#endregion
		//#region src/client/board/card-view.ts
		/** Display title: the raw title, or the untitled placeholder when blank.
		*  THE one blank-title judgment — card, board rows, detail header and delete
		*  confirms all read it, so a blank card can never show different faces per
		*  surface. Pure. */
		function titleOrUntitled(title, untitled) {
			return title.trim() === "" ? untitled : title;
		}
		/**
		* Derive the card's view-model. `live` is the native truth
		* (controller.liveStateOf); absent = status fallback (card without
		* controller). `sessionStateOf` resolves per-session dots (waiting >
		* running > idle); absent = no dots.
		*/
		function cardViewModelOf(task, opts = {}) {
			const running = hasOpenRun(task);
			const showingRunning = executing(task);
			const queued = pendingCommentCount(task);
			const runs = plainRunsOf(task);
			const lastResult = lastPlainResult(task);
			const isRefining = refining(task);
			const liveRunning = (opts.live ?? (task.status === "running" ? "running" : "idle")) === "running";
			const pendingCount = opts.pendingCount ?? 0;
			const active = liveRunning || pendingCount > 0 || isRefining;
			let primary;
			if (opts.waiting !== void 0) primary = {
				kind: "waiting",
				waiting: opts.waiting
			};
			else if (showingRunning) primary = { kind: "running" };
			else if (isRefining) primary = { kind: "refining" };
			else if (queued > 0) primary = {
				kind: "queued",
				count: queued
			};
			else if (task.status === "review" && lastResult === "failed") primary = { kind: "failed" };
			else if (task.status === "review") primary = {
				kind: "review",
				unviewed: (opts.unviewedCount ?? 0) > 0
			};
			else primary = { kind: "idle" };
			const dots = [];
			const ids = opts.sessionIds ?? [];
			const resolve = opts.sessionStateOf;
			if (resolve !== void 0) {
				const seen = /* @__PURE__ */ new Set();
				for (const sessionId of ids) {
					if (seen.has(sessionId)) continue;
					seen.add(sessionId);
					dots.push({
						sessionId,
						state: resolve(sessionId)
					});
					if (dots.length >= 3) break;
				}
			}
			const overflowDots = resolve !== void 0 ? Math.max(0, new Set(ids).size - dots.length) : 0;
			return {
				primary,
				runCount: runs.length,
				lastResult,
				queued,
				unviewedCount: opts.unviewedCount ?? 0,
				awaitingDecision: task.status === "review" && lastResult !== void 0,
				active,
				showingRunning,
				running,
				dots,
				overflowDots
			};
		}
		/**
		* One quiet "what's next" sentence for the card (scanning aid, never a
		* second status system — it names the same primary the chips already show,
		* plus the schedule horizon when armed). Returns undefined for idle cards
		* with nothing scheduled (no noise). The caller localizes the template;
		* this returns the structured fact so copy lives in one place.
		*/
		function cardNextActionOf(view, task) {
			switch (view.primary.kind) {
				case "waiting": return { kind: "waiting" };
				case "running": return { kind: "running" };
				case "refining": return { kind: "refining" };
				case "queued": return {
					kind: "queued",
					count: view.primary.count
				};
				case "failed": return { kind: "failed" };
				case "review": return view.primary.unviewed ? { kind: "review" } : void 0;
				case "idle":
					if (task.schedule?.enabled === true) return task.schedule.mode === "chain" ? { kind: "chain" } : { kind: "scheduled" };
					return;
			}
		}
		//#endregion
		//#region src/client/board/session-chip.ts
		/** The waiting-kind locale key, typed (never a string cast). */
		function waitingKeyOf(kind) {
			switch (kind) {
				case "approval": return "waiting.approval";
				case "plan-review": return "waiting.plan-review";
				case "question": return "waiting.question";
			}
		}
		/**
		* The result → chip COLOR mapping — the one place that decides an execution
		* result's tint (failed red, succeeded green, cancelled/unknown muted). A
		* surface that carries its OWN settled label (the card's "N 次执行", the
		* refine summary's result word) still takes its color from here, so the tint
		* can never drift from the state chip's.
		*/
		function resultChipKind(result) {
			return result === "failed" ? "error" : result === "succeeded" ? "success" : "muted";
		}
		/**
		* The one chip derivation: waiting/running are live (warn + spinner, the
		* waiting label names the interaction kind); succeeded/failed/cancelled are
		* settled and take the caller's label pair (`settled` for succeeded — the
		* execution result vs the linked activity, `idle` for cancelled — a bound
		* session's no-activity state, a run's cancelled result). `idleTitle` is the
		* hover meaning of the IDLE label — only a linked row passes it (未运行 needs
		* the one-line explanation; a run's 已取消 is self-evident). Locale-free
		* logic stays in session-display; this is the presentation mapping only.
		*/
		function sessionStateChip(state, waitingKind, settled, idle, idleTitle) {
			switch (state) {
				case "waiting": return waitingKind !== void 0 ? {
					kind: "warn",
					label: t(waitingKeyOf(waitingKind)),
					spinner: true
				} : {
					kind: "warn",
					label: t("detail.result.running"),
					spinner: true
				};
				case "running": return {
					kind: "warn",
					label: t("detail.result.running"),
					spinner: true
				};
				case "succeeded": return {
					kind: "success",
					label: t(settled)
				};
				case "failed": return {
					kind: "error",
					label: t("detail.result.failed")
				};
				case "cancelled": return idleTitle !== void 0 ? {
					kind: "muted",
					label: t(idle),
					title: t(idleTitle)
				} : {
					kind: "muted",
					label: t(idle)
				};
			}
		}
		//#endregion
		//#region src/client/board/TaskCard.tsx
		/**
		* Task card: the board's column item. Clicking opens the task detail — it
		* never executes anything directly (detail holds the Run button). Cards are
		* draggable onto other columns; the drop semantics are decided by the board
		* through resolveCardDrop.
		*/
		/** Tooltip for the schedule chip: THE one summary grammar (shared with the
		*  detail's disclosure and the overview) — honest about the rule's readiness:
		*  chain reports its run budget, cron its next due instant, paused its
		*  blocking status, blocked its cause (plus the failure word when the last
		*  run failed — two orthogonal causes, both named). */
		function scheduleChipTitle(task) {
			return scheduleSummary(task);
		}
		/** The open run's state text: either working ("进行中") or blocked on the
		*  user ("等待回应 · 计划确认"). Pure so the chip composition is testable. */
		function runningStateLabel(waiting) {
			return waiting !== void 0 ? `${t("card.waiting")} · ${t(waitingKeyOf(waiting))}` : t("detail.result.running");
		}
		/** The settled-run count label: "N 次执行" / "N runs". */
		function settledChipLabel(runs) {
			return `${runs} ${t("board.runs")}`;
		}
		/** Whether the card shows the blocked-automation chip: an armed rule that
		*  cannot drive anything (empty prompt — a reason, not a pause). Pure so the
		*  badge composition is testable; the title reuses the schedule summary
		*  grammar, which already names the blocking reason. */
		function showsBlockedChip(task) {
			return task.schedule?.enabled === true && ruleReadiness(task).kind === "blocked";
		}
		/** Whether any ENABLED session rule is blocked on the empty prompt (the
		*  session-rule twin of {@link showsBlockedChip}): a custom-instruction rule
		*  carries its own content and never reads the prompt, so only `usePrompt`
		*  rules count — read through the rule's own readiness, never re-derived. */
		function showsSessionBlocked(task) {
			if (task.rules === void 0) return false;
			return task.rules.some((rule) => sessionRuleReadiness(task, rule).kind === "blocked");
		}
		/** Whether the automation slot collapses to the single blocked chip: any
		*  blocked automation (task schedule or session rule) owns the slot — the
		*  schedule/chain/progress text yields instead of stacking four chips for
		*  one cause. THE priority table for the card's automation badges. */
		function blockedAutomation(task) {
			return showsBlockedChip(task) || showsSessionBlocked(task);
		}
		/** One card in a column — a PURE state summary: title, description, source
		*  line (workspace / bound session), the updated stamp, and the status chips
		*  (what the task IS doing: running / waiting / scheduled / chaining / failed
		*  paused / queued / refining / new). The run window (start/end/duration) and
		*  the comment timeline live in the detail — cards never carry content that
		*  belongs to the conversation pages. */
		function TaskCard({ task, selected, workspaceTitleOf, boundTitleOf, waiting, pendingCount, pendingTitle, unviewed, unviewedCount, hasUnviewedRun, awaitingDecision, onMoveStep, onClick, onQuickRun, onColorPick, live, dots, overflowDots, nextAction, dotTitleOf }) {
			const [dragging, setDragging] = (0, react.useState)(false);
			const latest = latestExecutionOf(task);
			const view = cardViewModelOf(task, {
				live,
				pendingCount,
				...waiting !== void 0 ? { waiting } : {},
				unviewedCount
			});
			const runs = plainRunsOf(task);
			const lastPlain = runs[runs.length - 1];
			const running = view.running;
			const showingRunning = view.showingRunning;
			const queuedComments = view.queued;
			const chaining = task.schedule?.enabled === true && task.schedule.mode === "chain" && task.status === "running";
			const sourceLabel = cardSourceLabel(task, taskBindsOf(task).some((bind) => bind.kind === "session") ? boundTitleOf?.(task) ?? "" : "", task.workspaceId !== void 0 ? workspaceTitleOf(task.workspaceId) : "");
			const readiness = ruleReadiness(task);
			const pausedFailed = readiness.kind === "paused" && readiness.status === "review" && lastPlain !== void 0 && lastPlain.result === "failed";
			const active = view.active;
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
				role: "button",
				tabIndex: 0,
				className: `${board_module_css_default.card}${dragging ? ` ${board_module_css_default.dragging}` : ""}${selected ? ` ${board_module_css_default.selectedCard}` : ""}`,
				style: task.color !== void 0 ? { "--card-tint": task.color } : void 0,
				"data-status": task.status,
				"data-task-id": task.id,
				"data-unviewed": unviewed ? "" : void 0,
				"data-active": active ? "" : void 0,
				draggable: true,
				onClick,
				onKeyDown: (event) => {
					if (onMoveStep !== void 0 && (event.key === "[" || event.key === "]")) {
						if (event.ctrlKey || event.metaKey || event.altKey) return;
						event.preventDefault();
						onMoveStep(event.key === "]" ? 1 : -1);
						return;
					}
					if (event.key === "Enter" || event.key === " ") {
						event.preventDefault();
						onClick(event);
					}
				},
				title: task.description !== "" ? task.description : task.title,
				"aria-label": onMoveStep === void 0 ? void 0 : t("card.keyboardLabel", {
					title: titleOrUntitled(task.title, t("card.untitled")),
					column: t(STATUS_KEY[task.status])
				}),
				onDragStart: (event) => {
					setDragging(true);
					event.dataTransfer.setData("text/plain", task.id);
					event.dataTransfer.effectAllowed = "move";
					const source = event.currentTarget;
					const clone = source.cloneNode(true);
					clone.style.position = "fixed";
					clone.style.left = "-9999px";
					clone.style.top = "0";
					clone.style.width = `${source.offsetWidth}px`;
					for (const hidden of clone.querySelectorAll("[data-ghost-hide]")) hidden.style.display = "none";
					document.body.appendChild(clone);
					event.dataTransfer.setDragImage(clone, source.offsetWidth / 2, source.offsetHeight / 2);
					window.setTimeout(() => {
						clone.remove();
					}, 0);
				},
				onDragEnd: () => {
					setDragging(false);
				},
				children: [
					onQuickRun !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.cardQuickRun,
						"data-ghost-hide": "",
						role: "button",
						tabIndex: running ? -1 : 0,
						title: t("card.quickRun"),
						"aria-disabled": running ? true : void 0,
						onClick: (event) => {
							if (running) return;
							event.stopPropagation();
							onQuickRun();
						},
						onKeyDown: (event) => {
							if (running) return;
							if (event.key === "Enter" || event.key === " ") {
								event.preventDefault();
								event.stopPropagation();
								onQuickRun();
							}
						},
						children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, { name: "play" })
					}),
					selected === true && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.cardSelectedBadge,
						"data-ghost-hide": "",
						"aria-hidden": "true",
						children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, { name: "check" })
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
						className: board_module_css_default.cardTitleRow,
						children: [task.color !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
							className: board_module_css_default.cardColorMark,
							style: { background: task.color },
							"aria-hidden": "true"
						}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
							className: board_module_css_default.cardTitle,
							children: titleOrUntitled(task.title, t("card.untitled"))
						})]
					}),
					task.description !== "" && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.cardExcerpt,
						children: task.description
					}),
					nextAction !== void 0 && nextAction !== "" && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.cardNext,
						children: nextAction
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
						className: board_module_css_default.cardMeta,
						children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
							className: board_module_css_default.cardMetaRow,
							children: [sourceLabel !== "" && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
								className: board_module_css_default.cardWorkspace,
								title: sourceLabel,
								children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
									className: board_module_css_default.cardWorkspaceDot,
									"aria-hidden": "true"
								}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
									className: board_module_css_default.cardWorkspaceName,
									children: sourceLabel
								})]
							}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
								className: board_module_css_default.cardTime,
								title: formatDateTime(task.updatedAt),
								children: [
									t("board.updated"),
									" ",
									formatTime(task.updatedAt)
								]
							})]
						}), (() => {
							const primaryChip = showingRunning ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Chip, {
								kind: "warn",
								fill: false,
								title: waiting !== void 0 ? t("card.waitingTitle", { kind: t(waitingKeyOf(waiting)) }) : void 0,
								icon: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
									className: board_module_css_default.spinner,
									"aria-hidden": "true"
								}),
								children: runningStateLabel(waiting)
							}) : pendingCount > 0 ? /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(Chip, {
								kind: "warn",
								fill: false,
								title: pendingTitle,
								children: [
									t("card.pending"),
									" ",
									pendingCount
								]
							}) : awaitingDecision === true ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Chip, {
								kind: "warn",
								fill: false,
								title: t("card.awaitingDecisionTitle"),
								children: t("card.awaitingDecision")
							}) : refining(task) ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Chip, {
								kind: "warn",
								fill: false,
								title: t("card.refiningTitle"),
								children: t("card.refining")
							}) : lastPlain !== void 0 ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Chip, {
								kind: resultChipKind(lastPlain.result),
								fill: false,
								children: settledChipLabel(runs.length)
							}) : void 0;
							const automationChips = /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [
								task.schedule?.enabled === true && !blockedAutomation(task) && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Chip, {
									fill: false,
									title: scheduleChipTitle(task),
									children: task.schedule.mode === "chain" ? t("card.chain") : t("card.scheduled")
								}),
								task.schedule?.enabled === true && task.schedule.maxRuns !== void 0 && !blockedAutomation(task) && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(Chip, {
									kind: "muted",
									fill: false,
									title: t("card.batchProgress"),
									children: [
										task.schedule.runCount,
										"/",
										task.schedule.maxRuns
									]
								}),
								blockedAutomation(task) && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Chip, {
									kind: "error",
									fill: false,
									title: scheduleChipTitle(task),
									children: t("card.autoBlocked")
								}),
								chaining && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Chip, {
									kind: "warn",
									fill: false,
									title: t("card.chainingTitle"),
									children: t("card.chaining")
								}),
								pausedFailed && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Chip, {
									kind: "error",
									fill: false,
									title: t("card.autoPausedFailedTitle"),
									children: t("card.autoPausedFailed")
								}),
								queuedComments > 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(Chip, {
									kind: "warn",
									fill: false,
									title: t("card.commentQueueTitle"),
									children: [
										t("card.commentQueue"),
										" ",
										queuedComments
									]
								}),
								unviewed && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Chip, {
									kind: "warn",
									fill: false,
									title: t(hasUnviewedRun ? "card.newContentTitle" : "card.newCommentTitle"),
									children: hasUnviewedRun ? `${t("card.newContent")} ${unviewedCount}` : t("card.newComment")
								})
							] });
							if (!(primaryChip !== void 0 || task.schedule?.enabled === true || latest !== void 0 || queuedComments > 0 || unviewed || pausedFailed || chaining)) return null;
							return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
								className: board_module_css_default.cardBadges,
								children: [primaryChip, automationChips]
							});
						})()]
					}),
					dots !== void 0 && dots.length > 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
						className: board_module_css_default.cardSessions,
						children: [
							dots.map((dot) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
								className: board_module_css_default.cardSessionDot,
								"data-state": dot.state,
								title: dotTitleOf?.(dot.sessionId) ?? dot.sessionId,
								"aria-hidden": "true"
							}, dot.sessionId)),
							(overflowDots ?? 0) > 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
								className: board_module_css_default.cardSessionMore,
								"aria-hidden": "true",
								children: ["+", String(overflowDots)]
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
								className: board_module_css_default.visuallyHidden,
								children: t("card.sessionsForAt", {
									sessions: dots.map((dot) => dotTitleOf?.(dot.sessionId) ?? dot.sessionId).join("；"),
									n: String(dots.length + (overflowDots ?? 0))
								})
							})
						]
					}),
					onColorPick !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.cardColorBar,
						"data-ghost-hide": "",
						onClick: (event) => {
							event.stopPropagation();
						},
						children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(ColorSwatches, {
							value: task.color,
							onChange: (color) => {
								onColorPick(color);
							}
						})
					})
				]
			});
		}
		//#endregion
		//#region src/client/board/context-meter.ts
		/** The three bar segments, in native order and with the native tint classes. */
		const CONTEXT_SEGMENTS = [
			{
				key: "systemTokens",
				className: "meterSystem",
				label: "context.system"
			},
			{
				key: "toolsTokens",
				className: "meterTools",
				label: "context.tools"
			},
			{
				key: "messageTokens",
				className: "meterMessages",
				label: "context.messages"
			}
		];
		/**
		* Approximate context occupancy, using the native rounding and upper clamp.
		* The numerator prefers `projectedTokens` (the provider sample carried
		* forward over surface movement — so compaction shows immediately) and falls
		* back to the bare sample; null until both the numerator and a route
		* capacity are known.
		*/
		function contextOccupancy(pressure) {
			const usedTokens = pressure?.projectedTokens ?? pressure?.pressureTokens;
			if (usedTokens === void 0 || pressure?.contextWindow === void 0) return void 0;
			return {
				percent: Math.min(100, Math.round(usedTokens / pressure.contextWindow * 100)),
				usedTokens,
				contextWindow: pressure.contextWindow
			};
		}
		/**
		* Split the occupancy bar into colored segments by the breakdown composition,
		* or fall back to one uncolored full-width segment when the composition is
		* unknown or empty. Widths follow the native math: each bucket's share of
		* `percent` (the occupancy percentage), filtered to visible (> 0) parts —
		* which also drops any bucket that contributes no tokens.
		*/
		function contextSegments(occupancy, breakdown) {
			const total = breakdown === void 0 ? 0 : breakdown.systemTokens + breakdown.toolsTokens + breakdown.messageTokens;
			if (breakdown === void 0 || total === 0) return [{
				key: "total",
				className: void 0,
				width: occupancy.percent
			}];
			return CONTEXT_SEGMENTS.map((segment) => ({
				key: segment.key,
				className: segment.className,
				width: occupancy.percent * (breakdown[segment.key] / total)
			})).filter((part) => part.width > 0);
		}
		/**
		* Compact token count: `< 1000` as-is, then `K`/`M` with one decimal under
		* 100 (matching the native `formatTokens` exactly: `639K`, `1M`).
		*/
		function formatTokens(n) {
			const scaled = (v) => v >= 100 ? String(Math.round(v)) : String(Math.round(v * 10) / 10);
			if (n < 1e3) return String(n);
			if (n < 1e6) return `${scaled(n / 1e3)}K`;
			return `${scaled(n / 1e6)}M`;
		}
		/**
		* Decode rate (native TPS): tokens per second, rounded to a whole number.
		* Returns the FIGURE only — the `tok/s` unit rides the locale key, like every
		* other usage fragment (`formatTokens` never carries its own label either).
		* A non-positive window means "no measurable decode yet": undefined, so the
		* whole fragment hides instead of printing `0 tok/s` (same grammar as the
		* `(llmMs > 0 || toolMs > 0)` time-line gate in the panel).
		*/
		function formatTps(decodeTokens, decodeMs) {
			if (!Number.isFinite(decodeTokens) || !Number.isFinite(decodeMs) || decodeMs <= 0) return void 0;
			return String(Math.max(0, Math.round(decodeTokens / decodeMs * 1e3)));
		}
		/**
		* Prompt-cache hit rate: cached reads over all input (`cacheRead /
		* (uncached + cacheRead)`), rounded to a whole percent — the native "缓存命中"
		* figure. Returns the FIGURE only (`%` rides the locale key). A zero
		* denominator means "no input measured yet": undefined, never NaN.
		*/
		function cacheHitRate(cacheReadTokens, uncachedInputTokens) {
			if (!Number.isFinite(cacheReadTokens) || !Number.isFinite(uncachedInputTokens)) return void 0;
			const denominator = uncachedInputTokens + cacheReadTokens;
			if (denominator <= 0) return void 0;
			return String(Math.round(cacheReadTokens / denominator * 100));
		}
		//#endregion
		//#region src/client/board/markdown-parser.ts
		const HEADING_RE = /^(#{1,3})\s+(.*)$/;
		const FENCE_RE = /^```([\w-]*)\s*$/;
		const HR_RE = /^-{3,}$|^\*{3,}$|^_{3,}$/;
		const QUOTE_RE = /^>\s?(.*)$/;
		const BULLET_RE = /^\s*[-*+]\s+(.*)$/;
		const NUMBERED_RE = /^\s*(\d+)[.)]\s+(.*)$/;
		/**
		* Inline tokenizer: bold / italic / code / link / escapes, everything else
		* text. NON-GLOBAL on purpose: parseInline recurses into itself for nested
		* emphasis/link text, and a shared `g`-flag regex would let an inner call
		* advance the one global lastIndex, corrupting the outer loop into an
		* infinite same-match spawn. Instead the regex is re-run against the
		* remaining substring at every step, so recursion is always safe.
		*/
		const INLINE_RE = /\*\*([^*]+?)\*\*|\*([^*\n]+?)\*|`([^`]+)`|\[([^\]]+)\]\(([^)\s]+)\)|\\([\\`*_[\]]|$)/;
		/** Only these link targets may become real <a> elements — anything else
		*  (javascript:, data:, vbscript:, …) renders as plain text instead. */
		const SAFE_LINK_RE = /^(https?:|mailto:|#|\/)/i;
		function parseInline(source) {
			const nodes = [];
			let cursor = 0;
			let match;
			while ((match = INLINE_RE.exec(source.slice(cursor))) !== null) {
				const abs = cursor + match.index;
				const consumed = match[0].length;
				if (consumed === 0) break;
				pushText(nodes, source.slice(cursor, abs));
				const [, bold, italic, code, linkText, linkUrl, escaped] = match;
				if (bold !== void 0) nodes.push({
					t: "bold",
					children: parseInline(bold)
				});
				else if (italic !== void 0) nodes.push({
					t: "italic",
					children: parseInline(italic)
				});
				else if (code !== void 0) nodes.push({
					t: "code",
					s: code
				});
				else if (linkText !== void 0 && linkUrl !== void 0) if (SAFE_LINK_RE.test(linkUrl)) nodes.push({
					t: "link",
					url: linkUrl,
					children: parseInline(linkText)
				});
				else pushText(nodes, `[${linkText}](${linkUrl})`);
				else if (escaped !== void 0) pushText(nodes, escaped);
				cursor = abs + consumed;
			}
			pushText(nodes, source.slice(cursor));
			return nodes;
		}
		/** Append a text run, coalescing with a trailing text node so escaped
		*  segments (e.g. `\*not bold\*`) rejoin into a single clean node. */
		function pushText(nodes, text) {
			if (text === "") return;
			const last = nodes[nodes.length - 1];
			if (last?.t === "text") nodes[nodes.length - 1] = {
				t: "text",
				s: last.s + text
			};
			else nodes.push({
				t: "text",
				s: text
			});
		}
		/** Is the line the start of a paragraph-breaking block (blank line excluded)? */
		function isBlockStart(line) {
			return HEADING_RE.test(line) || FENCE_RE.test(line) || HR_RE.test(line) || QUOTE_RE.test(line) || BULLET_RE.test(line) || NUMBERED_RE.test(line);
		}
		/** Parse the whole source into top-level blocks. */
		function parseMarkdown(source) {
			const lines = source.split("\n");
			const blocks = [];
			let index = 0;
			const pushParagraph = (start, end) => {
				const text = lines.slice(start, end).filter((line) => line.trim() !== "").join("\n");
				if (text.trim() !== "") blocks.push({
					t: "paragraph",
					children: parseInline(text)
				});
			};
			while (index < lines.length) {
				const line = lines[index];
				const trimmed = line.trim();
				const heading = HEADING_RE.exec(trimmed);
				const fence = FENCE_RE.exec(trimmed);
				const hr = HR_RE.test(trimmed);
				const quote = QUOTE_RE.exec(line);
				const bullet = BULLET_RE.exec(line);
				const numbered = NUMBERED_RE.exec(line);
				if (heading !== null) {
					blocks.push({
						t: "heading",
						level: heading[1].length,
						children: parseInline(heading[2])
					});
					index += 1;
				} else if (fence !== null) {
					const lang = fence[1] ?? "";
					const body = [];
					index += 1;
					while (index < lines.length && !FENCE_RE.test(lines[index].trim())) {
						body.push(lines[index]);
						index += 1;
					}
					if (index < lines.length) index += 1;
					blocks.push({
						t: "codeblock",
						lang,
						text: body.join("\n")
					});
				} else if (hr) {
					blocks.push({ t: "hr" });
					index += 1;
				} else if (quote !== null) {
					const body = [];
					while (index < lines.length && QUOTE_RE.exec(lines[index]) !== null) {
						body.push(QUOTE_RE.exec(lines[index])[1]);
						index += 1;
					}
					blocks.push({
						t: "quote",
						children: parseMarkdown(body.join("\n"))
					});
				} else if (bullet !== null || numbered !== null) {
					const ordered = numbered !== null;
					const items = [];
					while (index < lines.length) {
						const b = BULLET_RE.exec(lines[index]);
						const n = NUMBERED_RE.exec(lines[index]);
						if (ordered ? n === null : b === null) break;
						items.push(parseInline((ordered ? n[2] : b[1]).trim()));
						index += 1;
					}
					blocks.push({
						t: "list",
						ordered,
						items
					});
				} else if (trimmed === "") index += 1;
				else {
					const start = index;
					while (index < lines.length && lines[index].trim() !== "" && !isBlockStart(lines[index])) index += 1;
					pushParagraph(start, index);
				}
			}
			return blocks;
		}
		//#endregion
		//#region src/client/board/Markdown.tsx
		/**
		* Markdown renderer for board surfaces: maps the parser's node tree
		* (markdown-parser.ts) to React elements with the board's design tokens — NO
		* HTML strings are ever produced, so the preview has the native look and
		* there is no injection surface. Used by the shared transcript rows and the
		* comment thread, so every conversation surface previews markdown
		* identically.
		*/
		function renderInline(node, key) {
			switch (node.t) {
				case "text": return node.s;
				case "bold": return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("strong", {
					className: board_module_css_default.mdStrong,
					children: node.children.map(renderInline)
				}, key);
				case "italic": return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("em", {
					className: board_module_css_default.mdEm,
					children: node.children.map(renderInline)
				}, key);
				case "code": return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("code", {
					className: board_module_css_default.mdCode,
					children: node.s
				}, key);
				case "link": return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("a", {
					className: board_module_css_default.mdLink,
					href: node.url,
					target: "_blank",
					rel: "noreferrer",
					children: node.children.map(renderInline)
				}, key);
			}
		}
		function renderBlock(block, key) {
			switch (block.t) {
				case "paragraph": return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
					className: board_module_css_default.mdParagraph,
					children: block.children.map(renderInline)
				}, key);
				case "heading": return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("h3", {
					className: board_module_css_default.mdHeading,
					"data-level": block.level,
					children: block.children.map(renderInline)
				}, key);
				case "codeblock": return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("pre", {
					className: board_module_css_default.mdCodeBlock,
					children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("code", { children: block.text })
				}, key);
				case "list": {
					const items = block.items.map((children, index) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)("li", { children: children.map(renderInline) }, index));
					return block.ordered ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)("ol", {
						className: board_module_css_default.mdList,
						children: items
					}, key) : /* @__PURE__ */ (0, react_jsx_runtime.jsx)("ul", {
						className: board_module_css_default.mdList,
						children: items
					}, key);
				}
				case "quote": return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("blockquote", {
					className: board_module_css_default.mdQuote,
					children: block.children.map(renderBlock)
				}, key);
				case "hr": return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("hr", { className: board_module_css_default.mdRule }, key);
			}
		}
		/** Render a text source as markdown-previews blocks (empty text renders nothing). */
		function Markdown({ text }) {
			return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(react_jsx_runtime.Fragment, { children: (0, react.useMemo)(() => parseMarkdown(text), [text]).map(renderBlock) });
		}
		//#endregion
		//#region src/client/board/review-transcript.ts
		/**
		* Review-page transcript: fold a raw session-history event list into the
		* tail of the conversation, following the native harness folding rules:
		* - `user/message` with `source.kind === 'user'` is a user bubble (the
		*   message's text blocks).
		* - `user/message` with any other source kind (plugin/system injections —
		*   skills, AGENTS.md, runtime context, cron notices…) is a context row:
		*   the native conversation shows these as a weak "context injection" row,
		*   never as a user bubble. The review page renders them the same way,
		*   collapsed (the full text lives in the native session page).
		* - `assistant/message` carries its message under `data.message` (with
		*   `turn`/`step`/`usage` alongside) — different from the flat
		*   `user/message` shape — and its text/reasoning blocks form the
		*   assistant message.
		* - Tool results and every other event are not part of the message
		*   surface and are skipped.
		* Pure and framework-free so the fold unit-tests in isolation.
		*/
		/**
		* Fold history events into the conversation tail (see module doc). Only
		* append-surface user and assistant messages produce lines; everything else
		* (tool results, chunks, turn markers) is skipped.
		*/
		function foldTranscript(events) {
			const lines = [];
			for (const event of events) {
				const at = typeof event.time === "number" ? event.time : 0;
				const fallbackId = String(event.seq ?? lines.length);
				if (event.type === "user/message") {
					const data = event.data;
					if (typeof data !== "object" || data === null) continue;
					if (data.source?.kind !== "user") {
						lines.push({
							kind: "context",
							id: String(data.id ?? fallbackId),
							plugin: typeof data.source?.plugin === "string" ? data.source.plugin : "context",
							summary: typeof data.source?.summary === "string" ? data.source.summary : "",
							at
						});
						continue;
					}
					const text = contentTextOf(data.content);
					const images = contentImagesOf(data.content);
					if (text === "" && images.length === 0) continue;
					lines.push({
						kind: "message",
						id: String(data.id ?? fallbackId),
						role: "user",
						text,
						...images.length > 0 ? { images } : {},
						at
					});
				} else if (event.type === "assistant/message") {
					const data = event.data;
					if (typeof data !== "object" || data === null) continue;
					const message = data.message;
					if (typeof message !== "object" || message === null) continue;
					const text = contentTextOf(message.content);
					const images = contentImagesOf(message.content);
					if (text === "" && images.length === 0) continue;
					const usage = data.usage;
					lines.push({
						kind: "message",
						id: String(message.id ?? fallbackId),
						role: "assistant",
						text,
						...images.length > 0 ? { images } : {},
						at,
						...usage !== void 0 && isUsage(usage) ? { usage } : {}
					});
				}
			}
			return lines;
		}
		/**
		* The durable image refs one message content carries (structural): parts of
		* the shape `{type:'image', attachment:{attachmentId, mediaType, name?}}` —
		* exactly what the host stores after promoting the submitted bytes. Anything
		* malformed is skipped (never a crash on an unknown part).
		*/
		function contentImagesOf(content) {
			if (!Array.isArray(content)) return [];
			const out = [];
			const seen = /* @__PURE__ */ new Set();
			for (const part of content) {
				if (typeof part !== "object" || part === null) continue;
				const row = part;
				if (row.type !== "image") continue;
				const attachment = row.attachment;
				if (typeof attachment !== "object" || attachment === null) continue;
				const ref = attachment;
				if (typeof ref.attachmentId !== "string" || ref.attachmentId === "") continue;
				if (seen.has(ref.attachmentId)) continue;
				seen.add(ref.attachmentId);
				out.push({
					attachmentId: ref.attachmentId,
					mediaType: typeof ref.mediaType === "string" ? ref.mediaType : "image/png",
					...typeof ref.name === "string" && ref.name !== "" ? { name: ref.name } : {}
				});
			}
			return out;
		}
		/** Structural guard for the native token-accounting payload. */
		function isUsage(value) {
			if (typeof value !== "object" || value === null) return false;
			const usage = value;
			return typeof usage.inputTokens === "number" && typeof usage.outputTokens === "number";
		}
		/** Sum the token accounting of every assistant message in a transcript. */
		function sumUsage(lines) {
			let total;
			for (const line of lines) {
				if (line.kind !== "message" || line.usage === void 0) continue;
				const usage = line.usage;
				total = {
					inputTokens: (total?.inputTokens ?? 0) + usage.inputTokens,
					outputTokens: (total?.outputTokens ?? 0) + usage.outputTokens,
					...usage.cacheReadTokens !== void 0 ? { cacheReadTokens: (total?.cacheReadTokens ?? 0) + usage.cacheReadTokens } : {},
					...usage.cacheWriteTokens !== void 0 ? { cacheWriteTokens: (total?.cacheWriteTokens ?? 0) + usage.cacheWriteTokens } : {},
					...usage.reasoningTokens !== void 0 ? { reasoningTokens: (total?.reasoningTokens ?? 0) + usage.reasoningTokens } : {}
				};
			}
			return total;
		}
		/**
		* The element that OWNS the scroll for a piece of content: the nearest
		* self-or-ancestor that DECLARES itself a scroll container (`overflow-y:
		* auto|scroll`), falling back to the region itself.
		*
		* Declared, not "currently scrollable": measuring `scrollHeight > clientHeight`
		* to decide makes the answer depend on how much text happens to be loaded, so
		* a short region escalated to an ANCESTOR and its "滑到最新" scrolled the whole
		* panel — the wrong range, and invisible until content grew. Ownership is a
		* layout fact (who was built to scroll), so a container-query width change
		* still needs no JS mode switch: the wide rail declares the comments box a
		* scroller, the narrow folds block does instead, and the same code finds the
		* right one in both.
		*/
		function resolveScroller(element) {
			let node = element;
			while (node !== null) {
				const style = getComputedStyle(node);
				if (style.overflowY === "auto" || style.overflowY === "scroll") return node;
				node = node.parentElement;
			}
			return element;
		}
		/** The watermark of a loaded result (tail seq; 0 when empty). Kept for the
		*  legacy settle path; the live poll/accumulate paths fold from state. */
		function watermarkOf(result) {
			return result.events[result.events.length - 1]?.seq ?? result.events.length;
		}
		/**
		* Keep a following scroll region pinned to its latest output whenever its
		* size changes — a ResizeObserver (registered a disposer) re-scrolls to the
		* bottom while the user is at the bottom. This also fires once on
		* registration, so a surface opened before its layout settles (e.g. the
		* review page's right rail grows as its async context meter / config load,
		* shrinking the comment thread) still lands on the latest. The observer
		* watches BOTH the content region and its resolved scroller, so a width
		* change that moves the scroll elsewhere is still covered. Content-driven
		* re-scrolls stay with the caller's follow effects; this only covers layout.
		*/
		function useResizeFollow(scrollRef, atBottomRef, remountKey, initialToBottom = true) {
			(0, react.useEffect)(() => {
				const content = scrollRef.current;
				if (content === null) return;
				const apply = () => {
					const root = resolveScroller(content);
					if (root !== null && atBottomRef.current) root.scrollTop = root.scrollHeight;
				};
				const observer = new ResizeObserver(apply);
				observer.observe(content);
				const root = resolveScroller(content);
				if (root !== null && root !== content) observer.observe(root);
				if (!initialToBottom) atBottomRef.current = false;
				apply();
				return () => {
					observer.disconnect();
				};
			}, [
				scrollRef,
				remountKey,
				initialToBottom
			]);
		}
		/**
		* One FOLLOW mechanism, shared by every live list (transcript tail, comment
		* thread): measure and pin against the RESOLVED scroller, and see its scroll
		* events through a window-level capture listener (scroll does not bubble, but
		* it does run the capture phase from the window down). A surface therefore
		* keeps following, reporting and jumping correctly whether its own region
		* scrolls (wide panel) or an ancestor does (stacked narrow panel) — no
		* per-mode code, no mobile fork.
		*/
		function useFollowScroll(scrollRef, atBottom, setAtBottom, changedKey, remountKey, initialToBottom = true) {
			const atBottomRef = (0, react.useRef)(atBottom);
			(0, react.useEffect)(() => {
				atBottomRef.current = atBottom;
			});
			useResizeFollow(scrollRef, atBottomRef, remountKey, initialToBottom);
			const measure = (0, react.useCallback)(() => {
				const root = resolveScroller(scrollRef.current);
				if (root === null) return;
				setAtBottom(root.scrollHeight - root.scrollTop - root.clientHeight < 24);
			}, [scrollRef, setAtBottom]);
			(0, react.useEffect)(() => {
				const handler = () => measure();
				document.addEventListener("scroll", handler, true);
				return () => document.removeEventListener("scroll", handler, true);
			}, [measure]);
			(0, react.useEffect)(() => {
				const root = resolveScroller(scrollRef.current);
				if (root === null || !atBottom) return;
				root.scrollTop = root.scrollHeight;
			}, [
				scrollRef,
				changedKey,
				atBottom,
				remountKey
			]);
			return {
				measure,
				jumpToBottom: (0, react.useCallback)(() => {
					const root = resolveScroller(scrollRef.current);
					if (root === null) return;
					root.scrollTop = root.scrollHeight;
					setAtBottom(true);
				}, [scrollRef, setAtBottom])
			};
		}
		/**
		* The sticky "滑到最新" affordance shown in a scroll region (transcript or
		* comment thread) when the user has scrolled away from the bottom: one click
		* returns to the latest output. Hidden while the region is at the bottom,
		* where new content already auto-follows.
		*/
		function JumpToLatest({ atBottom, onJump }) {
			if (atBottom) return null;
			return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
				type: "button",
				className: board_module_css_default.reviewJumpLatest,
				onClick: onJump,
				title: t("review.jumpLatest"),
				"aria-label": t("review.jumpLatest"),
				children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, { name: "arrowDown" })
			});
		}
		/**
		* Manage one live transcript tail.
		* @param controller - the board controller (transcript reader).
		* @param sessionId - the session whose tail to show (undefined = idle).
		* @param reloadKey - a value whose change forces a full reload (e.g. the
		*   execution list length, so an injected/settled round refreshes at once).
		* @param onResult - optional callback with every load result (e.g. the
		*   review page reads the native projections riding the tail page).
		*/
		function useTranscriptTail(controller, sessionId, reloadKey = void 0, onResult) {
			const [events, setEvents] = (0, react.useState)(void 0);
			const [error, setError] = (0, react.useState)(false);
			const [hasMore, setHasMore] = (0, react.useState)(false);
			const [loadingEarlier, setLoadingEarlier] = (0, react.useState)(false);
			const [pageError, setPageError] = (0, react.useState)(false);
			/** The deployment serves no page endpoint (terminal — the button goes away). */
			const [pageUnsupported, setPageUnsupported] = (0, react.useState)(false);
			const [atBottom, setAtBottom] = (0, react.useState)(true);
			const scrollRef = (0, react.useRef)(null);
			const floorRef = (0, react.useRef)(void 0);
			const hasMoreRef = (0, react.useRef)(false);
			hasMoreRef.current = hasMore;
			const throughSeqRef = (0, react.useRef)(void 0);
			const eventsRef = (0, react.useRef)(void 0);
			eventsRef.current = events;
			const watermarkRef = (0, react.useRef)(void 0);
			const aliveRef = (0, react.useRef)(true);
			(0, react.useEffect)(() => () => {
				aliveRef.current = false;
			}, []);
			const onResultRef = (0, react.useRef)(onResult);
			onResultRef.current = onResult;
			const lines = events === void 0 ? void 0 : foldTranscript(events);
			const { measure, jumpToBottom } = useFollowScroll(scrollRef, atBottom, setAtBottom, lines);
			/** Fold + publish a successful tail read (the reload AND the poll share
			*  it — one settlement grammar). */
			const settle = (0, react.useCallback)((result) => {
				setError(false);
				const next = watermarkOf(result);
				setHasMore(result.hasMore === true);
				floorRef.current = result.floorSeq;
				throughSeqRef.current = result.throughSeq;
				watermarkRef.current = next;
				setEvents([...result.events]);
				onResultRef.current?.(result);
			}, []);
			/** Full reload: re-read the tail. A transient failure is not a dead end —
			*  the light poll keeps re-reading every 3s and ANY success clears the
			*  error (self-healing without a second retry mechanism). */
			const reload = (0, react.useCallback)(() => {
				if (sessionId === void 0) return;
				controller.loadTranscript(sessionId).then((result) => {
					if (!aliveRef.current) return;
					if (result === void 0) {
						setError(true);
						return;
					}
					settle(result);
				});
			}, [
				controller,
				sessionId,
				settle
			]);
			(0, react.useEffect)(() => {
				setEvents(void 0);
				setError(false);
				setHasMore(false);
				setLoadingEarlier(false);
				setPageError(false);
				setPageUnsupported(false);
				floorRef.current = void 0;
				throughSeqRef.current = void 0;
				watermarkRef.current = void 0;
				reload();
			}, [
				reload,
				reloadKey,
				sessionId
			]);
			(0, react.useEffect)(() => {
				if (sessionId === void 0) return;
				const poll = () => {
					controller.loadTranscript(sessionId).then((result) => {
						if (!aliveRef.current || result === void 0) return;
						setError(false);
						setHasMore(result.hasMore === true);
						if (result.floorSeq !== void 0) floorRef.current = result.floorSeq;
						if (result.throughSeq !== void 0) throughSeqRef.current = result.throughSeq;
						const incoming = [...result.events];
						const watermark = incoming.length === 0 ? 0 : incoming[incoming.length - 1]?.seq ?? incoming.length;
						setEvents((current) => {
							if (current === void 0) {
								watermarkRef.current = watermark;
								onResultRef.current?.(result);
								return incoming;
							}
							const known = /* @__PURE__ */ new Set();
							for (const event of current) if (event.seq !== void 0) known.add(event.seq);
							let fresh = false;
							const merged = [...current];
							for (const event of incoming) if (event.seq === void 0 || !known.has(event.seq)) {
								merged.push(event);
								fresh = true;
							}
							if (fresh || watermarkRef.current === void 0) {
								watermarkRef.current = watermark;
								onResultRef.current?.(result);
								return merged;
							}
							return current;
						});
					});
				};
				const timer = setInterval(poll, 3e3);
				const onVisibility = () => {
					if (!document.hidden) poll();
				};
				document.addEventListener("visibilitychange", onVisibility);
				return () => {
					clearInterval(timer);
					document.removeEventListener("visibilitychange", onVisibility);
				};
			}, [
				controller,
				sessionId,
				settle
			]);
			return {
				lines,
				error,
				hasMore,
				loadingEarlier,
				pageError,
				pageUnsupported,
				atBottom,
				scrollRef,
				onScroll: measure,
				jumpToBottom,
				reload,
				loadEarlier: (0, react.useCallback)(() => {
					if (sessionId === void 0 || loadingEarlier) return;
					if (!hasMoreRef.current) return;
					const floor = floorRef.current ?? eventsRef.current?.[0]?.seq;
					if (floor === void 0) return;
					setLoadingEarlier(true);
					controller.loadTranscriptPage(sessionId, floor, throughSeqRef.current).then((page) => {
						if (!aliveRef.current) return;
						setLoadingEarlier(false);
						if (page === void 0) {
							setPageError(true);
							return;
						}
						if (page.refused !== void 0) {
							if (page.refused === "remote/unavailable") setPageUnsupported(true);
							else setPageError(true);
							return;
						}
						setPageError(false);
						setPageUnsupported(false);
						const root = resolveScroller(scrollRef.current);
						const distance = root === null ? void 0 : root.scrollHeight - root.scrollTop;
						setEvents((current) => {
							const known = /* @__PURE__ */ new Set();
							if (current !== void 0) {
								for (const event of current) if (event.seq !== void 0) known.add(event.seq);
							}
							const earlier = page.events.filter((event) => event.seq === void 0 || !known.has(event.seq));
							const merged = [...earlier, ...current ?? []];
							if (earlier.length > 0 || page.floorSeq !== floorRef.current) {
								if (page.floorSeq !== void 0) floorRef.current = page.floorSeq;
								const watermark = merged.length === 0 ? 0 : merged[merged.length - 1]?.seq ?? merged.length;
								watermarkRef.current = watermark;
								return merged;
							}
							return current ?? merged;
						});
						setHasMore(page.hasMore === true);
						if (root !== null && distance !== void 0) requestAnimationFrame(() => {
							requestAnimationFrame(() => {
								root.scrollTop = root.scrollHeight - distance;
							});
						});
					});
				}, [
					controller,
					sessionId,
					loadingEarlier
				])
			};
		}
		//#endregion
		//#region src/client/board/comment-thread.ts
		/** The display state of one round, absent any UI concerns. */
		function commentRoundState(round, cruiseOn) {
			if (round.external === true && round.endedAt === void 0) return "running";
			if (round.endedAt !== void 0) return round.result ?? "cancelled";
			if (round.injectedAt !== void 0) return "running";
			return cruiseOn ? "queued" : "saved";
		}
		/**
		* The comment thread of one native session — every comment round injected
		* into (or queued for) that `sessionId`, oldest first. THE single source for
		* both surfaces:
		* - the execution review page shows the thread of its execution's session;
		* - the linked-session panel shows the thread of its own session.
		* Both anchors (execution `parentExecutionId` and linked `sessionAnchor`)
		* are ignored for membership — they only ever point at this same session, so
		* comments typed on either surface are visible on both. Rounds that carry no
		* sessionId yet (none today: newCommentRound always sets it) fall back to
		* session-anchored attribution for legacy safety.
		*/
		function sessionCommentsOf(task, sessionId, cruiseOn) {
			return task.executions.filter((round) => round.comment !== void 0 && (round.sessionId === sessionId || round.sessionId === void 0 && round.sessionAnchor === sessionId)).map((round) => ({
				round,
				state: commentRoundState(round, cruiseOn)
			}));
		}
		/**
		* The 1-based queue position of a pending round within ITS OWN SESSION'S
		* lane (saved comments for that session, submission order) — what a
		* 「排队中 · 第 N 位」 chip shows.
		*
		* The lane is the session, not the card: comments waiting for a DIFFERENT
		* conversation are not ahead of this one, and would otherwise read as
		* 「第 3 位」 while their own session is idle and about to take them. Returns
		* 0 when the round is not in the pending set (settled, injected, or not a
		* comment round at all).
		*/
		function queuePositionOf(task, roundId) {
			const target = task.executions.find((round) => round.id === roundId);
			if (target === void 0) return 0;
			const lane = target.sessionId ?? target.sessionAnchor;
			const index = task.executions.filter((round) => round.comment !== void 0 && round.endedAt === void 0 && round.injectedAt === void 0 && round.external !== true).filter((round) => (round.sessionId ?? round.sessionAnchor) === lane).sort((a, b) => a.startedAt - b.startedAt).findIndex((round) => round.id === roundId);
			return index < 0 ? 0 : index + 1;
		}
		/** The chip color of a comment display state. */
		function commentKindOf(state) {
			switch (state) {
				case "succeeded": return "success";
				case "failed": return "error";
				case "running":
				case "queued": return "warn";
				default: return "muted";
			}
		}
		/** The latest round of one session's comment thread (the 「最新」 source). */
		function latestCommentView(task, sessionId, cruiseOn) {
			const views = sessionCommentsOf(task, sessionId, cruiseOn);
			if (views.length === 0) return void 0;
			const latest = views[views.length - 1];
			return {
				text: latest.round.comment ?? "",
				stateKey: commentStateKey(latest.state),
				at: latest.round.startedAt
			};
		}
		/** The locale key of a comment state's chip label (callers pass `{ n }` for queued). */
		function commentStateKey(state) {
			switch (state) {
				case "succeeded": return "review.commentSucceeded";
				case "failed": return "review.commentFailed";
				case "cancelled": return "review.commentCancelled";
				case "running": return "review.commentRunning";
				case "queued": return "review.commentQueued";
				case "saved": return "review.commentPending";
			}
		}
		//#endregion
		//#region src/client/board/CommentsThread.tsx
		/**
		* Shared comment-thread rendering for every surface that shows comment
		* rounds: the execution review page (execution-anchored comments) and the
		* linked-session panel (session-anchored, drive-mode comments). One item
		* grammar — comment text, state chip, submission time, pending-round cancel,
		* the slash-command marker and the settled outcome text — so both panels
		* always read as the same widget.
		*/
		/** Renders the task's comment views (oldest first) with cancel affordances. */
		function CommentsThread({ task, views, onCancel }) {
			if (views.length === 0) return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
				className: board_module_css_default.detailText,
				children: t("review.noComments")
			});
			return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("ul", {
				className: board_module_css_default.reviewComments,
				children: views.map((view) => {
					const position = queuePositionOf(task, view.round.id);
					const cancellable = view.state === "saved" || view.state === "queued";
					return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("li", {
						className: board_module_css_default.reviewComment,
						children: [
							/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
								className: board_module_css_default.reviewCommentText,
								children: [
									view.round.command === true && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
										className: board_module_css_default.reviewCommentCommand,
										"aria-hidden": "true",
										children: "/"
									}),
									view.round.comment !== void 0 && view.round.comment !== "" && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Markdown, { text: view.round.comment }),
									view.round.imageOnly === true && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
										className: board_module_css_default.reviewCommentImage,
										children: t("review.commentImage")
									}),
									view.round.promptImages !== void 0 && view.round.promptImages.length > 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
										className: board_module_css_default.reviewCommentImage,
										children: t("review.commentImages", { n: String(view.round.promptImages.length) })
									}),
									view.round.promptFiles !== void 0 && view.round.promptFiles.length > 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
										className: board_module_css_default.reviewCommentImage,
										children: t("review.commentFiles", { n: String(view.round.promptFiles.length) })
									})
								]
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
								className: board_module_css_default.reviewCommentMeta,
								children: [
									/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Chip, {
										kind: commentKindOf(view.state),
										children: view.state === "queued" ? t("review.commentQueued", { n: String(position) }) : t(commentStateKey(view.state))
									}),
									/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
										className: board_module_css_default.reviewCommentTime,
										children: formatDateTime(view.round.startedAt)
									}),
									cancellable && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
										type: "button",
										className: board_module_css_default.reviewCommentCancel,
										onClick: () => {
											onCancel(view.round.id);
										},
										children: t("review.commentCancel")
									})
								]
							}),
							view.round.command === true && view.round.error !== void 0 && view.round.error !== "" && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
								className: `${board_module_css_default.executionError}${view.state === "succeeded" ? ` ${board_module_css_default.reviewCommandOutcome}` : ""}`,
								children: view.round.error
							}),
							view.state === "failed" && view.round.command !== true && view.round.error !== void 0 && view.round.error !== "" && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
								className: board_module_css_default.reviewCommentError,
								children: view.round.error
							})
						]
					}, view.round.id);
				})
			});
		}
		//#endregion
		//#region src/client/board/InteractionCard.tsx
		/**
		* Pending native-interaction card: the plan/question the session's agent is
		* waiting on, rendered IN PLACE over the composer so the user answers right
		* there — in the review page's comment rail, the linked-session panel and the
		* requirement-refinement panel alike (all three read this one component).
		*
		* PARITY WITH THE NATIVE COMPOSER IS THE CONTRACT. The board does not invent
		* its own question UI: it renders the same request the native
		* `QuestionComposer` renders, with the same parts and the same behaviour —
		* eyebrow + title, collapse / dismiss-all icons, the option list (numbered
		* radios, or checkboxes for a multi-select, each with its optional
		* description and the 推荐 badge split off the label suffix), the custom
		* answer field appended to the list (or standing alone as a block when the
		* question offers no options), the pager (上一题 / 第 n / 总数 / 下一题), the
		* near-field error line, and the skip + next/submit pair. Behaviour follows
		* the same rules: a single-select choice auto-advances to the next question;
		* typing a custom answer clears the single-select choice (and vice versa);
		* Enter continues (Shift+Enter breaks a line, an IME composition never
		* submits); a multi-select may carry options AND custom text; an incomplete
		* batch jumps to the first unanswered question and says so; a failed submit
		* keeps the card open with the reason on it.
		*
		* ANSWERING IS IN PLACE, THROUGH THE OFFICIAL CARRIER. The pending question
		* arrives from the official `pendingInteractions` snapshot (the same source
		* the native composer reads), so `answer`/`cancel` settle the very request
		* the native surface would settle — nothing new is registered, first answer
		* still wins, and the other surface's card drops with the next snapshot
		* notification. A host whose snapshot entries carry data but no action keeps
		* the read-only shell (kind + one navigate affordance); the waiting banner
		* stays available there, so a proven wait never reaches the UI as silence.
		*/
		/** The 推荐 suffix the native surface splits off an option label. */
		const RECOMMENDED_SUFFIX = /\s*(?:\((?:recommended|推荐)\)|（(?:recommended|推荐)）)\s*$/i;
		/** Split the recommendation suffix without changing the answer value. */
		function splitRecommended(label) {
			return RECOMMENDED_SUFFIX.test(label) ? {
				label: label.replace(RECOMMENDED_SUFFIX, ""),
				recommended: true
			} : {
				label,
				recommended: false
			};
		}
		/** Whether an event belongs to an active IME composition (native rule). */
		function isComposing(event) {
			return event.nativeEvent.isComposing === true || event.nativeEvent.keyCode === 229;
		}
		/** Whether a draft carries an answer (an option picked or custom text). */
		function draftAnswered(draft) {
			return draft.selected.length > 0 || (draft.custom?.trim() ?? "") !== "";
		}
		/** Whether a draft is complete (answered or explicitly skipped). */
		function draftCompleted(draft) {
			return draftAnswered(draft) || draft.skipped === true;
		}
		/** Fresh drafts for one batch (never shared between carriers). */
		function freshDrafts(questions) {
			return questions.map(() => ({ selected: [] }));
		}
		/** The interaction card (see module doc). Renders nothing while idle. */
		function InteractionCard({ question, shellWaiting, sessionId, controller }) {
			if (shellWaiting !== void 0 && (question === void 0 || !controller.questionAnswerInPlace)) return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
				className: board_module_css_default.interactionCard,
				"data-plan": shellWaiting === "plan-review" ? "true" : void 0,
				children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
					className: board_module_css_default.interactionCardBody,
					children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Chip, {
						kind: "warn",
						title: t("review.planAwaiting"),
						children: t("review.planAwaiting")
					}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.interactionQuestion,
						children: t("review.interactionBodyMissing")
					})]
				}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
					className: board_module_css_default.interactionActions,
					children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
						variant: "primary",
						onClick: () => {
							controller.openSession(sessionId);
						},
						children: t("review.interactionGoAnswer")
					})
				})]
			});
			if (question === void 0) return null;
			if (!controller.questionAnswerInPlace) return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(MirrorQuestionCard, {
				question,
				sessionId,
				controller
			});
			const planQuestion = question.isPlanReview ? planQuestionOf(question) : void 0;
			return planQuestion === void 0 ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)(QuestionFlow, {
				question,
				sessionId,
				controller
			}) : /* @__PURE__ */ (0, react_jsx_runtime.jsx)(PlanReviewCard, {
				question,
				item: planQuestion,
				sessionId,
				controller
			});
		}
		/**
		* The generic question flow — one carrier, many questions, one answer batch.
		* Drafts live in this component and are re-created whenever the carrier's
		* identity changes, so a second request can never inherit the first one's
		* half-typed answers.
		*/
		function QuestionFlow({ question, sessionId, controller }) {
			const [progress, setProgress] = (0, react.useState)(() => ({
				key: question.rpcId,
				index: 0,
				drafts: restoreQuestionDrafts(draftStore, question.rpcId) ?? freshDrafts(question.questions)
			}));
			const [busy, setBusy] = (0, react.useState)(void 0);
			const [error, setError] = (0, react.useState)(void 0);
			const [minimized, setMinimized] = (0, react.useState)(false);
			const bodyId = (0, react.useId)();
			const focused = (0, react.useRef)(/* @__PURE__ */ new Set());
			const { index, drafts } = progress.key === question.rpcId ? progress : {
				key: question.rpcId,
				index: 0,
				drafts: freshDrafts(question.questions)
			};
			const item = question.questions[index];
			const draft = drafts[index] ?? { selected: [] };
			const options = item?.options ?? [];
			const hasOptions = options.length > 0;
			const last = index === question.questions.length - 1;
			const replaceProgress = (nextIndex, nextDrafts) => {
				setProgress({
					key: question.rpcId,
					index: nextIndex,
					drafts: nextDrafts
				});
			};
			(0, react.useEffect)(() => {
				if (progress.key !== question.rpcId) return;
				saveQuestionDrafts(draftStore, question.rpcId, progress.drafts);
			}, [progress, question.rpcId]);
			const updateDraft = (update, nextIndex = index) => {
				replaceProgress(nextIndex, drafts.map((entry, entryIndex) => entryIndex === index ? update(entry) : entry));
				setError(void 0);
			};
			const choose = (label) => {
				updateDraft((entry) => item?.multiSelect === true ? {
					...entry,
					selected: entry.selected.includes(label) ? entry.selected.filter((selected) => selected !== label) : [...entry.selected, label],
					skipped: false
				} : {
					selected: [label],
					custom: "",
					skipped: false
				}, item?.multiSelect !== true && !last ? index + 1 : index);
			};
			const setCustom = (text) => {
				updateDraft((entry) => ({
					...entry,
					selected: item?.multiSelect === true ? entry.selected : [],
					custom: text,
					skipped: false
				}));
			};
			const continueFromCustom = (event) => {
				if (event.key !== "Enter" || event.shiftKey || isComposing(event)) return;
				event.preventDefault();
				continueFlow();
			};
			const submit = (values) => {
				const missing = values.findIndex((entry) => !draftCompleted(entry));
				if (missing >= 0) {
					replaceProgress(missing, values);
					setError(t("review.interactionIncomplete"));
					return;
				}
				setBusy("answer");
				setError(void 0);
				controller.answerQuestion(question.rpcId, sessionId, answerBatchOf(question, values)).then((accepted) => {
					setBusy(void 0);
					if (!accepted) {
						setError(t("review.interactionRejected"));
						return;
					}
					draftStore.clear(questionDraftKey(question.rpcId));
				});
			};
			const continueFlow = () => {
				if (!draftAnswered(draft)) {
					setError(t("review.interactionUnanswered"));
					return;
				}
				if (!last) {
					replaceProgress(index + 1, drafts);
					setError(void 0);
					return;
				}
				submit(drafts);
			};
			const skip = () => {
				const nextDrafts = drafts.map((entry, entryIndex) => entryIndex === index ? {
					selected: [],
					custom: "",
					skipped: true
				} : entry);
				replaceProgress(last ? index : index + 1, nextDrafts);
				setError(void 0);
				if (!last) return;
				submit(nextDrafts);
			};
			const dismissAll = () => {
				setBusy("cancel");
				setError(void 0);
				controller.cancelQuestion(question.rpcId).then((accepted) => {
					setBusy(void 0);
					if (!accepted) setError(t("review.interactionRejected"));
				});
			};
			const disabled = busy !== void 0;
			/**
			* The option list is announced as `role="radiogroup"` (single-select) or `role="group"`
			* (multi-select), and a radiogroup PROMISES arrow-key navigation with roving tabindex.
			* Neither existed: the options were plain buttons reachable only by Tab, so the
			* announced interaction model was false and a five-option question cost five tab
			* stops. The roles were right; the behaviour was missing.
			*
			* WAI-ARIA radio grammar: Up/Down (and Left/Right, which the pattern also allows)
			* move within the group AND select, wrapping at both ends; Home/End jump to the
			* ends. For a checkbox group the arrows only move — toggling on arrow is checkbox
			* behaviour, not radio behaviour, and this group is a set of independent answers.
			* Enter keeps its existing job (submit the whole batch once every question is
			* complete); the two never collide because they are different keys.
			*/
			const optionRefs = (0, react.useRef)([]);
			const onOptionKeyDown = (event, optionIndex) => {
				const count = options.length;
				if (count === 0) return;
				let next;
				if (event.key === "ArrowDown" || event.key === "ArrowRight") next = (optionIndex + 1) % count;
				else if (event.key === "ArrowUp" || event.key === "ArrowLeft") next = (optionIndex - 1 + count) % count;
				else if (event.key === "Home") next = 0;
				else if (event.key === "End") next = count - 1;
				if (next === void 0) return;
				event.preventDefault();
				const target = options[next];
				if (target === void 0) return;
				if (item?.multiSelect !== true) choose(target.label);
				optionRefs.current[next]?.focus();
			};
			const selectedOptionIndex = options.findIndex((option) => draft.selected.includes(option.label));
			const rovingIndex = selectedOptionIndex >= 0 ? selectedOptionIndex : 0;
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("section", {
				className: board_module_css_default.interactionCard,
				"data-plan": void 0,
				"aria-label": item?.question ?? t("review.planAwaiting"),
				children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("header", {
					className: board_module_css_default.interactionHead,
					children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
						className: board_module_css_default.interactionHeading,
						children: [item?.header !== void 0 && item.header !== "" && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
							className: board_module_css_default.interactionEyebrow,
							children: item.header
						}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("h2", {
							className: board_module_css_default.interactionTitle,
							children: item?.question ?? ""
						})]
					}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
						className: board_module_css_default.interactionHeadActions,
						children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
							type: "button",
							className: board_module_css_default.iconButton,
							"aria-label": t(minimized ? "review.interactionExpand" : "review.interactionCollapse"),
							title: t(minimized ? "review.interactionExpand" : "review.interactionCollapse"),
							"aria-expanded": !minimized,
							"aria-controls": bodyId,
							disabled,
							onClick: () => {
								setMinimized((value) => !value);
							},
							children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, { name: minimized ? "chevronDown" : "arrowUp" })
						}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
							type: "button",
							className: board_module_css_default.iconButton,
							"aria-label": t("review.interactionAbandon"),
							title: t("review.interactionAbandon"),
							disabled,
							onClick: dismissAll,
							children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, { name: "close" })
						})]
					})]
				}), !minimized && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
					className: board_module_css_default.interactionBody,
					id: bodyId,
					children: [
						item?.detail !== void 0 && item.detail !== "" && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
							className: board_module_css_default.interactionDetail,
							children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Markdown, { text: item.detail })
						}),
						/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
							className: board_module_css_default.interactionOptions,
							role: item?.multiSelect === true ? "group" : "radiogroup",
							"aria-label": item?.question ?? "",
							children: [options.map((option, optionIndex) => {
								const selected = draft.selected.includes(option.label);
								const display = splitRecommended(option.label);
								return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("button", {
									type: "button",
									className: `${board_module_css_default.interactionOptionButton}${selected && item?.multiSelect !== true ? ` ${board_module_css_default.interactionOptionOn}` : ""}`,
									role: item?.multiSelect === true ? "checkbox" : "radio",
									"aria-checked": selected,
									"aria-label": display.label,
									ref: (element) => {
										optionRefs.current[optionIndex] = element;
									},
									tabIndex: optionIndex === rovingIndex ? 0 : -1,
									disabled,
									onClick: () => {
										choose(option.label);
									},
									onKeyDown: (event) => {
										onOptionKeyDown(event, optionIndex);
										if (event.defaultPrevented) return;
										if (event.key !== "Enter" || !drafts.every(draftCompleted)) return;
										event.preventDefault();
										submit(drafts);
									},
									children: [item?.multiSelect === true ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
										className: `${board_module_css_default.interactionCheck}${selected ? ` ${board_module_css_default.interactionCheckOn}` : ""}`,
										"aria-hidden": "true",
										children: selected && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, { name: "check" })
									}) : /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
										className: board_module_css_default.interactionOptionNumber,
										"aria-hidden": "true",
										children: optionIndex + 1
									}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
										className: board_module_css_default.interactionOptionCopy,
										children: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
											className: board_module_css_default.interactionOptionLine,
											children: [
												/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
													className: board_module_css_default.interactionOptionLabel,
													children: display.label
												}),
												display.recommended && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
													className: board_module_css_default.interactionBadge,
													children: t("review.interactionRecommended")
												}),
												option.description !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
													className: board_module_css_default.interactionOptionDescription,
													children: option.description
												})
											]
										})
									})]
								}, `${option.label}-${String(optionIndex)}`);
							}), hasOptions && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
								className: `${board_module_css_default.interactionCustomRow}${(draft.custom ?? "") !== "" ? ` ${board_module_css_default.interactionCustomRowOn}` : ""}`,
								children: [item?.multiSelect === true ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
									className: `${board_module_css_default.interactionCheck}${(draft.custom ?? "") !== "" ? ` ${board_module_css_default.interactionCheckOn}` : ""}`,
									"aria-hidden": "true",
									children: (draft.custom ?? "") !== "" && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, { name: "check" })
								}) : /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
									className: board_module_css_default.interactionOptionNumber,
									"aria-hidden": "true",
									children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, { name: "pencil" })
								}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(AnswerField, {
									variant: "inline",
									value: draft.custom ?? "",
									disabled,
									placeholder: t("review.interactionTypePlaceholder"),
									onChange: setCustom,
									onKeyDown: continueFromCustom
								})]
							})]
						}),
						!hasOptions && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(AnswerField, {
							variant: "block",
							focusOnMount: !focused.current.has(index),
							value: draft.custom ?? "",
							disabled,
							placeholder: t("review.interactionTypePlaceholder"),
							onFocus: () => {
								focused.current.add(index);
							},
							onChange: setCustom,
							onKeyDown: continueFromCustom
						})
					]
				}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("footer", {
					className: board_module_css_default.interactionFooter,
					children: [
						/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
							className: board_module_css_default.interactionPager,
							children: [
								/* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
									type: "button",
									className: board_module_css_default.iconButton,
									"aria-label": t("review.interactionPrev"),
									title: t("review.interactionPrev"),
									disabled: index === 0 || disabled,
									onClick: () => {
										replaceProgress(index - 1, drafts);
										setError(void 0);
									},
									children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, { name: "arrowLeft" })
								}),
								/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
									className: board_module_css_default.interactionProgress,
									children: t("review.interactionProgress", {
										n: String(index + 1),
										total: String(question.questions.length)
									})
								}),
								/* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
									type: "button",
									className: board_module_css_default.iconButton,
									"aria-label": t("review.interactionNextQuestion"),
									title: t("review.interactionNextQuestion"),
									disabled: last || disabled,
									onClick: () => {
										replaceProgress(index + 1, drafts);
										setError(void 0);
									},
									children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, { name: "arrowRight" })
								})
							]
						}),
						/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
							className: board_module_css_default.interactionFeedback,
							role: "status",
							children: error ?? ""
						}),
						/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
							className: board_module_css_default.interactionFooterActions,
							children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
								variant: "ghost",
								disabled,
								onClick: skip,
								children: t("review.interactionSkip")
							}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
								variant: "primary",
								disabled: disabled || !draftAnswered(draft),
								onClick: continueFlow,
								children: busy === "answer" ? t("review.interactionSubmitting") : last ? t("review.interactionSubmit") : t("review.interactionNext")
							})]
						})
					]
				})] })]
			});
		}
		/**
		* The auto-growing answer field (native grammar): a textarea over a hidden
		* mirror that owns the height, so a long answer soft-wraps and grows while
		* Shift+Enter still breaks a line; past the cap the textarea scrolls itself.
		* Mirror and textarea share font, line-height, padding and wrapping rules or
		* the two heights diverge.
		*/
		function AnswerField({ variant, value, placeholder, disabled, focusOnMount, onChange, onKeyDown, onFocus }) {
			const inputRef = (0, react.useRef)(null);
			(0, react.useEffect)(() => {
				if (focusOnMount === true) inputRef.current?.focus();
			}, [focusOnMount]);
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
				className: `${board_module_css_default.interactionField}${variant === "inline" ? ` ${board_module_css_default.interactionFieldInline}` : ` ${board_module_css_default.interactionFieldBlock}`}`,
				children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
					"aria-hidden": "true",
					className: board_module_css_default.interactionFieldMirror,
					children: `${value}\n`
				}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("textarea", {
					ref: inputRef,
					className: board_module_css_default.interactionFieldInput,
					value,
					disabled,
					rows: 1,
					placeholder,
					onFocus,
					onChange: (event) => {
						onChange(event.target.value);
					},
					onKeyDown
				})]
			});
		}
		/**
		* The plan under review: the plan body (Markdown, its own scroll region) with the
		* three decisions the hosted plan card itself offers — 去聊天里说 (dismiss the
		* request, keep discussing), 拒绝 and 确认执行. The approve label is the intent's
		* own, never inferred from option order.
		*
		* This card used to carry a fourth affordance, an amendment field whose text turned
		* a refusal into "revise with feedback". It was removed for parity: the hosted plan
		* card offers no such input, and a board card that invents an extra decision is not
		* the same card. `planDecisionAnswers` still accepts an amendment (the host API
		* supports revise-with-feedback), so restoring it is a UI change, not a new
		* capability — and `''` here is deliberate rather than an oversight.
		*/
		function PlanReviewCard({ question, item, sessionId, controller }) {
			const plan = item.detail ?? item.question;
			const [busy, setBusy] = (0, react.useState)(false);
			const [error, setError] = (0, react.useState)(void 0);
			const approve = (0, react.useMemo)(() => planDecisionAnswers(item, "approve", ""), [item]);
			const decline = (0, react.useMemo)(() => planDecisionAnswers(item, "decline", ""), [item]);
			const send = (answers) => {
				setBusy(true);
				setError(void 0);
				controller.answerQuestion(question.rpcId, sessionId, answers).then((accepted) => {
					setBusy(false);
					if (!accepted) setError(t("review.interactionRejected"));
				});
			};
			const discuss = () => {
				setBusy(true);
				setError(void 0);
				controller.cancelQuestion(question.rpcId).then((accepted) => {
					setBusy(false);
					if (!accepted) setError(t("review.interactionRejected"));
				});
			};
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("section", {
				className: board_module_css_default.interactionCard,
				"data-plan": "true",
				"aria-label": item.question,
				children: [
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
						className: board_module_css_default.interactionStrip,
						children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
							className: board_module_css_default.interactionStripDot,
							"aria-hidden": "true"
						}), t("review.planAwaiting")]
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
						className: board_module_css_default.interactionPlanBody,
						children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Markdown, { text: plan })
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
						className: board_module_css_default.interactionFooter,
						children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
							className: board_module_css_default.interactionFeedback,
							role: "status",
							children: error ?? ""
						}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
							className: board_module_css_default.interactionFooterActions,
							children: [
								/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
									variant: "ghost",
									disabled: busy,
									onClick: discuss,
									children: t("review.interactionDiscuss")
								}),
								/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
									variant: "ghost",
									disabled: busy,
									onClick: () => {
										send(decline);
									},
									children: t("review.planDecline")
								}),
								/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
									variant: "primary",
									disabled: busy,
									onClick: () => {
										send(approve);
									},
									children: t("review.planConfirm")
								})
							]
						})]
					})
				]
			});
		}
		/**
		* The read-only card for a carrier the board cannot settle (a snapshot entry
		* without its own answer/cancel, or no uiSession at all): the full batch —
		* every question, its detail, its options as plain text, never as clickable
		* answers — with ONE action: navigate to the native session, where the
		* official composer owns the call. Same chrome as the interactive card, so
		* the rail geometry never diverges between hosts. (A proven wait with no
		* readable body is the shell, rendered by the caller.)
		*/
		function MirrorQuestionCard({ question, sessionId, controller }) {
			const planQuestion = question.isPlanReview ? planQuestionOf(question) : void 0;
			const goAnswer = () => {
				controller.openSession(sessionId);
			};
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
				className: board_module_css_default.interactionCard,
				"data-plan": planQuestion !== void 0 ? "true" : void 0,
				children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
					className: board_module_css_default.interactionCardBody,
					children: planQuestion !== void 0 ? /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [
						/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Chip, {
							kind: "warn",
							title: t("review.planAwaiting"),
							children: t("review.planAwaiting")
						}),
						/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
							className: board_module_css_default.interactionQuestion,
							children: planQuestion.question
						}),
						planQuestion.detail !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
							className: board_module_css_default.interactionDetail,
							children: planQuestion.detail
						})
					] }) : /* @__PURE__ */ (0, react_jsx_runtime.jsx)(react_jsx_runtime.Fragment, { children: question.questions.map((item, index) => /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
						className: board_module_css_default.interactionQuestionBlock,
						children: [
							/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
								className: board_module_css_default.interactionQuestion,
								children: [
									/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Chip, {
										kind: "warn",
										title: t("review.questionIndex", {
											n: String(index + 1),
											total: String(question.questions.length)
										}),
										children: t("review.questionIndex", {
											n: String(index + 1),
											total: String(question.questions.length)
										})
									}),
									" ",
									item.question
								]
							}),
							item.detail !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
								className: board_module_css_default.interactionDetail,
								children: item.detail
							}),
							(item.options ?? []).length > 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
								className: board_module_css_default.interactionOptions,
								children: (item.options ?? []).map((option) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
									className: board_module_css_default.interactionOption,
									title: option.description,
									children: option.label
								}, option.label))
							})
						]
					}, item.id)) })
				}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
					className: board_module_css_default.interactionActions,
					children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
						variant: "primary",
						onClick: goAnswer,
						children: t("review.interactionGoAnswer")
					})
				})]
			});
		}
		//#endregion
		//#region src/client/board/session-panel.tsx
		/**
		* Shared session-panel components: the right-rail building blocks that every
		* session surface composes — the execution review page (ReviewDetail), the
		* linked-session panel (SessionDetail) and the refinement panel
		* (RefineSection). Each block is small and self-contained; the callers own
		* their rail layout and their semantic differences (a drive composer vs a
		* direct composer are deliberately NOT shared here — see the two panels).
		*
		* Everything reads the same session-generic faces (sessionInfo,
		* sessionConfig, transcript projections), so any block works for any native
		* session id.
		*/
		/** Model-select value encoding: provider + model, joined by a NUL separator. */
		const MODEL_SEP = "\0";
		/**
		* One message image: the durable ref is read back through the official
		* attachment RPC (cached + single-flighted by the wiring), then shown as a
		* click-to-open thumbnail. While loading it keeps its box (no layout jump);
		* a failed/absent read degrades to a quiet name chip, never a broken icon.
		*/
		function MessageImage({ controller, sessionId, image }) {
			const [src, setSrc] = (0, react.useState)(void 0);
			const [failed, setFailed] = (0, react.useState)(false);
			(0, react.useEffect)(() => {
				let alive = true;
				controller.loadImage(sessionId, image.attachmentId).then((value) => {
					if (!alive) return;
					if (value === void 0) setFailed(true);
					else setSrc(`data:${value.mediaType};base64,${value.data}`);
				});
				return () => {
					alive = false;
				};
			}, [
				controller,
				sessionId,
				image.attachmentId
			]);
			if (failed) return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
				className: board_module_css_default.reviewImageMissing,
				title: image.name ?? "",
				children: t("review.imageMissing")
			});
			if (src === void 0) return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
				className: board_module_css_default.reviewImageBox,
				"aria-hidden": "true"
			});
			return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("a", {
				className: board_module_css_default.reviewImageBox,
				href: src,
				target: "_blank",
				rel: "noreferrer",
				title: image.name ?? "",
				children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("img", {
					className: board_module_css_default.reviewImage,
					src,
					alt: image.name ?? "",
					loading: "lazy"
				})
			});
		}
		/**
		* One memoized transcript row. Props are the primitive render facts (never
		* the line object), so a light poll that re-folds the tail only re-renders
		* the rows whose content actually changed — long transcripts stay smooth.
		*/
		const TranscriptRow = (0, react.memo)(function TranscriptRow(props) {
			if (props.kind === "context") return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("li", {
				className: board_module_css_default.reviewContext,
				title: props.summary,
				children: [
					t("review.contextInjection"),
					" · ",
					props.plugin
				]
			});
			const images = props.images ?? [];
			const { controller, sessionId } = props;
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("li", {
				className: board_module_css_default.reviewMessage,
				"data-role": props.role,
				children: [images.length > 0 && controller !== void 0 && sessionId !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
					className: board_module_css_default.reviewImageRow,
					children: images.map((image) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)(MessageImage, {
						controller,
						sessionId,
						image
					}, image.attachmentId))
				}), props.text !== "" && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
					className: board_module_css_default.reviewMessageText,
					children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Markdown, { text: props.text })
				})]
			});
		});
		/**
		* The shared transcript-region content: optional header (the review page's
		* outcome banner), the waiting banner, error/loading/empty states, the
		* folded message list (optionally capped to the trailing N, with the native
		* "load earlier" affordance above it when the host holds older messages)
		* and the "滑到最新" pill. Callers own their scroll region and the tail
		* hook; this is pure rendering, so every live session surface looks and
		* behaves identically.
		*/
		function SessionTranscript({ lines, error, atBottom, jumpToBottom, waiting, maxLines, hasMore, loadingEarlier, pageError, pageUnsupported, onLoadEarlier, before, onRetry, sessionId, controller }) {
			const shown = lines === void 0 ? void 0 : maxLines === void 0 ? lines : lines.slice(-maxLines);
			const paging = hasMore === true && maxLines === void 0 && onLoadEarlier !== void 0 && pageUnsupported !== true;
			const pagingDead = hasMore === true && maxLines === void 0 && pageUnsupported === true;
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [
				before,
				/* @__PURE__ */ (0, react_jsx_runtime.jsx)(SessionWaitingNotice, { waiting }),
				paging && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
					className: board_module_css_default.transcriptEarlierRow,
					children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
						size: "sm",
						disabled: loadingEarlier === true,
						onClick: onLoadEarlier,
						children: t(loadingEarlier === true ? "review.loadingEarlierBusy" : "review.loadEarlier")
					}), pageError === true && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
						className: board_module_css_default.detailHint,
						children: t("review.loadEarlierFailed")
					})]
				}),
				pagingDead && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
					className: board_module_css_default.transcriptEarlierRow,
					children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
						className: board_module_css_default.detailHint,
						children: t("review.loadEarlierUnsupported")
					})
				}),
				error ? /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
					className: board_module_css_default.transcriptErrorRow,
					children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
						className: board_module_css_default.detailText,
						children: t("review.transcriptUnavailable")
					}), onRetry !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
						size: "sm",
						onClick: onRetry,
						children: t("review.retry")
					})]
				}) : shown === void 0 ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
					className: board_module_css_default.detailText,
					children: t("review.loading")
				}) : shown.length === 0 ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
					className: board_module_css_default.detailText,
					children: t("review.transcriptEmpty")
				}) : /* @__PURE__ */ (0, react_jsx_runtime.jsx)("ul", {
					className: board_module_css_default.reviewTranscript,
					children: shown.map((line) => line.kind === "context" ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)(TranscriptRow, {
						kind: "context",
						plugin: line.plugin,
						summary: line.summary
					}, line.id) : /* @__PURE__ */ (0, react_jsx_runtime.jsx)(TranscriptRow, {
						kind: "message",
						role: line.role,
						text: line.text,
						sessionId,
						controller,
						images: line.images
					}, line.id))
				}),
				/* @__PURE__ */ (0, react_jsx_runtime.jsx)(JumpToLatest, {
					atBottom,
					onJump: jumpToBottom
				})
			] });
		}
		/** One waiting banner, shared by every session surface. */
		function SessionWaitingNotice({ waiting }) {
			if (waiting === void 0) return null;
			return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Notice, {
				chip: t("review.waiting"),
				children: t("review.waitingTitle", { kind: t(waitingKeyOf(waiting)) })
			});
		}
		/**
		* The session's real workspace + composed Agent: read-only value rows INSIDE
		* the live config grid (the same row grammar as the model/effort/permission
		* selects, same field order as the task detail's 运行配置) — one module,
		* no separate facts strip, no explanatory paragraph. A value row renders in
		* the same box geometry as a select, so the read-only meaning is carried by
		* the absence of a dropdown affordance, never by prose.
		*/
		function SessionFacts({ info }) {
			if (info === void 0) return null;
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
				className: board_module_css_default.reviewConfigRow,
				children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
					className: board_module_css_default.reviewConfigLabel,
					children: t("review.sessionWorkspace")
				}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
					className: board_module_css_default.reviewConfigValue,
					title: info.cwd ?? void 0,
					children: info.cwd !== void 0 ? workspaceLabelOf(info.cwd) : t("review.sessionUnknown")
				})]
			}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
				className: board_module_css_default.reviewConfigRow,
				children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
					className: board_module_css_default.reviewConfigLabel,
					children: t("review.sessionAgent")
				}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
					className: board_module_css_default.reviewConfigValue,
					title: info.agentPreset ?? void 0,
					children: info.agentPreset !== void 0 ? info.agentPreset : t("review.sessionDefaultAgent")
				})]
			})] });
		}
		/**
		* The context meter block: native occupancy figure + colored composition
		* bar (from the transcript projections), the cumulative whole-log token
		* totals (from the `tokenUsage` projection — durable, not the paged-window
		* sum below), the whole-log turn/step figures (from `sessionStats`), or the
		* token-sum fallback strip when no projection is served. A high-occupancy
		* state (>= 85%) shifts the reading to the attention tone — the same warn
		* grammar as every board signal.
		*/
		function ContextMeterPanel({ projections, usage }) {
			const occupancy = contextOccupancy(projections?.contextPressure);
			const segments = occupancy !== void 0 ? contextSegments(occupancy, projections?.contextBreakdown) : void 0;
			const total = projections?.tokenUsage !== void 0 ? {
				inputTokens: projections.tokenUsage.uncachedInputTokens,
				outputTokens: projections.tokenUsage.outputTokens,
				cacheReadTokens: projections.tokenUsage.cacheReadTokens,
				cacheWriteTokens: projections.tokenUsage.cacheWriteTokens
			} : usage;
			const totalCumulative = projections?.tokenUsage !== void 0;
			if (occupancy !== void 0 && segments !== void 0) {
				const warn = occupancy.percent >= 85;
				return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
					className: board_module_css_default.reviewContextMeter,
					"data-warn": warn ? "" : void 0,
					"aria-label": t("review.meterOf", { percent: `${occupancy.percent}%` }),
					children: [
						/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
							className: board_module_css_default.reviewMeterHead,
							children: [
								/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
									className: board_module_css_default.reviewMeterReading,
									children: t("review.meterUsed")
								}),
								/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
									className: board_module_css_default.reviewMeterPercent,
									children: [occupancy.percent, "%"]
								}),
								/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
									className: board_module_css_default.reviewMeterFigures,
									children: t("review.meterFigures", {
										used: formatTokens(occupancy.usedTokens),
										window: formatTokens(occupancy.contextWindow)
									})
								})
							]
						}),
						/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
							className: board_module_css_default.reviewMeterBar,
							"aria-hidden": "true",
							children: segments.map((segment) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
								className: `${board_module_css_default.reviewMeterSegment}${segment.className !== void 0 ? ` ${board_module_css_default[segment.className]}` : ""}`,
								style: { width: `${segment.width}%` }
							}, segment.key))
						}),
						(() => {
							const breakdown = projections?.contextBreakdown;
							if (breakdown === void 0) return null;
							return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
								className: board_module_css_default.reviewMeterRows,
								children: [
									/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
										className: board_module_css_default.reviewMeterRow,
										children: [
											/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
												className: `${board_module_css_default.reviewMeterSwatch} ${board_module_css_default.meterSystem}`,
												"aria-hidden": "true"
											}),
											/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", { children: t("review.meterSystem") }),
											/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
												className: board_module_css_default.reviewMeterValue,
												children: ["~", formatTokens(breakdown.systemTokens)]
											})
										]
									}),
									/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
										className: board_module_css_default.reviewMeterRow,
										children: [
											/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
												className: `${board_module_css_default.reviewMeterSwatch} ${board_module_css_default.meterTools}`,
												"aria-hidden": "true"
											}),
											/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", { children: t("review.meterTools") }),
											/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
												className: board_module_css_default.reviewMeterValue,
												children: ["~", formatTokens(breakdown.toolsTokens)]
											})
										]
									}),
									/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
										className: board_module_css_default.reviewMeterRow,
										children: [
											/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
												className: `${board_module_css_default.reviewMeterSwatch} ${board_module_css_default.meterMessages}`,
												"aria-hidden": "true"
											}),
											/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", { children: t("review.meterMessages") }),
											/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
												className: board_module_css_default.reviewMeterValue,
												children: ["~", formatTokens(breakdown.messageTokens)]
											})
										]
									})
								]
							});
						})(),
						totalCumulative && total !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("p", {
							className: board_module_css_default.reviewUsage,
							children: [
								t("review.usageTotal"),
								" · ",
								t("review.usageInput", { n: formatTokens(total.inputTokens) }),
								" · ",
								t("review.usageOutput", { n: formatTokens(total.outputTokens) }),
								total.cacheReadTokens !== void 0 && ` · ${t("review.usageCacheRead", { n: formatTokens(total.cacheReadTokens) })}`,
								total.cacheWriteTokens !== void 0 && ` · ${t("review.usageCacheWrite", { n: formatTokens(total.cacheWriteTokens) })}`,
								(() => {
									const hit = total.cacheReadTokens !== void 0 ? cacheHitRate(total.cacheReadTokens, total.inputTokens) : void 0;
									return hit !== void 0 ? ` · ${t("review.usageHitRate", { p: hit })}` : null;
								})()
							]
						}),
						(() => {
							const stats = projections?.sessionStats;
							if (stats === void 0) return null;
							const ttft = stats.ttftSteps > 0 ? t("review.statsTtft", { d: formatDuration(stats.ttftMs / stats.ttftSteps) }) : void 0;
							const tps = formatTps(stats.decodeTokens, stats.decodeMs);
							return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("p", {
								className: board_module_css_default.reviewUsage,
								children: [
									t("review.statsTurns", {
										turns: String(stats.turns),
										steps: String(stats.steps)
									}),
									(stats.llmMs > 0 || stats.toolMs > 0) && ` · ${t("review.statsTime", {
										llm: formatDuration(stats.llmMs),
										tool: formatDuration(stats.toolMs)
									})}`,
									ttft !== void 0 && ` · ${ttft}`,
									tps !== void 0 && ` · ${t("review.usageTps", { n: tps })}`
								]
							});
						})()
					]
				});
			}
			if (total !== void 0) {
				if (totalCumulative) return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("p", {
					className: board_module_css_default.reviewUsage,
					children: [
						t("review.usageTotal"),
						" · ",
						t("review.usageInput", { n: formatTokens(total.inputTokens) }),
						" · ",
						t("review.usageOutput", { n: formatTokens(total.outputTokens) }),
						total.cacheReadTokens !== void 0 && ` · ${t("review.usageCacheRead", { n: formatTokens(total.cacheReadTokens) })}`,
						total.cacheWriteTokens !== void 0 && ` · ${t("review.usageCacheWrite", { n: formatTokens(total.cacheWriteTokens) })}`
					]
				});
				return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("p", {
					className: board_module_css_default.reviewUsage,
					children: [
						t("review.usage"),
						"：",
						t("review.usageInput", { n: String(total.inputTokens) }),
						" · ",
						t("review.usageOutput", { n: String(total.outputTokens) }),
						total.cacheReadTokens !== void 0 && ` · ${t("review.usageCacheRead", { n: String(total.cacheReadTokens) })}`,
						total.cacheWriteTokens !== void 0 && ` · ${t("review.usageCacheWrite", { n: String(total.cacheWriteTokens) })}`,
						total.reasoningTokens !== void 0 && ` · ${t("review.usageReasoning", { n: String(total.reasoningTokens) })}`
					]
				});
			}
			return null;
		}
		/**
		* The live session-config editor: model / reasoning effort / permission of
		* any native session, straight from the native APIs (the same sources the
		* harness's own selectors read). Permission applies through the native
		* `/permission` command registry — never a model turn. Self-contained: it
		* loads the model directory on mount and refreshes after every change; the
		* caller may bump `reloadKey` to force a re-read.
		*/
		function SessionConfigEditor({ sessionId, controller, permissionValue, permissionOptions, onChanged, reloadKey }) {
			const sessionConfig = controller.sessionConfig();
			const [sessionModels, setSessionModels] = (0, react.useState)(void 0);
			const [configFailed, setConfigFailed] = (0, react.useState)(false);
			const [configBusy, setConfigBusy] = (0, react.useState)(false);
			const [configMessage, setConfigMessage] = (0, react.useState)(void 0);
			const [permissionRows, setPermissionRows] = (0, react.useState)(void 0);
			const [chosen, setChosen] = (0, react.useState)(void 0);
			const aliveRef = (0, react.useRef)(true);
			(0, react.useEffect)(() => () => {
				aliveRef.current = false;
			}, []);
			(0, react.useEffect)(() => {
				if (!configBusy && permissionValue !== void 0 && permissionValue !== "" && permissionValue !== chosen) setChosen(void 0);
			}, [
				permissionValue,
				chosen,
				configBusy
			]);
			(0, react.useEffect)(() => {
				let alive = true;
				(async () => {
					const rows = await controller.runCatalog()?.listPermissions();
					if (alive) setPermissionRows(rows);
				})();
				return () => {
					alive = false;
				};
			}, [controller]);
			/** Re-read the live session panel (model selection + directory). */
			const reloadPanel = (0, react.useCallback)(() => {
				if (sessionConfig === void 0) return;
				sessionConfig.readModels(sessionId).then((result) => {
					if (!aliveRef.current) return;
					if (result === void 0) setConfigFailed(true);
					else {
						setConfigFailed(false);
						setSessionModels(result);
					}
				});
			}, [sessionConfig, sessionId]);
			const [retryNonce, setRetryNonce] = (0, react.useState)(0);
			(0, react.useEffect)(() => {
				reloadPanel();
			}, [
				reloadPanel,
				reloadKey,
				retryNonce
			]);
			const pendingRetryRef = (0, react.useRef)(false);
			const failedRef = (0, react.useRef)(false);
			failedRef.current = configFailed;
			const scheduleRetry = (0, react.useCallback)(() => {
				if (pendingRetryRef.current) return;
				pendingRetryRef.current = true;
				setTimeout(() => {
					pendingRetryRef.current = false;
					if (aliveRef.current) setRetryNonce((value) => value + 1);
				}, 2e3);
			}, []);
			(0, react.useEffect)(() => {
				const onVisibility = () => {
					if (!document.hidden && failedRef.current) scheduleRetry();
				};
				document.addEventListener("visibilitychange", onVisibility);
				return () => {
					document.removeEventListener("visibilitychange", onVisibility);
				};
			}, [scheduleRetry]);
			(0, react.useEffect)(() => {
				if (configFailed) scheduleRetry();
			}, [configFailed, scheduleRetry]);
			/** Apply a new model selection to the session. */
			const applyModel = (key) => {
				if (sessionConfig === void 0 || sessionModels === void 0) return;
				const sepIndex = key.indexOf(MODEL_SEP);
				if (sepIndex < 0) return;
				const provider = key.slice(0, sepIndex);
				const model = key.slice(sepIndex + 1);
				const effort = (sessionModels.groups.find((candidate) => candidate.provider === provider)?.models.find((candidate) => candidate.id === model))?.reasoning?.defaultEffort;
				setConfigBusy(true);
				setConfigMessage(void 0);
				sessionConfig.selectModel(sessionId, {
					provider,
					model,
					...effort !== void 0 ? { reasoningEffort: effort } : {}
				}).then((result) => {
					if (!aliveRef.current) return;
					setConfigBusy(false);
					setConfigMessage(result.ok ? t("review.configApplied") : result.error);
					if (result.ok) {
						reloadPanel();
						onChanged?.();
					}
				});
			};
			/** Apply a reasoning effort to the current model selection. */
			const applyEffort = (effort) => {
				if (sessionConfig === void 0 || sessionModels === void 0) return;
				const selection = sessionModels.current;
				setConfigBusy(true);
				setConfigMessage(void 0);
				sessionConfig.selectModel(sessionId, {
					provider: selection.provider,
					model: selection.model,
					...effort !== "" ? { reasoningEffort: effort } : {}
				}).then((result) => {
					if (!aliveRef.current) return;
					setConfigBusy(false);
					setConfigMessage(result.ok ? t("review.configApplied") : result.error);
					if (result.ok) {
						reloadPanel();
						onChanged?.();
					}
				});
			};
			/** Apply a permission preset to the session. */
			const applyPermission = (permission) => {
				if (sessionConfig === void 0 || permission === "") return;
				setConfigBusy(true);
				setConfigMessage(void 0);
				sessionConfig.setPermission(sessionId, permission).then((result) => {
					if (!aliveRef.current) return;
					setConfigBusy(false);
					setConfigMessage(result.ok ? t("review.configApplied") : result.error);
					if (result.ok) {
						reloadPanel();
						onChanged?.();
					} else setChosen(void 0);
				});
			};
			if (sessionConfig === void 0) return null;
			const options = permissionOptions ?? permissionRows;
			const value = chosen ?? permissionValue ?? "";
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("section", {
				className: board_module_css_default.reviewConfig,
				children: [
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.reviewConfigTitle,
						children: t("review.config")
					}),
					sessionModels === void 0 ? configFailed ? /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
						className: board_module_css_default.reviewConfigFailed,
						children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
							className: board_module_css_default.reviewConfigUnavailable,
							children: t("review.configUnavailable")
						}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
							size: "sm",
							onClick: scheduleRetry,
							children: t("review.retry")
						})]
					}) : /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.reviewConfigLoading,
						children: t("review.configLoading")
					}) : /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
						className: board_module_css_default.reviewConfigGrid,
						children: [
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)(SessionFacts, { info: controller.sessionInfo(sessionId) }),
							/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
								className: board_module_css_default.reviewConfigRow,
								children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
									className: board_module_css_default.reviewConfigLabel,
									children: t("review.model")
								}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
									className: board_module_css_default.selectWrap,
									children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("select", {
										className: board_module_css_default.input,
										value: `${sessionModels.current.provider}${MODEL_SEP}${sessionModels.current.model}`,
										disabled: configBusy,
										onChange: (event) => {
											applyModel(event.target.value);
										},
										children: sessionModels.groups.map((group) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)("optgroup", {
											label: group.provider,
											children: group.models.map((model) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)("option", {
												value: `${group.provider}${MODEL_SEP}${model.id}`,
												children: model.name ?? model.id
											}, `${group.provider}${MODEL_SEP}${model.id}`))
										}, group.provider))
									})
								})]
							}),
							(() => {
								const efforts = (sessionModels.groups.find((candidate) => candidate.provider === sessionModels.current.provider)?.models.find((candidate) => candidate.id === sessionModels.current.model))?.reasoning?.efforts ?? [];
								if (efforts.length === 0) return null;
								return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
									className: board_module_css_default.reviewConfigRow,
									children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
										className: board_module_css_default.reviewConfigLabel,
										children: t("review.effort")
									}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
										className: board_module_css_default.selectWrap,
										children: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("select", {
											className: board_module_css_default.input,
											value: sessionModels.current.reasoningEffort ?? "",
											disabled: configBusy,
											onChange: (event) => {
												applyEffort(event.target.value);
											},
											children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("option", {
												value: "",
												children: t("review.effortDefault")
											}), efforts.map((effort) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)("option", {
												value: effort.id,
												children: effort.name ?? effort.id
											}, effort.id))]
										})
									})]
								});
							})(),
							options !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
								className: board_module_css_default.reviewConfigRow,
								children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
									className: board_module_css_default.reviewConfigLabel,
									children: t("review.permission")
								}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
									className: board_module_css_default.selectWrap,
									children: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("select", {
										className: board_module_css_default.input,
										value,
										disabled: configBusy,
										onChange: (event) => {
											setChosen(event.target.value);
											applyPermission(event.target.value);
										},
										children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("option", {
											value: "",
											children: t("new.permissionDefault")
										}), options.map((row) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)("option", {
											value: row.id,
											children: permissionLabel(row.id, row.name)
										}, row.id))]
									})
								})]
							})
						]
					}),
					configMessage !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.reviewConfigMessage,
						children: configMessage
					})
				]
			});
		}
		/**
		* The rail head BODY: context meter + live config editor + session facts,
		* composed in the order every session panel shows them. SessionRail renders
		* it inside the shared Disclosure (the fold + chevron grammar) within the
		* rail's single scroll body; callers pass what they read from their
		* transcript hook (projections via onResult, folded lines). The permission
		* switcher's projection mapping is derived HERE — one source for every panel
		* (a caller-level mapping is how the linked panel lost it).
		*/
		function SessionRailHead({ sessionId, controller, projections, lines, onChanged, reloadKey }) {
			if (sessionId === void 0) return null;
			const livePermission = projections?.permissions;
			const permissionOptions = livePermission !== void 0 ? livePermission.options.map((option) => ({
				id: option.value,
				name: option.name,
				...option.description !== void 0 ? { description: option.description } : {}
			})) : void 0;
			const permissionValue = livePermission !== void 0 ? livePermission.currentValue : void 0;
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(ContextMeterPanel, {
				projections,
				usage: sumUsage(lines ?? [])
			}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(SessionConfigEditor, {
				sessionId,
				controller,
				onChanged,
				reloadKey,
				permissionValue,
				permissionOptions
			})] });
		}
		/**
		* THE session rail — the one composition every session panel renders (the
		* execution review page and the linked-session panel share it verbatim), and
		* THE height contract that ends the squeeze-and-clip family of bugs.
		*
		* It hands its caller TWO blocks, never four:
		*
		*   [ folds: live state row → collapsible CONFIG head → collapsible COMMENTS ]
		*   [ the caller's pinned composer                                           ]
		*
		* Desktop geometry: the rail is a flex column — the folds block absorbs the
		* height (its comments region owns the one scroll box inside it) and the
		* composer is `flex: none` below, so nothing can push the send box away.
		*
		* Narrow geometry (the panel's own container query): the rail box steps aside
		* (`display: contents`) and the panel becomes a THREE-ROW grid —
		* `folds / transcript / composer`. That is the whole fix for 「上下文一多就把
		* 评论和留言按钮全部截断」:
		*   - the folds block is capped (a fraction of the panel) and bounds a fully
		*     rendered head, so an expanded config can never eat the screen;
		*   - the comments box owns its capped scroll at every width (one grammar —
		*     滑到最新 drives it, desktop and phone);
		*   - the transcript owns the flexible middle row and always keeps room;
		*   - the composer is its own grid row at the panel's bottom edge — not the
		*     last item in someone else's scroll content (which is exactly how it used
		*     to end up below the fold, unreachable).
		* Because the folds block is capped and the comments box scrolls inside
		* its own cap, opening one fold never reflows the other, and 滑到最新
		* drives the box that actually holds the comments — at every width.
		*
		* Callers pass data and their send semantics; the grammar, the folds and the
		* hint line live here exactly once — no panel can drift again.
		*/
		function SessionRail({ stateChip, updatedAt, sessionId, controller, projections, lines, onChanged, reloadKey, task, thread, onCancelComment, interaction, shellWaiting, composer }) {
			const commentsScrollRef = (0, react.useRef)(null);
			const [commentsAtBottom, setCommentsAtBottom] = (0, react.useState)(true);
			const [narrowPanel, foldsRef] = useSurfaceNarrow("[data-dsh-taskboard-panel]", 600);
			const [headOpen, setHeadOpen] = (0, react.useState)(!narrowPanel);
			const [commentsOpen, setCommentsOpen] = (0, react.useState)(!narrowPanel);
			const threadFingerprint = thread.map((view) => `${view.round.id}:${view.state}`).join("|");
			const awaiting = interaction !== void 0 || shellWaiting !== void 0;
			const { measure: onCommentsScroll, jumpToBottom: jumpComments } = useFollowScroll(commentsScrollRef, commentsAtBottom, setCommentsAtBottom, threadFingerprint, commentsOpen || awaiting, awaiting);
			(0, react.useEffect)(() => {
				if (awaiting) jumpComments();
			}, [
				awaiting,
				interaction?.rpcId,
				shellWaiting,
				jumpComments
			]);
			const occupancy = contextOccupancy(projections?.contextPressure);
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
				className: board_module_css_default.sessionRailFolds,
				ref: foldsRef,
				children: [
					stateChip !== void 0 && updatedAt !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
						className: board_module_css_default.sessionFacts,
						children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Chip, {
							kind: stateChip.kind,
							icon: stateChip.spinner === true ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
								className: board_module_css_default.spinner,
								"aria-hidden": "true"
							}) : void 0,
							children: stateChip.label
						}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
							className: board_module_css_default.sessionFactTime,
							children: [
								t("detail.sessionUpdated"),
								" ",
								updatedAt
							]
						})]
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
						className: board_module_css_default.sessionRailHead,
						children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Disclosure, {
							title: t("review.railHeadTitle"),
							summary: occupancy !== void 0 ? `${occupancy.percent}%` : void 0,
							open: headOpen,
							onToggle: () => {
								setHeadOpen(!headOpen);
							},
							children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(SessionRailHead, {
								sessionId,
								controller,
								projections,
								lines,
								onChanged,
								reloadKey
							})
						})
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
						className: board_module_css_default.sessionRailComments,
						"data-open": commentsOpen || awaiting,
						children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Disclosure, {
							title: t("review.comments"),
							summary: awaiting ? t("review.waiting") : String(thread.length),
							open: commentsOpen || awaiting,
							onToggle: () => {
								if (!awaiting) setCommentsOpen(!commentsOpen);
							},
							children: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
								className: board_module_css_default.commentsScroll,
								ref: commentsScrollRef,
								onScroll: onCommentsScroll,
								children: [
									/* @__PURE__ */ (0, react_jsx_runtime.jsx)(CommentsThread, {
										task,
										views: thread,
										onCancel: onCancelComment
									}),
									awaiting && sessionId !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(InteractionCard, {
										question: interaction,
										shellWaiting: interaction === void 0 ? shellWaiting : void 0,
										sessionId,
										controller
									}, interaction !== void 0 ? interaction.rpcId : `shell-${sessionId}`),
									/* @__PURE__ */ (0, react_jsx_runtime.jsx)(JumpToLatest, {
										atBottom: commentsAtBottom,
										onJump: jumpComments
									})
								]
							})
						})
					})
				]
			}), composer] });
		}
		/**
		* The pinned composer of the rail — one grammar for every session panel:
		* prompt input (slash autocomplete) + attachment strip + send-mode switch
		* (排队/插话) + the primary send button + ONE quiet line under it.
		*
		* The explanation line belongs to the COMPOSER, not to the comment thread:
		* what 排队/插话 do, and why sending is refused (a finished task, a gone
		* session), is information a touch user must be able to reach — it can never
		* live only in a foldable explanation or a hover-only tooltip. Draft / steer
		* here (the per-session draft slot, shared across panels); the caller
		* supplies only the send semantics:
		*   - onDrive(text, images, files) schedules a session-anchored comment round
		*     (true = saved, the draft clears) — / commands route through the native
		*     registry; attachments ride the round and go out WITH it when the lane
		*     frees (排队 means wait, with or without attachments);
		*   - onSteer(text) delivers the comment straight to the session now;
		*   - onSteerImages(text, images) delivers text + images at once (插话 with a
		*     picture); onSteerFiles(text, images, files) the same with files. The
		*     send mode is the user's toggle — attachments never force steer.
		*/
		/** Stage one file's bytes on a session via the controller's upload face. */
		function useFileStager(controller, sessionId) {
			if (sessionId === void 0) return void 0;
			const upload = controller.uploadFile(sessionId);
			if (upload === void 0) return void 0;
			return async (target, file) => upload(target, file);
		}
		function SessionComposer({ controller, taskId, sessionId, placeholder, disabled, hint, onDrive, onSteer, onSteerImages, onSteerFiles }) {
			const storeKey = sessionId === void 0 ? void 0 : commentDraftKey(taskId, sessionId);
			const [restored] = (0, react.useState)(() => decodeCommentDraft(storeKey !== void 0 ? draftStore.get(storeKey) : void 0));
			const [draft, setDraft] = (0, react.useState)(restored.text);
			const [steer, setSteer] = (0, react.useState)(false);
			const [unrestoredFiles, setUnrestoredFiles] = (0, react.useState)(restored.fileNames);
			const [draftOversized, setDraftOversized] = (0, react.useState)(false);
			const stager = useFileStager(controller, sessionId);
			const attachments = useComposerImages(COMMENT_IMAGE_BUDGET, 9, void 0, stager !== void 0 && sessionId !== void 0 ? {
				sessionId,
				stage: stager
			} : void 0);
			const { images: attachedImages, files: attachedFiles, setImages, setFiles, addFiles, dropProps } = attachments;
			const [sendError, setSendError] = (0, react.useState)(void 0);
			(0, react.useEffect)(() => {
				if (storeKey === void 0) return;
				const { value, imagesDropped } = encodeCommentDraft(draft, attachedImages, attachedFiles);
				draftStore.set(storeKey, value);
				setDraftOversized(imagesDropped);
			}, [
				storeKey,
				draft,
				attachedImages,
				attachedFiles
			]);
			(0, react.useEffect)(() => {
				if (restored.images.length > 0) setImages(restored.images);
			}, []);
			const clear = () => {
				setDraft("");
				setImages([]);
				setFiles([]);
				setSendError(void 0);
				setUnrestoredFiles([]);
				setDraftOversized(false);
				if (storeKey !== void 0) draftStore.clear(storeKey);
			};
			const submit = () => {
				const text = draft.trim();
				if (text === "" && attachedImages.length === 0 && attachedFiles.length === 0 || disabled === true) return;
				const restore = () => {
					setDraft(text);
					setImages(attachedImages);
					setFiles(attachedFiles);
				};
				clear();
				if (steer) {
					(attachedFiles.length > 0 && onSteerFiles !== void 0 ? onSteerFiles(text, attachedImages, attachedFiles) : attachedImages.length > 0 ? onSteerImages(text, attachedImages) : onSteer(text)).then((ok) => {
						if (!ok) restore();
					});
					return;
				}
				if (!onDrive(text, attachedImages, attachedFiles)) restore();
			};
			const busyLabel = attachments.busy ? attachBusyLabel(attachments.busyKind) : void 0;
			const draftNotice = draftOversized ? t("review.draftImagesDropped") : unrestoredFiles.length > 0 ? t("review.draftFilesGone", { names: unrestoredFiles.slice(0, 3).join("、") }) : void 0;
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
				className: board_module_css_default.reviewComposer,
				...dropProps,
				children: [
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)(PromptInput, {
						value: draft,
						onChange: (next) => {
							setDraft(next);
						},
						placeholder,
						rows: 3,
						controller,
						sessionId
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)(AttachmentStrip, {
						images: attachedImages,
						files: attachedFiles,
						onAdd: (files) => {
							if (pickedHasFiles(files)) setUnrestoredFiles([]);
							addFiles(files);
						},
						onRemoveImage: (id) => {
							setImages(attachedImages.filter((image) => image.id !== id));
						},
						onRemoveFile: (id) => {
							setFiles(attachedFiles.filter((file) => file.id !== id));
						},
						busy: attachments.busy,
						busyLabel,
						error: attachments.error ?? sendError ?? draftNotice
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
						className: board_module_css_default.reviewComposerRow,
						children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(SendModeToggle, {
							steer,
							onChange: setSteer
						}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
							variant: "primary",
							disabled: draft.trim() === "" && attachedImages.length === 0 && attachedFiles.length === 0 || disabled === true,
							onClick: submit,
							children: t("review.commentSend")
						})]
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
						className: board_module_css_default.detailHint,
						children: hint ?? t("detail.sessionDriveHint")
					})
				]
			});
		}
		//#endregion
		//#region src/client/board/interaction.ts
		/**
		* Read the latest `todo/write` snapshot out of a session's events (last-write
		* wins — the newest event holds the whole list). Returns the todo rows, or
		* undefined when the session never wrote one.
		*/
		function latestSessionTodos(events) {
			if (!Array.isArray(events)) return void 0;
			for (let index = events.length - 1; index >= 0; index -= 1) {
				const event = events[index];
				if (event.type !== "todo/write") continue;
				const data = event.data;
				if (!Array.isArray(data?.todos)) return void 0;
				return data.todos.map((row) => {
					if (typeof row !== "object" || row === null) return void 0;
					const entry = row;
					if (typeof entry.content !== "string" || entry.content === "") return void 0;
					const status = entry.status;
					const normalized = status === "in_progress" || status === "completed" ? status : "pending";
					return {
						content: entry.content,
						status: normalized
					};
				}).filter((row) => row !== void 0);
			}
		}
		/** Subagent statuses that mean "finished" (the tolerant set: a native
		*  reshape that adds a new word degrades to "still active" — the context
		*  block shows work in flight, never finished work). 'inactive' is the
		*  native word the host bridge maps activity to; the legacy words stay for
		*  older bridge payloads. */
		const FINISHED_SUBAGENT_STATUS = /* @__PURE__ */ new Set([
			"inactive",
			"finished",
			"completed",
			"done",
			"settled"
		]);
		/**
		* Whether the session context is WORTH showing: at least one OPEN todo, an
		* ACTIVE goal, or an unfinished subagent. Fully-done things are not the
		* readout's business — when everything listed is finished, the block hides
		* entirely (the "todo 全完成还显示上下文" issue); an unknown subagent
		* status is treated as active (shown) so a host reshape never hides running
		* work. THE one predicate every context surface reads.
		*/
		function contextWorthOf(context) {
			if ((context.todos ?? []).some((todo) => todo.status !== "completed")) return true;
			if (context.goal?.active === true) return true;
			if ((context.subagents ?? []).some((sub) => sub.status === void 0 || !FINISHED_SUBAGENT_STATUS.has(sub.status))) return true;
			return false;
		}
		//#endregion
		//#region src/client/board/use-interaction.ts
		/**
		* Pending native question + session context over the composer: the review
		* page, session panel and refinement panel all render one surface while the
		* session's agent is awaiting a human decision (a plan for confirmation or an
		* ask_user_question), alongside the session's live to-do / goal / subagent
		* readout.
		*
		* The pending QUESTION comes from the controller's question face
		* (questionPendingOf / subscribeQuestions) — on 0.1.5 the official pending
		* snapshot, whose carrier the card answers through in place (identical to the
		* native question card); on older hosts the live tracker whose frame settles
		* the suspended call. The card appears/disappears with the real tool
		* lifecycle either way. The to-do list still rides the transcript tail
		* (last-write-wins `todo/write` snapshot); goal + subagents come from the
		* narrow session-state bridge when the host registered it.
		*/
		const STATE_URL = "/api/dsh-task-board/session-state";
		/** One 3s poll: transcript → todos; bridge → goal + subagents. The board's
		*  tree stays mounted while hidden (the conversation view takes over), so the
		*  poll pauses whenever the board is not visible — `data-dsh-taskboard-active`
		*  on <html> is the ONE visibility marker (board-mount owns it) — and resumes
		*  with an immediate refresh the moment the board reappears. */
		function useSessionContext(controller, sessionId) {
			const [context, setContext] = (0, react.useState)({});
			(0, react.useEffect)(() => {
				if (sessionId === void 0) {
					setContext({});
					return;
				}
				let alive = true;
				let timer;
				const visible = () => document.documentElement.hasAttribute("data-dsh-taskboard-active");
				const pollTranscript = () => {
					controller.loadTranscript(sessionId).then((result) => {
						if (!alive || result === void 0) return;
						const projected = result.projections?.goal;
						const goal = projected === void 0 ? void 0 : projected === null || projected.phase === "complete" ? null : {
							title: projected.objective,
							active: true,
							id: projected.id,
							phase: projected.phase,
							roundsStarted: projected.roundsStarted,
							...projected.blockedReason !== void 0 ? { blockedReason: projected.blockedReason } : {}
						};
						setContext((current) => ({
							...current,
							todos: result.projections?.todos ?? latestSessionTodos(result.events),
							...goal !== void 0 ? { goal: goal === null ? void 0 : {
								...goal,
								...goal.id !== void 0 && current.goal?.id === goal.id && current.goal.activation !== void 0 ? { activation: current.goal.activation } : {}
							} } : {}
						}));
					});
				};
				const pollSessionState = () => {
					fetch(`${STATE_URL}?sessionId=${encodeURIComponent(sessionId)}`).then((response) => response.ok ? response.json() : void 0).then((view) => {
						if (!alive) return;
						setContext((current) => ({
							...current,
							...current.goal === void 0 ? { goal: view?.goal } : {},
							subagents: view?.subagents
						}));
					}).catch(() => {});
				};
				const poll = () => {
					pollTranscript();
					pollSessionState();
				};
				const observer = new MutationObserver(() => {
					if (visible()) poll();
				});
				observer.observe(document.documentElement, {
					attributes: true,
					attributeFilter: ["data-dsh-taskboard-active"]
				});
				if (visible()) poll();
				timer = window.setInterval(() => {
					if (visible()) {
						pollTranscript();
						pollSessionState();
					}
				}, 3e3);
				return () => {
					alive = false;
					if (timer !== void 0) window.clearInterval(timer);
					observer.disconnect();
				};
			}, [controller, sessionId]);
			(0, react.useEffect)(() => {
				if (sessionId === void 0) return void 0;
				const verbs = controller.goalVerbs(sessionId);
				if (verbs === void 0) return void 0;
				verbs.get().then((raw) => {
					const view = raw;
					if (view === null || typeof view !== "object") return;
					if (view.activation !== "armed" && view.activation !== "disarmed") return;
					const activation = view.activation;
					setContext((current) => current.goal === void 0 ? current : {
						...current,
						goal: {
							...current.goal,
							activation
						}
					});
				}).catch(() => {});
				return controller.subscribeGoalActivation(sessionId, (change) => {
					setContext((current) => {
						if (current.goal === void 0) return current;
						if (change === void 0) {
							if (current.goal.activation === void 0) return current;
							const { activation: _dropped, ...rest } = current.goal;
							return {
								...current,
								goal: rest
							};
						}
						return {
							...current,
							goal: {
								...current.goal,
								activation: change.activation
							}
						};
					});
				});
			}, [controller, sessionId]);
			return context;
		}
		/** The pending wire question for one session (mirror-driven, reactive).
		*  Module-private: display surfaces read {@link useAwaitingCard}. */
		function useWireQuestion(controller, sessionId) {
			const [question, setQuestion] = (0, react.useState)(() => controller.questionPendingOf(sessionId));
			(0, react.useEffect)(() => {
				if (sessionId === void 0) {
					setQuestion(void 0);
					return;
				}
				setQuestion(controller.questionPendingOf(sessionId));
				return controller.subscribeQuestions(() => {
					setQuestion(controller.questionPendingOf(sessionId));
				});
			}, [controller, sessionId]);
			return question;
		}
		/**
		* What the comment interface shows for a session's wait: parsed content, or —
		* when the session list proves a plan/question wait but no content parsed —
		* an honest shell (kind + navigate) instead of blank nothing. The shell is
		* the backstop against carrier-shape drift on any present or future host:
		* a proven wait can never again reach the UI as silence. Every surface with
		* a comment composer reads this one hook (review page, session panel,
		* refinement answers) — never useWireQuestion directly for display.
		*/
		function useAwaitingCard(controller, sessionId) {
			const question = useWireQuestion(controller, sessionId);
			const [waiting, setWaiting] = (0, react.useState)(() => controller.pendingInteractionOf(sessionId));
			(0, react.useEffect)(() => {
				if (sessionId === void 0) {
					setWaiting(void 0);
					return;
				}
				setWaiting(controller.pendingInteractionOf(sessionId));
				return controller.subscribe(() => {
					setWaiting(controller.pendingInteractionOf(sessionId));
				});
			}, [controller, sessionId]);
			const card = awaitingOf(question, waiting);
			if (card?.type === "shell") return {
				question: void 0,
				shell: card.waitingKind
			};
			return {
				question,
				shell: void 0
			};
		}
		//#endregion
		//#region src/client/board/goal-strip.tsx
		/**
		* The session goal strip: the board's GoalBar — a goal glyph, the phase
		* label, the truncated objective, and the native verbs (pause / resume /
		* edit-inline / clear), verbatim the harness GoalBar grammar: loading
		* (undefined), no goal (null) and complete goals render nothing; a cleared
		* goal stays hidden until a new id arrives; one action at a time
		* (single-flight); failures land on an inline error line (never a silent
		* no-op, never a throw at the click site).
		*
		* Durable state arrives as the projected whole snapshot (the context's goal,
		* same source as the native surface); the CAS ref for each verb is read at
		* call time from the live binding (the RPC guards staleness). Creation lives
		* on the `/goal` command, not here — exactly like the official strip.
		*/
		/** Strip label per visible phase; complete goals render nothing. */
		function phaseLabel(phase, activation) {
			if (phase === "paused") return t("review.goalPhasePaused");
			if (phase === "blocked") return t("review.goalPhaseBlocked");
			if (activation === "disarmed") return t("review.goalPhaseDisarmed");
			return t("review.goalPhaseActive");
		}
		function GoalStrip({ sessionId, controller, goal, activation }) {
			const verbs = controller.goalVerbs(sessionId);
			const [editing, setEditing] = (0, react.useState)(false);
			const [draft, setDraft] = (0, react.useState)("");
			const [pending, setPending] = (0, react.useState)(false);
			const [actionError, setActionError] = (0, react.useState)(null);
			const [clearedGoalId, setClearedGoalId] = (0, react.useState)(null);
			const pendingRef = (0, react.useRef)(false);
			const goalId = goal?.id;
			(0, react.useEffect)(() => {
				setEditing(false);
				setActionError(null);
				setClearedGoalId(null);
			}, [goalId]);
			const runAction = (0, react.useCallback)(async (action) => {
				if (pendingRef.current) return void 0;
				pendingRef.current = true;
				setPending(true);
				setActionError(null);
				const result = await action();
				pendingRef.current = false;
				setPending(false);
				if (!result.ok) setActionError(`${result.error.message} (${result.error.code})`);
				return result;
			}, []);
			const startEdit = (0, react.useCallback)(() => {
				setDraft(goal?.title ?? "");
				setEditing(true);
			}, [goal?.title]);
			const handleEdit = (0, react.useCallback)(async (call) => {
				const trimmed = draft.trim();
				if (trimmed === "") return;
				if ((await runAction(() => call(trimmed)))?.ok) setEditing(false);
			}, [draft, runAction]);
			const handleClear = (0, react.useCallback)(async (call, clearedId) => {
				if ((await runAction(call))?.ok && clearedId !== void 0) setClearedGoalId(clearedId);
			}, [runAction]);
			if (verbs === void 0 || goal === void 0 || goal === null) return null;
			if (goal.phase === "complete") return null;
			if (goalId !== void 0 && goalId === clearedGoalId) return null;
			const phase = goal.phase ?? (goal.active ? "active" : void 0);
			if (phase === void 0) return null;
			const showResume = phase === "paused" || phase === "active" && activation === "disarmed";
			const showPause = phase === "active" && activation !== "disarmed";
			if (editing) return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
				className: board_module_css_default.goalStrip,
				"data-goal-strip": "",
				children: [
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)("input", {
						className: `${board_module_css_default.input} ${board_module_css_default.goalStripInput}`,
						type: "text",
						"aria-label": t("review.goalObjectiveAria"),
						value: draft,
						onChange: (event) => {
							setDraft(event.target.value);
						},
						onKeyDown: (event) => {
							if (event.key === "Enter") handleEdit(verbs.edit);
							if (event.key === "Escape") {
								event.preventDefault();
								event.stopPropagation();
								setEditing(false);
							}
						},
						disabled: pending,
						autoFocus: true
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
						className: board_module_css_default.goalStripActions,
						children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
							type: "button",
							className: board_module_css_default.iconButton,
							"aria-label": t("review.goalSave"),
							title: t("review.goalSave"),
							disabled: pending || draft.trim() === "",
							onClick: () => {
								handleEdit(verbs.edit);
							},
							children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, { name: "check" })
						}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
							type: "button",
							className: board_module_css_default.iconButton,
							"aria-label": t("review.goalCancel"),
							title: t("review.goalCancel"),
							disabled: pending,
							onClick: () => {
								setEditing(false);
							},
							children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, { name: "close" })
						})]
					}),
					actionError !== null && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.goalStripError,
						children: actionError
					})
				]
			});
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
				className: board_module_css_default.goalStrip,
				"data-goal-strip": "",
				children: [
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
						className: board_module_css_default.sessionContextRow,
						children: [
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Chip, {
								fill: false,
								className: board_module_css_default.sessionContextGoalOn,
								children: phaseLabel(phase, activation)
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
								className: board_module_css_default.sessionContextText,
								title: goal.title,
								children: goal.title
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
								className: board_module_css_default.goalStripActions,
								children: [
									showResume && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
										type: "button",
										className: board_module_css_default.iconButton,
										"aria-label": t("review.goalResume"),
										title: t("review.goalResume"),
										disabled: pending,
										onClick: () => {
											runAction(verbs.resume);
										},
										children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, { name: "play" })
									}),
									showPause && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
										type: "button",
										className: board_module_css_default.iconButton,
										"aria-label": t("review.goalPause"),
										title: t("review.goalPause"),
										disabled: pending,
										onClick: () => {
											runAction(verbs.pause);
										},
										children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, { name: "pause" })
									}),
									/* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
										type: "button",
										className: board_module_css_default.iconButton,
										"aria-label": t("review.goalEdit"),
										title: t("review.goalEdit"),
										disabled: pending,
										onClick: startEdit,
										children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, { name: "pencil" })
									}),
									/* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
										type: "button",
										className: board_module_css_default.iconButton,
										"aria-label": t("review.goalClear"),
										title: t("review.goalClear"),
										disabled: pending,
										onClick: () => {
											handleClear(verbs.clear, goalId);
										},
										children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, { name: "close" })
									})
								]
							})
						]
					}),
					phase === "blocked" && goal.blockedReason !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
						className: board_module_css_default.goalStripBlocked,
						children: goal.blockedReason.message
					}),
					actionError !== null && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.goalStripError,
						children: actionError
					})
				]
			});
		}
		//#endregion
		//#region src/client/board/SessionContextBlock.tsx
		/**
		* Session context readout in the rail head: the session's live to-do list,
		* active goal and subagents — shown when the session HAS any of those, hidden
		* completely when it has none (a deterministic all-or-nothing rule, no
		* partial flicker).
		*
		* The TODO part is HARNESS-ISOMORPHIC: it reads the official `todos`
		* projection (the exact host-computed whole list the harness's own TodoPanel
		* renders), shows the same three status glyphs, the same per-status progress
		* counts in the header (zero segments omitted), the same default-collapsed
		* posture — so a harness upgrade flows in automatically and the board never
		* displays a second, drifting grammar.
		*
		* LAYOUT: one quiet header row (icon + title + progress summary + chevron).
		* The expanded list has two placements, chosen by the host surface via the
		* `className` prop:
		*  - DEFAULT (refine panel): a POPOVER anchored under the head — absolute,
		*    opaque menu surface, its own capped scroll region — so expanding never
		*    pushes the surrounding blocks.
		*  - DOCK (review / session header, `.reviewHeaderContext`): the panel is an
		*    IN-FLOW full-width row of the header grid (the wrap dissolves via
		*    `display: contents` while open, head button keeps the context cell, the
		*    panel takes the panel row), height-capped with its own scroll — expanding
		*    pushes the body down instead of painting over it, so the readout can
		*    never overlap the transcript, the run config or the comments at any
		*    width. There is no third placement: no caller may invent another
		*    positioning context for this panel.
		* Either way expanding can never eat the composer or push siblings away.
		*/
		/** One todo glyph: pending = dashed circle, in_progress = the shared spinner,
		*  completed = the shared check — the same three-state read as the harness's
		*  TodoPanel. */
		function TodoGlyph({ status }) {
			if (status === "completed") return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, {
				name: "check",
				className: board_module_css_default.sessionContextGlyphDone
			});
			if (status === "in_progress") return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
				className: `${board_module_css_default.spinner} ${board_module_css_default.sessionContextGlyphActive}`,
				"aria-hidden": "true"
			});
			return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
				className: board_module_css_default.sessionContextGlyphPending,
				"aria-hidden": "true"
			});
		}
		/** The deterministic context readout (see module doc): self-contained — the
		*  collapsed/expanded state and the outside-click dismissal live here.
		*  `className` lets a surface switch the expanded panel from its DEFAULT
		*  popover to the IN-FLOW dock row: the review/session header passes
		*  `.reviewHeaderContext` (the open wrap dissolves so the panel joins the
		*  header grid as its own full-width row); the refine panel keeps the default
		*  popover.
		*  `sessionId` + `controller` switch the goal row from the read-only legacy
		*  text to the interactive goal strip (pause / resume / edit / clear through
		*  the official verbs); absent = read-only (old hosts without remote.goals). */
		function SessionContextBlock({ context, className, sessionId, controller }) {
			const [open, setOpen] = (0, react.useState)(false);
			const panelId = (0, react.useId)();
			const wrapRef = (0, react.useRef)(null);
			(0, react.useEffect)(() => {
				if (!open) return;
				const onDown = (event) => {
					if (wrapRef.current !== null && !wrapRef.current.contains(event.target)) setOpen(false);
				};
				document.addEventListener("mousedown", onDown);
				return () => {
					document.removeEventListener("mousedown", onDown);
				};
			}, [open]);
			const todos = context.todos ?? [];
			const done = todos.filter((item) => item.status === "completed").length;
			const active = todos.filter((item) => item.status === "in_progress").length;
			const pending = todos.length - done - active;
			const liveSubagents = (context.subagents ?? []).filter((sub) => sub.status === void 0 || !FINISHED_SUBAGENT_STATUS.has(sub.status));
			const subagentCount = liveSubagents.length;
			const goalActive = context.goal?.active === true;
			if (!contextWorthOf({
				...context,
				subagents: liveSubagents
			})) return null;
			const summary = [
				...done > 0 ? [t("review.todosDone", { n: String(done) })] : [],
				...active > 0 ? [t("review.todosActive", { n: String(active) })] : [],
				...pending > 0 ? [t("review.todosPending", { n: String(pending) })] : [],
				goalActive ? t("review.goalActiveWith") : void 0,
				subagentCount > 0 ? t("review.subagents", { n: String(subagentCount) }) : void 0
			].filter((part) => part !== void 0).join(" · ");
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
				className: `${board_module_css_default.sessionContextWrap}${className !== void 0 ? ` ${className}` : ""}`,
				"data-open": open ? "" : void 0,
				ref: wrapRef,
				children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("button", {
					type: "button",
					className: board_module_css_default.sessionContextHead,
					"aria-expanded": open,
					"aria-controls": panelId,
					onClick: () => {
						setOpen((value) => !value);
					},
					children: [
						/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, {
							name: "checklist",
							className: board_module_css_default.sessionContextLead
						}),
						/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
							className: board_module_css_default.sessionContextTitle,
							children: t("review.sessionContext")
						}),
						/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
							className: board_module_css_default.sessionContextSummary,
							title: summary,
							children: summary
						}),
						/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, {
							name: "chevronDown",
							className: board_module_css_default.sessionContextChevron
						})
					]
				}), open && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
					className: board_module_css_default.sessionContextPanel,
					id: panelId,
					children: [
						todos.length > 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("ul", {
							className: board_module_css_default.sessionContextTodos,
							children: todos.map((todo, index) => /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("li", {
								className: board_module_css_default.sessionContextTodo,
								"data-status": todo.status,
								children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
									className: board_module_css_default.sessionContextTodoGlyph,
									"aria-hidden": "true",
									children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(TodoGlyph, { status: todo.status })
								}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
									className: board_module_css_default.sessionContextText,
									"data-done": todo.status === "completed" ? "" : void 0,
									children: todo.content
								})]
							}, `${todo.content}-${index}`))
						}),
						goalActive && (sessionId !== void 0 && controller !== void 0 && controller.goalVerbs(sessionId) !== void 0 ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)(GoalStrip, {
							sessionId,
							controller,
							goal: context.goal,
							activation: context.goal?.activation
						}) : /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
							className: board_module_css_default.sessionContextRow,
							children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Chip, {
								fill: false,
								className: board_module_css_default.sessionContextGoalOn,
								children: t("review.goalActive")
							}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
								className: board_module_css_default.sessionContextText,
								children: context.goal.title
							})]
						})),
						subagentCount > 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
							className: board_module_css_default.sessionContextBlock,
							children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Chip, {
								fill: false,
								className: board_module_css_default.sessionContextSubagent,
								children: t("review.subagents", { n: String(subagentCount) })
							}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("ul", {
								className: board_module_css_default.sessionContextSubList,
								children: liveSubagents.map((sub) => /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("li", {
									className: board_module_css_default.sessionContextSubItem,
									children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
										className: board_module_css_default.sessionContextSubDot,
										"aria-hidden": "true"
									}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
										className: board_module_css_default.sessionContextText,
										title: sub.status ?? void 0,
										children: sub.title
									})]
								}, sub.title))
							})]
						})
					]
				})]
			});
		}
		//#endregion
		//#region src/client/board/RefineSection.tsx
		/**
		* Requirement refinement: the backlog task's AI-assisted research panel.
		*
		* A backlog task is a loose idea; one click starts a refine round in the
		* task's refine session (created lazily through the same session machinery
		* as executions and inheriting the task's run configuration — nothing extra
		* to configure). The AI researches with its own tools, asks the user
		* anything unclear (the board surfaces the wait read-only — answering stays
		* in the native session), and finally delivers a ready-to-run execution
		* prompt that the user applies onto the task with one button — nothing is
		* applied automatically.
		*
		* Layout: header (title + live status + view-session escape) above the
		* shared transcript tail (auto-follow + 滑到最新), then the answer bar
		* (input + send), then the apply action.
		*/
		function RefineSection({ controller, task }) {
			const sessionId = task.refineSessionId;
			const active = refining(task);
			const rounds = refineRoundsOf(task);
			const lastRound = rounds[rounds.length - 1];
			const waiting = controller.pendingInteractionOf(sessionId);
			const context = useSessionContext(controller, sessionId);
			const pendingInteraction = useAwaitingCard(controller, sessionId);
			const [draft, setDraft] = (0, react.useState)(() => decodeCommentDraft(draftStore.get(refineDraftKey(task.id))).text);
			const [applied, setApplied] = (0, react.useState)(false);
			const [unrestoredFiles, setUnrestoredFiles] = (0, react.useState)(() => decodeCommentDraft(draftStore.get(refineDraftKey(task.id))).fileNames);
			const [draftOversized, setDraftOversized] = (0, react.useState)(false);
			(0, react.useEffect)(() => {
				const restored = decodeCommentDraft(draftStore.get(refineDraftKey(task.id)));
				setDraft(restored.text);
				setUnrestoredFiles(restored.fileNames);
				setDraftOversized(false);
			}, [task.id]);
			const { lines, error, atBottom, scrollRef, onScroll, jumpToBottom, reload, hasMore, loadingEarlier, loadEarlier } = useTranscriptTail(controller, sessionId, rounds.length);
			const resultText = (0, react.useMemo)(() => {
				if (lines === void 0) return void 0;
				for (let index = lines.length - 1; index >= 0; index -= 1) {
					const line = lines[index];
					if (line.kind === "message" && line.role === "assistant" && line.text.trim() !== "") return line.text;
				}
			}, [lines]);
			const stager = useFileStager(controller, sessionId);
			const attachments = useComposerImages(COMMENT_IMAGE_BUDGET, 9, void 0, stager !== void 0 && sessionId !== void 0 ? {
				sessionId,
				stage: stager
			} : void 0);
			(0, react.useEffect)(() => {
				const restored = decodeCommentDraft(draftStore.get(refineDraftKey(task.id)));
				if (restored.images.length > 0) attachments.setImages(restored.images);
			}, [task.id]);
			(0, react.useEffect)(() => {
				const { value, imagesDropped } = encodeCommentDraft(draft, attachments.images, attachments.files);
				draftStore.set(refineDraftKey(task.id), value);
				setDraftOversized(imagesDropped);
			}, [
				task.id,
				draft,
				attachments.images,
				attachments.files
			]);
			const send = () => {
				const text = draft.trim();
				if (text === "") return;
				const images = attachments.images;
				const files = attachments.files.map(toPromptFile);
				if (controller.answerRefine(task.id, text, images.length > 0 ? images.map(toPromptImage) : void 0, files.length > 0 ? files : void 0)) {
					setDraft("");
					draftStore.clear(refineDraftKey(task.id));
					attachments.setImages([]);
					attachments.setFiles([]);
					setUnrestoredFiles([]);
					setDraftOversized(false);
					setApplied(false);
				}
			};
			const draftNotice = draftOversized ? t("review.draftImagesDropped") : unrestoredFiles.length > 0 ? t("review.draftFilesGone", { names: unrestoredFiles.slice(0, 3).join("、") }) : void 0;
			const apply = () => {
				if (resultText === void 0) return;
				if (controller.applyRefineResult(task.id, resultText)) setApplied(true);
			};
			const idle = sessionId === void 0 && !active;
			return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Section, {
				title: t("detail.refine"),
				className: board_module_css_default.refineSection,
				action: sessionId !== void 0 ? /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(Button, {
					size: "sm",
					onClick: () => {
						controller.openSession(sessionId);
					},
					children: [t("detail.viewSession"), " →"]
				}) : void 0,
				children: idle ? /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
					className: board_module_css_default.refineIdle,
					children: [
						/* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
							className: board_module_css_default.detailText,
							children: t("detail.refine.idleHint")
						}),
						/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
							variant: "primary",
							disabled: !refinable(task),
							title: !refinable(task) ? t("detail.refine.emptyHint") : void 0,
							onClick: () => {
								controller.startRefine(task.id, isEnglish());
							},
							children: t("detail.refine.start")
						}),
						!refinable(task) && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
							className: board_module_css_default.detailHint,
							children: t("detail.refine.emptyHint")
						})
					]
				}) : /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
						className: board_module_css_default.refineHeader,
						children: [active ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Chip, {
							kind: "warn",
							children: waiting !== void 0 ? t("review.waiting") : t("detail.result.running")
						}) : lastRound === void 0 ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Chip, {
							kind: "muted",
							children: t("detail.refine.noResult")
						}) : /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Chip, {
							kind: resultChipKind(lastRound.result),
							children: lastRound.result === "failed" ? t("detail.result.failed") : t("detail.result.succeeded")
						}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
							className: board_module_css_default.refineRounds,
							children: t("detail.refine.rounds", { n: String(rounds.length) })
						})]
					}),
					lastRound?.result === "failed" && lastRound.error !== void 0 && lastRound.error !== "" && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.executionError,
						children: lastRound.error
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
						className: board_module_css_default.refineTranscriptArea,
						ref: scrollRef,
						onScroll,
						children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
							className: board_module_css_default.refineTail,
							children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(SessionTranscript, {
								lines,
								error,
								atBottom,
								jumpToBottom,
								maxLines: 12,
								hasMore,
								loadingEarlier,
								onLoadEarlier: loadEarlier,
								onRetry: reload,
								sessionId,
								controller
							})
						})
					}),
					active && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(SessionWaitingNotice, { waiting }),
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
						className: board_module_css_default.refineInputArea,
						...attachments.dropProps,
						children: [
							(pendingInteraction.question !== void 0 || pendingInteraction.shell !== void 0) && sessionId !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(InteractionCard, {
								question: pendingInteraction.question,
								shellWaiting: pendingInteraction.shell,
								sessionId,
								controller
							}, pendingInteraction.question !== void 0 ? pendingInteraction.question.rpcId : `shell-${sessionId}`),
							sessionId !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(SessionContextBlock, {
								context,
								sessionId,
								controller
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)(PromptInput, {
								value: draft,
								onChange: (next) => {
									setDraft(next);
								},
								placeholder: t("detail.refine.answerPlaceholder"),
								rows: 3,
								controller,
								sessionId
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)(AttachmentStrip, {
								images: attachments.images,
								files: attachments.files,
								onAdd: (files) => {
									if (pickedHasFiles(files)) setUnrestoredFiles([]);
									attachments.addFiles(files);
								},
								onRemoveImage: (id) => {
									attachments.setImages(attachments.images.filter((image) => image.id !== id));
								},
								onRemoveFile: (id) => {
									attachments.setFiles(attachments.files.filter((file) => file.id !== id));
								},
								busy: attachments.busy,
								busyLabel: attachments.busy ? attachBusyLabel(attachments.busyKind) : void 0,
								error: attachments.error ?? draftNotice
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
								variant: "primary",
								disabled: draft.trim() === "",
								onClick: send,
								children: t("detail.refine.send")
							})
						]
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
						className: board_module_css_default.refineActionArea,
						children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
							variant: "primary",
							className: board_module_css_default.refineApplyButton,
							disabled: resultText === void 0,
							title: resultText === void 0 ? t("detail.refine.noResult") : void 0,
							onClick: apply,
							children: t("detail.refine.apply")
						}), applied && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
							className: board_module_css_default.detailHint,
							children: t("detail.refine.applied")
						})]
					})
				] })
			});
		}
		//#endregion
		//#region src/client/board/SessionFrame.tsx
		/**
		* Shared detail-panel shell for every session surface on the board — the
		* execution review page (ReviewDetail) and the linked-session view
		* (SessionDetail): one backdrop + container + header (title + type badge +
		* context readout + actions + close) + body.
		*
		* The body is ONE DOM with TWO geometries, keyed on the PANEL's own width:
		* - wide: two columns — the conversation owns the left height, the rail owns
		*   the right ([folds block] → [composer]).
		* - narrow: three stacked rows — [folds (capped, its own scroller)] →
		*   [conversation] → [composer]. The conversation and the send box are what a
		*   phone user sees first; the config and the comment thread are folds at the
		*   top of the panel, and neither can ever push the other (or the composer)
		*   out of reach.
		*
		* THE header grammar (both widths, one DOM):
		* - wide: ONE line — title + badge | context readout | actions | ×. Every
		*   element rides the same `align-items: center` baseline, so the badge,
		*   「刷新」,「查看会话」 and the close are all on the title's horizontal
		*   plane (the 「标题栏不在同一水平面」 fix).
		* - narrow: TWO deterministic lines — line 1 = title + badge + × on ONE
		*   horizontal plane (the close at the top-right corner, the title
		*   single-line ellipsis so the three never drift onto different baselines);
		*   line 2 = the context readout LEFT + the action cluster RIGHT (the
		*   context sits at 刷新's left, one baseline).
		*
		* The session context readout lives IN the header (its one home — the user
		* asked for it "at 刷新's left, one horizontal plane"), NOT in the rail.
		*
		* The shell is PURE LAYOUT: business content — the review page's context
		* meter, live config, comment thread and composer, or the linked view's
		* read-only facts — stays in the caller. That boundary is deliberate: the
		* execution semantics (comment drives the task) and the linked-session
		* semantics (read-only live view) must never leak into each other through
		* a shared component.
		*
		* The whole backdrop PORTALS to the board box (the same layer every nested
		* overlay anchors to). ReviewDetail/SessionDetail render from INSIDE the task
		* detail panel, whose `position:relative + overflow:hidden` would otherwise
		* anchor and clip the backdrop — on a phone (where the detail is nearly the
		* whole box and its height is content-driven) that made the comment panel
		* "pop open" into an unseeable sliver. Portaling gives the frame the full
		* board box on every surface, exactly like the shared Dialog.
		*/
		/** The shared panel frame (see module doc). */
		function SessionFrame({ title, badge, ariaLabel, actions, context, contextSessionId, controller, main, rail, onClose }) {
			useEscapeStack(onClose);
			const panelRef = (0, react.useRef)(null);
			const { onKeyDown } = useDialogFocus(panelRef);
			return (0, react_dom.createPortal)(/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
				className: board_module_css_default.modalBackdrop,
				onMouseDown: (event) => {
					if (event.target === event.currentTarget) onClose();
				},
				children: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
					ref: panelRef,
					tabIndex: -1,
					className: board_module_css_default.review,
					role: "dialog",
					"aria-modal": "true",
					"aria-label": ariaLabel,
					"data-dsh-taskboard-panel": "",
					onKeyDown,
					children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("header", {
						className: board_module_css_default.reviewHeader,
						children: [
							/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
								className: board_module_css_default.reviewTitleWrap,
								children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("h2", {
									className: board_module_css_default.reviewTitle,
									title,
									children: title
								}), badge !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
									className: board_module_css_default.reviewBadge,
									children: badge
								})]
							}),
							context !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(SessionContextBlock, {
								context,
								className: board_module_css_default.reviewHeaderContext,
								sessionId: contextSessionId,
								controller
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
								className: board_module_css_default.reviewActions,
								children: actions
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
								type: "button",
								className: board_module_css_default.iconButton,
								"aria-label": t("detail.close"),
								onClick: onClose,
								children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, { name: "close" })
							})
						]
					}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
						className: board_module_css_default.reviewBody,
						children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
							className: board_module_css_default.reviewMain,
							children: main
						}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("aside", {
							className: board_module_css_default.reviewRail,
							children: rail
						})]
					})]
				})
			}), boardBox());
		}
		//#endregion
		//#region src/client/board/ReviewDetail.tsx
		/**
		* Review page: one execution's review surface. Opened by clicking an
		* execution-history row, it shows the session's recent conversation
		* (the transcript tail, folded from raw history events following the
		* native harness rules) plus a live session panel — the execution
		* session's current model/reasoning effort (native models API), its
		* permission (switched through the native `/permission` command registry —
		* never a model turn), its real workspace/Agent facts (from the
		* session-list summary, read-only), and the native context meter. When the
		* execution session is blocked on the user (approval / plan review /
		* question), a waiting banner explains it and points at "查看会话". The
		* layout is two columns: the conversation owns the full left height; the
		* right rail is THE shared SessionRail — live state row, the collapsible
		* config head, the collapsible comment thread (with the pending interaction
		* card), the session-context dock and the pinned composer. On a phone the two
		* columns stack (transcript on top, rail below) so the composer lands at the
		* panel's bottom edge.
		* The thread shows only the comments of the execution being reviewed — each
		* execution's page shows its own, the injection queue is per session (the
		* lane is the conversation, not the card). Both
		* the transcript and the comment list auto-follow the latest output while at
		* the bottom, with a "滑到最新" button when scrolled up. A comment whose
		* first character is '/' is a slash command executed through the native
		* command registry, exactly like the native composer. The native session
		* page remains the place for the full transcript ("查看会话").
		*/
		/** The review page (see module doc). */
		function ReviewDetail({ controller, task, execution, onClose }) {
			const [snapshot, setSnapshot] = (0, react.useState)(controller.getSnapshot());
			(0, react.useEffect)(() => controller.subscribe(() => setSnapshot(controller.getSnapshot())), [controller]);
			const current = snapshot.tasks.find((candidate) => candidate.id === task.id) ?? task;
			const sessionId = execution.sessionId;
			const cruiseOn = snapshot.cruise.enabled;
			const comments = sessionId !== void 0 ? sessionCommentsOf(current, sessionId, cruiseOn) : [];
			const context = useSessionContext(controller, sessionId);
			const pendingInteraction = useAwaitingCard(controller, sessionId);
			const [projections, setProjections] = (0, react.useState)(void 0);
			const [configReloadKey, setConfigReloadKey] = (0, react.useState)(0);
			(0, react.useEffect)(() => {
				controller.markExecutionViewed(task.id, execution.id);
			}, []);
			const { lines, error: transcriptError, hasMore: transcriptHasMore, loadingEarlier: transcriptLoadingEarlier, pageError: transcriptPageError, pageUnsupported: transcriptPageUnsupported, atBottom: transcriptAtBottom, scrollRef: transcriptScrollRef, onScroll: onTranscriptScroll, jumpToBottom: jumpTranscript, reload: reloadTranscript, loadEarlier: loadEarlierTranscript } = useTranscriptTail(controller, sessionId, current.executions.length, (result) => {
				setProjections(result.projections);
			});
			/** Full refresh (manual 刷新 / after a config change): transcript + the
			*  shared config editor (via its reload key). */
			const reload = (0, react.useCallback)(() => {
				reloadTranscript();
				setConfigReloadKey((key) => key + 1);
			}, [reloadTranscript]);
			(0, react.useEffect)(() => {
				setConfigReloadKey((key) => key + 1);
			}, [current.executions.length, current.status]);
			const waiting = controller.pendingInteractionOf(sessionId);
			const stateChip = sessionStateChip(sessionDisplay(current, execution, waiting, sessionId !== void 0 && controller.nativeRunningOf(sessionId)).state, waiting, "detail.result.succeeded", "detail.result.cancelled");
			const updatedAt = formatDateTime(execution.endedAt ?? execution.startedAt);
			const blockedByOpenRun = hasOpenRun(current);
			return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(SessionFrame, {
				title: current.title,
				ariaLabel: `${t("review.title")} · ${current.title}`,
				context,
				contextSessionId: sessionId,
				controller,
				actions: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
						size: "sm",
						onClick: reload,
						title: t("review.refresh"),
						children: t("review.refresh")
					}),
					sessionId !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(Button, {
						size: "sm",
						onClick: () => {
							controller.openSession(sessionId);
						},
						children: [t("detail.viewSession"), " →"]
					}),
					current.status === "review" && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
						size: "sm",
						disabled: blockedByOpenRun,
						title: blockedByOpenRun ? t("board.notifyApproveBlocked") : void 0,
						onClick: () => {
							if (!controller.approveTask(current.id)) return;
							onClose();
						},
						children: t("board.notifyApprove")
					}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
						size: "sm",
						disabled: blockedByOpenRun,
						title: t("review.sendBackTitle"),
						onClick: () => {
							controller.moveTask(current.id, "todo");
							onClose();
						},
						children: t("review.sendBack")
					})] })
				] }),
				main: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
					className: board_module_css_default.reviewTranscriptScroll,
					ref: transcriptScrollRef,
					onScroll: onTranscriptScroll,
					children: sessionId === void 0 ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
						className: board_module_css_default.detailText,
						children: t("review.noSession")
					}) : /* @__PURE__ */ (0, react_jsx_runtime.jsx)(SessionTranscript, {
						lines,
						error: transcriptError,
						atBottom: transcriptAtBottom,
						jumpToBottom: jumpTranscript,
						waiting,
						hasMore: transcriptHasMore,
						loadingEarlier: transcriptLoadingEarlier,
						pageError: transcriptPageError,
						pageUnsupported: transcriptPageUnsupported,
						onLoadEarlier: loadEarlierTranscript,
						onRetry: reloadTranscript,
						sessionId,
						controller,
						before: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
							className: board_module_css_default.reviewOutcome,
							children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Chip, {
								kind: stateChip.kind,
								title: stateChip.title,
								icon: stateChip.spinner === true ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
									className: board_module_css_default.spinner,
									"aria-hidden": "true"
								}) : void 0,
								children: stateChip.label
							}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
								className: board_module_css_default.reviewOutcomeMeta,
								children: [
									t("detail.executionEnded"),
									" ",
									execution.endedAt !== void 0 ? formatDateTime(execution.endedAt) : "—"
								]
							})]
						})
					})
				}),
				rail: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(SessionRail, {
					stateChip,
					updatedAt,
					sessionId,
					controller,
					projections,
					lines,
					onChanged: reload,
					reloadKey: configReloadKey,
					task: current,
					thread: comments,
					onCancelComment: (id) => controller.cancelComment(id),
					interaction: pendingInteraction.question,
					shellWaiting: pendingInteraction.shell,
					composer: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(SessionComposer, {
						controller,
						taskId: current.id,
						sessionId,
						placeholder: t("review.commentPlaceholder"),
						disabled: sessionId === void 0,
						onDrive: (text, images, files) => controller.submitComment(current.id, execution.id, text, text.startsWith("/"), images.map(toPromptImage), files.map(toPromptFile)) !== void 0,
						onSteer: (text) => sessionId === void 0 ? Promise.resolve(false) : controller.steerComment(current.id, sessionId, text).then((result) => result.ok),
						onSteerImages: (text, images) => sessionId === void 0 ? Promise.resolve(false) : controller.steerCommentWithImages(current.id, sessionId, text, images.map(toPromptImage)).then((result) => result.ok),
						onSteerFiles: (text, images, files) => sessionId === void 0 ? Promise.resolve(false) : controller.steerCommentWithImages(current.id, sessionId, text, images.map(toPromptImage), files.map(toPromptFile)).then((result) => result.ok)
					})
				}),
				onClose
			});
		}
		//#endregion
		//#region src/client/board/SessionDetail.tsx
		/**
		* Session detail: the linked-session panel opened by clicking a session row.
		* It shares the exact shell (SessionFrame), the right rail (SessionRail) and
		* the composer grammar (SessionComposer) with the execution review page —
		* one panel grammar for every session. This file is only the linked-session
		* semantics: the live row (state chip + updated time), the session's own
		* comment thread, and the send paths (排队 = dispatcher queue, 插话 =
		* deliver straight to the native session now; a comment on a done task
		* revives it — the message drives the task, never a dead end).
		*/
		/** The linked-session panel (see module doc). */
		function SessionDetail({ controller, task, sessionId, onClose }) {
			const row = controller.linkedOf(task).find((candidate) => candidate.sessionId === sessionId);
			const waiting = row?.pendingInteraction;
			const [projections, setProjections] = (0, react.useState)(void 0);
			const { lines, error: transcriptError, hasMore: transcriptHasMore, loadingEarlier: transcriptLoadingEarlier, pageError: transcriptPageError, pageUnsupported: transcriptPageUnsupported, atBottom, scrollRef, onScroll, jumpToBottom, reload, loadEarlier } = useTranscriptTail(controller, row === void 0 ? void 0 : sessionId, void 0, (result) => {
				setProjections(result.projections);
			});
			const cruiseOn = controller.getSnapshot().cruise.enabled;
			const thread = sessionCommentsOf(task, sessionId, cruiseOn);
			const context = useSessionContext(controller, sessionId);
			const pendingInteraction = useAwaitingCard(controller, sessionId);
			const liveGone = row === void 0;
			const sessionState = waiting !== void 0 ? "waiting" : row?.running === true ? "running" : row?.completed === true ? "succeeded" : void 0;
			const stateChip = sessionState !== void 0 ? sessionStateChip(sessionState, waiting, "detail.linkedDone", "detail.linkedIdle", "detail.idleHint") : void 0;
			const updatedAt = row !== void 0 ? formatDateTime(row.updatedAt) : void 0;
			const hint = liveGone ? t("detail.sessionUnavailable") : void 0;
			return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(SessionFrame, {
				title: sessionRowTitleOf(controller.sessionTitle(sessionId), t("detail.sessionUntitled")),
				badge: t("detail.sessionCountBadge", { n: String(controller.sessionsOf(task).length) }),
				ariaLabel: t("detail.sessionPanel"),
				context,
				contextSessionId: sessionId,
				controller,
				actions: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
					size: "sm",
					onClick: reload,
					title: t("review.refresh"),
					children: t("review.refresh")
				}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(Button, {
					size: "sm",
					onClick: () => {
						controller.openSession(sessionId);
					},
					children: [t("detail.viewSession"), " →"]
				})] }),
				main: row === void 0 ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
					className: board_module_css_default.detailText,
					children: t("detail.sessionUnavailable")
				}) : /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
					className: board_module_css_default.reviewTranscriptScroll,
					ref: scrollRef,
					onScroll,
					children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(SessionTranscript, {
						lines,
						error: transcriptError,
						atBottom,
						jumpToBottom,
						waiting,
						hasMore: transcriptHasMore,
						loadingEarlier: transcriptLoadingEarlier,
						pageError: transcriptPageError,
						pageUnsupported: transcriptPageUnsupported,
						onLoadEarlier: loadEarlier,
						onRetry: reload,
						sessionId,
						controller
					})
				}),
				rail: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(SessionRail, {
					stateChip,
					updatedAt,
					sessionId,
					controller,
					projections,
					lines,
					onChanged: reload,
					task,
					thread,
					onCancelComment: (id) => controller.cancelComment(id),
					interaction: pendingInteraction.question,
					shellWaiting: pendingInteraction.shell,
					composer: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(SessionComposer, {
						controller,
						taskId: task.id,
						sessionId,
						placeholder: t("detail.sessionDrivePlaceholder"),
						disabled: liveGone,
						hint,
						onDrive: (text, images, files) => controller.submitSessionComment(task.id, sessionId, text, text.startsWith("/"), images.map(toPromptImage), files.map(toPromptFile)) !== void 0,
						onSteer: (text) => controller.steerComment(task.id, sessionId, text).then((result) => result.ok),
						onSteerImages: (text, images) => controller.steerCommentWithImages(task.id, sessionId, text, images.map(toPromptImage)).then((result) => result.ok),
						onSteerFiles: (text, images, files) => controller.steerCommentWithImages(task.id, sessionId, text, images.map(toPromptImage), files.map(toPromptFile)).then((result) => result.ok)
					})
				}),
				onClose
			});
		}
		//#endregion
		//#region src/client/board/NewSessionModal.tsx
		/**
		* New-session dialog (the task detail's 会话 area "新建会话"): composes one
		* fresh native session with the shared run-config fields and binds it to the
		* task. Shares the run-config block (RunConfigFields) with the task form —
		* the same preset picker, the same field editor, zero drift — but nothing
		* else: a session has no title/description/prompt/landing column. The
		* initial value is the task's own run configuration (what the task runs
		* with is what the new session composes with; absent fields = deployment
		* defaults). Fully nested overlay: rendered inside the task detail, so it
		* MUST portal (see Dialog).
		*/
		/** The task's own run config as the dialog's starting value (absent = default). */
		function taskConfigOf(task) {
			return {
				...task.workspaceId !== void 0 ? { workspaceId: task.workspaceId } : {},
				...task.provider !== void 0 ? { provider: task.provider } : {},
				...task.model !== void 0 ? { model: task.model } : {},
				...task.reasoningEffort !== void 0 ? { reasoningEffort: task.reasoningEffort } : {},
				...task.agentPreset !== void 0 ? { agentPreset: task.agentPreset } : {},
				...task.permission !== void 0 ? { permission: task.permission } : {}
			};
		}
		/** Read back a previous draft (a half-typed name and a picked model must
		*  survive closing the dialog); anything malformed falls back to the task's
		*  own config — the draft is a convenience, never a source of truth. */
		function readDraft(task) {
			const raw = draftStore.get(newSessionDraftKey(task.id));
			if (raw === void 0) return {
				title: "",
				config: taskConfigOf(task)
			};
			try {
				const parsed = JSON.parse(raw);
				if (typeof parsed !== "object" || parsed === null) return {
					title: "",
					config: taskConfigOf(task)
				};
				const row = parsed;
				const config = typeof row.config === "object" && row.config !== null ? row.config : taskConfigOf(task);
				return {
					title: typeof row.title === "string" ? row.title : "",
					config
				};
			} catch {
				return {
					title: "",
					config: taskConfigOf(task)
				};
			}
		}
		/** The "create a configured session for this task" dialog (see module doc). */
		function NewSessionModal({ controller, task, onClose, onCreated }) {
			const key = newSessionDraftKey(task.id);
			const seeded = (0, react.useState)(() => readDraft(task))[0];
			const [config, setConfig] = (0, react.useState)(seeded.config);
			const [title, setTitle] = (0, react.useState)(seeded.title);
			const [busy, setBusy] = (0, react.useState)(false);
			const [error, setError] = (0, react.useState)(void 0);
			const [partial, setPartial] = (0, react.useState)(void 0);
			/** Keep the draft in step with the fields (an empty form clears the slot). */
			const stash = (nextTitle, nextConfig) => {
				if (nextTitle === "" && Object.keys(nextConfig).length === 0) draftStore.clear(key);
				else draftStore.set(key, JSON.stringify({
					title: nextTitle,
					config: nextConfig
				}));
			};
			const submit = () => {
				if (busy) return;
				setBusy(true);
				setError(void 0);
				setPartial(void 0);
				controller.createTaskSession(task.id, {
					...config,
					title
				}).then((result) => {
					setBusy(false);
					if (!result.ok) {
						setError(result.error);
						return;
					}
					draftStore.clear(key);
					if (result.titleError !== void 0) {
						setPartial(t("detail.sessionNewTitleFailed", { error: result.titleError }));
						onCreated(result.sessionId);
						return;
					}
					onCreated(result.sessionId);
					onClose();
				});
			};
			return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Dialog, {
				label: t("detail.sessionNewTitle"),
				onClose,
				title: t("detail.sessionNewTitle"),
				portal: true,
				children: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("form", {
					className: board_module_css_default.modalForm,
					onSubmit: (event) => {
						event.preventDefault();
						submit();
					},
					children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
						className: board_module_css_default.modalScroll,
						children: [
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
								className: board_module_css_default.detailHint,
								children: t("detail.sessionNewHint")
							}),
							partial !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
								className: board_module_css_default.detailHint,
								children: partial
							}),
							error !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
								className: board_module_css_default.formError,
								children: error
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("label", {
								className: board_module_css_default.field,
								children: [
									/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
										className: board_module_css_default.fieldLabel,
										children: t("detail.sessionNewTitleLabel")
									}),
									/* @__PURE__ */ (0, react_jsx_runtime.jsx)("input", {
										className: board_module_css_default.input,
										value: title,
										placeholder: t("detail.sessionNewTitlePlaceholder"),
										onChange: (event) => {
											setTitle(event.target.value);
											stash(event.target.value, config);
										}
									}),
									/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
										className: board_module_css_default.fieldHint,
										children: t("detail.sessionNewTitleHint")
									})
								]
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)(RunConfigFields, {
								value: config,
								onChange: (next) => {
									setConfig(next);
									stash(title, next);
								},
								controller
							})
						]
					}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("footer", {
						className: board_module_css_default.modalFooter,
						children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
							onClick: onClose,
							children: t("new.cancel")
						}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
							type: "submit",
							variant: "primary",
							disabled: busy,
							children: t("detail.sessionNewSubmit")
						})]
					})]
				})
			});
		}
		//#endregion
		//#region src/client/board/AddSessionModal.tsx
		/**
		* Add-existing-session picker: the touch/keyboard equivalent of dragging a
		* native session from the sidebar onto the task's session drop zone. Lists
		* every host session the task is not already bound to (the board's own
		* catalog — the same rows the '@' menu falls back to), tap one to bind it as
		* an added source (persisted, additive, instantly reconciled — exactly the
		* drag's effect). Nested inside the detail dialog, so it portals to the board
		* box and its body is the single `.modalScroll` scroller (the panel geometry
		* contract).
		*/
		function AddSessionModal({ controller, task, onClose }) {
			const [query, setQuery] = (0, react.useState)("");
			const bound = controller.relatedSessionIdSet(task);
			const needle = query.trim().toLowerCase();
			const rows = controller.referenceSessionCatalog().filter((row) => !bound.has(row.sessionId)).filter((row) => needle === "" || row.label.toLowerCase().includes(needle) || row.sessionId.toLowerCase().includes(needle)).slice(0, 50);
			const pick = (sessionId) => {
				controller.addTaskSource(task.id, {
					kind: "session",
					sessionId
				});
				onClose();
			};
			return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Dialog, {
				title: t("detail.addSessionTitle"),
				label: t("detail.addSessionTitle"),
				onClose,
				portal: true,
				children: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
					className: board_module_css_default.modalScroll,
					children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("input", {
						className: board_module_css_default.input,
						type: "text",
						value: query,
						placeholder: t("detail.addSessionSearch"),
						"aria-label": t("detail.addSessionSearch"),
						onChange: (event) => {
							setQuery(event.target.value);
						}
					}), rows.length === 0 ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
						className: board_module_css_default.detailText,
						children: t("detail.addSessionEmpty")
					}) : /* @__PURE__ */ (0, react_jsx_runtime.jsx)("ul", {
						className: board_module_css_default.addSessionList,
						children: rows.map((row) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)("li", { children: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("button", {
							type: "button",
							className: board_module_css_default.addSessionRow,
							onClick: () => {
								pick(row.sessionId);
							},
							children: [
								/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, {
									name: "link",
									className: board_module_css_default.sessionRowIcon
								}),
								/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
									className: board_module_css_default.addSessionLabel,
									title: row.sessionId,
									children: row.label
								}),
								/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, {
									name: "arrowRight",
									className: board_module_css_default.addSessionChevron
								})
							]
						}) }, row.sessionId))
					})]
				})
			});
		}
		//#endregion
		//#region src/client/board/SessionRow.tsx
		/**
		* One session row in a task's detail — THE single row component for every
		* session of a task (run sessions and bound external sessions alike). One
		* skeleton, written once: an identity slot + status chip + right-aligned
		* actions on the top line, a meta line below, and the run-row-only extras
		* (comment summary / live dynamics / error) as the footer slot. Clicking the
		* row activates it — the review page for a run row, the session panel
		* otherwise — while the row's own affordances stay on the row and never
		* bubble into the click. Row-specific data (run index vs workspace label,
		* times vs last-updated) is computed by the caller and passed in; the grammar
		* (chip + spinner, ghost "查看会话", quiet "隐藏", keyboard activation)
		* lives here once.
		*/
		/** The unified session row (one grammar for every session of a task). */
		function SessionRow({ state, chip, leading, meta, footer, handle, sessionId, onActivate, onOpenSession, onHide, hideTitle, draggable, onDragStart, onDragEnd, onRename, renameTitle }) {
			const glow = state === "waiting" || state === "running" ? "attention" : "none";
			const [renaming, setRenaming] = (0, react.useState)(false);
			const [draft, setDraft] = (0, react.useState)("");
			const [saving, setSaving] = (0, react.useState)(false);
			const [renameError, setRenameError] = (0, react.useState)(void 0);
			const startRename = () => {
				setDraft("");
				setRenameError(void 0);
				setRenaming(true);
			};
			const commitRename = () => {
				if (saving) return;
				const trimmed = draft.trim();
				if (trimmed === "") {
					setRenameError(t("detail.renameEmpty"));
					return;
				}
				setSaving(true);
				setRenameError(void 0);
				onRename?.(trimmed).then(() => {
					setSaving(false);
					setRenaming(false);
				}, (error) => {
					setSaving(false);
					setRenameError(String(error));
				});
			};
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("li", {
				className: board_module_css_default.sessionRow,
				"data-state": state,
				"data-waiting": state === "waiting" ? "true" : void 0,
				"data-glow": glow,
				"data-session-id": sessionId ?? void 0,
				role: "button",
				tabIndex: 0,
				draggable: draggable === true,
				onClick: onActivate,
				onDragStart,
				onDragEnd,
				onKeyDown: (event) => {
					const target = event.target;
					if (target instanceof HTMLElement && target.closest("input, textarea, select, [contenteditable=\"true\"], button") !== null && target !== event.currentTarget) return;
					if (event.key === "Enter" || event.key === " ") {
						event.preventDefault();
						onActivate();
					}
				},
				children: [
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
						className: board_module_css_default.sessionRowTop,
						children: [
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
								className: board_module_css_default.sessionRowLead,
								children: leading
							}),
							chip !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
								className: board_module_css_default.sessionRowChip,
								children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Chip, {
									kind: chip.kind,
									title: chip.title,
									icon: chip.spinner === true ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
										className: board_module_css_default.spinner,
										"aria-hidden": "true"
									}) : void 0,
									children: chip.label
								})
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
								className: board_module_css_default.sessionRowActions,
								children: [
									handle !== void 0 ? /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("button", {
										type: "button",
										className: board_module_css_default.sessionRowHandle,
										onClick: (event) => {
											event.stopPropagation();
											onOpenSession();
										},
										title: sessionId,
										children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
											className: board_module_css_default.rowActionText,
											children: handle
										}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, {
											name: "arrowRight",
											className: board_module_css_default.rowActionIcon
										})]
									}) : sessionId !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(Button, {
										size: "sm",
										onClick: (event) => {
											event.stopPropagation();
											onOpenSession();
										},
										title: sessionId,
										children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
											className: board_module_css_default.rowActionText,
											children: t("detail.viewSession")
										}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, {
											name: "arrowRight",
											className: board_module_css_default.rowActionIcon
										})]
									}),
									onRename !== void 0 && sessionId !== void 0 && !renaming && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("button", {
										type: "button",
										className: board_module_css_default.rowHide,
										title: renameTitle,
										onClick: (event) => {
											event.stopPropagation();
											startRename();
										},
										children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
											className: board_module_css_default.rowActionText,
											children: t("detail.rename")
										}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, {
											name: "pencil",
											className: board_module_css_default.rowActionIcon
										})]
									}),
									/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("button", {
										type: "button",
										className: board_module_css_default.rowHide,
										onClick: (event) => {
											event.stopPropagation();
											onHide();
										},
										title: hideTitle,
										children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
											className: board_module_css_default.rowActionText,
											children: t("detail.hide")
										}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, {
											name: "eyeOff",
											className: board_module_css_default.rowActionIcon
										})]
									})
								]
							})
						]
					}),
					renaming ? /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
						className: board_module_css_default.sessionRenameRow,
						onClick: (event) => {
							event.stopPropagation();
						},
						children: [
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("input", {
								className: `${board_module_css_default.input} ${board_module_css_default.sessionRenameInput}`,
								value: draft,
								autoFocus: true,
								disabled: saving,
								placeholder: renameTitle,
								"aria-label": renameTitle,
								onChange: (event) => {
									setDraft(event.target.value);
								},
								onKeyDown: (event) => {
									if (event.key === "Enter") {
										event.preventDefault();
										commitRename();
									}
									if (event.key === "Escape") {
										event.preventDefault();
										event.stopPropagation();
										setRenaming(false);
									}
								}
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
								size: "sm",
								variant: "primary",
								disabled: saving,
								onClick: commitRename,
								children: t("detail.renameOk")
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
								size: "sm",
								disabled: saving,
								onClick: () => {
									setRenaming(false);
								},
								children: t("detail.renameCancel")
							})
						]
					}) : /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.sessionRowMeta,
						children: meta
					}),
					renameError !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.executionError,
						children: renameError
					}),
					footer
				]
			});
		}
		//#endregion
		//#region src/client/sidebar-drag.ts
		/**
		* Sidebar drag contract + classification: how a drag that lands on the board
		* from the native sidebar (a workspace folder or a session) becomes a task.
		*
		* The native workspace browser already starts drags for BOTH row kinds and
		* stamps their identity into `text/plain` — a folder row sets
		* `text/plain = workspaceId` (its `row.key`), a session row sets
		* `text/plain = sessionId` (its `node.id`). So the board does NOT need to
		* re-stamp anything; it just classifies the incoming `text/plain` (after
		* ruling out its own card drags, which use `text/plain = task id`) against
		* the known native ids. The board's own MIME types are kept as an explicit,
		* forward-compatible channel (they take priority when present).
		*
		* Pure helpers — no DOM coupling — so they unit-test cleanly.
		*/
		/** MIME a session drag is stamped with by the board (explicit channel). */
		const SESSION_MIME = "application/x-dsh-task-board-session";
		/** MIME a workspace-folder drag is stamped with by the board (explicit channel). */
		const WORKSPACE_MIME = "application/x-dsh-task-board-workspace";
		/**
		* Whether a drag's advertised types mark it as a sidebar-drag candidate.
		* Types are the ONLY reliable signal during dragover/dragenter — the drag
		* data store is in protected mode there and `getData` returns empty in
		* Chrome — so the board latches the candidate from `dataTransfer.types`
		* and reads the actual payload (`externalDragOf`) only at drop time. The
		* board's own card drags also carry `text/plain`, so the caller must
		* exclude them (its drag-source ref) before latching.
		*/
		function candidateExternalDrag(types) {
			return types.includes("application/x-dsh-task-board-session") || types.includes("application/x-dsh-task-board-workspace") || types.includes("text/plain");
		}
		/** Read a board-stamped sidebar drag from a drop's dataTransfer (own MIME only). */
		function readSidebarDrag(data) {
			const sessionId = data.getData(SESSION_MIME);
			if (sessionId !== "") return {
				kind: "session",
				id: sessionId
			};
			const workspaceId = data.getData(WORKSPACE_MIME);
			if (workspaceId !== "") return {
				kind: "workspace",
				id: workspaceId
			};
		}
		/** Normalize a possibly-composite native key to its id tail. */
		function idOfKey(key) {
			return key.split(":").pop()?.split(/[\\/]/).pop() ?? key;
		}
		/**
		* Classify a drop's dataTransfer as a sidebar drag: own MIME first, then the
		* native `text/plain` (a folder = workspaceId, a session = sessionId), after
		* ruling out the board's own card drags (`text/plain = task id`).
		* @param data - the drop's dataTransfer.
		* @param isKnownBoardTask - whether a `text/plain` value is a board task id
		*   (in which case it is the board's own card drag, never external).
		* @param resolve - (id) => 'session' | 'workspace' | undefined against the
		*   known native snapshots.
		*/
		function externalDragOf(data, isKnownBoardTask, resolve) {
			const own = readSidebarDrag(data);
			if (own !== void 0) return own;
			const text = data.getData("text/plain");
			if (text === "" || isKnownBoardTask(text)) return void 0;
			const id = idOfKey(text);
			const kind = resolve(id);
			if (kind === void 0) return void 0;
			return {
				kind,
				id
			};
		}
		//#endregion
		//#region src/client/board/TaskDetail.tsx
		/**
		* Task detail: the full view of one task — content, prompt, execution
		* history — and the only place execution can be triggered. Also offers
		* delete (with confirmation), manual status moves, and a jump to the
		* execution's session transcript.
		*/
		/** Status → shared-chip color (detail badge). */
		const STATUS_CHIP = {
			backlog: "neutral",
			todo: "neutral",
			running: "warn",
			review: "neutral",
			done: "success"
		};
		/** The one comment summary of a session row (count + newest body + time),
		*  shared verbatim by run rows and linked rows: a body-less round leaves the
		*  slot to the row's own state chip + time — never a state word dressed up as
		*  content. */
		function CommentSummary({ task, sessionId, cruiseOn }) {
			const comments = sessionId !== void 0 ? sessionCommentsOf(task, sessionId, cruiseOn) : [];
			const latest = sessionId !== void 0 ? latestCommentView(task, sessionId, cruiseOn) : void 0;
			if (latest === void 0) return null;
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
				className: board_module_css_default.executionComments,
				title: latest.text,
				children: [
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.executionCommentsCount,
						children: t("detail.comments", { n: String(comments.length) })
					}),
					latest.text !== "" && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.executionCommentsLatest,
						children: t("detail.latestComment", { text: latest.text })
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.executionCommentsTime,
						children: latest.at !== void 0 ? formatTime(latest.at) : ""
					})
				]
			});
		}
		/** One session row of a task — THE single row grammar for every session
		*  (run rows open their review page + show comments/dynamics/error; linked
		*  rows show the workspace label + idle chip). The state chip comes from the
		*  ONE derivation (sessionStateChip) — only the settled-label pair differs
		*  between a run row (the execution result) and a linked row (the bound
		*  session's activity). */
		function SessionActionRow({ row, task, controller, cruiseOn, workspaceTitleOf, onReviewExecution, onOpenSessionPanel, draggable, onDragStart, onDragEnd }) {
			const isRun = row.executionId !== void 0;
			const sessionId = row.sessionId;
			if (isRun) {
				const execution = task.executions.find((candidate) => candidate.id === row.executionId);
				if (execution === void 0) return null;
				const session = sessionDisplay(task, execution, row.display.waitingKind, controller.nativeRunningOf(sessionId));
				const times = sessionTimes(task, execution);
				const isActive = session.state === "running" || session.state === "waiting";
				const runWorkspace = task.workspaceId !== void 0 ? workspaceTitleOf(task.workspaceId) : "";
				return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(SessionRow, {
					state: session.state,
					chip: sessionStateChip(session.state, session.waitingKind, "detail.linkedDone", "detail.result.cancelled"),
					leading: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
						className: board_module_css_default.sessionRowLeading,
						title: row.title,
						children: [
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, {
								name: "play",
								className: board_module_css_default.sessionRowIcon
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
								className: board_module_css_default.sessionRowName,
								children: row.title
							}),
							runWorkspace !== "" && runWorkspace !== row.title && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
								className: board_module_css_default.sessionRowWorkspace,
								children: runWorkspace
							})
						]
					}),
					meta: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [
						t("detail.executionStarted"),
						" ",
						formatDateTime(times.startedAt),
						" · ",
						t("detail.executionEnded"),
						" ",
						times.endedAt !== void 0 ? formatDateTime(times.endedAt) : "—",
						times.duration !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [" · ", t("detail.duration", { d: formatDuration(times.duration) })] })
					] }),
					footer: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [
						/* @__PURE__ */ (0, react_jsx_runtime.jsx)(CommentSummary, {
							task,
							sessionId,
							cruiseOn
						}),
						isActive && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
							className: board_module_css_default.executionDynamics,
							children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
								className: board_module_css_default.executionDynamicsLabel,
								children: session.waitingKind !== void 0 ? t("detail.handleHint", { kind: t(waitingKeyOf(session.waitingKind)) }) : t("detail.sessionActive")
							})
						}),
						execution.error !== void 0 && execution.error !== "" && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
							className: board_module_css_default.executionError,
							children: execution.error
						})
					] }),
					handle: session.state === "waiting" && sessionId !== void 0 ? t("detail.handle") : void 0,
					sessionId,
					draggable,
					onDragStart,
					onDragEnd,
					onRename: async (title) => {
						const result = await controller.renameTaskSession(task.id, sessionId, title);
						if (!result.ok) throw new Error(result.error);
					},
					renameTitle: t("detail.renameSessionTitle"),
					onActivate: () => {
						onReviewExecution(execution);
					},
					onOpenSession: () => {
						if (sessionId !== void 0) controller.openSession(sessionId);
					},
					onHide: () => {
						controller.hideTaskSession(task.id, sessionId);
					},
					hideTitle: t("detail.hideRow")
				});
			}
			const chip = sessionStateChip(row.display.state, row.display.waitingKind, "detail.linkedDone", "detail.linkedIdle", "detail.idleHint");
			const window = sessionWindowOf(task, sessionId);
			return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(SessionRow, {
				state: row.display.state,
				chip,
				leading: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
					className: board_module_css_default.sessionRowLeading,
					children: [
						/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, {
							name: "link",
							className: board_module_css_default.sessionRowIcon
						}),
						/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
							className: board_module_css_default.sessionRowName,
							title: row.title,
							children: row.title
						}),
						row.workspaceLabel !== void 0 && row.workspaceLabel !== row.title && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
							className: board_module_css_default.sessionRowWorkspace,
							children: row.workspaceLabel
						})
					]
				}),
				meta: window.startedAt !== void 0 ? /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [
					t("detail.executionStarted"),
					" ",
					formatDateTime(window.startedAt),
					" · ",
					t("detail.executionEnded"),
					" ",
					window.endedAt !== void 0 ? formatDateTime(window.endedAt) : "—",
					window.duration !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [" · ", t("detail.duration", { d: formatDuration(window.duration) })] })
				] }) : /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [
					t("detail.sessionUpdated"),
					" ",
					formatDateTime(row.updatedAt)
				] }),
				footer: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(CommentSummary, {
					task,
					sessionId,
					cruiseOn
				}),
				handle: row.display.state === "waiting" ? t("detail.handle") : void 0,
				sessionId,
				draggable,
				onDragStart,
				onDragEnd,
				onRename: async (title) => {
					const result = await controller.renameTaskSession(task.id, sessionId, title);
					if (!result.ok) throw new Error(result.error);
				},
				renameTitle: t("detail.renameSessionTitle"),
				onActivate: () => {
					onOpenSessionPanel(sessionId);
				},
				onOpenSession: () => {
					controller.openSession(sessionId);
				},
				onHide: () => {
					controller.hideTaskSession(task.id, sessionId);
				},
				hideTitle: t("detail.hideRow")
			});
		}
		/** The automation module (task-level orchestration): one collapsed line = the
		*  live state; the expanded body is the SHARED AutomationEditor — the same
		*  full editor the board's 自动化 overview task cards render, so the
		*  task-level schedule (按时间表 / 完成后接续) and the session rules have
		*  exactly ONE UI and the board CAN edit everything the detail can. */
		function AutomationSection({ controller, task }) {
			const schedule = task.schedule;
			const [open, setOpen] = (0, react.useState)(() => schedule?.enabled === true);
			(0, react.useEffect)(() => {
				if (schedule?.enabled === true) setOpen(true);
			}, [task.id, schedule?.enabled]);
			const summary = scheduleSummary(task);
			return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Disclosure, {
				title: t("detail.schedule"),
				summary,
				open,
				onToggle: () => {
					setOpen(!open);
				},
				children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(AutomationEditor, {
					controller,
					task,
					embedded: true
				})
			});
		}
		/** Task detail overlay. */
		function TaskDetail({ controller, task, workspaceTitleOf, dragSourceRef, requestSessionId, onRequestSessionConsumed }) {
			useEscapeStack(() => {
				detailEscape();
			});
			const detailPanelRef = (0, react.useRef)(null);
			const [confirmDelete, setConfirmDelete] = (0, react.useState)(false);
			const [confirmRemoveSession, setConfirmRemoveSession] = (0, react.useState)(void 0);
			const [draft, setDraft] = (0, react.useState)(void 0);
			const [editError, setEditError] = (0, react.useState)(void 0);
			const [runError, setRunError] = (0, react.useState)(void 0);
			const [draftRestored, setDraftRestored] = (0, react.useState)(false);
			const [configOpen, setConfigOpen] = (0, react.useState)(false);
			const [reviewExecution, setReviewExecution] = (0, react.useState)(void 0);
			const [linkedSession, setLinkedSession] = (0, react.useState)(void 0);
			(0, react.useEffect)(() => {
				if (requestSessionId !== void 0) {
					setLinkedSession(requestSessionId);
					onRequestSessionConsumed?.();
				}
			}, [requestSessionId, onRequestSessionConsumed]);
			const [showNewSession, setShowNewSession] = (0, react.useState)(false);
			const [showAddSession, setShowAddSession] = (0, react.useState)(false);
			const current = task;
			const referenceSession = controller.referenceSessionOf(current.id);
			(0, react.useEffect)(() => {
				setDraft(void 0);
				setEditError(void 0);
				setDraftRestored(false);
				const stored = draftStore.get(editDraftKey(task.id));
				if (stored !== void 0) try {
					const parsed = normalizeDraft(JSON.parse(stored));
					if (parsed !== void 0) {
						setDraft(parsed);
						setDraftRestored(true);
					}
				} catch {
					draftStore.clear(editDraftKey(task.id));
				}
			}, [task.id]);
			const busy = hasOpenRun(current);
			const customizedCount = [
				current.workspaceId,
				current.agentPreset,
				current.provider,
				current.model,
				current.reasoningEffort,
				current.permission
			].filter((value) => value !== void 0 && value !== "").length;
			const configSummary = customizedCount === 0 ? t("detail.runConfigDefault") : t("detail.runConfigCustom", { n: String(customizedCount) });
			const sessions = controller.sessionsOf(current);
			const hiddenIds = hiddenSessionIdsOf(current);
			const [bindDropActive, setBindDropActive] = (0, react.useState)(false);
			const bindDropLatch = (0, react.useRef)(false);
			const [bindDropFlash, setBindDropFlash] = (0, react.useState)(false);
			const bindDropTimer = (0, react.useRef)(void 0);
			(0, react.useEffect)(() => {
				const clear = () => {
					bindDropLatch.current = false;
					setBindDropActive(false);
					clearSessionDrag();
				};
				window.addEventListener("drop", clear);
				window.addEventListener("dragend", clear);
				return () => {
					window.removeEventListener("drop", clear);
					window.removeEventListener("dragend", clear);
					if (bindDropTimer.current !== void 0) clearTimeout(bindDropTimer.current);
				};
			}, []);
			const [sessionDragId, setSessionDragId] = (0, react.useState)(void 0);
			const [sessionGap, setSessionGap] = (0, react.useState)(void 0);
			const sessionGapRef = (0, react.useRef)(void 0);
			const sessionListRef = (0, react.useRef)(null);
			const sessionIndicatorRef = (0, react.useRef)(null);
			const clearSessionDrag = () => {
				setSessionDragId(void 0);
				setSessionGap(void 0);
				sessionGapRef.current = void 0;
			};
			/** The exact insertion slot of a session-row drag at `dropY` (content coords). */
			const sessionGapAt = (dropY) => {
				const container = sessionListRef.current;
				if (container === null || sessionDragId === void 0) return {
					beforeId: void 0,
					top: 0
				};
				const gap = insertionGapOf(Array.from(container.querySelectorAll("[data-session-id]")).map((element) => ({
					id: element.getAttribute("data-session-id") ?? "",
					rect: element.getBoundingClientRect()
				})), dropY, sessionDragId, 8);
				const containerTop = container.getBoundingClientRect().top;
				return {
					beforeId: gap.beforeId,
					top: indicatorTopOf(gap.top, containerTop, container.scrollTop, container.scrollHeight)
				};
			};
			/** Apply a session insertion gap to the UI — THE one gap-write path
			*  (dragover and the auto-scroll frame share it). */
			const applySessionGap = (0, react.useCallback)((dropY) => {
				const gap = sessionGapAt(dropY);
				sessionGapRef.current = gap;
				const indicator = sessionIndicatorRef.current;
				if (indicator !== null) indicator.style.top = `${gap.top}px`;
				setSessionGap((current) => current !== void 0 && current.beforeId === gap.beforeId ? current : gap);
			}, [sessionGapAt]);
			const detailBodyRef = (0, react.useRef)(null);
			useDragAutoScroll((0, react.useCallback)(() => detailBodyRef.current, []), sessionDragId !== void 0, applySessionGap);
			/** Latch an external sidebar drag once, on entry into the zone. */
			const onZoneDragEnter = (event) => {
				if (!dragSourceRef.current && candidateExternalDrag(Array.from(event.dataTransfer.types))) {
					bindDropLatch.current = true;
					setBindDropActive(true);
				}
			};
			/** Allow the drop only for a latched external drag. */
			const onZoneDragOver = (event) => {
				if (bindDropLatch.current) event.preventDefault();
			};
			/** A sidebar session/workspace dropped on the zone ADDS the live source to
			*  this task (an already-bound source is an idempotent no-op — never a
			*  replace); board card drags resolve to undefined and are left untouched.
			*  Every drop ends by clearing the latch. */
			const onZoneDrop = (event) => {
				bindDropLatch.current = false;
				setBindDropActive(false);
				const external = externalDragOf(event.dataTransfer, (id) => controller.getSnapshot().tasks.some((task) => task.id === id), (id) => controller.externalKindOf(id));
				if (external === void 0) return;
				event.preventDefault();
				event.stopPropagation();
				const bind = external.kind === "session" ? {
					kind: "session",
					sessionId: external.id
				} : {
					kind: "workspace",
					workspaceId: external.id
				};
				controller.addTaskSource(current.id, bind);
				setBindDropFlash(true);
				if (bindDropTimer.current !== void 0) clearTimeout(bindDropTimer.current);
				bindDropTimer.current = setTimeout(() => {
					setBindDropFlash(false);
				}, 600);
			};
			const [promptCopied, setPromptCopied] = (0, react.useState)(false);
			const [promptCopyFailed, setPromptCopyFailed] = (0, react.useState)(false);
			const promptCopyTimer = (0, react.useRef)(void 0);
			(0, react.useEffect)(() => () => {
				if (promptCopyTimer.current !== void 0) clearTimeout(promptCopyTimer.current);
			}, []);
			const editing = draft !== void 0;
			const { onKeyDown: onDetailKeyDown } = useDialogFocus(detailPanelRef, editing);
			/** Enter edit mode with a draft of the current record. */
			const startEditing = () => {
				setDraft(draftFromTask(current));
				setEditError(void 0);
				setDraftRestored(false);
			};
			/** New-task copy of this task ("复制为模板"): fresh card, same content and
			*  run config, landing in 待规划; runs/links/session rules are not copied,
			*  the schedule rides disarmed (controller.copyTask). */
			const duplicateTask = () => {
				if (controller.copyTask(current.id) !== void 0) controller.closeTask();
			};
			/** Snapshot this task into the named template library ("存为模板"):
			*  content + run config, no bindings/runs/schedule — the stamped card
			*  never surprise-fires. The detail stays open (the source is untouched). */
			const [templateSaved, setTemplateSaved] = (0, react.useState)(false);
			const saveAsTemplate = () => {
				if (controller.saveTemplate(current.id) === void 0) return;
				setTemplateSaved(true);
				if (promptCopyTimer.current !== void 0) clearTimeout(promptCopyTimer.current);
				promptCopyTimer.current = setTimeout(() => {
					setTemplateSaved(false);
				}, 2e3);
			};
			/** Copy the execution prompt to the clipboard (best-effort; no throw). */
			const copyPrompt = () => {
				if (current.prompt === "") return;
				const flash = (state) => {
					setPromptCopied(state === "ok");
					setPromptCopyFailed(state === "failed");
					if (promptCopyTimer.current !== void 0) clearTimeout(promptCopyTimer.current);
					promptCopyTimer.current = setTimeout(() => {
						setPromptCopied(false);
						setPromptCopyFailed(false);
					}, 1500);
				};
				navigator.clipboard?.writeText(current.prompt).then(() => {
					flash("ok");
				}, () => {
					flash("failed");
				});
			};
			/** Persist the draft; title may be blank (the first real run supplements
			*  it) — updateTask only rejects an unknown task. */
			const saveEdit = () => {
				if (draft === void 0) return;
				if (!controller.updateTask(current.id, draftToUpdatePatch(draft))) return;
				setDraft(void 0);
				setEditError(void 0);
				setDraftRestored(false);
				draftStore.clear(editDraftKey(current.id));
			};
			/** Discard the draft and leave edit mode (explicit discard clears too). */
			const cancelEdit = () => {
				setDraft(void 0);
				setEditError(void 0);
				setDraftRestored(false);
				draftStore.clear(editDraftKey(current.id));
			};
			const detailEscape = () => {
				if (draft !== void 0) cancelEdit();
				else controller.closeTask();
			};
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
				className: board_module_css_default.modalBackdrop,
				onMouseDown: (event) => {
					if (event.target === event.currentTarget) controller.closeTask();
				},
				children: [
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
						ref: detailPanelRef,
						tabIndex: -1,
						className: board_module_css_default.detail,
						role: "dialog",
						"aria-modal": "true",
						"aria-label": t("detail.title"),
						onKeyDown: onDetailKeyDown,
						children: [
							/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("header", {
								className: board_module_css_default.detailHeader,
								children: [
									/* @__PURE__ */ (0, react_jsx_runtime.jsx)("h2", {
										className: board_module_css_default.detailTitle,
										children: titleOrUntitled(current.title, t("card.untitled"))
									}),
									/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Chip, {
										kind: STATUS_CHIP[current.status],
										children: t(STATUS_KEY[current.status])
									}),
									!editing && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
										onClick: startEditing,
										children: t("detail.edit")
									}),
									/* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
										type: "button",
										className: board_module_css_default.iconButton,
										"aria-label": t("detail.close"),
										onClick: () => {
											controller.closeTask();
										},
										children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, { name: "close" })
									})
								]
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
								className: board_module_css_default.detailBody,
								ref: detailBodyRef,
								children: [
									editing && draft !== void 0 ? /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [
										/* @__PURE__ */ (0, react_jsx_runtime.jsx)(TaskForm, {
											draft,
											onChange: (next) => {
												setDraft(next);
												draftStore.set(editDraftKey(current.id), JSON.stringify(next));
											},
											controller,
											sessionId: referenceSession
										}),
										editError !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
											className: board_module_css_default.formError,
											children: editError
										}),
										draftRestored && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
											className: board_module_css_default.detailHint,
											children: t("detail.editDraftRestored")
										})
									] }) : /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [
										/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Section, {
											title: t("detail.description"),
											children: current.description !== "" ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
												className: board_module_css_default.contentBlock,
												children: current.description
											}) : /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
												className: board_module_css_default.detailEmptyField,
												children: t("detail.emptyField")
											})
										}),
										/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Section, {
											title: t("detail.prompt"),
											children: current.prompt === "" ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
												className: board_module_css_default.detailBlockedField,
												children: t("detail.promptEmpty")
											}) : /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
												className: board_module_css_default.promptBlock,
												children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("pre", {
													className: board_module_css_default.promptBlockText,
													children: current.prompt
												}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
													type: "button",
													className: board_module_css_default.promptCopy,
													title: promptCopied ? t("detail.copied") : promptCopyFailed ? t("detail.copyFailed") : t("detail.copyPrompt"),
													"aria-label": t("detail.copyPrompt"),
													onClick: copyPrompt,
													children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, { name: promptCopied ? "check" : "copy" })
												})]
											})
										}),
										/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Disclosure, {
											title: t("detail.runConfig"),
											summary: configSummary,
											open: configOpen,
											onToggle: () => {
												setConfigOpen(!configOpen);
											},
											children: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("dl", {
												className: board_module_css_default.configGrid,
												children: [
													/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
														className: board_module_css_default.configRow,
														children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("dt", {
															className: board_module_css_default.configLabel,
															children: t("new.workspace")
														}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("dd", {
															className: board_module_css_default.configValue,
															children: current.workspaceId !== void 0 ? workspaceTitleOf(current.workspaceId) : t("new.workspaceDefault")
														})]
													}),
													/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
														className: board_module_css_default.configRow,
														children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("dt", {
															className: board_module_css_default.configLabel,
															children: t("new.agentPreset")
														}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("dd", {
															className: board_module_css_default.configValue,
															children: current.agentPreset !== void 0 ? current.agentPreset : t("new.agentPresetDefault")
														})]
													}),
													/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
														className: board_module_css_default.configRow,
														children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("dt", {
															className: board_module_css_default.configLabel,
															children: t("new.model")
														}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("dd", {
															className: board_module_css_default.configValue,
															children: current.provider !== void 0 && current.model !== void 0 ? `${current.provider} / ${current.model}` : t("new.modelDefault")
														})]
													}),
													/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
														className: board_module_css_default.configRow,
														children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("dt", {
															className: board_module_css_default.configLabel,
															children: t("new.effort")
														}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("dd", {
															className: board_module_css_default.configValue,
															children: current.reasoningEffort !== void 0 ? current.reasoningEffort : t("new.effortDefault")
														})]
													}),
													/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
														className: board_module_css_default.configRow,
														children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("dt", {
															className: board_module_css_default.configLabel,
															children: t("new.permission")
														}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("dd", {
															className: board_module_css_default.configValue,
															children: current.permission !== void 0 ? permissionLabel(current.permission) : t("new.permissionDefault")
														})]
													})
												]
											})
										})
									] }),
									/* @__PURE__ */ (0, react_jsx_runtime.jsx)(AutomationSection, {
										controller,
										task: current
									}),
									/* @__PURE__ */ (0, react_jsx_runtime.jsxs)(Section, {
										title: `${t("detail.sessions")} ${sessions.length}`,
										children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("p", {
											className: board_module_css_default.sessionHintRow,
											children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
												className: `${board_module_css_default.detailHint} ${board_module_css_default.sessionHintText}`,
												children: t("detail.executionHint")
											}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
												className: board_module_css_default.sessionToolbarActions,
												children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
													size: "sm",
													variant: "ghost",
													title: t("detail.addSessionTitle"),
													onClick: () => {
														setShowAddSession(true);
													},
													children: t("detail.addSession")
												}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(Button, {
													size: "sm",
													title: t("detail.sessionNewTitle"),
													onClick: () => {
														setShowNewSession(true);
													},
													children: ["+ ", t("detail.sessionNew")]
												})]
											})]
										}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
											className: board_module_css_default.sessionDropZone,
											"data-bindactive": bindDropActive ? "" : void 0,
											"data-flash": bindDropFlash ? "" : void 0,
											onDragEnter: onZoneDragEnter,
											onDragOver: onZoneDragOver,
											onDrop: onZoneDrop,
											children: [sessions.length === 0 ? plainRunsOf(current).length > 0 || taskBindsOf(current).length > 0 ? null : /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
												className: board_module_css_default.detailText,
												children: t("detail.noExecution")
											}) : /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("ul", {
												className: board_module_css_default.sessionList,
												ref: sessionListRef,
												"data-reordering": sessionDragId !== void 0 ? "" : void 0,
												onDragOver: (event) => {
													if (sessionDragId === void 0) return;
													event.preventDefault();
													applySessionGap(event.clientY);
												},
												onDrop: (event) => {
													if (sessionDragId === void 0) return;
													event.preventDefault();
													const gap = sessionGapRef.current;
													const dragged = sessionDragId;
													clearSessionDrag();
													controller.reorderTaskSession(current.id, dragged, gap?.beforeId);
												},
												children: [sessionGap !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
													ref: sessionIndicatorRef,
													className: board_module_css_default.dropIndicator,
													style: { top: sessionGap.top },
													"aria-hidden": "true"
												}), sessions.map((row) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)(SessionActionRow, {
													row,
													task: current,
													controller,
													cruiseOn: controller.getSnapshot().cruise.enabled,
													workspaceTitleOf,
													onReviewExecution: (execution) => {
														setReviewExecution(execution);
													},
													onOpenSessionPanel: (sessionId) => {
														setLinkedSession(sessionId);
													},
													draggable: true,
													onDragStart: (event) => {
														setSessionDragId(row.sessionId);
														event.dataTransfer.effectAllowed = "move";
														event.dataTransfer.setData("text/plain", row.sessionId);
													},
													onDragEnd: clearSessionDrag
												}, row.sessionId))]
											}), hiddenIds.size > 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
												className: board_module_css_default.hiddenTray,
												children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
													className: board_module_css_default.hiddenTrayHead,
													children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
														className: board_module_css_default.hiddenTrayTitle,
														children: t("detail.hiddenTrayTitle", { n: String(hiddenIds.size) })
													}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
														size: "sm",
														variant: "ghost",
														onClick: () => {
															controller.unhideTaskSessions(current.id);
														},
														children: t("detail.restoreHidden")
													})]
												}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("ul", {
													className: board_module_css_default.hiddenTrayList,
													children: Array.from(hiddenIds).map((sessionId) => /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("li", {
														className: board_module_css_default.hiddenTrayRow,
														children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
															className: board_module_css_default.hiddenTrayName,
															title: sessionId,
															children: controller.sessionTitle(sessionId) ?? sessionId
														}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
															className: board_module_css_default.hiddenTrayActions,
															children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
																size: "sm",
																variant: "ghost",
																onClick: () => {
																	controller.unhideTaskSession(current.id, sessionId);
																},
																children: t("detail.restoreOne")
															}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
																size: "sm",
																variant: "dangerGhost",
																onClick: () => {
																	setConfirmRemoveSession(sessionId);
																},
																children: t("detail.delete")
															})]
														})]
													}, sessionId))
												})]
											})]
										})]
									}),
									current.status === "backlog" && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(RefineSection, {
										controller,
										task: current
									}),
									/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Section, {
										title: t("board.status"),
										children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
											className: board_module_css_default.moveRow,
											children: MANUAL_STATUSES.map((status) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
												disabled: current.status === status || busy,
												onClick: () => {
													controller.moveTask(current.id, status);
												},
												children: t(`status.move.${status}`)
											}, status))
										})
									})
								]
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("footer", {
								className: board_module_css_default.detailFooter,
								children: [
									/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
										className: board_module_css_default.detailFooterGroup,
										children: [
											editing ? /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
												variant: "primary",
												onClick: saveEdit,
												children: t("detail.save")
											}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
												onClick: cancelEdit,
												children: t("detail.cancel")
											})] }) : /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
												variant: "primary",
												disabled: busy || !taskExecutable(current),
												title: taskExecutable(current) ? t("detail.rerunHint") : t("detail.promptEmpty"),
												onClick: () => {
													if (!taskExecutable(current)) return;
													setRunError(void 0);
													controller.rerunTask(current.id).then((accepted) => {
														if (accepted) {
															controller.closeTask();
															return;
														}
														setRunError(t("detail.runBlockedBusy"));
													});
												},
												children: plainRunsOf(current).length === 0 ? t("detail.run") : t("detail.rerun")
											}),
											runError !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
												className: board_module_css_default.formError,
												children: runError
											}),
											/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
												title: t("detail.duplicateTitle"),
												onClick: duplicateTask,
												children: t("detail.duplicate")
											}),
											/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
												title: t("detail.saveTemplateTitle"),
												onClick: saveAsTemplate,
												children: templateSaved ? t("detail.savedTemplate") : t("detail.saveTemplate")
											})
										]
									}),
									/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
										className: board_module_css_default.detailFooterDanger,
										children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
											variant: "danger",
											onClick: () => {
												setConfirmDelete(true);
											},
											children: t("detail.delete")
										})
									}),
									/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
										className: board_module_css_default.detailMeta,
										children: [
											t("board.created"),
											" ",
											formatTime(current.createdAt)
										]
									})
								]
							})
						]
					}),
					confirmDelete && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(ConfirmDialog, {
						title: t("delete.title"),
						message: t("delete.confirm", { name: titleOrUntitled(current.title, t("card.untitled")) }),
						confirmLabel: t("delete.ok"),
						danger: true,
						onCancel: () => {
							setConfirmDelete(false);
						},
						onConfirm: () => {
							setConfirmDelete(false);
							controller.deleteTask(current.id);
							controller.closeTask();
						}
					}),
					confirmRemoveSession !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(ConfirmDialog, {
						title: t("detail.sessionRemoveTitle", { name: controller.sessionTitle(confirmRemoveSession) ?? confirmRemoveSession }),
						message: t("detail.sessionRemoveConfirm"),
						confirmLabel: t("detail.sessionRemoveOk"),
						danger: true,
						onCancel: () => {
							setConfirmRemoveSession(void 0);
						},
						onConfirm: () => {
							controller.removeTaskSession(current.id, confirmRemoveSession);
							setConfirmRemoveSession(void 0);
						}
					}),
					reviewExecution !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(ReviewDetail, {
						controller,
						task: current,
						execution: reviewExecution,
						onClose: () => {
							setReviewExecution(void 0);
						}
					}),
					linkedSession !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(SessionDetail, {
						controller,
						task: current,
						sessionId: linkedSession,
						onClose: () => {
							setLinkedSession(void 0);
						}
					}),
					showNewSession && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(NewSessionModal, {
						controller,
						task: current,
						onClose: () => {
							setShowNewSession(false);
						},
						onCreated: () => {
							setBindDropFlash(true);
							if (bindDropTimer.current !== void 0) clearTimeout(bindDropTimer.current);
							bindDropTimer.current = setTimeout(() => {
								setBindDropFlash(false);
							}, 600);
						}
					}),
					showAddSession && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(AddSessionModal, {
						controller,
						task: current,
						onClose: () => {
							setShowAddSession(false);
						}
					})
				]
			});
		}
		//#endregion
		//#region src/client/board/AutomationPanel.tsx
		/**
		* AutomationPanel: the board header's "自动化" overview. ONE place to see AND
		* manage BOTH automatic-driving kinds of every ACTIVE automated task — the
		* task-level schedule (cron / 完成后接续) and the session-level rules.
		*
		* Membership = live automation only (`automationTasksOf`: an enabled schedule
		* or at least one session rule — a disarmed rule is not listed as if it were
		* running). EVERY card is one expand row: identity header + a single live
		* summary line (the ONE summary grammar — a task-level schedule renders
		* scheduleSummary, a rules-only task renders its rule count) that expands
		* into the SHARED AutomationEditor verbatim. The enable switch lives ONLY in
		* the editor (one switch, one place, no duplicated affordances), and the
		* board has every capability the detail has (including 完成后接续).
		*/
		/** One automated task in the overview: identity + ONE expand row (live
		*  summary) + the shared full editor when expanded. */
		function AutomationTaskCard({ controller, task, onClose }) {
			const [expanded, setExpanded] = (0, react.useState)(false);
			const editorId = (0, react.useId)();
			const summary = task.schedule !== void 0 ? scheduleSummary(task) : t("auto.ruleCount", { n: String(task.rules?.length ?? 0) });
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("section", {
				className: board_module_css_default.autoTask,
				children: [
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("header", {
						className: board_module_css_default.autoTaskHead,
						children: [
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
								className: board_module_css_default.autoTaskTitle,
								title: task.title,
								children: task.title
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Chip, {
								kind: "muted",
								children: t(STATUS_KEY[task.status])
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
								size: "sm",
								title: t("auto.openDetail"),
								onClick: () => {
									controller.openTask(task.id);
									onClose();
								},
								children: t("auto.openDetail")
							})
						]
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("button", {
						type: "button",
						className: board_module_css_default.autoTaskExpand,
						"aria-expanded": expanded,
						"aria-controls": editorId,
						title: t("auto.editAutomation"),
						onClick: () => {
							setExpanded((value) => !value);
						},
						children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, {
							name: "chevronDown",
							className: board_module_css_default.autoTaskChevron
						}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
							className: board_module_css_default.autoTaskSummary,
							title: summary,
							children: summary
						})]
					}),
					expanded && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
						id: editorId,
						children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(AutomationEditor, {
							controller,
							task
						})
					})
				]
			});
		}
		/** The unified automation overview panel (see module doc). */
		function AutomationPanel({ controller, onClose }) {
			const [snapshot, setSnapshot] = (0, react.useState)(controller.getSnapshot());
			(0, react.useEffect)(() => controller.subscribe(() => setSnapshot(controller.getSnapshot())), [controller]);
			const automated = automationTasksOf(snapshot.tasks);
			return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Dialog, {
				title: t("auto.title"),
				label: t("auto.title"),
				onClose,
				className: board_module_css_default.autoModal,
				children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
					className: board_module_css_default.modalScroll,
					children: automated.length === 0 ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
						className: board_module_css_default.autoEmpty,
						children: t("auto.empty")
					}) : /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
						className: board_module_css_default.autoList,
						children: automated.map((task) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)(AutomationTaskCard, {
							controller,
							task,
							onClose
						}, task.id))
					})
				})
			});
		}
		//#endregion
		//#region src/client/board/time-parse.ts
		/**
		* Free-typed time parsing for the cruise TimeField, plus the canonical text
		* formatting. The field reads like any text input: type a time, press Enter
		* or blur, done. Accepts the common short forms and normalizes them; anything
		* unparseable returns undefined (the caller flags it inline, never swallows
		* the user's text). Pure and framework-free so it unit-tests in isolation.
		*/
		/** Format an epoch millisecond as the field's canonical text (`YYYY-MM-DD HH:mm`). */
		function formatTimeInput(ms) {
			const date = new Date(ms);
			const pad = (value) => String(value).padStart(2, "0");
			return `${date.getFullYear()}-${pad(date.getMonth() + 1)}-${pad(date.getDate())} ${pad(date.getHours())}:${pad(date.getMinutes())}`;
		}
		/**
		* Parse a free-typed time text into epoch ms (minute precision):
		* - `HH:mm`              → today at that clock
		* - `MM-DD HH:mm`        → this year
		* - `YYYY-MM-DD HH:mm`   → exact (also accepts a `T` separator)
		* - empty / whitespace   → undefined (a clear, not an error)
		* Any other text returns undefined, meaning "unparseable".
		*/
		function parseTimeText(text, now = Date.now()) {
			const trimmed = text.trim();
			if (trimmed === "") return void 0;
			const exact = /^(\d{4})-(\d{1,2})-(\d{1,2})[ T](\d{1,2}):(\d{2})$/.exec(trimmed);
			if (exact !== null) return compose(exact[1], exact[2], exact[3], exact[4], exact[5]);
			const monthDay = /^(\d{1,2})-(\d{1,2})[ T](\d{1,2}):(\d{2})$/.exec(trimmed);
			if (monthDay !== null) {
				const base = new Date(now);
				return compose(String(base.getFullYear()), monthDay[1], monthDay[2], monthDay[3], monthDay[4]);
			}
			const clock = /^(\d{1,2}):(\d{2})$/.exec(trimmed);
			if (clock !== null) {
				const base = new Date(now);
				return compose(String(base.getFullYear()), String(base.getMonth() + 1), String(base.getDate()), clock[1], clock[2]);
			}
		}
		/** Compose validated fields into epoch ms; rejects impossible dates (02-30). */
		function compose(year, month, day, hour, minute) {
			const y = Number(year);
			const mo = Number(month);
			const d = Number(day);
			const h = Number(hour);
			const mi = Number(minute);
			if (!Number.isInteger(y) || !Number.isInteger(mo) || !Number.isInteger(d) || !Number.isInteger(h) || !Number.isInteger(mi)) return void 0;
			if (mo < 1 || mo > 12 || d < 1 || d > 31 || h < 0 || h > 23 || mi < 0 || mi > 59) return void 0;
			const date = new Date(y, mo - 1, d, h, mi, 0, 0);
			if (date.getFullYear() !== y || date.getMonth() !== mo - 1 || date.getDate() !== d) return void 0;
			return date.getTime();
		}
		//#endregion
		//#region src/client/board/TimeField.tsx
		/**
		* TimeField: a date-time field that reads like a plain text input — focus
		* selects all, type `YYYY-MM-DD HH:mm` (or the short forms `MM-DD HH:mm` /
		* `HH:mm`) and press Enter or blur; unparseable text flags inline and is
		* never swallowed. The calendar button opens the native date picker
		* (`showPicker`) and merges its chosen date with the field's current time
		* (09:00 when empty). Value is epoch ms; undefined = empty. Used by the
		* cruise window editor (start / optional end).
		*/
		function TimeField({ label, hint, placeholder, value, onChange, allowEmpty = true }) {
			const [text, setText] = (0, react.useState)(value !== void 0 ? formatTimeInput(value) : "");
			const [error, setError] = (0, react.useState)(false);
			const dateRef = (0, react.useRef)(null);
			(0, react.useEffect)(() => {
				setText(value !== void 0 ? formatTimeInput(value) : "");
				setError(false);
			}, [value]);
			const commit = () => {
				const parsed = parseTimeText(text);
				if (parsed === void 0) {
					if (text.trim() === "" && allowEmpty) {
						onChange(void 0);
						setError(false);
					} else setError(true);
					return;
				}
				setError(false);
				onChange(parsed);
			};
			/** Open the native date picker through the invisible date input. */
			const pickDate = () => {
				const input = dateRef.current;
				if (input === null) return;
				try {
					input.showPicker();
				} catch {}
			};
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
				className: board_module_css_default.timeField,
				children: [
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.timeFieldLabel,
						children: label
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
						className: board_module_css_default.timeFieldRow,
						children: [
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("input", {
								className: `${board_module_css_default.input}${error ? ` ${board_module_css_default.inputInvalid}` : ""}`,
								value: text,
								placeholder: placeholder ?? t("board.cruiseTimePlaceholder"),
								"aria-label": label,
								onFocus: (event) => {
									event.target.select();
								},
								onChange: (event) => {
									setText(event.target.value);
									setError(false);
								},
								onBlur: commit,
								onKeyDown: (event) => {
									if (event.key === "Enter") {
										event.preventDefault();
										commit();
										event.currentTarget.blur();
									}
								}
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
								type: "button",
								className: board_module_css_default.timeFieldCalendar,
								title: t("board.cruiseCalendar"),
								"aria-label": t("board.cruiseCalendar"),
								onClick: pickDate,
								children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, { name: "calendar" })
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("input", {
								ref: dateRef,
								type: "datetime-local",
								tabIndex: -1,
								"aria-hidden": "true",
								className: board_module_css_default.timeFieldHiddenDate,
								onChange: (event) => {
									const value = event.target.value;
									if (value === "") return;
									const [datePart, clock] = value.split("T");
									const [year, month, day] = datePart.split("-").map(Number);
									const [hour, minute] = clock.split(":").map(Number);
									const picked = new Date(year, month - 1, day, hour, minute);
									onChange(picked.getTime());
									setText(formatTimeInput(picked.getTime()));
									setError(false);
								}
							})
						]
					}),
					error ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.timeFieldError,
						children: t("board.cruiseTimeInvalid")
					}) : hint !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: board_module_css_default.timeFieldHint,
						children: hint
					})
				]
			});
		}
		//#endregion
		//#region src/core/update-check.ts
		/**
		* Parse the numeric `x.y.z` prefix of a version string. Missing segments
		* default to 0; pre-release/build suffixes are ignored (a suffix never makes
		* a version newer — the numeric triple decides).
		* @param version - the version string.
		* @returns the `[major, minor, patch]` triple.
		*/
		function parseVersionParts(version) {
			const match = version.trim().match(/^(\d+)(?:\.(\d+))?(?:\.(\d+))?/);
			if (match === null) return [
				0,
				0,
				0
			];
			const num = (segment) => segment === void 0 ? 0 : Number(segment);
			return [
				num(match[1]),
				num(match[2]),
				num(match[3])
			];
		}
		/**
		* Compare two version strings by their numeric triples.
		* @param a - first version.
		* @param b - second version.
		* @returns -1 when a < b, 0 when equal, 1 when a > b.
		*/
		function compareVersions(a, b) {
			const pa = parseVersionParts(a);
			const pb = parseVersionParts(b);
			const pairs = [
				[pa[0], pb[0]],
				[pa[1], pb[1]],
				[pa[2], pb[2]]
			];
			for (const [x, y] of pairs) {
				if (x < y) return -1;
				if (x > y) return 1;
			}
			return 0;
		}
		/**
		* Whether `latest` is strictly newer than `current`.
		* @param latest - the published latest version.
		* @param current - the running copy's version.
		* @returns true only when latest is newer (equal or older reads as up to date).
		*/
		function isNewerVersion(latest, current) {
			return compareVersions(latest, current) > 0;
		}
		/**
		* Build the update commands for one install mode. Local checkouts pull and
		* rebuild in place; source installs re-add the GitHub spec; npm installs
		* re-add the package at latest; an unknown mode shows both remote commands
		* rather than guessing.
		* @param mode - the classified install mode.
		* @param packageName - the package name (the npm command's subject).
		* @param githubSpec - the `github:` spec (absent when the repo URL is unknown).
		* @returns the copyable actions in display order.
		*/
		function updateActionsFor(mode, packageName, githubSpec) {
			const npm = {
				kind: "npm",
				command: `dsh plugin --profile web add ${packageName}@latest`
			};
			if (mode === "local") return [{
				kind: "local",
				command: "git pull && pnpm build"
			}];
			if (mode === "github") return githubSpec !== void 0 ? [{
				kind: "github",
				command: `dsh plugin --profile web add ${githubSpec}`
			}] : [npm];
			if (mode === "npm") return [npm];
			return githubSpec !== void 0 ? [npm, {
				kind: "github",
				command: `dsh plugin --profile web add ${githubSpec}`
			}] : [npm];
		}
		/**
		* Whether the local checkout trails the remote (unpublished commits exist).
		* A missing remote reading is never evidence — it reads as "not behind".
		* @param git - the checkout status.
		* @returns true only when both SHAs are known and differ.
		*/
		function isGitBehind(git) {
			return git.remoteHead !== void 0 && git.remoteHead !== "" && git.head !== "" && git.remoteHead !== git.head;
		}
		/**
		* Shorten a full SHA for display (the full value still travels the wire).
		* @param sha - the full SHA.
		* @returns the first 7 characters (or the input when shorter).
		*/
		function shortSha(sha) {
			return sha.length <= 7 ? sha : sha.slice(0, 7);
		}
		//#endregion
		//#region package.json
		var name = "@firetruck666/dsh-task-board";
		var version = "0.2.116";
		//#endregion
		//#region src/client/update-source.ts
		/** The bundle's own package identity (the host-unreachable fallback). */
		const BUNDLED_PACKAGE_NAME = name ?? "dsh-task-board";
		/** The bundle's own version (the host-unreachable comparison baseline). */
		const BUNDLED_VERSION = version ?? "unknown";
		/** Fetch one JSON document with a hard bound; any failure reads as undefined. */
		async function boundedJson(url, timeoutMs) {
			const ctrl = new AbortController();
			const timer = setTimeout(() => {
				ctrl.abort();
			}, timeoutMs);
			try {
				const response = await fetch(url, {
					headers: { accept: "application/json" },
					signal: ctrl.signal
				});
				if (!response.ok) return void 0;
				return await response.json();
			} catch {
				return;
			} finally {
				clearTimeout(timer);
			}
		}
		/** Whether a string is one of the four install modes. */
		function isMode(value) {
			return value === "npm" || value === "github" || value === "local" || value === "unknown";
		}
		/**
		* Read the installed source through the host route. An old host without the
		* route answers 404 (or hangs), which degrades to undefined like any other
		* failure — the button then compares the bundle version against npm with an
		* unknown install mode.
		* @param timeoutMs - per-request bound (default 15s, the transport's budget).
		* @returns the source view, or undefined when unreadable.
		*/
		async function fetchUpdateSource(timeoutMs = 15e3) {
			const body = await boundedJson("/api/dsh-task-board/update", timeoutMs);
			if (typeof body !== "object" || body === null) return void 0;
			const envelope = body;
			if (envelope.ok !== true || typeof envelope.value !== "object" || envelope.value === null) return;
			const value = envelope.value;
			if (typeof value.packageName !== "string" || typeof value.version !== "string" || !isMode(value.mode)) return;
			const git = value.git;
			return {
				packageName: value.packageName,
				version: value.version,
				...typeof value.spec === "string" ? { spec: value.spec } : {},
				mode: value.mode,
				...typeof value.githubSpec === "string" ? { githubSpec: value.githubSpec } : {},
				...git !== void 0 && typeof git === "object" && git !== null && typeof git.head === "string" && typeof git.dirty === "boolean" ? { git: {
					head: git.head,
					...typeof git.remoteHead === "string" ? { remoteHead: git.remoteHead } : {},
					dirty: git.dirty
				} } : {}
			};
		}
		/**
		* Read the published latest from the public npm registry packument
		* (`dist-tags.latest` plus its publish instant from `time`).
		* @param packageName - the scoped package name (slash-encoded for the URL).
		* @param timeoutMs - per-request bound (default 15s).
		* @returns the latest version, or undefined when unreadable.
		*/
		async function fetchNpmLatest(packageName, timeoutMs = 15e3) {
			const body = await boundedJson(`https://registry.npmjs.org/${packageName.replace("/", "%2F")}`, timeoutMs);
			if (typeof body !== "object" || body === null) return void 0;
			const packument = body;
			const latest = packument["dist-tags"]?.latest;
			if (typeof latest !== "string" || latest === "") return void 0;
			const published = packument.time?.[latest];
			return {
				version: latest,
				...typeof published === "string" ? { publishedAt: published } : {}
			};
		}
		//#endregion
		//#region src/client/board/task-search.ts
		/**
		* Board-wide task search: one input, every surface a task owns. A task
		* matches when EVERY whitespace-separated term appears ANYWHERE in its
		* title, description, execution prompt, comment bodies or linked-session
		* titles (case-insensitive AND — narrowing as you type, never a surprise
		* OR flood). Empty query matches all (the filter is a sieve, not a gate).
		*
		* Pure and framework-free so the matcher unit-tests in isolation; TaskBoard
		* supplies the linked titles from its existing resolvers (no new data
		* plumbing — the haystack is assembled at the call site).
		*/
		/** All searchable text of one task (the caller appends session titles). */
		function taskHaystack(task, sessionTitles = []) {
			const comments = task.executions.map((round) => round.comment ?? "").filter((text) => text !== "");
			return [
				task.title,
				task.description,
				task.prompt,
				...comments,
				...sessionTitles
			].join("\n");
		}
		/** Whether a task matches a raw query string (blank query = match). */
		function matchTask(task, query, sessionTitles = [], facets = {}) {
			const { terms, qualifiers } = parseBoardQuery(query);
			if (terms.length > 0) {
				const haystack = taskHaystack(task, sessionTitles).toLowerCase();
				if (!terms.every((term) => haystack.includes(term))) return false;
			}
			return qualifiers.every((qualifier) => matchQualifier(task, qualifier, facets));
		}
		/**
		* Split a raw query into plain terms plus recognized qualifiers (see
		* QUALIFIER_KEYS/QUALIFIER_VALUES for the key set and enumerated values).
		* Matching is case-insensitive; `has:` accepts `auto`, `is:` accepts
		* `unread`/`read`, `ws:` takes any text —
		* quoted (`ws:"a b"`) when the name holds a space, first word otherwise.
		* Anything unrecognized stays a literal search term: an unknown qualifier
		* narrows like ordinary text instead of failing.
		*/
		function parseBoardQuery(query) {
			const terms = [];
			const qualifiers = [];
			for (const token of splitFilterTokens(query)) {
				const quoted = /^ws:"([^"]*)"$/i.exec(token);
				if (quoted !== null) {
					const quotedValue = quoted[1] ?? "";
					if (quotedValue.trim() !== "") qualifiers.push({
						key: "ws",
						value: quotedValue.trim().toLowerCase()
					});
					else terms.push(token.toLowerCase());
					continue;
				}
				const raw = token.toLowerCase();
				const separator = raw.indexOf(":");
				if (separator > 0) {
					const key = raw.slice(0, separator);
					const value = raw.slice(separator + 1);
					if (!value.includes("\"") && value !== "" && QUALIFIER_KEY_SET.has(key)) {
						if (ENUM_VALUE_SET.has(`${key}:${value}`) || FREE_TEXT_KEYS.has(key)) {
							qualifiers.push({
								key,
								value
							});
							continue;
						}
					}
				}
				terms.push(raw);
			}
			return {
				terms,
				qualifiers
			};
		}
		/** Whether one qualifier holds (unknown facets read absent = no match).
		*  The predicate reads THE registry above — no second table, so matching
		*  can never drift from parsing: whatever the parser accepts, this resolves
		*  through the same definition. */
		function matchQualifier(task, qualifier, facets) {
			const full = `${qualifier.key}:${qualifier.value}`;
			if (ENUM_VALUE_SET.has(full)) return QUALIFIER_DEFS[full]?.test(task, facets, qualifier.value) === true;
			if (FREE_TEXT_KEYS.has(qualifier.key)) return QUALIFIER_DEFS[`${qualifier.key}:`]?.test(task, facets, qualifier.value) === true;
			return false;
		}
		const QUALIFIER_DEFS = {
			"has:auto": { test: (_task, facets) => facets.hasAutomation === true },
			"has:color": { test: (task) => task.color !== void 0 },
			"is:unread": { test: (_task, facets) => facets.isUnviewed === true },
			"is:read": { test: (_task, facets) => facets.isUnviewed === false },
			"ws:": {
				test: (_task, facets, value) => (facets.workspaceTitle ?? "").toLowerCase().includes(value),
				freeText: true
			}
		};
		/** Qualifier keys the filter understands — derived from the registry. */
		const QUALIFIER_KEYS = [...new Set(Object.keys(QUALIFIER_DEFS).map((full) => `${full.slice(0, full.indexOf(":"))}:`))];
		/** Enumerated qualifier values by key — derived from the registry. */
		const QUALIFIER_VALUES = Object.fromEntries(QUALIFIER_KEYS.map((key) => [key, Object.keys(QUALIFIER_DEFS).filter((full) => full.startsWith(key) && QUALIFIER_DEFS[full]?.freeText !== true)]).filter(([, values]) => values.length > 0));
		/** All enumerated values (every `key:value` the completion can offer). */
		const ENUMERATED_VALUES = Object.values(QUALIFIER_VALUES).flat();
		/** Parser key set, derived (no second hardcode: a key added to the registry
		*  parses, hints and completes at once). */
		const QUALIFIER_KEY_SET = new Set(QUALIFIER_KEYS.map((key) => key.slice(0, -1)));
		/** Enumerated `key:value` set, derived (the parser accepts exactly these). */
		const ENUM_VALUE_SET = new Set(ENUMERATED_VALUES);
		/** Free-text keys, derived (registry entries flagged freeText). */
		const FREE_TEXT_KEYS = new Set(Object.keys(QUALIFIER_DEFS).filter((full) => QUALIFIER_DEFS[full]?.freeText === true).map((full) => full.slice(0, -1)));
		/** Completion candidates for the token being typed (at most 8, key-first):
		*  a key prefix offers keys (`h` → `has:`), a bare key offers its values
		*  (`has:` → `has:auto`), a value prefix narrows them (`has:a` → `has:auto`).
		*  Free-text keys (`ws:`) and complete tokens offer nothing — the
		*  native datalist narrows the offered set further as typing continues.
		*  The token reads THE single scanner (a `ws:"..."` span is one token —
		*  completion never fires from inside quotes). */
		function completeBoardQuery(query) {
			const token = splitFilterTokens(query).pop()?.toLowerCase() ?? "";
			if (token === "" || token.includes("\"")) return [];
			const separator = token.indexOf(":");
			if (separator < 0) return QUALIFIER_KEYS.filter((key) => key.startsWith(token)).slice(0, 8);
			const key = `${token.slice(0, separator)}:`;
			const values = QUALIFIER_VALUES[key];
			if (values === void 0) return [];
			const prefix = token.slice(separator + 1);
			if (ENUMERATED_VALUES.includes(token)) return [];
			return values.filter((value) => value.startsWith(`${key}${prefix}`)).slice(0, 8);
		}
		/** Assemble a full query from a completion candidate: the last token is
		*  replaced in place (earlier tokens, their case and every separator survive
		*  — a native datalist swaps the WHOLE value, so candidates must arrive as
		*  whole queries, never bare tokens). A trailing separator means a fresh
		*  empty token: the candidate appends instead of replacing. The boundary
		*  reads THE single scanner (no trailing separator ⇒ the last token is the
		*  string's suffix — a `ws:"..."` span can never be torn, even if a caller
		*  passes a candidate for a quoted context). */
		function applyCompletion(query, candidate) {
			if (query === "" || /\s$/.test(query)) return `${query}${candidate}`;
			const tokens = splitFilterTokens(query);
			const last = tokens[tokens.length - 1];
			if (last === void 0) return candidate;
			return `${query.slice(0, query.length - last.length)}${candidate}`;
		}
		/** Split a filter query into removable tokens — THE one scanner both the
		*  parser and the overview chips read, so chips remove exactly what the
		*  parser sees. Only `ws:"..."` spans stay atomic (the one quoted form);
		*  every other quote splits normally (unclosed/non-ws quotes are literal
		*  text to the parser, so the chips must show them split too). */
		function splitFilterTokens(query) {
			const tokens = [];
			const flush = (text) => {
				for (const part of text.split(/\s+/)) if (part !== "") tokens.push(part);
			};
			const pattern = /(^|\s)(ws:"[^"]*")/gi;
			let last = 0;
			let match;
			pattern.lastIndex = 0;
			while ((match = pattern.exec(query)) !== null) {
				flush(query.slice(last, match.index));
				tokens.push(match[2] ?? match[0]);
				last = match.index + match[0].length;
			}
			flush(query.slice(last));
			return tokens;
		}
		/** Remove the token at `index` (single-token removal for the overview
		*  chips — Clear-all is just `setFilter('')`, never this). Out-of-range
		*  indexes return the query unchanged. */
		function removeFilterToken(query, index) {
			const tokens = splitFilterTokens(query);
			if (index < 0 || index >= tokens.length) return query;
			tokens.splice(index, 1);
			return tokens.join(" ");
		}
		//#endregion
		//#region src/client/board/notifications.ts
		/** THE row key (`task|session|kind`): snooze stamps, drawer keys and the
		*  unseen set all derive from this one constructor — never a retyped
		*  template, so the three can never disagree on identity. */
		function noteKeyOf(note) {
			return `${note.taskId}|${note.sessionId}|${note.kind}`;
		}
		/** Metadata-orthogonal row order: lexicographic key comparison for sort
		*  tiebreaks (stable across metadata writes, drags and reorders). */
		function compareNoteKey(a, b) {
			const left = noteKeyOf(a);
			const right = noteKeyOf(b);
			return left < right ? -1 : left > right ? 1 : 0;
		}
		/**
		* Full three-tier view: waiting sessions first (newest task first), then
		* unviewed review tasks (failed before succeeded, newest settle first).
		* `isUnviewed` decides the review tier (the board passes `taskUnviewed`);
		* absent/false = waiting-only (legacy behavior). `linkedIdsOf` supplies live
		* linked-session ids per task so a bound-but-never-run waiting session still
		* notifies (same related set as the live state).
		*/
		function notificationsExOf(tasks, pendingOf, titleOf, isUnviewed = taskUnviewed, linkedIdsOf = () => []) {
			const waiting = [];
			const seen = /* @__PURE__ */ new Set();
			const push = (task, sessionId) => {
				if (sessionId === void 0) return;
				const waitingKind = pendingOf(sessionId);
				if (waitingKind === void 0) return;
				const key = `${task.id}|${sessionId}`;
				if (seen.has(key)) return;
				seen.add(key);
				const round = task.executions.find((candidate) => candidate.sessionId === sessionId);
				waiting.push({
					taskId: task.id,
					taskTitle: task.title,
					sessionId,
					sessionTitle: titleOf(sessionId),
					waitingKind,
					kind: "waiting",
					at: round !== void 0 ? round.endedAt ?? round.startedAt : task.updatedAt
				});
			};
			for (const task of tasks) for (const { sessionId } of relatedSessionIdsOf(task, linkedIdsOf(task))) push(task, sessionId);
			waiting.sort((a, b) => b.at - a.at || compareNoteKey(a, b));
			const review = [];
			for (const task of tasks) {
				if (task.status !== "review" || !isUnviewed(task)) continue;
				const runs = plainRunsOf(task);
				const latest = runs[runs.length - 1];
				const sessionId = latest?.sessionId ?? task.executions[task.executions.length - 1]?.sessionId ?? task.id;
				if ([...seen].some((key) => key.startsWith(`${task.id}|`))) continue;
				const result = lastPlainResult(task);
				review.push({
					taskId: task.id,
					taskTitle: task.title,
					sessionId,
					sessionTitle: titleOf(sessionId),
					kind: "review",
					...result !== void 0 ? { result } : {},
					at: latest?.endedAt ?? task.updatedAt
				});
			}
			review.sort((a, b) => {
				const rank = (result) => result === "failed" ? 0 : result === "succeeded" ? 1 : 2;
				const rankDiff = rank(a.result) - rank(b.result);
				if (rankDiff !== 0) return rankDiff;
				if (b.at !== a.at) return b.at - a.at;
				return compareNoteKey(a, b);
			});
			return [...waiting, ...review];
		}
		function boardDemandOf(tasks, pendingOf, linkedIdsOf = () => []) {
			let waiting = 0;
			let review = 0;
			for (const task of tasks) {
				const counted = /* @__PURE__ */ new Set();
				for (const { sessionId } of relatedSessionIdsOf(task, linkedIdsOf(task))) {
					if (sessionId === void 0 || counted.has(sessionId)) continue;
					if (pendingOf(sessionId) === void 0) continue;
					counted.add(sessionId);
					waiting += 1;
				}
				if (task.status === "review" && lastPlainResult(task) !== void 0) review += 1;
			}
			return {
				total: waiting + review,
				waiting,
				review
			};
		}
		/**
		* Fold notification rows by task (same-task rows share one head — the first,
		* which the waiting-first ordering already ranked loudest). Pure view-layer
		* grouping: `notificationsExOf` keeps its signature so existing callers and
		* tests never change; the bell badge and the drawer list fold the same way.
		*/
		function foldNotesByTask(notes) {
			const folded = [];
			const index = /* @__PURE__ */ new Map();
			for (const note of notes) {
				const existing = index.get(note.taskId);
				if (existing !== void 0) {
					existing.items.push(note);
					existing.count += 1;
					continue;
				}
				const entry = {
					head: note,
					items: [note],
					count: 1
				};
				index.set(note.taskId, entry);
				folded.push(entry);
			}
			return folded;
		}
		//#endregion
		//#region src/client/board/batch-run.ts
		/**
		* Batch run (the organize bar's apex action): which selected cards a
		* "批量执行" tap actually fires. The filter is deliberately thin — only the
		* execution gate (`taskExecutable`: a non-blank prompt) belongs here. Lane
		* business, concurrency budget and revive rules stay inside `runTask` (the
		* single launch point): a card that cannot start right now simply stays put,
		* visibly, instead of failing silently somewhere in the bar.
		*
		* Pure and framework-free so the selection rule unit-tests in isolation.
		*/
		/** The selected cards worth firing, in board order (stable, meaningful). */
		function runnableIds(tasks, selectedIds) {
			const selected = new Set(selectedIds);
			return tasks.filter((task) => selected.has(task.id) && taskExecutable(task)).map((task) => task.id);
		}
		/** The cluster partition, ONE table: `clusterOf` derives from it and the
		*  Dialog's filter map reads it, so a new row kind changes exactly one place. */
		const CLUSTER_KINDS = {
			run: ["started", "settled"],
			comment: [
				"comment",
				"queued",
				"running"
			],
			other: [
				"created",
				"refined",
				"direct",
				"external"
			]
		};
		/** Map one row kind onto its fold cluster (derived from the single table). */
		function clusterOf(kind) {
			for (const cluster of Object.keys(CLUSTER_KINDS)) if (CLUSTER_KINDS[cluster].includes(kind)) return cluster;
			return "other";
		}
		/** THE group-key constructor (`task|day|cluster`): grouping, expansion state
		*  and remainder keys all derive from this one function — never a retyped
		*  template — so folded identity can never disagree with itself. */
		function activityGroupKeyOf(taskId, day, cluster) {
			return `${taskId}|${day}|${cluster}`;
		}
		/** THE remainder-row key for a folded group: `groupKey + '|rest'` — one
		*  constructor beside the group key, so the two can never drift apart
		*  (notification folds reuse it with their task-scoped key). */
		function remainderKeyOf(groupKey) {
			return `${groupKey}|rest`;
		}
		/**
		* Fold feed rows into object-day groups (GetStream-style aggregation keyed on
		* object × day, with the filter cluster as the third leg). Single-item groups
		* render exactly like unfolded rows, so sparse feeds look byte-identical to
		* before — only genuinely busy object-days gain a header. Pure: no truncation
		* here (caps stay at the feed/window layer), no unread marking (feed rows
		* carry no unread signal by contract — unread breathes on the card only).
		*/
		function groupActivityByObjectDay(items, dayOf) {
			const groups = [];
			const index = /* @__PURE__ */ new Map();
			for (const item of items) {
				const day = dayOf(item.at);
				const cluster = clusterOf(item.kind);
				const key = activityGroupKeyOf(item.taskId, day, cluster);
				const existing = index.get(key);
				if (existing !== void 0) {
					existing.items.push(item);
					continue;
				}
				const group = {
					key,
					taskId: item.taskId,
					taskTitle: item.taskTitle,
					day,
					cluster,
					items: [item]
				};
				index.set(key, group);
				groups.push(group);
			}
			return groups;
		}
		/**
		* Split a live newest-first feed into the frozen window and the queued
		* remainder: the view shows the oldest `base` rows (what was on screen when
		* the drawer opened or the pill was last tapped); newer arrivals queue behind
		* the pill instead of shoving the rows being read. A negative base means
		* "unfrozen" (first frame, filter mid-typing): the whole live list shows
		* with zero queued, so the freeze engaging a beat later changes nothing
		* visible. Shrinking feeds clamp to the whole list. Pure.
		*/
		function freezeFeed(live, base) {
			if (base < 0) return {
				frozen: [...live],
				fresh: 0
			};
			const fresh = Math.max(0, live.length - base);
			return {
				frozen: live.slice(Math.max(0, live.length - base)),
				fresh
			};
		}
		/**
		* Split a group's rows into the shown head and the folded remainder count.
		* Generic over the row shape (feed rows and notification rows share the cap
		* discipline, never a second copy): the header always counts the full group
		* (the count never lies about the cap). The limit normalizes defensively
		* (non-finite → the default cap, negatives → 0, fractions → floor): an open
		* generic must never lie about counts no matter who calls it next. Pure so
		* the cap unit-tests without rendering.
		*/
		function splitGroupItems(items, limit = 10) {
			const capped = Number.isFinite(limit) ? Math.max(0, Math.floor(limit)) : 10;
			if (items.length <= capped) return {
				shown: [...items],
				rest: 0
			};
			return {
				shown: items.slice(0, capped),
				rest: items.length - capped
			};
		}
		/**
		* Collect every notable moment of every task, newest first. Running rounds
		* ARE moments (their start/observation/injection lights the feed); an empty
		* comment body stays (the row shows a placeholder) — the view decides
		* trimming, never the derivation.
		*/
		function activityOf(tasks, filter = {}, isUnviewed) {
			const events = boardEventsOf(tasks);
			const items = [];
			for (const event of events) {
				if (event.kind === "waiting") continue;
				const mapped = toActivityItem(event);
				if (mapped === void 0) continue;
				if (filter.onlyUnviewed === true && isUnviewed !== void 0) {
					const task = tasks.find((candidate) => candidate.id === event.taskId);
					if (task === void 0 || !isUnviewed(task, event.at)) continue;
				}
				if (filter.kinds !== void 0 && filter.kinds.length > 0 && !filter.kinds.includes(mapped.kind)) continue;
				if (filter.query !== void 0 && filter.query.trim() !== "") {
					const query = filter.query.trim().toLowerCase();
					if (!`${mapped.taskTitle} ${mapped.text ?? ""}`.toLowerCase().includes(query)) continue;
				}
				items.push(mapped);
			}
			return items.slice(0, 50);
		}
		/** Map one shared event onto the feed's row grammar. */
		function toActivityItem(event) {
			const base = {
				key: event.key,
				taskId: event.taskId,
				taskTitle: event.taskTitle,
				at: event.at,
				state: event.state,
				...event.result !== void 0 ? { result: event.result } : {},
				...event.text !== void 0 ? { text: event.text } : {},
				...event.sessionId !== void 0 ? { sessionId: event.sessionId } : {}
			};
			switch (event.kind) {
				case "created": return {
					...base,
					kind: "created"
				};
				case "run": return {
					...base,
					kind: event.state === "settled" ? "settled" : "started"
				};
				case "comment":
					if (event.state === "queued") return {
						...base,
						kind: "queued"
					};
					if (event.state === "running") return {
						...base,
						kind: "running"
					};
					return {
						...base,
						kind: "comment"
					};
				case "external": return {
					...base,
					kind: "external"
				};
				case "direct": return {
					...base,
					kind: "direct"
				};
				case "refined": return {
					...base,
					kind: "refined"
				};
				default: return;
			}
		}
		//#endregion
		//#region src/core/flow-metrics.ts
		/** Trailing window for throughput (28 days = 4 weeks). */
		const THROUGHPUT_WINDOW_MS = 28 * 864e5;
		/** Elapsed cycle days for one task: the FIRST trip (first running → first
		*  done after it). Rework (running→done→running→done) measures once — the
		*  first delivery, never the rework tail (stated here, never re-debated per
		*  call). Elapsed calendar days, review included. Undefined when the trip is
		*  incomplete, unrecorded (legacy rows), or reaches into the future (clock
		*  skew is data dirt — the same law as throughput). */
		function cycleDaysOf(task, now = Date.now()) {
			const history = task.statusHistory;
			if (history === void 0) return void 0;
			const effective = history.filter((entry) => entry.at <= now);
			const start = effective.find((entry) => entry.status === "running");
			if (start === void 0) return void 0;
			const end = effective.find((entry) => entry.status === "done" && entry.at >= start.at);
			if (end === void 0) return void 0;
			return Math.max(0, (end.at - start.at) / 864e5);
		}
		/** The p-th percentile of an ascending-sorted sample (nearest-rank);
		*  undefined on empty input (no sample, no number — never zero-fill). */
		function percentileOf(sorted, p) {
			if (sorted.length === 0) return void 0;
			return sorted[Math.min(sorted.length - 1, Math.max(0, Math.ceil(p / 100 * sorted.length) - 1))];
		}
		/** Done task count per week over the trailing window. The atom is the
		*  COMPLETION EVENT (a `done` entry in the ledger), never the current
		*  column: a task revived for a follow-up comment keeps the completion it
		*  already earned (columns aggregate sessions, history records events). Count
		*  caliber is PER TASK (at most one per window, by its last completion) —
		*  rework within the window does not double-count; a double delivery reads
		*  as one steady completion, never two. Future instants never count (clock
		*  skew is data dirt, not throughput). */
		function throughputPerWeek(tasks, now = Date.now()) {
			const floor = now - THROUGHPUT_WINDOW_MS;
			let count = 0;
			for (const task of tasks) {
				const history = task.statusHistory;
				if (history === void 0) continue;
				const doneAt = history.filter((entry) => entry.status === "done" && entry.at <= now).map((entry) => entry.at).pop();
				if (doneAt !== void 0 && doneAt >= floor) count++;
			}
			return count / 4;
		}
		/** The board's flow summary: samples (measurable first trips), the p85
		*  cycle in days, and weekly throughput. The two halves gate INDEPENDENTLY:
		*  direct-to-done completions (no running leg) count for throughput but can
		*  never form a cycle — throughput without a cycle still shows, a cycle
		*  without recent completions still shows, zero of both stays quiet (never
		*  a pseudo-number before real history exists). */
		function flowSummaryOf(tasks, now = Date.now()) {
			const cycles = tasks.map((task) => ({
				task,
				days: cycleDaysOf(task, now)
			})).filter((entry) => entry.days !== void 0).map((entry) => entry.days).sort((a, b) => a - b);
			const p85 = percentileOf(cycles, 85);
			return {
				samples: cycles.length,
				...p85 !== void 0 ? { p85Days: p85 } : {},
				perWeek: throughputPerWeek(tasks, now)
			};
		}
		//#endregion
		//#region src/client/bundle-freshness.ts
		/**
		* Decide freshness from the two version strings.
		* An unknown or empty host reading is never evidence — it reads as `unknown`
		* so a broken route can never trigger a reload loop or a false warning.
		* @param bundled - the version baked into the running bundle.
		* @param host - the version the host reports, when readable.
		* @returns the freshness decision.
		*/
		function freshnessOf(bundled, host) {
			if (host === void 0 || host === "") return "unknown";
			if (bundled === "" || bundled === "unknown") return "unknown";
			return host === bundled ? "current" : "stale";
		}
		/** The default guard prefix (one per plugin, never shared with other keys). */
		const DEFAULT_GUARD_PREFIX = "dsh.taskBoard.bundleReload.v1";
		/**
		* The ONE way to re-enter the boot graph: navigate to the same document with a
		* cache-busting query parameter. The boot document carries the artifact
		* revision the browser then requests, so re-fetching the document (not merely
		* reloading a cached one) is what actually picks up a new bundle. The extra
		* parameter is inert for the shell's own auth (it reads `token`, and an
		* already-authenticated request is served straight from the cookie).
		*/
		function reloadForFreshBundle() {
			const url = new URL(window.location.href);
			url.searchParams.set("_", String(Date.now()));
			window.location.replace(url.toString());
		}
		/**
		* The freshness state: probe once at boot, hold the verdict, reload at most
		* once per (bundled, host) pair, and let the board subscribe for display.
		*/
		var BundleFreshnessState = class {
			state = "unknown";
			host;
			retried = false;
			listeners = /* @__PURE__ */ new Set();
			deps;
			constructor(deps) {
				this.deps = deps;
			}
			/** The current view (a fresh object every call — safe for React state). */
			snapshot() {
				return {
					state: this.state,
					bundled: this.deps.bundled,
					...this.host !== void 0 ? { host: this.host } : {},
					retried: this.retried
				};
			}
			/** Subscribe to view changes; returns the disposer. */
			subscribe(listener) {
				this.listeners.add(listener);
				return () => {
					this.listeners.delete(listener);
				};
			}
			/** The guard key for one pair (a new host version is allowed one retry). */
			guardKey() {
				return `${this.deps.guardPrefix ?? DEFAULT_GUARD_PREFIX}:${this.deps.bundled}->${this.host ?? ""}`;
			}
			/** Whether this pair already consumed its one reload. */
			guarded() {
				const storage = this.deps.storage;
				if (storage === void 0) return false;
				try {
					return storage.getItem(this.guardKey()) === "1";
				} catch {
					return false;
				}
			}
			/** Mark the pair as retried (best effort — the display never depends on it). */
			markGuarded() {
				const storage = this.deps.storage;
				if (storage === void 0) return;
				try {
					storage.setItem(this.guardKey(), "1");
				} catch {}
			}
			/**
			* Read the host's version once and act on the verdict. Never throws: an
			* unreadable route leaves the state `unknown` (the board then says nothing).
			* @returns the resulting view.
			*/
			async probe() {
				let host;
				try {
					host = await this.deps.readHostVersion();
				} catch {
					host = void 0;
				}
				this.host = host;
				this.state = freshnessOf(this.deps.bundled, host);
				this.retried = this.state === "stale" && this.guarded();
				this.emit();
				if (this.state === "stale" && !this.retried) {
					this.markGuarded();
					this.retried = true;
					this.emit();
					try {
						this.deps.reload();
					} catch {}
				}
				return this.snapshot();
			}
			/**
			* Keep watching after the boot probe: re-check whenever the page returns to
			* the foreground and on a slow interval. This is what makes a host restart
			* reach a page that is ALREADY open — without it the single boot-time probe
			* runs once and an open document never learns the server moved on, which is
			* exactly the "I restarted everything and the page still looks old" loop.
			* The one-reload-per-pair guard makes repeated probes harmless.
			* @param intervalMs - slow re-probe period (default 60s).
			* @returns the disposer removing both triggers.
			*/
			watch(intervalMs = 6e4) {
				const doc = globalThis.document;
				const probeIfVisible = () => {
					if (doc !== void 0 && doc.visibilityState === "hidden") return;
					this.probe();
				};
				doc?.addEventListener("visibilitychange", probeIfVisible);
				const timer = globalThis.setInterval(probeIfVisible, intervalMs);
				return () => {
					doc?.removeEventListener("visibilitychange", probeIfVisible);
					globalThis.clearInterval(timer);
				};
			}
			/** Notify subscribers (one tick, never re-entrant work). */
			emit() {
				for (const listener of [...this.listeners]) try {
					listener();
				} catch (error) {
					console.error("[dsh-task-board] freshness listener failed", error);
				}
			}
		};
		//#endregion
		//#region src/client/client-report.ts
		/** Round to a tenth of a pixel (enough for judgement, small on the wire). */
		function round(value) {
			return Math.round(value * 10) / 10;
		}
		/** One element's box, or undefined when it is absent or has no area. */
		function boxOf(element) {
			if (element === null || element === void 0) return void 0;
			const rect = element.getBoundingClientRect();
			if (rect.width === 0 && rect.height === 0) return void 0;
			return {
				top: round(rect.top),
				bottom: round(rect.bottom),
				height: round(rect.height)
			};
		}
		/**
		* Measure the board's live geometry through the caller's own class names.
		* Reads only what exists: an unmounted board reports no boxes, never throws.
		* @param landing - the scoped class names of the landmarks.
		* @param root - the subtree to search (the document in production).
		* @returns the box map keyed by landmark name.
		*/
		function collectBoardBoxes(landing, root = document) {
			const boxes = {};
			const add = (key, selector) => {
				const box = boxOf(root.querySelector(`.${selector}`));
				if (box !== void 0) boxes[key] = box;
			};
			add("header", landing.header);
			add("modes", landing.modes);
			add("search", landing.search);
			add("strip", landing.strip);
			add("pill", landing.pill);
			add("columns", landing.columns);
			add("firstColumn", landing.firstColumn);
			add("primary", landing.primary);
			add("thumbBar", landing.thumbBar);
			return boxes;
		}
		/**
		* Compose and send one report. Never throws and never blocks the board: the
		* request is fire-and-forget with a short bound.
		* @param version - the bundle version baked into this page.
		* @param landing - the scoped class names of the landmarks.
		* @param fetchImpl - fetch seam (tests).
		* @param root - the subtree to measure (tests).
		* @returns a promise that resolves once the attempt settled (success or not).
		*/
		async function sendClientReport(version, landing, fetchImpl = fetch, root = document) {
			const body = {
				version,
				href: typeof location === "undefined" ? "" : location.href,
				...typeof navigator !== "undefined" && typeof navigator.userAgent === "string" ? { ua: navigator.userAgent } : {},
				...typeof window !== "undefined" ? {
					viewport: {
						width: window.innerWidth,
						height: window.innerHeight
					},
					dpr: window.devicePixelRatio
				} : {},
				boxes: collectBoardBoxes(landing, root)
			};
			const ctrl = new AbortController();
			const timer = setTimeout(() => {
				ctrl.abort();
			}, 5e3);
			try {
				await fetchImpl("/api/dsh-task-board/client-report", {
					method: "POST",
					headers: { "content-type": "application/json" },
					body: JSON.stringify(body),
					signal: ctrl.signal
				});
			} catch {} finally {
				clearTimeout(timer);
			}
		}
		//#endregion
		//#region src/client/board/TaskBoard.tsx
		/**
		* Board view: the multi-column kanban that replaces the middle column while
		* active. Cards open the task detail (never execute directly); the header
		* offers filter, new-task, the auto-cruise toggle, and a back-to-chat escape.
		*
		* Drag & drop: same-column drags reorder cards, cross-column drags keep the
		* classic move/rerun/reject semantics. Reorder uses a half-split anchor —
		* the pointer's Y against each card's midpoint decides whether the drop
		* lands before or after it — computed on the column container (event
		* delegation), so both directions work symmetrically for any column size.
		* The insertion point is mirrored in a ref (written synchronously on every
		* dragover, read at drop) so the drop always matches the preview; only the
		* indicator rendering goes through state.
		*/
		/** The activity feed's chip: success greens, failure reds, everything else quiet. */
		function activityChipOf(item) {
			if (item.kind === "settled") {
				if (item.result === "succeeded") return {
					kind: "success",
					label: t("board.activitySucceeded")
				};
				if (item.result === "failed") return {
					kind: "error",
					label: t("board.activityFailed")
				};
				if (item.result === "cancelled") return {
					kind: "muted",
					label: t("board.activityCancelled")
				};
				return {
					kind: "neutral",
					label: t("board.activitySettled")
				};
			}
			if (item.kind === "comment") return {
				kind: "neutral",
				label: t("board.activityComment")
			};
			if (item.kind === "refined") return {
				kind: "neutral",
				label: t("board.activityRefined")
			};
			if (item.kind === "started") return {
				kind: "neutral",
				label: t("board.activityStarted")
			};
			if (item.kind === "queued") return {
				kind: "neutral",
				label: t("board.activityQueued")
			};
			if (item.kind === "running") return {
				kind: "neutral",
				label: t("board.activityRunning")
			};
			if (item.kind === "direct") return {
				kind: "neutral",
				label: t("board.activityDirect")
			};
			if (item.kind === "external") return {
				kind: "neutral",
				label: t("board.activityExternal")
			};
			return {
				kind: "neutral",
				label: t("board.activityCreated")
			};
		}
		/**
		* The install-mode label the update dialog shows (one branch per mode — the
		* locale dict keys cannot be built dynamically, so the mapping lives here).
		*/
		function updateModeLabel(mode) {
			if (mode === "npm") return t("board.update.mode.npm");
			if (mode === "github") return t("board.update.mode.github");
			if (mode === "local") return t("board.update.mode.local");
			return t("board.update.mode.unknown");
		}
		/** The one-line hint above one copyable update command. */
		function updateHintOf(kind) {
			if (kind === "npm") return t("board.update.hint.npm");
			if (kind === "github") return t("board.update.hint.github");
			return t("board.update.hint.local");
		}
		/**
		* Human day label for an activity group header: 今天 / 昨天, else the
		* MM-DD slice of the YYYY-MM-DD bucket (year omitted — the feed is a
		* glance, and the full instant lives in each row's preview). Pure.
		*/
		function dayLabelOf(day, today) {
			if (day === today) return t("board.dayToday");
			if (day === dayBucketOf(Date.now() - 864e5)) return t("board.dayYesterday");
			return day.slice(5);
		}
		/**
		* The cruise settings in its two forms, one content: the anchored popover
		* beside the header pill on a wide board; the SAME children as a full Dialog
		* on a narrow board (surface bucket, same 680px as the CSS container). An
		* absolutely positioned popover cannot size itself against the board box
		* from inside the header row — on a phone it collapsed to a sliver (the
		* 「面板只剩一条」 report); the Dialog grammar (board-box relative,
		* scrollable body, pinned actions) is the honest form once the board is
		* narrow.
		*/
		function CruiseSettingsHost({ narrow, label, id, onClose, children }) {
			if (!narrow) return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
				className: board_module_css_default.cruisePopover,
				id,
				role: "menu",
				"aria-label": label,
				children
			});
			return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Dialog, {
				title: label,
				label,
				onClose,
				portal: true,
				className: board_module_css_default.autoModal,
				children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
					className: board_module_css_default.modalScroll,
					id,
					children
				})
			});
		}
		/** ONE inline message per window-range issue — explanatory, never jargon:
		*  the actual values and the one-night boundary are named, so a 3-days-early
		*  end says "3 天" and why it cannot be 次日. */
		function cruiseRangeError(issue, window) {
			if (issue === "both-empty") return t("board.cruiseWindowErrorStart");
			if (issue === "same-instant") return t("board.cruiseWindowErrorSame");
			if (issue === "end-too-early") {
				const startAt = window.startAt ?? 0;
				const endAt = window.endAt ?? 0;
				const days = Math.max(1, Math.ceil((startAt - endAt) / DAY_MS));
				return t("board.cruiseWindowErrorRange", {
					end: formatCruiseTime(endAt, startAt),
					start: formatCruiseTime(startAt, startAt),
					days: String(days)
				});
			}
			if (issue === "start-past") return t("board.cruiseWindowErrorStartPast");
			return t("board.cruiseWindowErrorEndPast");
		}
		/** The window row tooltip: the exact instants behind its one-line label. */
		function cruiseWindowTitleOf(window) {
			const grammar = cruiseWindowGrammarOf(window);
			if (grammar.kind === "range") return `${formatDateTime(grammar.startAt)} → ${formatDateTime(grammar.endAt)}`;
			if (grammar.kind === "from-start") return formatDateTime(grammar.startAt);
			return formatDateTime(grammar.endAt);
		}
		/** Board component; subscribes to the controller snapshot. */
		function TaskBoard({ controller, freshness }) {
			const [snapshot, setSnapshot] = (0, react.useState)(controller.getSnapshot());
			(0, react.useEffect)(() => controller.subscribe(() => setSnapshot(controller.getSnapshot())), [controller]);
			const [freshnessView, setFreshnessView] = (0, react.useState)(() => freshness?.snapshot());
			(0, react.useEffect)(() => {
				if (freshness === void 0) return void 0;
				setFreshnessView(freshness.snapshot());
				return freshness.subscribe(() => {
					setFreshnessView(freshness.snapshot());
				});
			}, [freshness]);
			const bundleStale = freshnessView?.state === "stale";
			(0, react.useEffect)(() => {
				if (!snapshot.boardOpen) return;
				sendClientReport(BUNDLED_VERSION, {
					modes: board_module_css_default.boardModes,
					search: board_module_css_default.search,
					strip: board_module_css_default.columnTabs,
					pill: board_module_css_default.columnTab,
					header: board_module_css_default.boardHeader,
					columns: board_module_css_default.columns,
					firstColumn: board_module_css_default.column,
					primary: board_module_css_default.primaryButton,
					thumbBar: board_module_css_default.thumbBar
				});
			}, [snapshot.boardOpen]);
			const [filter, setFilter] = (0, react.useState)("");
			const [showNew, setShowNew] = (0, react.useState)(false);
			const [showAutomation, setShowAutomation] = (0, react.useState)(false);
			const [showNotify, setShowNotify] = (0, react.useState)(false);
			const [notifyFilter, setNotifyFilter] = (0, react.useState)("all");
			const [expandedFoldKey, setExpandedFoldKey] = (0, react.useState)(void 0);
			(0, react.useEffect)(() => {
				setExpandedFoldKey(void 0);
			}, [notifyFilter]);
			(0, react.useEffect)(() => {
				setApproveBlockedKey(void 0);
			}, [notifyFilter]);
			const [snoozed, setSnoozed] = (0, react.useState)({});
			const [failedSession, setFailedSession] = (0, react.useState)(void 0);
			const [approveBlockedKey, setApproveBlockedKey] = (0, react.useState)(void 0);
			const [drawerOpenedAt, setDrawerOpenedAt] = (0, react.useState)(void 0);
			(0, react.useEffect)(() => {
				if (!showNotify) {
					setSnoozed({});
					setFailedSession(void 0);
					setApproveBlockedKey(void 0);
					setDrawerOpenedAt(void 0);
					setExpandedFoldKey(void 0);
				}
			}, [showNotify]);
			const [showActivity, setShowActivity] = (0, react.useState)(false);
			const [activityKind, setActivityKind] = (0, react.useState)("all");
			const [activityQuery, setActivityQuery] = (0, react.useState)("");
			const [activityUnviewed, setActivityUnviewed] = (0, react.useState)(false);
			const [activityShown, setActivityShown] = (0, react.useState)(30);
			const [expandedActivityKey, setExpandedActivityKey] = (0, react.useState)(void 0);
			const [expandedGroupKey, setExpandedGroupKey] = (0, react.useState)(void 0);
			const groupInitRef = (0, react.useRef)(false);
			(0, react.useEffect)(() => {
				if (showActivity) {
					setActivityShown(30);
					setExpandedActivityKey(void 0);
					setExpandedGroupKey(void 0);
				} else setFailedSession(void 0);
			}, [
				showActivity,
				activityKind,
				activityQuery,
				activityUnviewed
			]);
			const notes = (0, react.useMemo)(() => notificationsExOf(snapshot.tasks, (sessionId) => controller.pendingInteractionOf(sessionId), (sessionId) => controller.sessionTitle(sessionId) ?? sessionId, (task) => taskUnviewed(task), (task) => controller.linkedOf(task).map((row) => row.sessionId)), [
				snapshot.tasks,
				snapshot,
				controller
			]);
			const demand = (0, react.useMemo)(() => boardDemandOf(snapshot.tasks, (sessionId) => controller.pendingInteractionOf(sessionId), (task) => controller.linkedOf(task).map((row) => row.sessionId)), [
				snapshot.tasks,
				snapshot,
				controller
			]);
			const visibleNotes = (0, react.useMemo)(() => notes.filter((note) => {
				if (notifyFilter === "waiting" && note.kind !== "waiting") return false;
				if (notifyFilter === "review" && note.kind !== "review") return false;
				if ((snoozed[noteKeyOf(note)] ?? -1) >= note.at) return false;
				return true;
			}), [
				notes,
				notifyFilter,
				snoozed
			]);
			const foldedNotes = (0, react.useMemo)(() => foldNotesByTask(notes.filter((note) => (snoozed[noteKeyOf(note)] ?? -1) < note.at)), [notes, snoozed]);
			const foldedVisible = (0, react.useMemo)(() => foldNotesByTask(visibleNotes), [visibleNotes]);
			const unseenCount = (0, react.useMemo)(() => drawerOpenedAt === void 0 ? 0 : notes.filter((note) => note.at > drawerOpenedAt).length, [notes, drawerOpenedAt]);
			const openNotify = () => {
				const at = Date.now();
				const maxAt = notes.reduce((max, note) => Math.max(max, note.at), at);
				setDrawerOpenedAt(maxAt);
				setShowNotify(true);
			};
			const renderNotifyBell = () => {
				const total = foldedNotes.length;
				const label = total > 0 ? t("board.notifyCount", { n: total > 99 ? "99+" : String(total) }) : t("board.notify");
				return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("button", {
					type: "button",
					className: `${board_module_css_default.iconButton} ${board_module_css_default.notifyBell}`,
					"aria-label": unseenCount > 0 ? t("board.notifyNew", {
						base: label,
						n: String(unseenCount)
					}) : label,
					title: t("board.notify"),
					onClick: openNotify,
					children: [
						/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, { name: "bell" }),
						unseenCount > 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
							className: board_module_css_default.notifyDot,
							"aria-hidden": "true"
						}),
						total > 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
							className: board_module_css_default.notifyBadge,
							"aria-hidden": "true",
							children: total > 99 ? "99+" : String(total)
						})
					]
				});
			};
			const activityFeed = (0, react.useMemo)(() => {
				if (!showActivity) return [];
				const kinds = activityKind === "all" ? void 0 : [...CLUSTER_KINDS[activityKind]];
				return activityOf(snapshot.tasks, {
					...kinds !== void 0 ? { kinds: [...kinds] } : {},
					...activityQuery.trim() !== "" ? { query: activityQuery } : {},
					...activityUnviewed ? { onlyUnviewed: true } : {}
				}, (task, at) => at > taskViewedBaseline(task));
			}, [
				showActivity,
				snapshot.tasks,
				activityKind,
				activityQuery,
				activityUnviewed
			]);
			const [feedBase, setFeedBase] = (0, react.useState)(-1);
			(0, react.useEffect)(() => {
				if (showActivity) setFeedBase(activityFeed.length);
			}, [
				showActivity,
				activityKind,
				activityQuery,
				activityUnviewed
			]);
			(0, react.useEffect)(() => {
				if (!showActivity || groupInitRef.current) return;
				groupInitRef.current = true;
				const first = activityFeed[0];
				if (first === void 0) return;
				setExpandedGroupKey(activityGroupKeyOf(first.taskId, dayBucketOf(first.at), clusterOf(first.kind)));
			}, [showActivity, activityFeed]);
			(0, react.useEffect)(() => {
				if (!showActivity) groupInitRef.current = false;
			}, [showActivity]);
			const [organizing, setOrganizing] = (0, react.useState)(false);
			const [engineNote, setEngineNote] = (0, react.useState)(void 0);
			const [updatePhase, setUpdatePhase] = (0, react.useState)("idle");
			const [updateSource, setUpdateSource] = (0, react.useState)(void 0);
			const [updateLatest, setUpdateLatest] = (0, react.useState)(void 0);
			const [showUpdate, setShowUpdate] = (0, react.useState)(false);
			const [updateCopiedAt, setUpdateCopiedAt] = (0, react.useState)(void 0);
			const [updateCopyFailedAt, setUpdateCopyFailedAt] = (0, react.useState)(void 0);
			const updateTimer = (0, react.useRef)(void 0);
			(0, react.useEffect)(() => () => {
				if (updateTimer.current !== void 0) clearTimeout(updateTimer.current);
			}, []);
			/** 点一下即查：两路并行，任一能证明"有新版"即 available，否则按 npm 定论。 */
			const checkUpdate = () => {
				if (updatePhase === "checking") return;
				setUpdatePhase("checking");
				(async () => {
					const [source, latest] = await Promise.all([fetchUpdateSource(), fetchNpmLatest(BUNDLED_PACKAGE_NAME)]);
					const current = source?.version ?? BUNDLED_VERSION;
					setUpdateSource(source);
					setUpdateLatest(latest ?? void 0);
					const npmNewer = latest !== void 0 && isNewerVersion(latest.version, current);
					const gitNewer = source?.mode === "local" && source.git !== void 0 && isGitBehind(source.git);
					if (npmNewer || gitNewer) {
						setUpdatePhase("available");
						setShowUpdate(true);
						return;
					}
					if (latest === void 0) {
						setUpdatePhase("failed");
						return;
					}
					setUpdatePhase("latest");
					if (updateTimer.current !== void 0) clearTimeout(updateTimer.current);
					updateTimer.current = setTimeout(() => {
						setUpdatePhase("idle");
					}, 2500);
				})();
			};
			/** 复制一条更新命令（与详情页复制同一闪现文法，best-effort 不抛）。 */
			const copyUpdateCommand = (index, command) => {
				const flash = (ok) => {
					setUpdateCopiedAt(ok ? index : void 0);
					setUpdateCopyFailedAt(ok ? void 0 : index);
					if (updateTimer.current !== void 0) clearTimeout(updateTimer.current);
					updateTimer.current = setTimeout(() => {
						setUpdateCopiedAt(void 0);
						setUpdateCopyFailedAt(void 0);
					}, 1500);
				};
				navigator.clipboard?.writeText(command).then(() => {
					flash(true);
				}, () => {
					flash(false);
				});
			};
			const updateCurrent = updateSource?.version ?? BUNDLED_VERSION;
			const updateNpmNewer = updateLatest !== void 0 && isNewerVersion(updateLatest.version, updateCurrent);
			const updateGit = updateSource?.mode === "local" ? updateSource.git : void 0;
			const updateGitBehind = updateGit !== void 0 && isGitBehind(updateGit);
			const updatePublishedMs = updateLatest?.publishedAt !== void 0 ? Date.parse(updateLatest.publishedAt) : NaN;
			const updateLabel = (() => {
				if (updatePhase === "checking") return t("board.update.checking");
				if (updatePhase === "latest") return t("board.update.latest");
				if (updatePhase === "failed") return t("board.update.failed");
				if (updatePhase === "available") return updateNpmNewer && updateLatest !== void 0 ? t("board.update.available", { v: updateLatest.version }) : t("board.update.availableGit");
				return t("board.update.check");
			})();
			(0, react.useEffect)(() => {
				if (engineNote === "stale" && !(snapshot.engine.synced && snapshot.engine.hostProto < 2)) setEngineNote(void 0);
			}, [
				engineNote,
				snapshot.engine.synced,
				snapshot.engine.hostProto
			]);
			const [selectedCards, setSelectedCards] = (0, react.useState)([]);
			const toggleCard = (id) => {
				setSelectedCards((current) => current.includes(id) ? current.filter((cardId) => cardId !== id) : [...current, id]);
			};
			const clearSelection = () => {
				setSelectedCards([]);
			};
			const liveIds = (0, react.useMemo)(() => {
				const present = new Set(snapshot.tasks.map((task) => task.id));
				return selectedCards.filter((id) => present.has(id));
			}, [snapshot.tasks, selectedCards]);
			(0, react.useEffect)(() => {
				if (liveIds.length !== selectedCards.length) setSelectedCards(liveIds);
			}, [liveIds, selectedCards.length]);
			const exitOrganize = () => {
				setOrganizing(false);
				clearSelection();
			};
			/** 批量换色（undefined = 移除颜色，经单一色表文法的「移除颜色」点触发）。 */
			const applyColorToSelected = (color) => {
				const targets = snapshot.tasks.filter((task) => liveIds.includes(task.id));
				for (const task of targets) controller.setTaskColor(task.id, color);
			};
			/** 批量删除（确认后）；删除会同步取消板上的选中集。 */
			const [confirmDeleteSelected, setConfirmDeleteSelected] = (0, react.useState)(false);
			const deleteSelected = () => {
				for (const id of liveIds) controller.deleteTask(id);
				clearSelection();
			};
			const [narrow, narrowRef] = useSurfaceNarrow("[data-dsh-taskboard-view]", 680);
			const boardRef = (0, react.useRef)(null);
			const boardRefs = (0, react.useCallback)((node) => {
				boardRef.current = node;
				narrowRef.current = node;
			}, [narrowRef]);
			const [cruiseOpen, setCruiseOpen] = (0, react.useState)(false);
			const [limitText, setLimitText] = (0, react.useState)(String(snapshot.cruise.limit));
			(0, react.useEffect)(() => {
				setLimitText(String(snapshot.cruise.limit));
			}, [snapshot.cruise.limit]);
			const cruiseWrapRef = (0, react.useRef)(null);
			(0, react.useEffect)(() => {
				if (!cruiseOpen || narrow) return;
				const onDown = (event) => {
					if (cruiseWrapRef.current !== null && !cruiseWrapRef.current.contains(event.target)) setCruiseOpen(false);
				};
				document.addEventListener("mousedown", onDown);
				return () => {
					document.removeEventListener("mousedown", onDown);
				};
			}, [cruiseOpen, narrow]);
			const [windowStart, setWindowStart] = (0, react.useState)(void 0);
			const [windowEnd, setWindowEnd] = (0, react.useState)(void 0);
			const [cruiseError, setCruiseError] = (0, react.useState)(void 0);
			(0, react.useEffect)(() => {
				if (!cruiseOpen) return;
				setCruiseError(void 0);
			}, [cruiseOpen]);
			/**
			* 添加一条巡航定时窗口：开始与结束均可选（至少一个；留空结束=到点开、之后
			* 保持开；留空开始=立即视为开、到点关）。结束早于开始 = 跨午夜（如 22:00 →
			* 02:00）合法：归一化自动 +1 天处理，绝不拒绝；每个非法形态（双空/同时刻/
			* 早于开始超过一天/开始已过/结束已过）都有专属提示，绝不带病写入。
			*/
			const addCruiseWindow = () => {
				if (windowStart === void 0 && windowEnd === void 0) {
					setCruiseError(t("board.cruiseWindowErrorStart"));
					return;
				}
				const candidate = {
					...windowStart !== void 0 ? { startAt: windowStart } : {},
					...windowEnd !== void 0 ? { endAt: windowEnd } : {}
				};
				const issue = windowRangeIssueOf(normalizeWindow(candidate), cruiseNow);
				if (issue !== void 0) {
					setCruiseError(cruiseRangeError(issue, candidate));
					return;
				}
				if (duplicateWindowOf(cruiseSchedule, candidate) !== void 0) {
					setCruiseError(t("board.cruiseWindowErrorDuplicate"));
					return;
				}
				controller.setCruiseSchedule([...cruiseSchedule, candidate]);
				setWindowStart(void 0);
				setWindowEnd(void 0);
				setCruiseError(void 0);
			};
			const cruiseSchedule = snapshot.cruise.schedule;
			const cruiseNow = Date.now();
			const cruiseStateLine = (() => {
				const line = cruiseStatusLineOf(snapshot.cruise, cruiseNow);
				if (line.kind === "window-on") return line.endAt !== void 0 ? t("board.cruiseStatusWindow", { time: formatCruiseTime(line.endAt, cruiseNow) }) : t("board.cruiseStatusWindowHold");
				if (line.kind === "manual-on") return t("board.cruiseStatusManualOn");
				if (line.kind === "manual-off") return line.endAt !== void 0 ? t("board.cruiseStatusManualOff", { time: formatCruiseTime(line.endAt, cruiseNow) }) : t("board.cruiseStatusManualOffHold");
				if (line.kind === "scheduled-off") return t("board.cruiseStatusScheduled", { time: formatCruiseTime(line.startAt, cruiseNow) });
				return t("board.cruiseStatusOff");
			})();
			const [dragOver, setDragOver] = (0, react.useState)(void 0);
			const undoDisarm = controller.undoableDisarmOf();
			const [dragReject, setDragReject] = (0, react.useState)(void 0);
			const [dragId, setDragId] = (0, react.useState)(void 0);
			const [dropGap, setDropGap] = (0, react.useState)(void 0);
			const dropGapRef = (0, react.useRef)(void 0);
			const cardsRefs = (0, react.useRef)({});
			const indicatorRefs = (0, react.useRef)({});
			const rejectTimer = (0, react.useRef)(void 0);
			(0, react.useEffect)(() => () => {
				if (rejectTimer.current !== void 0) clearTimeout(rejectTimer.current);
			}, []);
			useFlipRegion(boardRef, dragId !== void 0);
			const columnsRef = (0, react.useRef)(null);
			const [activeColumn, setActiveColumn] = (0, react.useState)(COLUMNS[0].status);
			/** The track's column left-edges (identity order = COLUMNS order). */
			const columnLefts = (root) => Array.from(root.querySelectorAll("section[data-status]")).map((section) => section.offsetLeft);
			const columnIndex = (status) => COLUMNS.findIndex((column) => column.status === status);
			const syncActiveColumn = (0, react.useCallback)(() => {
				const root = columnsRef.current;
				if (root === null) return;
				const sections = Array.from(root.querySelectorAll("section[data-status]"));
				if (sections.length === 0) return;
				const status = sections[activeColumnIndexAt(root.scrollLeft, sections.map((section) => section.offsetLeft))]?.dataset.status;
				if (status !== void 0) setActiveColumn(status);
			}, []);
			const jumpToColumn = (status) => {
				const root = columnsRef.current;
				if (root === null) return;
				root.scrollLeft = scrollLeftForColumn(columnIndex(status), columnLefts(root), root.clientWidth, root.scrollWidth);
			};
			/**
			* The compact column navigator is announced as `role="tablist"` / `role="tab"`, and
			* an ARIA tablist PROMISES arrow-key navigation plus roving tabindex — neither of
			* which existed: the tabs were plain buttons with no key handling, so the announced
			* interaction model was false and a keyboard user had five tab stops to walk. The
			* roles were right (this really is a tablist over five panes); the behaviour was
			* missing, so the behaviour is what got written.
			*
			* WAI-ARIA tablist grammar: Left/Right move (wrapping), Home/End jump to the ends,
			* and only the active tab stays in the page's tab order.
			*/
			const columnTabRefs = (0, react.useRef)({});
			const onColumnTabKeyDown = (event, status) => {
				const at = columnIndex(status);
				let next;
				if (event.key === "ArrowRight") next = (at + 1) % COLUMNS.length;
				else if (event.key === "ArrowLeft") next = (at - 1 + COLUMNS.length) % COLUMNS.length;
				else if (event.key === "Home") next = 0;
				else if (event.key === "End") next = COLUMNS.length - 1;
				if (next === void 0) return;
				event.preventDefault();
				const target = COLUMNS[next]?.status;
				if (target === void 0) return;
				jumpToColumn(target);
				columnTabRefs.current[target]?.focus();
			};
			(0, react.useEffect)(() => {
				const root = columnsRef.current;
				if (root === null) return;
				let lastWidth = root.clientWidth;
				const observer = new ResizeObserver(() => {
					if (root.clientWidth === lastWidth) return;
					lastWidth = root.clientWidth;
					root.scrollLeft = scrollLeftForColumn(columnIndex(activeColumn), columnLefts(root), root.clientWidth, root.scrollWidth);
				});
				observer.observe(root);
				return () => {
					observer.disconnect();
				};
			}, [activeColumn]);
			const [dropAccept, setDropAccept] = (0, react.useState)(void 0);
			const externalRef = (0, react.useRef)(false);
			const dragSourceRef = (0, react.useRef)(false);
			/**
			* Full reset of every transient drag state — after a drop, a drag end,
			* a window-level drop/dragend, or any cancel. The one reset to rule them
			* all: no path may clear only part of the drag UI and leave a stale
			* highlight behind.
			*/
			const clearDrag = () => {
				externalRef.current = false;
				dragSourceRef.current = false;
				setDropAccept(void 0);
				setDragId(void 0);
				setDropGap(void 0);
				dropGapRef.current = void 0;
				setDragOver(void 0);
				autoScrollRef.current = null;
			};
			(0, react.useEffect)(() => {
				window.addEventListener("drop", clearDrag);
				window.addEventListener("dragend", clearDrag);
				return () => {
					window.removeEventListener("drop", clearDrag);
					window.removeEventListener("dragend", clearDrag);
				};
			}, []);
			/** Classify a drag as a sidebar drag (own MIME, else native text/plain). */
			const externalOf = (event) => externalDragOf(event.dataTransfer, (id) => snapshot.tasks.some((task) => task.id === id), (id) => controller.externalKindOf(id));
			const selected = selectedTaskOf(snapshot);
			const [detailSessionRequest, setDetailSessionRequest] = (0, react.useState)(void 0);
			const openTaskAtSession = (taskId, sessionId) => {
				const known = sessionId !== void 0 && controller.sessionTitle(sessionId) !== void 0;
				setDetailSessionRequest(known && sessionId !== void 0 ? {
					taskId,
					sessionId
				} : void 0);
				controller.openTaskFromNotification(taskId, known ? sessionId : void 0);
			};
			const workspaceTitleOf = (workspaceId) => {
				return (controller.runCatalog()?.listWorkspaces().find((candidate) => candidate.id === workspaceId))?.title ?? workspaceId;
			};
			const visible = snapshot.tasks.filter((task) => matchTask(task, filter, controller.linkedOf(task).map((row) => row.title), {
				...task.workspaceId !== void 0 ? { workspaceTitle: workspaceTitleOf(task.workspaceId) } : {},
				hasAutomation: hasLiveAutomation(task),
				isUnviewed: taskUnviewed(task)
			}));
			const cardClick = (id, event) => {
				if (organizing || event?.ctrlKey === true || event?.metaKey === true) {
					toggleCard(id);
					return;
				}
				const task = snapshot.tasks.find((candidate) => candidate.id === id);
				const waitingSessionId = task === void 0 ? void 0 : taskPendingCount(task, (sessionId) => controller.pendingInteractionOf(sessionId)).items[0]?.sessionId;
				if (waitingSessionId !== void 0 && controller.sessionTitle(waitingSessionId) !== void 0) {
					openTaskAtSession(id, waitingSessionId);
					return;
				}
				controller.openTask(id);
			};
			const orgColorValue = (() => {
				let seen;
				let mixed = false;
				let any = false;
				for (const task of snapshot.tasks) {
					if (!liveIds.includes(task.id)) continue;
					any = true;
					if (task.color === void 0) mixed = true;
					else if (seen === void 0) seen = task.color;
					else if (seen !== task.color) mixed = true;
				}
				if (!any || mixed) return void 0;
				return seen;
			})();
			const draggedTask = dragId !== void 0 ? snapshot.tasks.find((candidate) => candidate.id === dragId) : void 0;
			/** Id of the card element under the pointer, when the pointer is on one. */
			const cardIdAt = (event) => event.target.closest("[data-task-id]")?.getAttribute("data-task-id") ?? void 0;
			/** The nearest insertion gap of a drag at `dropY`, relative to the cards container. */
			const gapAt = (status, dropY) => {
				const container = cardsRefs.current[status];
				if (container == null || dragId === void 0) return {
					beforeId: void 0,
					top: 0
				};
				const containerTop = container.getBoundingClientRect().top;
				const gap = insertionGapOf(Array.from(container.querySelectorAll("[data-task-id]")).map((element) => ({
					id: element.getAttribute("data-task-id") ?? "",
					rect: element.getBoundingClientRect()
				})), dropY, dragId, 8);
				return {
					beforeId: gap.beforeId,
					top: indicatorTopOf(gap.top, containerTop, container.scrollTop, container.scrollHeight)
				};
			};
			/** Apply a insertion gap to the UI: THE one gap-write path (dragover and the
			*  auto-scroll frame share it — the indicator stays truthful mid-scroll).
			*  React state only churns when the STRUCTURE of the gap changes
			*  (status + beforeId); the indicator's top is written straight into the
			*  DOM on every call, so a pointer move or a scroll can never trigger a
			*  re-render — no flicker, no storm. */
			const applyGap = (0, react.useCallback)((status, dropY) => {
				const gap = gapAt(status, dropY);
				dropGapRef.current = {
					status,
					beforeId: gap.beforeId,
					top: gap.top
				};
				const indicator = indicatorRefs.current[status];
				if (indicator !== null && indicator !== void 0) indicator.style.top = `${gap.top}px`;
				setDropGap((current) => current !== void 0 && current.status === status && current.beforeId === gap.beforeId ? current : {
					status,
					beforeId: gap.beforeId,
					top: gap.top
				});
			}, [gapAt]);
			const autoScrollRef = (0, react.useRef)(null);
			useDragAutoScroll((0, react.useCallback)(() => autoScrollRef.current?.cards ?? null, []), dragId !== void 0 || dropAccept !== void 0, (0, react.useCallback)((pointerY) => {
				const target = autoScrollRef.current;
				if (target !== null) applyGap(target.status, pointerY);
			}, [applyGap]));
			/** Create a bound task from an external sidebar drag (session or workspace). */
			const createFromSidebar = (drag, dropStatus) => {
				const bind = drag.kind === "session" ? {
					kind: "session",
					sessionId: drag.id
				} : {
					kind: "workspace",
					workspaceId: drag.id
				};
				controller.createBoundTask(bind, {
					title: controller.boundSourceTitleOf(bind),
					description: "",
					prompt: "",
					status: landingStatusOf(dropStatus)
				});
			};
			/**
			* Column-level drop: same-column drops reorder (the half-split anchor the
			* last dragover computed); cross-column drops keep the classic
			* move/rerun/reject semantics; an external sidebar drag (session/workspace)
			* creates a bound task in this column. Every drop ends with a full drag
			* reset, so no transient highlight can survive the gesture. The moved card
			* settles into place via the board's FLIP animation — the drop feedback is
			* the card itself, never a column-edge flash.
			*/
			const handleDrop = (status) => (event) => {
				event.preventDefault();
				const gapRef = dropGapRef.current;
				const gapOf = (target) => gapRef !== void 0 && gapRef.status === target ? gapRef.beforeId : void 0;
				const external = externalOf(event);
				const id = dragId ?? event.dataTransfer.getData("text/plain");
				clearDrag();
				if (external !== void 0) {
					event.stopPropagation();
					createFromSidebar(external, status);
					return;
				}
				const task = snapshot.tasks.find((candidate) => candidate.id === id);
				if (task === void 0) return;
				if (dragId !== void 0 && task.status === status) {
					controller.moveTask(task.id, status, gapOf(status));
					return;
				}
				applyCardDrop(task, status);
			};
			/**
			* THE one place a card is asked to change column. Both the pointer path (drop)
			* and the keyboard path (`[` / `]`) come through here, so the keyboard can never
			* reach a transition a drag would refuse — the guard, the run-vs-move judgment
			* and the refusal notice exist once. The insertion position is read from the
			* live drop-gap ref (undefined for a keyboard move, which lands at the tail —
			* the same place a drop with no previewed gap lands).
			*/
			const applyCardDrop = (task, status) => {
				const gapRef = dropGapRef.current;
				const beforeId = gapRef !== void 0 && gapRef.status === status ? gapRef.beforeId : void 0;
				const decision = resolveCardDrop(task, status);
				if (decision.kind === "move") controller.moveTask(task.id, decision.status, beforeId);
				else if (decision.kind === "run") controller.rerunTask(task.id);
				else if (decision.kind === "reject") {
					setDragReject({
						status,
						reason: decision.reason
					});
					if (rejectTimer.current !== void 0) clearTimeout(rejectTimer.current);
					rejectTimer.current = setTimeout(() => {
						setDragReject(void 0);
					}, 600);
				}
			};
			/** Step a card one column along the board's own COLUMNS order (keyboard). */
			const stepCard = (task, direction) => {
				const next = adjacentStatus(task.status, direction);
				if (next === void 0) return;
				applyCardDrop(task, next);
			};
			const engineWaitVisible = snapshot.stats.queued > 0 || snapshot.tasks.some((task) => pendingCommentCount(task) > 0);
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
				ref: boardRefs,
				className: board_module_css_default.board,
				"data-dsh-taskboard-board": "",
				"data-dragging": dragId !== void 0 ? "" : void 0,
				onDragEnter: (event) => {
					if (!dragSourceRef.current && candidateExternalDrag(Array.from(event.dataTransfer.types))) externalRef.current = true;
				},
				onDragOver: (event) => {
					if (externalRef.current) event.preventDefault();
				},
				onDrop: (event) => {
					const external = externalOf(event);
					clearDrag();
					if (external !== void 0) {
						event.preventDefault();
						createFromSidebar(external, "todo");
					}
				},
				children: [
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("header", {
						className: board_module_css_default.boardHeader,
						children: [
							demand.total > 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("button", {
								type: "button",
								className: board_module_css_default.boardDemand,
								title: t("board.demandTitle"),
								"aria-label": `${t("board.demand", {
									n: String(demand.waiting),
									m: String(demand.review)
								})}。${t("board.demandTitle")}`,
								onClick: openNotify,
								children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
									className: board_module_css_default.boardDemandDot,
									"aria-hidden": "true"
								}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
									className: board_module_css_default.boardDemandText,
									children: t("board.demand", {
										n: String(demand.waiting),
										m: String(demand.review)
									})
								})]
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
								className: `${board_module_css_default.boardRow} ${board_module_css_default.boardRowNav}`,
								children: [
									/* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
										type: "button",
										className: board_module_css_default.boardBack,
										"aria-label": t("board.close"),
										title: t("board.close"),
										onClick: () => {
											controller.closeBoard();
										},
										children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, { name: "arrowLeft" })
									}),
									/* @__PURE__ */ (0, react_jsx_runtime.jsx)("h2", {
										className: board_module_css_default.boardTitle,
										children: t("board.title")
									}),
									/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", { className: board_module_css_default.boardSpacer }),
									(() => {
										const flow = flowSummaryOf(snapshot.tasks, Date.now());
										const flowParts = [...flow.samples > 0 && flow.p85Days !== void 0 ? [flow.p85Days < 1 ? t("board.flowSubDay") : t("board.flowDays", { n: String(Math.round(flow.p85Days)) })] : [], ...flow.perWeek > 0 ? [t("board.flowThroughput", { n: String(Math.round(flow.perWeek * 100) / 100) })] : []];
										const stateParts = [
											...snapshot.stats.running > 0 ? [t("board.statusRunning", { n: String(snapshot.stats.running) })] : [],
											...snapshot.stats.queued > 0 ? [t("board.statusQueued", { n: String(snapshot.stats.queued) })] : [],
											...flowParts.length > 0 ? [t("board.flowStats", { parts: flowParts.join(" · ") })] : [],
											...snapshot.skips.overlap + snapshot.skips.missed > 0 ? [t("board.statusSkipped", { n: String(snapshot.skips.overlap + snapshot.skips.missed) })] : [],
											...(() => {
												const armedSchedule = snapshot.tasks.some((task) => task.schedule?.enabled === true);
												const quiet = !snapshot.cruise.enabled && !armedSchedule;
												const viewer = snapshot.engine.synced && !snapshot.engine.held;
												const lastOkAt = snapshot.heartbeat.lastOkAt;
												return !quiet && !viewer && lastOkAt !== void 0 && isHeartbeatStale(lastOkAt, Date.now()) ? [t("board.heartbeatStale", { time: formatDateTime(lastOkAt) })] : [];
											})()
										];
										const engineStale = snapshot.engine.synced && snapshot.engine.hostProto < 2;
										const engineViewer = snapshot.engine.synced && !engineStale && !snapshot.engine.held && engineWaitVisible;
										if (stateParts.length === 0 && !engineStale && !engineViewer && !bundleStale) return null;
										return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
											className: board_module_css_default.boardState,
											children: [
												stateParts.length > 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
													className: board_module_css_default.boardStatus,
													children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
														className: board_module_css_default.boardStatusDot,
														"aria-hidden": "true"
													}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
														className: board_module_css_default.boardStatusText,
														children: stateParts.join(" · ")
													})]
												}),
												bundleStale && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("button", {
													type: "button",
													className: board_module_css_default.boardStatusButton,
													"data-warn": "true",
													title: t("board.bundleStaleTitle"),
													onClick: () => {
														reloadForFreshBundle();
													},
													children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
														className: board_module_css_default.boardStatusDot,
														"aria-hidden": "true"
													}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
														className: board_module_css_default.boardStatusText,
														children: t("board.bundleStale", {
															c: freshnessView?.bundled ?? "",
															s: freshnessView?.host ?? ""
														})
													})]
												}),
												engineStale && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("button", {
													type: "button",
													className: board_module_css_default.boardStatusButton,
													"data-warn": "true",
													onClick: () => {
														setEngineNote("stale");
													},
													children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
														className: board_module_css_default.boardStatusDot,
														"aria-hidden": "true"
													}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
														className: board_module_css_default.boardStatusText,
														children: t("board.engineStale")
													})]
												}),
												engineViewer && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("button", {
													type: "button",
													className: board_module_css_default.boardStatusButton,
													onClick: () => {
														setEngineNote("viewer");
													},
													children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
														className: board_module_css_default.boardStatusDot,
														"aria-hidden": "true"
													}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
														className: board_module_css_default.boardStatusText,
														children: t("board.engineViewer")
													})]
												})
											]
										});
									})(),
									/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
										className: board_module_css_default.cruiseWrap,
										ref: cruiseWrapRef,
										children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
											className: board_module_css_default.cruisePill,
											children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Switch, {
												checked: snapshot.cruise.enabled,
												onChange: (next) => {
													controller.setCruiseEnabled(next);
												},
												label: t("board.cruise"),
												title: t("board.cruiseTitle")
											}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
												type: "button",
												className: board_module_css_default.cruiseMore,
												"aria-label": t("board.cruiseSettings"),
												title: t("board.cruiseSettings"),
												"aria-expanded": cruiseOpen,
												"aria-controls": "dsh-tb-cruise-settings",
												onClick: () => {
													setCruiseOpen(!cruiseOpen);
												},
												children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, { name: "chevronDown" })
											})]
										}), cruiseOpen && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(CruiseSettingsHost, {
											narrow,
											label: t("board.cruiseSettings"),
											id: "dsh-tb-cruise-settings",
											onClose: () => {
												setCruiseOpen(false);
											},
											children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
												className: board_module_css_default.cruisePopoverHead,
												children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Switch, {
													checked: snapshot.cruise.enabled,
													onChange: (next) => {
														controller.setCruiseEnabled(next);
													},
													label: t("board.cruise"),
													title: t("board.cruiseTitle")
												}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
													className: board_module_css_default.cruisePopoverLimit,
													children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
														className: board_module_css_default.cruisePopoverLabel,
														children: t("board.cruiseLimitShort")
													}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("input", {
														className: board_module_css_default.cruiseLimit,
														type: "number",
														min: 1,
														max: MAX_CRUISE_LIMIT,
														value: limitText,
														title: t("board.cruiseLimit"),
														"aria-label": t("board.cruiseLimit"),
														onChange: (event) => {
															setLimitText(event.target.value);
															const value = Number(event.target.value);
															if (event.target.value.trim() !== "" && Number.isInteger(value) && value >= 1) controller.setCruiseLimit(value);
														},
														onBlur: () => {
															setLimitText(String(snapshot.cruise.limit));
														}
													})]
												})]
											}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
												className: board_module_css_default.cruiseSchedule,
												children: [
													/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
														className: board_module_css_default.cruiseScheduleHead,
														children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
															className: board_module_css_default.cruiseScheduleTitle,
															children: t("board.cruiseSchedule")
														}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
															className: board_module_css_default.cruiseStateLine,
															children: cruiseStateLine
														})]
													}),
													cruiseSchedule.length === 0 ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
														className: board_module_css_default.detailHint,
														children: t("board.cruiseScheduleEmpty")
													}) : /* @__PURE__ */ (0, react_jsx_runtime.jsx)("ul", {
														className: board_module_css_default.cruiseWindowList,
														children: cruiseSchedule.map((window, index) => /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("li", {
															className: board_module_css_default.cruiseWindowRow,
															style: { animationDelay: `${index * 20}ms` },
															children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
																className: board_module_css_default.cruiseWindowTime,
																title: cruiseWindowTitleOf(window),
																children: cruiseWindowLabelOf(window, cruiseNow)
															}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
																type: "button",
																className: board_module_css_default.rowHide,
																title: t("board.cruiseWindowRemove"),
																onClick: () => {
																	controller.setCruiseSchedule(cruiseSchedule.filter((_, i) => i !== index));
																},
																children: t("board.cruiseWindowRemove")
															})]
														}, `${window.startAt ?? "open"}-${window.endAt ?? "open"}`))
													}),
													/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
														className: board_module_css_default.cruiseWindowAdd,
														children: [
															/* @__PURE__ */ (0, react_jsx_runtime.jsx)(TimeField, {
																label: t("board.cruiseWindowStart"),
																placeholder: t("board.cruiseWindowStartPlaceholder"),
																value: windowStart,
																onChange: (next) => {
																	setWindowStart(next);
																	setCruiseError(void 0);
																}
															}),
															/* @__PURE__ */ (0, react_jsx_runtime.jsx)(TimeField, {
																label: t("board.cruiseWindowEnd"),
																placeholder: t("board.cruiseWindowEndPlaceholder"),
																value: windowEnd,
																onChange: (next) => {
																	setWindowEnd(next);
																	setCruiseError(void 0);
																}
															}),
															cruiseError !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
																className: board_module_css_default.formError,
																children: cruiseError
															}),
															/* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
																className: board_module_css_default.detailHint,
																children: t("board.cruiseSemanticsHint")
															}),
															/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
																size: "sm",
																onClick: addCruiseWindow,
																children: t("board.cruiseWindowAdd")
															})
														]
													})
												]
											})]
										})]
									}),
									/* @__PURE__ */ (0, react_jsx_runtime.jsxs)(Button, {
										variant: "primary",
										className: board_module_css_default.boardNewTask,
										onClick: () => {
											setShowNew(true);
										},
										children: ["+ ", t("board.new")]
									})
								]
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
								className: `${board_module_css_default.boardRow} ${board_module_css_default.boardRowTools}`,
								children: [
									/* @__PURE__ */ (0, react_jsx_runtime.jsx)("input", {
										className: board_module_css_default.search,
										type: "search",
										placeholder: t("board.search"),
										value: filter,
										list: "dsh-tb-qualifiers",
										onChange: (event) => {
											setFilter(event.target.value);
										},
										"aria-label": t("board.search")
									}),
									/* @__PURE__ */ (0, react_jsx_runtime.jsx)("datalist", {
										id: "dsh-tb-qualifiers",
										children: completeBoardQuery(filter).map((candidate) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)("option", { value: applyCompletion(filter, candidate) }, candidate))
									}),
									/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
										className: board_module_css_default.boardModes,
										children: [
											/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
												variant: "ghost",
												pressed: organizing,
												onClick: () => {
													organizing ? exitOrganize() : setOrganizing(true);
												},
												children: t("board.organize")
											}),
											/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
												variant: "ghost",
												title: t("board.automationTitle"),
												onClick: () => {
													setShowAutomation(true);
												},
												children: t("board.automation")
											}),
											/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
												variant: "ghost",
												className: board_module_css_default.modeDynamic,
												title: t("board.activityTitle"),
												onClick: () => {
													setFeedBase(-1);
													setShowActivity(true);
												},
												children: t("board.activity")
											}),
											/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
												variant: "ghost",
												title: t("board.update.check"),
												disabled: updatePhase === "checking",
												onClick: () => {
													if (updatePhase === "available" && !showUpdate) setShowUpdate(true);
													else checkUpdate();
												},
												children: updateLabel
											}),
											renderNotifyBell()
										]
									}),
									dragReject !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
										className: board_module_css_default.dragRejectHint,
										role: "status",
										"aria-live": "polite",
										children: t("board.dragRejectBusy", { column: t(STATUS_KEY[dragReject.status]) })
									}),
									undoDisarm !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("p", {
										className: board_module_css_default.undoDisarmRow,
										role: "status",
										children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
											className: board_module_css_default.undoDisarmText,
											children: t("board.undoDisarmText", { title: titleOrUntitled(undoDisarm.title, t("card.untitled")) })
										}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("button", {
											type: "button",
											className: board_module_css_default.boardStatusButton,
											onClick: () => {
												controller.undoMoveToDone();
											},
											children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
												className: board_module_css_default.boardStatusDot,
												"aria-hidden": "true"
											}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
												className: board_module_css_default.boardStatusText,
												children: t("board.undoDisarm")
											})]
										})]
									}),
									/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
										className: board_module_css_default.columnTabs,
										role: "tablist",
										"aria-label": t("board.title"),
										children: COLUMNS.map((column) => {
											const count = visible.filter((task) => task.status === column.status).length;
											return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("button", {
												type: "button",
												role: "tab",
												ref: (element) => {
													columnTabRefs.current[column.status] = element;
												},
												className: board_module_css_default.columnTab,
												"aria-selected": activeColumn === column.status,
												"aria-label": t(STATUS_KEY[column.status]),
												tabIndex: activeColumn === column.status ? 0 : -1,
												"data-active": activeColumn === column.status ? "true" : void 0,
												onClick: () => {
													jumpToColumn(column.status);
												},
												onKeyDown: (event) => {
													onColumnTabKeyDown(event, column.status);
												},
												children: [
													/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
														className: board_module_css_default.statusDot,
														"data-status": column.status,
														"aria-hidden": "true"
													}),
													/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
														className: board_module_css_default.columnTabLabel,
														children: t(STATUS_SHORT_KEY[column.status])
													}),
													/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
														className: board_module_css_default.columnTabCount,
														children: String(count)
													})
												]
											}, column.status);
										})
									})
								]
							}),
							splitFilterTokens(filter).length > 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
								className: board_module_css_default.filterChips,
								role: "group",
								"aria-label": t("board.search"),
								children: [splitFilterTokens(filter).map((token, index) => /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
									className: board_module_css_default.filterChip,
									children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Chip, {
										kind: "neutral",
										fill: false,
										children: token
									}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
										type: "button",
										className: board_module_css_default.filterChipRemove,
										"aria-label": t("board.filterRemove", { token }),
										title: t("board.filterRemove", { token }),
										onClick: () => {
											setFilter(removeFilterToken(filter, index));
										},
										children: "×"
									})]
								}, `${index}:${token}`)), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
									type: "button",
									className: board_module_css_default.feedAction,
									onClick: () => {
										setFilter("");
									},
									children: t("board.filterClear")
								})]
							}),
							(organizing || liveIds.length > 0) && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
								className: board_module_css_default.boardRow,
								children: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
									className: board_module_css_default.organizeBar,
									children: [
										/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
											className: board_module_css_default.organizeGroup,
											children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
												className: board_module_css_default.organizeLabel,
												children: t("board.organizeColor")
											}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(ColorSwatches, {
												value: orgColorValue,
												onChange: applyColorToSelected
											})]
										}),
										/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
											className: board_module_css_default.organizeCount,
											children: t("board.organizeCount", { n: String(liveIds.length) })
										}),
										/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
											className: board_module_css_default.organizeActions,
											children: [
												liveIds.length > 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
													size: "sm",
													variant: "primary",
													title: t("board.organizeRunTitle"),
													disabled: runnableIds(snapshot.tasks, liveIds).length === 0,
													onClick: () => {
														for (const id of runnableIds(snapshot.tasks, liveIds)) controller.runTask(id, "manual");
													},
													children: t("board.organizeRun", {
														m: String(runnableIds(snapshot.tasks, liveIds).length),
														n: String(liveIds.length)
													})
												}),
												/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
													size: "sm",
													onClick: () => {
														setSelectedCards(visible.map((task) => task.id));
													},
													children: t("board.organizeSelectAll")
												}),
												/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
													size: "sm",
													disabled: liveIds.length === 0,
													onClick: clearSelection,
													children: t("board.organizeClear")
												}),
												/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
													size: "sm",
													onClick: exitOrganize,
													children: t("board.organizeDone")
												}),
												liveIds.length > 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
													className: board_module_css_default.organizeDanger,
													children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
														size: "sm",
														variant: "danger",
														disabled: liveIds.length === 0,
														onClick: () => {
															setConfirmDeleteSelected(true);
														},
														children: t("board.organizeDelete")
													})
												})
											]
										})
									]
								})
							}),
							confirmDeleteSelected && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(ConfirmDialog, {
								title: t("board.deleteSelectedTitle", { n: String(liveIds.length) }),
								message: t("board.deleteSelectedConfirm"),
								confirmLabel: t("board.deleteSelectedOk", { n: String(liveIds.length) }),
								danger: true,
								onConfirm: () => {
									setConfirmDeleteSelected(false);
									deleteSelected();
								},
								onCancel: () => {
									setConfirmDeleteSelected(false);
								}
							}),
							engineNote !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(Dialog, {
								label: t(engineNote === "stale" ? "board.engineStale" : "board.engineViewer"),
								title: t(engineNote === "stale" ? "board.engineStale" : "board.engineViewer"),
								onClose: () => {
									setEngineNote(void 0);
								},
								portal: true,
								children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
									className: board_module_css_default.modalScroll,
									children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
										className: board_module_css_default.detailText,
										children: t(engineNote === "stale" ? "board.engineStaleHint" : "board.engineViewerHint")
									}), engineNote === "stale" && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [
										/* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
											className: board_module_css_default.detailHint,
											children: t("board.engineNoteProto", {
												n: String(snapshot.engine.hostProto),
												min: "2"
											})
										}),
										/* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
											className: board_module_css_default.detailHint,
											children: snapshot.engine.bootedAt === void 0 ? t("board.engineNoteBootUnknown") : t("board.engineNoteBoot", { time: formatDateTime(snapshot.engine.bootedAt) })
										}),
										/* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
											className: board_module_css_default.detailHint,
											children: t("board.engineNoteStaleMulti")
										})
									] })]
								}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("footer", {
									className: board_module_css_default.modalFooter,
									children: [controller.canRecheckSeat() && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
										onClick: () => {
											controller.recheckSeat();
										},
										children: t("board.engineNoteRecheck")
									}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
										variant: "primary",
										onClick: () => {
											setEngineNote(void 0);
										},
										children: t("board.engineNoteOk")
									})]
								})]
							}),
							showUpdate && updatePhase === "available" && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(Dialog, {
								label: t("board.update.title"),
								title: t("board.update.title"),
								onClose: () => {
									setShowUpdate(false);
								},
								portal: true,
								children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
									className: board_module_css_default.modalScroll,
									children: [
										/* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
											className: board_module_css_default.detailText,
											children: t("board.update.current", { v: updateCurrent })
										}),
										updateSource !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
											className: board_module_css_default.detailHint,
											children: t("board.update.source", { mode: updateModeLabel(updateSource.mode) })
										}),
										updateNpmNewer && updateLatest !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
											className: board_module_css_default.detailText,
											children: t("board.update.newVersion", { v: updateLatest.version })
										}),
										updateNpmNewer && Number.isFinite(updatePublishedMs) && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
											className: board_module_css_default.detailHint,
											children: t("board.update.published", { time: formatDateTime(updatePublishedMs) })
										}),
										updateGitBehind && updateGit?.remoteHead !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
											className: board_module_css_default.detailHint,
											children: t("board.update.gitBehind", {
												head: shortSha(updateGit.head),
												remote: shortSha(updateGit.remoteHead)
											})
										}),
										updateGit !== void 0 && !updateGitBehind && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
											className: board_module_css_default.detailHint,
											children: t("board.update.gitUpToDate")
										}),
										updateGit !== void 0 && updateGit.dirty && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
											className: board_module_css_default.detailHint,
											children: t("board.update.gitDirty")
										}),
										updateActionsFor(updateSource?.mode ?? "unknown", updateSource?.packageName ?? BUNDLED_PACKAGE_NAME, updateSource?.githubSpec).map((action, index) => {
											const copied = updateCopiedAt === index;
											const failed = updateCopyFailedAt === index;
											return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", { children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
												className: board_module_css_default.detailHint,
												children: updateHintOf(action.kind)
											}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("p", {
												className: board_module_css_default.detailText,
												children: [
													/* @__PURE__ */ (0, react_jsx_runtime.jsx)("code", { children: action.command }),
													" ",
													/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
														size: "sm",
														title: copied ? t("detail.copied") : failed ? t("detail.copyFailed") : t("board.update.copy"),
														"aria-label": t("board.update.copy"),
														onClick: () => {
															copyUpdateCommand(index, action.command);
														},
														children: copied ? t("detail.copied") : failed ? t("detail.copyFailed") : t("board.update.copy")
													})
												]
											})] }, action.kind);
										}),
										/* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
											className: board_module_css_default.detailHint,
											children: t("board.update.restart")
										})
									]
								}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("footer", {
									className: board_module_css_default.modalFooter,
									children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
										variant: "primary",
										onClick: () => {
											setShowUpdate(false);
										},
										children: t("detail.close")
									})
								})]
							})
						]
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
						className: board_module_css_default.columns,
						ref: columnsRef,
						"data-dsh-tb-columns": "",
						onScroll: syncActiveColumn,
						children: COLUMNS.map((column) => {
							const tasks = visible.filter((task) => task.status === column.status).sort((a, b) => a.order - b.order);
							const sameColumnDrag = dragId !== void 0 && draggedTask?.status === column.status;
							return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("section", {
								className: board_module_css_default.column,
								"data-status": column.status,
								"data-dragover": dragOver === column.status ? "" : void 0,
								"data-dragreject": dragReject?.status === column.status ? "" : void 0,
								"data-dropaccept": dropAccept === column.status ? "link" : void 0,
								onDragOver: (event) => {
									const cardsEl = cardsRefs.current[column.status];
									if (cardsEl !== null && cardsEl !== void 0) autoScrollRef.current = {
										status: column.status,
										cards: cardsEl
									};
									if (externalRef.current) {
										event.preventDefault();
										if (dropGapRef.current !== void 0) {
											dropGapRef.current = void 0;
											setDropGap(void 0);
										}
										setDragOver(void 0);
										setDropAccept(column.status);
										return;
									}
									event.preventDefault();
									const decision = dragId !== void 0 && draggedTask !== void 0 ? resolveCardDrop(draggedTask, column.status) : void 0;
									const insertable = sameColumnDrag || decision?.kind === "move";
									event.dataTransfer.dropEffect = insertable ? "move" : "none";
									if (!insertable) {
										if (dropGapRef.current !== void 0) {
											dropGapRef.current = void 0;
											setDropGap(void 0);
										}
										setDragOver(column.status);
										return;
									}
									setDragOver(void 0);
									applyGap(column.status, event.clientY);
								},
								onDragLeave: (event) => {
									const related = event.relatedTarget;
									if (related !== null && event.currentTarget.contains(related)) return;
									setDragOver((current) => current === column.status ? void 0 : current);
								},
								onDrop: handleDrop(column.status),
								children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("header", {
									className: board_module_css_default.columnHeader,
									children: [
										/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
											className: board_module_css_default.statusDot,
											"data-status": column.status,
											"aria-hidden": "true"
										}),
										/* @__PURE__ */ (0, react_jsx_runtime.jsx)("h3", {
											className: board_module_css_default.columnTitle,
											title: t(COLUMN_HINT_KEY[column.status]),
											children: t(STATUS_KEY[column.status])
										}),
										/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
											className: board_module_css_default.columnCount,
											children: tasks.length
										})
									]
								}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
									className: board_module_css_default.cards,
									ref: (element) => {
										cardsRefs.current[column.status] = element;
									},
									onDragStart: (event) => {
										const id = cardIdAt(event);
										if (id !== void 0) {
											dragSourceRef.current = true;
											setDragId(id);
											setDropGap(void 0);
											dropGapRef.current = void 0;
										}
									},
									onDragEnd: clearDrag,
									children: [
										dropGap !== void 0 && dropGap.status === column.status && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
											ref: (element) => {
												indicatorRefs.current[column.status] = element;
											},
											className: board_module_css_default.dropIndicator,
											style: { top: dropGap.top },
											"aria-hidden": "true"
										}),
										tasks.map((task) => {
											const pending = taskPendingCount(task, (sessionId) => controller.pendingInteractionOf(sessionId));
											const waiting = pending.items[0]?.waitingKind;
											const pendingTitle = pending.items.length === 0 ? "" : pending.items.map((item) => {
												if (item.executionId !== void 0) {
													const index = plainRunsOf(task).findIndex((run) => run.id === item.executionId) + 1;
													return t("card.pendingItem", {
														n: String(index),
														kind: t(waitingKeyOf(item.waitingKind))
													});
												}
												return t("card.pendingRefine", { kind: t(waitingKeyOf(item.waitingKind)) });
											}).join("；");
											const relatedIds = [...controller.relatedSessionIdSet(task)];
											const dotStateOf = (sessionId) => {
												if (controller.pendingInteractionOf(sessionId) !== void 0) return "waiting";
												if (controller.nativeRunningOf(sessionId)) return "running";
												return "idle";
											};
											const dots = relatedIds.slice(0, 3).map((sessionId) => ({
												sessionId,
												state: dotStateOf(sessionId)
											}));
											const overflowDots = Math.max(0, relatedIds.length - dots.length);
											const view = cardViewModelOf(task, {
												live: controller.liveStateOf(task.id),
												pendingCount: pending.count,
												...waiting !== void 0 ? { waiting } : {},
												unviewedCount: taskUnviewedCount(task)
											});
											const nextFact = cardNextActionOf(view, task);
											const nextAction = nextFact === void 0 ? void 0 : (() => {
												switch (nextFact.kind) {
													case "waiting": return t("card.nextWaiting");
													case "running": return t("card.nextRunning");
													case "refining": return t("card.nextRefining");
													case "queued": return t("card.nextQueued", { n: String(nextFact.count ?? 0) });
													case "failed": return t("card.nextFailed");
													case "review": return t("card.nextReview");
													case "scheduled": return t("card.nextScheduled");
													case "chain": return t("card.nextChain");
												}
											})();
											return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(TaskCard, {
												task,
												live: controller.liveStateOf(task.id),
												boundTitleOf: (candidate) => {
													const binds = taskBindsOf(candidate);
													return binds.length > 0 ? controller.boundSourceTitleOf(binds[0]) : "";
												},
												workspaceTitleOf,
												waiting,
												pendingCount: pending.count,
												pendingTitle,
												unviewed: taskUnviewed(task),
												unviewedCount: taskUnviewedCount(task),
												hasUnviewedRun: taskUnviewedCount(task) > 0,
												awaitingDecision: view.awaitingDecision,
												onMoveStep: (direction) => {
													stepCard(task, direction);
												},
												selected: selectedCards.includes(task.id),
												onClick: (event) => {
													cardClick(task.id, event);
												},
												onQuickRun: taskExecutable(task) ? () => {
													controller.rerunTask(task.id);
												} : void 0,
												onColorPick: (color) => {
													controller.setTaskColor(task.id, color);
												},
												dots,
												overflowDots,
												nextAction,
												dotTitleOf: (sessionId) => {
													const title = controller.sessionTitle(sessionId) ?? sessionId;
													const state = dotStateOf(sessionId);
													return state === "waiting" ? `${title} · ${t(waitingKeyOf(controller.pendingInteractionOf(sessionId)))}` : state === "running" ? `${title} · ${t("detail.result.running")}` : title;
												}
											}, task.id);
										}),
										tasks.length === 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
											className: board_module_css_default.columnEmpty,
											role: "status",
											children: [
												snapshot.tasks.length === 0 ? t("board.emptyFirstRun") : filter.trim() !== "" ? t("board.emptyFiltered") : t("board.empty"),
												snapshot.tasks.length === 0 && column.status === "backlog" && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
													className: board_module_css_default.columnEmptyHint,
													children: t("board.emptyFirstRunHint")
												}),
												snapshot.tasks.length > 0 && filter.trim() === "" && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
													className: board_module_css_default.columnEmptyHint,
													children: t(COLUMN_HINT_KEY[column.status])
												})
											]
										})
									]
								})]
							}, column.status);
						})
					}),
					selected !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(TaskDetail, {
						controller,
						task: selected,
						workspaceTitleOf,
						dragSourceRef,
						requestSessionId: detailSessionRequest !== void 0 && detailSessionRequest.taskId === selected.id ? detailSessionRequest.sessionId : void 0,
						onRequestSessionConsumed: () => {
							setDetailSessionRequest(void 0);
						}
					}),
					showNew && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(NewTaskModal, {
						controller,
						onClose: () => {
							setShowNew(false);
						}
					}),
					showAutomation && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(AutomationPanel, {
						controller,
						onClose: () => {
							setShowAutomation(false);
						}
					}),
					showNotify && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Dialog, {
						title: t("board.notify"),
						label: t("board.notify"),
						onClose: () => {
							setShowNotify(false);
							setSnoozed({});
						},
						portal: true,
						children: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
							className: board_module_css_default.modalScroll,
							children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
								className: board_module_css_default.feedTools,
								role: "group",
								"aria-label": t("board.notify"),
								children: [
									/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
										className: board_module_css_default.feedFilterGroup,
										children: [
											"all",
											"waiting",
											"review"
										].map((kind) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
											type: "button",
											className: board_module_css_default.feedFilter,
											"data-active": notifyFilter === kind ? "" : void 0,
											"aria-pressed": notifyFilter === kind,
											onClick: () => {
												setNotifyFilter(kind);
											},
											children: t(`board.notifyFilter.${kind}`)
										}, kind))
									}),
									notes.length > 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
										type: "button",
										className: board_module_css_default.feedAction,
										onClick: () => {
											controller.markAllViewed();
										},
										title: t("board.notifyMarkAllTitle"),
										children: t("board.notifyMarkAll")
									}),
									visibleNotes.some((note) => note.kind === "review") && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
										type: "button",
										className: board_module_css_default.feedAction,
										onClick: () => {
											controller.markTasksViewed([...new Set(visibleNotes.filter((note) => note.kind === "review").map((note) => note.taskId))]);
										},
										title: t("board.notifyMarkGroupTitle"),
										children: t("board.notifyMarkGroup")
									})
								]
							}), visibleNotes.length === 0 ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
								className: board_module_css_default.detailText,
								children: t(notes.length === 0 ? "board.notifyEmpty" : "board.notifyFilterEmpty")
							}) : /* @__PURE__ */ (0, react_jsx_runtime.jsx)("ul", {
								className: board_module_css_default.notifyList,
								children: (() => {
									const renderNotifyRow = (note) => {
										const key = noteKeyOf(note);
										const taskTitle = titleOrUntitled(note.taskTitle, t("card.untitled"));
										return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("li", { children: [
											/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
												className: board_module_css_default.notifyRow,
												"data-kind": note.kind,
												children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("button", {
													type: "button",
													className: board_module_css_default.notifyMain,
													title: note.taskTitle,
													"aria-label": taskTitle,
													onClick: () => {
														setShowNotify(false);
														openTaskAtSession(note.taskId, note.sessionId);
													},
													children: [
														/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
															className: board_module_css_default.notifyTask,
															children: taskTitle
														}),
														note.kind === "waiting" && note.waitingKind !== void 0 ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Chip, {
															kind: "warn",
															fill: false,
															children: t(waitingKeyOf(note.waitingKind))
														}) : /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Chip, {
															kind: note.result === "failed" ? "error" : "success",
															fill: false,
															children: t(note.result === "failed" ? "board.notifyReviewFailed" : "board.notifyReview")
														}),
														/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
															className: board_module_css_default.notifySession,
															title: note.sessionId,
															children: note.sessionTitle
														})
													]
												}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
													className: board_module_css_default.notifyActions,
													children: note.kind === "waiting" ? /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
														type: "button",
														className: board_module_css_default.feedAction,
														onClick: () => {
															if (!controller.openSession(note.sessionId)) setFailedSession(note.sessionId);
														},
														children: t("board.notifyGoSession")
													}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
														type: "button",
														className: board_module_css_default.feedAction,
														onClick: () => {
															setSnoozed((current) => ({
																...current,
																[key]: note.at
															}));
														},
														title: t("board.notifySnoozeTitle"),
														children: t("board.notifySnooze")
													})] }) : /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
														type: "button",
														className: board_module_css_default.feedAction,
														onClick: () => {
															if (controller.approveTask(note.taskId)) setApproveBlockedKey(void 0);
															else setApproveBlockedKey(key);
														},
														children: t("board.notifyApprove")
													}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
														type: "button",
														className: board_module_css_default.feedAction,
														onClick: () => {
															controller.markTaskViewed(note.taskId);
														},
														children: t("board.notifyMarkOne")
													})] })
												})]
											}),
											failedSession === note.sessionId && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
												className: board_module_css_default.detailHint,
												children: t("detail.sessionUnavailable")
											}),
											approveBlockedKey === key && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
												className: board_module_css_default.detailHint,
												children: t("board.notifyApproveBlocked")
											})
										] }, key);
									};
									return foldedVisible.map((entry) => {
										if (entry.count === 1) return renderNotifyRow(entry.head);
										const foldedOpen = expandedFoldKey === entry.head.taskId;
										const head = entry.head;
										const headTitle = titleOrUntitled(head.taskTitle, t("card.untitled"));
										const headWaiting = entry.items.find((item) => item.kind === "waiting");
										const headHasReview = entry.items.some((item) => item.kind === "review");
										return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("li", { children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
											className: board_module_css_default.notifyRow,
											"data-kind": head.kind,
											children: [
												/* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
													type: "button",
													className: board_module_css_default.feedAction,
													"aria-expanded": foldedOpen,
													"aria-label": headTitle,
													"aria-controls": `dsh-tb-notify-fold-${entry.head.taskId}`,
													onClick: () => {
														setExpandedFoldKey((current) => current === entry.head.taskId ? void 0 : entry.head.taskId);
													},
													children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, {
														name: "chevronDown",
														className: board_module_css_default.detailChevron
													})
												}),
												/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("button", {
													type: "button",
													className: board_module_css_default.notifyMain,
													title: head.taskTitle,
													"aria-label": headTitle,
													onClick: () => {
														setShowNotify(false);
														openTaskAtSession(head.taskId, head.sessionId);
													},
													children: [
														/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
															className: board_module_css_default.notifyTask,
															children: headTitle
														}),
														/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Chip, {
															kind: "neutral",
															fill: false,
															children: `×${entry.count}`
														}),
														/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
															className: board_module_css_default.notifySession,
															title: head.sessionId,
															children: head.sessionTitle
														})
													]
												}),
												/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
													className: board_module_css_default.notifyActions,
													children: [headWaiting !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
														type: "button",
														className: board_module_css_default.feedAction,
														onClick: () => {
															if (!controller.openSession(headWaiting.sessionId)) setFailedSession(headWaiting.sessionId);
														},
														children: t("board.notifyGoSession")
													}), headHasReview && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
														type: "button",
														className: board_module_css_default.feedAction,
														onClick: () => {
															controller.markTaskViewed(head.taskId);
														},
														children: t("board.notifyMarkOne")
													})]
												})
											]
										}), foldedOpen && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("ul", {
											className: board_module_css_default.notifyList,
											id: `dsh-tb-notify-fold-${entry.head.taskId}`,
											children: (() => {
												const split = splitGroupItems(entry.items);
												return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [split.shown.map(renderNotifyRow), split.rest > 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("li", { children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
													className: board_module_css_default.detailHint,
													children: t("board.activityGroupRest", { n: String(split.rest) })
												}) }, remainderKeyOf(entry.head.taskId))] });
											})()
										})] }, entry.head.taskId);
									});
								})()
							})]
						})
					}),
					showActivity && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Dialog, {
						title: t("board.activity"),
						label: t("board.activity"),
						onClose: () => {
							setShowActivity(false);
						},
						portal: true,
						children: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
							className: board_module_css_default.modalScroll,
							children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
								className: board_module_css_default.feedTools,
								role: "group",
								"aria-label": t("board.activity"),
								children: [
									/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
										className: board_module_css_default.feedFilterGroup,
										children: [
											"all",
											"run",
											"comment",
											"other"
										].map((kind) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
											type: "button",
											className: board_module_css_default.feedFilter,
											"data-active": activityKind === kind ? "" : void 0,
											"aria-pressed": activityKind === kind,
											onClick: () => {
												setFeedBase(-1);
												setActivityKind(kind);
											},
											children: t(`board.activityFilter.${kind}`)
										}, kind))
									}),
									/* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
										type: "button",
										className: board_module_css_default.feedFilter,
										"data-active": activityUnviewed ? "" : void 0,
										"aria-pressed": activityUnviewed,
										onClick: () => {
											setFeedBase(-1);
											setActivityUnviewed((current) => !current);
										},
										children: t("board.activityUnviewed")
									}),
									/* @__PURE__ */ (0, react_jsx_runtime.jsx)("input", {
										className: board_module_css_default.feedSearch,
										type: "search",
										placeholder: t("board.activitySearch"),
										value: activityQuery,
										onChange: (event) => {
											setFeedBase(-1);
											setActivityQuery(event.target.value);
										},
										"aria-label": t("board.activitySearch")
									})
								]
							}), (() => {
								const live = activityFeed;
								const { frozen: feed, fresh } = freezeFeed(live, feedBase);
								if (feed.length === 0) return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
									className: board_module_css_default.detailText,
									children: t("board.activityEmpty")
								});
								const shown = feed.slice(0, activityShown);
								const todayBucket = dayBucketOf(Date.now());
								const groups = /* @__PURE__ */ new Map();
								for (const item of shown) {
									const day = dayBucketOf(item.at);
									const list = groups.get(day);
									if (list !== void 0) list.push(item);
									else groups.set(day, [item]);
								}
								const renderActivityRow = (item) => {
									const chip = activityChipOf(item);
									const expanded = expandedActivityKey === item.key;
									const title = titleOrUntitled(item.taskTitle, t("card.untitled"));
									return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("li", { children: [
										/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
											className: board_module_css_default.notifyRow,
											"data-kind": item.kind,
											children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("button", {
												type: "button",
												className: board_module_css_default.notifyMain,
												title: item.taskTitle,
												"aria-label": title,
												"aria-expanded": expanded,
												"aria-controls": `dsh-tb-activity-${item.key}`,
												onClick: () => {
													setExpandedActivityKey((current) => current === item.key ? void 0 : item.key);
												},
												children: [
													/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
														className: board_module_css_default.notifyTask,
														children: title
													}),
													/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Chip, {
														kind: chip.kind,
														fill: false,
														children: chip.label
													}),
													/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
														className: board_module_css_default.notifySession,
														title: item.text ?? "",
														children: item.text !== void 0 && item.text.trim() !== "" ? item.text.slice(0, 24) : formatTime(item.at)
													})
												]
											}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
												className: board_module_css_default.notifyActions,
												children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
													type: "button",
													className: board_module_css_default.feedAction,
													onClick: () => {
														setShowActivity(false);
														openTaskAtSession(item.taskId, item.sessionId);
													},
													children: t("board.activityOpen")
												}), item.sessionId !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
													type: "button",
													className: board_module_css_default.feedAction,
													onClick: () => {
														if (!controller.openSession(item.sessionId)) setFailedSession(item.sessionId);
													},
													children: t("board.notifyGoSession")
												})]
											})]
										}),
										expanded && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
											className: board_module_css_default.feedPreview,
											id: `dsh-tb-activity-${item.key}`,
											children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
												className: board_module_css_default.detailText,
												children: item.text !== void 0 && item.text.trim() !== "" ? item.text : formatDateTime(item.at)
											}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
												className: board_module_css_default.detailHint,
												children: t("board.activityPreviewHint", { time: formatDateTime(item.at) })
											})]
										}),
										failedSession === item.sessionId && item.sessionId !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
											className: board_module_css_default.detailHint,
											children: t("detail.sessionUnavailable")
										})
									] }, item.key);
								};
								const renderObjectGroup = (group) => {
									if (group.items.length <= 1) return group.items.length === 1 ? renderActivityRow(group.items[0]) : null;
									const groupExpanded = expandedGroupKey === group.key;
									const title = titleOrUntitled(group.taskTitle, t("card.untitled"));
									const split = groupExpanded ? splitGroupItems(group.items) : {
										shown: [],
										rest: 0
									};
									return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("li", { children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
										className: board_module_css_default.notifyRow,
										"data-kind": group.items[0].kind,
										children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("button", {
											type: "button",
											className: board_module_css_default.notifyMain,
											title: group.taskTitle,
											"aria-label": title,
											"aria-expanded": groupExpanded,
											"aria-controls": `dsh-tb-activity-group-${group.key}`,
											onClick: () => {
												setExpandedGroupKey((current) => current === group.key ? void 0 : group.key);
											},
											children: [
												/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Icon, {
													name: "chevronDown",
													className: board_module_css_default.detailChevron
												}),
												/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
													className: board_module_css_default.notifyTask,
													children: title
												}),
												/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Chip, {
													kind: "neutral",
													fill: false,
													children: `×${group.items.length}`
												}),
												/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
													className: board_module_css_default.notifySession,
													children: formatTime(group.items[0].at)
												})
											]
										}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
											className: board_module_css_default.notifyActions,
											children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
												type: "button",
												className: board_module_css_default.feedAction,
												onClick: () => {
													setShowActivity(false);
													openTaskAtSession(group.taskId, void 0);
												},
												children: t("board.activityOpen")
											})
										})]
									}), groupExpanded && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("ul", {
										className: board_module_css_default.notifyList,
										id: `dsh-tb-activity-group-${group.key}`,
										children: [split.shown.map(renderActivityRow), split.rest > 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("li", { children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
											className: board_module_css_default.detailHint,
											children: t("board.activityGroupRest", { n: String(split.rest) })
										}) }, remainderKeyOf(group.key))]
									})] }, group.key);
								};
								return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [
									fresh > 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
										type: "button",
										className: board_module_css_default.feedAction,
										onClick: () => {
											setFeedBase(live.length);
										},
										children: t("board.activityNew", { n: String(fresh) })
									}),
									[...groups.entries()].map(([day, items]) => /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("section", {
										"aria-label": dayLabelOf(day, todayBucket),
										children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("h3", {
											className: board_module_css_default.feedDay,
											children: dayLabelOf(day, todayBucket)
										}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("ul", {
											className: board_module_css_default.notifyList,
											children: groupActivityByObjectDay(items, dayBucketOf).map(renderObjectGroup)
										})]
									}, day)),
									feed.length > shown.length && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
										type: "button",
										className: board_module_css_default.feedAction,
										onClick: () => {
											setActivityShown((current) => current + 30);
										},
										children: t("board.activityMore", { n: String(feed.length - shown.length) })
									})
								] });
							})()]
						})
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("nav", {
						className: board_module_css_default.thumbBar,
						"aria-label": t("board.thumbBar"),
						children: [
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
								className: board_module_css_default.thumbNew,
								children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
									variant: "primary",
									onClick: () => {
										setShowNew(true);
									},
									children: t("board.new")
								})
							}),
							renderNotifyBell(),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Button, {
								variant: "ghost",
								title: t("board.activityTitle"),
								onClick: () => {
									setFeedBase(-1);
									setShowActivity(true);
								},
								children: t("board.activity")
							})
						]
					})
				]
			});
		}
		//#endregion
		//#region src/client/board/keyboard-inset.ts
		/**
		* Keyboard inset — the ONE mechanism keeping floating panels fully visible
		* under the mobile soft keyboard.
		*
		* When a text field takes focus on a phone, the keyboard eats the bottom of
		* the VISUAL viewport while the layout viewport (and every absolute panel
		* sized against it) stays whole — the panel's header, then its title, slide
		* above the screen edge (the 「添加已有会话」标题被顶没 report). The watcher
		* measures the overlap from `visualViewport` and writes it into
		* `--dsh-tb-kb`; the floating backdrop adds that to its bottom padding, so
		* every capped panel simply shrinks to the space the keyboard leaves —
		* header on top, actions at the bottom, nothing buried. One variable, all
		* overlays follow (no per-dialog keyboard plumbing anywhere).
		*
		* The noise floor separates a keyboard (≥ ~150px) from the browser's dynamic
		* toolbars (~60-120px of height change), which must NOT resize the stage.
		*/
		/** Pure overlap math: keyboard pixels occluding the bottom of the layout box. */
		function keyboardOverlapPx(innerHeight, visibleHeight, visibleTop, noiseFloorPx = 150) {
			const overlap = innerHeight - visibleHeight - visibleTop;
			return overlap >= noiseFloorPx ? Math.round(overlap) : 0;
		}
		/** Watch and write `--dsh-tb-kb` on `root`. @returns the disposer. */
		function watchKeyboardInset(root) {
			const viewport = globalThis.visualViewport;
			if (viewport === void 0 || viewport === null) return () => {};
			const apply = () => {
				const px = keyboardOverlapPx(globalThis.innerHeight, viewport.height, viewport.offsetTop);
				if (px === 0) root.style.removeProperty("--dsh-tb-kb");
				else root.style.setProperty("--dsh-tb-kb", `${px}px`);
			};
			apply();
			viewport.addEventListener("resize", apply);
			viewport.addEventListener("scroll", apply);
			return () => {
				viewport.removeEventListener("resize", apply);
				viewport.removeEventListener("scroll", apply);
				root.style.removeProperty("--dsh-tb-kb");
			};
		}
		//#endregion
		//#region src/client/board-mount.tsx
		/**
		* Board view mounting.
		*
		* The `conversation` slot is single-occupant (ui-conversation) and external
		* plugins cannot declare slots, so the board takes over the center column at
		* the DOM level: a container is appended inside the `[data-pane="conversation"]`
		* grid item (an extra trailing child React never manages), and a stylesheet
		* rule hides the conversation content while the board is active. Toggling is
		* a data attribute on <html> — no React involvement, so the conversation
		* subtree underneath stays mounted and stateful.
		*/
		/** The center column: the official `data-pane="conversation"` marker first,
		* then the css-module class (legacy shells without the marker). The plugin
		* never depends on an external compatibility shim.
		*/
		const CONVERSATION_COLUMN_SELECTOR = "[data-pane=\"conversation\"], [class*=\"centerCol\"]";
		const ACTIVE_ATTR = "data-dsh-taskboard-active";
		/** Find the center column, or undefined while the frame is not mounted. */
		function conversationColumn() {
			return document.querySelector(CONVERSATION_COLUMN_SELECTOR) ?? void 0;
		}
		/**
		* Mount the board React tree into the center column and bind its visibility
		* to the controller's boardOpen state.
		* @param controller - the board controller driving the view.
		* @param freshness - the stale-bundle verdict to render (see bundle-freshness).
		* @returns disposer unmounting the tree and restoring the column.
		*/
		function mountBoard(controller, freshness) {
			let root;
			let container;
			let detachKeyboardInset;
			const ensure = () => {
				if (container !== void 0) return;
				const column = conversationColumn();
				if (column === void 0) return;
				container = document.createElement("div");
				container.dataset.dshTaskboardView = "";
				column.appendChild(container);
				detachKeyboardInset = watchKeyboardInset(document.documentElement);
				root = (0, react_dom_client.createRoot)(container);
				root.render(/* @__PURE__ */ (0, react_jsx_runtime.jsx)(TaskBoard, {
					controller,
					freshness
				}));
			};
			const waitObserver = new MutationObserver(() => {
				ensure();
			});
			waitObserver.observe(document.body, {
				childList: true,
				subtree: true
			});
			const applyActive = () => {
				if (controller.getSnapshot().boardOpen) document.documentElement.setAttribute(ACTIVE_ATTR, "");
				else document.documentElement.removeAttribute(ACTIVE_ATTR);
			};
			const unsubscribe = controller.subscribe(applyActive);
			applyActive();
			ensure();
			return () => {
				waitObserver.disconnect();
				unsubscribe();
				detachKeyboardInset?.();
				detachKeyboardInset = void 0;
				document.documentElement.removeAttribute(ACTIVE_ATTR);
				root?.unmount();
				root = void 0;
				container?.remove();
				container = void 0;
			};
		}
		//#endregion
		//#region src/client/SidebarFooter.tsx
		/**
		* The footer action controller: owns the display snapshot and the toggle
		* callback. The toggle is a settable handle — mountUiBody binds the real
		* board controller later, so this controller can be created (and the slot
		* registered) before the heavier board stack exists. A click before binding
		* is QUEUED (pendingOpen) and fires on bindBoard — the slow-network window
		* where the board is still syncing never eats a user's tap.
		*/
		var SidebarFooterController = class {
			store;
			open = false;
			pending = false;
			toggleFn;
			/** Bind the board's toggle callback (and its open state) once the UI mounts.
			*  A queued click fires immediately; the pending flag clears. */
			bindBoard(open, toggle) {
				this.toggleFn = toggle;
				this.setOpen(open());
				if (this.pending) {
					this.pending = false;
					this.publish();
					toggle();
				}
			}
			/** Reflect a board open/close transition into the row's highlight. */
			setOpen(open) {
				if (this.open === open) return;
				this.open = open;
				this.publish();
			}
			/** Detach the board and clear the highlight (disposal path). */
			dispose() {
				this.toggleFn = void 0;
				this.pending = false;
				this.open = false;
				this.store?.set({
					boardOpen: false,
					pendingOpen: false
				});
			}
			/**
			* Build the face the slot registration injects. Called once by the slot
			* machinery; the component receives the snapshot store via useSidebarFooter.
			*/
			inject() {
				this.store ??= createSnapshotStore({
					boardOpen: this.open,
					pendingOpen: false
				});
				return {
					hooks: { sidebarFooter: this.store },
					toggle: () => {
						if (this.toggleFn !== void 0) {
							this.toggleFn();
							return;
						}
						if (!this.pending) {
							this.pending = true;
							this.publish();
						}
					}
				};
			}
			publish() {
				this.store?.set({
					boardOpen: this.open,
					pendingOpen: this.pending
				});
			}
		};
		/**
		* Render the sidebar footer action: a labeled row in the wide column, an icon
		* in the rail/collapsed form. The shell supplies `wide` (its column state);
		* the accent shows while the board is open, and the pending treatment while a
		* queued click is waiting for the board to bind (slow network).
		*/
		function SidebarFooter(props) {
			const state = props.useSidebarFooter((snapshot) => snapshot);
			const label = t("entry.label");
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("button", {
				type: "button",
				className: board_module_css_default.sidebarFooterAction,
				"data-pending": state.pendingOpen ? "true" : void 0,
				"aria-label": state.pendingOpen ? t("entry.opening") : label,
				title: label,
				onClick: props.toggle,
				children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
					className: board_module_css_default.sidebarFooterIcon,
					"aria-hidden": "true",
					children: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("svg", {
						viewBox: "0 0 16 16",
						width: "16",
						height: "16",
						fill: "none",
						stroke: "currentColor",
						strokeWidth: "1.3",
						strokeLinecap: "round",
						strokeLinejoin: "round",
						children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("rect", {
							x: "2",
							y: "2.5",
							width: "12",
							height: "11",
							rx: "1.5"
						}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("path", { d: "M2 6.5h12M6.5 6.5v7" })]
					})
				}), props.wide === true ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
					className: board_module_css_default.sidebarFooterLabel,
					children: label
				}) : null]
			});
		}
		//#endregion
		//#region src/client/route-scope.ts
		/** Build an `unavailable` snapshot. */
		function unavailableSnapshot() {
			return {
				status: "unavailable",
				value: void 0,
				base: void 0,
				user: void 0,
				revision: void 0,
				writable: false,
				mode: "memory"
			};
		}
		/** Translate a route view into a client scope snapshot. */
		function toSnapshot(view) {
			if (view === void 0 || !view.available) return unavailableSnapshot();
			return {
				status: "ready",
				value: view.value,
				base: view.base,
				user: view.user,
				revision: view.revision,
				writable: view.writable ?? false,
				mode: "host"
			};
		}
		/**
		* A reactive settings scope backed by the plugin's host route. It mirrors the
		* shape of `SettingsScope<T>` (getSnapshot/subscribe/set/unset/dispose) so the
		* card form and fixture-free tests can treat it as a drop-in.
		*/
		var RouteSettingsScope = class {
			namespace;
			snapshot = {
				status: "loading",
				value: void 0,
				base: void 0,
				user: void 0,
				revision: void 0,
				writable: false,
				mode: "host"
			};
			listeners = /* @__PURE__ */ new Set();
			disposed = false;
			/**
			* Serialized request lane: the initial load and every mutation run one at a
			* time, so a slow load response can never overwrite the fresh view of a
			* write that ran after it, and a write's revision always comes from the
			* snapshot the previous request settled (the SDK scope's write queue).
			*/
			lane = Promise.resolve();
			/**
			* @param namespace - the settings namespace this scope reads (route path tail).
			*/
			constructor(namespace) {
				this.namespace = namespace;
				this.enqueue(() => this.load());
			}
			/** @returns the current snapshot (stable reference until the next change). */
			getSnapshot() {
				return this.snapshot;
			}
			/**
			* Observe snapshot replacements.
			* @param listener - invoked after each snapshot change.
			* @returns the disposer removing this listener.
			*/
			subscribe(listener) {
				this.listeners.add(listener);
				return () => {
					this.listeners.delete(listener);
				};
			}
			/**
			* Queue one field write through the route and adopt the returned fresh view.
			* The write carries the latest known namespace revision for stale-write
			* fencing, matching the SDK scope's revision contract.
			* @param field - scalar field inside the namespace section.
			* @param value - JSON-shaped value selected by the user.
			*/
			async set(field, value) {
				await this.enqueue(async () => {
					await this.mutate([{
						op: "set",
						path: [field],
						value
					}], this.snapshot.revision);
				});
			}
			/**
			* Queue one field clear so it re-inherits the composition layer.
			* @param field - scalar field inside the namespace section.
			*/
			async unset(field) {
				await this.enqueue(async () => {
					await this.mutate([{
						op: "unset",
						path: [field]
					}], this.snapshot.revision);
				});
			}
			/** Stop all listeners and drops further updates. */
			dispose() {
				this.disposed = true;
				this.listeners.clear();
			}
			/** Run one request on the serialized lane (order = arrival stay order). */
			enqueue(task) {
				const run = this.lane.then(task);
				this.lane = run.catch(() => void 0);
				return run;
			}
			/** Fetch the namespace view through the route and adopt it. */
			async load() {
				try {
					const response = await fetch(`/api/${this.namespace}/settings`);
					if (!response.ok) {
						this.settle(unavailableSnapshot());
						return;
					}
					const envelope = await response.json();
					this.settle(toSnapshot(envelope.ok ? envelope.value : void 0));
				} catch {
					this.settle(unavailableSnapshot());
				}
			}
			/** Send path ops through the route and adopt the returned fresh view. */
			async mutate(ops, expectedRevision) {
				try {
					const response = await fetch(`/api/${this.namespace}/settings`, {
						method: "POST",
						headers: { "content-type": "application/json" },
						body: JSON.stringify(expectedRevision === void 0 ? { ops } : {
							ops,
							expectedRevision
						})
					});
					if (!response.ok) {
						this.settle(unavailableSnapshot());
						return;
					}
					const envelope = await response.json();
					this.settle(toSnapshot(envelope.ok ? envelope.value : void 0));
				} catch {
					this.settle(unavailableSnapshot());
				}
			}
			/** Adopt a new snapshot and notify subscribers. */
			settle(next) {
				if (this.disposed) return;
				this.snapshot = next;
				for (const listener of [...this.listeners]) listener();
			}
		};
		//#endregion
		//#region \0dsh-css:src/client/settings-card.module.css.mjs
		const css = ".KApx_W_card{border:1px solid var(--dsw-alias-border-l2);background:var(--dsw-alias-bg-layer-3);border-radius:8px;list-style:none;transition:border-color .16s,background .16s;overflow:hidden}.KApx_W_header{cursor:pointer;text-align:left;width:100%;font:inherit;background:0 0;border:0;align-items:center;gap:8px;padding:10px 14px;display:flex}.KApx_W_header:hover{background:var(--dsw-alias-interactive-bg-hover)}.KApx_W_headText{flex-direction:column;flex:1;gap:2px;min-width:0;display:flex}.KApx_W_name{color:var(--dsw-alias-label-primary);font-weight:600}.KApx_W_description{color:var(--dsw-alias-label-tertiary);font-size:12px}.KApx_W_pending{color:var(--dsw-alias-state-warn-primary);font-size:12px}.KApx_W_chevron{color:var(--dsw-alias-label-tertiary);transition:transform .12s}.KApx_W_chevronOpen{transform:rotate(180deg)}.KApx_W_body{flex-direction:column;gap:14px;padding:0 14px 14px;display:flex}.KApx_W_readOnly{color:var(--dsw-alias-label-tertiary);margin:0;font-size:12px}.KApx_W_footer{justify-content:flex-end;align-items:center;gap:8px;display:flex}.KApx_W_failed{color:var(--dsw-alias-state-error-primary);margin:0 auto 0 0;font-size:12px}.KApx_W_discard,.KApx_W_save{font:inherit;cursor:pointer;border-radius:6px;padding:5px 12px;font-size:13px}.KApx_W_discard{border:1px solid var(--dsw-alias-border-l2);color:var(--dsw-alias-label-secondary);background:0 0}.KApx_W_discard:hover:not(:disabled){color:var(--dsw-alias-label-primary);border-color:var(--dsw-alias-label-dimmed)}.KApx_W_save{border:1px solid var(--dsw-alias-button-info-fill);background:var(--dsw-alias-button-info-fill);color:var(--dsw-alias-label-primary-foreground)}.KApx_W_save:hover:not(:disabled){border-color:var(--dsw-alias-button-info-hover);background:var(--dsw-alias-button-info-hover)}.KApx_W_discard:active:not(:disabled),.KApx_W_save:active:not(:disabled){transform:translateY(1px)}.KApx_W_discard:focus-visible:not(:disabled),.KApx_W_save:focus-visible:not(:disabled){outline:2px solid var(--dsw-alias-state-business-primary);outline-offset:-2px}.KApx_W_discard:disabled,.KApx_W_save:disabled{opacity:.5;cursor:default}.KApx_W_field{flex-direction:column;gap:4px;display:flex}.KApx_W_head{align-items:center;gap:8px;display:flex}.KApx_W_label{color:var(--dsw-alias-label-primary);font-size:13px;font-weight:500}.KApx_W_badges{align-items:center;gap:8px;display:flex}.KApx_W_badge{background:var(--dsw-specific-input-major);color:var(--dsw-alias-label-secondary);border-radius:999px;padding:1px 6px;font-size:11px}.KApx_W_reset{color:var(--dsw-alias-state-business-primary);cursor:pointer;background:0 0;border:0;justify-content:center;align-items:center;min-height:24px;margin:0 -6px;padding:0 6px;font-size:11px;display:inline-flex}.KApx_W_reset:hover:not(:disabled){text-decoration:underline}.KApx_W_reset:active:not(:disabled){opacity:.8;text-decoration:underline}.KApx_W_reset:focus-visible{outline:2px solid var(--dsw-alias-state-business-primary);outline-offset:-2px}.KApx_W_reset:disabled{opacity:.5;cursor:default}.KApx_W_input,.KApx_W_select{border:1px solid var(--dsw-alias-border-l2);font:inherit;color:var(--dsw-alias-label-primary);background:var(--dsw-specific-input-major);border-radius:6px;padding:6px 8px;font-size:13px}.KApx_W_input:disabled,.KApx_W_select:disabled{opacity:.6}.KApx_W_hint{color:var(--dsw-alias-label-secondary);margin:0;font-size:12px}.KApx_W_header:focus-visible{outline:2px solid var(--dsw-alias-state-business-primary);outline-offset:-2px}.KApx_W_header:active{background:var(--dsw-alias-interactive-bg-hover)}.KApx_W_header,.KApx_W_discard,.KApx_W_save,.KApx_W_reset{transition:background-color .12s,color .12s,border-color .12s,transform .12s}@media (prefers-reduced-motion:reduce){.KApx_W_card,.KApx_W_header,.KApx_W_chevron,.KApx_W_discard,.KApx_W_save,.KApx_W_reset{transition:none}}";
		const tagId = "@firetruck666/dsh-task-board/settings-card.module.css";
		if (typeof document !== "undefined" && document.querySelector("style[data-plugin-css=" + JSON.stringify(tagId) + "]") === null) {
			const tag = document.createElement("style");
			tag.dataset.plugin = "@firetruck666/dsh-task-board";
			tag.dataset.pluginCss = tagId;
			tag.textContent = css;
			document.head.appendChild(tag);
		}
		var settings_card_module_css_default = {
			"badge": "KApx_W_badge",
			"badges": "KApx_W_badges",
			"body": "KApx_W_body",
			"card": "KApx_W_card",
			"chevron": "KApx_W_chevron",
			"chevronOpen": "KApx_W_chevronOpen",
			"description": "KApx_W_description",
			"discard": "KApx_W_discard",
			"failed": "KApx_W_failed",
			"field": "KApx_W_field",
			"footer": "KApx_W_footer",
			"head": "KApx_W_head",
			"headText": "KApx_W_headText",
			"header": "KApx_W_header",
			"hint": "KApx_W_hint",
			"input": "KApx_W_input",
			"label": "KApx_W_label",
			"name": "KApx_W_name",
			"pending": "KApx_W_pending",
			"readOnly": "KApx_W_readOnly",
			"reset": "KApx_W_reset",
			"save": "KApx_W_save",
			"select": "KApx_W_select"
		};
		//#endregion
		//#region src/client/PluginSettingsCard.tsx
		/**
		* Shared chrome for the plugin settings card: a disclosure header naming the
		* plugin and what its settings govern, the controls inside, and the save that
		* writes them. Renders always — an unavailable namespace shows a hint in place
		* of the controls rather than disappearing. Mirrors the official ui-plugin-config
		* PluginCard in a self-contained slice (this package must not depend on a
		* sibling UI package).
		*/
		/**
		* Render one plugin settings card.
		* @param props - the plugin's copy keys, its form state, and its controls.
		* @returns the card; an unavailable namespace renders a hint instead of controls.
		*/
		function PluginSettingsCard(props) {
			const [open, setOpen] = (0, react.useState)(false);
			const { state } = props;
			const title = props.t(props.titleKey);
			const bodyId = (0, react.useId)();
			if (!state.available) return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("li", {
				className: settings_card_module_css_default.card,
				children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("button", {
					type: "button",
					className: settings_card_module_css_default.header,
					"aria-expanded": open,
					"aria-controls": bodyId,
					"aria-label": `${props.t(open ? "settings.collapse" : "settings.expand")}: ${title}`,
					onClick: () => {
						setOpen(!open);
					},
					children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
						className: settings_card_module_css_default.headText,
						children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
							className: settings_card_module_css_default.name,
							children: title
						}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
							className: settings_card_module_css_default.description,
							children: props.t(props.descriptionKey)
						})]
					}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: open ? settings_card_module_css_default.chevronOpen : settings_card_module_css_default.chevron,
						children: "▾"
					})]
				}), open ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
					className: settings_card_module_css_default.body,
					id: bodyId,
					children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
						className: settings_card_module_css_default.readOnly,
						role: "status",
						children: props.t("settings.unavailable")
					})
				}) : null]
			});
			const blocked = !state.dirty || state.invalid || state.saving;
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("li", {
				className: settings_card_module_css_default.card,
				children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("button", {
					type: "button",
					className: settings_card_module_css_default.header,
					"aria-expanded": open,
					"aria-controls": bodyId,
					"aria-label": `${props.t(open ? "settings.collapse" : "settings.expand")}: ${title}`,
					onClick: () => {
						setOpen(!open);
					},
					children: [
						/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
							className: settings_card_module_css_default.headText,
							children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
								className: settings_card_module_css_default.name,
								children: title
							}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
								className: settings_card_module_css_default.description,
								children: props.t(props.descriptionKey)
							})]
						}),
						state.dirty ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
							className: settings_card_module_css_default.pending,
							children: props.t("settings.unsaved")
						}) : null,
						/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
							className: open ? settings_card_module_css_default.chevronOpen : settings_card_module_css_default.chevron,
							children: "▾"
						})
					]
				}), open ? /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
					className: settings_card_module_css_default.body,
					id: bodyId,
					children: [
						!state.writable ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
							className: settings_card_module_css_default.readOnly,
							role: "status",
							children: props.t("settings.readOnly")
						}) : null,
						props.children,
						/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
							className: settings_card_module_css_default.footer,
							children: [
								state.failed ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
									className: settings_card_module_css_default.failed,
									role: "status",
									children: props.t("settings.saveFailed")
								}) : null,
								/* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
									type: "button",
									className: settings_card_module_css_default.discard,
									disabled: !state.dirty || state.saving,
									onClick: props.onDiscard,
									children: props.t("settings.discard")
								}),
								/* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
									type: "button",
									className: settings_card_module_css_default.save,
									disabled: blocked,
									onClick: props.onSave,
									children: props.t(!state.saving ? "settings.save" : "settings.saving")
								})
							]
						})
					]
				}) : null]
			});
		}
		/** A staged boolean field: 继承 / 开 / 关. */
		function BooleanField(props) {
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
				className: settings_card_module_css_default.field,
				children: [
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
						className: settings_card_module_css_default.head,
						children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("label", {
							className: settings_card_module_css_default.label,
							htmlFor: props.id,
							children: props.label
						}), props.overridden ? /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
							className: settings_card_module_css_default.badges,
							children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
								className: settings_card_module_css_default.badge,
								children: props.overriddenLabel
							}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
								type: "button",
								className: settings_card_module_css_default.reset,
								disabled: props.disabled,
								onClick: props.onReset,
								children: props.resetLabel
							})]
						}) : null]
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("select", {
						id: props.id,
						className: settings_card_module_css_default.select,
						value: props.text,
						disabled: props.disabled,
						onChange: (event) => {
							props.onEdit(event.target.value);
						},
						children: [
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("option", {
								value: "",
								children: props.inheritLabel
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("option", {
								value: "true",
								children: props.onLabel
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("option", {
								value: "false",
								children: props.offLabel
							})
						]
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
						className: settings_card_module_css_default.hint,
						children: props.hint
					})
				]
			});
		}
		//#endregion
		//#region src/client/settings-form.ts
		/** A boolean field, edited through true/false draft text. */
		function booleanField(field) {
			return {
				field,
				format: (value) => typeof value === "boolean" ? String(value) : "",
				parse: (text) => {
					if (text === "true") return {
						kind: "set",
						value: true
					};
					if (text === "false") return {
						kind: "set",
						value: false
					};
				}
			};
		}
		/**
		* Stages one card's edits over one settings namespace and writes them on save.
		*
		* The Host is the only authority on whether a value was accepted — its
		* validators own the constraints no schema can express — so the outcome is
		* read back from the section rather than predicted here. A save that did not
		* land keeps its drafts, so the user can correct them instead of retyping.
		*/
		var CardForm = class {
			scope;
			specs;
			staged = /* @__PURE__ */ new Map();
			listeners = /* @__PURE__ */ new Set();
			saving = false;
			failed = false;
			/** @param scope - the bound settings scope for this card's namespace. */
			constructor(scope, specs) {
				this.scope = scope;
				this.specs = new Map(specs.map((spec) => [spec.field, spec]));
				scope.subscribe(() => {
					this.publish();
				});
			}
			/** Publish a projection of this form, rebuilt whenever the scope or a draft changes. */
			bind(project) {
				const store = createSnapshotStore(project());
				this.listeners.add(() => {
					store.set(project());
				});
				return store;
			}
			/** Read the card-level state: what the Host serves, and what a save would do. */
			shell() {
				const snapshot = this.scope.getSnapshot();
				const plan = this.plan();
				return {
					available: snapshot.status === "ready",
					writable: snapshot.writable,
					dirty: plan.length > 0,
					invalid: plan.some((item) => item.run === void 0),
					saving: this.saving,
					failed: this.failed
				};
			}
			/** Read one field's state from the effective section and its staged draft. */
			field(field) {
				const spec = this.specOf(field);
				const staged = this.staged.get(field);
				if (staged === void 0) return {
					text: spec.format(this.sectionValue(field)),
					overridden: this.stored(field),
					invalid: false
				};
				const write = staged.clear ? { kind: "clear" } : spec.parse(staged.text);
				return {
					text: staged.text,
					overridden: write?.kind === "set",
					invalid: write === void 0
				};
			}
			/** The actions the card's slot registration injects. */
			actions() {
				return {
					edit: (field, text) => {
						this.stage(field, {
							text,
							clear: false
						});
					},
					resetField: (field) => {
						this.stage(field, {
							text: this.specOf(field).format(this.baseValue(field)),
							clear: true
						});
					},
					save: () => {
						this.save();
					},
					discard: () => {
						if (this.staged.size === 0 && !this.failed) return;
						this.staged.clear();
						this.failed = false;
						this.publish();
					}
				};
			}
			/**
			* Write every staged edit, then re-seed from what the Host accepted.
			* @returns settlement after every write and the read-back.
			*/
			async save() {
				const plan = this.plan();
				const writes = plan.flatMap((item) => item.run === void 0 ? [] : [item.run]);
				if (plan.length === 0 || this.saving || writes.length !== plan.length) return;
				this.saving = true;
				this.failed = false;
				this.publish();
				let landed = true;
				for (const write of writes) landed = await write() && landed;
				if (landed) this.staged.clear();
				this.saving = false;
				this.failed = !landed;
				this.publish();
			}
			/**
			* Every staged edit a save would write. An entry whose draft is not a value
			* its field accepts carries no write: the form is still dirty, and the save
			* refuses rather than dropping the edit. A staged edit that matches the
			* effective section is not a write at all.
			* @returns the planned writes, in the order the fields were staged.
			*/
			plan() {
				const plan = [];
				for (const [field, staged] of this.staged) {
					const spec = this.specOf(field);
					if (staged.clear) {
						if (this.stored(field)) plan.push({
							field,
							run: () => this.clear(field)
						});
						continue;
					}
					if (staged.text === spec.format(this.sectionValue(field))) continue;
					const write = spec.parse(staged.text);
					if (write === void 0) plan.push({
						field,
						run: void 0
					});
					else if (write.kind === "clear") plan.push({
						field,
						run: () => this.clear(field)
					});
					else plan.push({
						field,
						run: () => this.store(field, write.value)
					});
				}
				return plan;
			}
			async clear(field) {
				await this.scope.unset(field);
				return !this.stored(field);
			}
			async store(field, value) {
				await this.scope.set(field, value);
				return this.userLayer()?.[field] === value;
			}
			stage(field, edit) {
				this.staged.set(field, edit);
				this.failed = false;
				this.publish();
			}
			specOf(field) {
				const spec = this.specs.get(field);
				if (spec === void 0) throw new Error(`settings card has no field ${field}`);
				return spec;
			}
			snapshotOf() {
				return this.scope.getSnapshot();
			}
			sectionValue(field) {
				return this.snapshotOf().value?.[field];
			}
			baseValue(field) {
				return this.snapshotOf().base?.[field];
			}
			userLayer() {
				return this.snapshotOf().user;
			}
			stored(field) {
				const user = this.userLayer();
				return user !== void 0 && Object.hasOwn(user, field);
			}
			publish() {
				for (const listener of this.listeners) listener();
			}
		};
		//#endregion
		//#region src/client/TaskBoardSettingsCard.tsx
		/** Bridges the `dsh-task-board` scope onto the card's staged form. */
		var TaskBoardSettingsCardController = class {
			form;
			store;
			/** @param scope - the bound settings scope for the `dsh-task-board` namespace. */
			constructor(scope) {
				this.form = new CardForm(scope, [booleanField("enabled"), booleanField("announceToAgent")]);
				this.store = this.form.bind(() => this.projection());
			}
			projection() {
				return {
					...this.form.shell(),
					enabled: this.form.field("enabled"),
					announceToAgent: this.form.field("announceToAgent")
				};
			}
			/**
			* Build the face the card's slot registration injects.
			* @returns the card's snapshot and its form actions.
			*/
			inject() {
				return {
					hooks: { taskBoardSettingsCard: this.store },
					...this.form.actions()
				};
			}
		};
		/**
		* Render the task-board card.
		* @param props - locale copy, the card snapshot, and its form actions.
		* @returns the card.
		*/
		function TaskBoardSettingsCard(props) {
			const { t } = props;
			const state = props.useTaskBoardSettingsCard((snapshot) => snapshot);
			const disabled = !state.writable;
			const fieldProps = {
				overriddenLabel: t("settings.overridden"),
				resetLabel: t("settings.reset"),
				invalidLabel: t("settings.invalidNumber"),
				disabled
			};
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(PluginSettingsCard, {
				t,
				titleKey: "settings.title",
				descriptionKey: "settings.description",
				state,
				onSave: props.save,
				onDiscard: props.discard,
				children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(BooleanField, {
					id: "settings-task-board-enabled",
					label: t("settings.enabled"),
					hint: t("settings.enabledHint"),
					inheritLabel: t("settings.inherit"),
					onLabel: t("settings.on"),
					offLabel: t("settings.off"),
					...fieldProps,
					...state.enabled,
					onEdit: (text) => {
						props.edit("enabled", text);
					},
					onReset: () => {
						props.resetField("enabled");
					}
				}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(BooleanField, {
					id: "settings-task-board-announce",
					label: t("settings.announceToAgent"),
					hint: t("settings.announceToAgentHint"),
					inheritLabel: t("settings.inherit"),
					onLabel: t("settings.on"),
					offLabel: t("settings.off"),
					...fieldProps,
					...state.announceToAgent,
					onEdit: (text) => {
						props.edit("announceToAgent", text);
					},
					onReset: () => {
						props.resetField("announceToAgent");
					}
				})]
			});
		}
		//#endregion
		//#region src/client/index.ts
		/** Deployed bundle version (diagnostic only — never rendered in the UI). */
		const BOARD_VERSION = version ?? "unknown";
		/** Locale namespace this plugin owns. */
		const NS = "dsh-task-board";
		/** Settings namespace the settings card edits (the Host plugin registers it). */
		const TASK_BOARD_NS = "dsh-task-board";
		/** localStorage key for the auto-cruise state (toggle + concurrency limit). */
		const CRUISE_STORAGE_KEY = "dsh.taskBoard.cruise.v1";
		/** localStorage key where a diverging pre-sync local ledger is parked (the
		*  host truth wins on first connect; the local copy is never silently lost). */
		const PRE_SYNC_BACKUP_KEY = "dsh.taskBoard.preSync.v1";
		/** Read the four local board keys as a single view (the sync migration source). */
		function readLocalView() {
			let cruise = {
				enabled: false,
				limit: 5,
				schedule: []
			};
			try {
				const raw = localStorage.getItem(CRUISE_STORAGE_KEY);
				if (raw !== null) {
					const parsed = JSON.parse(raw);
					cruise = {
						enabled: parsed.enabled === true,
						...typeof parsed.manual === "boolean" ? { manual: parsed.manual } : {},
						limit: typeof parsed.limit === "number" && Number.isInteger(parsed.limit) && parsed.limit >= 1 ? parsed.limit : 5,
						schedule: Array.isArray(parsed.schedule) ? parsed.schedule : []
					};
				}
			} catch (error) {
				console.error("[dsh-task-board] local cruise read failed", error);
			}
			return {
				tasks: new LocalStorageTaskStore().load(),
				cruise,
				schedulePresets: new LocalStoragePresetStore().load(),
				runPresets: new LocalStorageRunPresetStore().load()
			};
		}
		/** Write the four local board keys from a view (the offline first-paint mirror). */
		function writeMirror(view) {
			new LocalStorageTaskStore().save(view.tasks);
			new LocalStoragePresetStore().save(view.schedulePresets);
			new LocalStorageRunPresetStore().save(view.runPresets);
			try {
				localStorage.setItem(CRUISE_STORAGE_KEY, JSON.stringify(view.cruise));
			} catch (error) {
				console.error("[dsh-task-board] cruise mirror write failed", error);
			}
		}
		/** THE one client-remote resolution: the host command registry by registered
		*  name (`remote.commands`), with the parent remote service's child property
		*  as the fallback. Three surfaces read the same bridge — the slash menu, the
		*  slash-command send and the /permission switch — so the resolution grammar
		*  and its "unavailable" warning live here once, never in three copies.
		*  Nothing is hard-coded: commands registered by DSH or any plugin show up
		*  without a plugin update. */
		function commandsOf(ctx) {
			const commands = ctx.get("remote.commands") ?? ctx.get("remote")?.commands;
			if (commands === void 0) console.warn("[dsh-task-board] slash commands unavailable: no remote.commands bridge");
			return commands;
		}
		/** THE one native model-selection write: the sessions.selectModel API — the
		*  run config's model route and the session panel's model selector submit
		*  the same wire call, so the mapping exists once. */
		async function selectModelOf(api, sessionId, selection) {
			const response = await api.sessions.selectModel({
				sessionId,
				provider: selection.provider,
				model: selection.model,
				...selection.reasoningEffort !== void 0 ? { reasoningEffort: selection.reasoningEffort } : {}
			});
			return response.result.ok ? { ok: true } : {
				ok: false,
				error: `${response.result.error.code}: ${response.result.error.message}`
			};
		}
		/**
		* Required services (fiber inject waiting — the runtime must be up first).
		*
		* `slots` is listed here as a hard service dependency even though it is not
		* declared in `dsh.client.inject` in package.json: the `slots` service is
		* seeded by the web shell itself (the platform seed table in
		* web-platform.ts's PLATFORM_MODULES), so it is not a package this plugin
		* needs the loader to bring up. `sessions` / `workspaces` are the client
		* object-layer services (dsh-api-session-controller / workspace-controller),
		* `connection` the wire carrier, `locale` the copy service, and `remote` the
		* Typert-generated Host API namespaces — all real alpha.3 services, with the
		* package-name edges declared in `dsh.client.inject`. `uiSession` is the
		* session-UI adapter (dsh-client-ui-session): the board only subscribes to
		* its official `pendingInteractions` snapshot (read-only — answering stays
		* in the native session), never registering a waterfall listener of its own.
		*/
		const inject = [
			"slots",
			"sessions",
			"workspaces",
			"locale",
			"remote",
			"uiSession"
		];
		/**
		* Mount the task board.
		* @param ctx - client root context (services: sessions, workspaces).
		*/
		function apply(ctx) {
			ctx.effect(() => ctx.locale.register(NS, {
				zh,
				en
			}), "dsh-task-board: dictionaries");
			const freshness = new BundleFreshnessState({
				bundled: BOARD_VERSION,
				readHostVersion: async () => (await fetchUpdateSource())?.version,
				reload: reloadForFreshBundle,
				storage: (() => {
					try {
						return window.sessionStorage;
					} catch {
						return;
					}
				})()
			});
			ctx.effect(() => {
				freshness.probe();
				return freshness.watch();
			}, "dsh-task-board: bundle freshness probe + watch");
			const footer = new SidebarFooterController();
			ctx.effect(() => ctx.slots.inject("sidebar.footer.action", () => ctx.slots.register({
				name: "sidebar.footer.action",
				key: "dsh-task-board",
				id: "dsh-task-board",
				order: 110,
				label: () => t("entry.label"),
				locale: NS,
				inject: () => footer.inject()
			}, SidebarFooter)), "dsh-task-board: sidebar footer action");
			const scope = new RouteSettingsScope(TASK_BOARD_NS);
			ctx.effect(() => {
				const settingsCard = new TaskBoardSettingsCardController(scope);
				ctx.slots.inject("settings.plugin.item", () => ctx.slots.register({
					name: "settings.plugin.item",
					key: "dsh-task-board",
					locale: NS,
					inject: () => settingsCard.inject()
				}, TaskBoardSettingsCard));
				return () => {
					scope.dispose();
				};
			}, "dsh-task-board: settings card + scope");
			let uiDisposer;
			let mounting = false;
			/** The live enabled decision (the mount gate): ready scope → its value,
			*  unavailable scope → the composition default (true), loading → false. */
			const currentEnabled = () => {
				const snapshot = scope.getSnapshot();
				return snapshot.status === "ready" ? snapshot.value?.enabled ?? true : snapshot.status === "unavailable";
			};
			const mountUi = () => {
				if (uiDisposer !== void 0 || mounting) return;
				mounting = true;
				mountUiBody().catch((error) => {
					console.error("[dsh-task-board] mount failed:", error);
				}).finally(() => {
					mounting = false;
				});
			};
			const mountUiBody = async () => {
				if (uiDisposer !== void 0) return;
				const sessions = ctx.sessions;
				const workspaces = ctx.workspaces;
				const api = buildApi(ctx);
				const sync = new BoardSyncClient({
					transport: createBoardTransport(),
					defer: (fn, ms) => {
						const timer = setTimeout(fn, ms);
						return () => clearTimeout(timer);
					},
					visibility: {
						is: () => document.visibilityState === "visible",
						onVisible: (cb) => {
							const listener = () => {
								if (document.visibilityState === "visible") cb();
							};
							document.addEventListener("visibilitychange", listener);
							return () => document.removeEventListener("visibilitychange", listener);
						},
						onHidden: (cb) => {
							const listener = () => {
								if (document.visibilityState === "hidden") cb();
							};
							document.addEventListener("visibilitychange", listener);
							return () => document.removeEventListener("visibilitychange", listener);
						}
					}
				});
				sync.onBackup((view) => {
					try {
						localStorage.setItem(PRE_SYNC_BACKUP_KEY, JSON.stringify({
							savedAt: Date.now(),
							...view
						}));
						console.warn("[dsh-task-board] local board data differed from the host truth; kept a copy under", PRE_SYNC_BACKUP_KEY);
					} catch (error) {
						console.error("[dsh-task-board] pre-sync backup failed", error);
					}
				});
				const store = new SyncedTaskStore(sync, new LocalStorageTaskStore());
				const sendComment = async (sessionId, text, images, mode = "queue", files) => {
					const content = text.trim() !== "" ? [{
						type: "text",
						text
					}] : [];
					if (images !== void 0) for (const image of images) content.push({
						type: "image",
						mediaType: image.mediaType,
						data: image.data,
						...image.name !== void 0 ? { name: image.name } : {}
					});
					if (files !== void 0) for (const file of files) content.push({
						type: "file",
						receiptId: file.receiptId
					});
					if (content.length === 0) return {
						ok: false,
						error: "empty message"
					};
					const response = await api.sessions.prompt({
						sessionId,
						mode,
						content
					});
					return response.result.ok ? { ok: true } : {
						ok: false,
						error: `${response.result.error.code}: ${response.result.error.message}`
					};
				};
				const sendCommand = async (sessionId, line) => {
					const commands = commandsOf(ctx);
					if (commands === void 0) return {
						ok: true,
						matched: false
					};
					try {
						const result = await commands.execute(sessionId, line, [], new AbortController().signal);
						if (!result.ok) return {
							ok: false,
							error: `${result.error.code}: ${result.error.message}`
						};
						return {
							ok: true,
							matched: result.value !== void 0,
							...result.value !== void 0 ? { outcome: {
								kind: result.value.result.kind,
								...result.value.result.text !== void 0 ? { text: result.value.result.text } : {}
							} } : {}
						};
					} catch (error) {
						console.warn("[dsh-task-board] slash command execution failed:", error);
						return {
							ok: false,
							error: String(error)
						};
					}
				};
				const driverEntries = /* @__PURE__ */ new Map();
				ctx.effect(() => () => {
					for (const { dispose } of driverEntries.values()) dispose();
					driverEntries.clear();
				});
				const sessionDriverEntry = (id) => {
					const bound = sessions.binding(id);
					if (bound === void 0) return void 0;
					let entry = driverEntries.get(bound.session);
					if (entry === void 0) {
						entry = sessionDriverOf(bound.session);
						driverEntries.set(bound.session, entry);
					}
					return entry;
				};
				const sessionAgentStore = new LocalStorageSessionAgentStore();
				const exec = new ExecutionService({
					sessions: {
						list: sessions.list,
						binding: (id) => {
							const entry = sessionDriverEntry(id);
							return entry === void 0 ? void 0 : { session: entry.driver };
						}
					},
					workspaces: { list: { getSnapshot: () => ({
						items: workspaces.list.getSnapshot().items.map((item) => {
							const raw = item;
							return {
								workspaceId: item.workspaceId,
								...typeof raw.path === "string" ? { path: raw.path } : {},
								...Array.isArray(raw.sessionIds) ? { sessionIds: raw.sessionIds } : {}
							};
						}),
						recentWorkspaceId: void 0
					}) } },
					createSession: async (workspaceId) => {
						const target = workspaceId;
						const runtime = ctx.sessions;
						if (runtime.create !== void 0) return runtime.create({ workspaceId: target });
						const response = await api.sessions.create({ workspaceId: target });
						if (!response.result.ok) throw new Error(`${response.result.error.code}: ${response.result.error.message}`);
						return response.result.value.sessionId;
					},
					history: { loadTail: async (sessionId) => {
						const response = await api.sessions.history({
							sessionId,
							maxMessages: 20
						});
						return response.result.ok ? { events: response.result.value.events.map((entry) => entry.event) } : void 0;
					} },
					selectModel: (sessionId, selection) => selectModelOf(api, sessionId, selection),
					selectAgentPreset: async (sessionId, agentPreset) => {
						const response = await api.agentPresets.select({
							sessionId,
							agentPreset
						});
						return response.result.ok ? { ok: true } : {
							ok: false,
							error: `${response.result.error.code}: ${response.result.error.message}`
						};
					},
					onAgentApplied: (sessionId, preset) => {
						const ledger = sessionAgentStore.load();
						sessionAgentStore.save(recordApplied(ledger, sessionId, preset, Date.now()));
					},
					sendComment: (sessionId, text, mode, images) => sendComment(sessionId, text, images, mode),
					sendCommand,
					renameSession: async (sessionId, title) => {
						const response = await api.sessions.rename({
							sessionId,
							title
						});
						return response.result.ok ? { ok: true } : {
							ok: false,
							error: `${response.result.error.code}: ${response.result.error.message}`
						};
					}
				});
				const readTranscriptRaw = async (sessionId) => {
					const response = await api.sessions.history({
						sessionId,
						maxMessages: 30
					});
					if (!response.result.ok) return void 0;
					const value = response.result.value;
					const projections = value.projections?.values;
					return {
						events: value.events.map((entry) => entry.event),
						hasMore: value.hasMore === true,
						...value.floorSeq !== void 0 ? { floorSeq: value.floorSeq } : {},
						...value.throughSeq !== void 0 ? { throughSeq: value.throughSeq } : {},
						...pickTranscriptProjections(projections)
					};
				};
				const readTranscriptPage = async (sessionId, beforeSeq, throughSeq) => {
					try {
						const response = await api.sessions.page({
							sessionId,
							beforeSeq,
							maxMessages: 50,
							...throughSeq !== void 0 ? { throughSeq } : {}
						});
						if (!response.result.ok) {
							console.error("[dsh-task-board] transcript page refused", response.result.error.code, response.result.error.message);
							return {
								events: [],
								hasMore: true,
								refused: response.result.error.code
							};
						}
						return {
							events: response.result.value.events.map((entry) => entry.event),
							hasMore: response.result.value.hasMore === true,
							...response.result.value.floorSeq !== void 0 ? { floorSeq: response.result.value.floorSeq } : {}
						};
					} catch (error) {
						console.error("[dsh-task-board] transcript page read failed", error);
						return {
							events: [],
							hasMore: true,
							refused: "thrown"
						};
					}
				};
				const transcriptLoader = createTranscriptReader({
					read: async (sessionId) => {
						try {
							return await readTranscriptRaw(sessionId);
						} catch (error) {
							console.error("[dsh-task-board] transcript read failed", error);
							return;
						}
					},
					defer: (fn, ms) => {
						const timer = setTimeout(fn, ms);
						return () => clearTimeout(timer);
					}
				});
				const imageReader = createTranscriptReader({
					read: async (key) => {
						const split = key.indexOf("|");
						const sessionId = key.slice(0, split);
						const attachmentId = key.slice(split + 1);
						try {
							const response = await api.sessions.attachment({
								sessionId,
								attachmentId
							});
							if (!response.result.ok) return void 0;
							return {
								data: response.result.value.data,
								mediaType: response.result.value.attachment.mediaType
							};
						} catch (error) {
							console.error("[dsh-task-board] attachment read failed", error);
							return;
						}
					},
					defer: (fn, ms) => {
						const timer = setTimeout(fn, ms);
						return () => clearTimeout(timer);
					},
					ttlMs: 10 * 6e4
				});
				const readFileAsBase64 = (file) => new Promise((resolve) => {
					const reader = new FileReader();
					reader.onload = () => {
						const result = typeof reader.result === "string" ? reader.result : "";
						const comma = result.indexOf(",");
						resolve(comma >= 0 ? result.slice(comma + 1) : void 0);
					};
					reader.onerror = () => resolve(void 0);
					reader.readAsDataURL(file);
				});
				const uploadFile = async (sessionId, file) => {
					try {
						const data = await readFileAsBase64(file);
						if (data === void 0 || data === "") return {
							ok: false,
							error: "unreadable file"
						};
						const response = await api.fileUploads.upload({
							sessionId,
							data,
							name: file.name
						});
						if (!response.result.ok) return {
							ok: false,
							error: `${response.result.error.code}: ${response.result.error.message}`
						};
						return {
							ok: true,
							receiptId: response.result.value.receiptId
						};
					} catch (error) {
						console.error("[dsh-task-board] file upload failed", error);
						return {
							ok: false,
							error: String(error)
						};
					}
				};
				const fetchSlashCommands = async (sessionId) => {
					const commands = commandsOf(ctx);
					if (commands === void 0) return [];
					try {
						const result = await commands.list(sessionId);
						if (!result.ok) {
							console.warn("[dsh-task-board] slash commands unavailable:", result.error.code, result.error.message);
							return [];
						}
						return result.value.map((descriptor) => ({
							name: descriptor.name,
							description: descriptor.description,
							...descriptor.input !== void 0 && descriptor.input.hint !== void 0 ? { hint: descriptor.input.hint } : {},
							kind: "command"
						}));
					} catch (error) {
						console.warn("[dsh-task-board] slash commands unavailable:", error);
						return [];
					}
				};
				const fetchSlashSkills = async (sessionId) => {
					try {
						const response = await api.skills.list({ sessionId });
						if (!response.result.ok) {
							console.warn("[dsh-task-board] slash skills unavailable:", response.result.error.code, response.result.error.message);
							return [];
						}
						return response.result.value.skills.map((skill) => ({
							name: skill.name,
							description: skill.modelInvocable ? skill.description : `${t("prompt.skillUserOnly")} · ${skill.description}`,
							kind: "skill"
						}));
					} catch (error) {
						console.warn("[dsh-task-board] slash skills unavailable:", error);
						return [];
					}
				};
				const referenceBridge = (() => {
					const fileReferences = ctx.get("remote.fileReferences") ?? ctx.get("remote")?.fileReferences;
					const sessionReferenceResolver = ctx.get("remote.sessionReferenceResolver") ?? ctx.get("remote")?.sessionReferenceResolver;
					if (fileReferences === void 0 && sessionReferenceResolver === void 0) {
						console.warn("[dsh-task-board] @ reference menu unavailable: no remote file/session bridge");
						return;
					}
					return {
						...fileReferences !== void 0 ? { fileReferences } : {},
						...sessionReferenceResolver !== void 0 ? { sessionReferenceResolver } : {}
					};
				})();
				const uiSession = ctx.get("uiSession");
				const mirror = uiSession !== void 0 ? new PendingMirror(uiSession) : void 0;
				if (mirror === void 0) console.warn("[dsh-task-board] question mirror unavailable: no uiSession bridge (navigate-to-answer degraded)");
				const questionTracker = new QuestionTracker(api);
				const controller = new BoardController({
					store,
					exec,
					questionRpc: mirror ?? questionTracker,
					presetStore: new SyncedPresetStore(sync, new LocalStoragePresetStore()),
					runPresetStore: new SyncedRunPresetStore(sync, new LocalStorageRunPresetStore()),
					sessionAgentStore,
					requestLaunch: (taskId, trigger) => {
						if (sync.isSynced()) sync.requestLaunch(taskId, trigger);
					},
					seatRecheck: async () => {
						if (sync.isSynced()) await sync.renewLease();
					},
					sessions: {
						list: sessions.list,
						exists: (id) => sessions.list.getSnapshot().byId[id] !== void 0,
						open: (id) => sessions.open(id)
					},
					workspaces: { list: {
						getSnapshot: () => {
							const snap = workspaces.list.getSnapshot();
							return {
								items: snap.items.map((item) => {
									const raw = item;
									return {
										id: item.workspaceId,
										title: item.title,
										...Array.isArray(raw.sessionIds) ? { sessionIds: raw.sessionIds } : {}
									};
								}),
								archivedSessionIds: snap.archivedSessionIds
							};
						},
						subscribe: (fn) => workspaces.list.subscribe(fn)
					} },
					reference: referenceBridge,
					cruiseStorage: new SyncedCruiseStore(sync, {
						read: () => {
							try {
								const raw = localStorage.getItem(CRUISE_STORAGE_KEY);
								if (raw === null) return void 0;
								const parsed = JSON.parse(raw);
								return {
									enabled: parsed.enabled === true,
									...parsed.manual === true || parsed.manual === false ? { manual: parsed.manual } : {},
									limit: parsed.limit
								};
							} catch (error) {
								console.error("[dsh-task-board] cruise state read failed", error);
								return;
							}
						},
						write: (state) => {
							try {
								localStorage.setItem(CRUISE_STORAGE_KEY, JSON.stringify(state));
							} catch (error) {
								console.error("[dsh-task-board] cruise state write failed (persistence skipped)", error);
							}
						}
					}),
					transcript: transcriptLoader,
					transcriptPage: readTranscriptPage,
					loadImage: (sessionId, attachmentId) => imageReader(`${sessionId}|${attachmentId}`),
					uploadFile,
					goalService: {
						bindingOf: (sessionId) => sessions.binding(sessionId)?.session,
						remote: ctx.get("remote.goals"),
						subscribeActivation: (sessionId, listener) => {
							const remote = ctx.get("remote");
							if (remote === void 0 || typeof remote.$on !== "function") return () => {};
							try {
								return remote.$on("goal/activation-changed", (event) => {
									const payload = event;
									if (payload === null || typeof payload !== "object" || payload.sessionId !== sessionId) return;
									const goal = payload.goal;
									if (goal === void 0) {
										listener(void 0);
										return;
									}
									if (typeof goal.id !== "string" || typeof goal.revision !== "number" || goal.activation !== "armed" && goal.activation !== "disarmed") return;
									listener({
										id: goal.id,
										revision: goal.revision,
										activation: goal.activation
									});
								});
							} catch {
								return () => {};
							}
						}
					},
					sessionConfig: {
						readModels: async (sessionId) => {
							try {
								const response = await api.sessions.models({ sessionId });
								if (!response.result.ok) return void 0;
								const value = response.result.value;
								return {
									current: {
										provider: value.current.provider,
										model: value.current.model,
										...value.current.reasoningEffort !== void 0 ? { reasoningEffort: value.current.reasoningEffort } : {}
									},
									groups: value.groups.map((group) => ({
										provider: group.id,
										models: group.models.map((model) => ({
											id: model.id,
											name: model.name,
											...model.reasoning !== void 0 ? { reasoning: {
												efforts: model.reasoning.efforts.map((effort) => ({
													id: effort.id,
													name: effort.name
												})),
												...model.reasoning.defaultEffort !== void 0 ? { defaultEffort: model.reasoning.defaultEffort } : {}
											} } : {}
										}))
									}))
								};
							} catch (error) {
								console.error("[dsh-task-board] session models read failed", error);
								return;
							}
						},
						selectModel: (sessionId, selection) => selectModelOf(api, sessionId, selection),
						setPermission: async (sessionId, permission) => {
							const commands = commandsOf(ctx);
							if (commands === void 0) return {
								ok: false,
								error: "permission commands unavailable: no remote.commands bridge"
							};
							try {
								const result = await commands.execute(sessionId, `/permission ${permission}`, [], new AbortController().signal);
								if (!result.ok) return {
									ok: false,
									error: `${result.error.code}: ${result.error.message}`
								};
								if (result.value === void 0) return {
									ok: false,
									error: "the host offers no /permission command for this session"
								};
								const outcome = result.value.result;
								return outcome.kind === "success" ? { ok: true } : {
									ok: false,
									error: outcome.text ?? "permission switch failed"
								};
							} catch (error) {
								console.warn("[dsh-task-board] permission switch failed:", error);
								return {
									ok: false,
									error: String(error)
								};
							}
						}
					},
					sessionMessage: sendComment,
					sessionCommand: sendCommand,
					runCatalog: {
						listWorkspaces: () => workspaces.list.getSnapshot().items.map((item) => ({
							id: item.workspaceId,
							title: item.title ?? item.workspaceId
						})),
						listModelGroups: async () => {
							try {
								const response = await api.sessions.models({});
								if (!response.result.ok) return [];
								return response.result.value.groups.map((group) => ({
									provider: group.id,
									models: group.models.map((model) => ({
										id: model.id,
										name: model.name,
										efforts: (model.reasoning?.efforts ?? []).map((effort) => ({
											id: effort.id,
											name: effort.name
										}))
									}))
								}));
							} catch (error) {
								console.error("[dsh-task-board] model catalog read failed", error);
								return [];
							}
						},
						listAgentPresets: async () => {
							const response = await api.agentPresets.list({});
							if (!response.result.ok) return [];
							return response.result.value.presets.map((preset) => ({
								id: preset.id,
								name: preset.name,
								description: preset.description,
								isDefault: preset.isDefault
							}));
						},
						listPermissions: async () => {
							try {
								const envelope = await (await fetch("/api/dsh-task-board/permissions", { headers: { accept: "application/json" } })).json();
								if (envelope.ok !== true || envelope.value?.available !== true) return void 0;
								const options = envelope.value.options;
								if (!Array.isArray(options)) return void 0;
								return options.map((option) => ({
									id: option.id,
									name: option.name,
									description: option.description
								}));
							} catch (error) {
								console.error("[dsh-task-board] permission catalog fetch failed", error);
								return;
							}
						},
						listSlashCandidates: async () => {
							const list = sessions.list.getSnapshot();
							const sessionId = list.current ?? Object.keys(list.byId)[0];
							if (sessionId === void 0) {
								console.warn("[dsh-task-board] slash catalog unavailable: no session to scope it to");
								return;
							}
							const [commands, skills] = await Promise.all([fetchSlashCommands(sessionId), fetchSlashSkills(sessionId)]);
							return [...commands, ...skills];
						}
					}
				});
				controller.start();
				const detachTurnWatcher = questionTracker.addSessionListener((sessionId, event) => {
					const turn = nativeTurnOf(event);
					if (turn !== void 0) controller.recordNativeTurn(sessionId, turn);
				});
				const detachActivityWake = watchSessionActivity(ctx, (sessionId, stamp) => {
					controller.recordActivityWake(sessionId, stamp);
				});
				controller.untitledSessionLabel = t(UNTITLED_SESSION_KEY);
				sync.onRemote((view) => {
					controller.applyRemote(view);
					writeMirror(view);
				});
				sync.onEngine((held) => {
					const proto = sync.hostProtoVersion();
					if (proto !== void 0) controller.setHostProto(proto);
					controller.setHostBoot(sync.hostBootTime());
					controller.setEngine(held);
				});
				sync.onCommand((command) => {
					controller.runTask(command.taskId, command.trigger);
				});
				const scheduler = new SchedulerService({
					tasks: () => controller.getSnapshot().tasks,
					now: () => Date.now(),
					runTask: (id) => controller.runTask(id, "schedule"),
					applySchedule: (id, nextRunAt, lastTriggeredAt, runCount, disable) => controller.applyScheduleNextRun(id, nextRunAt, lastTriggeredAt, runCount, disable),
					ready: () => sessions.list.getSnapshot().phase === "ready" && (!sync.isSynced() || sync.isEngine()),
					onSkips: (stats) => {
						controller.setSchedulerSkips(stats);
					},
					onHeartbeat: (hb) => {
						controller.setSchedulerHeartbeat(hb.okAt);
					},
					cruiseTick: (now) => controller.tickCruise(now),
					sessionRulesTick: (now) => controller.tickSessionRules(now),
					environment: {
						addEventListener: (type, listener) => document.addEventListener(type, listener),
						removeEventListener: (type, listener) => document.removeEventListener(type, listener)
					}
				});
				scheduler.start();
				const disposers = [];
				try {
					footer.bindBoard(() => controller.getSnapshot().boardOpen, () => controller.toggleBoard());
					const unsubscribeFooter = controller.subscribe(() => {
						footer.setOpen(controller.getSnapshot().boardOpen);
					});
					disposers.push(() => {
						unsubscribeFooter();
						footer.dispose();
					});
					disposers.push(mountBoard(controller, freshness));
				} catch (error) {
					console.error("[dsh-task-board] mount failed:", error);
				}
				sync.start(readLocalView).then((mode) => {
					console.info(`[dsh-task-board] boot v${BOARD_VERSION} sync=${mode} engine=${sync.isEngine()}`);
					if (!currentEnabled()) {
						sync.dispose();
						return;
					}
					if (mode !== "synced") return;
					writeMirror(sync.view());
					controller.syncActive = true;
					const proto = sync.hostProtoVersion();
					if (proto !== void 0) controller.setHostProto(proto);
					controller.setHostBoot(sync.hostBootTime());
					controller.setEngine(sync.isEngine());
					controller.applyRemote(sync.view());
				}).catch((error) => {
					console.error("[dsh-task-board] sync settle failed (staying on the local mirror):", error);
				});
				uiDisposer = () => {
					for (const dispose of disposers.splice(0)) dispose();
					detachTurnWatcher();
					detachActivityWake();
					scheduler.dispose();
					sync.dispose();
					controller.dispose();
					questionTracker.dispose();
					uiDisposer = void 0;
				};
			};
			const syncEnabled = () => {
				if (currentEnabled()) mountUi();
				else uiDisposer?.();
			};
			scope.subscribe(syncEnabled);
			syncEnabled();
		}
		//#endregion
		exports.apply = apply;
		exports.inject = inject;
		return module.exports;
	}
});

//# sourceMappingURL=client.js.map