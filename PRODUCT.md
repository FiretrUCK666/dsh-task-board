# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

**任何 DSH 用户**——公开插件，会被陌生人装上（用户确认）。所以界面必须自解释：
默认值保守、设置可发现、陌生操作不给惊吓。不能靠「装的人知道自己在干什么」来省解释成本。

**主要使用场景**：一个人在多个设备上管自己的 AI 工作。同一个 DSH 部署，桌面浏览器
常开，手机浏览器随时掏出。手机上不是「查看版」——新建、执行、作答、评论都要能做。

**用户手里的活是什么**：把想法变成能跑的 Prompt，交给 AI 真跑，跑完能复盘、能接着跑下一轮。
工作会跨越几天、跨越很多轮，期间用户会关掉页面、换设备、忘记上次停在哪。

**用户明确表示不需要什么**：不需要自己去记「哪些在跑、哪些等我」。这正是要解决的问题——

> 「管得顺：从想法到执行再到复盘，手上不丢事、不重复劳动」（用户选定的成功定义）

## Product Purpose

DSH Web 界面上的任务看板插件：侧边栏入口 + 五列看板（待规划 / 待办 / 进行中 / 待审核 /
已完成），任务交给 **DSH 自己的会话**真实执行，执行状态自动回写到卡片上。

它为什么存在：**任务在哪、跑到哪、等我做什么**这三件事，在此之前只存在于用户的记忆和
散落的会话里。板把这笔账接下来，让用户不必靠记忆维持。

成功意味着：用户从想法到执行再到复盘，手上不丢事、不重复劳动。板能放心挂着跑，
只在真需要用户的时候叫用户。

## Positioning

**任务不是被「记录」在执行工具里，而是被执行在 DSH 会话里——板是这笔账的账本。**

邻接产品复制不了的地方在于：执行载体就是 DSH 原生会话。因此

- 执行过程在 DSH 原生会话列表里可见可点进去，不是板里另开的一个黑盒；
- 板内评论区直接就是那一次会话的输入面：`/` 斜杠命令、`@` 引用（文件 / 文件夹 / 会话）、
  图片字节直发、任意文件原字节上传，与主界面输入框**同一套机制**，不是自建桥；
- agent 挂起（计划确认、提问）时，交互卡实时出现在板内评论区，**就在卡上作答**，
  与 DSH 原生提问卡同一批部件、同一套行为；
- 运行状态由会话**推导**出来，不由人手填。

一个独立的看板应用无法复制这条链路：它没有会话，只能做一个更漂亮的待办清单。

## Operating Context

- **载体**：DSH Web GUI 的插件（cordis 插件，host + client 双半区）。安装方式为
  npm 包 / GitHub / 本地目录三种，都经 `dsh plugin --profile web add`。
- **设备**：桌面与手机浏览器，同一部署。跨 origin、跨设备经 SSE 实时同步；
  窄屏自动进入紧凑布局（参照板自身宽度 680px，不是窗口宽度，也不是 `@media(max-width)`）。
- **语言**：界面文案为 **zh-first 字典 + 英文回退**，按 document language 选择。
  这是当前实现的既成状态（源码注释明示），不是设计偏好。
- **工作流事实**：定时 / 巡航 / 接续这类时间驱动的行为，同一时刻只由一个打开的界面执行
  （host 租约仲裁）；正在看的那台设备持有引擎席位，另一台切到前台后立即接管。
- **数据**：看板真相在 host 的存储单元，落成单一 JSON 文件；浏览器是离线副本。
- **权限**：DSH 的权限预设（agent / 工作区 / 模型 / 思考程度 / 权限）在板内可配，
  与 DSH 设置同一套；看板不是权限系统的第二实现。

## Capabilities and Constraints

### 已确认能力（用户可见的产品面）

五列看板与卡片摘要；真实执行（含 `/plan` 等原生命令）；评论线程与插话/排队两态；
图片与任意文件上行；交互卡就地作答（选项 / 自定义答案 / 上一题 / 提交 / 拒绝）；
任务级自动化（cron 定时、
完成后接续）与会话级规则（每会话至多一条），两半互相独立；自动巡航（全局并发上限 +
定时窗口，总开关是主权）；并行按会话计算（板头「并行数」是唯一闸门，设 1 即严格串行）；
拖拽整理（跨列移动、同列重排、拖侧栏会话/工作区进板建卡）；批量选色与批量执行；
模板与运行配置预设（默认预设不可删，悬挂回退部署默认）；通知中心与动态页；
多端同步与引擎席位；手机与窄屏布局。

### 绑定约束（用户确认，未来任何改动都不能破）

1. **不改 DSH 源码、不引入新运行时依赖**——运行时依赖面只有 `schemastery`。
2. **双端同等**——禁止靠隐藏标签、隐藏控件、缩小字号来省空间；窄屏与桌面同一套代码、
   同一套交互，不允许另写一条副路径。容器装不下时让位而非压扁。
3. **颜色与字体不自持**——不自己拥有调色板，全部消费 DSH 的语义令牌，跟随主题与皮肤。
4. **禁 emoji**——代码、注释、文案、文档、提交信息一律不得出现 emoji 字符。
5. **数据以人可读的单一文件落盘**，用户能直接备份；删掉那个文件等于清空看板。
6. **离线优先**——断线时看板照常可用，恢复后自动追平。

### 明确未决（不是遗漏，是尚未决定）

- 除用户自己以外的实际采用规模与反馈渠道，目前无数据。
- 除中英文之外的界面语言，未规划。

## Brand Commitments

- **名称**：任务看板；插件 id `dsh-task-board`（id 决定加载、路由、存储与数据键，
  包名只是安装标识，两者不得混用）。
- **口吻**：对用户说实话——部分失败就诚实展示部分失败，不假装成功也不回滚掩盖；
  错误就近说明原因（例如「这条没有 Prompt，所以不能执行」），不甩一个失败状态了事。
- **术语**：任务（task）/ 会话（session）/ 车道 = 会话 / 巡航 / 完善 / 接续 /
  交互卡 / 动态。板内文案与 README 用同一套词，不另造近义词。
- 无 logo、无字体资产、无营销素材；README 与包描述是唯一对外表达。

## Evidence on Hand

**真实存在**：

- 完整可跑的插件本体与覆盖各域的测试套件（`tests/`，一个 src 模块一个 spec）；
- `README.md` / `README.en.md`（成对维护，中文为权威）；
- `AGENTS.md`：机制、不变量、硬性规范；
- `src/client/board.module.css`：设计系统的**事实来源**，含令牌别名层与
  各条规则背后的用户原话（「圆变方」「上面有空隙下面紧贴」等）；
- `src/client/locales.ts`：全部用户可见词汇的 zh/en 对照（键集严格成对）；
- 三个 CSS 布局契约 spec（`review-page` / `mobile-contract` / `card-contract`）。

**不存在，未来的工作不得编造**：用户量、性能基准、客户案例、媒体报道、认证、
定价、路线图承诺。

**界面截图基线**：没有提交视觉回归金标准；`DESIGN.md` 是本项目第一份成文的视觉记录。

## Product Principles

1. **真执行**：任务经 DSH 会话真实执行，运行状态从会话推导——状态由机制而非人手维护，
   所以板在这一点上不会撒谎。
2. **板上不丢事**：进入板的工作必有去处。完成态留言自动回待办并继续驱动，链武装即开跑，
   草稿永不因为离开页面而消失。用户不需要一直盯着它。
3. **唯一判定、唯一入口**：同一件事全项目只有一个判定函数、一个 UI 入口
   （执行门禁、运行预设编辑器、错误边框、自动化编辑器、时间与状态映射）。
   板上说的就是真的，不存在两个说法。
4. **双端同等**：手机与桌面是同一等公民，用换行 / 换列 / 让位 / 短名 / 折叠 /
   提高地板来表达适配；藏掉、缩小、另写一套都算没做完。
5. **跟随宿主**：颜色、字体、玻璃与深浅主题一律跟随 DSH 与皮肤；板不自己拥有外观，
   因此换皮肤、换主题时板自动跟着走，不需要逐处适配。

## Accessibility & Inclusion

- **键盘可达**：所有控件可 Tab 到、有可见焦点环；Dialog 有唯一的 Escape 出口。
- **读屏**：控件普遍带 `aria-label`（含把完整名称保留在无障碍标签里的短名控件），
  列导航用 `role="tablist"`，分组用 `role="group"`。
- **动效**：`prefers-reduced-motion` 下装饰动效降级；但**状态指示器不消失**——呼吸光环
  本身是「在跑 / 未读」这条消息，只降速降幅，不置零（置零会把「活着」读成「卡住」）。
- **触屏**：没有 hover，因此承载必要信息的说明不能只挂在 `title=` 上，必须可点可达；
  触屏尺寸只加隐形的人体工学热区，不改变视觉尺寸。
- **无特定法规或标准被确立**（如 WCAG 等级）；上述为当前实现的事实，不是合规声明。
