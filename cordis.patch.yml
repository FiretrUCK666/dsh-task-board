# 本插件在 profile 里注册的那一行。安装命令把它登记进 profile 的
# `dsh.profile.bundles`，dsh 装配时读这个文件把下面的行插进去——所以安装者
# 不需要手写任何东西。`id` 是插件 id（路由、浏览器资源、存储单元都由它派生），
# `name` 是 pnpm 安装的包名（带 npm 作用域，可以跟 id 不同）。
- insert:
    - id: dsh-task-board
      name: '@firetruck666/dsh-task-board'
